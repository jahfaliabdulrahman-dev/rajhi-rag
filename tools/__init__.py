"""أدواتُ المشروع كحزمةٍ **نظامية** لا `namespace`.

**ولماذا هذا الملف موجود وهو شبه فارغ:** بلا `__init__.py` يصير `tools` حزمةَ namespace،
فيُدمج مع **أي** مجلدٍ اسمه `tools` على `sys.path` — وبيئةُ التشغيل عندنا تحمل
`PYTHONPATH=…/.hermes/hermes-agent` وفيه `tools/` ⇒ فحين يُستورَد `tools` أوّلَ مرّة بترتيبٍ
غير محسوب، يُخزَّن في `sys.modules` بمسارِ الطرف الآخر، ثم **لا ينفعه** إدخالُ مسار المشروع
لاحقاً (`sys.path.insert`) ⇒ `ModuleNotFoundError: No module named 'tools.eval_pack'`
في تسعة اختبارات مرّاتٍ متفرقة. الحزمةُ النظامية تحسم الترتيب: **أولُ مطابقةٍ على `sys.path` تفوز**
والمشروعُ يُدخل نفسه في المقدّمة قبل أي استيراد. (مُثبتٌ بالدليل، لا مُرجَّحاً.)
"""