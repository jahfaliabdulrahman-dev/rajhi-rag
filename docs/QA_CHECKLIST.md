# قائمة الفحص قبل التسليم — rajhi-rag

> وُلدت من حوادث فعلية: قالب يعرض صفوفاً وهمية قُرئت كعطل، أرقام منزاحة ×100 مرّت «نظيف السلسلة»، تشغيل المالك على سطح مختلف عن سطح الفحص، وبيانات كشوف حقيقية تسرّبت إلى fixtures في آخر لحظة. كل بند هنا ثمنه ألم — لا تحذفه.

## ١. البوابة الآلية (إلزامية قبل أي «تم»)

```bash
cd ~/Projects/rajhi-rag
source .venv/bin/activate
python tools/qa_gate.py --full
```

يغطي: الاختبارات (حالياً 753 اختباراً — **وكلُّها تنجح في الشجرة الكاملة** (بأدلّة المحاسبة والمفتاح) ·
**+١١ ضابطاً صافياً في جولة مراجعة ٥٦** (٦٩٨ ⇒ **٧١٠** بالعدّ الآليّ: الصيغُ الأربع لبصمة الحزمة **والسطرُ الحقيقيّ الطويل** · الاتّجاهان المُعاكسان · ضابطُ عمى اليونيكود · الدورُ وسببُه من الأمر المجرّد · الاتّجاهُ الخامس بمحلّل YAML وبأربعة أسماء · الاتّجاهُ السادس للأعلام · وملفٌّ جديدٌ لـ§٢٧ بضابطَيه — **وضابطان مسحوبان**: «الحدُّ المُعلَن للنافذة» بعد أن سقطت النافذةُ بالقياس، وضابطٌ مكرَّرٌ كان يُحتسب في العدد) **+ضوابطُ جولة ٥٧** (حارسُ الشاهد بدلَ الحارس المنزوع · والحالاتُ الأربع لصيغة إعلان الدور · وحالةُ «لم يُقَس» في مسح البوّابات · وسمُّ ملفّ الـworkflow الثاني) **⇒ ٧٣٠ بالعدّ الآليّ** (**+١ في تنفيذ حدّ الترويسة** — SP-3 بأمر المالك: `test_the_declaration_is_read_in_the_header_only` و`test_a_headingless_file_longer_than_the_header_is_body` مقابل ضابطٍ **مُحوَّل** (كان يقيس المتنَ كلَّه)، وصفرُ مسحوب) (**+١ في إغلاق R13**: `test_the_gate_goldens_agree_with_the_locked_rule` في `tests/test_gate_goldens.py` — وحدةٌ خفيفةٌ مُدرَجةٌ في الـCI) (ومنه **+٧** ضوابطَ في إغلاق مقاعد الجولة ٥٧ — بالعدّ الآليّ:
`git diff 294eefa 513b61b -- tests/ | grep -c '^+def test_'` ⇒ **٧** وصفرُ مسحوب: `test_the_summary_counts_each_state_in_its_own_slot` (S-1) ·
`test_a_collection_error_is_not_a_bite` (ST-2) · `test_the_cutoff_exempts_no_answer_by_accident` (S-2) ·
`test_the_tatweel_form_coverage_is_measured_not_described` (S-3) ·
`test_a_field_carrying_a_leading_backtick_is_not_a_declaration` (S-4) ·
`test_an_unpaired_fence_does_not_hide_a_real_declaration` (ST-6) ·
`test_the_answer_class_is_measured_not_assumed` (SP-2)) **+١٥ ضابطًا في جولة مراجعة ٥٢** ثمّ **إحدى عشرةَ ضابطًا في جولة إصلاح مقاعدها** و**أربعةٌ لبوّابات الانكشاف** (`tests/test_hook_gates.py` · `.githooks/pre-commit`) (`tests/test_spend_unit.py` ٥ · وثلاثٌ في كلٍّ من `tests/test_eval_proof_rows.py` و`tests/test_state_union.py`) — (٢٠٢٦-٠٩-٢٤ · ⚠ **وهذه الأعدادُ مكتوبةٌ بيدٍ** — والعددُ المحروسُ آليًّا هو جملةُ `حالياً N` وحدها في `docs/claims.json`؛ فتُقاس عند الشكّ بـ`pytest --collect-only -q tests/ | tail -1`): `tests/test_state_union.py` (٦: تعارضُ ملفّ الحالة يُحلّ حتمًا — على **عيّنةٍ ملتقطةٍ** من تعارضٍ وقع فعلًا) · `tests/test_mailbox_ownership.py` (٢: صندوقُ المدقّق بلا تقرير منفّذ · وكلُّ نقلٍ/حذفٍ تحت `handoff/` مُعلَنٌ في `handoff/RENAMES.md`) · `tests/test_eval_cap_loop.py` (+٢ سلوكيّتان: السقفُ من رصيدٍ **غير صفريّ** · والسجلُّ يحمل الأثرَ كاملًا بطولٍ مطابق) · `tests/test_eval_proof_rows.py` (+٥: الموضعُ داخل النطاق والسلسلة · الموضعُ = **الصفوفُ المطابِقة** · ولا مطابقةَ تضيع بصمت) · **وسُحِب** حارسٌ نصّيّ كان يمنح ثقةً كاذبة (R52-3) ·
وفي نسخةٍ بلا أدلّةِ المحاسبة (2026-09-24، **استنساخٌ نظيفٌ فعليّ** بلا `data/local_sample` ولا `data/training` ولا `data/eval_pack`): **مسحوبٌ — انظر التصحيح أدناه** (**⛔ تصحيحٌ مؤرَّخ (٢٠٢٦-٠٩-٢٤ بعد مراجعة ٥٠):** الرقمُ المنشورُ هنا سابقًا (**538/4/48**) كان قياسًا على
شجرةٍ **مشوَّهة**: الأمرُ حذف `data/` كاملًا، وفيه ملفٌّ **متتبَّع** (`data/sample/statement_sample.pdf`) ⇒ حذفُه
يُبدّل الفروق. **وقياسُ الاستنساخ الحقيقيّ (٢٠٢٦-٠٩-٢٤ · `8db6c39` · بلا حذف أيّ ملفٍّ متتبَّع): 555 نجحت · 4 تسقط · 47 تُخطّى = 606** — والساقطةُ الأربعُ **سابقتان وبيئيّتان بالاسم** (ثلاثةُ حرّاس مبالغٍ تحتاج تاريخَ git/المفتاح في الاستنساخ · `test_skip_declares_itself`)، و**`test_public_claims` لم يعد يسقط** بعد مزامنة اللقطة. **أمرُ الرقم الجديد:** `pytest tests/ -q` في استنساخٍ حقيقيّ من `0f7716f` (وليس `3699f19` — فالالتزامُ الذي حُسب عليه الرقمُ المسحوب قديمٌ، و٦٠٦ لا تصحّ عليه قبل ضوابط هذه الجولة). وقبل إضافة ضوابط الترقيم كانت **537/4/44 = 585** وكلاهما مُبقًى مؤرَّخًا). **(قديمًا: 537 نجحت · 4 تسقط · 44 تُخطّى = 585** (تُغلق على مجموعِ المجموعة ✓؛ والساقطةُ أربعةٌ بالأسماء: ثلاثةُ حرّاس مبالغٍ تحتاج تاريخَ git/المفتاح في الاستنساخ · `test_skip_declares_itself` (إشارةُ فشلٍ من البيئة لا من السلوك — ح٩) · **واختبارُ الوثائق ينجح هناك بعد إعادة توليد اللقطة** (كان يسقط: حاجزُ CI الأعمى — أُصلح ٢٠٢٦-٠٩-٢٤)) بأمر `pytest tests/ -q` — والفاشلةُ ثلاثةٌ بالأسماء: فحصان من حرّاس المبالغ (غيابُ المفتاح والمانيفست) و`test_skip_declares_itself` (إشارةُ فشلٍ من البيئة لا من السلوك — ح٩)؛ وهذا الشطرُ **خارج حراسة `render_claims`** ⇒ يُقرأ بوصفه قياساً مؤرَّخاً على التزامٍ مذكور، وتوحيدُه مؤجَّلٌ مُعلَن) · **صفرُ xfail وصفرُ دَينٍ مُعلَن** — وقد انتهى دَينُ المبالغ بإعادة كتابة قِيَمه لا بإعفاء مواضعه) · **qa_gate: ٥/٥ تمرّ** في قياسٍ مؤرَّخ (2026-09-25 · `--full`: الاختبارات · `parser goldens` · صحّةُ الخدمة · تحويلُ السطح · الفحصُ الشاملُ على العيّنة) — وكان **٣/٥** من 2026-09-23 إلى 2026-09-25 — والفاشلتان **سابقتان لهذا العمل**: قِيس على `2a25b02`
في شجرةٍ نظيفة فأعطى الفشلَ نفسَه (`parser goldens`: `٦١,١٦٥١٢ → 61165.12` والمتوقَّع `35832.43`)
⇒ فالرقمُ مُعلَنٌ لا مُخفي · **وقد صُحّح (٢٠٢٦-٠٩-٢٥ · R13):** القيمةُ في مفتاح `٦١,١٦٥١٢` صارت `61165.12` = ما توثّقه `src/statement_qa/legacy/arabic_digit_parser.py:19` ويثبّته `tests/test_parser.py`؛ **والعلّةُ كانت نسختين من الحقيقة** (جدولُ البوّابة نسخةٌ ثانية بلا ضابطٍ يقابلها) ⇒ العِلاجُ نسخةٌ واحدة (`PARSER_GOLDENS` في `tools/qa_gate.py`) يقرؤها ضابطٌ في الـCI فلا يفترقان صامتين. **والعلّتان الأخريان من الصنف نفسه، وأُغلقتا ٢٠٢٦-٠٩-٢٥:** (ب) **ثابتا الصفّ الأوّل في الصفحة ١٠** كانا لا يُغلقان السلسلة حسابًا (`٦٧٦` + الأوّل ≠ الثاني) ⇒ استُبدلا **بحكمٍ حسابيّ** (رصيدُ الصفّ = ختامُ ص٩ + دائنُه) لا بأرقامٍ محفوظة، بعد أن قُرئ رندرُ الصفحة بصرًا فوافقَ السلسلة؛ (ج) **قفلُ الأنواع** على مبلغٍ ثابت صار **صفرَ صفوفٍ** في العيّنة (أبدلته ورشةُ التطهير برقمٍ لا من المصدر) ⇒ صار القفلُ **يكتشف المبلغَ المتكرّر حسابًا** (≥٤ صفوف) ثمّ يقابل **أنواعَه الأربعة** بالاسم. **ودرسٌ أمنيٌّ مُثبت:** محاولتي الأولى كتبت قيمتَي الصفّ نصًّا، فسقط ضابطُ السقاطة (`test_the_baseline_cannot_be_raised_by_its_own_debtor` — والتشغيلُ موثَّقٌ في سجلّ الجلسة)، وبصمةُ القيمتين **داخل مجموعة المصدر** (تُعاد بالقياس: `fingerprint(...) in load_deny()` ⇒ True) ⇒ كانتا **مبلغين حقيقيّين** سيدخلان ملفًّا متتبَّعًا. **وتصحيحان من مقاعد المراجعة:** (١) اسمُ القاعدة الصحيح **القاعدة ١٢** («لا يُعفى موضع، تُغيَّر القيمة» — `tools/amount_guard.py:539`)، و«القاعدة ١٤» **لا وجودَ لها** في المستودع وكانت في نصّي خطأً؛ (٢) الحارسُ كان **أعمى** عن الثابتين القديمين (بصمتاهما خارج مجموعته) ⇒ فلم يكونا حمراوين بسببه، بل لأنّهما **لا يُغلقان السلسلة**. **وحدٌّ أُغلِق (٢٠٢٦-٠٩-٢٥ · البند ①):** القراءةُ **غيرُ حتميّة** — تشغيلٌ أعطى «١ مشتبه» والتالي «صفر»، فكانت البوّابةُ تبدّل لونَها بلا تبدّلِ شفرة. **والسياسةُ الآن مُعلَنةٌ ومُقاسة:** قراءةٌ واحدة؛ فإن لم تكن نظيفة ⇒ **إعادةُ قراءةٍ واحدة**، والقاعدةُ الحاسمةُ **لا براءةَ بلا قراءةٍ نظيفة** (والتقاطعُ **تشخيصيٌّ** يسمّي العطبَ المتكرّرَ بمعرّفه ولا يحكم) ⇒ دالّةٌ خالصةٌ `decide_two_runs` في `tools/qa_gate.py`؛ **والشكوكُ تُقاس بالهويّة** (`صفحة:صفّ`) لا بالعدّاد، والعدّادُ يُقابَل بالهويّة (اختلافُهما عطبُ لافتةٍ لا ضجيجُ قراءة). **ودليلُ العضّ:** `tests/test_gate_flake_policy.py` (٩ حالات: القاعدة · فقدُ الصفّ · القراءةُ الأنحفُ · مخالفةُ الفوتر على القراءتين · عقدُ القياس `RUN_FIELDS`) + سمومُ **م١٣/م١٤/م١٥/م١٦/م١٧/م١٨/م١٩** في `guard_bite_sweep` ⇒ **22/22** (كان 18/18 قبل شرط مراجعة ٥٨ الذي أضاف **سجلَّ اللاحتميّة وميزانيّتَه**، ثمّ 19/19، ثمّ م١٧/م١٨ في إغلاق مراجعة ٥٩، **ثمّ 22/22 بإغلاق البوابة (٣) ٢٠٢٦-٠٩-٢٥**: م١٩ يُلغي **سقفَ قرار المالك** فيجب أن يسقط ضابطُ الحدّ). **وثلاثةُ حدودٍ اصطادتها المقاعدُ الثلاثة (٢٠٢٦-٠٩-٢٥ · `deleg_4ea9c13d` · `APPROVE WITH FIXES` ×٣) وأُغلقت:** قراءةٌ ثانيةٌ **أنحفُ** لا تُبرّئ (`second.n_rows < first.n_rows` ⇒ أحمر مُسمّى) · ومخالفةُ أوراكل الفوتر **لا تُغسَل** (دخلت في تعريف «النظيفة» + تُعلَن مقاماتُ القراءتين في السطر) · و**المقامُ أُغلق** (`f_possible == cov[1]` ⇒ لا صفحةَ تخرج من الأوراكل صامتة). **والخطرُ الباقي مُعلَن:** عطبٌ يظهر في قراءةٍ واحدةٍ ويغيب في ثانيةٍ **نظيفةٍ غيرِ أنحفَ ومطابقةِ الفوتر** يُبرَّأ **مُعلَنًا** — وهذا هو **التخفيضُ المتعمَّدُ الوحيد** للحكم (و«لا بتخفيض الحكم» في تقرير `20260925-0723` نصٌّ **سابقٌ يُنسَخ بهذا**، لا يُعاد كتابتُه لأنه سجلٌّ). **ونتيجةُ المراجعة المستقلّة (ثلاثةُ مقاعد: المعايير · المواصفة · البنية · الجولة ٥٨ · `deleg_c5e95877`): `APPROVE WITH FIXES` ×٣** — وأُغلقت بنودُها في التزام التصحيح: (١) **الوعدُ بالـCI صار صحيحًا بالقياس**: `tests/test_parser.py` نفسه **لا يصلح** للـCI الخفيف — فقد سقط الـCI عند إدراجه (تشغيلُ الـCI على `de00196` ⇒ `ModuleNotFoundError: No module named 'polars'` · `rc=2`)، فنُقل الضابطُ إلى وحدةٍ خفيفةٍ مستقلّة **`tests/test_gate_goldens.py`** أُدرجت في `.github/workflows/publish-guard.yml` وفي قائمة `docs/GATES.md` (والاتجاهان يُقابَلان تساويًا في `tests/test_gate_registry.py`) ⇒ الـCI **يشغّله فعلًا**، وكان قبلها **وعدًا بلا مصدر**؛ (٢) **سمٌّ للقفل الجديد** في `tools/guard_bite_sweep.py` (م١٢) ⇒ الحصيلةُ **15/15**؛ (٣) **تهيئةُ مسارٍ صريحة** في `tests/test_parser.py` فلا يسقط مُفردًا بـ`ModuleNotFoundError`؛ (٤) تصحيحُ **المدة** (من `c7b5483` ٢٠٢٦-٠٩-٢٢ ٢٢:٢٩ إلى ٢٠٢٦-٠٩-٢٥ ٠٧:٠٣ ≈ **٢٫٤ يومًا** لا ستّة) و**عددِ الملفّات** (أربعةٌ لا خمسة). **وحدودٌ مُعلَنةٌ باقية (لم تُغلَق):** مقامُ أوراكل الفوتر يُسقِط الصفحاتَ غيرَ القابلة للتحقق بلا وسم (`MIN_FOOTER_COMPARABLE = 9`) ⇒ ثقبٌ ضيّقٌ يُوصى بإغلاقه باشتراط `f_possible == عدد الصفحات` · ومفتاحُ كشف «المبلغ المتكرّر» **نصّيّ** لا قيميّ (`Decimal`) فتُفرّقه صيغُ الكتابة (والفشلُ مغلقٌ: أحمر لا أخضر) · و`tools/audit_types.py:93` ما زال يقفل على `Decimal("4331.11")` وهو مبلغٌ أبدلته ورشةُ التطهير ⇒ **قياسٌ ميّت** يُصلَح أو يُنزَع · والأثرُ المطلق للصفّ الأول في ص١٠ يعتمد اليوم على ختام ص٩ + أوراكل الفوتر (وليس على ثابتين مطلقين). وهذه الأعداد مُشتقّةٌ بـ`tools/render_claims.py` لا مكتوبةٌ بيد.

**وفي نسخةٍ نظيفة: ثبّت المرجعَ صريحًا قبل تشغيل الحرّاس.** نسخةٌ مأخوذةٌ من **المسار المحلّيّ** ترث
`refs/heads/main` المحلّيّ — وهو في هذا المستودع **مأخوذٌ في worktree آخر** (`~/Projects/rajhi-rag-claude`)
وقد يكون متقادماً بعشرات الالتزامات (قِيس ٧٠). ⇒ شغّل الحرّاسَ بعد
`git update-ref refs/remotes/origin/main <الالتزام المدفوع>`، وإلّا فسقوطُ حارسِ الدفع إنذارٌ كاذبٌ **يجعل
القياسَ لا يُصدَّق** (وقع فعلًا ٢٠٢٦-٠٩-٢٥). والقاعدة: الحرّاسُ تقرأ `origin/main`، و`refs/heads/main` ليس لنا.

**أي FAIL = لا تسليم.** أصلح وأعد التشغيل. (نادراً ما يكون الفشل تذبذبَ قراءة عابراً — **أعد التشغيل مرة قبل أي تشخيص عميق**، وإن تكرر الفشل في الموضع نفسه فذاك عطل حقيقي.)

## ٢. الفحص اليدوي (خمس دقائق)

1. **نفس السطح الذي سيستخدمه المالك** — افتح `http://127.0.0.1:7860` بمتصفح حقيقي وامشِ المسار كاملاً (رفع → زر → نتيجة). فحص API وحده لا يكفي.
2. **القيم مقابل الأصل** — افتح رندر الصفحة عينياً وقارن أول ٣ صفوف بمخرجات التطبيق. السلسلة «النظيفة» لا تكشف انزياحاً موحّداً (×100) — المقارنة العينية إلزامية (وقد صار أوراكل الفوتر يلتقط الأشكال الموحّدة آلياً، لكن العين تبقى الحكم الأخير).
3. **سطح واحد فقط** — لا قوالب/عروض توضيحية موازية أمام المالك؛ أي سطح ثانٍ يجب أن يحوّل أو يُغلق.
4. **الخصوصية** — كشوف حقيقية لا تُرفع إلى git ولا تُشارك مع طرف ثالث ولا يُبنى عليها أي سطح عام؛ الـfixture العام اصطناعي. **وبصراحة كاملة:** صور كل صفحة تُرسل إلى **مزوّد القراءة البصرية السحابي** في الطلب نفسه — وهذا قيد مُعلَن (يُعالج بمسار معالجة محلية، لا يُنكر)، فلا تعرض على عميل حقيقي معالجة لم يوافق عليها.
5. **الخدمات الدائمة** — `launchctl list | grep rajhi` (app + redirect + healthcheck). بعد أي تعديل كود: أعِد التثبيت عبر `tools/install_services.sh` إن لزم، **ولا تفعل `kickstart` أثناء احتمال تشغيل المالك** — إعادة التشغيل تقتل مهمته الجارية.
6. **التوثيق** — commit + سطر في `references/rajhi-rag-state.md`: ما فُحص وما لم يُفحص، وبأي دليل.

## ٣. قبل أي push — حزمة النشر الآمن (G3)

```bash
python tools/publish_guard.py --tree --history   # يجب: 0 BLOCK قبل أي push
```

- **التفعيل مرة واحدة لكل نسخة clone:** `git config core.hooksPath .githooks` — يشغّل **خطّافَين**: `pre-commit` (يمنع مساراتِ الأدلّة/السرّ · الأسرارَ بنمطٍ لكلّ صنف عبر `tools/secret_scan.py` · أشكالَ مبالغَ حقيقيّة) و`pre-push` (حارسُ النشر + حارسُ المبالغ على المدى). وحاجز CI في `.github/workflows/publish-guard.yml`.
- **ضوابطُ مراجعة ٥٤–٥٥ (قواعدُ صارت ميكانيكيّة):** `tests/test_turn_derivation.py` — الدورُ يُشتقّ ·
  وطلبُ قرارٍ من المالك يجعل الدورَ له · **وصيغةُ المدقّق بستّ خانات تُرى** (كانت عمياء) · **و`--check` يقابل
  السطرَ حرفيًّا بالمُؤشِّر**: قيمةٌ محفوظةٌ أو صيغةٌ أخرى (`Turn:` · بلا علامتين · بحرفٍ كبير) أو غيابُ السطر ⇒ `1`
  (النسخةُ الأسبق نَجَت منها أربعُ صيغٍ — قاسها المقعدُ التركيبيّ) · **وضابطٌ حيّ على `handoff/STATE.md` نفسه**.
  `tests/test_secret_scan.py` — أصنافُ المفاتيح · **وللكاشف P-4**: غريبٌ يُكشَف (بخمس صيغٍ نثريّة مقيسة) ·
  وموجودٌ لا · ورقمٌ طويلٌ ليس معرّفاً · **وبصمةُ حقلِ هاشٍ صريح (`sha256=` · `spec_sha` · `checksum`) لا
  تُكشَف** · **ولا يُطبع المعرّف** · **وسقفُ معدّلِ الإيجابيّات على الكوربوس مُعلَن ومحروس**.
  `tests/test_report_names.py` — كلُّ تقريرٍ أُودِع بعد القطع يطابق اسمُه زمنَه · و«استثناءٌ تاريخيٌّ زال
  سببُه» يُكشَف · **واسمٌ بلا زمنٍ أُودِع بعد القاعدة يُكشَف** (لا يتخطّى صامتاً) — والنمطُ والاصطلاحُ
  **مصدرٌ واحد** من `tools/turn.py`. والقواعدُ ٢٣–٢٦ في `docs/handoff-protocol.md`.
- **حتميّةُ نسخةٍ نقيّة (مراجعة ٥٣ · القياسُ الرابع):** `tests/_local_evidence.py` تُعلن تخطّي الفحوص التي
  تقيس على `data/` غيرِ المنشور ⇒ **`616 passed · 55 skipped · 0 failed`** في نسخةٍ نقيّة.
  (قبلها: `7 failed, 613 passed` — ورقمٌ يصلح في نسخةٍ واحدة لا يُقنع مدقّقاً.) وتُعاد بالأمر:
  `git clone ~/Projects/rajhi-rag /tmp/c && cd /tmp/c && <interpreter> -m pytest -q`.
- **ضوابطُ مراجعة ٥٣ (جولةُ إصلاحات البوّابة الجديدة):** `tests/test_secret_scan.py` (٦: كلُّ صنفٍ من المفاتيح يُطابَق · والمقاطعُ وحدها لا تُطابَق · والقيمةُ لا تُطبع · وغيابُ الفحص ⇒ `rc=2` فشلٌ مُغلَق) · `tests/test_guard_bite_sweep_logic.py` (٤: «سقط» مقابل «تعذّر التشغيل» · والمفسّرُ يُحلّ لا يُكتب) · `tests/test_refs_probe_exit_code.py` (+٢: قائمةُ المعرّفات من غير المُتتبَّع · وغيابُها `UNMEASURED`).
  - ⚠ **والإعدادُ محلّيّ لا ينتقل مع الاستنساخ**: نسخةٌ بلا تفعيلٍ لا تحرسها الخطّافات (CI وحدَه يعمل عند الجميع). ضابطٌ يقرأ سطرَ التركيب من ملفّ الخطّاف نفسه: `tests/test_hook_gates.py`.
- **ممنوعات الرفع (BLOCK):** مسارات منزل مطلقة · اسم ملف الكشف الأصلي · الأسماء العائلية الواردة في الكشف · سلاسل أرقام ≥10 (حسابات/بطاقات) · أي ملف تحت `data/local_sample/` · ملفات `.env` · وسائط غير مسموحة.
- **الاستثناءات:** فقط في `.publish-allowlist` وبسبب مكتوب — لا استثناء بلا مبرر.
- **عند اكتشاف BLOCK تاريخي:** أعِد كتابة التاريخ بـ `git-filter-repo --replace-text` (مثال منفَّذ: تنظيف fixtures قديمة قبل أول نشر) ثم أعد الفحص.

## ٤. مبدأ حاكم

لا يُسلَّم شيء **لم يُضغط زرّه من متصفح حقيقي على نفس الرابط**، و**لم تُقارن قيمه بأصله**، و**لم يجتز حزمة النشر قبل أي push**. «يعمل عندي على مسار آخر» ليس دليلاً.
