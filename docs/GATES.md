# سجلُّ البوّابات — بوّابةٌ لا تُعرَف ليست بوّابة

**P-8 · تبنّاه المدقّق في مراجعة ٥٥.** سببه المقيس: بوّابتان محلّيّتان لا تعملان إلا في نسخةٍ مُهيّأة
(`core.hooksPath` **لا ينتقل** مع `clone`)، وقائمةُ ما يشغّله الـCI كانت أقصرَ ممّا يظنّ الجميع.

**وكيف يُحرَس هذا الملفّ نفسه:** `tests/test_gate_registry.py` يقيسه في اتجاهاتٍ **لا عددَ لها هنا**
(العددُ يُقرأ من الملفّ لا من نثر — وهذا حكمُ الاتّجاه (٤) على نفسه)، وكلُّها مقروءةٌ من الملفّات لا من الذاكرة: (١) كلُّ ملفٍّ في `.githooks/` **وكلُّ أداةٍ تُنادى منه أو من الـCI** مذكورةٌ هنا؛
(٢) **قائمةُ الـCI المُعلَنة أدناه = مجموعةُ ملفّات `pytest` في كلّ ملفّات الـworkflows** (تساوياً لا احتواءً
· **والمدى بالـglob لا باسم ملفّ** — R57-4: كان اسماً واحداً فصار ملفٌّ ثانٍ **يثبّت ما نُزع**)؛
(٣) **كلُّ أداةٍ في صفوف هذا السجلّ لها ملفُّ ضابطٍ يذكرها**؛ (٤) **وأيّ عبارةٍ عدديّة عن الـCI هنا يجب أن
تطابق العددَ المقيس** (ولذلك لا يوجد في هذا الملفّ رقمٌ مكتوبٌ بيدٍ عن عدد الملفّات أو الضوابط)؛
(٥) **كلُّ خطوةِ `run:` في أيّ workflow لها معرّفٌ `gate_*` مذكورٌ في §٣ — ولا معرّفَ شبحاً هنا**
(وهو الاتّجاهُ الذي ثقبه المدقّق في مراجعة ٥٦: خطوةُ CI مصطنعةٌ مرّت صامتةً عبر الاتّجاهات الأربعة،
**وعُمِّم على كلّ ملفّات الـglob في R57-4**)؛
(٦) **كلُّ عَلَمٍ `` `--x` `` مذكورٍ في صفٍّ أدناه له وجودٌ في مصدر أداتِه** — قاسه القياسُ لا المراجعة:
`--foreign-ids` كان مذكوراً هنا وفي الخارطة **قبل أن يوجد**، فمرّ على أمرٍ حقيقيّ فأعطى `unrecognized`).

## ١ · طبقاتُ التشغيل (حيث تعمل فعلاً)

| الطبقة | الملفّ | متى | حاجزٌ حقيقيّ؟ |
| :--- | :--- | :--- | :--- |
| **ما قبل الالتزام** | `.githooks/pre-commit` | على الفهرس (`--staged`) | ⚠ **محلّيّ فقط** — يحتاج `git config core.hooksPath .githooks`، **ولا يُورَث بالنسخ** |
| **ما قبل الدفع** | `.githooks/pre-push` | على مراجع الدفع | ⚠ محلّيّ فقط (نفسُ الشرط) |
| **CI** | `.github/workflows/publish-guard.yml` | كلُّ `push` و`pull_request` | ✅ غيرُ قابل للتجاوز (مع حماية الفرع) — لكنّه **أضيقُ مدىً**: خطواتُه **مُعرَّفةٌ ومُسجَّلة أدناه (§٣)** وقائمةُ ملفّاتٍ محدودة |
| **دوريّ** (خارج المستودع) | مهمّةُ Hermes `refs-exposure-watch` · الاثنين ٩ص | أسبوعيّاً | ✅ يعمل والبوّاباتُ الأخرى **عمياء** عن مداه |

## ٢ · البوّابات

| # | البوّابة | ما تُمسكه | تعمل في | الضابطُ الذي يُثبت أنّها تعضّ | الفشل |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | `tools/secret_scan.py` | أصنافُ المفاتيح · **المعرّفاتُ الغريبة** (`--foreign-ids`) | pre-commit (المُدرَج) | `tests/test_secret_scan.py` (موجبةٌ وسالبةٌ صريحة، وسقفُ معدّلٍ على الكوربوس) | `rc=2` فشلٌ **مُغلَق** |
| 2 | `tools/publish_guard.py` | بياناتٌ حقيقيّة/PII في الشجرة **والتاريخ ورسائل الالتزام** | pre-push · الـCI | `tests/test_publish_guard.py` · `tests/test_gate_bites.py` | `exit 1` |
| 3 | `tools/amount_guard.py` | شكلُ مبلغٍ حقيقيٍّ في أيّ ملفٍّ مُتتبَّع أو غيرِ مُهمَل | pre-commit · pre-push · الـCI | `tests/test_amount_guard.py` | `exit 1` · غيابُ المفتاح ⇒ يُعلَن ما فُحص |
| 4 | `tools/static_gate.py` | اسمٌ لا يُحلّ · ربطٌ لا يُقرأ · استيرادٌ بلا مستعمل | pre-push (**إن وُجد `pyflakes`**) · الـCI | `tests/test_static_gate.py` (يستدعي الأداةَ ويقيس مخرَجها) | `BLOCK` |
| 5 | `tools/render_claims.py --check` | رقمٌ منشورٌ خالف مصدرَه | الـCI | `tests/test_public_claims.py` | `exit 1` |
| 6 | `tools/turn.py --check` | **غيابُ مُؤشِّر الدور أو تحوّلُه إلى قيمة/صيغةٍ أخرى** في `handoff/STATE.md` · **وإعلانُ التوقّف يُقرأ بصيغةٍ واحدة** (`status: AWAITING_FOUNDER`) من **ترويسة** آخر تقرير ومن **ترويسة** `handoff/STATE.md` (قبل أوّل `## ` وضمن ٣٠ سطراً)، ولا يُقرأ داخلَ كتلة شِفرة — **وما يُهمَل في المتن يُعرَض تنبيهاً** (R57-2 · SP-3 بأمر المالك: الأكثرُ حرصاً ووضوحاً) | **لا خطّافَ — الاختبارات فقط** | `tests/test_turn_derivation.py` · `tests/test_report_names.py` | `1` تحوّل/غياب · `2` تعذّرُ القياس |
| 7 | اسمُ التقرير يحمل زمنَ إيداعه | انزياحُ الاسم عن زمن الالتزام (±٢٠ د) بعد `CUTOFF` · **واسمٌ بلا زمنٍ أُودِع بعد القاعدة** | الـCI (عبر `tests/test_report_names.py`) | `tests/test_report_names.py` | فشلُ اختبار |
| 8 | الأدلّةُ لا تُدفَع | ملفّاتُ بيانات/Eval مُتتبَّعة | الـCI (خطوةٌ مستقلّة مُعرَّفة) + pre-commit (المسارات المحرّمة) | **خطوةُ الـCI نفسُها** (لا ضابطَ pytest لها — يُعلَن) + معرّفُها **في §٣ وحدها** (نسخةٌ واحدةٌ فلا تتقادم) | `exit 1` |
| 9 | `tools/guard_bite_sweep.py` | **هل تعضّ كلُّ بوّابةٍ فعلاً؟** (تصنيفٌ **رباعيّ** من مخرَج pytest: عَضّ · مرّت · **لم يُقَس (تخطٍّ)** · تعذّر) | يدويًّا | `tests/test_guard_bite_sweep_logic.py` | «تعذّر التشغيل» عطبٌ لا نجاح · **و«لم يُقَس» لا تُقرأ «مرّت»** (R57-5) · **ويُشغَّل وحده:** يحقن السمومَ في **شجرة العمل** (يعدّل ملفّات البوّابات ثم يسترجعها) ⇒ فلا يُوازى مع بوّابةٍ تقرأ الشجرةَ أو تنسخها: قِيس (الجولة ٦٢) أنّ مسبارَ الهبوط سقط في ضابطٍ **لأنّه نسخ الشجرةَ أثناء الحقن** — عطبُ قياسٍ لا عطبُ شيفرة (وفُحص الأثرُ: لا ملفَّ `.bak_bite` شاردًا بعده، والقياسُ المنفردُ يخضرّ) |
| 10 | `tools/refs_exposure_probe.py` | مراجعُ `refs/pull/*` والتزاماتُ ما قبل التنقية | **مهمّةُ Hermes الأسبوعيّة** (لا CI — قرارُ المالك: سجلٌّ عامٌّ لا يُعلَن فيه ما لم يُطهَّر) | `tests/test_refs_probe_exit_code.py` | `rc=1` + `STILL EXPOSED` |
| 11 | `.githooks/pre-commit` · `pre-push` | المنعُ قبل الالتزام/الدفع (سرٌّ · مبلغ · مسارُ دليل) | محلّيّ (بشرط التهيئة) | `tests/test_hook_gates.py` (**يُنفّذ الخطّافَ فعلاً**) | `exit 1` |
| 12 | **حارسُ الجواب (P-9)** — `tests/test_answer_names_witness.py` | جوابٌ للمنفّذ **لا يُسمّي حُكماً**: حقلُ `in-reply-to:` غائب، أو فارغ، أو يشير إلى **الملفّ نفسِه** (شاهدٌ ذاتيّ)، أو إلى مسارٍ **ليس تحت `handoff/claude/`** (الحُكمُ يُصدره المدقّق ⇒ فقرارُ المنفّذ و`README.md` ليسا حُكماً)، أو إلى **ملفٍّ ساكنٍ بلا زمنٍ في اسمه** (ليس حُكماً · R59-2: `STATE.md` و`gate-injection-harness.py` قِيس أنّهما كانا يمرّان)، أو إلى حُكمٍ **أُودِع بعد الجواب**، أو إلى **حُكمٍ ليس أحدثَ ما سبق الجواب** (الحدُّ ٢ المُعلَن صار مربوطًا · R59-2)، أو إلى مسارٍ **غيرِ موجودٍ في الشجرة المدفوعة** (§٢٧: «الشاهدُ يُدفَع قبل أن يُجاب») · **وصنفٌ مُعلَنٌ بالهويّة** (`class:` في ملفٍّ واحدٍ مُسمّى) لا يُطالَب بشاهد — ويُشترَط ألّا يحمل `in-reply-to` أصلاً · **وصنفُ ما ليس جوابًا يُسمّى باسمه وصنفه** (`NON_ANSWER_FILES_SINCE_RULE` قاموسًا لا عددًا · R61-1) | الـCI (خطوةُ الـsuite) | `tests/test_answer_names_witness.py` (وفيه ضابطُ سمٍّ في الذاكرة · وضابطُ غيابِ الحارس المنزوع · ومسبارُ الحدّين) + سمُّ **م١٧** في `tools/guard_bite_sweep.py` | فشلُ اختبار · **وحدُّ الطريقة مُعلَن** (ما قبل `WITNESS_FROM` دَينٌ مقيس · وصنفُ الأجوبة يُقاس بعد القاعدة · و**صحّةُ كونِه المراجعةَ المقصودة بعينها غيرُ مقيسة** — والربطُ إلى «الأحدث» تقريبٌ معلَن) |
| 13 | `tools/qa_gate.py` — **البوّابةُ الإلزاميّةُ قبل التسليم** | قيمُ العيّنة (ص١/ص٢ · تتابعُ ص٩→ص١٠) · **الجدولُ الذهبيُّ للمُحلّل** · أوراكلُ الفوتر **ومقامُه** (لا صفحةَ تخرج من الأوراكل بلا حكم) · أنواعُ المبلغ المتكرّر · **وسياسةُ اللاحتميّة** (قراءةٌ ثانيةٌ عند الشكوك · لا براءةَ بلا قراءةٍ نظيفة · ولا قراءةٌ أنحفُ تُبرّئ · **وثقبُ مقام التغطية عطبُ نظافةٍ لا سقوطٌ صامت** — R60-2) · **والدليلُ يُكتب بالأداة في كلّ تشغيلٍ شامل** | **يدويًّا** (`--full` · يتطلّب الخدمةَ والنموذج) — و`--quick` في النسخة المُهيّأة | `tests/test_gate_goldens.py` · `tests/test_gate_flake_policy.py` (ضوابطُ خفيفةٌ بلا شبكة) + **سمومُها م١٢–م١٥ وم٢٣** في `tools/guard_bite_sweep.py` | `rc=1` — وكلُّ سقوطٍ يُسمّي سببَه، والإقلاعُ غيرُ النظيف يطبَع `[flaky_read]` بقياساتِ القراءتين، **ودليلُ التشغيل يُكتب بنفسه حتى عند السقوط** · **وسطحُ أدوات السلسلة** (`src/statement_qa/qa_tools.py`: `sum`/`count`/`extremes`/`closing`/`page_summary`/`page_rows`/`search_rows`) **يُقاس بضوابطه** `tests/test_qa_tools.py` (**٢١ ضابطًا** — حالًا: `pytest tests/test_qa_tools.py`)، **وهو خارج قائمة §٣** لسببٍ مقيسٍ لا مظنون: تبعيّاتُ الخطوة الخفيفة لا تحمل `langchain_core` (قياسُ الاستبعاد وعلّتُه في `docs/ROADMAP.md` صفّ ١·٢ — R64-F1/F2: رقمٌ بلا بيئته دعوى) |
| 14 | **سجلُّ اللاحتميّة (شرطُ مراجعة ٥٨)** — `handoff/FLAKY_READS.md` + `tests/test_flake_ledger.py` | وسمُ `[flaky_read]` **يُطبع ولا يُعدّ** ⇒ فكلُّ تشغيلٍ محفوظٍ فيه الوسم يجب أن يكون له **قيدٌ** في السجلّ، وعددُ القيود يساوي ثابتاً مُعلَناً (فلا إضافةَ صامتة) · **وميزانيّةٌ تحمرّ** إن تجاوز عددُ **الأحداث المُلوَّثة** **١** في نافذة **آخر ٥** تشغيلاتٍ محفوظة — **والحدثُ = تشغيلٌ مُلوَّثٌ محفوظ أو قيدٌ بلا دليلٍ محفوظ** (S6 · إغلاقُ مراجعة ٦١: كان العدُّ من ملفّات الأدلّة وحدَها وقيدُ السجلّ الحيّ دليلُه «لم يُحفَظ» ⇒ قُرئت 0/1 وسجلٌّ فيه واقعة) · **والوسمُ يُستورَد من مُنتِجه** (`FLAKY_MARK` في `tools/qa_gate.py`) فلا حرفان يفترقان · **ودليلُ التشغيل يُنزَّه من مسارات المنزل عند الكتابة** (`_sanitize`) · **واسمُ الدليل وترويستُه من زمنٍ واحد** | الـCI (خطوةُ الـsuite) | `tests/test_flake_ledger.py` (وفيه سمٌّ مصنوع للنافذة والميزانيّة **وحالةُ القيد بلا دليل** · ومقابلةُ نثرِ السجلّ بثوابته **بالرموز لا بحضور النصّ**) + سمومُها **م١٦–م١٨** في `tools/guard_bite_sweep.py` | فشلُ اختبار · **وحدُّه مُعلَن** (النافذةُ من الأدلّة **المحفوظة** لا من كلّ تشغيل · **والدليلُ يكتبه `qa_gate --full` بنفسه في كلّ تشغيلٍ ناجحٍ أو ساقط (R60-2)** فلا تخرج واقعةٌ من النافذة بيدِ كاتبٍ نسي · وأثرُ ما قبل السجلّ لا يُحسب) |
| 15 | **قرارُ مالكٍ مؤرَّخ على *ثمن* الإفراج (البوابة ٣)** — يُقرأ بـ`tools/eval_pack.py` (القراءتان `--build`/`--verify`) والمالِكُ **`tools/gate3.py`** (كائنُ القرار + دالّتاه النقيّتان + قارئُ الثمن `capture_withheld`/`price_of_release` + صيغتُه `price_label` + حرسُ العدد `is_count`) | قرارٌ **بلا تاريخ** أو **بلا سقفٍ معلَن** أو **بلا مجتمعٍ معلَن** لا يُقبل قراراً · و**لا يُغطّي عطباً** · و**التقاطعُ `∩ = ∅` لا يُرخى بقرارٍ أبدًا** (قِيس في review-60 أن السقفَ كان يُرخيه حتى ١٣٣ صفحة) · والقرارُ يقع على **الثمن**: صفحاتٌ من الحزمة محجوبةٌ عن التدريب، **سقفُ ثمنِ الإفراج 133 صفحة**، وفوقه يعود الحكمُ «شرطيًّا» يُعرض على المالك · و**ثمنٌ غيرُ مقيس ⇒ فشلٌ مُغلَق** (لا صفرٌ يُبرّئ · **و`true` ليس عددًا** — `is_count`) · و**قرارُ الحزمة المُلتزم يُقابَل بقرار الأداة ولا يُقرأ منه** · و**مقابلةُ عدّاد الالتقاط بقائمته المُعلَّلة تُحكَم في القارئين معًا** (`listed_matches`) | `--verify` يقيس **التقاطعَ والثمنَ** من القرص (والبناءُ يكتبهما في `pack.json`) | `tests/test_eval_pack.py` (ضوابط: تاريخٌ/سقفٌ/مجتمعٌ · **التداخلُ عطبٌ مُسمًّى لا قرارُ مالك** · سقفُ الثمن لا يتمدّد · الثمنُ غيرُ المقيس يفشل مُغلَقًا · `true` ليس ثمنًا · القائمةُ تخالف العدّاد في البناء **وفي `--verify`** · القرارُ المُلتزمُ في الحزمة لا يتعفّن · **حزمةٌ مُحرَّفةٌ تُسقط `--verify` بـ`rc=1` وباسمٍ مطبوع — بنداء الأمر نفسِه**) + سمومُه **م١٩** (سقفُ الثمن) · **م٢٠** (البوابةُ لا تُرخى بقرار) · **م٢٤** (العددُ الحقيقيّ ليس `bool`) في `tools/guard_bite_sweep.py` | فشلُ اختبار · أو `rc=1` من `--verify` — **والقرارُ يُطبع بتاريخه وقياسِه** لا يُدَّعى · **وحدُّه مُعلَن:** الضابطُ الحيّ على `data/eval_pack/pack.json` **يُتخطّى في الـCI باسمه** (`data/` غيرُ متتبَّعة) ⇒ المُقاسُ هناك أرضيّةُ `tmp_path` (تقيس البناءَ والمقابلةَ والقرارَ)، والحزمةُ الحيّةُ تُقاس في شجرة المالك — والتخطّي «لم يُقَس» لا «بوّابةٌ عضّت» |
| 16 | **مسبارُ الهبوط (P-11 · تبنّاه المالك من توصية مراجعة ٥٩)** — `tools/landing_probe.py` + خطوةُ `gate_landing` | **الثابتُ الذي انكسر ثلاثَ مرّات:** «هل تهبط مراجعة؟» — R57-1 (حارسُ المكان) · R58-1 (عدّادُ الكشيدة) · R59-1 (ضابطُ مواضع النثر): في كلّ مرّةٍ كان **نصُّ المدقّق مُدخَلاً إلى راتشتٍ يملكه المنفّذ** ⇒ فمجردُ هبوطِ نصٍّ يُحمرّ الـCI. المسبارُ يبني **نسخةً مؤقّتة** (`--no-hardlinks` · فرعٌ واحد)، يُودِع فيها **مراجعةً اصطناعيّةً** بزمن اللحظة تحت `handoff/claude/` تحمل كلَّ ما تصطاده العدّادات (صيغةُ الكشيدة · «N سطراً في M ملفّ» · `status:` في المتن · `in-reply-to:` مقتبَسًا · كلمة REPORT · معرّفُ التزامٍ غريب) ثمّ يُشغّل **قائمةَ ضوابط الـCI هناك** | **الـCI (خطوةُ `gate_landing`)** — ويدويًّا `--from-worktree` للمسح · و`--poison name-stamp` لإثبات العضّ | `tests/test_landing_probe.py` (قائمةٌ تُقرأ من الـworkflow بمقابلةٍ مستقلّة · الخطوةُ موصولةٌ ومُسجَّلة · شكلُ الشاهد · الاسمُ والسمّ · فشلٌ مُغلَق · **`HEAD` ليس اسمَ فرع** — حاجبُ مراجعة ٦١: `checkout` في `pull_request` يُنتج رأسًا مفصولًا فكان `--branch HEAD` يُسقط القياسَ ويُحمرّ الـCI في كلّ طلبِ دمج) + `tools/guard_bite_sweep.py`: **م٢١** (المسبارُ يكشف مراجعةً لا تهبط) · **م٢٢** (الصنفُ يعود حيًّا فيُمسَك) · **و`MUST_NAME`** يرفض عَضًّا بلا الاسم المتوقَّع في مخرَجه (يُحصى «تعذّر قياس» لا شهادة) **وضابطٌ يمنع مدخلةً ميتةً فيه** (إعادةُ تسميةٍ في `CASES` تُحمِّر) **وشكلُ الأمر صريحٌ** (قائمةٌ ⇒ سكربت · نصٌّ ⇒ ضابطُ pytest) | `rc=1` باسمِ ما سقط · و`rc=2` **تعذّرُ قياس** (لا نسخةَ · لا pytest · لا قائمةَ مقروءة) — **لا يُقرأ نظافة** · **وحدُّه مُعلَن:** لا يقيس فحصَ التاريخ (ثقيلٌ ولا يتغيّر بهبوط نصّ) ولا بوّاباتِ المال/الثوابت/اللقطة (تقرأ الشيفرةَ لا صندوقَ المراسلة)، والمقيسُ شجرةٌ **مُودَعة**، وقياسُه **١٧–١٨ ثانية** |
| 17 | **مُدقِّقُ الدفع: CI لكلِّ مرجع (ملاحظةُ مراجعة ٦١ «R61-1 — ملحقُ الدفع»؛ وهي **ثانيةٌ** بالاسم لِـ«R61-1» في خطة ٦١ = بندُ قاموس ما ليس جواباً (الصفّ ١٢) — تعارضُ تسميةٍ مُعلَن لا يُخفى)** — `tools/ci_report.py` + تذكيرٌ في `.githooks/pre-push` | **«ولا يُقال أخضرُ بلا دليل»:** ملحقُ دفعٍ قال «CI ناجح» وقِيس مرجعٌ واحدٌ من المراجع المدفوعة، وفرعان مدفوعان كانا **أحمرَين** — فالصنفُ ليس سهواً في جملة، بل **ادّعاءٌ بلا موضعِ قياس**. الأداةُ تُنتج الموضع: نتيجةُ CI **لكلِّ مرجعٍ دُفع**، **مقيسةً على الالتزام المدفوع** (`<remote>/<branch>` — والمرجعُ يُطبَّع إلى اسم فرعٍ أوّلًا، والريموتاتُ **تُسأل git** لا تُكتب بيد؛ R64-F7) لا على ما في اليد ولا على تشغيلٍ قديم؛ ولا «أخضر» لمرجعٍ أحمرَ أو **جارٍ** أو **بلا تشغيل** أو **غيرِ مقروء** (تعذّرُ `gh`) — ويُعلَن التزامٌ محلّيٌّ متقدّمٌ على المدفوع | **يدويًّا** بعد كلّ دفع (`python3 tools/ci_report.py <refs…>` · و`--json` للسجلّ) — **وتذكيرًا في خطّاف `pre-push`** (`--pre-push` يقرأ سطورَ الدفع من `stdin` **ويقرأ وجهةَ الدفع** (الحقل الثالث) — لا الحقلَ الأول: `(delete)`/`HEAD`/`refs/tags/…` **مقيسٌ** أنّ git يُرسلها — ويطبع الأمرَ بمراجعها **ويُعلن ما لا يُقاس** (وسمٌ · حذفٌ)، **ولا يمسّ الشبكة**، **وخروجُه صفرٌ دائماً** (`exit 0` صريحاً بعده: R62-F1 — كان آخرَ سطرٍ فيخرج الخطّافُ 1 بلا سببٍ فيُرفض دفعُ وسمٍ/حذفِ فرع/دفعٌ بلا جديد، وهو حافزُ تجاوزِ الخطّاف (no-verify) المنصوصُ تحذيرُه في رأسه)) | `tests/test_ci_report.py` (أحمرُ يُسمّى · **تشغيلٌ على التزامٍ آخرَ لا يُبرّئ الرأس** · بلا تشغيل · جارٍ · تعذّرُ القياس ⇒ رمز ٢ · **الدفعةُ كلُّها تُقاس لا أوّلُها** · **المقيسُ هو المدفوع لا المحلّيّ** · التذكيرُ يسمّي الأمرَ ولا يحكم · **أشكالُ `stdin` الأربعةُ المقيسةُ من git تُقرأ بوجهتها** · **`--pre-push` لا يُسقط دفعاً في كلّ شكل** · **تشغيلٌ بلا `headSha` ⇒ «غيرُ مقروء» (فشلٌ مُغلَق)** · **`--json` يُستهلك آليًّا** (stdout = JSON وحده وإعلانُ الحدّ على stderr) · **«غيرُ المقروء ليس نظيفاً»**) + `tests/test_hook_gates.py` (**يُنفّذ الخطّافَ الحقيقيّ**: وسمٌ · حذفُ فرع · رأسٌ مفصول · `stdin` فارغ ⇒ صفرٌ في كلّها · **ودفعُ وسمٍ حقيقيّ إلى ريموتٍ محلّيّ يمرّ**) + سمُّاه **م٢٥** (تشغيلٌ بلا مرساة يُوسَم أخضر) · **م٢٦** (الخطّافُ يُسقط دفعاً مشروعاً — يُعيد الأسطرَ التي كانت · R62-F1) · **وضابطُ سِمَةِ الريموتات** (`test_the_remote_list_is_asked_fresh_and_never_cached_globally` — يعضّ على إعادة أيّ تخزينٍ عالميٍّ يُبطل سِمَةَ `_run` · مقعدُ البنية) — **وهو في `tests/test_ci_report.py` لا في `tools/guard_bite_sweep.py`** (قِيس في مراجعة إغلاق ٦٤: الإدراجُ وقع قبل لاحقة «في `tools/guard_bite_sweep.py`» التي تخصّ م٢٥/م٢٦ فقرأه الصفُّ في الملفّ الخطأ، **وخالف عمودَ الحدّ في الصفّ نفسِه**) · و«**29 passed**» في وصف طفرة (ب) هو عدُّ ما قبل الإصلاح: **يُرخّى بلحظته** (`1e1182b`=**٢٩** · `39beb36`=**٣٠** · `e07440b`=**٣١** · **٣٢** بعد ضابط عقد القائمة الفارغة) | `rc=1` بمرجعٍ ليس أخضرَ (بالاسم) · و`rc=2` تعذّرُ القياس («غيرُ المقروء ليس نظيفاً») — **وحدُّه مُعلَن:** يقيس نتيجةَ تشغيلٍ على GitHub لا صحّةَ الشجرة · **ووجوبُ حملِ التقرير لمخرَج الأداة قاعدةٌ مُعلَنة لا بوّابة** (الحاملُ بشريٌّ يذكّره `pre-push`؛ والموضعُ القائم `tests/test_report_names.py` يقيس **أسماءَ** التقارير لا مضمونَها) · **وعبرَ الـCI**: `tests/test_ci_report.py` في قائمة §٣ والـworkflow تساوياً · **وصيغُ المرجع كلُّها تُطبَّع إلى اسم فرعٍ واحد** (`refs/heads/x` · `refs/remotes/<remote>/x` · `<remote>/x`) **وأسماءُ الريموتات تُسأل git** لا تُكتب بيد (R64-F1/F7: كان `refs/heads/main` يُنتج `origin/refs/heads/main` فيسقط إلى المرجع المحلّيّ فيُوسَم الرأسُ المدفوع «قديمًا» كذبًا · وكان ريموتٌ غيرُ `origin`/`upstream` يمرّ بلا تحويل فيعود الـ`BLOCK` الكاذب) · **ولا سمَّ للخاصيّة الجديدة في `guard_bite_sweep`، والسببُ مُعلَنٌ ومقيس** (R64، طلبُ المقاعد): `branch_of`/`remotes` **دالّتان داخل أداةٍ يدويّةٍ لا بوّابة** — وعُرفُ «لكلّ بوّابةٍ سمّ» يقع على البوّابات — وسمُّها البديلُ **الطفرةُ في نسخةٍ**: (أ) إسقاطُ التطبيع (`branch = ref` **في الموضعين معًا**: `_pushed_sha` و`verdict`) ⇒ **2 failed** ومنه `test_a_tracking_ref_is_measured_by_its_branch_name`؛ وطفرةُ موضعٍ واحدٍ (`_pushed_sha` وحدَه) ⇒ **1 failed** = `test_a_git_style_ref_resolves_to_the_pushed_commit_not_the_local_one` (فالموضعُ يُسمّى بتمامه — قياسُ مقعدَي المعايير والبنية في مراجعة الإغلاق) · (ب) إبدالُ الدورة على `remotes()` بقائمةٍ مغلقة `["origin"]` ⇒ **لم يُسقط شيئًا (29 passed)** ⇒ فكُتب الضابطُ الغائبُ `test_a_remote_that_is_not_origin_is_tried_when_it_holds_the_pushed_commit`، وبإعادة الطفرة **1 failed** ✓ (فالإغلاقُ صار مسنودًا بضابطٍ يعضّ لا بادّعاء) |

## ٣ · قائمةُ الـCI المُعلَنة (تُقابَل بالـworkflow تساوياً)

خطواتُ الـCI: `publish_guard --tree --history --ci` · `amount_guard --ci` · `static_gate` ·
`render_claims --check` · خطوةُ الأدلّة، ثم ملفّاتُ `pytest` هذه بالضبط:

```
tests/test_compile_all.py
tests/test_requirements_lock.py
tests/test_public_claims.py
tests/test_gate_bites.py
tests/test_gate_registry.py
tests/test_secret_scan.py
tests/test_report_names.py
tests/test_turn_derivation.py
tests/test_hook_gates.py
tests/test_static_gate.py
tests/test_amount_guard.py
tests/test_ci_report.py
tests/test_answer_names_witness.py
tests/test_gate_goldens.py
tests/test_gate_flake_policy.py
tests/test_flake_ledger.py
tests/test_landing_probe.py
tests/test_eval_pack.py
tests/test_local_reader_probe.py
```

**ومعرّفُ كلّ خطوةٍ مُسجَّلٌ هنا — تُقابَل بالـworkflow في الاتّجاهين (لا خطوةَ بلا معرّفٍ مُسجَّل، ولا معرّفَ شبح):**

```
gate_pii · gate_amount · gate_static · gate_claims · gate_suite · gate_landing · gate_data_untracked · gate_amount_key · gate_amount_ratchet
```

**و`gate_landing` (P-11 · أُضيف في الجولة ٦١ بقرار المالك):** يقيس **الثابتَ** لا العدّادات — *«هل تهبط
مراجعة؟»* — لأنّ ثلاثةَ إصلاحاتٍ للصنف نفسه (`R57-1` حارسُ المكان · `R58-1` عدّادُ الكشيدة · `R59-1`
ضابطُ مواضع النثر) سقط كلٌّ منها في جولة: **نصُّ المدقّق كان مُدخَلاً إلى راتشتٍ يملكه المنفّذ**.
الأداةُ: `tools/landing_probe.py` — تنسخ الشجرةَ (`--no-hardlinks` · فرعٌ واحد)، تُودِع فيها **مراجعةً
اصطناعيّة** بزمن اللحظة **بساعة الشجرة لا بساعة العدّاء (R65-1: منطقةُ التسمية `+03`، وفوق أحدثِ ختمٍ
فيها ⇒ تخرج من كلّ نافذة `(شاهد, جواب]` فلا تُسقِط جوابًا حقيقيًّا في تشغيلٍ بتوقيت UTC)** تحت `handoff/claude/` تحمل كلَّ ما تصطاده العدّادات، ثم تُشغّل قائمةَ الضوابط
هناك. **وقائمةُ المقيس تُقرأ من هذا الـworkflow نفسِه** (لا نسخةَ ثانية تتقادم).
**وحدُّها مُعلَن:** لا تقيس `--history` (لا يتغيّر بهبوط نصّ · وثقيل) ولا بوّاباتِ المال/الثوابت/اللقطة
(تقرأ الشيفرةَ لا صندوقَ المراسلة) — والمقيسُ شجرةٌ مُودَعة، والعطبُ يُسمّى خارجًا بـ`rc=1`.

**وحدُّ ذلك مُعلَن:** بقيةُ ضوابط المستودع (`publish_guard` · `guard_bite_sweep` · `refs_probe`) **لا يعبرها
الـCI اليوم** ⇒ حَرْسُها في النسخة المُهيّأة أو في المهمّة الأسبوعيّة، والسجلُّ يذكر ذلك **لكلّ صفٍّ** بدل ادّعاءِ
تغطيةٍ عامّة. ولا رقمَ مكتوبٌ بيدٍ هنا: العددُ يُقاس من قائمة الـworkflow ويقابله الضابطُ.

**وتوسيعان مقيسان (بعد قياس جولة ٥٥):** أُضيف `tests/test_hook_gates.py` (يُنفّذ الخطّافَ فعلاً بـsubprocess
⇒ يجعل الطبقةَ ١ محروسةً في الـCI لا بالتهيئة المحلّيّة وحدها) و`tests/test_static_gate.py` ·
`tests/test_amount_guard.py` (ضوابطُ أدواتٍ يشغّلها الـCI نفسُه فكان من غير المتّسق إبقاءُ ضوابطها خارجه).
وقياسُ القبول: في **نسخةٍ نقيّة** (بلا `data/` وبلا `.venv`) ⇒ ملفّاتُ الـCI كلُّها أخضر، والمرشّحون الثلاثة
(`tests/test_hook_gates.py` · `tests/test_static_gate.py` · `tests/test_amount_guard.py`) كانوا `34 passed,
14 skipped` **بتخطٍّ مُعلَن لا صامت** — وهو **قياسٌ مؤرَّخٌ على جولة ٥٥** لا حيًّا: الملفّاتُ نمت بعده، والعددُ
الحاليُّ يُقاس بالأمر نفسه (`pytest tests/test_hook_gates.py tests/test_static_gate.py tests/test_amount_guard.py`)
ولا يُنقَل من هنا (قاعدةُ «مؤشِّرٌ لا قيمةٌ مخزَّنة»).
