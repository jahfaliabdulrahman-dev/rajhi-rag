# ROADMAP — مسارَا rajhi-rag

> **مسارَان لا واحد**: (أ) **تسليمٌ أكاديمي** — مشروع تخرّج على LangChain، يُقاس بمقاييس اللجنة.
> (ب) **هدفٌ نهائي** — منتجٌ يدفع أحدٌ مقابله، ويُقاس بـ«هل دفع أحدٌ مقدَّماً».
>
> **كيف تُقرأ**: كل بند **مربوطٌ ببوابة** (شرطُ إنجازٍ يُقاس بأمر). بندٌ بلا بوابةٍ ليس بنداً.
> والحالةُ أمام كل بند **مقيسةٌ بتاريخها** — لا مأخوذةٌ من نيّة. مصادرُ الحالة: أمرٌ في السطر، أو رسالةٌ في
> `handoff/` مُسمّاة. و`🔶` = بدأ ولم يُكمل · `⬜` = لم يبدأ · `✅` = مُنتهٍ ببوابته.
>
> **آخر قياس:** 2026-09-24 · الفرعُ العاملُ `feat/item4-pack-free-capture-44` (PR #119) · **ورأساه يُقرآن لا يُكتبان**:
> `git rev-parse --short origin/main` · `git rev-parse --short HEAD` · وحالةُ الدمج بـ`gh pr view 119 --json mergeStateStatus,mergeable`.
> ⚠ **قاعدةٌ لا زينة** (مراجعة ٥٢ · R52-5، وصُحّحت في الجولة نفسها): كان هنا `main = 51b2e9d` و«رأسُ العمل `01e4c14`»،
> فقِيس بعدها أنّ الرأسَ تحرّك بثلاثة التزامات — ثم **كُتب الرأسُ الجديد في الجملة التي تمنع كتابته**، فأمسكه مقعدُ
> المعايير: ⇒ **لا يُكتب SHA رأسٍ متحرّك في وثيقة، ولا في سطرٍ يمنع ذلك** (وإلا صار الدليلُ ضدَّ نفسه).
> وSHA يُذكر لِما **لا يتحرّك**:
> أساسُ دمجٍ · التزامُ سجلٍّ · بصمةُ حزمةٍ مجمَّدة. · المسار (أ) مصدره `handoff/claude/20260920-231400-university-build-changes.md`
> والمسار (ب) مصدره `handoff/claude/audit/20260917-strategy-market-review.md` + `docs/MARKET_EVIDENCE.md` + `references/product-journey.md`.

---

## القاعدة الحاكمة للمسارين معاً

> **نموذجٌ واحد · عقودٌ كثيرة.** و**السلسلة حَكَمٌ** في كل خليّة · و**لا رقم بلا دليل** ·
> و**ما لم يُثبت يُسمّى ولا يُخمَّن**. أي بندٍ يناقض هذه الأربعة **يُرفض ولو نجح**.

---

# المسار (أ) — الجامعة · تسليمٌ يقيسه غيرُنا

**تعريف «تمّ»**: البنود **٤ · ٢ · ١ · ٥** خضراء + حزمة تقييمٍ واحدة تُطبع أرقامها + **الثوابتُ الأربعة لم
تتحرّك**. وقيمُها **لا تُكتب في نصّ** (تُمنع بالقاعدة الأمنية): تُقرأ بأمرٍ من مصدرها — `Σمدين` و`Σدائن` و`الإقفال`
من بوابة الإقفال على الملف المُسلَّم، وعددُ الصفوف من تقرير التشغيلة. **أيُّ ظهورٍ لمبلغٍ حقيقيّ في الشجرة يُسقط
الدفع** (`tools/amount_guard.py`).

## أ-١ · حزمة تقييم مجمّدة ▸ البند ٤  `🔶 قيد التنفيذ`
> **البوابة**: حزمةٌ مُختمة + ٣ مقاييس للقراءة + ٣ للسؤال، تُطبع بأمرٍ واحد يُعيد إنتاجها.

- [x] **البوابة (١) — ختمُ هوية الكوربوس**: `3e2d360a665c88aa` مُعلنة في `corpus_provenance` (2026-09-22)
- [x] **البوابة (٢) — إعادةُ الالتقاط من الكاش $0**: 416 صفحة · 209 حجزاً · 3,841 صفّاً · 9.5s
- [x] **البوابة (٣) — أداةُ الحزمة مكتوبةٌ من الصفر**: `tools/eval_pack.py` + `docs/EVAL_PACK.md` + 10 اختبارات
- [ ] **إغلاق حكم البوابة (٣)**: التقاطع 100 صفحة — **قرارٌ لا عطب** (ثمنُ إلغاء قاعدة `%3` = 100 صفحة · $0) ⇒ يحتاج قراراً
- [ ] **البوابة (٤) — مفتاحا المستند `(doc_id, page)` + زمن/محتوى**: حالتها **`مُهيّأة` لا `مُختبَرة`**
- [x] **تجميد الـ٢٠٠ صفحة** بقاعدةٍ حتمية + حقيقتها الأرضية من **السلسلة** (لا من قراءة نموذج) — 2026-09-24: `--verify` PASS · ختمُ المحتوى **٢٠٠/٢٠٠** · التقاطع **٠**
- [x] **٥٠ سؤالاً** عربياً + الإجابة + **الصفوف التي تُثبتها** — الأسئلةُ كُتبت ونُفِّذت (**٥٠** · بصمةُ النصّ `4facf540abd4`)، **وأُغلق الناقص (٢٠٢٦-٠٩-٢٤):** `--truth` يُطبع **موضعَ الإثبات** لكلّ سؤال، و`--validate` **يُسقط** السؤالَ
  الذي لا موضعَ له (`classify_proof`) ⇒ والاستثناءُ الوحيدُ **مُعلَنٌ باسمه وسببه**: `abs-02` (الصفحة ٦٢٩:
  صفوفٌ بلا رصيد ⇒ لا موضعَ في عالم الصفوف · وإعادةُ الصياغة تُسقط بصمةَ الـ٢٠٠) · **آخرُ قياس (٢٠٢٦-٠٩-٢٤):** أربعُ جولاتٍ منفَّذة على البصمة نفسها (`4facf540abd4`) ⇒ **٣٠ · ٢٧ · ٢٨ · ٣٠** من ٥٠، وعلى الأسئلة النظيفة المشتركة (٣٣) **٢٣/٢٣/٢٣/٢٣** مع **سؤالين متذبذبين** (`num-04` · `num-08`) ⇒ **المنتِجُ ليس حتميًّا تمامًا** · وصدقُ الاستشهاد **شبهُ غيرِ مقيس** (١–٣ سجلاتٍ محكومةٍ من عشرةٍ في كلّ ذراع)
- [x] **المقاييس الثلاثة تُطبع**: دقّة الرقم · **صدق الاستشهاد من الأثر** · صحّة الامتناع — تُعاد بأمرٍ واحد وبلا صرف (`--rescore`)، 2026-09-24
- [x] **سمّا حرس**: تغييرُ بصمة صفحةٍ مقيَّدة ⇒ يسقط ✓ (**١١ سمًّا مُلتزماً** في `tests/test_content_seal_poisons.py`) · **سؤالٌ بلا صفوف إثبات ⇒ يسقط** ✓ **كُتب** (`tools/eval_questions.py::classify_proof` · وضابطٌ في `tests/test_eval_proof_rows.py` يشمل كشفَ الاستثناء المتقادم) — ٢٠٢٦-٠٩-٢٤

## أ-٢ · `Measured`: القيمة وإثباتها كائنٌ واحد ▸ البند ٢  `⬜ لم يبدأ`
> **البوابة**: بناءُ رقمٍ بلا مصدر **يرمي استثناءً**، والملف المُسلَّم يُقابل بالقديم **صفّاً بصفّ**.
> **القياس اليوم**: لا `class Measured` في `src/` ولا `tools/` (`grep -rn 'class Measured' --include=*.py src tools` ⇒ فارغ)

- [ ] النوع `Measured(amount: Decimal, source, note)` — والبناء بلا مصدر يفشل بالبناء لا بالفحص
- [ ] يمرّ عليه **كلُّ** الأنبوب: القراءة ⇒ الاشتقاق ⇒ التقرير ⇒ الإكسل
- [ ] **تخفيفُ** الفحوص التي كانت تحرس الصنف بعد أن صار غير قابل للنقض (لا تشديد)

## أ-٣ · دفتر SQLite مصدراً والإكسل/PDF رَندر ▸ البند ١  `⬜ لم يبدأ`
> **البوابة**: الإكسل المُرندَر **من الدفتر** = الملف الحالي **صفّاً بصفّ** — فرقٌ واحد = توقّف.
> **القياس اليوم**: لا `sqlite3` في `src/` · `tools/` · `app.py` (`grep -rln sqlite3` ⇒ فارغ)

- [ ] المخطَّط + **القيود** (`CHECK` للمرساة + `proof_source` · `TEXT` لكل مبلغ لا `REAL`)
- [ ] `AFTER INSERT/UPDATE/DELETE` triggers لـ`row_fts` (بلاها ينطفئ الفهرس **صامتاً**)
- [ ] تطبيعٌ عربي (`remove_diacritics 2` + عمود مُطبَّع: همزات · تاء مربوطة · تطويل)
- [ ] المناظير `coverage` / `unproven` تُشتقّ «ما لم يُثبت» — لا يُكتب بيد
- [ ] **الاختبار الحاسم**: الرندر من الدفتر = الحالي صفّاً بصفّ
- [ ] FTS5 يُقاس على الخمسين سؤالاً **قبل** أي حذف لـFAISS

## أ-٤ · رفع طبقة السؤال إلى LangChain 1.0 ▸ البند ٥  `🔶 جزئيّ`
> **البوابة**: سؤالٌ يعود بكائنٍ مقيَّد نوعاً فيه `cited_row_ids` مُصرَّحٌ بها، ويُقابَل بالأثر.
> **القياس اليوم**: `create_agent` موجود (`qa.py:130`) · و**`response_format` غائب** (`grep -rn response_format src app.py` ⇒ فارغ).

- [ ] `response_format=Answer` ⇒ الاستشهاد يُسلَّم **من النوع** لا يُقصّ من النثر
- [ ] مقياس صدق الاستشهاد يُقابَل **بالأثر** ⇒ «النوع يُصلح الاستخراج · والأثر يُصلح الصدق»
- [x] سقفُ الإعادات **رقمٌ في العقد** (`retry_budget: 3` · `recursion_limit: 12` — والافتراضي المثبَّت **10,007**)
- [ ] الأثرُ والامتناع **وسيطاً (middleware)**
- [ ] ⛔ **لا LangChain في مسار الاستخراج** — حتميٌّ بلا إطار

## أ-٥ · القلب يقرأ العقد ▸ البند ٣  `⬜ لم يبدأ`
> **البوابة**: إضافةُ تصميمٍ جديد **بلا تعديل بايثون** · ولا نصَّ بنكٍ مثبّتاً في `src/`.
> **القياس اليوم**: `grep -rln 'profiles/' src app.py` ⇒ **فارغ** (العقد يقرؤه `tools/verify_close.py` وحده)

- [ ] تمريرُ قوائم الأوصاف بدل تكرارها (`row_audit.py:21`) — **واتبع الأعطال لا التخمين**
- [ ] تصميمٌ ثانٍ يُضاف بملفٍّ وحده

## أ-٦ · واجهةُ الكشف الرقمي + المصنِّف ▸ البند ٧  `🔶`
> **البوابة**: كشفُ التطبيق يمرّ من **نفس** البوابة بلا استثناء، وصفحةُ صورةٍ في PDF «رقمي» تُحوَّل لمسار المسح.
> **القياس اليوم**: `text_reader.py` موجود والقارئ يجتاز **30 فحصاً ALL PASS**، لكن **`app.py` لا يستدعيه**.

- [ ] مصنِّف `text/image/ocr_overlay` + توجيه تلقائي
- [ ] تبويبٌ في الواجهة للرقمي · وإعلانُ حدوده («0 كسور» = اتّساقٌ داخلي لا صدورٌ عن المصرف)

## أ-٧ · وحدة الأرقام العربية (البند ٨) · موازاة الصفحات (البند ٩)  `⬜`
- [ ] وحدةٌ واحدة بجدول حالات: `,` عشرية/آلاف · `٫` · ثلاث مجموعات (أنتجت **٣٨٥ فرقاً كاذباً**)
- [ ] موازاةُ الصفحات (الصفحات مستقلّة · 15.97ث × 629 ≈ 2.8 ساعة تسلسلياً)

## أ-٨ · التقاطُ عيّنات التدريب ▸ البند ٦  `✅ مُنتهٍ`
- [x] 416 صفحة · 209 حجزاً حتمياً · 3,841 صفّاً · **$0** · `data/training/` مُتجاهَل
- [x] كلُّ حقلٍ يُعلن مصدره · والرصيد يُقرن بالقيمة لا بالموقع (**21 لم تُوصل فأُعلنت**)
- [x] قناع الترويسة **مُقاس**: 0 حبر في الشريط المحجوب في 416/416 صورة

## أ-٩ · مخرَجُ التسليم نفسه  `⬜`
- [ ] جدولُ المقاييس في التقرير (رقمٌ لا سرد) + سردُ **الحدود** بنصّها («لا يشهد أن الورق صادرٌ عن المصرف»)
- [ ] فصلُ المنهجية: العقد · التلقينة · الشاهد · بوابة القبول — **وحدودُ كلٍّ منها**
- [ ] إثباتُ العامّية: تصميمٌ ثانٍ + نسبٌ من كشف التطبيق (لا ملفٌّ واحد)

---

# المسار (ب) — الهدف النهائي · منتجٌ يدفع له أحد

**تعريف «تمّ»**: **دفعةٌ مقدَّمة واحدة** من مَن ليس المالك + خليّةٌ واحدة مؤهَّلة (بنك × مدخل) + إدخالُ بنكٍ
جديد **≤ يومين** بلا تعديل النواة. وما دون الدفعة المقدَّمة **ليس إثباتاً** — ولا تُقرأ عشرُ محادثاتٍ كنجاح.

## ب-٠ · قراراتُ المالك الستة (من التقرير الاستراتيجي) — **كلُّها معلَّقة**
- [ ] **الفئة المستهدفة** (محامٍ · خبير حسابي · مدقّق · شركة عائلية — والصفحةُ `docs/WHO_IS_THIS_FOR.md` جاهزة)
- [ ] **الشكل**: خدمةٌ تؤدّيها، أم برمجيةٌ تُسلَّم
- [ ] **السعر** — والعائد مُقاس: السوق 0.13–0.19$/صفحة وكلفتنا ~0.014$ ⇒ **هامشٌ نحو سبعة أضعاف** · **والمنافس الحقيقي ساعاتٌ بشرية لا برنامج**
- [ ] **مسار الخصوصية**: سحابة (اليوم) أم محلي (المرحلة ٤) — **وهو مانع البيع الأول**
- [ ] **الترخيص**: `LICENSE` يقول «للعرض والمراجعة فقط» و`README` يقول «لم يُحدَّد بعد» ⇒ **تناقضٌ قائم** يُوحَّد قبل أي بيع
- [ ] **اسمُ المستودع** المرتبط ببنكٍ حقيقي

## ب-١ · إثباتُ الطلب — الـ٣٠/٦٠ يوماً (الوعاء: `docs/MARKET_EVIDENCE.md`)
> **الوعد**: ٣٠ يوماً **صفرُ ميزةٍ جديدة** (حتى 2026-10-17) · ٦٠ يوماً **١٠ محادثات** (حتى 2026-11-16) · **سقفُ 100$** كلفة سحابية.
> **القياس اليوم**: جدولُ المحادثات **فارغ** (م-٠١…م-١٠ بلا صفوف) ⇒ **صفرُ محادثةٍ مسجّلة**.

- [ ] ١٠ محادثات جادّة مسجّلة (لا محادثةَ بلا صف · والاقتباسُ الحرفي مطلوب)
- [ ] ٣ ملفاتٍ حقيقية مُسلَّمة (التسليم **فعلُ ثقة** والكلام مجاناً)
- [ ] ٣ أسعارٍ مذكورة **بلا تلقين**
- [ ] **١ دفعةٌ مقدَّمة — وهذا هو دليل الحكم**
- [ ] القياس ١: **الإدخال اليدوي لـ100 صفّ بساعة توقيت** ⇒ يُبنى عليه التسعير (0$ · ٣٠ دقيقة)
- [ ] القياس ٢: **30 صفحة من بنك ثانٍ** (`tools/scale_slice.py`) ⇒ ≥90% نظافة = منتج (~0.5$)
- [ ] القياس ٣: **نموذجٌ بصري ثانٍ** على الصفحات الذهبية ⇒ بديلٌ **مقيس** لا مذكور (~0.14$)
- [ ] القياس ٤: **معالجةٌ محلية على 5 صفحات** ⇒ إزالةُ مانع البيع الأول (0$)

**معايير التوقف (تُقرأ من العدّاد لا من الشعور):**
- [ ] منتجٌ برمجي: ٦٠ يوماً و١٠ محادثات بلا دفعةٍ مقدّمة ⇒ **يُوقف**
- [ ] السحابة: رفضُ اثنين لسبب الخصوصية **ولم يقبلا بديلاً محلياً** ⇒ يُوقف المسار السحابي
- [ ] تعدّد البنوك: بنكٌ ثانٍ < 90% ⇒ معايرةٌ لا بنية (وإلا يُوقف)
- [ ] المستهلك العام: **مُوقفٌ فوراً** — لا منافسةَ الأدوات الإنجليزية بكلفة نموذجٍ سحابي

## ب-٢ · مصفوفةُ التأهيل — لا تُفتح خليّةٌ للعملاء قبل تأهيلها
> **شرطُ التأهيل ستةٌ كلها لازمة**: كشفٌ مرجعيّ كامل · أرقامٌ ذهبية من الورق · الهوية تقفل ≤0.005 ·
> بوابة ALL PASS على ملفٍ مُسلَّم · اختباراتُ الحالات الشاذة · **إصدارٌ مُعلن** (تلقينة+نموذج+عتبات = بصمةُ الخليّة).

- [x] **راجحي × ممسوح**: 629 صفحة · بوابة **ALL PASS** · الهوية 0.00 · المجاميع مطابقة
- [ ] **راجحي × ممسوح — إغلاقُ شرط الحالات الشاذة**: ميل · حبر باهت · صفحة ناقصة · أرقام عربية-هندية وفارسية · صفحة بيضاء · وصفٌ يخالف الاتجاه ⇒ **الاختبارُ الحالي على عطبٍ مبرمج (9/9)، وورقُ بيد بشر لم يُوفَّر بعد** (`pg-281.jpg`)
- [x] **راجحي × تطبيق (رقمي)**: 441 صفّاً · 30 فحصاً ALL PASS · رصيدٌ جارٍ مؤكَّد · $0
- [ ] **الرقمي — الواجهة**: لا يُسلَّم لعميلٍ قبل تبويبٍ في `app.py`
- [ ] **بصمةُ الإصدار لكل خليّة** على ملفٍّ معلن (تغييرُ التلقينة/النموذج/العتبات ⇒ **إعادةُ تأهيل** بلا استثناء)

## ب-٣ · المنتج — رحلةُ العميل الثمانية
> **الوحدة**: كشفٌ واحد · لا حسابٌ يتراكم عندنا · **الذاكرة في ملف العميل** · **XLSX هو المنتج و PDF هو العرض**.

- [ ] ٠ المدخل — اختيارُ البنك (عقد) + نوع المدخل + **إعلانُ المسار وثمنه وزمنه**
- [ ] ١ الفحص الأولي — لكل صفحة مشكلة **ثمنُ قرارها** (أُعيد تصويرها / تخطَّها ⇒ فجوةٌ بمقدارها)
- [ ] ٢ القراءة الخلفية — مشرفٌ يستأنف من الكاش ⇒ **لا يُدفع ثمن صفحةٍ مرتين** · والانتظارُ على الشاشة صفر
- [ ] ٣ بطاقة السلامة — المجاميع مقابل ملخص البنك · الهوية تُقفل · **لا تسليمَ بلا ALL PASS**
- [ ] ٤ الاستفسار — **بالجملة لا بالصفّ** · قصاصةٌ بجانب كل سؤال · «لا أعرف» متاح · فصلُ إقرار القراءة عن إقرار الواقعة
- [ ] ٤·٥ لوح الهوية الحيّ — **لا قرارَ بلا ثمنٍ معروض**
- [ ] ٥ التشكيل — حزمٌ جاهزة (محكمة · تدقيق · مراجعة شخصية)
- [ ] ٦ الاعتماد — شاشةُ التوقيع (التوقيعُ بعد قراءة المحضر لا قبله)
- [ ] ٧ التسليم — كلُّ رقمٍ يحمل صنفه (`مطبوع` · `مُشتق` · `مُقَرّ` · `متنازع` · `غير مُثبت`) + **ملف الحالة**

## ب-٤ · الخصوصية والكلفة (يفتحهما النموذج المحلي)
> **البوابة (من خارطة النموذج)**: النموذج المُدرَّب **لا يحلّ محلّ العامّ** حتى يتفوّق عليه على **الحزمة المجمّدة** في: إغلاق السلسلة · مطابقة التذييل · **وعلى الصفحات الرديئة بالذات**.

- [ ] ٣ تصاميم + **≥ 15,000 عيّنة** مُتحقَّقة (لنا اليوم تصميمان؛ والعائد 5,789/تصميم ⇒ ٣ تصاميم = 17,367)
- [ ] نموذجُ أساس **يناسب العتاد**: M4/24GB ⇒ صنف الـ**0.9B** (PaddleOCR-VL · GLM-OCR · MinerU) **مرشّحٌ أول**، و8B بشرط استئجار GPU (بندٌ ماليُّ يُعلن قبل المرحلة)
- [ ] مقياسُ الاعتماد = **نسبةُ إغلاق السلسلة** على الحزمة المجمّدة — لا خسارةُ التدريب
- [ ] ⛔ لا نموذجَ لكل بنك · ولا تدريبَ قبل ٣ تصاميم · ولا وسمَ بشرياً في المجموعة

## ب-٥ · الالتزام النظامي (مانعُ بيعٍ حقيقي)
- [ ] **نظام حماية البيانات السعودي + ضوابط النقل خارج المملكة** — القراءة اليوم سحابية، ولا قائمةَ دولٍ «كافية» ⇒ كلُّ نقلٍ يحتاج بنوداً معتمدة (لا ملفَّ امتثالٍ في المستودع اليوم: `grep -rln 'حماية البيانات|سدايا' docs/` ⇒ التكليف وحده)
- [ ] توحيدُ `LICENSE` مع `README` (تناقضٌ قائم)
- [ ] **خطّةُ استمرارية** (Bus factor): `docs/RESUME.md` موجود — يُختبر باستئنافٍ فعليّ بعد أسبوع

## ب-٦ · التوسّع — بنوك ٢–٤ بالعقد وحده
- [ ] حزمةُ إدخال بنك: ١٠–٢٠ صفحة ⇒ مسبار قدرات ⇒ مسوّدة عقد ⇒ قياس ⇒ قبول/رفض بعتبة
- [ ] **إدخالُ بنكٍ ≤ يومين بلا تعديل نواة** (بوابةُ الانتقال)
- [ ] مصفوفةُ قدرات تُنشر مع كل بنك (تقول للعميل ما يُضمن ولِمَ)
- [ ] ⚠ السؤالُ الأول لكل بنك جديد: **هل يطبع رصيداً جارياً؟** (وإلا سقط الشاهد الأول)

---

# ما هو مفتوحٌ الآن (ليس خارطة — عملٌ جارٍ بتاريخه)

| # | البند | بيد من | حالته المقيسة |
|---|---|---|---|
| ١ | **PR #119** (البند ٤ — حزمة التقييم): مراجعات ٤٦→٥٢ أُغلقت · ٤ حاجبات + ٣ مقاعدَ داخليّة + ٢٥ ضابطًا | سليمان | **يُقاس لا يُكتب**: `pytest -q` (والعددُ المحروسُ في `docs/QA_CHECKLIST.md` + `docs/claims.json`) · `gh pr view 119 --json state` — **مُغلق: مدموجٌ بـ`--rebase-merges` بكلمة المالك** (وما بعده يُقاس لا يُكتب) |
| ١·١ | عرضٌ مدفوع ٢ (~$0.1) ثم ١ (~$0.5) | المالك | ضابطُ التوقّف وُجد ✓ · ويبقى **حدُّ المفتاح عند المزوّد** إجراءً بيده |
| ١·٢ | ثقبُ سطح أدوات السلسلة: لا أداةَ تُسرَد صفوفَ صفحة ⇒ عائلةُ `argmax_row` غيرُ قابلةٍ للإجابة | سليمان | مُعلَنٌ ومفتوح (P-5) — **وأثره: صدقُ الاستشهاد شبهُ غيرِ مقيس** |
| ١·٣ | صفحة ٦٢٩: ١٧ صفًّا بلا رصيدٍ مطبوع ⇒ `abs-02` وحدها تمسّها | المالك | إعادةُ صياغتها **تُسقط شهادةَ الـ٢٠٠ سجل** ⇒ قرارُه |
| ١·٤ | **إبطالُ رمز Claude** المتسرّب | المالك | الإبطالُ حسابيٌّ (`claude auth status` = `loggedIn:false`) ⇒ نقرتُه |
| ٢ | **طلبُ GitHub Support** لإزالة `refs/pull/*` | **المالك** | تذكرة `4785819` — وحُذفت `101..108` (**HTTP 404 ×٨** ✓) لكن **الكائناتُ لا تزال قابلةً للسحب**: القياس ٢٠٢٦-٠٩-٢٤ ⇒ `STILL EXPOSED` (**١١٣** مرجعاً · **٦٥٢** التزاماً بالمرآة · **١٢٣** شكلاً من مبالغَ حقيقيّة داخل ١٥٦٤ blobاً · `cached view` يردّ **200**×٢) ⇒ **النصُّ الجاهز لإعادة الفتح في `docs/security/GITHUB_SUPPORT_REQUEST.md`** |
| ٣ | **إعادةُ استنساخٍ إلزاميةٌ لكل من له نسخة** (التاريخ أُعيد كتابته) | المالك | `git pull` سيفشل **بالعمد** — يلزم `clone` جديد |
| ٤ | المخزونُ المؤجَّل (٦ بنود: توحيد `publish_guard` · ترقيم البروتوكول · أوضاع `argparse` · صيغة `check_review_report` · أربعُ إخفاقاتٍ في نسخةٍ نظيفة · طلبُ GitHub) | سليمان | مُعلَنٌ بمالكه في `handoff/sulaiman/20260923-120000-…` |
| ٥ | تحريرُ ١٣٣ صفحة (32.6٪ من صفوف التدريب) | المالك | **$3.36** — لا تُصرَف بلا سؤالك |
| ٦ | شهادةُ مصدر الورق (`Ref. No` عنوانُ جهةٍ تُسأل لا شهادةٌ منها) | المالك | لا شاهدَ في المنظومة يمسّ أصل الورق |
| ٧ | ورقٌ معطوبٌ بيد بشر (`pg-281.jpg`) | المالك | الهارنسُ مُقاس (9/9 على عطبٍ مبرمج) |

---

# المستبعدات (مصرَّحٌ بها كي لا تُعاد)

| ⛔ | لماذا |
|---|---|
| `REAL` لأي مبلغ | يهدم الأساس — `TEXT` + `Decimal` دائماً |
| إطارٌ ثانٍ | LangChain وحده، **وفي الاستدلال وحده** |
| تدريبٌ قبل ٣ تصاميم و١٥٬٠٠٠ عيّنة | يحفظ الشكل لا المهمّة |
| نموذجٌ لكل بنك | بيضةٌ ودجاجة · لا يتوسّع · صيانةٌ أبدية |
| وسمٌ بشري في مجموعة التدريب | ميزتُنا أنّ وسمنا **رياضيّ** |
| حذفُ الشهود بعد النموذج | النموذجُ قارئٌ آخر — **والسلسلة تبقى الحَكَم** |
| وعدُ بنكٍ قبل مسبار القدرات | قد لا يطبع رصيداً جارياً |
| بياناتٌ حقيقية في git | `publish_guard` + `amount_guard` حارساها — لا تُضعَفا |
| بيعُ لمحامٍ/مدقّق قبل مسار الخصوصية | يمنعه النظامُ قبل أن يمنعه عيبٌ برمجي |
| المستهلكُ العام | لا منافسةَ على أسعار الأدوات الإنجليزية |

---

> **ما لم يُثبت في هذه الوثيقة:** أن الحالات المعلَّمة `✅` أُعيد قياسُها اليوم — بعضُها مُقيَّدٌ بشهادة رسالةٍ في
> `handoff/` لا بأمرٍ شغّلته الآن · وأن تقديرات الأيام (٨–١٠ · ٣–٤ · ٢–٣) **تقديرُ كلود** لا قياس · وأن قائمة
> المقاييس الثلاثة هي ما تشترطه اللجنة **بالاسم** — من وصف المشروع لا منّا · وأن `main` بلا بنودٍ مفتوحةٍ
> غير مسجَّلة هنا — المُسجَّلُ ما وجدتُه في `STATE.md` ورسائل `handoff/`.
