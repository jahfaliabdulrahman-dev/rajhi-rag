# PLAN — rajhi-rag · خطة إغلاق فجوات ورشة «ماذا لو» №1

> **الحالة (2026-09-14):** المشروع على **بوابة النشر** (لم تبدأ). عُقدت ورشة «ماذا لو» الأولى
> (`docs/what-if/01-workshop-rajhi-rag.ar.md`) وحدّدت فجوات P1→P3 + دلتا جديدة. هذه الخطة تُغلقها كلها.
> **حالة المشروع المُتحقق منها** في `~/.hermes/skills/ai-study-mentoring/references/rajhi-rag-state.md`
> (المصدر الحيّ؛ هذا الملف يُعاد كتابته لا يُلحق عليه).

## خريطة الفجوات (من الورشة) → المخرجات

| # | الفجوة (من التقرير) | الأولوية | المخرج المُنفَّذ | بوابة النجاح |
|---|---------------------|----------|------------------|--------------|
| 1 | **footer oracle** — الحارس الوحيد لمفارقة الدقة (انزياح ×100 يمرّ «نظيف السلسلة») | P1 | `src/statement_qa/footer_oracle.py` + دمج في خط القراءة + قفل في البوابة | 10/10 صفحات مطابقة في التشغيل الكامل + اختبارات تصادم ×100 |
| 2 | **حزمة النشر الآمن G3** — سكانر pre-push + حاجز CI + ممنوعات الرفع + فحص تاريخ الريبو | P1 | `tools/publish_guard.py` + `.githooks/pre-push` + `.github/workflows/` + تعقيم المسارات + فحص تاريخي مُنفَّذ | صفر BLOCK على الشجرة والتاريخ |
| 3 | **سلايس توسيع 30–50 صفحة** بقياس ثلاثي (نظافة/زمن/كلفة) + سياسة توقف | P2 | `tools/scale_slice.py` + تشغيل حقيقي 30 صفحة | تقرير `slice_report.md` بأرقام فعلية |
| 4 | **عقد G2** — إيقاع أسبوعي للـsuspects + سياسة تجميد الـprompts | P2 | `docs/OPERATIONS.md` | العقد مكتوب ومربوط بالبوابة |
| 5 | **صحة الخدمة + مراقبة خفيفة + رسالة عطل واضحة** | P3 | `tools/health_check.py` + `tools/health_monitor.py` + وظيفة launchd | فحص حي 4/4 + سجل مراقبة يتجدد |
| 6 | **ملف الاستئناف** (Bus factor) | P3 | `docs/RESUME.md` | استئناف في 10 دقائق بلا إعادة اكتشاف |
| 7 | **فحص ترتيب الصفحات/الانعكاس** (جديد) | دلتا | `src/statement_qa/ordering.py` (تواريخ + التحامات السلسلة) | كاشف انعكاس مُختبَر |
| 8 | **era detector** — بصمة صيغة لكل صفحة (جديد) | دلتا | `src/statement_qa/era.py` + التقاط الرموز الخام في القارئ | بصمة لكل صفحة + إنذار تحوّل |
| 9 | **قفل job واحد** (جديد) | دلتا | `src/statement_qa/job_lock.py` + دمج في التطبيق | رفض تشغيل ثانٍ برسالة واضحة |
| 10 | **رسالة «بنك غير مدعوم بعد»** (جديد) | دلتا | `src/statement_qa/bank_check.py` + رسالة عربية في الواجهة | غير الراجحي → رسالة مفهومة لا فشل غامض |
| 11 | **جسر استرجاع «آخر رصيد»** | دلتا | `qa.py`/`app.py` boost لصفحة الختام | جواب «آخر رصيد» يستشهد الصفحة الأخيرة |
| 12 | **حدود النسخة العامة في About** + لغة ادعاء نطاق صريحة | دلتا | تحديث `ABOUT` + تنبيه الرفع | نص صريح: عينة/بنك واحد/خصوصية |

## قواعد ثابتة (لا تُخترق أثناء التنفيذ)
- الخصوصية: كشوف حقيقية لا تُرفع؛ الـfixture العام اصطناعي؛ لا أسماء/أرقام حقيقية في ملفات مُتتبَّعة.
- القارئ المجمّد: لا نعيد كتابة `FRONTIER_PROMPT` ولا `norm_num` — الاستئناس فقط.
- كل قيمة «مقفلة» في البوابة مقارنة عينياً بالرندر أولاً؛ والبوابة خضراء قبل أي «تم».
- لا `launchctl kickstart` للخدمة أثناء احتمال استخدام المالك (تقتل مهمة جارية).

## المراحل (كل مرحلة: تنفيذ → اختبارات → بوابة → commit)

- [x] **A. footer oracle** — قراءة إطار الصفحة السفلي (مدين/دائن/رصيد **تراكمياً**) + مقارنة حتمية + تصادم ×100 + دمج + قفل بوابة + اختبارات. **الدليل:** `qa_gate --full` 5/5 مع «تحقق الفوتر: 10/10 مطابق».
- [x] **B. حزمة النشر الآمن** — guard + hook + CI + تعقيم (plists→قوالب، fixtures بلا أسماء/أرقام حقيقية) + **تنظيف تاريخ الريبو** (filter-repo) + قائمة ممنوعات. **الدليل:** `publish_guard --tree --history` = 0 BLOCK/0 WARN، وفحص مستقل عبر كل المراجعات = 0.
- [x] **C. الدلتا الأمنية** — era detector + ordering + job lock + bank check + جسر «آخر رصيد» + نصوص About/الادعاء. مدمجة في `app.py` ومقفلة باختبارات.
- [~] **D. سلايس 30 صفحة** — أداة `scale_slice.py` جاهزة وتشغيل حي جارٍ (checkpoint لكل صفحة + حواجز توقف).
- [x] **E. التشغيل والاستئناف** — health_check + monitor (launchd، كل 10 د) + `docs/RESUME.md` + `docs/OPERATIONS.md` (G2/G3) + سجل الشكوك.
- [ ] **F. الإغلاق** — بوابة `--full` خضراء · تحديث state ref · commit · تقرير المالك.

**دليل الإغلاق النهائي:** تشغيل `python tools/qa_gate.py --full` أخضر (يشمل 10/10 footer)
+ `python tools/publish_guard.py --tree --history` بلا BLOCK + تقرير السلايس بالأرقام.
