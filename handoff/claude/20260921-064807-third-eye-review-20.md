```
id:      20260921-064807-claude
from:    claude
to:      sulaiman
type:    REVIEW
step:    فحص 4a2c819→f713add (v20) — الأربعةُ كلُّها مُنفَّذة ومُتحقَّقٌ منها بأسمامي · **وشُبِكت المسحةُ الحقيقية في شجرتي فشغّلتُ البوابة ٥ والكوربوس لأول مرة** · وظهر خطأٌ أصلُه **أنا**
commit:  f713addd0040dde2cb8fc3ce6dc625064c8776d0
verdict: APPROVE — كلُّ ما ادّعته v20 تحقّقتُ منه بنفسي. ومعه **بندٌ جديد أُصحّح فيه خطئي أنا** (§٤) لا يخصّ هذا التسليم.
```

> **أوّلُ APPROVE أكتبه في هذه السلسلة.** ومعناه ما في العقد بالضبط: «الادّعاءاتُ
> مُتحقَّقٌ منها والبوابات خضراء» — لا «ممتاز». وبندُ §٤ **ليس مأخذاً على v20**.

---

## ١) ✅ القاعدةُ رُجِّعت — وقِستُها بسمّي أنا لا بقراءة الفرق

| الصيغة (`N` = رقم) | 15 رقماً | 18 رقماً |
|---|---|---|
| ملتصق · بشُرَط · بنقطة · رُباعياتٌ بفراغ | محجوب ✓ | محجوب ✓ |
| **ثلاثاتٌ بفراغ `NNN NNN NNN …`** | **محجوب ✓** | **محجوب ✓** |
| **ثلاثاتٌ بجدولة `\t`** | **محجوب ✓** | **محجوب ✓** |

**الثقبُ مُغلق في كل صيغةٍ جرّبتُها** (وبأرقامٍ تحقّقتُ أولاً أنها ليست سُلّماً).

**والاستثناءُ صار في محلّه:** سطرٌ واحد، **مسارٌ بعينه** (لا نجمة)، وبسببٍ مكتوب.
وهو أضيقُ أثراً من قاعدةٍ تُرخي الشجرة كلها — كما قلتَ.

## ٢) ✅ والسمّ الغائب — يمنع الرجوع فعلاً

أعدتُ حقنَ الاستثناء المُزال بحرفه في `_digit_variants`، فسقط **بالاسم**:

```
FAILED  test_a_three_wide_space_grouped_account_is_blocked
FAILED  test_a_page_number_list_is_exempted_by_a_written_allowlist_entry
2 failed, 23 passed
```

⇒ **الرجوعُ ممنوعٌ بفحصٍ لا بعزم.** وهذا هو الفرق الذي كان ناقصاً في v19.

## ٣) ✅ والتأكيدُ الطائوي — صار يعضّ

لم أُصفّر الرقم (سمٌّ خشن) بل **نصّفتُه** — سمٌّ لا يُميَّز إلا بحسابٍ حقيقي:

```
"spend_on_pages_outside_the_count_usd": round(error_spend * 0.5, 4)
⇒ FAILED test_the_spend_on_pages_outside_the_count_is_measured   (1 failed, 4 passed)
```

⇒ المشتقُّ داخل الاختبار **يقيس فعلاً**، ولا يصدُق بذاته. ✓

## ٤) ⛔ بندٌ جديد — **وأصلُه خطأٌ مني، أُصحّحه قبل أن يُبنى عليه**

شُبِكت `data/local_sample/` في شجرتي أثناء هذه الجولة (رابطان إلى مسحة المالك
الحقيقية، 06:45). فصرتُ أرى ما لم أكن أراه، **وأوّلُ ما كشفه: دعوى كتبتُها أنا.**

في حكمي على v18 (§٥) قلت: «المرجعُ كتبته **v2 نفسها** ⇒ تفوّقُها حدٌّ أعلى». فأخذتَها
وكتبتَها في ملفّ الحكم المدفوع. **والتشغيلةُ نفسها تقول غيرَ ذلك:**

```
corpus_provenance.reader          = None
legacy_unstamped_pages            = 629
declaration  = «غير مختم — قُرئ قبل تسجيل الهوية»
note         = «لا يُعرف من الكاش أي تلقينةٍ أنتجتها — والتلقينتان v1/v2
                كانتا تُقاسان في هذه الفترة»
أختامُ الكاش (629 نقطة): model=None · prompt_version=None  →  629 صفحة
وذراعا FM-2 كلاهما: run = data/local_sample/slice_629p   ← هذا الكوربوس بعينه
```

⇒ **نسبُ المرجع مجهول، لا «v2».** والملفُّ المدفوع يقول بـ`⚠️` إنه معلوم.

**وأدقُّ ما في الأمر أنه يُثبت درسَك مرّتين:**
1. `reference_bias` **نصٌّ مكتوبٌ بيد داخل الكاتب** (سطر 109)، و`grep` على
   `corpus_provenance|reader_stamp|legacy_unstamped` في `fm2_verdict.py` ⇒ **صفر**.
   فالجملةُ الوحيدةُ غيرُ المشتقّة داخل ملفٍّ مشتقّ هي **الجملةُ الخاطئة**.
2. وهي من صنف `6fca869` نفسه: **إعلانٌ يُفحص بنفسه لا بمصدره.**

**والتصحيحُ ليس عكسَ الدعوى بل إسقاطُها:** لا يجوز أن يُقال «التحيّزُ لصالح v2»
ولا «المقياسُ محايد» — **الاتجاهُ مجهول**، لأن المرجع قراءةٌ سابقةٌ **بتلقينةٍ غير
معروفة** وكلتاهما كانت تُقاس حينها. وما يبقى صحيحاً بلا تحفّظ: **الذراعان طابقا
المرجعَ على 3721 زوجاً سواءً** — وهذه لا تتأثر بجهالة النسب.

**الطلب:** أن يُشتقّ `reference_bias` من `corpus_provenance` في التشغيلة (فيقول
«مجهول» حين تكون `reader = None`)، لا أن يُكتب نصّاً. وسمُّه: اجعل النسبَ معلوماً
في عيّنةٍ ⇒ يجب أن تتغيّر الجملة.

## ٥) 🆕 وشغّلتُ ما لم أستطعه في ستّ جولات: البوابة ٥ والكوربوس الحقيقي

| العقد يوثّق | المقيس عندي الآن |
|---|---|
| 5770/5793 نظيف | **5770 / 5793 · النسبة 0.996** ✓ |
| فوتر 621 ok | **ok=621 · mismatch=0 · absent=4 · gap=2** ✓ |
| 0 منزاح | **`backwards=[]` · `duplicates={}`** ✓ |
| الفجوتان 427/626 | **[426→427 · الغائبتان 426,427] · [625→626 · الغائبتان 625,626]** ✓ |

**وبنيتُ التصدير وشغّلتُ البوابة على الكوربوس الحقيقي (المخرج في مجلدٍ مؤقّت خارج
المستودع، ولم أطبع منه بياناً):**

```
to_xlsx : صفوف 5809 · صفحات 629 · بلا إثبات 10 · مرفوضة من البوابة 0
verify_close --profile profiles/al-rajhi.json  →  RESULT: ALL PASS ✓   (exit 0)
```

⇒ **«كوربوس المسح ALL PASS» لم تعد نصّاً أقرؤه منك — قِستُها.** وكذلك
`footer_role = cumulative_printed_totals` في التشغيلة ⇒ استدراكُ v17 نُفِّذ فعلاً
على الورق الحقيقي، فلا تُرَدُّ التشغيلةُ القديمة كما خشيت.

## ٦) ✅ وفرقُ البيئة الذي أعلنتَ أنك **لم تقس سببه** — قِستُه لك

```
SKIPPED tests/test_page_gate.py:190   المسحة الحقيقية غير موجودة على هذا الجهاز
SKIPPED tests/test_page_gate.py:201   (نفسه)
SKIPPED tests/test_text_reader.py:74  الكشف الرقمي محليّ لا يدخل المستودع
```

فالثلاثةُ مشروطةٌ بمسحة المالك والكشف الرقمي. **وبعد أن شُبِكا في شجرتي: 331 نجحت ·
0 تخطّى** — مطابقٌ لبيئتك تماماً. ⇒ **الفرقُ حدُّ المدقّق لا عطبٌ**، والبندُ يُغلق.
و`claims.json` = **331** ✓ و`QA_CHECKLIST` صارت تقولها بدقّة ✓.

## ٧) البوابات وبوابة ٦

| | |
|---|---|
| `pytest tests/ -q` | **331 نجحت · 0 تخطّى** · exit 0 ✓ |
| سمُّ رجوع القاعدة | يسقط بالاسم ✓ |
| سمُّ تنصيف الصرف | يسقط بالاسم ✓ |
| `publish_guard` · `static_gate` | آمن للدفع · PASS ✓ |
| **البوابة ٥ (كوربوس معتمد)** | **مطابقةٌ تامة** ✓ |
| **الكوربوس الحقيقي عبر البوابة** | **ALL PASS ✓ · exit 0** |
| **بوابة ٦ (الملفات)** | **8 مُعلنة = 8 فعلية** ✓ — وقد صحّحتَ ما أخذتُه عليك |

**لم أتحقق من:** `qa_gate --full` (~$0.20) — لم تُشغَّل: v20 لا يلمس مسار القراءة الحيّ
(حارس + اختبارات + وثائق) · والذراعان لا يُعاد تشغيلهما (كلفة) ⇒ أرقامُ FM-2 تبقى
**اشتقاقاً من ملفَّيك**. **وحدٌّ جديد عليّ:** شجرتي صارت تحمل بياناتِ عميلٍ حقيقية
⇒ لا أنقل منها رقماً ولا اسماً إلى أي رسالة، ومخرجاتي منها خارج المستودع.

## ٨) next

1. **§٤ (البند الوحيد)**: `reference_bias` يُشتقّ من `corpus_provenance` — ويقول
   **«النسبُ مجهول»** حين `reader = None`، مع سمّه. **والخطأُ الأصلي خطئي، والتصحيح
   عليّ أُعلنه كما أعلنتُ دعواي.**
2. وما عدا ذلك: **v20 مقبولةٌ كاملة** — الأربعةُ مُنفَّذة، وكلُّ واحدةٍ منها سقطت تحت سمٍّ
   صوّبتُه عليها بنفسي.
