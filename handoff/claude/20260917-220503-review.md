id:      20260917-220503-claude
from:    claude
to:      sulaiman
type:    REVIEW
step:    البوابة 5 — تنفيذ حكم `20260917-214008-review.md` على `4a2e9ff · 1169de2 · 545c217 · a466f52`
commit:  a466f52   (الشكل الكامل يُشعل `long_digits` في حارسنا — انظر ◽٤-٤)
verdict: REQUEST_CHANGES
evidence:
  - معزول: `git worktree add .claude/worktrees/review-gate5 -b claude/review-gate5 origin/main` ثم
    `git pull --rebase origin main` → `HEAD = a466f52` مطابق لـ`origin/main`. لم أقترب من نسخة المنفّذ.
  - **البوابة ١** `pytest tests/ -q` → `172 passed in 1.73s` ✓
  - **البوابة ١ في شرط CI** (بلا `data/local_sample`) → `172 passed in 0.79s` — **صفر متخطّى** ✓✓
    (كان `168 passed, 1 skipped`؛ هذا أهمّ إصلاح في الدفعة وقد تحقّقتُ منه في الشرط نفسه لا بالادعاء.)
  - **البوابة ٣** `python3 tools/publish_guard.py --tree --history` →
    `tree: 101 · history: 535 · messages: 67 — 0 BLOCK / 1 WARN` ✓ (الـWARN هو المُعلن)
  - **بوابة 0 ما زالت مُثبتة:** 535 كائناً + رسائل الالتزام والمؤلفين → **0 ظهور** للسلسلة المسرَّبة ✓
  - **البوابة ٦** `git diff --stat 8f89e39..a466f52` → 13 ملفاً · 153+/21− · وفحصتُ كل التزام على حدة
  - بوابة النطاق: 20 سؤالاً (6 من حكمي · 7 كلمات ملتبسة · 7 رفض يجب أن يبقى) + 6 هجمات جديدة
  - الحارس: 9 صيغ تجزئة رقمية · واختبار نطاق الاستثناء على ثلاثة مسارات
  - `tools/group_verify.py --results …` ومقارنة مخرجه بـ`group_verification.json` الذي كتبه في نفس التشغيل
  - **الكلفة السحابية: $0.00** — لم أشغّل البوابة ٢ ولا `refusal_test`: لا بند أدناه يتوقّف عليهما،
    والتغييرات في `scope.py` و`publish_guard.py` و`render_claims.py` لا تمسّ مسار القراءة.

findings:

## ✅ ما أُغلق فعلاً — وتحقّقتُ منه لا صدّقته

**المانع ١ (بوابة النطاق) مُغلق تماماً.** الجمل الستّ التي أعطيتُها كلها `in_scope` الآن:

```
in_scope  | ما رصيد الحساب؟          in_scope  | ما أعلى رصيد في الحساب؟
in_scope  | كم مجموع مسحوبات الحساب؟  in_scope  | اعرض حركات الحساب في 2019
in_scope  | متى فُتح الحساب؟          in_scope  | كم عدد حركات هذا الحساب؟
```

والكلمات الملتبسة انحلّت: الرياض (مدينة) · الخليج (مطعم) · الجزيرة (طيران) · الاستثمار (رسوم) ·
الجوال (فاتورة) — كلها `in_scope`. **وبلا انحدار:** الرفض ما زال يقع على «بنك الرياض» و«الإنماء»
و«بنك ساب» و«البنك الأهلي» و«رقم جوال» و«رقم الهوية» و«كلمة المرور». هذا عمل نظيف.

**وأُغلق أيضاً:** ABOUT (نصّه الجديد **أدقّ** ممّا طلبتُ — يسمّي التوزيع المقيس بدل نسبة قديمة) ·
لقطة الأرقام تعمل في CI عبر `docs/claims.json` · سجلات التحكيم الثلاثة صارت `old ≠ new`.

**وأُصحّح نفسي في بند:** قلتُ إن الكاتب يسجّل `old_value == new_value`. قرأتُ
`tools/arbitrate_footer.py:75-91`: `old_value = str(stored)` صحيح لأي تحكيم **جديد**، وفيه حارس
صريح للحالة (ب). العيب كان **أثر هجرة** لا عيب كاتب — وقرارك بإبقاء الأداة خلافاً لتوصيتي مبرَّر
ومكتوب. الملاحظة الصحيحة الباقية منه صغيرة: `raw_parsed` في ص616 ما زال `626906.4` بمنزلة واحدة.

---

## ⛔ ١ — قاعدة `joined_digits` لا ترى الصيغة التي يستعملها الملف نفسه، والملف مُستثنى منها فوق ذلك

طلبتُ `ast` على الرموز؛ نُفِّذ تعبير منتظم ثالث على السطر. النتيجة — تسع صيغ تجزئة:

```
BLOCK:concat_digits   | "A" + "B"
BLOCK:joined_digits   | "".join(("A","B"))          ← أُغلقت ✓
BLOCK:joined_digits   | "".join(["A","B"])          ← أُغلقت ✓
BLOCK:long_digits     | f-string بالأجزاء            ← (كانت مُغلقة أصلاً)
PASSES                | _PARTS = ("A", "B")         ← ⛔ **الصيغة التي يستعملها المستودع اليوم**
PASSES                | _P = ["A","B"] ثم "".join(_P)
PASSES                | "A" "B"                     ← ضمّ ضمني: بايثون يدمجه وقت التحليل
PASSES                | "-".join(("A","B"))
```

الأجزاء على سطر والـ`join` على سطر آخر ⇒ لا سطر يحمل الزوج، فالقاعدة السطرية لا تراه.
**وهذا هو شكل `tests/test_publish_guard.py` بالضبط** (`_PARTS = (...)` ثم `"".join(_PARTS)`).

**والأخطر: الاستثناء.** `.publish-allowlist` كسب سطراً:
`joined_digits :: tests/test_publish_guard.py :: قيم اختبار الحارس الاصطناعية (نسخ تاريخية)`

و`_allowed()` يطابق **المسار** فقط — لا يفرّق شجرةً من تاريخ:

```
PASSES (allowlisted) | حمولة joined_digits في tests/test_publish_guard.py
BLOCK:joined_digits  | نفسها في tests/other_test.py
BLOCK:joined_digits  | نفسها في docs/note.md
```

⇒ الملف الذي وقعت فيه الحادثة صار يحمل **استثناءين مستقلين** من القاعدة التي وُلدت منه: صيغته
تتخطّاها، والاستثناء يُعطّلها عليه في الشجرة أيضاً لا في التاريخ وحده. والسبب المكتوب يخصّ
**النسخ التاريخية** — فالاستثناء أوسع من مبرّره.

**لا بيانات مسرَّبة اليوم**: القيم اصطناعية، و535 كائناً + الرسائل = 0 ظهور. البند عن **الدفاع** لا
عن **الأثر**: من يستبدل `_PARTS` بقيم حقيقية غداً يمرّ كما مرّ في `dc0e862`.

**المطلوب (أحد الاثنين، لا كلاهما):**
- `ast.parse` على كل `.py` مُتتبَّع: اطوِ كل تعبير ثابت (`+` · `join` على ثوابت · ضمّ ضمني · f-string
  على ثوابت) واطبّق `long_digits` على الناتج. يُنهي العائلة كلها بقاعدة واحدة بدل ثلاث؛ أو
- إن بقي الاستثناء: **ضيّقه إلى التاريخ** (`joined_digits :: history:tests/test_publish_guard.py`)
  واجعل الشجرة مفحوصة — أي حقل يُبنى من أجزاء في هذا الملف يُولَّد من
  `make_synthetic_statement.py` بدل أن يُكتب كثوابت.

**الاختبار الفاصل:** ضع `_PARTS = ("<عشرة أرقام>", ...)` في `tests/test_publish_guard.py` وشغّل
`publish_guard --tree`. يجب أن يخرج BLOCK. اليوم يخرج PASS.

## ⚠ ٢ — سطر حكم `group_verify` ما زال يناقض السطرين اللذين طبعهما قبله بثانية

أُصلح نصف البند: الأسطر التفصيلية صارت تطبع النقص المقيس ✓. وسطر الحكم لم يُمسّ:

```
✗ نقص مقيس: 427 — مدين -1790.45 · دائن -9218.90 (رصيد مطابق: True)
✗ نقص مقيس: 626 — مدين -8092.05 · دائن -1343.16 (رصيد مطابق: True)
الحكم: 3 صفحة … صارت موثّقة بالمجموع؛ **0 نطاق بنقص مقيس**؛ 1 نطاق غير محسوم
```

و`group_verification.json` من نفس التشغيل: `{'verified': 622, 'mismatch': 2, 'undecidable': 1}`.

والعدّ انقلب من تضخيم إلى تقليل: كان «6 صفحة» (يضمّ ص173 وحكمها `ok` أصلاً)، وصار **«3 صفحة»**
وهو عدد **النطاقات** لا الصفحات. الصفحات التي لم تكن `ok` وصارت موثّقة **خمس**: 172 · 484 · 485 ·
602 · 603 — وهو الرقم الذي كتبتَه أنت يدوياً في رسالة `6f56405`. الأداة أخطأته مرتين في اتجاهين.

**المطلوب:** سطر الحكم يُشتق من نفس القاموس المكتوب في JSON — `sum(1 for s in groups if s['status']
== 'mismatch')` و`len({p for s in spans_verified for p in s['pages'] if was_not_ok(p)})` — لا من
عدّادات موازية. هذا هو نفس شكل العيب الذي أغلقتَه في `slice_report.md`؛ ظهوره في أداة جديدة يعني
أن الدرس لم يصر قاعدة.

## ⚠ ٣ — التسليم بلا رسالة، وSTATE يحمل نصّي أنا

`a466f52` غيّر **`handoff/STATE.md` وحده** (سطران). لا رسالة في `handoff/sulaiman/` لهذه الدفعة:
لا ترويسة، ولا `evidence`، ولا قائمة ملفات، ولا sha مُعلن. العقد §1 و§2 و§5-٤ تجعل ذلك لازماً،
والبوابة ٦ تُقاس عليه. بنيتُ قائمة الملفات بنفسي من `git diff --name-only` — وهذا عملك لا عملي.

وفي `STATE.md`: `step` محدَّث، لكن `next` ما زال نصّي («sulaiman يعالج بندَي المنع…») و`updated`
ما زال `21:40` — أي وقتي أنا. من يقرأ الملف اليوم يرى دوراً انتهى وحالةً تقول إنه لم يبدأ.

## ◽ ٤ — بقايا صغيرة (لا تمنع، تُذكر لتُقيَّد)

1. **`حوّلت من حسابي إلى بنك الرياض كم مرة؟`** و**`كم حوّلت لبنك الجزيرة؟`** ما زالتا `out_of_scope`.
   وهما سؤالان **داخل الوثيقة**: التحويل الصادر إلى بنك آخر صفٌّ في كشف الراجحي. الإشارة الفاصلة
   ليست اسم البنك بل **الفعل**: «رصيدي/حسابي لدى X» خارج · «حوّلت إلى X / دفعت لـX» داخل.
   أضيق كثيراً من البند الأصلي — لذلك ملاحظة لا مانع.
2. **`كم رصيدي في الراجحي وفي بنك الرياض؟`** → `in_scope` (يغلب «الراجحي»). السؤال المركّب يمرّ
   كاملاً وفيه نصف لا جواب له. الجواب الصحيح: أجب الشقّ الداخلي **وقل صراحةً** إن الشقّ الآخر خارج الكشف.
3. **`raw_parsed` في ص616** = `626906.4` بمنزلة واحدة (`quantize` لم يُطبَّق على السجل المُهاجَر).
4. **`long_digits` يُشعلها هاش git.** حاولتُ كتابة الـsha الكامل لهذا التسليم في ترويسة الرسالة
   فأعطى الحارس `BLOCK` — لأن الهاش السداسي عشري يحوي شوطاً من عشرة أرقام. وقد أصابك هذا قبلي
   (امتنعتَ عن لصق مُعرّف تشغيل CI للسبب نفسه). إنذار كاذب على نصّ لا يحمل بياناً شخصياً، وعلاجه من
   عائلة `_is_digit_table`: استثنِ شوطاً محاطاً بسياق سداسي عشري (`[0-9a-f]{7,40}`) أو مسبوقاً بـ
   `commit:`/`sha`. وإلا فالحارس يعلّم الطرفين تجنُّب كتابة الأدلة — وهو عكس غرضه.

---

## الحكم

**REQUEST_CHANGES على بند واحد مانع (١) وبندين يُصلحان في نصف ساعة (٢، ٣).**

وأقولها بوضوح حتى لا يُقرأ الحكم أثقل ممّا هو: **هذه أنظف دفعة حتى الآن.** المانع الأول من الجولة
السابقة — بوابة تُنكر «ما رصيد الحساب؟» — مُغلق بالبرهان وبلا انحدار، و172 اختباراً تعمل **في شرط
CI بلا تخطٍّ واحد**، والحارس 0 BLOCK، وبوابة 0 ما زالت مُثبتة على 535 كائناً، وABOUT صار أصدق
ممّا طلبتُ، وصحّحتَ لي بنداً كنتُ مخطئاً فيه بحجّة مكتوبة.

المانع الباقي واحد ومحدَّد: **قاعدة خصوصية وُلدت من حادثة، ولا تحمي اليوم الملف الذي وقعت فيه** —
لا بصيغته ولا بعد استثناءٍ مبرَّرُه أضيق من نطاقه. لا أثر اليوم، والدفاع نظري. وهذا بالضبط ما كان
عليه الحال قبل `dc0e862`.

next:    (١) `ast` على الثوابت — أو تضييق الاستثناء إلى التاريخ وتوليد قيم الاختبار.
         الاختبار الفاصل مكتوب أعلاه ويُشغَّل محلياً بلا كلفة.
         (٢) سطر حكم `group_verify` يُشتق من JSON. (٣) رسالة تسليم في `handoff/sulaiman/` + `next`
         و`updated` في STATE. لا تُشغّل البوابة ٢ لأجل هذه الجولة: لا شيء منها يمسّ مسار القراءة.
         سأراجع على الـsha التالي.
