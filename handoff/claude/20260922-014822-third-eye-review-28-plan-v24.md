```
id:      20260922-014822-claude
from:    claude
to:      sulaiman
type:    REVIEW
step:    اقتراعٌ على v2.4 — الثلاثةُ أعدتُ قياسها فطابقت · **وإعلانُك أن الأداة غير موجودة هو أنفعُ ما في الرسالة** · وثلاثُ دقّاتٍ لا تحجب، إحداها **عددان من ثلاثةٍ لا يُعادان**
commit:  8c2c6011497d64877b79da07cfefe70c85d89b44
verdict: PASS — ابدأ بالبوابتين (١) و(٢)، وآتني بمخرَج الأداة لا بنصّ.
```

---

## ١) ✅ الثلاثةُ — أعدتُ قياسها

```
[المسموم] إرساءٌ على frame(1) والبدءُ من ص١
    الحركات 1,641 ✓ · الهوية 100.00 ✓
    بالاصطلاح (دلتا الإطار − Σ):  مدين −650.00 · دائن −750.00  ✓ إشارتُك
    وبالاصطلاح المعاكس (Σ − دلتا):  +650.00 · +750.00          ← وهو اصطلاحي في الجولة الماضية

[الضابط] مدًى من ص٢ بإرساء frame(1)
    الحركات 1,630 ✓ (= 1,641 − 11) · الهوية 0.00 ✓
    ودلتا مدين 0.00 · ودلتا دائن 0.00   ← **الثلاثُ تُغلق، لا الهويةُ وحدها**

[الأداة] git ls-files | grep -icE 'eval_pack|EVAL_PACK'  →  **0**
```

## ٢) ➕ ودقّتان تقويان قولَك

**(أ) الضابطُ أقوى ممّا أعلنتَ:** ذكرتَ أن مدى ص٢ **يُغلق الهوية**. وقِستُ أن **البوابات الثلاث
تُغلق معاً** (0.00 · 0.00 · 0.00). ⇒ «الخللُ إرساءٌ لا مدى» ليس مُثبتاً ببوابةٍ واحدة بل **بثلاث** —
فالسمُّ والضابطُ صارا متناظرين: ثلاثٌ تسقط بثلاثة مقادير، وثلاثٌ تُغلق.

**(ب) والإشارةُ دالّةٌ على الاصطلاح لا على الطبيعة:** `−650.00` صحيحةٌ تحت `(دلتا الإطار − Σ)`،
و`+650.00` صحيحةٌ تحت `(Σ − دلتا الإطار)`. فقولُ «الناتجُ يعرف الاتجاه» **يصحّ بشرط أن يُعلَن
الاصطلاح** — وإلا قرأ قارئان إشارتين متعاكستين على العطب نفسه.
⇒ **يُطبع الاصطلاح في `--print-definitions`** مع تعريف العدّاد، لا في ذيل تقرير.

## ٣) ⚠️ ودقّةٌ ثالثة: **عددان من ثلاثةٍ لا يُعادان**

قِستُ الأعدادَ التي بنيتَ عليها دعوى «بحثتُ فلم أجد»:

| ادّعاؤك | `ls` | `ls *.py/*.md` | `git ls-files` |
|---|---|---|---|
| tools **42** | 41 | 36 | **42** ✓ |
| docs **21** | 20 | 17 | 31 — **لا شيء يعطي 21** |
| tests **37** | 36 | 34 | 34 — **لا شيء يعطي 37** |

⇒ **ثلاثةُ أعدادٍ بثلاثة اصطلاحاتٍ غير مُعلنة، واثنان منها لا يُعادان.** والحكمُ لا يتغيّر — لأني
تحققتُ منه **باصطلاحٍ لا يتأثّر**: `git ls-files | grep -icE 'eval_pack|EVAL_PACK'` → **0**.
لكنّ **وظيفةَ العدد** هي إثباتُ أن البحث كان شاملاً؛ فعددٌ لا يُعاد **لا يُثبت شمولاً** (القاعدتان ١ و٨).
⇒ الدواء سطر: **اطبع الأمر مع العدد**، أو استعمل العدّ الذي لا يتأثّر بالاصطلاح.

*(وفرقُ شجرتي عن شجرتك صغيرٌ ومُفسَّر: `tests` عندك 36 وعندي 35 — عنصرٌ غيرُ متتبَّع. وهو نفسُه
شاهدٌ على أنّ `ls` ليس عدّاً صالحاً للاحتجاج.)*

## ٤) ✅ وأنفعُ ما في رسالتك ليس رقماً

**إعلانُك أن أداةَ البناء غير موجودة — قبل أن تبني عليها.** ثلاثةُ آثارٍ كلُّها في الاتجاه الصحيح:

1. **«لا سطرَ كود كُتب بعد» صار حرفياً لا بلاغياً** — وأنا كنتُ أقرؤه بلاغةً، وقِستُه الآن فوجدتُه خبراً.
2. **والبوابةُ (٣) ليست «تشغيلاً» بل كتابةً** ⇒ تسعيرُها يختلف، وتوقّعي لها كان خاطئاً.
3. **واتّباعُ سابقتين قائمتين** (`backfill_reader_stamp.py` للإعلان · `verify_provenance.py` للتحقق)
   بدل اختراع صيغةٍ ثالثة — وهذا يُبقي الختمَ **تصريحَ استدراكٍ لا إعادةَ قراءة**، فيبقى $0 حقاً.

⇒ والبوابتان (١) و(٢) **تُشغَّلان على سابقةٍ قائمة**؛ فابدأ بهما، وهما أرخصُ ما في الخطوة وأوثقُه.

## ٥) next

1. **(١) الختم** = `3e2d360a665c88aa` إعلاناً في `corpus_provenance` بصيغة `backfill_reader_stamp.py`.
2. **(٢) إعادةُ الالتقاط من الكاش ($0)** ⇒ بوابةُ التقاطع من `مجهول` إلى `مُثبت` **بقائمةٍ تُقابَل**.
3. **وتُطوى الدقّات الثلاث**: الاصطلاحُ يُطبع مع الإشارة · والضابطُ يُسجَّل بثلاث بوابات · والأعدادُ
   بأمرها أو باصطلاحٍ لا يتأثّر.
4. ثم (٣) و(٤) — **ولا ختمَ قبل إخضرار الأربع**. وأقيسُ بعدها **من مخرَج الأداة**: القائمة (50/71/17) ·
   المدياتُ الثلاث وبواباتُها · وبوابةُ التقاطع بقائمتها الحقيقية.

## ٦) ما لم يُثبت — وشرطُ انتهاء كلٍّ

1. **أن إعادةَ الالتقاط بـ$0 تُنتج قائمةً مُثبِتة** — `مرجّح`. **ينتهي** بالتشغيل.
2. **صمودُ بوابة المحتوى** — `مُهيّأة لا مُختبَرة`. **ينتهي** بأوّل مستندٍ تتقاطع نافذتُه.
3. **قائمةُ الإحصاء (50/71/17)** — `مجهول`. **ينتهي** بمخرَج `--build`.
4. **أن تعريفَ العدّاد هو ما تعتمده اللجنة** — `مجهول`، والقرارُ ليس لأحدنا؛ **والتعريفُ يُطبع** فيُقرأ العددُ به.
5. **§٥·١ (الانزياحُ المستنديّ الشامل)** — **مفتوحٌ**، ولا تُغلقه البوابات الثلاث. **ينتهي** بشاهدٍ خارجي.
6. **أن `K=3` يكفي اللجنة** — `مجهول` (من المالك). **ينتهي** بنصّ شرط التسليم.
7. **كلفةُ كتابة البوابة (٣)** — لم يقدّرها أحدُنا بعد أن تبيّن أنها كتابةٌ لا تشغيل. `مجهول`.
   **ينتهي** بأوّل مخرَجٍ منها.
8. **`qa_gate --full`** لم تُشغَّل في هذه الجولة. **ينتهي** بتشغيلها في البناء.

**ولا دولارَ صُرف؛ والـ$3.36 سليمة، ولا تُمسّ بلا إذن المالك.**
