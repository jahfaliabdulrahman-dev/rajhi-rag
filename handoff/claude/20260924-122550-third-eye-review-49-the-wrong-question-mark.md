```
id:      20260924-122550-claude
from:    claude
to:      sulaiman
type:    REVIEW
step:    مراجعة ٤٩ — PR #119 (`feat/item4-pack-free-capture-44`) · المدى `3729d7e...856c886` (الإصلاح `3699f19` · التصحيحات `bac1919` · `856c886`) — وهو «العرضُ ٤» في وقفة التسليم، بلا كلفةِ نموذج
commit:  856c886 (رأسُ الفرع = رأسُ PR #119 · فحصا `publish-guard` في CI أخضران على الرأس)
verdict: **REQUEST CHANGES** — ستّةُ بنودٍ حاجبة (R49-1..R49-6)؛ وP-1 وS-5 مُغلقان فعلًا بضابطٍ يعضّ، والأرقامُ المنشورة تُعاد حرفيًّا
next:    سليمان: R49-1..R49-6 — كلُّها مجّانيّة (لا نداءَ نموذج) — المالك: العروضُ الأربعة (توصيتي في آخر الملف) + إبطالُ الرمز (خارج المستودع، ومستقلٌّ عن كلّ ما هنا)
```

## Verdict — الحكم

**REQUEST CHANGES.** المُغلقُ فعلًا: **P-1** (قاعدةُ الفهرس) بضوابطَ تسقط عند إعادة الحقن، و**S-5**. والأرقامُ المنشورة
تُعاد على نسخٍ **حرفيًّا** (٣٠·٢٧·٢٨·٣٠ و`27/40 · 24/42 · 26/43 · 29/45`). لكن من البنود المانعة التسعة في REVIEW-48:
**أربعةٌ لم يُمسّ كودُها** (S-2 · S-3 · S-4 · P-3)، وواحدٌ منها **«رُدّ» بقياسٍ على الحرف الخطأ** (S-4)، والجدولُ العامّ (P-4)
باقٍ كما هو؛ واثنان من إصلاحات الدلتا الثلاثة **بلا ضابطٍ أصلًا** (P-2 · S-1). والوثيقةُ العامّة **تُعيد تثبيتَ** «السلسلةُ لا
تتّصل» بأمرٍ لا يقيسها — والقياس: **٦١٩ من ٦٢١ زوجًا تتّصل**، والاستثناءان هما الفجوتان المعروفتان.

## Target — المدى والبيئة

| البند | القيمة | كيف قِيس |
|---|---|---|
| المُدقَّق | `3729d7e...856c886`: ٤ التزامات (`ca02b55` وثائق · `3699f19` إصلاح · `bac1919` تصحيحات · `856c886` إعادةُ تسمية) · ١٢ ملفًّا +٥٧٥/−٣٢ | `git diff --stat 3729d7e 856c886` |
| بيئتي | نسخةٌ مستنسخةٌ خاصّة عند `856c886` + `data/` بنسخٍ copy-on-write (`local_sample` · `training` · `eval_pack` · `sample` + المفتاح) · مفسّرُ المشروع (Python 3.11.15) **و`PYTHONPATH` مُلغًى** (العُرف ١٢) · الشجرةُ المشتركة لم تُلمس (العُرف ١٧) | `git status --porcelain` فارغٌ قبل وبعد |
| الأدلّةُ المدفوعة | الأذرعُ الأربع من أرشيف الأدلّة خارج المستودع (`eval-runs/20260924-4facf540abd4/`) · **بصماتٌ ١٦/١٦** · وكلُّ إعادة حكمٍ على **نسخ** (الأصلُ لم يُلمس) | `shasum -a 256 -c SHA256SUMS` |
| الكلفة | **$0** — لا نداءَ نموذج؛ و`qa_gate --full` لم يُشغَّل (نداءاتُ VLM) | — |

## Verification — ما ادّعيتَه وما قِسته

| ادّعاؤك | قياسي | الوسم |
|---|---|---|
| ٥٩٠ اختبارًا · rc=0 | النسخةُ الكاملة بالبيانات: **`590 passed`** · rc=0 | مُثبت |
| النسخةُ النظيفة `538 · 4 · 48 = 590` | نسخةٌ بلا بيانات **بمفسّر المشروع**: **`542 passed · 4 failed · 44 skipped = 590`** — الأربعةُ الساقطةُ نفسُها بالأسماء؛ والفرقُ (أربعةُ اختباراتٍ تنجح عندي وتُتخطّى عندك) يوافق اختباراتٍ تتخطّى بحسب المفسّر (`importorskip("numpy")` · `pyflakes` · …) — مرجّح، لا مُثبت | لم يُعَد إنتاجًا (البيئةُ غيرُ مسمّاة) |
| الضوابطُ الجديدة حمراءُ قبل وخضراءُ بعد | إعادةُ حقنٍ في `856c886`: الذروةُ من صفوف الصفحة ⇒ **٣ ✗** · قبولُ الفهرس المحلّيّ ثانيةً ⇒ **١ ✗** | مُثبت |
| إصلاحُ القصّ (P-2) وتغطيةُ الحزمة (S-1) | إعادةُ `[:40]` ⇒ **590 ✓** · حذفُ `trace_suspect` ⇒ **590 ✓** · تعميةُ `arms_cover_the_pack` ⇒ **590 ✓** | **بلا ضابط** (R49-5) |
| المجاميعُ بالمصحَّح `٣٠·٢٧·٢٨·٣٠` | `--rescore` على نسخ ⇒ A `30/50` · B `27/50` · C `28/50` · D `30/50` | مُثبت |
| الجدولُ المنشور `27/40 · 24/42 · 26/43 · 29/45` | `eval_run_compare.py` على النسخ ⇒ **مطابقٌ حرفيًّا** · rc=0 · المستبعَد ١٠/٨/٧/٥ = ٣٠ · النوائب ٢/٨/٣/٤ = ١٧ | مُثبت |
| «١٢ من ١٦ انقلابًا = أثرُ القصّ» | من السجلات الحيّة مقابل المصحَّح: **١٧ انقلابًا = ١٢ على سجلاتٍ مشكوكة + ٥ غيرها**؛ وأحدُ «الخمسة» (`C:chn-03`) ليس سؤالَ فهرس | ١٢ ✓ · ١٦ ✗ |
| دعوى S-4 «مردودة» | رُدّت بقياس **U+061F** والدعوى عن **U+003F**؛ ومن طرفٍ إلى طرف: علامةُ المُنتِج `?` ⇒ الحاجبُ **يقبلها** | **الردُّ باطل** (R49-1) |
| «السلسلةُ لا تتّصل» ثبتت بإعادة القياس | الأمرُ المذكور يطبع **٤ صفوفٍ من الصفحة ٢ وحدها**؛ والقياس: **٦١٩/٦٢١ تتّصل** بحركة أوّل صفّ | **منقوض** (R49-4) |
| صفحة ٦٢٩: ١٧ صفًّا بلا رصيد ⇒ `build_rows` يُسقطها ⇒ `max_page = 628` | مُثبت: ١٧ صفًّا · ٠ برصيد · **٠ بتاريخ** · ٦ موسومةٌ `not_a_movement` (و٦٢٦–٦٢٨: ٢١/٢١ بتاريخٍ ورصيد) | الآليّةُ مُثبتة · الوصفُ منازَع (R49-8) |
| `amount_guard` · `static_gate` · `render_claims` | rc=0 · rc=0 · rc=0 | مُثبت |
| «publish-guard 0 BLOCK» | `publish_guard --tree --history` ⇒ **BLOCK ١** (`handoff/STATE.md:18` من `d311c76` على الفرع المحلّيّ `backup-review-18` وحده — لا ريموتَ يحمله)؛ والخطّافُ يمسح مدى الدفع فمرّ الدفع | مختلطٌ بلا أمر (R49-11) |
| `qa_gate` ٣/٥ سابقٌ للعمل | الجزءُ المجّانيّ: **٣/٤** (`parser goldens` بالفشل نفسه) · و`--full` لم يُشغَّل (مدفوع) | مُثبتٌ في حدّه |

## Spec — هل هو الشيءُ الصحيح؟

- **R49-1 (P1)** `tools/eval_questions.py:417` × `tools/eval_stamp.py:14` — **S-4 مفتوح، و«ردُّه» قاس الحرفَ الخطأ.**
  REVIEW-48 كتب `is_publishable({spec_sha:"?"}, "?") = True` بعلامة **U+003F**؛ والتقرير (§١) قاس `is_publishable('؟','؟')`
  بعلامة **U+061F** — وهي `PLACEHOLDER` نفسُها — فأعطى `False` وأعلن الدعوى «لم تُعَد إنتاجاً ⇒ مردودة».
  على `856c886`: `is_publishable({'spec_sha':'?'}, '?')` ⇒ **`True`** · ومن طرفٍ إلى طرف: ملفُّ أسئلةٍ غيرُ مقروء ⇒ `_spec_sha()`
  يُرجع `'?'` ⇒ **الحاجبُ يقبل علامةَ المُنتِج نفسِه** (`True`) · و`classify` تضعها في «المخالفة» لا «البلا-بصمة».
  و`tools/eval_stamp.py` **لم يتغيّر** منذ `3729d7e`. · **مُثبت**
  الحركة: كما في REVIEW-48 (علامةٌ واحدة يملكها `eval_stamp` ويُنتجها المُنتِج نفسُه) + ضابطٌ يشغّل **المُنتِج** على ملفٍّ غيرِ مقروء ويشترط `False`.

- **R49-2 (P1)** `tools/eval_questions.py:651-657` · `:449` · `:410-417` — **S-3 مفتوح، وسمٌّ واحدٌ يكشف ثلاثَ ثغرات.**
  على نسخةٍ من الذراع D، بملفّ أسئلةٍ فيه `cnt-01` ⇒ الصفحة **9999**، ونصُّ `abs-02` مُستبدَلٌ بنصٍّ لم يُطرح قطّ:
  `--validate` ⇒ «١ مرسًى مكسور» **rc=1** · و`--rescore --questions <ذلك> --out <نسخة>` ⇒ **rc=0**، و`cnt-01` **✅**
  (حقيقةُ صفحةٍ غيرِ موجودة تُشتقّ **صفرًا** لا «لا حقيقة»، فتُطابق رقمًا في الجواب)، والمجموع `30/50` مطبوعٌ **قابلًا للنشر**
  بلا تحذير بصمة (البصمةُ قُرئت من الملفّ الافتراضيّ لا من الملفّ الذي حُكم به)، **وملفُّ الذراع أُعيدت كتابتُه في مكانه** (تغيّرت بصمتُه).
  أي: الفرعُ يعود قبل الحاجب (`:655` قبل `:657`) · والكتابةُ تسبق الرفض (`:449`) · والبصمةُ لا تشهد على الملفّ المقروء (`:410`). · **مُثبت**
  الحركة: بوّابةٌ واحدةٌ قبل الفرعين · `--rescore` يكتب ملفًّا **جديدًا** · و`_spec_sha(path)` من الملفّ المقروء فعلًا.

- **R49-3 (P1)** `tools/eval_run_compare.py:60-62` · `:64` · `:86` — **P-3 مفتوح، وهو الذي يصنع «التذبذب» المنشور.**
  المقارنةُ وإعادةُ الحكم تعدّان النوائب السبعةَ عشر (A٢ · B٨ · C٣ · D٤) أجوبةً راسبة. والقياسُ على النسخ المصحَّحة:

  | الأساس | عددُ الأسئلة | A · B · C · D | المدى |
  |---|---|---|---|
  | الكلّ | ٥٠ | ٣٠ · ٢٧ · ٢٨ · ٣٠ | ٢٧–٣٠ |
  | أساسُ الأداة (استبعادٌ لكلّ ذراعٍ وحدها) | ٤٠ · ٤٢ · ٤٣ · ٤٥ | ٢٧ · ٢٤ · ٢٦ · ٢٩ | «٢٤–٢٩، عرض ٥» |
  | مشتركٌ بلا مقصوص | ٣٦ | ٢٥ · ٢٣ · ٢٥ · ٢٥ | ٢٣–٢٥ |
  | **مشتركٌ بلا مقصوصٍ ولا نائب** | **٣٣** | **٢٣ · ٢٣ · ٢٣ · ٢٣** | **صفر** |

  ومن الثمانية «المتذبذبة»: `abs-06` و`cnt-06` نائبُ B · `chn-02` و`chn-04` و`cit-09` أثرٌ مشكوك · `cit-02` كلاهما ·
  **ونظيفان فقط: `num-04` و`num-08`** (ينقلبان في اتجاهين متعاكسين). ⇒ «الحتميّةُ مُقاسة (٢٧–٣٠)» و«٢٤–٢٩» الجديدة **لا تقيسان النموذج**:
  تقيسان المهلات والقصّ — و**أربعةَ مقاماتٍ مختلفة** وُلدت في هذه الدلتا (يُستبعد لكلّ ذراعٍ وحدها ثم يُؤخذ `min/max` لأعدادٍ خامّة). · **مُثبت**
  الحركة: P-3 كما كُتب (النائبُ خارج المقام + عمودُ تغطية) + استبعادٌ **مشترك** (معرّفٌ مشكوكٌ أو نائبٌ في أيّ ذراع يُستبعد من الكلّ) + التذبذبُ والمدى على الأساس نفسه.

- **R49-4 (P1)** `docs/EVAL_PACK.md:351-357` — **«السلسلةُ لا تتّصل» أُعيد تثبيتُها في وثيقةٍ عامّة بأمرٍ لا يقيسها — والقياسُ ينقضها.**
  الاقتباسُ يقول: أُعيد القياسُ «على عالم السلسلة نفسه» فأعطى «الفصلَ نفسَه في الأزواج الأربعة كلِّها»، و«الفروقُ مضاعفاتٌ مدوَّرة ⇒ اشتباهُ قراءةٍ منهجيّ يستحقّ بندًا مستقلًّا».
  والأمرُ المذكور (`[... if r['page'] in (2,3,52,53,242,243,486,487)][:4]`) يُرجع ٤ صفوف **كلُّها من الصفحة ٢** (قِيس: الصفحات `[2, 2, 2, 2]`) — لا زوجَ واحدًا.
  والقياس (قيمٌ منطقيّة فقط، بلا مبالغ): في الأزواج الأربعة **`رصيدُ أوّل صفٍّ في b − رصيدُ آخر صفٍّ في a = −حركةُ أوّل صفٍّ في b` بالضبط**؛
  وعلى الكشف كلِّه: **٦٢١ زوجًا متتاليًا قابلًا للقياس ⇒ ٦١٩ تتّصل بحركة أوّل صفّ**، والاستثناءان `426→427` و`625→626` —
  وهما بعينهما **الفجوتان المعروفتان ٤٢٧/٦٢٦** (البوّابة ٥ في `docs/handoff-protocol.md` §٤).
  ⇒ السلسلةُ **تتّصل**؛ و«رصيدُ الآخر ≠ رصيدِ الأوّل» هو حركةُ أوّل صفٍّ لا غير، و«المضاعفاتُ المدوَّرة» هي تلك الحركاتُ نفسُها.
  واشتقاقُ `row_chain` (`equal: False`) صحيحٌ حرفيًّا؛ **الاستنتاجُ** («لا تتّصل» + «اشتباهُ قراءة») هو الخاطئ — ويفتح بندًا وهميًّا.
  **والتقريرُ يناقض التزامَه:** §٤ بند ٥ «يُشطب بصراحة»، و`bac1919` يُبقيه ورسالتُه «لم تُشطب رغم طلب المراجعة». ولم تطلب REVIEW-48 شطبًا:
  طلبت **تصحيحًا مؤرَّخًا** لأنّ القياسَ يخالفه. · **مُثبت**
  الحركة: تصحيحٌ مؤرَّخ «كان: لا تتّصل · صار: تتّصل في ٦١٩/٦٢١ والاستثناءان هما الفجوتان المعروفتان (الأمر…)» وحذفُ «اشتباه القراءة».

- **R49-5 (P1)** `tools/eval_questions.py:442-443` · `:542-543` · `tools/eval_run_compare.py:108-109` — **اثنان من إصلاحات الدلتا الثلاثة بلا ضابط، وحركةُ S-1 نصّت عليه.**
  إعادةُ الحقن على `856c886` (السويتُ كاملًا في كلّ مرّة): إعادةُ `[:40]` ⇒ **590 passed** · حذفُ `rec["trace_suspect"] = True` ⇒ **590 passed** ·
  `"arms_cover_the_pack": True` ⇒ **590 passed**. ولا اختبارَ يذكر `trace_suspect` ولا `trace_excluded` ولا `arms_cover_the_pack` ولا `missing_ids` (`grep` ⇒ ٠).
  و`records_never_truncated` ما زالت **الحشوَ** الذي وصفه S-1 (`records <= ids_expected`) وتُطبع `True` بجانب الثابت الجديد؛ ووصفُ الملفّ (`:7`) ما زال يَعِد بفحص «الطولُ = العدّاد» غيرِ الموجود.
  العُرف ٨ في البروتوكول والقاعدة ٢ في التكليف: «ضابطٌ لا يفشل عند إعادة الحقن ليس ضابطاً». · **مُثبت**
  الحركة: ثلاثةُ سمومٍ اختباراتٍ دائمة (ذراعٌ مقصوصة ⇒ `rc≠0` · أثرُ ٤٠ بلا `trace_len` ⇒ مستبعَدٌ ومُعلَن · الأثرُ الكاملُ يُحفظ مع `trace_len`)، وإزالةُ الحشو أو جعلُه حقيقيًّا.

- **R49-6 (P1)** — **S-2 وP-4 لم يُمسّا، وP-6 أُغلق على الورق وحده.**
  **S-2:** `tools/eval_questions.py:499` كما هو — `cap = int(budget/0.01)` ⇒ `--budget 0.6` ⇒ **٦٠ > ٥٠**. وهذا يخصّ **العرضين ١ و٢ مباشرةً** (كلاهما مدفوع): العلَمُ الذي يُفترض أن يسقّف الصرفَ لا يوقف بكلفةٍ مقيسة.
  **P-6:** الأرقامُ صُحّحت تصحيحًا مؤرَّخًا (ملفُّ الأدلّة + التقرير) — لكنّ مصدرَ الكلفة ما زال فرقَ عدّاد المفتاح المشترك (`:473` · `:555-563`) ⇒ أوّلُ جولتين متداخلتين تُنتجان رقمًا لا يُعزل من جديد.
  **P-4:** الجدولُ العامّ «المقاييسُ الساريةُ الآن» (`docs/EVAL_PACK.md:459-467`: ١٥/٣٠ · ٦/١٠ · ٣/٧ · **٢٤/٤٧** · ٤/٧) لم يُمسّ في الدلتا، والأرقامُ المصحَّحة
  لا توجد إلا في ملفّات التسليم — وأحدُها في صندوق المدقّق (R49-9). والتكليفُ (ج) سمّى هذا الجدولَ أوّلًا. · **مُثبت**
  الحركة: كما في REVIEW-48 — والسقفُ وP-6 جذرٌ واحد: `usage.cost` لكلّ طلب، والسقفُ يوقف عند بلوغه.

## Standards — هل تُرك العملُ نظيفًا؟

- **R49-7 (P2)** `handoff/sulaiman/20260924-0621-REPORT-48-fix-first-executed.md` — **التقريرُ لا يقول ما لم يفعله.**
  النطاقُ المُعلَن «أ + ب + ج + د»، والبندُ «أ» فيه تسعةُ مانعة؛ S-2 · S-3 · P-3 · P-4 لم تُمسّ **ولا تظهر في «ما لم يُثبت»**، وS-4 معدودةٌ «مردودة» (R49-1).
  والعنوان «١٢ من ١٦» هو **١٢ من ١٧** بجدوله نفسه (قصّ ٦+٣+٣+٠ · غيره ١+٠+٣+١)؛ و«الأثرُ الحقيقيُّ للقاعدة ٥ انقلابات» يضمّ `C:chn-03`
  — سؤالُ سلسلةٍ لا تمسّه قاعدةُ الفهرس — ⇒ «الخمسة» أثرُ كلِّ تغييرات الحاكم منذ الجولة الحيّة لا أثرُ القاعدة. · **مُثبت**
  الحركة: جدولٌ للتسعة: مُغلق / جزئيّ / مفتوح، ولكلٍّ أمرُه.

- **R49-8 (P2)** `tools/refusal_test.py:99` (التقريرُ يقول `:97`، وهو سطرُ `chain_derive`) · اقتراحُ `abs-02` في §٥ من التقرير — **صفحة ٦٢٩: الآليّةُ صحيحة والوصفُ لا — وللخيارين ثمنٌ غيرُ معلن.**
  قِيس: `pg-629` فيها ١٧ صفًّا خامًّا · **٠ بتاريخ** · ٠ برصيد · ٦ وسمها القارئُ `not_a_movement`؛ بينما ٦٢٦–٦٢٨: ٢١/٢١ بتاريخٍ ورصيد
  ⇒ الأرجحُ أنها **ليست حركات** (صفحةُ ملخّص) كما رجّحت REVIEW-48 — ودليلُ إنجاز (د) («سطرٌ: ٦٢٩ = ملخّص/حركات بمصدره») غائبٌ عن التقرير.
  ⇒ **الخيار (أ)** (إبقاءُ الصفوف بلا رصيدٍ في السلسلة) يُدخل حينئذٍ ١٧ سطرًا بلا تاريخ في سلسلةٍ يقرؤها الاسترجاعُ والمجاميعُ والذروة.
  ⇒ **الخيار (ب)** (مُسعَّرٌ «$0»): قِيس على نسخةٍ خاصّة — نصُّ `abs-02` المقترح يغيّر البصمة `4facf540abd4 → 6b87477f0ca4` ⇒ `--rescore` **rc=3**
  (٥٠/٥٠ مخالفةً في كلّ ذراع) والمقارنةُ **rc=1** ⇒ **السجلاتُ المدفوعةُ الـ٢٠٠ كلُّها تصير غيرَ قابلةٍ للنشر** (T-2). ويجعل «الامتناعَ» جوابًا
  متوقَّعًا عن سؤالٍ حقيقتُه ١٧ ⇒ **يُثبّت عطبَ اليوم سلوكًا صحيحًا في المقياس**. · **مُثبت** (وكونُها ملخّصًا: مرجّح)
  الحركة: المالكُ ينظر صورةَ الصفحة ٦٢٩ (ملخّصٌ أم حركات؟) — المُدخلُ الوحيد الذي لا يملكه المستودع؛ فإن كانت ملخّصًا فالعطبُ في **بوّابة النطاق**
  (تُنكر صفحةً موجودة) لا في السلسلة؛ وأيُّ تعديلٍ لملفّ الأسئلة ينتظر T-2 (بصمةٌ لكلّ سؤال).

- **R49-9 (P2)** `handoff/claude/20260924-0155-REPORT-item4-review-fixes-and-measured-runs.md` · `handoff/STATE.md` — **S-7 تفاقم.**
  `bac1919` **عدّل** (+١٢/−١) ملفًّا في صندوق المدقّق بدل نقله إلى `handoff/sulaiman/` كما طلب S-7 — والقاعدةُ الحديديّة ١: «لا أحد يعدّل ملفات الطرف الآخر».
  و`handoff/STATE.md` على الفرع ما زال «مراجعة ٤٥» — **أربعُ جولاتٍ من التقادم** الآن (٤٥ ⇒ ٤٧ ⇒ ٤٨ ⇒ ٤٩)، والتكليفُ نصّ على تحديثه في الإقفال. · **مُثبت**

- **R49-10 (P3)** `handoff/sulaiman/20260924-0630-REVIEW-48-…` — اسمُه ٠٦٣٠ وترويستُه «٠٦:٣٠ (+٠٣)» والتزامُه **٠٦:٠٤:٠٦**، والإصلاحُ `3699f19` (٠٦:١٩)
  يسبق الوقتَ المكتوبَ في المراجعة التي يُصلحها. الصنفُ نفسُه في R47-4 — وصحّحه التقريرُ لملفّه وحده (٠٨٠٠ ⇒ ٠٦٢١). · **مُثبت**

- **R49-11 (P3)** `docs/QA_CHECKLIST.md:13` · `tools/publish_guard.py:483` — **البيئةُ غيرُ مسمّاة.** `538/4/48` لا يُعاد بمفسّر المشروع (`542/4/44`)؛
  و«0 BLOCK» بلا أمر، والأمرُ الموثّق (`--tree --history`) يعطي BLOCK من فرعٍ **محلّيّ** (`rev-list --objects --all` يقرأ `backup-review-18`) —
  لا شيءَ عامّ. والحركة: استثناءٌ مبرَّر أو قصرُ `--history` على المراجع المنشورة؛ وحذفُ الفرع الاحتياطيّ قرارُ المالك. · **مُثبت**

## Structure — هل تُركت البنيةُ أحسن؟

- **R49-12 (P2)** `tools/eval_questions.py:114-121` — **«عالمُ الصفوف الواحد» مكتوبٌ في الوصف لا في الكود.**
  الوصفُ يقول «ولا يُشتقّ شيءٌ من صفوف الصفحة وحدها»؛ والكودُ يشتقّ `argmax_row` من السلسلة، **وسبعةَ أنواعٍ أخرى** (`footer` · `page_sum_all` · `count_rows` ·
  `last_row_balance` · `row_chain` · `rows_matching` · `date_encoding`) من صفوف الصفحة. صنفُ T-1 نفسُه («قاعدةٌ في موضعين تنقسم») داخل دالّةٍ واحدة.
  وكذلك الاستبعاد: `--rescore` يطبع المجموعَ **شاملًا** المشكوكة (A `30/50`) والمقارنةُ تستبعدها (`27/40`) — القاعدةُ في أداةٍ دون أخرى. · **مُثبت**
  الحركة: إمّا وصفٌ صادق («الذروةُ وحدها»)، أو مالكٌ واحدٌ لعالم الصفوف؛ والاستبعادُ في وحدةٍ واحدةٍ تستوردها الأداتان (كما صار `eval_stamp`).

- **R49-13 (P3)** `docs/EVAL_PACK.md:353-358` — الاقتباسُ ابتلع بقيّةَ الفقرة الأصليّة (استمرارٌ كسول): «شيك» · تعادلُ الصفحة ٢ · ١٦٥٥ صفًّا
  صارت داخل الاقتباس بعد الأمر. · **مُثبت**
- **R49-14 (P3)** `.github/workflows/publish-guard.yml:42-48` — CI لا يشغّل ضوابطَ الحاكم: الجناحُ الخفيف أربعةُ ملفّات. و`test_eval_row_numbering.py`
  ينجح في نسخةٍ بلا بيانات ⇒ مرشّحٌ للجناح الخفيف **إن** استُورد بلا اعتماديّات ML (لم يُقَس في CI). · **مرجّح**

## Declined to judge — خارجُ نطاقي أو قرارُ المالك

- **التسريب (البند «ب»)**: البروفايلاتُ الأربعة والرمز — خارج المستودع (§١ القاعدة ٦)؛ لم أتحقّق. **والإبطالُ فعلُ المالك، ومستقلٌّ عن كلّ ما هنا.**
- صحّةُ أجوبة النموذج نفسِها بمعيارٍ خارج الحاكم.
- أرقامُ العدّاد (`$0.4891` · `$2.9333`): من السجلات الحيّة؛ لم أُعِد قراءتها.
- العروضُ الأربعة: القرارُ للمالك — وتوصيتي أدناه.

## Not proven — ما لم يُثبت

- أنّ ٦٢٩ صفحةُ ملخّص: مرجّحٌ قويّ (٠/١٧ بتاريخ · ٦/١٧ `not_a_movement`)، والشاهدُ القاطعُ الصورة.
- أنّ المتذبذبَين النظيفَين (`num-04` · `num-08`) لا حتميّةُ نموذجٍ لا حاكم — لم أحلّلهما.
- أثرُ إصلاح P-1 على جولاتٍ قادمة؛ و`qa_gate --full` (مدفوع).
- أنّ ضوابطَ الحاكم تُستورد في CI الخفيف (R49-14).

## توصيتي في العروض الأربعة (للمالك)

| # | العرض | توصيتي | لماذا (مقيس) |
|---|---|---|---|
| ٤ | مقعدٌ على الدلتا ~$0.5–1 | **نُفِّذ بهذه المراجعة — بلا كلفةِ نموذج** | مستقلٌّ عن كاتب الإصلاح |
| ١ | جولةٌ منفردة ~$0.5 | **ليس الآن** — بعد R49-2 · R49-3 · R49-5 · R49-6 (مجّانيّة) | وP-3 وS-2 مفتوحان ⇒ الرقمُ الجديد يحمل العللَ نفسَها؛ وسؤالُ الحتميّة له جوابٌ مجّانيٌّ الآن: **٢٣·٢٣·٢٣·٢٣ على ٣٣ مشتركة** |
| ٢ | أداةُ سرد الصفوف + ٨ أسئلة ~$0.1 | **نعم، بعد الإصلاحات المجّانيّة** (قاعدتُك: «بعد إصلاح الحُكَم») | من ٣٢ سجلًّا في العائلة: ١٣ لم يبلغ أثرُها الصفحة · ١٣ نائبُ مهلة · ٤ فهرسٌ/مبلغٌ خاطئ · ١ مقصوص · ١ صحيح ⇒ الفجوةُ حقيقيّة، والمهلاتُ تُخرَج من المقام أوّلًا |
| ٣ | صفحة ٦٢٩ | **لا (أ) ولا (ب) الآن** | (ب) ليس $0 (٢٠٠ سجلٍّ مدفوعٍ تصير غيرَ قابلةٍ للنشر)؛ (أ) يُدخل أسطرًا بلا تاريخ؛ والصورةُ تحسم |

{المُدخلات: تقريرُ ٤٨ + الدلتا `3729d7e...856c886` + الأذرعُ الأربعُ المدفوعة (١٦/١٦)} → {البرهان: نسختان خاصّتان (بالبيانات وبدونها) · ٥٩٠ و٥٤٢/٤/٤٤ · إعادةُ حكمٍ على نسخٍ تُطابق المنشورَ حرفيًّا · ٥ إعادات حقن (٢ تعضّ · ٣ لا) · سمٌّ واحدٌ يكشف S-3 وS-4 · قياسُ ٦٢١ زوجًا متّصلًا · تسعيرُ الخيار (ب) · $0} → {الحالة: REQUEST CHANGES · P-1 وS-5 مُغلقان · S-2 · S-3 · S-4 · P-3 · P-4 مفتوحة · والقرارُ للمالك في العروض}
