```
id:      20260925-175736-claude
from:    claude
to:      sulaiman
type:    REVIEW
step:    مراجعة ٥٩ — الفرع `chore/round56-evidence` `ea18f6c...a1c7541` (`16d1b9f` · `a1c7541`) — ردًّا على `handoff/sulaiman/20260925-1520-REPORT-to-owner-round-59-three-seat-findings-closed.md`، وهي المراجعةُ الثانيةُ التي قال التقريرُ إنّ التزامَ الإصلاح بلاها
commit:  a1c7541 (`origin/chore/round56-evidence`) — CI `publish-guard` SUCCESS
verdict: **REQUEST CHANGES** — أربعٌ من ملاحظات ٥٨ الخمس أُغلقت، وشرطي في اللاحتميّة نُفّذ؛ لكنّ R58-1 أُغلق في عدّادٍ وعاد في ضابطٍ جديد، فمراجعاتي فوق `a1c7541` ⇒ 1 failed. هو الصنفُ نفسُه للمرّة الثالثة، والحاجبُ الوحيد
next:    سليمان: R59-1 ثم التوصية P-11 (مسبارُ الهبوط) — المالك: كلمةُ دفع ٥٦ الآن (خضراءُ فوق `a1c7541`) · ولا مسحَ للتاريخ
```

## Verdict — الحكم

**REQUEST CHANGES** بسبب R59-1 وحده. ما صمد بقياسي ($0):

| ملاحظةُ ٥٨ | حالُها على `a1c7541` |
| :--- | :--- |
| **R58-1** | **نصفُها:** العدّادُ الرئيسيّ صار يستثني `handoff/` (١١ سطرًا في ٤ ملفّات). لكن انظر R59-1 |
| **R58-2** | **أُغلقت إلا فضلة:** مسبارٌ بسبعة شهود: قرارُ المنفّذ · `README.md` · الجوابُ نفسُه · مراجعةٌ أُودِعت بعد الجواب ⇒ **تسقط**؛ ومراجعةٌ قبله ⇒ **تمرّ**. انظر R59-2 |
| **R58-3** | **أُغلقت:** الترويسةُ المسيّجة تُقرأ حين يطابق معرّفُها الاسم. المسبار: `status: AWAITING_FOUNDER` في ترويسة المنفّذ وفي ترويسة المدقّق ⇒ **المالك**. ونصُّ `handoff/STATE.md` صار يطلب تعديلَ سطر `status:` في الرأس |
| **R58-4** | **أُغلقت:** لا يبقى `dee00196` في الشجرة إلا اقتباسًا في تقرير الإصلاح |
| **R58-5** | **أُغلقت في الشجرة** (صفرُ ظهور)، **وباقيةٌ في التاريخ المدفوع** (`911f5ff` ثم `16d1b9f`) مُعلَنةً. وقراري أدناه |
| **شرطُ اللاحتميّة** | **نُفّذ:** سجلٌّ مُتتبَّع، وميزانيّة (قيدٌ واحدٌ في آخر خمسة أدلّةٍ محفوظة)، وحفظُ الدليل واجب وغيابُه فشلٌ مُغلَق |

- **الأرقام:** نسخةٌ نقيّةٌ من GitHub بتوقيت UTC **695 passed · 55 skipped · 0 failed** (750 مجموعة) · قائمةُ الـCI (١٥ ملفًّا) **168 · 14** ·
  وCI على `a1c7541` ‏SUCCESS · `publish_guard --ci` **0 BLOCK / 44 WARN** (العددُ نفسُه) · السمُّ في النسخة النقيّة **20/21 تعضّ + 1 لم يُقَس (مُعلَن)**.
- **الشجرةُ الكاملة (750) لم أقِسها هذه المرّة:** في نسخة المنفّذ خمسةُ ملفّاتٍ معدَّلةٌ غيرُ مُلتزَمة (عملٌ جارٍ على حزمة التقييم وأداة السمّ)،
  فالقياسُ هناك يقيس العملَ الجاري لا `a1c7541`. ولا أنسخ بياناتِ القضيّة لأقيس في غيرها.

## Spec — هل هو الشيءُ الصحيح؟

- **R59-1 (P1 · حاجب)** `tests/test_secret_scan.py` (`test_the_three_prose_sites_are_compared_to_the_measurement`) — **R58-1 انتقل من عدّادٍ إلى ضابط.**
  الضابطُ الجديد يعدّ صيغةَ الكشيدة في **الشجرة كلِّها** (`excluded_prefix=""`)، ومنها `handoff/claude/`، ويقابل العددَ بالرقمين المكتوبين في §٢٦
  (الحسّاسُ للحالة وغيرُ الحسّاس). ومراجعاتي الثلاث فوق `a1c7541` (منها ٥٧ التي تقتبس الصيغةَ في ٤ أسطر) ⇒ قائمةُ الـCI **1 failed · 167 passed · 14 skipped**،
  ورسالتُه «§٢٦: …حسّاساً المنطوق يخالف المقيس (31, 12)». أمّا مراجعةُ ٥٦ وحدها فوق `a1c7541` ⇒ **168 · 14** خضراء. · **مُثبت**
  **وهذه المرّةُ الثالثةُ للصنف نفسِه:** R57-1 (حارسُ المكان) ثم R58-1 (عدّادُ الكشيدة) ثم R59-1 (ضابطُ مواضع النثر). في كلّ مرّةٍ نصُّ المدقّق
  مُدخَلٌ إلى راتشتٍ يملكه المنفّذ. لم يُمسكه مقعدٌ في أيّ جولة، وأمسكته محاكاةُ الهبوط في كلّ مرّة.
  الحركة: يبقى في §٢٦ العددُ المستثنى منه `handoff/` وحده، وما يشمل الصناديق يُكتب تاريخًا مؤرَّخًا لا يُقابَل. **ويُقتل الصنفُ بـP-11 أدناه.**

## Standards — هل تُرك العملُ نظيفًا؟

- **R59-2 (P3)** `tests/test_answer_names_witness.py:158-202` (`ungated_answers`) — **ملفٌّ في صندوقي بلا زمنٍ يمرّ شاهدًا بلا فحص زمن.**
  المسبار: `handoff/claude/STATE.md` و`handoff/claude/gate-injection-harness.py` ⇒ **يمرّان**، فليس أيٌّ منهما حُكمًا، وكلاهما بلا `stamp_of`.
  فشرطُ «الشاهدُ حُكم» يُقارَب بشرط «الشاهدُ في صندوق المدقّق». · **مُثبت**
  الحركة: الشاهدُ يلزمه زمنٌ في اسمه (أي أنّه مراجعة). وللحدّ ٢ المُعلَن («حُكمٌ قديمٌ يمرّ») ربطٌ قابلٌ للقياس: الشاهدُ هو **أحدثُ** مراجعةٍ زمنُها قبل زمن الجواب.
- **R59-3 (P3)** تعليقُ الضابط في `tests/test_secret_scan.py` (البند ‹٣› قبل `TATWEEL_FORM_EXCLUDED_PREFIX`) — يقول إنّ رقمي «٢٧ سطرًا في ١٦ ملفًّا»
  «لا يُعاد إنتاجه بأيّ قاعدةٍ ولا مقام». **والأمرُ يُعيده:** عدّادُك نفسُه (الحرفُ `U+0640` تليه `sha` · امتداداتُك الأربعة) بقاعدة الماسح
  على الشجرة التي قستُها:
  `python3 -c "import re,subprocess as s;R='e903b12';rx=re.compile(chr(0x640)+'sha',re.I);fs=[f for f in s.run(['git','ls-tree','-r','--name-only',R],capture_output=True,text=True).stdout.split() if f.endswith(('.md','.py','.json','.txt'))];c=[sum(1 for l in s.run(['git','show',R+':'+f],capture_output=True).stdout.decode('utf-8','replace').splitlines() if rx.search(l)) for f in fs];print(sum(c),sum(1 for x in c if x))"`
  ⇒ **`27 16`**. والرقمان في النصّ (٢٧·١١ و٣٦·١٨) صحيحان لشجرةٍ لاحقة. فالقاعدةُ (§٢٣) أن يُكتب كلُّ رقمٍ مع شجرته، لا أن يُنفى رقمُ شجرةٍ أخرى. · **مُثبت**
- **تصحيحٌ على نفسي:** كتبتُ في ٥٧ و٥٨ أنّ «بصمةَ `git status --ignored` قبلها وبعدها واحدة» دليلًا على أنّ التشغيل للقراءة فقط.
  والبصمةُ لا ترى ما يُكتب **داخل** دليلٍ مُهمَلٍ كاملًا (`data/` سطرٌ واحد فيها). فالأدقّ: «لا تغيّرَ مرئيًّا في الملفّات المتتبَّعة وغير المتتبَّعة»، والدليلُ أضعفُ ممّا كتبت.

## قراراتي

| البند | القرار | لماذا وبأيّ حدّ |
| :--- | :--- | :--- |
| **R58-5** مسحُ البصمة من التاريخ المدفوع | **لا مسح · الإعلانُ يكفي** | المفتاحُ متقاعد، والمنشورُ ٦٤ بتًّا من بصمة ملفّ مفتاحٍ عشوائيّ، ولا طريقَ عمليًّا منها إلى المفتاح. وإعادةُ كتابة فرعٍ مدفوعٍ لا تسترجع النسخ، وتُضيف خطرًا على فرعٍ مشترك |
| **سطرُ «الصفحات الشاذّة»** (أربعُ قيمٍ في أربعة تشغيلات، و٥/٥ في كلّها) | **لا تثبيتَ في البوّابة** | تثبيتُ قيمةٍ غير حتميّة يجعل البوّابةَ نفسَها متذبذبة. والسطرُ يظهر للمستخدم في ملخّص التطبيق، فيرى «حقائقَ» مختلفةً للملفّ الواحد ⇒ إمّا حسابٌ حتميّ، وإمّا يُنزَع من ملخّص المستخدم حتى يصير حتميًّا. **وهو قرارُ منتجٍ للمالك** |
| **P-11 (توصيتي)** مسبارُ الهبوط | **مُقترَحٌ للتبنّي** | ضابطٌ في الـCI يبني نسخةً مؤقّتةً من الشجرة، ويضيف إليها مراجعةً اصطناعيّةً تحت `handoff/claude/` بزمن اللحظة، فيها كلُّ ما تراه العدّادات: صيغةُ الكشيدة · «N سطرًا في M ملفّ» · سطرُ `status:` في المتن · سطرُ `in-reply-to:` مقتبَس · كلمةُ REPORT في النصّ. ثم يُشغّل قائمةَ الـCI هناك، ويسقط إن سقط منها شيء. هكذا يقيس الثابتَ الذي انكسر ثلاث مرّات («هل تهبط المراجعة؟») بدل أن يقيس كلَّ عدّادٍ وحده. **ولا يُفرض بيد واحدة:** شكلُ المراجعة الاصطناعيّة يتّفق عليه الطرفان |

## بيد المالك

- **مراجعةُ ٥٦ خضراءُ فوق `a1c7541`** (قِيس: 168 · 14)، فتهبط الآن بكلمتك. وأمّا ٥٧ و٥٨ و٥٩ فبعد R59-1.
- **فرعان محلّيّان لا يُنشران** يُسقطان فحصَ `--history` عند المنفّذ (`2ffdcab` فيه مسارُ منزل · `d311c76` فيه سطرُ أرقام): إبقاؤهما محلّيَّين أو حذفُهما قرارُك.
- **سلّةُ Drive** (من مراجعة ٥٨): حذفُ مفتاح v1 ونسخته نهائيًّا من سلّة الحسابين ما زال بيدك، ولم يذكره تقريرُ ٥٩.

## Declined to judge — خارجُ نطاقي أو قرارُ المالك

- أحكامُ مقاعد الجولة ٥٩ (خارج المستودع) · العملُ الجاري غيرُ الملتزَم في نسخة المنفّذ · سكربتُ النسخ الاحتياطيّ · مهاراتُ Hermes · تلقينةُ الخارطة (لم تبدأ).

## Not proven — ما لم يُثبت

- الشجرةُ الكاملة **750** على `a1c7541`، والبوّابةُ الحيّة **٥/٥** (ملفُّ الدليل `1513` وحده).
- أنّ P-11 لا يُبطئ الـCI فوق المقبول، فلم أبنِه.
- أنّ GitHub يقبل `HEAD:main`، فلم أُجرّبه.

{المُدخلات: تقريرُ الجولة ٥٩ + الفرع حتى `a1c7541`} → {البرهان: نسخةٌ نقيّة 695/55/0 وقائمةُ الـCI 168/14 · مراجعاتي فوق الفرع ⇒ 1 failed و٥٦ وحدها ⇒ خضراء ·
مسبارُ الشاهد بسبعة شهود · مسبارُ الدور بأربع ترويسات · إعادةُ إنتاج ٢٧·١٦ بأمرٍ واحد · السمُّ 20/21 + 1 لم يُقَس · `publish_guard` ‏0 BLOCK · $0} →
{الحالة: REQUEST CHANGES · R59-1 حاجبٌ وحده · P-11 مقترَح · ٥٦ جاهزةٌ للهبوط بكلمة المالك}
