# سجل الضعفاء (Weakness Register) — rajhi-rag · 2026-09-17

> كل بند يحمل: الدليل (ملف:سطر + أمر + مخرج) · الأثر على رقم أو قرار حقيقي · الحل · بديلاً · الكلفة.
> **الأولوية:** `STOP` = يمسّ صحة رقم حقيقي أو يسبّب أثراً لا رجعة فيه · `P1` · `P2` · `P3`.
> **كل الأدلة أُنتجت محلياً بكلفة سحابية = $0.00.**
> الأوامر تُنفَّذ من جذر المستودع بـ`.venv/bin/python`. «الكاش» = `data/local_sample/slice_629p/results/`.

---

## STOP

### S1 — رقم حساب حقيقي مُعاد تركيبه داخل ملف مُتتبَّع، والحارس أعمى عنه

| | |
|---|---|
| **المحور** | M3 — الخصوصية |
| **الموضع** | `tests/test_publish_guard.py:18-19` (البناء من شطرين) و`:32` (إعادة تركيب الوسم الكامل `FRACCT/…FR`) |
| **الدليل** | فحصتُ الكاش الحقيقي بحثاً عن السلسلة التي يبنيها الملف: **22 ظهوراً في 10 صفحات** (المواقع 1→18) داخل أوصاف تحويل بالشكل `FRACCT/…FR` و`FRACCT/…FR/`. ثم: `python3 tools/publish_guard.py --tree` → `tree: 68 — 0 BLOCK / 0 WARN`. و`git ls-files tests/test_publish_guard.py` → الملف مُتتبَّع. |
| **الأثر** | المستودع عام. السلسلة الرقمية الحقيقية موجودة فيه الآن، مقسومة على `+` بحيث **تعجز قاعدة `long_digits` عن رؤيتها بنيوياً**. وترويسة الملف (`:6-8`) تقول حرفياً إنه «لا يحوي أي literal بصيغة حساب» — فالاعتقاد الخاطئ موثّق. الأثر **لا رجعة فيه** بمجرد الفهرسة. |
| **المفارقة** | الـcommit الذي أدخلها هو `dc0e862` بعنوان «no account-like literal may exist even in the guard's own tests» — أي أن **الإصلاح هو ما رمّز الرقم**. |
| **الحل** | استبدل السلسلتين بقيمة مُخترعة لا تظهر في الكشف (مثلاً بادئة `999…`)، وتحقّق: أعد فحص الكاش بحثاً عن القيمة الجديدة = 0 ظهور. ثم **أعد كتابة التاريخ** (`git-filter-repo --replace-text`) لأن الشطرين في `dc0e862` أيضاً. |
| **البديل أ** | قاعدة جديدة في الحارس: `concat_digits` — تطبيع الأسطر بإزالة `" + "` و`"" + ""` قبل تطبيق `long_digits`. يمنع تكرار النمط، لكنه لا يُنظّف ما وقع. |
| **البديل ب** | منع قاعدة بنيوية: لا يجوز لأي اختبار أن يشتقّ بياناته من الملف الحقيقي — تُولَّد fixtures الاختبار من `make_synthetic_statement.py` حصراً. أقوى، لكنه يتطلب إعادة كتابة `test_publish_guard.py`. |
| **الكلفة** | الإصلاح: ~30 دقيقة. إعادة كتابة التاريخ: ~20 دقيقة + إبلاغ أي نسخة clone. **$0.00** |
| **التوصية** | افعل الثلاثة: القيمة الجديدة + قاعدة `concat_digits` + إعادة كتابة التاريخ. **قبل أي push.** |

---

### S2 — «فوتر 621 ok · 0 منزاح» لا يمكن إعادة إنتاجه من المستودع

| | |
|---|---|
| **المحور** | M1/M2 — الحَكَم وسلامة الأثر |
| **الموضع** | ثلاثة ملفات كاش (ص491 · ص564 · ص616) تحمل مفتاحاً اسمه `footer_reread` بالشكل `{"<field>": {"was": …, "now": …}}` |
| **الدليل ١** | `grep -rn "footer_reread" --include='*.py' --include='*.md' --include='*.sh' --include='*.yml' .` → **صفر نتائج**. المفتاح موجود في البيانات فقط، ولا آلية تُنتجه في الكود. |
| **الدليل ٢** | على الصفحات الثلاث، القيمة المخزّنة **لا تُشتق من حقل `raw` المخزّن بجوارها**: `_parse_amount(raw[field]) != Decimal(footer[field])` في ثلاث مواضع، وفقط فيها (فحصتُ 629×3 حقلاً). أي أن **سجل الإثبات يناقض الرقم الذي يستند إليه**. |
| **الدليل ٣** | أعدتُ اشتقاق الملف كله مرتين: بالقيم المخزّنة → `{ok 621, mismatch 0, unchecked 2, absent 4, gap 2}`؛ وبقيم مُعاد تحليلها من `raw` → **`{ok 615, mismatch 6, …}`** والصفحات المنزاحة = 491, 492, 564, 565, 616, 617. |
| **الدليل ٤** | الصفحات الثلاث نفسها تحمل `reread_rejected: "القراءة الجديدة لا تطابق دلتا الفوتر"` — أي أن **إعادة القراءة الآلية رفضت**، فالقيم الصحيحة جاءت من مكان آخر (عين المالك، كما يوثّق `suspects_log.md` نفسه: «المالك رصد أثرَي 100 و0.50 بعينه قبل الآلة»). |
| **الأثر** | `docs/handoff-protocol.md §4` يجعل «5770/5793 نظيف · فوتر 621 ok · 0 منزاح» **بوابة مراجعة رسمية (٥)** على المدقّق أن يتحقق منها. البوابة غير قابلة للتحقق المستقل: الطريق الوحيد إليها تعديل يدوي غير مُتتبَّع. وكل مَن يقرأ README/bootstrap يفهم «621» كناتج أنبوب آلي. |
| **الإنصاف** | البند **مُفصَح عنه نصّاً** في `suspects_log.md` («الأزواج الثلاثة: أخطاء إطاراتنا — صُحّحت في البيانات») بقيمه الثلاث الصحيحة. الخلل في **الإثبات والاسترجاع**، لا في الإخفاء. |
| **الحل** | أدخِل التحكيم البشري إلى مفردات النظام: حالة `ok(human)` أو حقل `arbitrated_by` يُعرض في كل تقرير، **ويُحدَّث `raw` أو يُحفظ `raw_original` بجواره** حتى يبقى الاشتقاق ممكناً. وأداة `tools/arbitrate_footer.py` (محلية، بلا API) تكتب التعديل + سببه + الدليل البصري. |
| **البديل أ** | عدم السماح بالتعديل اليدوي إطلاقاً: الصفحة الثلاث تبقى `mismatch` ويُعلَن «6 صفحات منزاحة، مصدرها خطأ قراءة إطار مؤكد بالعين». **أصدق، وأغلى إعلامياً.** |
| **البديل ب** | إعادة قراءة الإطارات الثلاثة حيّاً بقصاصة أضيق (المشكلة رقم واحد في خانة الآلاف) وقبول النتيجة آلياً. يعيد الإثبات لكن بكلفة ونتيجة غير مضمونة. |
| **الكلفة** | البديل أ: $0.00 + تعديل وثائق. الحل الكامل: ~نصف يوم. البديل ب: 3 نداءات ≈ **$0.05**. |
| **التوصية** | الحل الكامل (`arbitrated_by` + `raw_original`) — التحكيم البشري شرعي، لكنه يجب أن يُرى. |

---

## P1

### P1-1 — راية «مفارقة الدقة» ×100 لا يمكن أن تُرفع إلا على الصفحة الأولى

| | |
|---|---|
| **المحور** | M1 |
| **الموضع** | `src/statement_qa/footer_oracle.py:139-142` (`_x100`) و`:186-189` (حساب `is_paradox`) · `delta_status:228-257` (لا يحسبها إطلاقاً) · المستهلكون: `tools/scale_slice.py:413-415` (حاجز التوقف) و`app.py:545-546` (اللافتة) |
| **الدليل** | بنيتُ صفحة كل صفوفها منزاحة ×100 وشغّلتُ المقارنة: <br>• prior = 0 (الصفحة 1) → `is_paradox = True` ✓<br>• prior = (0.01, 0.01) → `is_paradox = **False**`<br>• prior = (500, 700) → **False** · prior = (50000, 70000) → **False**<br>السبب بنيوي: المقارنة التراكمية هي `prior + own` مقابل الفوتر، و`prior` يكسر نسبة الـ×100 حسابياً. <br>وعلى أساس **الدلتا** — أساس التقرير منذ `308aa2f` — `delta_status` **لا تُرجع مفتاح `is_paradox` أصلاً**، وعند `cum_broken=True` تعود `check_page_footer` بـ`{"status":"unchecked","compared":0}` بلا المفتاح ⇒ `chk.get("is_paradox")` = `None`. |
| **الدليل ٢** | `tests/test_footer_oracle.py:76-83` — الاختبار الذي «يقفل» السلوك يستدعي `check_page_footer(_chain_rows(), f)` **بلا `prior`** ⇒ يختبر شرط الصفحة الأولى وحده. اختبار أخضر يوثّق نصف الحقيقة. |
| **الأثر** | الانزياح ×100 **يُلتقط** (يُصنَّف `mismatch`) — فلا رقم كاذب يمرّ صامتاً. لكن: (أ) **حاجز التوقف في `scale_slice` ميت**: تشغيل مدفوع 629 صفحة يواصل إلى النهاية بدل أن يتوقف فوراً عند النمط الكارثي؛ (ب) لافتة «نمط زيغ ×100» لا تظهر للمستخدم؛ (ج) هذا هو **البند P1 رقم واحد من ورشة «ماذا لو» №1** — أُغلق بنصف حارس. |
| **الحل** | احسب الراية على **الدلتا**: `is_paradox = all(_x100(own[f], footer[f]-prev[f]) for f in mismatched)` — الدلتا لا تحمل prior فالنسبة تنجو. وعلى المسار التراكمي قارن `own` مقابل `footer − prior` بدل `prior+own` مقابل `footer`. |
| **البديل أ** | راية أوسع: `ratio_signature` تُرجع النسبة المقيسة (×100 · ÷100 · ×10 · أخرى) بدل بوليان. تلتقط الأخطاء «المكتفية داخلياً ×10» التي يذكرها `suspects_log` كنمط مرصود. |
| **البديل ب** | ألغِ الراية واجعل الحاجز يتوقف عند **أي** `mismatch` غير مُعلَّل. أبسط وأصلب، لكنه يوقف تشغيلات على حالات حميدة (انظر P2-6). |
| **الكلفة** | ~ساعتان + اختبارات تمرّر `prior` غير صفري. **$0.00** |
| **التوصية** | الحل + البديل أ معاً، وإصلاح الاختبار ليمرّر `prior` حقيقياً — وإلا تكرّر نفس الوهم. |

---

### P1-2 — لافتة الفوتر تقلّص مقامها: «621/621 مطابق» على ملف فيه 8 صفحات غير موثّقة

| | |
|---|---|
| **المحور** | M1/M2/M8 |
| **الموضع** | `app.py:538-551` — `f_possible = len(f_ok) + len(f_bad)` · `tools/scale_slice.py:481` (سطر الماركداون) · `tools/qa_gate.py:173-179` (البوابة ترث المقام) |
| **الدليل** | أعدتُ إنتاج منطق `app.py` حرفياً على أحكام الملف الكامل: اللافتة تقرأ<br>`تحقق الفوتر: 621/621 مطابق — غير قابلة للتحقق: 485، 603`<br>والصفحات المُسقَطة من المقام ومن النص: `absent = [172, 484, 602, 629]` و**`gap = [427, 626]`**. |
| **الدليل ٢** | `data/local_sample/slice_629p/slice_report.md` (الأثر البشري) يطبع: «621 مطابق · 0 غير مطابق · 2 غير قابل للتحقق · 4 غير مقروء» — **المجموع 627 من 629**. حالة `gap` غائبة عن سطر الماركداون كلياً (موجودة في الـJSON وفي سطر stdout فقط). |
| **الأثر** | صفحتا `gap` (427 و626) هما **أخطر شذوذ في الملف**: كل واحدة تجاور ورقتين مفقودتين من المسح، ودلتا إطارها تقيس حركات أوراق غائبة. تختفيان من اللافتة ومن التقرير البشري. والقارئ يرى **100%**. ثم `qa_gate` يؤكد `f_ok == f_possible` ⇒ تأكيده الفعلي «صفر mismatch»، لا «N صفحة موثّقة»؛ العتبة `f_possible >= 9` هي وحدها ما يمنع بوابة خضراء على عينة كلها `absent`. |
| **الحل** | المقام = **عدد الصفحات كلها**، والنص يسمّي كل فئة: `تحقق الفوتر: 621/629 موثّقة · 2 فجوة مسح (427، 626) · 4 بلا إطار · 2 غير قابلة للتحقق`. وفي `qa_gate` افصل التأكيدين: `mismatch == 0` **و** `ok >= <عتبة مُعلَنة للعينة>`. |
| **البديل أ** | ثلاثة أرقام صريحة: `موثّق/مفحوص/الكل` — يفصل «فُحص وطابق» عن «لم يُفحص». |
| **البديل ب** | نسبة تغطية مستقلة: `تغطية التحقق = 621/629 = 98.7%` بجوار `دقة التحقق = 621/621 = 100%`. الرقمان معاً يقولان الحقيقة كاملة. |
| **الكلفة** | ~ساعة (3 مواضع + سطر الماركداون + البوابة). **$0.00** |
| **التوصية** | البديل ب — رقمان لا يمكن الخلط بينهما. |

---

### P1-3 — «✓ الترقيم المطبوع متسلسل» يُطبع على تغطية 2.7٪، وعلى فجوة حقيقية

| | |
|---|---|
| **المحور** | M2 |
| **الموضع** | `src/statement_qa/ordering.py:108-125` (`summarize_page_numbers`) و`:88-90` (شرط التجاور) |
| **الدليل ١** | على كوربوس الـ629: `page_numbers = {checked: 17, unread: 612, …}` — الرقم المطبوع قُرئ في **17 موقعاً من 629 (2.7%)**. حقل `unread` يُحسب ولا يُعرض في أي مخرَج. |
| **الدليل ٢** | ملف 600 ورقة قُرئ فيه الرقم على 3 أوراق متتالية فقط → `summarize_page_numbers` تُخرج: **«الترقيم المطبوع: ✓ متسلسل (3 صفحة)»**. |
| **الدليل ٣** | `check_page_numbers([(1,1),(2,None),(3,50)])` — قفزة حقيقية من ١ إلى ٥٠ — تُخرج: **«الترقيم المطبوع: ✓ متسلسل (2 صفحة)»**. الدالة ترفض **ادّعاء فجوة** (صحيح: `p2 != p1+1 → continue`) ثم يدّعي المُلخِّص **التسلسل** (خطأ). |
| **الأثر** | هذه الإشارة هي ما «أقفل العدّ» (630 مطبوعاً مقابل 629 ورقة) وما يُعلن أنه «يكتشفه النظام بنفسه». في الواقع الفجوتان وُجدتا بنافذتين مدفوعتين مُوجَّهتين (`slice_gapcheck`, `slice_gapcheck2`) + مسح مونتاج **بعين وكيل** — لا بكاشف آلي على الملف. وعلى أي ملف جديد ستظهر «✓» خضراء على تغطية شبه معدومة. |
| **الحل** | اجعل الرسالة تحمل التغطية دائماً، و«لا أعرف» عند ضعفها: <br>`checked/total < 0.5` → `«الترقيم المطبوع: تغطية ناقصة (17/629) — لا حكم»`؛ ووجود أي زوج غير متجاور → `«…فجوة تغطية بين الموقعين X وY — غير محسوم»` لا «✓». |
| **البديل أ** | ارفع التغطية بدل تحسين الرسالة: `tools/page_numbers.py` يقرأ الرقم **محلياً من البكسل** (بلا API) لكل ورقة ويُغذّي `page_nos`. يحوّل الإشارة من 2.7% إلى ~97% بكلفة $0.00 لكنه يحتاج كاشفاً محلياً موثوقاً. |
| **البديل ب** | استنتاج التسلسل من مصدر ثانٍ: منحنى الانحراف التراكمي (موجود أصلاً في `suspects_log`) كإشارة مستقلة عن قراءة الرقم — تعمل على 100% من الصفحات. |
| **الكلفة** | الحل: ~ساعة. البديل أ: يوم. البديل ب: نصف يوم. كلها **$0.00** |
| **التوصية** | الحل فوراً (رسالة صادقة) + البديل ب (إشارة تغطي كل صفحة) — البديل أ لاحقاً. |

---

### P1-4 — طبقة الأسئلة عمياء تماماً عن طبقة التحقق

| | |
|---|---|
| **المحور** | M5/M2 |
| **الموضع** | `app.py:372,479,508,538-540` — `footer_checks` لا تُستعمل خارج مسار القراءة · `src/statement_qa/chunking.py:34-47` · `src/statement_qa/qa_tools.py` كله |
| **الدليل** | `grep -n "footer_checks" app.py` → 6 نتائج، كلها داخل `_process_pdf_locked` وملخّصه.<br>`grep -rn "footer\|verified\|status" src/statement_qa/qa.py qa_tools.py chunking.py` → لا شيء غير نصوص docstring.<br>`_fmt_row` يضيف `[صف مشبوه — يحتاج مراجعة]` لشكوك **السلسلة** فقط؛ لا شيء عن حكم الصفحة. |
| **الأثر** | كل جهاز التحقق (ok/mismatch/gap/unchecked/absent) يعيش في التبويب الأول ولا يصل التبويب الذي **تُستهلك فيه الأرقام**. سؤال يُجاب من صفوف ص427 (فجوة مسح بجوارها ورقتان مفقودتان) لا يُميَّز عن سؤال يُجاب من صفحة موثّقة 3/3. و`page_summary(page=427)` يُرجع إجماليات نظيفة بلا أي تحفّظ. هذا يكسر الادعاء المركزي «الرقم الذي لا يمكن إثباته لا يُقال» عند نقطة الاستهلاك بالذات. |
| **الحل** | مرّر `footer_checks` إلى `STATE` المقروء من الأدوات: كل أداة تُلحق بجوابها سطراً `⚠ يشمل صفحات غير موثّقة: 427 (فجوة مسح)، 172 (بلا إطار)`؛ و`_fmt_row`/`chunk_rows` يضيفان وسم الصفحة داخل نص القطعة ليراه النموذج. |
| **البديل أ** | «وضع صارم»: الأدوات تستبعد صفوف الصفحات غير الموثّقة افتراضياً وتقول كم استبعدت. أصلب، ويغيّر كل مجموع يراه المستخدم اليوم. |
| **البديل ب** | عمود «التحقق» في الجدول والتصدير (انظر P1-5) بلا لمس الأدوات. أرخص وأضعف: يحمي القارئ لا الحاسبة. |
| **الكلفة** | الحل: ~نصف يوم. البديل أ: يوم + إعادة قفل قيم البوابة. **$0.00** |
| **التوصية** | الحل — الإفصاح لا الحجب. البديل أ خياراً يُفعّله المستخدم. |

---

### P1-5 — الجدول والتصدير لا يحملان أي إفصاح عن التحقق

| | |
|---|---|
| **المحور** | M2 |
| **الموضع** | `app.py:273-297` — `_row_record`: عمود «الحالة» = `✓` / `⚠ مشبوه` / `◌ اتجاه غير محسوم` فقط |
| **الدليل** | العمود يُشتق من `r["ok"]` و`r["side"]` حصراً — وكلاهما من `chain_derive`. لا مسار يربط الصف بحكم صفحته. أي: صف من ص427 (`gap`) وصف من ص172 (`absent`) يظهران بـ`✓` تماماً كصف من صفحة موثّقة. |
| **الأثر** | التصدير هو المخرَج الذي يغادر النظام ويُبنى عليه قرار. يغادر **بلا الطبقة الوحيدة التي تميّز المُثبت عن غير المُثبت**. عمود «التواريخ المُستكملة» عولج بأمانة (`*` + `date_source`) — ونفس الانضباط لم يُطبَّق على التحقق. |
| **الحل** | عمود «التحقق» بأربع قيم: `✓ موثّق` · `◔ بلا إطار` · `⚠ فجوة مسح` · `— غير قابل للتحقق`؛ ورأس التصدير يحمل سطر ملخّص واحد. |
| **البديل أ** | علامة لصيقة بعمود «الحالة» (`✓*`) + مفتاح أسفل الجدول. أرخص، أقل وضوحاً. |
| **البديل ب** | تلوين صفوف الصفحات غير الموثّقة. جميل بصرياً، **يضيع في النسخ/التصدير** — مرفوض لهذا السبب. |
| **الكلفة** | ~ساعتان (يعتمد على P1-4). **$0.00** |
| **التوصية** | الحل — وهو نفس نمط `*` الذي أثبت نجاحه في التواريخ. |

---

### P1-6 — حارس النشر يُهزَم بمسافة واحدة، ولا يرى الملفات الثنائية ولا رسائل الـcommit

| | |
|---|---|
| **المحور** | M3 |
| **الموضع** | `tools/publish_guard.py:48-49` (`long_digits`) · `:122-133` (`_scan_text` يستخدم `search` + `continue`) · `:194` (تخطّي `BINARY_EXTS`) · `:204-207` (`UnicodeDecodeError → continue` بلا ملاحظة) · `:223` (`rev-list --objects` = blobs فقط) |
| **الدليل** | شغّلتُ قواعد الحارس مباشرة على حمولات اصطناعية (بلا كتابة أي شيء في المستودع). النتيجة: |

```
PASSES (no finding)   | IBAN بصيغته المكتوبة بمسافات
BLOCK:long_digits     | IBAN بلا مسافات               ← الضابط الوحيد الذي يعمل
PASSES (no finding)   | بطاقة بشُرَط  /  بطاقة بمسافات
PASSES (no finding)   | حساب بأرقام هندية مفصولة بمسافات
PASSES (no finding)   | سلّم أرقام أولاً ثم حساب حقيقي في نفس السطر
PASSES (no finding)   | صف كشف حقيقي (وصف + مبالغ منسّقة)
PASSES (no finding)   | export/ledger.csv  (تصدير الجدول)
PASSES (no finding)   | export/ledger.xlsx  ·  rows.zip  ·  rows.sqlite  ·  statement.tar.gz
WARN:name_token       | اسم العائلة وحده  ← تحذير لا يوقف الدفع
BLOCK:media_file      | صورة PNG  ← الضابط يعمل
```

| | |
|---|---|
| **الأثر** | أرجح تسريب فعلي ليس ملف بيانات (الـ`.gitignore` والـ`PATH_RULE` يغطّيانه) بل **لصق ثلاثة صفوف من الكشف في وثيقة/issue/README لتوضيح فكرة** — وهذا يمرّ بلا أي ملاحظة، وهو مذكور حرفياً في قائمة ممنوعاتك. و**التصدير موجود في الواجهة**: ملف `.xlsx` منه يمرّ صامتاً (لا BLOCK ولا حتى WARN — يُتخطّى عند فشل الفك). و`_is_digit_table` مع `search` يعني أن **سلّم أرقام واحد في أول السطر يُعمي القاعدة عن بقية السطر**. ورسائل الـcommit وبيانات المؤلف لا تُفحص إطلاقاً. |
| **الحل** | أربع إضافات صغيرة: <br>(١) `long_digits_spaced`: طبّع السطر بإزالة `[ \t\-_.]` بين الأرقام قبل التطبيق.<br>(٢) `undecodable`: كل blob مُتتبَّع لا يُفك كـUTF-8 ولا يُغطّيه `MEDIA_EXTS` ⇒ **BLOCK** (لا تخطٍّ صامت).<br>(٣) `finditer` بدل `search` في `_scan_text`.<br>(٤) فحص رسائل الـcommit: `git log --format=%B` عبر نفس `TEXT_RULES`. |
| **البديل أ** | أضف قاعدة «بصمة صف كشف»: سطر يجمع وصفاً عربياً + مبلغاً بصيغة `#,###.##` + رصيداً ⇒ WARN قاطع (وWARN يجب أن يوقف الدفع — انظر P3-6). التقاط أعلى، إنذار كاذب أعلى. |
| **البديل ب** | حارس بقائمة بيضاء بدل سوداء: لا يُدفع إلا ما يطابق أنماطاً مسموحة صراحةً (`*.py`, `*.md`, …) وبقيته يحتاج قراراً. أصلب بكثير، احتكاك يومي أعلى. |
| **الكلفة** | الحل: ~3 ساعات + اختبارات. البديل ب: يوم + انضباط دائم. **$0.00** |
| **التوصية** | الحل الرباعي كاملاً + البديل أ (لأن «الصف الحقيقي» هو السيناريو الواقعي). |

---

## P2

### P2-1 — تقرير السلايس يتلف عند إعادة التشغيل من الكاش

| | |
|---|---|
| **المحور** | M4/M8 |
| **الموضع** | `tools/scale_slice.py:170,181-182,429` — `per_page`/`recoveries`/`page_rereads`/`elapsed` تُبنى من **تنفيذ هذه الجلسة** فقط |
| **الدليل** | التقرير المخزّن للملف الكامل يقول `time: {elapsed_s: 16.8, avg_page_s: 0.0}` و`boundary_recoveries: []` و`page_rereads: []` — بينما الكاش يحمل `recovered` على **23 صفحة** و`reread` على **15 صفحة**، والوسيط الحقيقي للزمن **22.18 ث/صفحة** (مشتقاً من `ms_read+ms_footer`). نفس الشيء في 150p: التقرير `0.0 ث`، والكاش يحمل 4 و4 والوسيط **20.62 ث**. |
| **الأثر** | التقرير هو **أثر المشروع الأول**، وثلاثة من حقوله تُمحى بالعملية التي يُعلن عنها أنها مجانية. كل ادعاء زمني في PLAN («18.5 ث/صفحة» · «19.6 ث/صفحة» · «22.2 ث») لا يسنده أي ملف في المستودع اليوم. |
| **الحل** | اقرأ الحقول الثلاثة من الكاش لا من الجلسة: الزمن من `ms_read+ms_footer` (ووسيطاً لا متوسطاً)، والاستدراكات من مفاتيح `recovered`/`reread` المخزّنة. |
| **البديل** | `run_manifest.json` منفصل يُكتب مرة عند التشغيل الحيّ ولا يُلمس في أي replay. يفصل «ما جرى» عن «ما نستنتجه». |
| **الكلفة** | ~ساعتان. **$0.00** |

---

### P2-2 — دفتر الكلفة ينمو مع كل replay، و«649 نداءً» ليست عدد النداءات

| | |
|---|---|
| **المحور** | M9/M8 |
| **الموضع** | `tools/scale_slice.py:88-93` (`_sum_dicts`) · `:96-110` (`_bump_usage`) · `:267` (`{"calls": 1}` **لكل صفحة** لا لكل طلب) · `:285-292` (استدراك المرساة بلا علامة فشل مخزّنة) |
| **الدليل ١** | حاكيتُ replay كاملاً من الكاش: **صفحتان (427 و626) ما زالتا تُطلقان `recover_anchor`** لأن الفشل لا يُخزَّن (النجاح وحده يُخزَّن كـ`recovered`). أي أن «إعادة التشغيل من الكاش» تكلّف نداءين في كل مرة، و`_bump_usage` **يضيفهما إلى كاش الصفحة** ⇒ الدفتر يرتفع عند كل replay. |
| **الدليل ٢** | `calls` في الدفتر = **649**، وهو ما تسمّيه رسالة bootstrap «649 نداءً». لكن الحقل يُزاد بواحد لكل **صفحة** لا لكل طلب: القراءة والفوتر نداءان منفصلان (`read_rows_vlm` + `read_footer`). المقيس: 629 قراءة ناجحة + 625 إطاراً مقروءاً + ~20 محاولة اختيارية ⇒ **≈1278 طلباً فعلياً**. و`prompt_tokens` الكلي = 1,605,314 (≈2552/صفحة) يؤكد صورتين لا صورة. |
| **الدليل ٣** | `_sum_dicts` يجمع **أي مفتاح رقمي**، ولذلك يحمل دفتر `slice_gapcheck` مفتاحاً اسمه `page_no` بقيمة 3422 — لأن `read_rows_vlm` يكتب `stats["page_no"]` في نفس القاموس الذي يستقبل `usage`. |
| **الأثر** | الكلفة ($8.81) صحيحة ومؤكَّدة. **عدد النداءات خاطئ بمعامل ~2** في رسالة تسليم رسمية، ورقم الدفتر غير مستقر عبر الزمن، وبنية الدفتر تجمع حقولاً ليست كلفة. |
| **الحل** | عدّاد طلبات حقيقي داخل `chat_vlm_image` (المنفذ الوحيد) · `_sum_dicts` بقائمة مفاتيح بيضاء (`cost`, `*_tokens`) · تخزين `anchor_rejected` مثل `reread_rejected` بالضبط. |
| **الكلفة** | ~ساعتان. **$0.00** (ويوفّر نداءين لكل replay) |

---

### P2-3 — لا سقف كلفة ولا حاجز إخفاق ولا استئناف على مسار الواجهة

| | |
|---|---|
| **المحور** | M4 |
| **الموضع** | `app.py:400-410` — حلقة الصفحات: `except Exception: failed_pages.append(pg); cum_broken = True; continue`. لا `fail_streak`، لا `max_cost`، لا checkpoint |
| **الدليل** | قارن بـ`tools/scale_slice.py:254-257` (توقف عند 5 إخفاقات متتالية) و`:422-424` (سقف الميزانية) و`:191-227` (كاش لكل صفحة). المسار الوحيد الذي يستخدمه المالك من المتصفح لا يحمل أياً منها. |
| **الأثر** | انقطاع مزوّد أثناء رفع ملف كبير من الواجهة: كل صفحة تستهلك 4 محاولات بتراجع 10/20/40 ثانية (`vlm_reader.py:38-39,122-146`) ⇒ **~70 ثانية لكل صفحة فاشلة**، و629 صفحة ≈ **12 ساعة دوران** بلا نتيجة وبلا استئناف. والبند معترف به في bootstrap (بند مفتوح ١) لكن بلا حاجز مؤقت. |
| **الحل** | انقل الثلاثة إلى دالة مشتركة يستعملها المساران: `fail_streak >= 5` + سقف كلفة + كاش صفحة تحت `tempfile`. |
| **البديل** | احظر رفع >N صفحة من الواجهة برسالة تحوّل إلى `scale_slice`. يُنفَّذ في 20 دقيقة ويغلق الأثر اليوم. |
| **الكلفة** | الحل: يوم. البديل: 20 دقيقة. **$0.00** |
| **التوصية** | البديل الآن، الحل لاحقاً. |

---

### P2-4 — كتابة كاش الصفحة ليست ذرّية؛ ملف نصف مكتوب يقتل كل استئناف لاحق

| | |
|---|---|
| **المحور** | M4 |
| **الموضع** | `tools/scale_slice.py:241,271,302,346` (`cache.write_text`) و`:106-110` (`_bump_usage` يقرأ ثم يكتب) · القراءة عند `:196` `json.loads(cache.read_text(...))` بلا `try` |
| **الدليل** | القراءة على السطر 196 غير محروسة: أي ملف مقطوع يرفع `json.JSONDecodeError` ويُنهي التشغيل بتتبّع خام. والإصلاح الوحيد حذف الملف ⇒ **إعادة دفع كلفة صفحة مسدَّدة**. |
| **الأثر** | ليس فساداً صامتاً (يتعطّل بصوت) — لكنه يحوّل انقطاع كهرباء لحظياً إلى تشغيل مُعطَّل + كلفة مُعادة، في أداة كل قيمتها «الاستئناف لا يعيد ما سُدّد». |
| **الحل** | كتابة ذرّية: `tmp = cache.with_suffix(".tmp"); tmp.write_text(...); os.replace(tmp, cache)` — سطران. ولفّ القراءة بـ`try` يعامل الفساد كـ«غير مقروء» لا كانهيار. |
| **الكلفة** | ~20 دقيقة. **$0.00** |

---

### P2-5 — الوثائق العامة تحمل أرقاماً منزاحة عن مصادرها

| | |
|---|---|
| **المحور** | M8 (المهمة الإلزامية — المسح الكامل) |

| الوثيقة:السطر | الادعاء | المقيس | المصدر |
|---|---|---|---|
| `README.md:5` | **613/629** صفحة موثّقة | **621** | إعادة اشتقاق من الكاش + `slice_report.json` |
| `README.md:5` | «البقايا **العشر**» | **8** صفحات غير `ok` (2 unchecked + 4 absent + 2 gap) | نفس المصدر |
| `README.md:5` | «منها **3 أزواج متعادلة**» ما زالت بقايا | صُحّحت وأُغلقت (`suspects_log.md`) | تناقض داخلي بين الوثيقتين |
| `docs/QA_CHECKLIST.md:13` | الاختبارات «حالياً **78**» | **86** | `pytest tests/ -q` |
| `PLAN.md` G4 | **5761/5784** نظيف | **5770/5793** | إعادة الاشتقاق |
| `PLAN.md` G4 | **613/629** موثّقة | **621** | إعادة الاشتقاق |
| `PLAN.md` G4 | **19** مرساة | **23** مفتاح `recovered` في الكاش | عدّ مباشر |
| `PLAN.md` G4 | **$8.69** | **$8.809917** | دفتر الكاش |
| `PLAN.md` G2 | **18.5** ث/صفحة | **20.62** ث (وسيط 150p) | `ms_read+ms_footer` |
| `PLAN.md` D | «فوتر 10 مطابق/**20** كشف فجوة واحدة» | الأثر يقول `{ok: 10, mismatch: 20}` | صياغة تُقرأ «10 من 20» والحقيقة «10 ok و20 mismatch» — وهو تتالي ما قبل أساس الدلتا، والأثر لم يُعَد توليده |
| `app.py:735` (ABOUT) | «العيّنة المقيسة **10 صفحات (≈1.6%)**» | README يقول الملف كامل | ادعاءا نطاق متعارضان على سطحين يراهما الغريب |
| bootstrap | «$8.809917 على **649 نداءً**» | ≈**1278** طلباً | انظر P2-2 |
| `docs/ONBOARDING_NEW_FILE.md:31` | 629 · 99.6% · **621** موثّق | ✓ **صحيح** | الوثيقة الوحيدة المحدَّثة |

| | |
|---|---|
| **الأثر** | README هو أول ما يقرؤه الغريب، و«613» أقل من الواقع بثمانٍ — أي أن المشروع **يظلم نفسه** في اتجاه، ويبني على أرقام غير قابلة للتتبع في اتجاه آخر. والأخطر: قارئ يجمع «621 + 8 بقايا» مقابل «613 + 10 بقايا» لا يستطيع إغلاق 629 بأي من الروايتين. |
| **الحل** | لا رقم يُكتب يدوياً: `tools/render_claims.py` يقرأ `slice_report.json` ويُولّد كتلة الأرقام في README/PLAN/QA_CHECKLIST، واختبار يفشل عند الانزياح. |
| **البديل** | بند في `QA_CHECKLIST` §1: «كل رقم في وثيقة عامة يُعاد اشتقاقه قبل أي push». أرخص، ويعتمد على الانضباط — وهو ما فشل مرتين حتى الآن. |
| **الكلفة** | البديل: 10 دقائق. الحل: ~3 ساعات. **$0.00** |
| **التوصية** | الحل — لأن هذا الانزياح تكرّر ولن يتوقف بالنية. |

---

### P2-6 — أول صفحة في أي تشغيل جزئي تُصنَّف `mismatch` كاذباً

| | |
|---|---|
| **المحور** | M1 |
| **الموضع** | `footer_oracle.check_page_footer` عند `prior={0,0}` و`prev_footer=None` |
| **الدليل** | `slice_gapcheck` (يبدأ من 424): ص424 → `mismatch`، الفروق `debits: app=219.10 مقابل footer=436699.61` — أي مجموع الصفحة مقابل تراكمي الكشف كله. ونفس الشيء في `slice_gapcheck2` (يبدأ من 621): ص621 → `mismatch`. |
| **الأثر** | إنذار كاذب منهجي في كل نافذة تحقيق. `OPERATIONS.md` يستشهد بنافذة 424–431 كدليل نظيف ولا يذكر الـmismatch. وأي `qa_gate` على نافذة سيفشل (`assert "⚠" not in footer_seg`). ولو فُعّل «التوقف عند أي mismatch» (بديل P1-1/ب) لتعطّلت كل نافذة. |
| **الإنصاف** | لا كلفة مهدورة: شرط `pg > args.first` يمنع إطلاق إعادة قراءة مدفوعة على الصفحة الأولى. |
| **الحل** | الصفحة الأولى في تشغيل `--first > 1` تُصنَّف `unchecked` صراحةً برسالة «لا جار سابق — تحقق الدلتا غير متاح»، لا `mismatch`. |
| **البديل** | `--prior-from <slice>`: اقرأ التراكمي من تشغيل سابق فتصير النافذة قابلة للتحقق تراكمياً. أقوى، ويربط النوافذ بالتشغيل الكامل. |
| **الكلفة** | الحل: 30 دقيقة. البديل: ~3 ساعات. **$0.00** |

---

### P2-7 — `.env` الموصوف في README لا يقرؤه أي كود · المفتاح يأتي من مسار خاص خارج المستودع

| | |
|---|---|
| **المحور** | M6/M8 |
| **الموضع** | `src/statement_qa/vlm_reader.py:42-49` و`src/statement_qa/qa.py:74-84` |
| **الدليل** | `grep -rn "dotenv\|load_dotenv" --include='*.py' .` → **صفر استدعاءات** (و`python-dotenv` مُدرج في `requirements.txt`).<br>المفتاح يُقرأ من متغيّر البيئة، وإلا من `~/.hermes/.env` **بلا حراسة**. شغّلتُه بمنزل فارغ: `FileNotFoundError: [Errno 2] No such file or directory: <home>/.hermes/.env`. |
| **الأثر** | تعليمة README (`echo 'OPENROUTER_API_KEY=...' > .env`) **لا تعمل لأي أحد**. والغريب يحصل على خطأ يسمّي مساراً منزلياً لا يعرفه، من إطار وكلاء خاص، في مستودع عام. هذا أول احتكاك يواجهه أي مُجرِّب. |
| **الحل** | `load_dotenv()` في `app.py` وفي كل أداة سطر أوامر · لفّ قراءة `~/.hermes/.env` بـ`try` · رسالة واحدة واضحة: «ضع `OPENROUTER_API_KEY` في `.env` أو في البيئة». |
| **البديل** | احذف مسار `~/.hermes/.env` تماماً واكتفِ بالبيئة + `.env`. أنظف لمستودع عام، ويُجبر المالك على تغيير عادته. |
| **الكلفة** | ~30 دقيقة. **$0.00** |
| **التوصية** | البديل — لا يجوز لمستودع عام أن يحمل مسار إطار خاص في مسار المفاتيح. |

---

### P2-8 — «بديل tesseract» غير موصول بمسار القراءة إطلاقاً

| | |
|---|---|
| **المحور** | M4/M8 |
| **الدليل** | `grep -rn "tesseract\|pytesseract" --include='*.py' .` → الاستعمال الوحيد في `src/statement_qa/extract_text.py:40-42`، ولا يستورده أي من `vlm_reader` / `scale_slice` / `app.process_pdf` / `footer_oracle`. (يُستورد من `statement_qa/__init__.py` و`parser.py` فقط، وكلاهما خارج مسار القراءة الحيّ.) |
| **الأثر** | ورشة «ماذا لو» №1، بند 5 من أعلى 12، تُدرج «بديل مُقاس (tesseract)» تحصيناً ضد انقطاع/تغيّر المزوّد. **التحصين غير موجود.** المزوّد نقطة فشل وحيدة بلا أي بديل، والوثيقة تقول العكس. |
| **الحل** | إما تصل `extract_text` كمسار تدهور صريح («قراءة مُتدنّية الثقة — كل الصفوف تُعلَّم غير موثّقة»)، وإما **تشطب البند من الورشة** وتُعلن أن لا بديل. |
| **البديل** | بديل حقيقي: مزوّد VLM ثانٍ (نفس بروتوكول OpenRouter، نموذج آخر) خلف مفتاح `OPENROUTER_MODEL_VLM` الموجود — يُقاس على 10 صفحات ذهبية. |
| **الكلفة** | الشطب: $0.00. البديل: 10 صفحات ≈ **$0.14** + نصف يوم. |
| **التوصية** | اشطب الآن (لأن ادعاء تحصين غير موجود أسوأ من لا تحصين)، وقِس البديل حين يصبح أولوية. |

---

### P2-9 — جسر «آخر رصيد» يسقط على 9 من 15 صياغة، منها أشهرها

| | |
|---|---|
| **المحور** | M5 |
| **الموضع** | `src/statement_qa/qa.py:27-29` (`_CLOSING_RE`) |
| **الدليل** | اختبرتُ 15 صياغة عربية معقولة: |

```
MATCH  ما آخر رصيد؟   الرصيد الختامي   الرصيد النهائي   الرصيد الحالي   وش آخر رصيد
miss   اخر رصيد          ← بلا همزة: أشيع صيغة كتابة
miss   كم الرصيد؟        الرصيد المتبقي        كم تبقى في الحساب؟
miss   بكم أقفل الحساب؟  الرصيد في نهاية الكشف  الرصيد بعد آخر عملية
miss   آخر الأرصدة       ما الرصيد في آخر صفحة؟   closing balance
```

| | |
|---|---|
| **الأثر** | البند رقم 7 في أعلى 12 من الورشة («آخر رصيد لا يجيب») أُغلق على الصياغة التي اختُبر بها. `اخر رصيد` بلا همزة — وهي الطريقة الأكثر شيوعاً في الكتابة العربية اليومية — لا تُطابَق. |
| **الحل** | تطبيع الهمزات قبل المطابقة (`آ أ إ ← ا`) + توسيع النمط ليشمل `المتبقي|نهاية الكشف|أقفل|تبقى` — وإضافة `closing balance` للإنجليزية. |
| **البديل** | لا جسر بالتعبير المنتظم: `closing_balance()` أداة موجودة أصلاً، فليُترك للنموذج أن يستدعيها ويُذكر في `AGENT_SYSTEM_PROMPT` أن أي سؤال عن رصيد بلا تحديد يعني الختامي. يزيل هشاشة regex ويضع الاعتماد على النموذج. |
| **الكلفة** | الحل: 20 دقيقة + حالات اختبار. **$0.00** |
| **التوصية** | الحل — الجسر الحتمي أصدق من نية النموذج، لكن وسّعه. |

---

### P2-10 — سقوط صامت من «الأدوات الحتمية» إلى إجابة نموذج خالصة

| | |
|---|---|
| **المحور** | M5 |
| **الموضع** | `src/statement_qa/qa.py:175-185` |
| **الدليل** | `except Exception: answer = ""` ثم `if not answer: answer = _answer_plain(...); used = []`. أي استثناء في مسار الأدوات (مهلة · خطأ مزوّد · فشل استيراد · خطأ أداة) يُسقِط التنفيذ إلى `_answer_plain` — نموذج بلا أدوات — **بلا أي إشارة للمستخدم**. و`used_row_nos` تصير فارغة ⇒ لوحة الأدلة ترتد إلى `app._evidence_rows`، التي تبني الأدلة من **استشهادات النموذج نفسه** (`_answer_refs` يحلّل نص الجواب) وإلا من أول صفحتي استرجاع. |
| **الأثر** | الادعاء المركزي للطبقة الثالثة «النموذج يستدعي ويصوغ ولا يحسب أبداً» له **باب خلفي صامت**. وفي هذا الوضع، إن هلوس النموذج استشهاداً «(صفحة 5، صفوف 10–12)» فستعرض اللوحة صفوف 10–12 من ص5 بوصفها «الصفوف التي بُني عليها الجواب» — دليل واثق على جواب ملفَّق. |
| **الحل** | اجعل السقوط مرئياً: سطر أحمر «⚠ تعذّرت الأدوات — هذا الجواب وصفي ولم يُحسب، لا تعتمد أرقامه»، و**أفرغ لوحة الأدلة** بدل ملئها من نص النموذج. |
| **البديل** | لا سقوط إطلاقاً: فشل الأدوات ⇒ رسالة عطل. أصدق وأقسى؛ يفقد الأسئلة الوصفية التي لا تحتاج أدوات. |
| **الكلفة** | ~ساعة. **$0.00** |
| **التوصية** | الحل — مع إفراغ اللوحة (الدليل الكاذب أسوأ من لا دليل). |

---

### P2-11 — `requirements.txt` بلا قفل ⇒ لا بناء قابل للتكرار

| | |
|---|---|
| **المحور** | M6 |
| **الدليل** | 14 اعتماداً كلها `>=`؛ الحدود العليا موجودة فقط على عائلة langchain. `gradio>=5.0` · `sentence-transformers>=3.0` · `faiss-cpu>=1.8` · `polars>=1.0` مفتوحة تماماً. |
| **الأثر** | المستودع عام: `pip install -r requirements.txt` بعد شهر قد يبني تطبيقاً غير الذي قيست عليه كل الأرقام. Gradio 6 يكسر الواجهة (816 سطراً بلا اختبار واحد — انظر P3-4)، وترقية `sentence-transformers` تغيّر التضمينات ⇒ **ترتيب استرجاع مختلف لنفس السؤال** بلا أي إشعار. |
| **الحل** | `requirements.lock` من `pip freeze` للبيئة المقيسة + إبقاء `requirements.txt` للمدى المسموح، وCI يبني من القفل. |
| **البديل** | حدود عليا فقط (`gradio>=5,<6` …). أرخص وأضعف. |
| **الكلفة** | الحل: ~ساعة. البديل: 10 دقائق. **$0.00** |

---

### P2-12 — `--pre-push` يتخطّى معظم الـcommits عند الدفعات الكبيرة، ويسيء وصف ما فعل

| | |
|---|---|
| **المحور** | M3 |
| **الموضع** | `tools/publish_guard.py:251-254` |
| **الدليل** | `revs = set(sorted(revs)[:200])` — الترتيب على **نص الـSHA** لا على الزمن، فالمختار عيّنة عشوائية فعلياً؛ والرسالة المطبوعة تقول «first 200 commits». |
| **الأثر** | شجرة الرأس تُفحص دائماً (`scan_tree`)، فالحالة النهائية مغطّاة. الفجوة في **commit وسيط حمل بياناً ثم حُذف** — وهو بالضبط السيناريو الذي بُني له فحص التاريخ. الأثر منخفض اليوم (48 commit) ويرتفع مع الزمن أو أول دفعة لمستودع جديد. |
| **الحل** | لا تقتطع: `--pre-push` يفحص كل الـcommits المدفوعة (فحص الـblobs مُزال التكرار أصلاً عبر `seen`، فالكلفة خطية في الـblobs الجديدة لا في الـcommits). وإن لزم سقف، فليكن زمنياً وليُقل الحقيقة. |
| **الكلفة** | ~30 دقيقة. **$0.00** |

---

## P3

| # | الضعف | الدليل | الأثر | الحل | الكلفة |
|---|---|---|---|---|---|
| **P3-1** | `norm_num` يُرجع `float`؛ ورمز `.` أو `,` مفرداً يصير **0.00 صلباً** | `norm_num(',') → 0.0` و`norm_num('.') → 0.0` (`arabic_digit_parser.py:31-32`). فحصتُ الكاش: **5 رموز نقطية فقط، كلها بالنمط القانوني `.,..`** ⇒ **صفر حالات فعلية** | كامن: خلية غير مقروءة تُقرأ نقطةً واحدة تدخل السلسلة كرصيد صفر حقيقي. الـfloat آمن عند مقادير هذا الملف لكن بلا حارس مُعلَن | اقصر صفر-النقط على أنماط ≥ رمزين تحوي فاصلاً عشرياً؛ ووثّق حدّ المقدار الذي يبقى عنده `float` دقيقاً | 30 د |
| **P3-2** | `page_summary` تُسقط الصفوف غير محسومة الاتجاه بلا ذكر | `qa_tools.py:184-189` يشترط `side in (debit, credit)`؛ وخلافاً لـ`footer_oracle.page_totals` لا تُرجع عدّ `undecided`. المقيس: صف واحد في الكوربوس (ص190، بمبلغ 0.00 ⇒ بلا أثر اليوم) | كامن: إجمالي صفحة يُعرض ناقصاً حركةً حقيقية بلا تحفّظ | أرجِع `undecided` واذكره في نص الأداة | 20 د |
| **P3-3** | `_side_match` يُطابق الكل عند فلتر غير معروف | `qa_tools.py:45` `return True  # unknown filter` | «مجموع السحوبات» بفلتر مشوّه يُرجع مجموع كل الحركات بصياغة تبدو مُفلترة (يبقى `_filter_note` أثراً) | أرجِع خطأً صريحاً للنموذج بدل التوسّع الصامت | 15 د |
| **P3-4** | **صفر اختبارات تستورد `app.py`** | 86 اختباراً موزّعة على 9 ملفات، لا واحد منها يلمس الـ816 سطراً التي تحوي P1-2 وP1-5 | التغطية تقيس **الانحدار في الوحدات**، لا **السلوك عند نقطة الاستهلاك**. البوابة الوحيدة التي تلمس الواجهة (gate 5) تحتاج API + بيانات حقيقية + خدمة حيّة ⇒ لا تعمل في CI ولا عند غريب | افصل الدوال النقية من `app.py` (`_row_record` · بناء اللافتة · `_answer_refs` · `_evidence_rows`) إلى وحدة قابلة للاختبار | نصف يوم |
| **P3-5** | كاشف الصيغة إنذار يعمل على ~11% من الصفحات | `slice_report.md`: 70+ تحوّلاً و**23 صفحة «شاذة عن جيرانها»** من 629 | «الصيغة: متغيرة» سطر لا يُتّخذ عليه قرار ⇒ يُقرأ ويُتجاهَل ⇒ لن يُلاحَظ حين يعني شيئاً (جيل طباعة جديد فعلاً) | اربط الإنذار بأثر: تحوّل صيغة **مصحوب** بارتفاع شكوك أو انزياح فوتر فقط | ساعتان |
| **P3-6** | `WARN` لا يوقف الدفع ⇒ استثناءات `name_token` الثمانية بلا أثر تنفيذي | `publish_guard.py:306` `if blocks and not args.report_only: sys.exit(1)` — الـWARN لا يُغيّر شيئاً. و`.publish-allowlist` كلها `name_token` (WARN) | القائمة توحي بسياسة استثناءات مدروسة لقاعدة **لا تستطيع الحجب أصلاً**؛ أثرها الحقيقي إخفاء مواضع اسم العائلة عن عين المشغّل في مستودع عام | قرار صريح: إما تُرفع `name_token` إلى BLOCK وتبقى الاستثناءات الثمانية مبرَّرة فعلاً، وإما يُحذف تصنيف WARN ويُستبدل بتقرير منفصل | ساعة |
| **P3-7** | `job_lock` يبتر ملف القفل قبل أن يحاول أخذه · القفل لكل نسخة مجلد | `job_lock.py:34` `open(path, "w")` يبتر قبل `flock` ⇒ الخاسر يمحو `pid=` الفائز. و`LOCK_PATH` نسبي للمجلد ⇒ نسختان من المستودع = تشغيلان متزامنان | تشخيص «مَن يشغّل الآن؟» يُمحى بالضبط عند الحاجة إليه؛ والحماية من التشغيل المزدوج تسقط عبر النسخ | `open(path, "a+")` ثم الاقتطاع **بعد** نجاح `flock`؛ وقفل بمسار ثابت على مستوى المستخدم | 30 د |
| **P3-8** | المراقب يكتب سجلاً ولا يُنذر أحداً · ولا نبض للمراقب نفسه | `tools/health_monitor.py:9` صراحةً «records the truth, changes NOTHING» — لا قناة إشعار | «موت صامت» ينتقل من الخدمة إلى المراقب: لو توقف launchd عن تشغيله توقّف السجل عن النمو ولا أحد يعلم | سطر إشعار عند تحوّل OK→DOWN (`osascript -e 'display notification'`) + فحص «آخر سطر أقدم من 30 دقيقة» في `health_check.py` | ساعة |
| **P3-9** | `refusal_test` فيه حالتا رفض حقيقيتان فقط | `tools/refusal_test.py:35-44`: A (بنك آخر) وB (رقم جوال) لهما حكم؛ C أداة؛ **D بلا حكم قاطع** (`why = "لا حكم قاطع"`). لا حالة سنة غير موجودة، ولا سؤال مركّب نصفه موجود | الورشة تسمّي «الثقة الكاذبة خارج النطاق» خطراً نامياً؛ والمجموعة لم تنمُ | أضف: سنة خارج المدى · نصف سؤال موجود · مبلغ غير موجود · سؤال عن صفحة تتجاوز الملف | ساعة + ~$0.02 |
| **P3-10** | الفيّكسترة العامة لا تستطيع إظهار أي طبقة تحقق | استخرجتُ نص `data/sample/statement_sample.pdf`: 1 صفحة · طبقة نصية · **لا أرقام هندية · لا فاصلة عشرية عتيقة · لا صف إجماليات · لا رقم صفحة** ⇒ `footer_oracle` يعود `absent` و`check_page_numbers` يعود `checked=0` | المُجرِّب يرى «قارئ جدول» لا «مُدقّق». والمولّد **اصطناعي فعلاً** (تحققت: مفردات مُبرمَجة وبذرة ثابتة ولا أثر لبيانات حقيقية) — العيب في التمثيل لا في الخصوصية. وهو بند «شكل بلا روح» من الورشة، ما زال مفتوحاً | أضف إلى المولّد: صف إجماليات تراكمي + أرقاماً هندية + رقم صفحة مطبوعاً + صفحتين على الأقل، وارسمه صورةً بلا طبقة نصية | نصف يوم |
| **P3-11** | تعليمات المعايرة لملف جديد ناقصة | `ONBOARDING_NEW_FILE.md §3` يطلب إعادة قفل قيم ص1/ص2، ولا يذكر **`f_possible >= 9`** ولا **`total >= 95`** في `qa_gate.py:112,178` | ملف جديد بعينة 10 صفحات أقل غزارة يُفشل البوابة لأسباب لا يشرحها أي دليل | اجعل العتبتين ثوابت مسمّاة في رأس `qa_gate.py` وأدرجهما في قائمة §3 | 20 د |
| **P3-12** | CI بلا `permissions:` و`actions/checkout@v5` بلا تثبيت SHA | `.github/workflows/publish-guard.yml:11-14` | صلاحيات التوكن الافتراضية أوسع مما تحتاجه مهمة قراءة؛ والوسم المتحرك يعني كوداً طرف ثالث غير مثبَّت على حارس الخصوصية نفسه | `permissions: contents: read` + تثبيت الـaction على SHA | 15 د |
| **P3-13** | لا `LICENSE` ولا `SECURITY.md`؛ الترخيص «لم يُحدَّد» | `README.md:` قسم الترخيص · `ls` الجذر | مستودع عام بلا ترخيص = **لا إذن قانوني** لأحد بالاستعمال أو حتى بالنسخ؛ وبلا `SECURITY.md` لا طريق مُعلَن للإبلاغ عن ثغرة في مشروع كل موضوعه الخصوصية | قرار المالك (انظر «بانتظار المالك») + `SECURITY.md` بسطرين | 20 د |

---

## بانتظار المالك (قرارات لا تُنفَّذ بلا إذنه)

1. **S1 — إعادة كتابة تاريخ الريبو** لإزالة الشطرين من `dc0e862`. يكسر كل clone قائم.
2. **S2 — أي البديلين:** إعلان «615 موثّقة · 6 منزاحة (خطأ إطار مؤكد بالعين)» أم `arbitrated_by` صريح؟
3. **P2-7 — حذف مسار `~/.hermes/.env`** من الكود العام (يغيّر عادة تشغيل يومية).
4. **P3-13 — الترخيص:** MIT / Apache-2.0 / «كل الحقوق محفوظة، للعرض فقط»؟ الوضع الحالي أسوأ الثلاثة قانونياً.
5. **P2-3 — حدّ أقصى لعدد الصفحات من الواجهة** (عدد؟) حتى ينتقل السقف والحاجز إلى المسار المشترك.
