# البدائل المعمارية — rajhi-rag · 2026-09-17

> لكل مكوّن: بديلان على الأقل بمقايضة صريحة (جودة/كلفة/جهد/خطر/صيانة) + **توصية** + **سبب رفض البديل**
> + **«أرخص تغيير يحل 80% من الألم»**. القاعدة الحاكمة: لا إعادة كتابة بلا كلفة مقيسة وسبب ملموس.
> أرقام الكلفة مشتقّة من القياس الفعلي: **$0.0140/صفحة · 22.18 ث/صفحة (وسيط) · $8.81 و4.42 ساعة للملف الكامل**.

---

## ١) القارئ البصري

**القائم:** VLM سحابي واحد عبر OpenRouter (`google/gemini-3.7-flash`) — `vlm_reader.py`. نداءان لكل صفحة
(صفحة كاملة + قصاصة الإطار). **نقطة الفشل الوحيدة الخارجة عن السيطرة، وكل الكلفة هنا.**

| البديل | الجودة | الكلفة | الجهد | الخطر | الصيانة |
|---|---|---|---|---|---|
| **أ · مزوّد/نموذج ثانٍ خلف نفس البروتوكول** | مُقاسة بالمقارنة | ~$0.14 للمعايرة (10 صفحات) + ازدواج مؤقت | نصف يوم (`OPENROUTER_MODEL_VLM` موجود أصلاً) | منخفض — الأوراكل يحكم على المخرَج لا على المزوّد | منخفضة: سطران ونتيجة معايرة |
| **ب · VLM محلي (Qwen2-VL 7B على Apple Silicon)** | غير معروفة على أرقام هندية عتيقة — **تُقاس أو لا يُدّعى شيء** | $0 تشغيلياً · ~15GB قرص · زمن/صفحة أعلى غالباً | 3–5 أيام | متوسط-مرتفع: الأرقام الهندية والفواصل العتيقة هي بالضبط ما يفشل فيه الأصغر | مرتفعة: نماذج وأوزان وذاكرة |
| **ج · tesseract** | **مقيسة سلفاً بأنها فاشلة** — `page_numbers.py:23` يسجّل «tesseract: 0%» على قصاصات الأرقام، و`vlm_reader.py:7` «يشوّه أرقام الجداول الكثيفة» | $0 | يوم (الاعتماد مُثبَّت أصلاً وغير موصول) | مرتفع | منخفضة |
| **د · نموذج جدول مخصّص (تدريب/ضبط)** | أعلى سقفاً نظرياً | تسمية آلاف الصفوف + تدريب | أسابيع | مرتفع: مستخدم واحد، ملف واحد، لا بيانات تسمية | مرتفعة جداً |

**التوصية: أ.** هي التحصين الحقيقي للخطر رقم 5 في الورشة (تبعية المزوّد)، وتُنفَّذ اليوم لأن المنفذ
(`chat_vlm_image`) موحَّد بالفعل والأوراكل يحكم على المخرَج بصرف النظر عن مصدره.
**رفض ج:** الفشل مقيس ومكتوب في المستودع — والاحتفاظ به في `requirements.txt` وفي الورشة كـ«بديل»
هو ادعاء تحصين غير موجود (P2-8).
**رفض د:** مستخدم واحد وملف واحد؛ لا عائد يبرّر أسابيع.
**رفض ب الآن:** مقبول كبحث، مرفوض كخطة — لا يُدَّعى شيء عنه قبل قياسه على نفس العشر صفحات الذهبية.

> **أرخص تغيير يحل 80%:** اشطب tesseract من ادعاءات الورشة والاعتماديات (20 دقيقة)، واقفل
> `OPENROUTER_MODEL_VLM` على نموذج ثانٍ في تشغيل معايرة واحد بـ**$0.14** — عندها يصير للمشروع
> بديل **مقيس** بدل بديل **مذكور**.

---

## ٢) أساس التحقق

**القائم:** أوراكل الفوتر بأساسين — تراكمي (`check_page_footer`) ودلتا (`delta_status`، أساس التقرير
منذ `308aa2f`) + خمس حالات. **المقيس: 621/629 موثّقة، وهو أقوى ما في المشروع.**

| البديل | الجودة | الكلفة | الجهد | الخطر |
|---|---|---|---|---|
| **أ · إبقاء دلتا الفوتر + إصلاح راية ×100 + مقام صادق** (P1-1, P1-2) | يرفع الحارس من نصفه إلى كامله | $0 | يوم | منخفض — لا تغيير معماري |
| **ب · مطابقة صورة (قصّ خلية الرصيد وقراءتها ثانيةً بقصاصة ضيّقة عند الشك)** | يفصل «خطأ قراءتنا» عن «شذوذ الكشف» — وهو بالضبط ما احتاجته الأزواج الثلاثة | ~$0.004/قصاصة ⇒ الصفحات الست ≈ **$0.03** | يومان | منخفض: القبول يبقى مشروطاً بإغلاق الدلتا |
| **ج · تحقق بشري مُوجَّه (طابور: الصفحة + القصاصة + الرقمان + زرّان)** | يُشرعن التحكيم البشري الجاري فعلاً ويجعله قابلاً للتتبّع (S2) | $0 | يومان-ثلاثة | منخفض — بشرط تسجيل `arbitrated_by` |
| **د · مصدر ثالث مستقل (إجمالي الكشف الكلي إن طُبع في الصفحة الأخيرة)** | حارس على كل الملف لا على كل صفحة | نداء واحد ≈ $0.014 | نصف يوم | يعتمد على وجوده في الطباعة — **غير متحقَّق منه** |

**التوصية: أ ثم ج ثم ب.** أ يُغلق فجوة موجودة بلا معمار جديد. ج يحوّل أهم ثغرة إثبات في المشروع
(S2) إلى مسار موثَّق. ب يقلّل الحاجة إلى ج بمرور الوقت.
**رفض د الآن:** لم أتحقق من وجود إجمالي كلي مطبوع؛ بناء حارس على افتراض غير مقيس هو تكرار حرفي
لخطأ «دلالات الإطار» الذي حذّر منه `ONBOARDING_NEW_FILE.md §2`.

> **أرخص تغيير يحل 80%:** إصلاح `_x100` لتُحسب على الدلتا (سطران) + مقام اللافتة (سطر) — يعيدان الحياة
> إلى حاجز التوقف الوحيد ضد السيناريو الذي سمّته الورشة «قاتل». **ساعتان، $0.00.**

---

## ٣) طبقة الأسئلة

**القائم:** RAG (e5-small + FAISS، `k=4`) + وكيل LangChain بأدوات حتمية + ارتداد صامت إلى نموذج بلا أدوات.

| البديل | الجودة | الكلفة | الجهد | الخطر |
|---|---|---|---|---|
| **أ · تحويل السؤال إلى استعلام حتمي على الدفتر** (النموذج يُخرج فلتراً مُهيكلاً؛ التنفيذ كله محلي) | أعلى بكثير على الأسئلة الرقمية: لا رقم خارج الأدوات مطلقاً | نداء واحد رخيص لكل سؤال (بلا سياق قطع) | 3–4 أيام | منخفض: الفلتر المشوّه يُرفض صراحةً بدل أن يُخمَّن |
| **ب · إبقاء الهيكل + عتبة تشابه + حالة «لا أدلة» + كسر الارتداد الصامت** (P2-10) | يغلق البند المفتوح ٣ ويُظهر باب النموذج الخلفي | $0 | يوم | منخفض جداً |
| **ج · LLM بلا استرجاع (الجدول كله في السياق)** | 5793 صفاً ≈ >1.5M رمزاً ⇒ غير عملي على ملف كامل | مرتفعة جداً لكل سؤال | يوم | مرتفع: النموذج يحسب |

**التوصية: ب الآن، أ هدفاً.** ب يغلق ثغرة حقيقية بكلفة يوم. أ هو الشكل الصحيح نهائياً لأنه يجعل
«النموذج لا يحسب» **بنيوياً** لا **تعليمة في برومبت**.
**رفض ج:** يناقض العقد المركزي للمشروع ويكلّف أكثر.

> **أرخص تغيير يحل 80%:** مرّر أحكام الفوتر إلى الأدوات وإلى نص القطع (P1-4) + اكسر الارتداد الصامت
> (P2-10). **يوم واحد، $0.00** — ويغلق أخطر ما في طبقة الأسئلة: رقم غير موثّق يصل بلا تحفّظ.

---

## ٤) الواجهة

**القائم:** `app.py` — **816 سطراً**، ملف واحد: CSS + HERO + منطق القراءة + العرض + الأسئلة + ABOUT.
**صفر اختبارات تستورده** (P3-4).

| البديل | الجودة | الجهد | الخطر | الصيانة |
|---|---|---|---|---|
| **أ · تفكيك داخل Gradio: `ui/` (تخطيط) + `pipeline.py` (القراءة) + `render.py` (الدوال النقية)** | يفتح الـ80% غير المختبَر للاختبار | 2–3 أيام | منخفض: نقل بلا تغيير سلوك | تنخفض كثيراً |
| **ب · إبقاء الملف كما هو** | — | 0 | كل عيب عرضٍ يبقى غير قابل للاختبار؛ وترقية Gradio تكسر بلا إنذار (P2-11) | ترتفع مع كل إضافة |
| **ج · واجهة خارجية (FastAPI + جبهة)** | تحكّم كامل | أسبوعان+ | مرتفع: إعادة بناء RTL والثيم والمعرض من الصفر | مرتفعة |

**التوصية: أ، ومحدودة.** لا يلزم تفكيك كامل — يكفي إخراج **الدوال النقية** (`_row_record` · بناء سطر
اللافتة · `_answer_refs` · `_evidence_rows` · `_date_cell`) إلى وحدة واحدة. هذه بالضبط مواضع P1-2 وP1-5
وP2-10.
**رفض ج:** Gradio ليس سبب أي بند في هذا السجل؛ استبداله يدفع أسبوعين ثمناً لمشكلة غير قائمة.

> **أرخص تغيير يحل 80%:** ملف `render.py` واحد يضم الدوال النقية + عشرة اختبارات عليها.
> **نصف يوم**، ويحوّل أكبر كتلة غير مفحوصة في المشروع إلى كتلة مفحوصة.

---

## ٥) التخزين والحالة

**القائم:** 629 ملف JSON لكل تشغيل + `slice_report.json` (433KB) + حالة في الذاكرة (`STATE`).
**149MB لكل ملف كشف.**

| البديل | الاستعلام | التحقيق | الجهد | الخطر |
|---|---|---|---|---|
| **أ · DuckDB/SQLite واحد (صفحات + صفوف + إطارات + أحكام)** | SQL مباشر: «أرِني كل صفحة ok بمكوّن واحد» تصير سطراً | ممتاز: التحقيقات التي كتبتُ لها سكربتات اليوم تصير استعلامات | يومان | منخفض: الكاش يبقى مصدر الحقيقة والقاعدة مشتقّة منه |
| **ب · إبقاء JSON + فهرس مشتق (`index.parquet`) بجوار التقرير** | جيد لـPolars الموجود أصلاً | جيد | نصف يوم | منخفض جداً |
| **ج · إبقاء الوضع** | يتطلب سكربت لكل سؤال | ضعيف — وهو سبب فخّ `raw_rows` بلا `side` الموثّق | 0 | يتكرّر الفخّ مع كل محلّل جديد |

**التوصية: ب الآن، أ عند ثاني ملف.** ب يحل مشكلة التحقيق بنصف يوم. أ يصير مبرَّراً حين يصبح
السؤال «قارن الملف الأول بالثاني».
**رفض ج:** الفخّ الموثّق (كاش بلا `side` ⇒ أصفار ⇒ استنتاجان خاطئان) **بنيوي في هذا التمثيل**، وقد أوقع
المنفّذ مرة، وحذّرني منه التكليف — أي أنه يتكرّر.

**الأرشفة (149MB/ملف):** يبقى — `results/*.json` (الحقيقة القابلة لإعادة الاشتقاق، ~5MB) و`slice_report.json`.
يُهمَل — **`pages/*.png` و`slice_*.pdf` (≈144MB، >96%)**، فهما قابلان للتوليد من الأصل بـ`pdf2image` بلا أي نداء.
> **⚠ شرط:** الصور اليوم هي المصدر الوحيد لأي إعادة قراءة أو تحقق بصري. لا تُحذف إلا بعد التأكد من بقاء
> الـPDF الأصلي وأن `--dpi 200` يعيد إنتاج الصور نفسها بكسلاً ببكسل.

> **أرخص تغيير يحل 80%:** `tools/to_parquet.py` (نصف يوم) + قاعدة أرشفة مكتوبة في `OPERATIONS.md`.

---

## ٦) التشغيل

**القائم:** ثلاث خدمات launchd (تطبيق · تحويل `:7867` · فحص صحة كل 10 دقائق) — **دائمة لمستخدم واحد**.

| البديل | الملاءمة | الكلفة | الجهد | الخطر |
|---|---|---|---|---|
| **أ · إبقاء launchd + إشعار حقيقي ونبض للمراقب** (P3-8) | عالية: المستخدم واحد والسطح محلي | $0 | ساعة | منخفض |
| **ب · تشغيل عند الطلب (سكربت واحد يفتح ويغلق)** | يزيل «الموت الصامت» من جذره | $0 | نصف يوم | يفقد «الرابط الحيّ دائماً» الذي اعتاده المالك |
| **ج · خدمة مُدارة (Docker/سحابة)** | **يناقض قاعدة المشروع الأولى** — لا بيانات حقيقية تغادر الجهاز | تشغيلية دائمة | أيام | مرتفع: يفتح مساراً لرفع كشف حقيقي إلى سطح بعيد |

**التوصية: أ.** المشكلة ليست في launchd بل في أن المراقب **لا يُنذر** ولا نبض له.
**رفض ج قاطعاً:** قاعدة الخصوصية غير قابلة للتفاوض؛ وأي معمار يجعل رفع كشف حقيقي إلى سطح بعيد
**ممكناً** يجب أن يُرفض قبل أن يصير **سهلاً**.
**رفض ب:** يحل مشكلة ثانوية ويُفقد راحة يومية مُثبتة.

> **أرخص تغيير يحل 80%:** سطر إشعار عند تحوّل OK→DOWN + تأكيد في `health_check.py` أن آخر سطر في
> `rajhi-health.log` أحدث من 30 دقيقة (نبض المراقب نفسه). **ساعة، $0.00.**

---

## ٧) خفض الكلفة — مرتّباً بالأثر ÷ الجهد

**خط الأساس المقيس:** $8.81 · 4.42 ساعة · 1,605,314 رمز إدخال (≈2552/صفحة) · نداءان لكل صفحة.

| # | الإجراء | الوفر المتوقع | الخطر على الدقة | الجهد | ملاحظة |
|---|---|---|---|---|---|
| 1 | **تخطّي نداء الإطار على صفحة بلا إطار** (تُعرف من الصفحة السابقة أو بكشف حبر محلي) | 4 صفحات هنا؛ يرتفع كثيراً على ملفات أرث | **صفر** — الحالة `absent` أصلاً | نصف يوم | لا يمسّ أي برومبت مجمّد |
| 2 | **توازٍ 3–4 صفحات** | الزمن من 4.4 س إلى **~1.2 س**؛ الكلفة كما هي | يحتاج انتباهاً: السلسلة والفوتر تراكميان ⇒ اقرأ بالتوازي و**اشتقّ بالتسلسل** | يوم | القيد الحقيقي حدّ معدّل المزوّد لا التصميم |
| 3 | **تخزين البادئة (prompt caching)** للبرومبت المجمّد | البرومبت ثابت عبر 629 نداءً ⇒ وفر ملموس في رموز الإدخال | صفر | نصف يوم | يعتمد على دعم المزوّد — **لم أتحقق** |
| 4 | **تخطّي الصفحات الفارغة/شبه الفارغة** محلياً قبل النداء | يعتمد على الملف — **غير مقيس هنا** | منخفض إن كانت العتبة محافظة | نصف يوم | قِس أولاً: كم صفحة حبرها تحت العتبة؟ |
| 5 | **إصلاح استدراك المرساة المتكرر** (P2-2) | نداءان لكل replay | صفر | 20 دقيقة | أرخص بند في القائمة |
| 6 | خفض الدقة (DPI) للنطاقات «الواعدة» | غير مقيس | **مرتفع** — الأرقام الهندية العتيقة هي أول ما يضيع | يوم | **لا يُجرَّب قبل 1–3** |

**التوصية:** 5 ← 1 ← 3 ← 2. البند 6 **يؤجَّل**: المشروع كله قائم على أن الرقم المطبوع يُنسخ حرفياً،
وخفض الدقة يهاجم هذا الأساس مباشرةً مقابل وفر غير مقيس.
