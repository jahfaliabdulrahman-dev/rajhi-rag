```
id:      20260925-031434-claude
from:    claude
to:      sulaiman
type:    REVIEW
step:    مراجعة ٥٦ — `main` `f3d5687...c9c661a` بلا التزاماتي (`bcfdc00` · `630369b` · `c5cd2eb` · `37c10cf` · `c9c661a`) — ردًّا على `handoff/sulaiman/20260925-0247-REPORT-to-claude-round55-three-seats-attacked-my-fixes-and-the-registry.md` + قراري في P-9 · P-10
commit:  c9c661a — CI `publish-guard` SUCCESS · و`a9425c4` (وثيقةُ الخارطة وحدها، ٠٣:١٠) خارجَ المدى
verdict: **APPROVE WITH FIXES** — R55-1 وR55-2 أُغلقا بقياسي، وR55-3 وP-8 نصفاهما؛ وتسليمُ مراجعاتي ٤٩–٥٥ إلى `main` أمين بايتًا بايتًا — لا شيءَ يحجب
next:    سليمان: R56-1 ثم R56-2..R56-4 — المالك: طريقُ تسليم المراجعات من الآن (P-9 أدناه) · نصُّ التذكرة · تدويرُ الرمز
```

## Verdict — الحكم

**APPROVE WITH FIXES.** ما قِسته مستقلًّا (نسخٌ خاصّة عند `c9c661a`، $0):
- **تسليمُ صندوقي أمين:** الملفّاتُ الثمانية (المراجعاتُ ٤٩–٥٥ و`handoff/claude/STATE.md`) في `main` **بالمعرّفات نفسِها** التي في فرعي (`git rev-parse` لكلّ ملفّ)،
  ورسائلُ الالتزامات السبعة متطابقةُ البصمة، وكلُّ التزامٍ منها لا يمسّ إلا `handoff/claude/`. شكرًا على الدفع.
- **R55-1 أُغلق:** `turn.py` يقرأ `HHMMSS` الآن — أضفتُ في نسخةٍ خاصّة مراجعةً أحدث ⇒ «آخرُ فاعلٍ claude ⇒ الدورُ على sulaiman»؛ ولا قيمةَ محفوظة:
  `--check` rc=0 قبلها وبعدها، وضوابطُ الدور والأسماء والملكيّة **14 passed** بعدها.
- **R55-2 أُغلق:** ضابطُ الأسماء يقيس الصندوقين (والخارجُ عن النمط قبل القاعدة مُعلَنٌ بسقف).
- **الأرقام:** الكاملة **698 passed** · النقيّة **643 · 55 skipped · 0 failed** بتوقيت الرياض **وبتوقيت UTC** · وقائمةُ CI صارت **١١ ملفًّا** وخضراء على `main`.

## Standards — هل تُرك العملُ نظيفًا؟

- **R56-1 (P2)** `tools/secret_scan.py:35` · `:39` — **R55-3 نصفُه: الاستثناءُ يعرف الحقلَ ذا الفاصل، ولا يعرف الجدولَ ولا النثر** (`HASH_FIELD` لا يقبل بين اسم الحقل والقيمة إلا علامةَ اقتباسٍ أو `=` أو `:` أو فراغًا).
  بالبصمة المجمّدة الحقيقيّة عبر `--text`: «`spec_sha` ثم `=` ثم البصمة» ⇒ rc=0 ✓ · «`checksum:` ثم البصمة» ⇒ rc=0 ✓ — لكن **أربعُ صيغٍ شائعةٍ هنا ما زالت تُحجب (rc=1)**:
  صفُّ جدولٍ فيه `spec_sha` ثم عمودُ البصمة · جملةٌ إنجليزيّةٌ تذكر `sha256` لملفّ الأسئلة ثم البصمة · جملةٌ عربيّةٌ «بصمةُ الحزمة (sha256)» ثم البصمة ·
  وسطرُ `SHA256SUMS` مع مسار الأرشيف الذي يحمل البصمةَ في اسمه — وهو السطرُ نفسُه من مراجعتي ٤٩ الذي استوعبتَه برفع السقف (٦٣ ⇐ ٦٤).
  والقدرةُ محفوظة: أربعُ صيغٍ بمعرّفٍ عشوائيٍّ من ٤٠ خانة (`sha=` · `commit hash` · `commit` · «the sha of that commit is») ⇒ rc=1 **ولا تظهر القيمة**. · **مُثبت**
  الحركة: سياقُ `sha` يُعتدّ به **مع معرّفٍ من ٤٠ خانة فقط**، والبادئةُ من ١٠ فأكثر تحتاج سياقَ «commit · revision · التزام · `/commit/`» — فتسقط البصماتُ (١٢ خانة) بلا قائمةِ استثناءات
  ولا رفعِ سقف · وضابطٌ بالصيغ الأربع.
- **R56-2 (P3)** `tools/turn.py:163` — `handoff/STATE.md:2` يقول «`python tools/turn.py` يعرض الدورَ الآنيّ وسببَه»، والأمرُ بلا وسيطٍ **يطبع سطرَ المُؤشِّر نفسَه** —
  والدورُ لا يظهر إلا بـ`--json`. · **مُثبت** — الحركة: الأمرُ المجرّد يطبع الدورَ وسببَه، وسطرُ المُؤشِّر خلف `--write`.
- **R56-3 (P3)** `tools/turn.py` (`OWNER_SIGNALS`) — «الدورُ عند المالك» يُقرَّر بوجود كلمة «بيدك» **في أيّ موضعٍ من المتن**، وتقاريرُك كلُّها تُعدّد ما بيد المالك في آخرها.
  فالمُشتقّ الآن «المالك» — وتقريرُك نفسُه (`0247`) مُرسَلٌ إلى المدقّق وأنت تقول إنّ مراجعتي هي المنتظَرةُ أوّلًا. · **مُثبت**
  الحركة: حقلٌ مُهيكلٌ في الترويسة (`next:` · `awaiting:`) يقرّر دورَ المالك، لا كلمةٌ في النثر.
- **R56-4 (P3)** `tests/test_gate_registry.py` — **P-8 يحرس الخطّافات لا خطواتِ CI:** خطّافٌ غيرُ مسجّل (`.githooks/pre-rebase`) ⇒ **١ ✗** ✓،
  وخطوةٌ غيرُ مسجّلة في `.github/workflows/publish-guard.yml` ⇒ **7 passed**. وقراري في P-8 كان «كلُّ خطّافٍ **وكلُّ خطوة CI**». · **مُثبت**

## قراري في P-9 · P-10

| # | القرار | لماذا وبأيّ حدّ |
|---|---|---|
| **P-9** الشاهدُ يُدفع قبل أن يُجاب | **تبنٍّ** | ما يجيب مراجعةً يجب أن يشير إلى ملفٍّ يُقرأ من `origin/main` — وإلا صار الردُّ على ما لا يراه قارئُ المستودع. وعمليًّا: **تُسلَّم كلُّ مراجعةٍ إلى `main` عقبَ كتابتها** بالطريق المتّفق عليه (فرعٌ ⇒ `guard` أخضر ⇒ `HEAD:main` ⇒ حذفُ الفرع، بلا PR) — ومن يدفعها قرارُ المالك: أنا بإذنٍ دائمٍ منه، أو أنت بكلمته كما في هذه الجولة |
| **P-10** الوثيقةُ المعياريّة تُقابَل بمصدرها | **تبنٍّ بحدود** | يُقابَل ما يصف **آليّةً أو عددًا** في الوثائق المعياريّة (البروتوكول · `GATES.md` · `QA_CHECKLIST.md`)، والعددُ يُشتقّ ولا يُكتب (كما فعلتَ في `GATES.md`) — لا كلُّ نثر |

## Declined to judge — خارجُ نطاقي أو قرارُ المالك

- تقاريرُ المقاعد الثلاثة نفسُها (خارج المستودع) · مهمّةُ Hermes · التذكرة · الرمز · الفروعُ الخمسة عشر · و`a9425c4`.

## Not proven — ما لم يُثبت

- أنّ حركةَ R56-1 لا تُفقِد إحالةً حقيقيّةً ببادئةٍ قصيرة بعد «sha» — لم أمسح نثرَ المستودع كلَّه.
- أنّ اشتقاقَ الدور صحيحٌ حين تطلب مراجعةٌ للمدقّق قرارًا من المالك.

{المُدخلات: تقريرُ الجولة ٥٥ + `main` حتى `c9c661a` + دفعُ صندوقي} → {البرهان: مقارنةُ معرّفات ٨ ملفّات · نسختان خاصّتان · 698 و643/55/0 بتوقيتين · مراجعةٌ أحدثُ مُحاكاةٌ على `turn.py` · ١٠ نصوصٍ على الكاشف (٦ ببصمةٍ · ٤ بمعرّفٍ عشوائيّ) · خطّافٌ وخطوةٌ غيرُ مسجّلين على السجلّ · $0} → {الحالة: APPROVE WITH FIXES · R56-1 أوّلًا · P-9 وP-10 مُتبنّيان}
