id:      20260917-214008-claude
from:    claude
to:      sulaiman
type:    REVIEW
step:    مراجعة تنفيذ البوابات الأربع + العنقين — `20260917-212222-audit-request.md`
commit:  6f56405438f105e79c8823bb69c911bab4c56ca1   (فُحص على `16e2fa4` = origin/main، وهو `6f56405` + وثيقة الطلب فقط)
verdict: REQUEST_CHANGES
evidence:
  - `git worktree add --detach <tmp> origin/main` ثم من داخله:
  - `pytest tests/ -q` (بلا الكاش) → `168 passed, 1 skipped` · **مع** الكاش → `169 passed in 1.40s` ✓
    (المتخطّى: `test_public_claims.py:22` — «لا تقرير مقيس على هذه الآلة». رسالتك تقول `169 passed`؛
     الأدقّ أن تذكر أنه **168+1 متخطّى** على أي آلة بلا كاش، وهي حالة CI الدائمة.)
  - `python3 tools/publish_guard.py --tree --history` → `tree: 99 · history: 504 · messages: 62 — 0 BLOCK / 1 WARN` ✓
    (الحارس صار يفحص **رسائل الالتزام** — كما طُلب. والـWARN هو الذي أعلنتَه.)
  - **بوابة 0 مُثبتة آلياً:** مسحتُ كل كائنات المستودع بحثاً عن السلسلة القديمة:
    `rev-list --objects --all` (504 كائناً) + `cat-file --batch` + رسائل الالتزام والمؤلفين + `logs/HEAD`
    + `packed-refs` + `ORIG_HEAD` → **0 ظهور في كل موضع**. سؤالك ٥: **لا أثر باقياً.**
  - `tools/verify_provenance.py --results …` → `حقول: 1875 · تحكيم موثَّق: 3 · غير مبرَّر: 0 · ناقص: 0` ✓
  - `tools/group_verify.py --results …` → `أزواج إطارات: 625` · `verified 622 · mismatch 2 · undecidable 1`
  - إعادة اشتقاق مستقلة من `group_verification.json`: نطاق 427 = `−2390.50 / −3025.00`
    (يطابق `suspects_log.md` حرفياً) · نطاق 626 = `−1851.98 / −2671.00` (يطابق رسالتك حرفياً) ✓
  - إعادة اختبار راية ×100: صفحة 1 `True` · صفحة N تراكمياً `True` · **دلتا `True`** ·
    وبعد انقطاع أعلى المجرى `True` ⇒ **حاجز التوقف يُطلق** ✓ (كان ميتاً)
  - `summarize_page_numbers`: 3/600 → «⚠ تغطية ناقصة — لا حكم على التسلسل» · 0/629 → «لم يُقرأ» ✓
  - لافتة الواجهة على أحكام الـ629: `تحقق الفوتر: 621/621 مطابق (دقة)، وتغطية 621/629` +
    `تغطية التحقق: 621/629 (98.7%) — والباقي مسمّى بالاسم` ✓ (كانت «621/621 مطابق» وحدها)
  - إعادة تشغيل حمولات الهجوم الـ13 على الحارس (جدول كامل في الملحق أدناه)
  - **الكلفة السحابية لهذه المراجعة: $0.00** — لم أشغّل البوابة ٢ ولا `refusal_test` (الدرجة «ج»).
    الحكم لا يتوقف عليهما: البنود أدناه كلها مُثبتة محلياً، وREQUEST_CHANGES لا يحتاج تشغيلاً مدفوعاً.

findings:

## ⛔ ١ — البوابة تبتلع أشهر سؤال في المجال: «ساب» تُطابَق داخل «حِساب»

جواب سؤالك ٣ («أعطني جملة عربية واحدة تسقطها اختباراتُها») — عندي ستّ، وكلها من نفس الجذر.
`OTHER_BANKS` تحوي `"ساب"`، والمطابقة **جزئية على النص**، و`"ساب"` جزء من `"حساب"`:

```
out_of_scope  <- سؤال عن ساب  | ما رصيد الحساب؟
out_of_scope  <- سؤال عن ساب  | كم مجموع مسحوبات الحساب؟
out_of_scope  <- سؤال عن ساب  | متى فُتح الحساب؟
out_of_scope  <- سؤال عن ساب  | ما أعلى رصيد في الحساب؟
out_of_scope  <- سؤال عن ساب  | اعرض حركات الحساب في 2019
out_of_scope  <- سؤال عن ساب  | كم عدد حركات هذا الحساب؟
```

والنص المعروض للمستخدم **يكذب على الوثيقة**:
«هذا الكشف يخص مصرف الراجحي فقط، ولا يحتوي أي بيانات عن ساب. لا يمكنني الجواب عن حساب في مصرف آخر.»

**ولماذا لم تمسكها اختباراتك الخمسة عشر؟** لأن **كل** اختبار in_scope يستعمل «حساب» يذكر «الراجحي» معه،
و`OUR_BANK` يتقدّم على المطابقة:

```
in_scope                      | كم رصيد حسابي في بنك الراجحي؟     ← اختبارك (السطر 47)
out_of_scope  <- سؤال عن ساب  | كم رصيد حسابي؟                    ← نفس السؤال بلا اسم البنك
```

**والأثر حيّ:** `src/statement_qa/qa.py:259` يستدعي `classify` داخل `answer_question` ⇒ المسار الذي
تسلكه الواجهة. والقرار **قبل النموذج** ⇒ لا استرداد ولا تخفيف.

وليست «ساب» وحدها — كل رمز قصير يصطدم بكلمة عادية أو باسم تاجر/مدينة حقيقي في الأوصاف:

```
out_of_scope  <- سؤال عن الرياض      | كم سحبت من الصراف في الرياض؟      ← الرياض مدينة، والأوصاف تحمل المدن
out_of_scope  <- سؤال عن الخليج      | كم دفعت في مطاعم الخليج؟
out_of_scope  <- سؤال عن الجزيرة     | كم أنفقت على الجزيرة للطيران؟
out_of_scope  <- سؤال عن العربي      | ما مجموع تحويلات البنك العربي؟
out_of_scope  <- سؤال عن الاستثمار   | ما مجموع رسوم الاستثمار؟
out_of_scope  <- الجوال              | كم دفعت لفواتير الجوال؟           ← سداد فاتورة الجوال صفّ حقيقي
```

docstring الوحدة نفسها يقول: «A gate that swallows real questions is worse than no gate at all».
هذا هو الحكم، وقد تحقّق.

**المطلوب:** مطابقة بحدود كلمة لا بسلسلة جزئية (`\b` أو تطبيع + قائمة رموز كاملة)؛ واشتراط قرينة بنك
(«بنك» / «مصرف» / «حسابي في») قبل أي رمز قابل للالتباس؛ وإخراج المدن من قائمة البنوك؛ واستثناء
«الجوال» حين تسبقه «فاتورة/سداد/دفع». **واختبار in_scope واحد لكل كلمة ملتبسة بلا ذكر «الراجحي»** —
هذه هي البقعة العمياء التي أنتجت البند.

## ⛔ ٢ — إصلاح S1 نقل النمط إلى الصيغة الوحيدة التي لا يراها `concat_digits`

القاعدة الجديدة تمسك `"a" + "b"` فقط. والملف المُصلَح انتقل إلى `"".join(_PARTS)` — وعلّق على ذلك
بأنه ميزة: «Deliberately no literal concatenation».

```
BLOCK:concat_digits  | A = "AAAAAA" + "BBBB"          ← الصيغة القديمة (مُغلقة ✓)
PASSES               | _PARTS = ("AAAAAA", "BBBB")    ← الصيغة التي يستعملها المستودع الآن
PASSES               | A = "".join(("AAAAAA","BBBB"))
PASSES               | A = "".join(["AAAAAA","BBBB"])
PASSES               | A = f"{a}{b}"
```

**القيمة اليوم اصطناعية وتحققت منها** (0 ظهور للقديمة في كل الكائنات). لكن الملف الذي وقع فيه
التسريب صار **خارج حماية القاعدة التي وُلدت منه**، وتعليقه يشرح المخرج. أي استبدال لاحق للأجزاء
بقيم حقيقية يمرّ بصمت — وهذا حرفياً ما حدث في `dc0e862`.

**المطلوب:** افحص الرموز لا السطر — `ast` على كل ملف `.py` مُتتبَّع: أي تعبير ثابت يُنتج ≥10 رقماً
بعد الطيّ (`+` · `join` · `f-string` على ثوابت) = BLOCK. أو، الأبسط والأصلب: **امنع بنية «رقم مُجزّأ»
في `tests/` نهائياً** واجعل fixtures الاختبار تُولَّد من `make_synthetic_statement.py`.

## ⚠ ٣ — `group_verify` يناقض الملف الذي كتبه للتوّ (نفس شكل العيب الذي أغلقتَه في `slice_report.md`)

سطر الحكم يطبع:

```
الحكم: 6 صفحة كانت «غير قابلة للتحقق» صارت موثّقة بالمجموع؛ 0 نطاق بنقص مقيس (أوراق غائبة من المسح)؛ 1 غير محسوم
```

و`group_verification.json` الذي كتبه **في نفس التشغيل** يحمل:

```json
{"pages":[427],"bracket":[426,427],"status":"mismatch","diff_debits":"-2390.50","diff_credits":"-3025.00"}
{"pages":[626],"bracket":[625,626],"status":"mismatch","diff_debits":"-1851.98","diff_credits":"-2671.00"}
```

⇒ **«0 نطاق بنقص مقيس» بينما النقصان مقيسان ومخزّنان.** وهذان الرقمان هما أثمن ما في الأداة.

وثانياً: «6 صفحة **كانت غير قابلة للتحقق**» — لكن ص173 حكمها الأصلي `ok` (تحققتُ من `slice_report.json`).
العدّ الصحيح **5** كما كتبتَ في رسالتك يدوياً. أي أن الأداة تُضخّم، وأنت صحّحتها بيدك — وهذا يعمل مرة
ولا يعمل دائماً.

**المطلوب:** سطر الحكم يُشتق من نفس القاموس الذي يُكتب في JSON، لا من عدّاد موازٍ؛ و«صارت موثّقة»
تُحسب على الصفحات التي **لم تكن** `ok`.

## ⚠ ٤ — سجلّ التحكيم يسجّل «تغيّر من X إلى X»

الجوهر ممتاز: `by` · `why` · `at` · `raw_token` · `raw_parsed` · `evidence`. لكن الحقلين الأقرب
لقراءة آلية **متطابقان** في الثلاثة:

```
pg491: "old_value": "868353.27", "new_value": "868353.27"   (والصحيح: old = 248850.42)
pg564: "old_value": "972147.67", "new_value": "972147.67"
pg616: "old_value": "291783.40", "new_value": "291783.40"
```

القيمة قبل التحكيم نجت في `legacy_footer_reread` و`raw_parsed` فقط. (والأداة تطبع الفرق صحيحاً —
لأنها تقرأ `raw_parsed` لا `old_value`.) ⇒ من يقرأ السجل آلياً يرى «لا تغيير».
**المطلوب:** `old_value = raw_parsed`؛ و`quantize` على `raw_parsed` (ص616 مخزّن `874128.5` بمنزلة واحدة).

## ⚠ ٥ — ادعاء نطاق قديم ما زال حيّاً في السطح الذي يراه الغريب

`app.py:791` (ABOUT، التبويب الثالث): «العيّنة المقيسة **10 صفحات (≈1.6% من ملف 629 صفحة)**».
و`render_claims` يحرس README (3 أرقام) وPLAN (4) وQA_CHECKLIST (1) — **ولا يحرس `app.py`**.
⇒ «كل رقم مُعاد اشتقاقه» صحيح للوثائق، غير صحيح للواجهة. **المطلوب:** أدخِل ABOUT في `claims()`.

---

## أجوبة أسئلتك الستّ الباقية (باختصار، وبلا مجاملة)

**١ · `ungrounded`: وسم أم رفض؟** — **رفض**، على المسار الرقمي وحده. تقرير يُقدَّم لطرف ثالث لا يحتمل
رقماً موسوماً: الوسم يُقرأ مرة ويُنسى، والقرار يُبنى على الرقم. أبقِ الوسم للأسئلة **الوصفية** حيث لا
رقم أصلاً. القاعدة العملية: «إن كان الجواب يحمل رقماً ولم تمرّ به أداة ⇒ لا يُعرض».

**٢ · هوية النطاق: `ok` فقط؟** — نعم، ورفضك `mismatch` كمرجع صحيح: مرجع منزاح ينقل الانزياح إلى كل
ما يحيط به. لكن انتبه إلى `gap`: إطار صفحة الفجوة **سليم** والنقص فيما بينهما — إن استبعدتَه كمرجع
فقدتَ 4 نطاقات صالحة بلا سبب. أمّا «نطاق يشمل صفحة بلا صفوف»: له معنى واحد فقط — **إثبات أن الصفحة
فارغة فعلاً** (الفرق صفر). وهذا يستحق حالة مسمّاة `empty_confirmed` لا أن يُخلط بـ`verified`.

**٣ · أين تبتلع البوابة سؤالاً حقيقياً؟** — البند ١ أعلاه. ستّ جمل، أولها «ما رصيد الحساب؟».

**٤ · `test_public_claims`: يُبقى أم يُحذف؟** — **يُبقى.** الـchurn الذي تخشاه هو الغرض: الرقم يتغيّر
فيُجبرك الاختبار على تحديثه، وهذا بالضبط ما فشل يدوياً أربع مرات. لكن عالِج ضعفه: `skip` يجعله معطّلاً
في CI إلى الأبد. اجعل `render_claims.derive()` يكتب `docs/claims.json` (مشتقاً، مُتتبَّعاً، بلا PII)
ويقارن الاختبار الوثائق به ⇒ يعمل في CI بلا كاش، والكاش يجدّد الملف لا الاختبار.

**٥ · أثر باقٍ بعد ثلاث تمريرات؟** — **لا.** 504 كائناً + الرسائل + المؤلفون + `logs/HEAD` +
`packed-refs` + `ORIG_HEAD` = 0. تنبيه واحد: `.claude/worktrees/rajhi-rag-deep-audit-91bd79/`
كان يحمل في فهرسه (index) تعديلاً معلّقاً يُعيد القيمة القديمة إلى `tests/test_publish_guard.py` —
بقية من إعادة الكتابة. لم يُلتزم به ولم يُدفع، لكنه **موجود على القرص الآن**. احذف الـworktree
(`git worktree remove` ثم `prune`) بعد اعتماد هذه المراجعة.

**٦ · «غير محسومة» أم «معلومة النقص»؟** — الاثنتان، لأنهما حالتان مختلفتان ولا يصحّ توحيدهما:
ص427 وص626 = **«نقص مقيس: كذا مديناً وكذا دائناً من أوراق غائبة من المسح»** (تعرف الكمّ، لا التفاصيل).
ص629 = **«غير محسومة: لا مرجع بعدها»** (لا تعرف شيئاً). التقرير القضائي يحتمل الأولى ويرفض خلطها بالثانية.

**٧ · ما يستحق الحذف؟** — `tools/arbitrate_footer.py`. ثلاثة تحكيمات في تاريخ المشروع كله، وكلها
هُجّرت يدوياً؛ الأداة تضيف مساراً للكتابة في الكاش لا يستعمله أحد. أبقِ `verify_provenance.py` (يقرأ
ويحكم — قيمته دائمة) واحذف الكاتب. وراجع `tools/to_parquet.py` بنفس المعيار: إن لم يُستهلك الفهرس في
أي مسار، فهو دَين لا أصل.

---

## ملحق — إعادة تشغيل حمولات الهجوم الثلاث عشرة على الحارس

```
BLOCK:long_digits    | IBAN بمسافات            ← كان يمرّ ✓ أُغلق
BLOCK:long_digits    | بطاقة بشُرَط             ← كان يمرّ ✓ أُغلق
BLOCK:long_digits    | بطاقة بمسافات           ← كان يمرّ ✓ أُغلق
BLOCK:long_digits    | حساب هندي مفصول         ← كان يمرّ ✓ أُغلق
BLOCK:long_digits    | سلّم أرقام يُعمي السطر   ← كان يمرّ ✓ أُغلق (finditer)
BLOCK:opaque_binary  | xlsx · zip · sqlite · tar.gz  ← كانت تمرّ صامتة ✓ أُغلقت
BLOCK:concat_digits  | "a" + "b"               ← جديد ✓
PASSES               | "".join((...))          ← ⛔ البند ٢
PASSES               | صف كشف حقيقي             ← ما زال مفتوحاً (لم تدّعِ إغلاقه)
PASSES               | export/ledger.csv        ← ما زال مفتوحاً
```

الصف الحقيقي والـCSV ليسا خلفاً في الوعد — لم تعدني بهما. لكنهما يبقيان **أرجح تسريب فعلي**
(لصق ثلاثة صفوف في وثيقة أو issue)، وقد اقترحتُ لهما «بصمة صف كشف» في P1-6/البديل أ.

---

## الحكم

**REQUEST_CHANGES.** وهذا ليس تقليلاً من الدفعة: بوابة 0 مُغلقة **بالبرهان لا بالادعاء**
(0 أثر في 504 كائناً + الرسائل + الـreflog)، وS2 صارت لها آلية إثبات حقيقية، وراية ×100 صارت تعمل
في المسارين وبعد الانقطاع، والتغطية صارت مفصولة عن الدقة، وخمسة من ثقوب الحارس الستّة أُغلقت،
و169 اختباراً خضراء، والأرقام التي ادّعيتها في الرسالة **طابقت إعادة اشتقاقي المستقل**، بل كانت
أدقّ من سطر الحكم في أداتك (البند ٣).

المانع بند واحد: **بوابة النطاق ترفض «ما رصيد الحساب؟»**. بوابة حتمية قبل النموذج تعني أن العيب
حتميّ أيضاً — يقع في كل مرة، على أبسط سؤال، بلا استرداد. والبند ٢ يعيد فتح الباب الذي أُغلق للتوّ.
البنود ٣–٥ تُحلّ في ساعة مجتمعة.

next:    عالِج ١ و٢ (مانعان) و٣–٥ (سريعة)، وأضف اختبار in_scope واحداً لكل كلمة ملتبسة **بلا** ذكر
         «الراجحي» — فبقعة الاختبار العمياء هي ما أنتج البند ١. لا تُشغّل البوابة ٢ ولا `refusal_test`
         لأجل هذه الجولة: التغييرات في `scope.py` و`publish_guard.py` لا تمسّ مسار القراءة، والاختبارات
         المحلية تكفي للإثبات. سأراجع على الـsha التالي.
