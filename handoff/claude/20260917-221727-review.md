id:      20260917-221727-claude
from:    claude
to:      sulaiman
type:    REVIEW
step:    مراجعة `3dda7e8` — ملخّص group_verify · تجنيب الـsha · وخطة §٣ لمانع join
commit:  3dda7e8   (والعلاج في `d5c4c81` — انظر «تجاوز الحدّ» أدناه)
verdict: REQUEST_CHANGES
evidence:
  - معزول: `git worktree add .claude/worktrees/review-gate5 -b claude/review-gate5` + `git pull --rebase`
  - `pytest tests/ -q` → `172 passed` · وفي شرط CI (بلا كاش) → `172 passed` ✓
  - `publish_guard --tree --history` على `3dda7e8` → `103 · 547 · 69 — 0 BLOCK / 1 WARN` ✓
  - `group_verify --results …` → `2 نطاق بنقص مقيس` — مطابق لسطرَي `✗ نقص مقيس` وللـJSON ✓
  - **مقارنة قبل/بعد على بيانات حقيقية:** حمّلتُ نسخة الحارس من `b4f09c0` ومن `3dda7e8`، ومرّرتُ
    عليهما **كل سطر وصف في الكشف يحمل شوطاً رقمياً ≥24** (عددها 31):
    `BLOCK قبل → PASSES بعد = 30 من 31`
  - مصفوفة هجوم من 11 حمولة على `_in_hex_token` و6 صيغ تجزئة رقمية
  - **الكلفة السحابية: $0.00**

findings:

## ✅ ما أُغلق

**البند ٢ (ملخّص `group_verify`) مُغلق.** الحكم صار `2 نطاق بنقص مقيس` مطابقاً للسطرين المطبوعين
فوقه وللـJSON، والعدد المبهم صار رقمين مفصولين («6 صفحة داخل نطاقات … 3 منها بلا إطار خاص بها»).
لا تناقض داخلي بقي.

**البند ٣ (رسالة التسليم) مُغلق** — هذه الدفعة وصلت بترويسة وأدلة وقائمة، و`STATE` صُحّحت.

**وقرار الإرجاع صحيح.** «قاعدة لا تنجح مع عقدها ليست تحسيناً» — هذا هو الحكم الصائب، وشجرة حمراء
كانت ستكون أسوأ. الخطأ لم يكن في القاعدة بل في **الترتيب**: وُسِّعت قبل أن تُعاد قيم الاختبار.

---

## ⛔ ١ — تجنيب الـsha فتح ثقباً أوسع ممّا سدّ: **الأرقام نفسها محارف سداسية**

هذا خطئي أنا قبل أن يكون خطأك: كتبتُ «استثنِ شوطاً محاطاً بسياق سداسي عشري»، ونُفِّذ نصّي حرفياً.
لكن `HEX_CHARS = "0123456789abcdefABCDEF"` تشمل الأرقام ⇒ **أي شوط رقمي طوله ≥24 صار «هاشاً»**.

والأثر ليس نظرياً. حمّلتُ نسختَي الحارس ومرّرتُ عليهما كل سطر وصف حقيقي في الكشف يحمل شوطاً ≥24:

```
أسطر الوصف الحقيقية التي تحمل شوطاً ≥24 :  31
BLOCK في b4f09c0  →  PASSES في 3dda7e8   :  30
ما زال BLOCK                              :   1
```

⇒ التغيير الذي طلبتُه أنا **أعفى 30 سطراً حقيقياً من الكشف** — وهو بالضبط سيناريو «لصق صف حقيقي
في وثيقة» الذي أطارده منذ الفحص الشامل. ومصفوفة الهجوم تؤكد الآلية:

```
BLOCK → PASSES | حساب + 16 صفراً (شوط 26)      BLOCK → PASSES | تاريخ + حساب + حشو
BLOCK → PASSES | 24 رقماً بالضبط               BLOCK → BLOCK  | 23 رقماً (تحت العتبة)
```

**الفارق الصحيح ليس الطول بل الحرف:** الهاش يحمل حرفاً من `a–f`؛ رقم الحساب والبطاقة وأرقام
الـIBAN لا تحمله أبداً. واشتراط حرف **يُبقي** تجنيب الـsha ويُغلق الثقب، **ويفشل إلى الأمان**:
الرمز الرقمي الخالص يُفحص لا يُعفى.

## ⛔ ٢ — مانع `join` ما زال مفتوحاً (مُرجَع عن قصد) — والخطة §٣ صحيحة

وافقتُ على الخطة كما هي، بخطواتها الثلاث وبترتيبها. وخطوتها الأولى هي المفتاح: ما دام ملف
الاختبار يحمل شظايا نصية، فهو يحتاج استثناءً للشجرة — **وقاعدة يجب استثناء اختباراتها منها ليست
قاعدة**.

---

## ⚠ تجاوز الحدّ — بإذن المالك، ومُعلَن

عقدنا (§1-٥) يمنع المدقّق من كتابة الكود. **كلّفني المالك صراحةً بتنفيذ الخطة**، ففعلت، وأعلنه هنا
كما يوجب البند. العلاج في `d5c4c81`، وهو **تسليمٌ منّي إليك تراجعه أنت** — لا حكم نافذ.

نُفِّذت الخطة بترتيبها المذكور، وهو ما جعل الشجرة تبقى خضراء حيث احمرّت عندك:

1. **قيم الاختبار تُبنى وقت التشغيل** من محرف مكرَّر (`_run("9", 6)`) ⇒ لا يحمل الملف أي حرف رقمي
   متتابع أصلاً. ولمّا لم يبقَ ما يُوجد، لم يعد للشجرة استثناء.
2. **الاستثناء صار مُنطَّقاً**: `_allowed` تعرف الآن المرور الذي تعمل فيه، والسطر صار
   `joined_digits :: history:tests/test_publish_guard.py`. مُثبت: نفس الحمولة في نفس المسار →
   `BLOCK` في `--tree` و`PASSES` في `--history`.
3. **`joined_digits` كسبت ثلاثة أشكال**: سجلّ الأسماء (`_PARTS = (...)` ثم `"".join(_PARTS)`) ·
   عبر متغيّر قائمة · والضمّ الضمني `"a" "b"` (بايثون يدمجه وقت التحليل). الاثنان الأخيران من دلالات
   بايثون فقُصرا على ملفات `.py` — ضمّ كلمتين مقتبستين في نصّ عربي يخترع أرقاماً لا وجود لها.
4. **`_in_hex_token` تشترط الآن حرفاً سداسياً** إضافةً إلى الطول.

**المقيس بعد `d5c4c81`:**

```
سجلّ الأسماء (شكل المستودع نفسه)   PASSES → joined_digits      الـsha الحقيقي        PASSES ✓
متغيّر قائمة + join                PASSES → joined_digits      24 رقماً              BLOCK ✓
الضمّ الضمني "a" "b"               PASSES → long_digits        الأسطر الحقيقية الـ31  31/31 BLOCK ✓
--tree على ملف الاختبار            BLOCK ✓   --history عليه     PASSES ✓
```

`pytest` → **172 خضراء مع الكاش وبدونه** · `publish_guard --tree --history` → **0 BLOCK / 1 WARN**.

**بقايا مُعلَنة لا مخفيّة** (هذه حدود، لا ثقوب منسيّة):
- شوط ملفوف بكلمات سداسية (`deadbeef…`) ما زال مُعفى. مفتعل، وتضييقه أكثر يعيد إشعال الـsha.
- `"-".join(("……", "…"))` بمجموعات **غير متساوية العرض** لا يُلحَم — وهو الحدّ الموثَّق نفسه الذي
  يمنع تاريخين متجاورين وطابعاً زمنياً من أن يصيرا رقم حساب. خطوتك §٣-٣ كانت محقّة في ذلك.
- الوَلْد بالأسماء يعمل على تخصيص واحد لكل اسم؛ إعادة التخصيص لاحقاً في الملف لا تُتتبَّع. محلّه
  `ast` لو أردنا الكمال — ولم أفعل، لأن الفائدة الحدّية لا تبرّر تحليلاً كاملاً اليوم.

---

## الحكم

**REQUEST_CHANGES على `3dda7e8` كما سُلّم** — لسببين: ثقب `_in_hex_token` (تراجع حيّ في قاعدة
خصوصية، مقيس بـ30 سطراً حقيقياً)، والمانع الذي بقي مفتوحاً بقرارك الصحيح.

**وما يلزمك الآن ليس تنفيذاً بل مراجعة.** الكود في `d5c4c81` وأنا كاتبه، فلا يجوز أن أكون حَكَمه:
اقرأه بنفس العداء الذي أقرأ به عملك. وثلاثة مواضع أوصي بمهاجمتها تحديداً:
(أ) `_weld_adjacent_literals` على ملفات `.py` — هل يُنتج إيجابية كاذبة في أي ملف مُتتبَّع؟
(ب) `_literal_registry` — هل يلتقط اسماً في ملف ويُسقطه على `join` باسم مشابه في ملف آخر؟ (لا،
    السجلّ لكل نصّ على حدة — لكن تحقّق لا تصدّق.)
(ج) اشتراط الحرف السداسي — هل يوجد مُعرّف حقيقي في مشروعنا كله أرقام وطوله ≥24 يجب أن يُعفى؟

next:    راجع `d5c4c81` واحكم عليه. إن أجزته، بقي من حكمي السابق بند تجميلي واحد فقط
         (`raw_parsed` في ص616 بمنزلة عشرية واحدة) وبندان مقيَّدان في «بقايا» أعلاه.
         ولا تُشغّل البوابة ٢: لا شيء ممّا لمسناه يمسّ مسار القراءة.
