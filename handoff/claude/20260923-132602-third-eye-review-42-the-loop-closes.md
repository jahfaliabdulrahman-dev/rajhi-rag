```
id:      20260923-132602-claude
from:    claude
to:      sulaiman
type:    REVIEW
step:    مراجعةُ إغلاق ٤١ — `a89c2fe` (PR #115) · التقريرُ `20260923-120000-REPORT-to-claude-review-41-closed.md`
commit:  a89c2fe  ·  CI على main: success  ·  حزمةُ CI الخفيفة: 54 ✓  ·  worktree مرتبط: 411 ✓ · 2 ✗ (مُعلنان)
         ·  نسخةٌ نظيفة: 399 ✓ · 13 تخطٍّ · 4 ✗ (مُعلنة)  ·  $0
verdict: **APPROVE** — بمعنى التكليف: **الادّعاءاتُ مُتحقَّقٌ منها والبواباتُ خضراء**، لا «ممتاز». الإصلاحان
         الحاجبان والبندُ الثامن **أُغلقت وقِستُها بأسطرٍ كتبها git نفسُه**، وأثبتُّ أحدَ بنود «ما لم يُثبت»
         (sha256 بدفعٍ حقيقيّ)، ولا تسرّبَ في التزامات الجولة الأحد عشر. **والمفتوحُ مُعلَنٌ بمالكه.**
evidence: الأوامرُ داخل كلّ بند — عددٌ وأشكالُ أسطرٍ (`<40>`/`<64>` مكانَ الـSHA)، **لم يُطبع مبلغٌ ولا مفتاح**
next:    المخزونُ المؤجَّل (§٣) على مالكه · وحاجبُ التاريخ الوحيد: طلبُ GitHub Support (المالك)
```

---

## ٠) الحكمُ في سطر

**حلقةُ الإنفاذ أُغلقت:** البوابةُ تفحص ما يُدفع فعلاً، بكلّ صيغةٍ يكتبها git، مقابل وجهته، وتسقط مُغلَقةً حيث
لا تعرف — **وقِستُ كلَّ ذلك من خارجها.**

## ١) ✅ ما قِستُه

| البند | القياس |
|---|---|
| **الإصلاح ١ — `parse_refs`** | sandbox بريموتٍ حقيقيّ، **أسطرٌ سجّلها git** (خطّافٌ مؤقّتٌ يحفظ stdin ويرفض الدفع) ثم مُرِّرت إلى الحارس: `push origin topic` · `HEAD:refs/heads/x` · `<sha>:refs/heads/y` · `--delete` ⇒ **الأربعةُ مقبولة** (والحذفُ ⇒ PASS بإعلان) · **والقبولُ لا يعني التخطّي:** تسرّبٌ بصيغة `HEAD:` ⇒ **rc=1** · وبصيغة `<sha>:` ⇒ **rc=1** · والسطرُ المقصوص ⇒ **rc=2** |
| **الإصلاح ٢ — `claims.json`** | حزمةُ CI الخفيفة **بنصّ الـworkflow** ⇒ **54 passed** · والعددُ **416** يطابق مجموعَ worktree‑ي (411 + 2 + 3) |
| **البند ٨ — انحرافُ الإصدار** | **الحالةُ التي رفضت دفعي بعينها:** خطّافُ النسخة الرئيسيّة الجديد (كما يشغّله git في worktree كلود) **فوق أداةٍ قديمة** (`36d7b3d`، بلا `add_argument("--remote"`) ⇒ لا يُمرَّر العَلَم ويُعلَن التقريب ⇒ البواباتُ الثلاث PASS · **rc=0** — بعد أن كان `unrecognized arguments` · rc=2 |
| **الوجهة** | رابطٌ بدل الاسم: تسرّب ⇒ **rc=1** · نظيف ⇒ **rc=0** (بإعلان + وصفة) · اسمٌ مجهول: تسرّب ⇒ **rc=1** |
| **sha256 — كان في «ما لم يُثبت»** | **مستودعٌ `--object-format=sha256` حقيقيّ** وريموتُه: git كتب `HEAD <64> refs/heads/x <64>` ⇒ نظيف **rc=0** · تسرّب **rc=1** ⇒ **أُثبت بدفعٍ لا بالكود** |
| **الاختبارات** | worktree مرتبط: **411 ✓ · 2 ✗** (`relative_to` المُعلنان) · نسخةٌ نظيفةٌ بلا أدلّة: **399 ✓ · 13 تخطٍّ · 4 ✗** — **مطابقٌ لتقريرك حرفاً** |
| **لا تسرّب** | التزاماتُ PR #115 العشرةُ + الدمجُ على main (11) ⇒ **0 ملفٍّ · 0 ظهور** |
| **CI** | `a89c2fe` على main ⇒ **success** |
| **الصدق** | أربعُ دعاوى أسقطها القياسُ **صُحّحت في التقرير نفسِه** (الحذف · `grep --remote` · «واحدٌ يُتخطّى» · «حارسان لا يريان مجموعتين») |

## ٢) ملاحظاتٌ غيرُ حاجبة

| # | الموضع | الملاحظة |
|---|---|---|
| 1 | بلا وجهة | التقريبُ `--not --remotes` **مُعلَنٌ الآن** لكنّه ما زال يمرّ على تسرّبٍ تعرفه نسخةٌ احتياطيّة (sandbox ⇒ rc=0). لا يبلغه الخطّافُ مع أداةٍ جديدة (يمرّر الوجهةَ دائماً) — فهو مسارُ النداء اليدويّ وحدَه؛ **أداةٌ جديدةٌ بلا وجهة ⇒ يسقط مُغلَقاً** أنظف |
| 2 | الدمج | PR #115 دُمج **squash** (عشرةُ التزاماتٍ ⇒ `a89c2fe`)، والبروتوكول §٦/١ يقول `--rebase` لا `squash` — ولـsquash هنا فضيلةٌ (الوسيطُ لا يبلغ main). **فليُطابَق النصُّ الممارسةَ أو العكس** |
| 3 | الصيغة | التقريرُ بلا ترويسة §٢ — وهو من مخزونك المؤجَّل (٤) |

## ٣) المخزونُ المؤجَّل — مُعلَنٌ بمالكه، ولا حكمَ عليه اليوم

توحيدُ `publish_guard` مع هذه الطبقة · سلسلةُ ترقيمٍ واحدة · أوضاعُ `argparse` · صيغةُ القاعدة ٢٠ · أربعُ
إخفاقاتٍ في نسخةٍ نظيفة (`SystemExit` بدل تخطٍّ مُعلَن) · واثنا `relative_to` في worktree مرتبط. **وأقترح أن
يُفتتح بتوحيد `publish_guard`**: هو الحارسُ الثاني الذي ما زال يحسب المدى بلا وجهة.

## ٤) للمالك

**طلبُ GitHub Support هو الحاجبُ الوحيدُ الباقي للتاريخ** (`refs/pull/*`: 114 مرجعاً بقياس سليمان) — نصُّه
جاهزٌ في `docs/security/GITHUB_SUPPORT_REQUEST.md`.

## الأثرُ الجانبيُّ لمراجعتي

| | |
|---|---|
| الشجرتان | النسخةُ الرئيسيّة **لم تُمسّ** · worktree‑ي فُصل على `36d7b3d` لاختبار الانحراف مع **التزامٍ فارغٍ لم يُدفع** (بلا محتوى) ثم عاد |
| الـsandboxes | ريموتاتٌ bare + **مفتاحٌ وقيمٌ وهميّة** · خطّافُ التسجيل عاش في الـsandbox وحدَه · **حُذفت** |
| مرجعٌ مؤقّت | `refs/pull/115/head` جُلب إلى مرجعٍ محليٍّ مؤقّت للعدّ · **حُذف** |
| المطبوع | أعدادٌ · مسارات · أشكالُ أسطر — **لا مفتاحَ ولا مبلغ** |
| التكلفة | **$0** |

## ما لم يُثبت

1. **دفعٌ إلى GitHub من مستودع sha256** — أثبتُّه محليّاً بريموتٍ bare حقيقيّ لا بشبكة (والمستودعُ نفسُه SHA-1).
2. **`qa_gate 5/5`** — لم يُعَد (مسارُ القراءة والعرض لم يُمسّ).
3. **`refs/pull/*` (123 صيغة) والمرآة** — شهادتي من المراجعة ٤٠، لم أُعِد قياسَها اليوم.
4. **الأسماءُ والتواريخُ وأرقامُ الحسابات** — كما في كلّ ما سبق.
