```
id:      20260926-032854-claude
from:    claude
to:      sulaiman
type:    REVIEW
step:    مراجعة ٦٤ — `main` `032fcae...ba582ec` (PR ‏#124: `ea9f4e0` · `24de2e3` · `cac2e47` · `db564f9` · `ba582ec`) — ردًّا على التقرير الشامل للجولة ٦٣ (إغلاقُ R63-1 وR63-5 وR63-6 · لغزُ `publish_guard`)
commit:  ba582ec (`origin/main`) — CI `publish-guard` SUCCESS
verdict: **APPROVE WITH FIXES** — الأداةُ في الشجرة وتُعيد رقمَها حرفيًّا، واللافتةُ لا تعدو دليلَها، ومراجعةُ ٦٣ هبطت بايتًا بايتًا، ولغزُ الحارس حُسم كما وصفتَه؛ لكنّ القاعدةَ التي أنتجت ٤٨.٦٪ تحتسب أربعَ إصاباتٍ ليست قراءةً لمبلغٍ مطبوع، فالأداةُ تُعيد الرقمَ ولا تُثبت صحّتَه
next:    سليمان: R64-1 قبل أن يُحكم بالأداة على أيّ نموذج · ثم R64-2..4 — المالك: R63-2 (النسخةُ الثانية) · حذفُ الفرع المحلّيّ `chore/round56-seats-closure` · §٢٧ إن أراد الـPR
```

## Verdict — الحكم

**APPROVE WITH FIXES.** لا حاجب. ما صمد بقياسي ($0):
- **R63-1 شكلًا أُغلق:**
  - `tools/local_reader_probe.py` في الشجرة، ومتطلّباتُه مُعلَنة، والصفحاتُ الخمس ثابتٌ مُجمَّدٌ باسمه، ومخرجُه أعدادٌ فقط.
  - **الأمرُ المجرَّد يُعيد ١٧/٣٥ = ٤٨.٦٪ و٢٩/٣٥ = ٨٢.٩٪ حرفيًّا.** شغّلتُه بـVision في البيئة المعزولة المسمّاة في وثيقة القياس، على نسخةٍ خاصّةٍ عند `ba582ec` معها نسخةٌ من البيانات غيرِ المتتبَّعة (حُذفت بعد القياس).
  - رموزُ الخروج كلُّها مقيسة: ٠ · ٢ (بيئةُ المشروع بلا pyobjc) · ٣ · ٤ · ٥.
- **«ليست من الحزمة» صحيح:** قِستُه على الصفحات المئتين كلِّها (الإحصاءُ ٢–٥١ والمدياتُ الثلاثة)، فالتقاطعُ صفر. (والوثيقةُ تستشهد بالمديات وحدَها، أي ١٥٠ من ٢٠٠ — R64-4.)
- **R63-5 أُغلق:** صارت اللافتةُ لافتتين، ولا تُدَّعى «مبالغُ حقيقيّة» إلا مع بندٍ حقيقيّ، والوجهان مُختبَران.
- **R63-6 أُغلق مضمونًا:** مراجعةُ ٦٣ و`STATE.md` على `main` لهما بصمتا التزامي `178d0b1` نفسُهما، ومثلُهما مراجعة ٦٢. (هبطتا داخل `ea9f4e0` لا بالتزامهما، والمضمونُ هو ما طلبتُه.)
- **R63-3 لم يتكرّر:** شجرةُ جلستي السابقة ما زالت مسجَّلةً وحاضرةً ونظيفة.
- **لغزُ `publish_guard` كما قلتَ تمامًا:**
  - البنودُ الثلاثة من التزامٍ واحد هو `2ffdcab`، التوأمُ غيرُ المطهَّر لـ`e903b12` الذي في `main`.
  - لا يصل إليه إلّا الفرعُ المحلّيُّ `chore/round56-seats-closure`، ولا upstream له.
  - ونسخةُ `main` من الملفّ تطابق نمطَ القاعدة **٠** مرّة.
- **الأرقام:**
  - نسخةٌ نقيّةٌ من GitHub عند `ba582ec`: **785 passed · 57 skipped · 0 failed** (842 مجمَّعة). قائمةُ الـCI (١٩ ملفًّا): **271 · 16**.
  - خطواتُ الـCI التسعُ خضراء: `publish_guard --tree --history --ci` بلا BLOCK و44 WARN، و`landing_probe` ‏rc=0.
  - الشجرةُ الكاملة (النسخةُ الخاصّة نفسُها): **842 passed · 0 skipped**. والحارسان آمنٌ وPASS، و`static_gate` ‏PASS، والـclaims مطابقة، ولم يُكتب ملفٌّ واحد.
  - CI على `ba582ec` ‏SUCCESS. و`ci_report main` ‏rc=0، و`origin/main` ‏rc=1: فشلٌ مُغلَق، وملاحظتُك صحيحة.
- **لا انكشافَ من الـPR:** شجراتُ التزامات الـPR الخمسة تساوي شجراتِ التزامات `main` الخمسة، زوجًا زوجًا. فما في `refs/pull/124/head` موجودٌ في `main`.

## Spec — هل هو الشيءُ الصحيح؟

- **R64-1 (P2)** `tools/local_reader_probe.py`، القاعدةُ `قيمة` التي أنتجت الرقمَ المنشور — **الأداةُ تُعيد الرقمَ ولا تُثبت صحّتَه: ٤ من الإصابات الـ١٧ ليست قراءةً لمبلغٍ مطبوع.**
  - شغّلتُ دوالَّ الأداة نفسَها على الصفحات الخمس، وصنّفتُ لكلّ إصابةٍ رموزَ الصفحة التي طابقتها. طبعتُ البنيةَ وحدها، فلا قيمةَ طُبعت ولا كُتبت.
  - مخرجُ الأداة لكلّ صفحة (شكل · قيمة · أرقام · أرصدة): ١: ٣·٣·٣·٢ · ١٨٧: **٠·٢**·٠·٨ · ٣٢٠: ٤·٤·٤·٨ · ٤٠٤: ٤·٤·٤·٧ · ٥٣٩: **٢·٤**·٢·٤.
  - الإصاباتُ الأربع التي تزيدها `قيمة` على `شكل` (١٣ ⇒ ١٧) **كلُّها تطابقها رموزٌ بلا كسر**:
    - ثلاثٌ منها مبالغُ من خانةٍ واحدة، طابقها رقمٌ مفردٌ في موضعٍ آخر من الصفحة: خانةٌ داخل اسمٍ في سطرِ وصف، وخانةٌ في رأس سطرَي وصف.
    - والرابعةُ مبلغٌ طابقته أرقامُه داخل سطرِ وصفِ تحويل، لا عمودُ المبالغ.
  - **وتشخيصُك نفسُه يقول هذا عن ١٨٧:** تجربةُ المناطق أعطت المبالغ «٠/١٠ في كلّ منطقة»، وهي الصفحةُ الوحيدة بين الخمس ذاتُ عشرة مبالغ. والأداةُ تحتسب لها ٢/١٠، وكلتاهما عارضة.
  - **وتعليلُ الإصلاح لا يطابق هذه الصفحات.** الإصلاح يقول `Decimal("999") == Decimal("999.00")` بينما `"999" != "999.00"`.
    والقياس: `printed_amount` نصٌّ كما طُبع في **٤٦/٤٦** صفًّا (والأرصدة: ٣٩ نصًّا و٧ فارغة)، ولا واحدةَ من الإصابات الأربع يطابقها رمزٌ بكسر.
    فالنقاطُ الأربع لم تكن «استرجاعًا مُهدَرًا» بقاعدة النصّ، بل هي ما تمنحه قاعدةُ القيمة لأيّ عددٍ مساوٍ في أيّ موضعٍ من الصفحة.
    وتغييرُ هذه الجولة (نصّ ⇒ `Decimal`) حُكم عليه بمعيارٍ واحد: أن يُعيد الرقمَ المنشور، لا بما هي كلُّ إصابةٍ زائدة.
  - **لماذا يهمّ:**
    - ٨ من المبالغ الـ٣٥ من خانةٍ واحدة، والخانةُ المفردة حاضرةٌ في مكانٍ ما من صفحةٍ بنحو ٥٦ سطرًا.
    - والقاعدةُ مُعلَنةٌ مقياسًا لـ«كلِّ نموذجٍ محليٍّ بعد اليوم» (§٣ من الوثيقة)، فكلُّ نموذجٍ قادمٍ ينال الرصيدَ العارضَ نفسَه، وتبدو المسافةُ إلى حاجة الـ~١٠٠٪ أقصرَ ممّا هي.
    - وضابطُ الانحدار (`test_value_unifies_shapes_that_carry_the_same_number`) يُثبّت تساويَ القيمة، وهو صحيح. أمّا العطبُ ففي **المدى**: الصفحةُ كلُّها، وأيُّ رمز، والقيمُ ذاتُ الخانة الواحدة.
  - **والوحدة:** الـ٣٥ **قيمٌ متمايزةٌ في كلّ صفحة**، لا صفوف. فالصفحاتُ الخمس تحمل **٤٦** صفًّا مطبوعًا (الصفحةُ ١ وحدها ١١ صفًّا بخمس قيم).
    والحكمُ الذي يخدمه الرقمُ يُقاس بالصفّ («كلُّ صفٍّ ناقصٍ مبلغًا = صفٌّ لا يُغلق»). وعلى مستوى الصفّ: `شكل` ٢٠/٤٦ · `قيمة` ٢٤/٤٦.
  - يخالف القاعدةَ ١٤ (الوسمُ لا يعدو دليلَه)، وشرطَ R63-1 «تُثبَّت قواعدُ المطابقة قبل أن يُقاس بها نموذج». · **مُثبت**
  الحركة: قبل أن تحكم هذه الأداةُ على أيّ نموذج (④ · ب-٤):
  1. القاعدةُ التي تُنتج الرقمَ المنشور لا تحتسب قيمةً لا يطبعها الورقُ بتلك الصيغة، كأن تشترط الكسرَ الذي يطبعه العمود، أو تُربط بالصفّ والعمود.
  2. المبالغُ ذاتُ الخانة الواحدة تُعلَن منفصلة، كما تفعل `أرقام` أصلًا بـ`MIN_DIGITS_FOR_DIGITS_RULE`.
  3. الوثيقةُ تُعلن وحدةَ الرقم (قيمٌ متمايزة أم صفوف) وتُصحّح رقمَ المبالغ المنشور. والرقمُ القابلُ للدفاع على هذه الصفحات هو `شكل` **١٣/٣٥ = ٣٧.١٪**: لكلّ إصابةٍ فيه رمزٌ بكسرٍ من خانتين.
  4. الحكمُ لا يتغيّر: القارئُ المجّانيُّ خارج اللعبة في الحالين.

- **R64-2 (P3)** `tests/test_local_reader_probe.py::test_the_published_ratio_is_arithmetically_the_one_in_the_document` — **سُمّي الربطَ الذي طلبه مقعدُ المعايير، ولا يلمس الأداةَ ولا البيانات.**
  - يفحص `round(100*17/35, 1) == 48.6` على ثوابتَ مكتوبةٍ في الاختبار، ويفحص حضورَ أربع سلاسل في الوثيقة.
  - **السمّ:** جعلتُ `value_of` تُرجع `None` دائمًا، فلا تحتسب القاعدةُ المنشورة شيئًا، **وهذا الضابطُ يمرّ**.
  - (أمّا ضابطُ الصفحات الخمس حرفًا فيَعضّ: أبدلتُها بصفحاتٍ من الإحصاء فسقط.) · **مُثبت**
  الحركة: يُسمّى بما هو (اتّساقُ الوثيقة). والربطُ الحقيقيّ ضابطٌ محلّيٌّ يُشغّل الأداةَ على الصفحات الخمس ويُثبت أعدادَ كلّ صفحة، أعدادًا لا قيمًا، ويُتخطّى بسببٍ مُعلَنٍ عند غياب Vision أو البيانات.
  فتغييرُ قاعدةٍ يُحرّك الرقمَ يسقط في الموضع الوحيد الذي يستطيع قياسَه.

## Standards — هل تُرك العملُ نظيفًا؟

- **R64-3 (P3)** PR ‏#124 — **تكرّر R63-4 ولم يُذكر.**
  - §٢٧ على `main` لم يتغيّر («وبلا PR»)، ومراجعةُ ٦٣ طلبت المسارَ المُعلَن أو تعديلَ §٢٧ صراحةً إن أراد المالكُ الـPR. ولم يحدث أيٌّ منهما.
  - فكان #124 ثالثَ PR منذئذ، دُمج بإعادة التركيب، فصار لالتزامات `main` الخمسة هويّاتٌ جديدة، وبقي `refs/pull/124/head` (‏`314d598`) على GitHub خارج `main`.
  - وقستُ: الشجراتُ متساويةٌ زوجًا زوجًا، فلا انكشاف. والمخالفةُ مرّةً أخرى في المسار لا في المحتوى. · **مُثبت**
  الحركة: المسارُ المُعلَن، أو سطرٌ في §٢٧ بكلمة المالك. ويذكر التقريرُ ما فعله ببندٍ مفتوح، فلا يُغلَق بندٌ بالسكوت.

- **R64-4 (P3)** **أرقامٌ لا تحمل بيئتَها، أو تُقاس خطأً كما صيغت:**
  - «٨٤٢ مُجمَّعة · ٨٣٠ تمرّ · ١٢ تُتخطّى» بلا بيئة، و§٢٣ يقول إنّ الرقمَ بلا بيئته دعوى.
    قستُ ٨٤٢/٠ تخطٍّ في الشجرة الكاملة و٧٨٥/٥٧ في النسخة النقيّة، و٨٣٠/١٢ ليس أيًّا منهما. وما يصمد هو قولُ `QA_CHECKLIST`: «٨٤٢ — كلُّها تنجح في الشجرة الكاملة».
  - «المسحُ مرّةً واحدة» (عنوانُ `ba582ec` وتعليقُ الشيفرة) صادقٌ لـ`selected()` وحدها. عددتُ قراءاتِ `label.json`:
    التشغيلُ الافتراضيّ يقرأ **٥٧٧** قراءةً لـ٢٩٤ ملفًّا (نحو مرّتين: `design_dir` ثم `eligible`)، و`--list` ‏**٨٧١** (نحو ثلاث).
  - «ليست من الحزمة — مدياتُها [52–101] · [242–291] · [486–535]»: الخلاصةُ صحيحة، والدليلُ يسمّي ١٥٠ صفحةً من ٢٠٠.
    الإحصاءُ (٢–٥١) خارج المديات، والصفحةُ ١ بجواره. وهذا درسُ المجتمع نفسُه (§٦ البند ٧). وعلى المئتين كلِّها: صفر.
  الحركة:
  - كلُّ رقمٍ ومعه بيئتُه.
  - الوثيقةُ تستشهد بالحزمة كاملةً (الإحصاء والمديات).
  - وتعليقُ الشيفرة يقول «`selected` لا تُعيد المسح»، أو يصير المسحُ واحدًا فعلًا.

- **ملاحظة، والقرارُ للمالك: الفرعُ المحلّيّ** `chore/round56-seats-closure`
  - لا يحمل إلا التزامًا واحدًا، `2ffdcab`، التوأمَ غيرَ المطهَّر لـ`e903b12` الذي في `main`. والفرقُ بينهما ذلك الملفُّ وحده (+٨/−٣): التطهير.
  - خطّافُ الدفع يمنعه، لكنّ التقريرَ سمّى الخطرَ ولم يسمِّ الفرع.
  - توصيتي: يُحذف محلّيًّا، فلا يضيع شيء. والحذفُ بكلمة المالك.

## قراراتي

| البند | القرار | لماذا |
| :--- | :--- | :--- |
| **④ نموذجُ OCR محلّيّ (١–٣GB)** | **ما زال: لا تنزيلَ قبل R64-1 وأ-٧** | صارت الأداةُ تُعيد رقمَها، لكنّ قاعدتَها تحتسب إصاباتٍ عارضة، فالحكمُ على نموذجٍ بها حكمٌ على رصيدٍ مجّانيّ. وأ-٧ ما زال `⬜` (`docs/ROADMAP.md:104`)، والوثيقةُ نفسُها تجعله شرطًا «قبل أيّ مقارنةِ نموذج» |
| **الفرعُ المحلّيّ `chore/round56-seats-closure`** | **يُحذف (بكلمة المالك)** | لا شيءَ فيه إلا نسخةٌ غيرُ مطهَّرةٍ لما في `main` |

## Declined to judge — خارجُ نطاقي أو قرارُ المالك

- دقّةُ Vision محرّكًا (مِقياسٌ واحد، لا ب-٤) · الأرشيفُ ونسختُه الثانية (R63-2) · قرارُ صفحة ٦٢٩ في البند ٤ · FRAME.

## Not proven — ما لم يُثبت

- أحكامُ المقاعد الخمسة: مُلخَّصةٌ في التقرير ومتن الـPR، ونصوصُها ليست في الشجرة (الجولةُ ٥٦ أودعت مقاعدَها بنصّها).
- أنّ الإصاباتِ العارضةَ الأربع هي نفسُها على جهازٍ آخر أو إصدارٍ آخر من Vision: قِستُها مرّةً هنا، والأداةُ أعادت ١٧/٣٥ نفسَه.
- الاسترجاعُ المربوطُ بالموضع: لا أربط رمزًا بعمودٍ بلا إحداثيّات، فرقمُ الصفّ أعلاه ما زال عضويّةً في الصفحة كلِّها.
- إصابةُ الصفحة ١ التي قيمتُها رصيدٌ أيضًا في الصفحة نفسِها: يطابقها رمزٌ بكسر، ولا يُقاس أقُرئت في عمود المبلغ أم الرصيد.

{المُدخلات: التقريرُ الشامل للجولة ٦٣ ومتنُ PR ‏#124 + `main` حتى `ba582ec`} → {البرهان: نسخةٌ نقيّة 785/57/0 وقائمةُ الـCI 271/16 · الشجرةُ الكاملة 842/0 ·
الأداةُ بـVision ‏17/35 و29/35 · تصنيفُ رموز كلّ إصابة (بنيةً لا قيمًا) · سمٌّ في `value_of` · عدُّ قراءات `label.json` · تقاطعُ الحزمة كاملةً ·
مقارنةُ الشجرات زوجًا زوجًا · بصماتُ مراجعتي ٦٢/٦٣ و`STATE` · تتبّعُ كائن `home_path` إلى فرعه · $0} →
{الحالة: APPROVE WITH FIXES · لا حاجب · R64-1 قبل أن يُحكم بالأداة على نموذج}
