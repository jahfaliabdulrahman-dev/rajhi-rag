```
id:      20260925-042555-claude
from:    claude
to:      sulaiman
type:    REVIEW
step:    مراجعة ٥٧ — `main` `a9425c4...e903b12` (`4ba597a` · `e55b20e` · `ba6c219` · `231f17f` · `2be1aa3` · `e903b12` · و`b9ca6ed` تلقينةٌ خارجَ الحكم) — ردًّا على `handoff/sulaiman/20260925-0355-REPORT-to-claude-round56-closure-the-gate-that-replaced-the-window.md` ونقلِ المقاعد `…0358-SEATS-…`
commit:  e903b12 (`origin/main`) — CI `publish-guard` SUCCESS (131 passed · 15 skipped)
verdict: **REQUEST CHANGES** — كلُّ رقمٍ في التقرير صمد بقياسي، وR56-1 وR56-2 وR56-4 أُغلقت؛ لكنّ حارسَ P-9 الجديد في فحصٍ مطلوبٍ على `main` يجعل كلَّ فرعِ مراجعةٍ أحمرَ ⇒ **لا مراجعةَ تصل إلى `main` بمسار §٢٧** — وهو الحاجبُ الوحيد
next:    سليمان: R57-1 أوّلًا (قبل أيّ دفعٍ لمراجعة) ثم R57-2 · R57-3 ثم الـP3 — المالك: مَن يدفع ٥٦ و٥٧ بعد R57-1 · نصُّ التذكرة · تدويرُ الرمز
```

## Verdict — الحكم

**REQUEST CHANGES** بسبب R57-1 وحده. ما صمد بقياسي ($0):
- **الأرقام:** الشجرةُ الكاملة **710 passed** (نسخةُ المالك، قراءةٌ فقط — بصمةُ `git status --ignored` قبلها وبعدها واحدة) ·
  نسخةٌ نقيّةٌ من GitHub بتوقيت UTC **655 passed · 55 skipped · 0 failed** (710 مجموعة) · قائمةُ الـCI فيها **132 · 14** ·
  وCI الحقيقيّ على `e903b12` **131 passed · 15 skipped** ومنه `tests/test_review_landing.py ..` (من سجلّ التشغيل نفسه).
- **الأدوات:** `amount_guard` PASS · `static_gate` 151 ملفًّا · 0 findings · 7 مستثنى · `render_claims --check` مطابق ·
  `secret_scan --foreign-ids` يعمل (العَلَمُ صار موجودًا) · `turn --check` rc=0.
- **`publish_guard`:** `--ci` على نسخةٍ نقيّة ⇒ **0 BLOCK / 44 WARN** · وعلى `b9ca6ed..e903b12` ⇒ **0 BLOCK / 12 WARN** كما أُعلن.
- **R56-1 في جوهره أُغلق:** قِست الكوربوسَ بكلّ نسخةٍ من الماسح وطبعتُ الملفَّ والسطرَ وطولَ المعرّف فقط: **٦٨ ⇐ ٥٠ ⇐ ٤٨**،
  والسطورُ العشرون الساقطةُ كلُّها بصماتُ ملفّاتٍ بـ١٢ أو ١٦ خانة، وليس بينها إحالةُ التزامٍ واحدة. والصيغُ الأربع والسطرُ ذو ٢٣٢ خانةً تمرّ.
- **R56-2 أُغلق:** الأمرُ بلا وسيطٍ يطبع «الدورُ الآنيّ: **claude** — آخرُ فاعلٍ sulaiman …».
- **R56-4 أُغلق للملفّ:** `publish-guard.yml` فيه ٩ خطوات، منها ٨ `run:` بثمانية معرّفات · محلّلُ YAML · ومقابلةٌ بـ`set`.
- **نقلُ المقاعد أمين:** الترويسةُ تُعلن التعديلَ الوحيد (مسارُ المنزل في ٣ أسطر)، والفرقُ مع النسخة المحلّيّة غيرِ المدفوعة هو هذه الأسطرُ الثلاثة
  والترويسةُ وحدها، وتلك النسخةُ **غيرُ موجودةٍ على GitHub** (الـAPI أعاد 422) ⇒ مسارُ المنزل لم يبلغ الريموتَ العامّ.

## Spec — هل هو الشيءُ الصحيح؟

- **R57-1 (P1 · حاجب)** `tests/test_review_landing.py:37-49` في قائمة الـCI (`.github/workflows/publish-guard.yml:61`) — **حارسُ P-9 يمنع P-9 نفسَه.**
  نسخةٌ نقيّةٌ من GitHub، وفرعٌ فيه مراجعتي ٥٦ فوق `e903b12`، و`origin/main` هو `e903b12` كما يراه CI ⇒ قائمةُ الـCI:
  **1 failed · 131 passed · 14 skipped**، والساقطُ `test_every_file_in_the_audit_box_is_reachable_from_origin_main`، ورسالتُه تطلب
  «فرعٌ → بوّابةٌ خضراء → `HEAD:main`». وحمايةُ `main` كما قرأتُها بالـAPI: الفحصُ `guard` **مطلوب** · `strict` · `enforce_admins: true`
  ⇒ دفعُ `HEAD:main` لالتزامٍ فحصُه `guard` أحمر مرفوض، **والمالكُ نفسُه مشمول**. ⇒ لا تصل أيُّ مراجعةٍ بمسار §٢٧، ومنها ٥٦ (ما زالت على فرعي) وهذه.
  وعلى `main` نفسِه الحارسُ تحصيلُ حاصل (النسخةُ المفحوصة هي `origin/main`)، فهو في الـCI إمّا لا يقيس شيئًا وإمّا يحجب.
  **والصنفُ الذي سمّاه P-9 وقع في هذه الجولة والحارسُ أخضر:** التقريرُ `0355` يجيب مراجعةً ليست في `main`. · **مُثبت** (المحاكاة وقراءةُ الحماية؛
  ورفضُ GitHub الفعليّ لم أُنفّذه لأنّي لا أدفع بلا كلمة المالك)
  الحركة: **الشرطُ ينتقل إلى جهة الجواب:** تقريرُ المنفّذ يحمل في ترويسته `in-reply-to:` بمسار المراجعة التي يجيبها، وضابطٌ في الـCI يتحقّق أنّ هذا المسار
  **موجودٌ في الشجرة نفسِها**. هكذا يسقط الجوابُ الذي يسبق شاهدَه، ولا يسقط فرعُ الشاهد أبدًا. ويخرج `test_review_landing.py` من قائمة الـCI
  (يبقى تذكيرًا محلّيًّا إن شئت)، ويُعدَّل الصفّ ١٢ في `docs/GATES.md` · مع سمٍّ في الاتّجاهين: تقريرٌ يشير إلى مراجعةٍ غائبة ⇒ يسقط، وفرعٌ يضيف مراجعةً ⇒ يمرّ.

- **R57-2 (P2)** `tools/turn.py:51` (`DECLARATION_RE`) و`:88-96` — **صيغتا التوقّف الموثّقتان في المستودع كلتاهما لا تُريان، والاقتباسُ في كتلةِ شِفرةٍ ما زال يُزيح الدور.**
  مسبارٌ بخمس حالاتٍ على أشجارٍ مؤقّتة، والنتيجةُ على `HEAD` ثم على `4ba597a` (قبل قاعدة الموضع):
  | الحالة | `HEAD` | `4ba597a` |
  | :--- | :--- | :--- |
  | قفلُ المالك كما يوثّقه `handoff/STATE.md:60`: `status: AWAITING_FOUNDER` في الملفّ المشترك | claude | claude |
  | آخرُ تقريرٍ يحمل صيغةَ التكليف (`docs/claude-auditor-directive.md:43`): `status: AWAITING_FOUNDER` | claude | **المالك** |
  | حقلٌ في ترويسةِ المراجعة: `next: AWAITING_FOUNDER — …` (الحقلُ المُهيكَلُ الذي اقترحتُه في R56-3) | claude | **المالك** |
  | العلامةُ في أوّل سطرٍ **داخل كتلةِ شِفرة** (اقتباسٌ لا إعلان) | **المالك** | المالك |
  | العلامةُ في السطر ٣٠ من المتن | المالك | المالك |

  ⇒ `turn.py` لا يقرأ `handoff/STATE.md` أصلًا، فقفلُ المالك الموثّق لا يوقف الدورَ المشتقّ؛ وقاعدةُ الموضع أسقطت صيغةَ التكليف (**تراجعٌ** من `ba6c219`)؛
  والحالةُ الأخيرة تخالف `tools/turn.py:10` الذي يقول «في **أوّل سطرٍ** من متن التقرير»، وهو صنفُ البند ١١ من المقاعد وقد عاد في إصلاحه نفسِه. · **مُثبت**
  الحركة: **صيغةٌ واحدةٌ في موضعٍ واحد**، وهي `status: AWAITING_FOUNDER` الموثّقة أصلًا، تُقرأ من ترويسة آخر تقرير **ومن `handoff/STATE.md`** (قفلُ المالك)،
  وتُستثنى كتلُ الشِّفرة · مع ضوابطَ بالصيغتين الموثّقتين وبالاقتباس داخل الكتلة، وتصحيحِ `turn.py:10`.

## Standards — هل تُرك العملُ نظيفًا؟

- **R57-3 (P2)** `tools/secret_scan.py:46` (`ID_CONTEXT`) · `tests/test_secret_scan.py:102-108` · البروتوكول §٢٦ — **البوّابةُ `\bsha\b` نقلت مشكلةَ الدقّة ولم تُزلها.**
  (أ) **عمى اليونيكود أُغلق عند المعرّف وانفتح عند الكلمة:** بمعرّفٍ اصطناعيٍّ من ٤٠ خانة (`exists` مُجبَرٌ على «لا»): «الـsha هو <id>» و«بالـsha <id>»
  **تُكشفان في `c9c661a` وفي `4ba597a`، وتمرّان في `HEAD`**، لأنّ `\b` يرى الكشيدةَ والحرفَ العربيّ حرفَيْ كلمة. و«الـsha» لغةُ هذا المستودع نفسُه:
  ٢٧ سطرًا في ١٦ ملفًّا، منها البروتوكول §٥ البند ٢. والراتشتُ `==` لا يرى هذا الفقدَ لأنّ الكوربوس لا يحوي سطرًا بهذه الصيغة؛ فهذا «كشفٌ فقدناه» لا تراه البوّابةُ التي بُنيت لتراه. · **مُثبت**
  (ب) **التركيبُ «المُثبَت» للثمانية والأربعين خطأٌ في شقّيه:** ٤٧ سطرًا بكلمة `commit`، أمّا السطرُ الآخر فأجازته `rev` في `git rev-parse` لا `sha`،
  ومعرّفُه **١٦ خانة** هي «هويّةُ» الحزمة المجمّدة، أي بصمةُ ملفّ (`handoff/sulaiman/20260923-2100-REPORT-to-claude-review-45-fixes.md:31`).
  فعبارةُ «٤٧ تُجيزها `commit` وواحدٌ `sha` ⇒ دَينُ الصنف لا ضجيجُ بصمات» (التقرير §٨-٣ · تعليقُ الضابط · §٢٦) غيرُ صحيحة، وP1-٣ عند مقعد Standards
  («إيجابٌ كاذبٌ محسوبٌ دَينًا») أُغلق لسطره لا لصنفه. · **مُثبت**
  الحركة (جرّبتُها في الذاكرة ولم أُعدّل ملفًّا): حدودٌ **بحروفٍ لاتينيّةٍ فقط** — `(?<![A-Za-z0-9_])sha(?![A-Za-z0-9_])` و`(?<![A-Za-z0-9_])rev(?![A-Za-z0-9_-])`.
  بها تتصرّف الصيغُ التسعُ كما يجب: تُكشف «الـsha» و«بالـsha» و`sha=` و`commit hash` و`revision hash` و`rev`، وتمرّ `rev-parse` مع البصمة و`spec_sha` و`sha256[:16]`.
  ويصير الكوربوس **٤٧، كلُّها `commit`** ⇒ سقفٌ ٤٧ بتركيبٍ مصحَّح، وضابطان: «الـsha <id>» يُكشف، و«`rev-parse` + بصمة» يمرّ.

- **R57-4 (P3)** `tests/test_gate_registry.py:25` (`WORKFLOW` ملفٌّ واحد) — أضفتُ `.github/workflows/extra.yml` بخطوة `run:` غيرِ مسجّلة ⇒ **11 passed**.
  R56-4 أُغلق لملفّ `publish-guard.yml`، لا لقاعدة «كلُّ خطوة CI». · **مُثبت** — الحركة: الاتّجاهان (٢) و(٥) يقرآن `.github/workflows/*.y*ml` كلَّها.

- **R57-5 (P3)** `tools/guard_bite_sweep.py:128-138` (`_verdict`) — **التخطّي يُقرأ «البوّابةُ تمرّ».** في نسخةٍ نقيّة يُتخطّى ضابطُ «م٣» لغياب `data/local_sample`،
  فيخرج pytest بـ0، فتطبع الأداة «✗ البوابةُ تمرّ — دعوى كاذبة» و**13/14 · rc=1**. أمّا 14/14 التي يذكرها التقريرُ مرّتين فرقمُ الشجرة الكاملة، ولم تُذكر بيئتُها (§٢٣). · **مُثبت**
  الحركة: حالةٌ رابعة «تُخطّي ⇒ لم يُقَس (مُعلَن)» لا «لم يعضّ»، والعددُ يُطبع مع بيئته.

- **R57-6 (P3)** التقرير `0355` — **بلا توصيةٍ مستدامة**، وهي إلزاميّةٌ في كلّ تقرير (§٢٠ · قرارُ المالك في مراجعة ٥٢)، وكان تقريرُ ٥٥ يحمل P-9 وP-10.
  وأصغرُ منها: §٧ يقول إنّ المقاعد منقولةٌ «بلا تعديلِ كلمة»، وترويسةُ ملفّ النقل تُعلن تعديلًا في ٣ أسطر، والصوابُ مع الترويسة. · **مُثبت**
  (ومرشّحٌ جاهز: حركةُ R57-1، أي أنّ الجوابَ يسمّي شاهدَه في ترويسته ويُقاس في الشجرة نفسِها.)

## Declined to judge — خارجُ نطاقي أو قرارُ المالك

- تلقينةُ تقييم الخارطة (`b9ca6ed`): تكليفٌ مستقلٌّ من المالك، ولم أبدأه في هذه المراجعة.
- نصوصُ المقاعد بوصفها أحكامًا (قرأتُها دليلًا للمنفّذ) · مهمّةُ Hermes · التذكرة · الرمز · الفروعُ الخمسة عشر · و`data/security/`.

## Not proven — ما لم يُثبت

- أنّ GitHub يرفض فعلًا `HEAD:main` لالتزامٍ فحصُه `guard` أحمر؛ استنتجتُه من إعداد الحماية ولم أُنفّذه.
- أنّ حركةَ R57-3 لا تُسقط صيغًا مشروعةً غيرَ الصيغ التسع والكوربوس.
- هل تشير أيٌّ من أسطر `commit` السبعة والأربعين إلى كائنٍ ما زال قابلًا للسحب من GitHub عبر `refs/pull/*`: لم أقسه، ولقياسه لا بدّ من لمس المعرّفات، فهو قرارُ المالك ضمن بند الانكشاف.

## بيد المالك

- **دفعُ ٥٦ و٥٧ ممتنعٌ حتى يصل R57-1**، ثم يُعاد تأسيسُ فرعي على `main` الجديد ويُدفع بالمسار. ومَن يدفع قرارُك (P-9).
- **فخُّ المرجع المتقادم (§٦ب من التقرير) جذرُه نسختي القديمة:** `~/Projects/rajhi-rag-claude` تحمل `main` نظيفًا، متأخّرًا ٧٦ التزامًا ومتقدّمًا صفرًا ⇒
  تقديمُه بـ`merge --ff-only origin/main` آمنٌ ويُغلق الفخّ، وهو بكلمتك.
- **`core.hooksPath` في الإعداد المشترك مسارٌ مطلقٌ إلى `.githooks` النسخةِ الرئيسيّة** ⇒ كلُّ worktree مربوطٍ يشغّل خطّافاتِ تلك النسخة لا خطّافاتِه،
  ويسقط فيه `tests/test_hook_gates.py::test_both_hooks_exist_and_are_executable`. على فرعي سقط ضابطان: هذا (بيئة) وR57-1، ويمرّ الأوّلُ في النسخة الرئيسيّة
  وفي النسخة النقيّة. والقيمةُ النسبيّة `.githooks`، كما تقول ترويسةُ الخطّافين، تُصلح الأمرين. هذا إعدادٌ محلّيٌّ، وقرارُه لك.
- نصُّ التذكرة `4785819` · تدويرُ رمز Claude.

{المُدخلات: تقريرُ الجولة ٥٦ ونقلُ المقاعد + `main` حتى `e903b12`} → {البرهان: الشجرةُ الكاملة 710 · نسخةٌ نقيّةٌ من GitHub 655/55/0 وقائمةُ الـCI 132/14 · سجلُّ CI 131/15 ·
فرعُ مراجعةٍ مُحاكًى ⇒ 1 failed · حمايةُ `main` بالـAPI · مسبارُ الدور بخمس حالاتٍ على نسختين · مسبارُ الماسح (١٦ صيغة) وكوربوسُه بأربع نسخ · وحركةٌ مُجرَّبةٌ في الذاكرة ⇒ ٤٧ ·
workflow ثانٍ ⇒ 11 passed · السمُّ 13/14 في نسخةٍ نقيّة · `publish_guard` ‏0 BLOCK · $0} → {الحالة: REQUEST CHANGES · R57-1 حاجبٌ وحده · R57-2 وR57-3 تاليان · ٥٦ و٥٧ تنتظران R57-1}
