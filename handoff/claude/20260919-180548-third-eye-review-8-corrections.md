```

> **حجبٌ للنشر (معلن)**: النصّ حرفيّ كما كتبه المدقّق — ولا مسار مطلق فيه (المسارات بصيغة `~/`).

id:      20260919-180548-claude
from:    claude
to:      sulaiman
type:    REVIEW
step:    فحص التصحيحين على 907a1e2 — كسرتُ البديل فرأيت الحارسَين يعضّان · وخطأٌ عليّ أُقرّه · وخطأٌ في §٣ أردّه بالتاريخ
commit:  907a1e2bf89d103415cb1f18428b439316f33eab
verdict: PASS
```

---

## ١) التصحيحان — مقيسان، والحارسان يعضّان

### (أ) اسم فحص القيد

```python
label = (g.get("رقم الصفحة المطبوع") or g.get("الصفحة (ملف)") or "?")
```

المفتاح الصحيح أولاً، والموقع بديلاً. وعلى الملف المُسلَّم:

```
[PASS] قيد ص426 · 427 له مقدارا مدين ودائن — 9001.00/3025
[PASS] قيد ص625 · 626 له مقدارا مدين ودائن — 9977.98/2671
```

**وأحسنُ من إصلاح البوابة أنكم أصلحتم المصدر:** المصنّف نفسه صار يحمل
`رقم الصفحة المطبوع = '426 · 427'` لصفّ القيد — فالاسم من الورق لا من البوابة.

**والحارسان يعضّان — كسرتُ البديل عمداً لأتحقّق:**

```
label = (g.get("لا وجود له") or "?")        ← كسرتُه فصار الاسم «قيد ص?»
⇒ FAILED test_no_check_name_carries_an_unresolved_placeholder
⇒ FAILED test_the_gap_check_names_the_printed_sheets_not_the_file_position
```

اثنان لا واحد، وكلاهما بالاسم. ثم استعدتُ الملف ⇒ `28 passed`.

### (ب) عدد الأسمام

```
POISONS = 24   ·   28 tests   (24 سمّاً + العيّنة + التغطية + الحارسان الجديدان)
```

يطابق §١(ب) حرفاً.

---

## ٢) وخطأٌ عليّ — أُقرّه

قلتُ في رسالتي السادسة والسابعة إن فرع الـPR **لم يُحذف**. وأنتم على حقّ: شغّلتُ
`git fetch --all` **بلا `--prune`**، فبقي المرجع المحلي `origin/docs/pass-closure-and-disclosure`
يشير إلى فرعٍ محذوف من `origin` — فبنيتُ حكماً على مرجعٍ ميت في نسختي أنا.

وبـ`--prune` اليوم: `origin` فيه **`main` وحده**. و`bfda506` باقٍ عندي **كائناً
معلَّقاً** لا يُحلّ من نسخةٍ نظيفة — كما قلتم بالضبط.

**والقاعدة آخذها:** `git fetch --prune` قبل أي حكم على حالة فرع. وهي من صنف ما
كنتُ أطالبكم به: لا يُحكم على حالةٍ من مرجعٍ لم يُحدَّث.

---

## ٣) وخطأٌ في §٣ — أردّه بالتاريخ لا بالرأي

كتبتم أن `STATE.md:20` «**صُحّح قبل كتابتك بخمس دقائق** — الالتزام `b9e5e5f` —
17:09:15، ورسالتك 17:14:43: قرأتَ نسخةً سبقت الإصلاح».

**والتاريخ يقول غير ذلك.** الالتزام الذي سمّيتموه هو نفسه الذي كنتُ عليه، وفيه:

```
$ git show b9e5e5f:handoff/STATE.md | grep -n 'bfda506\|184f0be'
3:status: … (وما بعده إلى `184f0be` …)          ← صحيح
4:step:   … والفرع `bfda506` صار يتيماً …        ← صحيح (تسميةٌ مشروحة)
20:(sليمـان) review-v6-closure — 17:35 — `bfda506` — SUBMIT …   ← ⛔ هذا ما رصدتُه

$ git show 4c7d50d:handoff/STATE.md | grep -n '184f0be'
20:(sليمـان) review-v6-closure — 17:35 — `184f0be` — SUBMIT …   ← الإصلاح هنا
```

| | التوقيت |
|---|---|
| `b9e5e5f` (مؤلِّف 17:08:36 · **مُلتزِم 17:09:15** — الرقم الذي ذكرتموه) | **يحمل `bfda506` في السطر 20** |
| رسالتي السادسة | 17:14:43 |
| `4c7d50d` — وفيه الإصلاح | **17:43:27** |

فالسطر **لم يكن مُصلَحاً حين قرأتُه**؛ صُلح **بعد** رسالتي بنحو نصف ساعة — وهو
المسار الطبيعي والحسن: رُفعت ملاحظة فنُفِّذت. ولا شيء عليكم في ذلك إلا سطرٌ في
رسالةٍ نسب إليّ قراءةَ نسخةٍ قديمة.

> ولستُ أُثقل هذا: **خطأٌ لي وخطأٌ لكم، وكلاهما في السجلّ لا في العمل، وكلاهما
> يُقاس بأمر واحد.** وهذا بعينه ما جعل هذه الحلقة تعمل.

---

## ٤) التصريح على رسالة v6 — إقرارٌ، وصياغةٌ أدقّ للقاعدة

أضفتم في **كتلة التصحيح** تصريحاً يغطّي المتن، ولم تُعيدوا كتابة المتن. **هذا هو
الصواب**، وهو الوسط الذي يحفظ الأمرين: السجلّ لا يُحسَّن بأثر رجعي، والقارئ لا
يُترك مع مرجعٍ ميت بلا بيان.

والقاعدة التي كتبتُها في رسالتي السادسة كانت ناقصة؛ وصيغتها الأدقّ بعد هذه الجولة:

> **ما أُرسل لا يُعاد كتابة متنه. ويُضاف إليه — عند الحاجة — تصريحٌ مؤرَّخ في كتلة
> التصحيح، يُسمّي ما بطل ويُحيل إلى الحيّ. وما يزال حيّاً (وثائق · لقطات · حالة ·
> كود) يُصحَّح في مكانه.**

---

## ٥) ما قِسته على `907a1e2`

| | المقيس |
|---|---|
| البوابة | **25 فحصاً · `ALL PASS ✓`** · والاسمان من الورق |
| الحقن الداخلي | **28 نجحت** · `POISONS = 24` |
| الحقن الخارجي | **3/3 · exit=0** |
| المجموعة | **280 نجحت · 2 تخطّت** عندي (= 282 مع العيّنة المحلية) |
| الثوابت | 5,811 · Σمدين **594,683.32** · Σدائن **382,772.74** · **570.59** · 5,791 حركة · 5,791 تاريخاً |
| الصندوق | **ثمانية** من أحكامي في `main` — ومنها السادس والسابع ✓ |

---

## ٦) حدٌّ باقٍ — أذكره ولا أطلبه

الحارسان يعيشان في `test_gate_bites` على **العيّنة المبنيّة بـ`to_xlsx.build`** ⇒
يمسكان أي تراجع في **الباني**. وأمّا `verify_close.py` نفسه، إن أُعطي مصنّفاً
عمودَاه فارغان معاً لصفّ القيد، فما زال يطبع `قيد ص?` ويُنهي `ALL PASS` (جرّبتُه).

**وهذا في موضعه الصحيح:** الاسم زينةٌ لا قياس، والباني هو ما يُحرَس، وقد قلتُ في
رسالتي السابعة إنّي **لا أوصي** بتثبيت الأسماء كاملةً لأن ثمنه أكبر من مكسبه.
فالعلاج الذي اخترتموه متناسب — أُسجّل الحدّ لا أطالب بسدّه.

---

## ٧) المفتوح — بندان، لا يملك النظام أحدهما

1. **ورق معطوب بيد بشر** — صناعيّ حتى الآن.
2. **الشهادة على مصدر الورق** — معلنة في الملف، وبيد المالك.

---

## ٨) إعادة الإنتاج

```bash
V=~/Projects/rajhi-rag/.venv/bin/python

# الحارسان: اكسر البديل ⇒ اسمٌ فيه «?» ⇒ اختباران يسقطان بالاسم
#   label = (g.get("لا وجود له") or "?")   ثم: git checkout -- tools/verify_close.py
$V -m pytest tests/test_gate_bites.py -q        # ⇒ 28 passed بعد الاستعادة
$V -c "import sys;sys.path.insert(0,'tests');import test_gate_bites as t;print(len(t.POISONS))"  # 24

# §٣ بالتاريخ
git show b9e5e5f:handoff/STATE.md | sed -n 20p     # ⇒ bfda506
git show 4c7d50d:handoff/STATE.md | sed -n 20p     # ⇒ 184f0be
git log -1 --format='%cd' --date=format:'%H:%M:%S' b9e5e5f   # ⇒ 17:09:15

git fetch --prune && git branch -r                  # ⇒ origin/main وحده
```

---

next: لا شيء عليّ، ولا أطلب جولة. البند مُغلق، والتصحيحان منفَّذان، والخطأان
      (لي ولكم) مؤرَّخان في مكانهما. وما بقي بيد المالك.
