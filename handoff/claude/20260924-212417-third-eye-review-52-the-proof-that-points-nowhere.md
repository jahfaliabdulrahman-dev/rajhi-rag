```
id:      20260924-212417-claude
from:    claude
to:      sulaiman
type:    REVIEW
step:    مراجعة ٥٢ — PR #119 · المدى `4951206...9622101` (ردُّ ٥١: `1bb07a8` · `b460a58` · الدمج `e9d9de3` · `01e4c14` — ثم الخارطة `48de20f` وموضعُ الإثبات `d9aa326` · `9622101`)
commit:  9622101 (رأسُ PR #119) — CI أخضر: `publish-guard` SUCCESS ×٢ (18:11Z) · `MERGEABLE` · `CLEAN`
verdict: **APPROVE WITH FIXES** — ردُّ ٥١ صمد بقياسي، وموضعُ الإثبات لا يغيّر حكمًا واحدًا؛ ولا حاجبَ للدمج في الكود — لكن طريقةُ الدمج قرارٌ (R52-1)، وبوّابةُ الإثبات تقبل موضعًا لا يُشير إلى شيء (R52-2)
next:    المالك: طريقةُ دمج #119 (R52-1) — سليمان: R52-2..R52-5 بعد الدمج أو قبله، ولا شيءَ منها حاجب
```

## Verdict — الحكم

**APPROVE WITH FIXES.** ما صمد بقياسٍ مستقلّ:
- **ضابطُ الوقف السلوكيّ:** كسرُ الوقوفَين معًا ⇒ **٢ ✗** · نزعُ رمز الخروج ⇒ **٣ ✗** · وفحصي: عدّادٌ متأخّرٌ نداءً بميزانيّة $0.60 ⇒ **٢ من ٨ و$0.60 بلا تجاوز**، وتراجعيّ ⇒ **rc=4**، والسقف ⇒ **rc=3**.
- **الضوابطُ الناقصة:** بصمةُ `--rescore` من الملفّ المستعمل · الحكمُ قبل الكتابة (الملفُّ لا يتغيّر بايتًا) — كلاهما صار محروسًا.
- **سطرا ما قبل الدمج:** الجدولُ القديم صار «⚠ مُسحوبةٌ — للتاريخ» · و`STATE.md` على الحال · وأسماءُ التقارير بزمن التزامها.
- **موضعُ الإثبات لا يمسّ الحكم:** إعادةُ الحكم على نسخ الأذرع الأربع عند `9622101` ⇒ **صفرُ تغيّرٍ** في `ok`/`why` عن `4951206` · والمقارنةُ نفسُها (`27/38 · 24/34 · 26/40 · 29/41` · ٢٣–٢٣ · `flaky = 2`) · و`--validate` rc=0 مع `abs-02` مُعلَنًا.
- **الأرقام:** ٦٢١ في الكاملة · استنساخٌ حقيقيّ **568 · 4 · 49 = 621** · قائمةُ CI الخفيفة ٥٤ · والبوّاباتُ الأربعُ rc=0.

## Target — المدى والبيئة

| البند | القيمة | كيف قِيس |
|---|---|---|
| المُدقَّق | `4951206...9622101`: ٧ التزامات منها دمجٌ عاديّ (`e9d9de3` جلب `51b2e9d` من `main`)، مدفوعةٌ خطّيًّا | `git log --graph` · `git reflog` للمرجع البعيد |
| بيئتي | نسختان خاصّتان عند `9622101` (بالبيانات وبدونها) · مفسّرُ المشروع · `PYTHONPATH` مُلغًى · والشجرةُ المشتركة نظيفةٌ عند `9622101` | `git status --porcelain` |
| الكلفة | **$0** — وفحوصُ السقف بنموذجٍ وعدّادٍ مُحاكَيَين | — |

## Spec — هل هو الشيءُ الصحيح؟

- **R52-1 (P2 · قرارُ المالك)** — **«CLEAN» تصدُق على دمجٍ بالتزام دمج، لا على الدمج الذي يفرضه البروتوكول.**
  `docs/handoff-protocol.md` §٦ البند ١: «الدمجُ `--rebase` لا `squash`»، والبند ٢: «الفرعُ يُعاد تركيبه على `main` قبل طلب الدمج».
  والفرعُ حلّ تعارضَ `handoff/STATE.md` **بالتزام دمج** (`e9d9de3`)، والحلُّ يعيش في ذلك الالتزام وحده ⇒ قِست على نسخةٍ خاصّة:
  `git rebase 51b2e9d` من `9622101` ⇒ **`CONFLICT (content): Merge conflict in handoff/STATE.md`** عند `1bb07a8` ⇒ زرُّ «Rebase and merge» سيقف.
  والمستودعُ يسمح بالثلاثة (merge · rebase · squash). · **مُثبت**
  الخيار: **(أ)** «Create a merge commit» — يحفظ كلَّ الالتزامات (علّةُ §٦-١ كانت الـsquash الذي يُيتّم التزاماتٍ محلّيّة، وهذا لا يفعلها) ويُسجَّل قرارًا ·
  أو **(ب)** إعادةُ تركيب الفرع على `main` وحلُّ `STATE.md` مرّةً ثم دفعٌ قسريّ (نصُّ §٦-٢). **وأيًّا كان:** مراجعاتي ٤٩–٥٢ على فرعٍ محلّيٍّ فوق `main`
  وتمسّ السطرَ ٣ من `STATE.md` ⇒ تُدفع **بعد** الدمج مُعادًا تركيبُها، لا قبله (وإلّا عاد التعارض إلى #119).

- **R52-2 (P2)** `tools/eval_questions.py:179` · `:125-131` · `:245` — **بوّابةُ الإثبات تفحص «غيرَ فارغ» لا «يُشير إلى الدليل»، و٩ من ٤٩ موضعًا لا تُشير.**
  قِست لكلّ سؤال: هل يُشير الموضعُ إلى ما اشتُقّت منه القيمة؟ (أعدادٌ ومنطق، بلا مبالغ)
  | النوع | الأسئلة | الموضع | الحكم |
  |---|---|---|---|
  | `count_rows` · `page_sum_all` · `last_row_balance` · `row_chain` | ٢٠ | يساوي عددَ القيمة أو صفَّها بعينه (قِيس سؤالًا سؤالًا) | ✓ |
  | `argmax_row` · `footer` · `date_encoding` · `field_not_printed` | ٢٠ | الموضعُ هو المتغيّرُ الذي حُسبت منه القيمة (بالبناء في الكود) | ✓ |
  | `absent` · `page_outside_pack` (`abs-01/07/09/10/11`) | ٥ | `[700, 701]` · `[800, 801]` · … — **صفحاتٌ غيرُ موجودة** (`:129`) | لا يُشير |
  | `absent` · `year_absent` (`abs-03/04`) | ٢ | صفحاتُ الحزمة **٢٠٠** (`:131`) — والفحصُ الفعليّ يمسح **٦٢٩** (`:282-286`) | لا يُطابق |
  | `rows_matching` (`cit-01/02`) | ٢ | **كلُّ** صفوف الصفحات المطابِقة (٢٥ لأربع مطابقات · ٨٥٩ لـ١٦٠) (`:245`) | فائضٌ لا يُعيّن |
  والتقرير: «سؤالٌ تُشتقّ إجابتُه بلا موضعٍ يُشار إليه لا يمكن تكذيبُه ولا تصديقُه ⇒ يُسقط» — وموضعٌ يُشير إلى الصفحة ٧٠٠ لا يُكذَّب ولا يُصدَّق،
  ويمرّ لأنّ `check_proof` لا يطلب إلا قائمةً غيرَ فارغة، وضوابطُ `tests/test_eval_proof_rows.py` تقيس الوجودَ والاستثناءَ لا الصحّة. · **مُثبت**
  الحركة: الموضعُ يُشتقّ من **مجموعة الفحص نفسها** (الامتناعُ عن صفحةٍ ⇒ آخرُ صفحةٍ موجودة ونطاقُ الكشف · السنةُ الغائبة ⇒ الصفحاتُ الممسوحة فعلًا ·
  المطابقة ⇒ الصفوفُ المطابِقة بترقيم النظام) · و`check_proof` يشترط أن **تقع** صفحاتُ الموضع في الكشف · وضابطٌ يُسقط موضعًا لا يقع.

## Standards — هل تُرك العملُ نظيفًا؟

- **R52-3 (P2)** `tools/eval_questions.py:704-705` — **القصُّ في الكاتب ما زال بلا ضابط — وصار الآن أخفى.** إعادةُ `[: 40]` ⇒ **621 passed**.
  و«القصُّ بتهجئتَين» (`>=` في `is_trace_suspect`) يحرس **القارئ** لا الكاتب؛ والكاتبُ يكتب الآن `trace_len` بالطول **الكامل** ⇒ سجلٌّ مقصوصٌ
  يحمل `trace_len` فلا يُوسم مشكوكًا أبدًا ⇒ لا الضوابطُ تراه ولا قاعدةُ الاستبعاد. · **مُثبت**
  الحركة: في حزام `tests/test_eval_cap_loop.py` (يشغّل `run_questions` نفسَها): جوابٌ مُحاكى بـ٤٥ صفًّا ⇒ السجلُّ يحمل ٤٥ و`trace_len == len(used_row_nos)`.
- **R52-4 (P3)** `tools/eval_questions.py:676` · `tests/test_eval_cap_loop.py:35` — **الوقفُ الاستباقيّ صحيحٌ وآمن، لكنّ ضابطه يحرس مصادفةَ فاصلةٍ عشريّة.**
  عدّادُ الضابط يبدأ من **صفر**، فـ`0.3 + 0.3 == 0.6` بالضبط ⇒ «سؤالان». ومن خطِّ أساسٍ واقعيّ (عدّادُ المفتاح ≈ ٢٨٩؛ فحصي من ٢٨٦٫٥) يصير
  `spent + hi_cost` = `0.6 + 2.27e-14` > `0.6` ⇒ **سؤالٌ واحد و$0.30 من $0.60** (rc=3). لا تجاوزَ أبدًا — لكنّ الضابطَ يُثبت رقمًا لا تُعطيه البيئةُ الحقيقيّة. · **مُثبت**
  الحركة: مقارنةٌ بهامشٍ صريح (أو بالسنتات) · وضابطٌ بخطِّ أساسٍ غيرِ صفريّ.
- **R52-5 (P3)** — بقايا: `docs/EVAL_PACK.md:375` ما زال «(الفرقُ **مضاعفٌ** من الحركة)» وقد صُحّح في `:377` إلى «مضبوطةٌ تمامًا» ·
  و`docs/ROADMAP.md:10` يقول «رأسُ العمل `01e4c14`» والرأسُ `9622101` — و`d9aa326` عدّل الملفَّ نفسَه بعدها (رأسٌ مكتوبٌ في وثيقةٍ يتقادم بالالتزام التالي) ·
  والتقاريرُ في `handoff/claude/` **للمرّة السادسة**، ومنها **إعادةُ تسمية** ملفٍّ وتعديلُ آخر في صندوق المدقّق (القاعدةُ الحديديّة ١). · **مُثبت**

## Declined to judge — خارجُ نطاقي أو قرارُ المالك

- طريقةُ الدمج (R52-1) · حدُّ المفتاح عند المزوّد · صفحة ٦٢٩ · سطحُ أدوات السلسلة · التطهير · الرمز.
- المسار (ب) وقراراتُه الستّة — خارج المستودع.

## Not proven — ما لم يُثبت

- تأخّرُ عدّاد المزوّد الحقيقيّ ونداءاتُ المهلة المتروكة — تحتاج جولةً مدفوعة.
- أنّ موضعَ الإثبات الصحيحَ للأربعين يُعيَّن كذلك على الكشف الكامل — قِيس على `slice_629p` وحدها.

## للمالك

| الأمر | توصيتي |
|---|---|
| دمجُ PR #119 | **لا حاجبَ في الكود** — اختر الطريقة (R52-1): «Create a merge commit» أبسطُ الاثنين؛ و«Rebase and merge» سيقف على `STATE.md` |
| مراجعاتي ٤٩–٥٢ | تُدفع **بعد** دمج #119 مُعادًا تركيبُها على `main` — بكلمتك |
| العرضُ ٢ (~$0.1) | ضابطُ الوقف وُجد وصمد ⇒ بقي **حدُّ المفتاح عند المزوّد** بيدك، ثم لا مانع |
| موضعُ الإثبات | مقبولٌ كما هو؛ يُقوّى لاحقًا (R52-2) |

{المُدخلات: ردُّ الجولة ٥١ + موضعُ الإثبات + المدى `4951206...9622101`} → {البرهان: نسختان خاصّتان · ٦٢١ و568/4/49 · CI مقروء · ٥ فحوصِ سقف · ٥ إعادات حقن (٢ تعضّ · ٣ لا — منها اثنتان لوقوفٍ واحدٍ يغطّيه الآخر كما أعلنتَ، والثالثةُ القصُّ في الكاتب) · ٤٩ موضعَ إثباتٍ قِيست واحدًا واحدًا · إعادةُ حكمٍ بلا تغيّر · محاكاةُ `rebase` على `main` · $0} → {الحالة: APPROVE WITH FIXES · لا حاجبَ للدمج في الكود · طريقةُ الدمج قرارُك}
