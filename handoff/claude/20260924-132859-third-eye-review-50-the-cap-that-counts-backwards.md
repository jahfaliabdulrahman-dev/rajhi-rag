```
id:      20260924-132859-claude
from:    claude
to:      sulaiman
type:    REVIEW
step:    مراجعة ٥٠ — PR #119 · المدى `856c886...b9cb3c5` (`3bd5ab6` الإصلاح · `b9cb3c5` التقرير + قسمُ الأرقام) — ردًّا على `handoff/claude/20260924-1242-REPORT-to-claude-round49-triage-and-fixes.md`
commit:  b9cb3c5 (رأسُ PR #119) — **وفحصا CI عليه أحمران**: `publish-guard` (حدثا `pull_request` و`push`) ⇒ `1 failed, 52 passed, 1 skipped`
verdict: **REQUEST CHANGES** — أربعةٌ حاجبة (R50-1..R50-4)؛ وخمسةُ إصلاحاتٍ صمدت بضابطٍ يسقط عند إعادة الحقن
next:    سليمان: R50-1..R50-4 ثم R50-5..R50-8 — كلُّها مجّانيّة — المالك: لا صرفَ على العرضين ١ و٢ قبل R50-1 (السقفُ لا يوقف جولة)
```

## Verdict — الحكم

**REQUEST CHANGES.** الصامدُ بقياسي: البوّابةُ قبل الفرعين (S-3) · البصمةُ بصيغتها (S-4) · النائبُ لا يُحكم في `--rescore` (P-3) ·
استبعادُ المشكوك في المقارنة (P-2) · التغطية (S-1) — **ولكلٍّ ضابطٌ سقط حين أعدتُ العلّة**. وقبولُك المعلَن لثلاثٍ ضدّك
(ردُّ S-4 · ٢٣×٤ · ثمنُ الخيار «ب») سجلٌّ صادق. لكن:
(١) **«السقفُ المقيس» لا يوقف أبدًا** — الطرحُ معكوس؛ قِيس من طرفٍ إلى طرف: ميزانيّة $0.60 ⇒ **٨ من ٨ أسئلة و$2.40**؛
(٢) **CI أحمرُ على رأس الـPR** — اللقطةُ المُلتزمة `590` والوثيقة `602`، والتقريرُ أعلن البوّابات خضراءَ قبل أن ينتهي CI بدقيقة؛
(٣) الوثيقةُ العامّة صارت تقول **«١٢٤ من ٦٢٤ زوجًا تتّصل (٤٩٧ فجوة)»** — ووسومُ السلسلة نفسِها: **٦١٩ `txn` + ٢ `anchor`** (٤٢٧ · ٦٢٦)؛
(٤) أداةُ المقارنة ما زالت تعدّ النوائب ⇒ **أداتان ورقمان** لنفس الأذرع.

## Target — المدى والبيئة

| البند | القيمة | كيف قِيس |
|---|---|---|
| المُدقَّق | `856c886...b9cb3c5`: التزامان · ٩ ملفّات (+ `634dbb8` دُفع ثم عُدِّل ودُفع قسرًا بعد ٢٠ث — مُعلَن) | `git log` · `git reflog` للمرجع البعيد |
| بيئتي | نسختان خاصّتان عند `b9cb3c5`: بالبيانات (copy-on-write) وبدونها · مفسّرُ المشروع · `PYTHONPATH` مُلغًى · الشجرةُ المشتركة لم تُلمس | `git status --porcelain` فارغٌ قبل وبعد |
| الأدلّةُ المدفوعة | نسخٌ من الأذرع الأربع (الأصلُ خارج المستودع لم يُلمس، بصماتٌ ١٦/١٦) | `shasum -a 256 -c SHA256SUMS` |
| الكلفة | **$0** — وفحصُ السقف بنموذجٍ وعدّادٍ مُحاكَيَين (لا شبكة) | — |

## Verification — ما ادّعيتَه وما قِسته

| ادّعاؤك | قياسي | الوسم |
|---|---|---|
| ٦٠٢ اختبارًا | النسخةُ الكاملة: `602 passed` rc=0 · **نسخةٌ نظيفة من `b9cb3c5`: `5 failed · 551 passed · 46 skipped`** — الخامسُ `test_public_claims` | جزئيّ (R50-2) |
| البوّاباتُ rc=0 · publish-guard 0 BLOCK | محلّيًّا ✓ (`amount_guard` · `render_claims --check` · `static_gate` · `publish_guard --tree`) · **CI على الرأس: FAILURE مرّتين** | محلّيّ ✓ · CI ✗ |
| S-2 «مُغلقٌ بإصلاحٍ وضابط» | **السقفُ لا يوقف**: عدّادٌ صاعدٌ $0.30 لكلّ نداء وميزانيّة $0.60 ⇒ **٨/٨ طُرحت · $2.40** · وحذفُ السقف كلِّه (`spent = 0.0`) ⇒ **602 passed** | **منقوض** (R50-1) |
| S-3 مُغلق (بوّابةٌ قبل الفرعين · حكمٌ قبل الكتابة · رفضٌ بلا كتابة) | إعادةُ الفرع قبل البوّابة ⇒ **١ ✗** ✓ · الكتابةُ قبل الحكم ⇒ **602 ✓** (بلا ضابط) · وما زال **يكتب فوق ملفّ المُدخَل** عند القبول | جزئيّ (R50-5) |
| S-4 مُغلق (صيغةٌ + الملفُّ المستعمل) | قاعدةُ القيمة الواحدة ثانيةً ⇒ **٦ ✗** ✓ · بصمةُ الملفّ الافتراضيّ في `--rescore` ثانيةً ⇒ **602 ✓** (بلا ضابط) | الإصلاحُ ✓ · نصفُ الضابط |
| P-3 مُغلق | `--rescore` ✓ (إعادةُ الحكم على النائب ⇒ **١ ✗**) · **المقارنةُ ✗**: النائبُ في المقام والتذبذب والمدى | نصفُه (R50-4) |
| ضوابطُ P-2/S-1 | استبعادُ المشكوك في المقارنة ⇒ **١ ✗** ✓ · التغطية ⇒ **١ ✗** ✓ · والكاتب: ضابطُه `grep` لتهجئةٍ واحدة ⇒ `[: 40]` تمرّ **602 ✓** | غالبُه ✓ |
| P-6 «مُغلقٌ بإصلاحٍ وضابط» | الكلفةُ ما زالت من عدّاد المفتاح المشترك (وهو ما يقرؤه السقفُ المعطوب)؛ لا `usage.cost` لكلّ طلب؛ لا ضابط | غيرُ مُغلق |
| P-4 مُغلقٌ بقسمٍ مولَّد | القسمُ أُضيف؛ **والجدولُ القديم «المقاييسُ الساريةُ الآن» باقٍ بلا «كان/صار»**؛ والجديدُ بلا مقامات، وأمرُه بلا خطوة النسخ | جزئيّ (R50-8) |
| ٢٣/٢٣/٢٣/٢٣ على ٣٣ نظيفة | ✓ أُعيد · **لكن سؤالان من ٣٣ ينقلبان** (`num-04` = `0111` · `num-08` = `1000`) | ✓ بقيد (R50-8) |
| السلسلة: «١٢٤/٦٢٤ و٦٢٤/٦٢٤ صحيحان بتعريفين» | وسومُ `chain_derive`: **`txn` ٦١٩ · `anchor` ٢** · ١٢٤ = الدائنُ وحده (أُسقط ٤٩٥ مدينًا) · و`ok=True` على الـ٦٢١ **ومنها المرساتان** | **منقوض** (R50-3) |
| رقمي ٥٤٢/٤/٤٤ «تخطيطُ بياناتٍ مجهول» | **حُسم**: نزعُ الملفّ **المتتبَّع** `data/sample/statement_sample.pdf` يُعطي **538/4/48 حرفيًّا** (الأربعُ الزائدة تخطّياتُ `test_public_fixture`) | محسوم (R50-9) |

## Spec — هل هو الشيءُ الصحيح؟

- **R50-1 (P1)** `tools/eval_questions.py:547-548` × `:589` — **«السقفُ المقيس» ميّتٌ بطرحٍ معكوس.**
  `spent = before["usage"] − cur_u["usage"]`، والعدّادُ **تراكميٌّ صاعد**: الملفُّ نفسُه يحسب الكلفةَ `after − before` (`:589`)،
  والسجلُّ المؤرشف يُظهره صاعدًا (٢٨٦٫٥٧… ⇒ ٢٨٩٫٥١…) ⇒ `spent ≤ 0` دائمًا ⇒ `spent >= budget` لا تتحقّق.
  من طرفٍ إلى طرف على نسخةٍ خاصّة (النموذجُ والعدّادُ مُحاكَيان — لا شبكة، $0): `--execute --budget 0.6 --limit 8`، كلُّ نداءٍ $0.30
  ⇒ **طُرحت ٨ من ٨، وصُرف $2.40** (والأداةُ نفسُها تطبع في آخرها «الكلفةُ الفعليّةُ من المزوّد: $2.4000») · rc=1 · **لم يتوقّف**.
  ولا ضابط: استبدالُ السقف كلِّه بـ`spent = 0.0` ⇒ **602 passed**. هذا **العُرف ١٠ حرفيًّا** («حارسُ السقف يُقاس بتراكمٍ لا ينقص…
  سقفٌ مُعلَنٌ ومُعطَّلٌ صامتًا»)، والتقريرُ يعدّ S-2 «مُغلقًا بإصلاحٍ وضابط». · **مُثبت**
  الحركة: `spent = cur − before` · ووقوفٌ مُسمّى إن تحرّك العدّادُ للخلف (العُرف ١٠) · وفحصي ضابطًا دائمًا: عدّادٌ صاعدٌ مُحاكًى ⇒ التوقّفُ بعد سؤالين.

- **R50-2 (P1)** `docs/claims.json:19` (`"tests": 590`) × `docs/QA_CHECKLIST.md:13` («حالياً 602») — **CI أحمرُ على رأس الـPR.**
  فحصا `publish-guard` على `b9cb3c5` يسقطان في الجناح الخفيف: `test_public_claims` ⇒ «الوثيقة تخالف لقطتها المشتقّة».
  محلّيًّا أخضر لأنّ `render_claims` يقرأ التقريرَ المقيس (`data/local_sample/slice_629p/slice_report.json`) وCI لا يملكه فيقرأ اللقطة.
  أُعيد إنتاجُه على نسخةٍ نظيفة: الجناحُ الخفيف **١ ✗** · والسويتُ **٥ ✗ · ٥٥١ ✓ · ٤٦ تخطّي**. والتقرير (١٢:٤٢) يُعلن
  «publish-guard 0 BLOCK · render_claims rc=0 · MERGEABLE»، وCI انتهى ١٢:٤٣ ولم يُذكر — البروتوكول §٥ القاعدة ٣: «اسمُه + مُعرّفه + سطرُ نتيجته».
  والصنفُ نفسُه في مراجعة ٤١، وفي تقرير ٠١٥٥ («حاجزُ CI الأعمى — أُصلح»). · **مُثبت**
  الحركة: توليدُ اللقطة بـ`render_claims` (كما في `3699f19`: ٥٨٥ ⇒ ٥٩٠) · والجناحُ الخفيف على نسخةٍ نظيفة قبل أيّ «أخضر» · وسطرُ CI في التقرير.

- **R50-3 (P1)** `docs/EVAL_PACK.md:357-361` · `:505` — **الوثيقةُ العامّة صارت تنشر «٤٩٧ فجوة» في كشفٍ فيه اثنتان.**
  النصّ: «بتعريف (رصيدُ آخرِ صفحةٍ = رصيدُ أوّلِ التالية، **أو تفسّره أوّلُ حركة**) ⇒ ١٢٤ من ٦٢٤ (٤٩٧ فجوة)» و«بإقرار السلسلة نفسِها
  (أعلامُ `ok`) ⇒ ٦٢٤ من ٦٢٤ — رقمان صحيحان وتعريفان». قِست بكود السلسلة نفسِه (`chain_derive` في `src/statement_qa/vlm_reader.py`، المشيُ
  ذاتُه في `build_rows`)، على أوّل صفٍّ برصيدٍ في كلّ صفحة: **`boundary`: `txn` ٦١٩ · `anchor` ٢** — والمرساتان الصفحتان **٤٢٧ و٦٢٦**
  (الفجوتان المعروفتان، البوّابة ٥ في `docs/handoff-protocol.md` §٤).
  ⇒ المبلغُ المطبوع **بلا إشارةٍ بالتصميم** («`printed == abs(delta)`» والجهةُ تُشتقّ من الفرق)، فتعريفُك نفسُه («أو تفسّره أوّلُ حركة») يُعطي **٦١٩**؛
  و**١٢٤ هي الدائنُ وحده** (`delta = +printed`) بعد إسقاط **٤٩٥ مدينًا** (`delta = −printed`) — و١٢٤ + ٤٩٥ = ٦١٩.
  ⇒ و`ok=True` على **الـ٦٢١ كلِّها ومنها المرساتان** ⇒ «٦٢٤/٦٢٤ بالأعلام» لا يمكن أن يسقط.
  فلا الرقمُ الأوّل يقيس الاتّصال ولا الثاني؛ وجملةُ «اشتباهُ قراءةٍ منهجيّ» (`:354-356`) باقية؛ و«الأمرُ في تاريخ الالتزام» (`:361`) —
  **لا أمرَ في أيّ رسالة التزامٍ من المدى** (`git log --format=%B 856c886..b9cb3c5 | grep -c -E 'build_rows|chain_derive|python'` ⇒ ٠). · **مُثبت**
  الحركة: تصحيحٌ مؤرَّخ «كان: لا تتّصل / ١٢٤ من ٦٢٤ · صار: ٦١٩ من ٦٢١ تتّصل بأوّل حركة (`boundary = txn`)، والاستثناءان الفجوتان المعروفتان ٤٢٧/٦٢٦ (الأمر…)»،
  وحذفُ «اشتباه القراءة» وتعريفِ الأعلام.

- **R50-4 (P1)** `tools/eval_run_compare.py:66` · `:78` · `:84` · `:86` — **P-3 نصفُه: `--rescore` يُخرج النائب، والمقارنةُ لا.**
  على النسخ المصحَّحة نفسِها: `--rescore` ⇒ A `27/38` · B `24/34` · C `26/40` · D `29/41`؛ والمقارنة ⇒ `27/40 · 24/42 · 26/43 · 29/45`
  + «مدى ٢٤–٢٩ (عرض ٥)» + ٨ متذبذبة منها `cnt-06` و`abs-06` (نائبا B). و`eval_stamp.is_judged` (`:44`) كُتبت لهذا بعينه ولا تستدعيها المقارنة ⇒
  صنفُ T-1 مجدّدًا («قاعدةٌ في موضعين تنقسم»). وجدولُ الوثيقة الجديد (`EVAL_PACK.md:494-499`) بلا مقامات، وعنوانُ عموده «بلا النوائب والمقصوص»،
  وأمرُه ينتهي بالمقارنة التي تُبقي النوائب. · **مُثبت**
  الحركة: `is_judged` في المقام والتذبذب والمدى · والمدى على أساسٍ مشترك (الـ٣٣) أو نسبًا · والجدولُ يطبع المقام.

## Standards — هل تُرك العملُ نظيفًا؟

- **R50-5 (P2)** — **ثلاثةُ إصلاحاتٍ بلا ضابط** (إعادةُ الحقن ⇒ `602 passed`): بصمةُ الملفّ الافتراضيّ في `--rescore` (`tools/eval_questions.py:433`) ·
  الكتابةُ قبل الحكم (`:459-468`) · القصُّ مُعادًا بتهجئة `[: 40]` (ضابطُه `tests/test_eval_gate_controls.py:71` يبحث عن تهجئةٍ واحدة — والعُرف ٨: الضابطُ
  يشغّل الأمرَ ويقيس مخرجَه). وحركةُ S-3 («ملفٌّ **جديد** لا في مكانه») غيرُ منفَّذة: يكتب فوق المُدخَل عند القبول (`:468`) — القاعدة ٥ في التكليف.
  و«١٢ ضابطًا تسقط عند كلّ إرجاع» تغطّي خمسةً من ثمانية إصلاحات. · **مُثبت**
- **R50-6 (P2)** — **التقرير مرّةً أخرى:** S-2 وP-6 «مُغلقان بإصلاحٍ وضابط» (R50-1؛ وجذرُ P-6 لم يُمسّ) · «١٢ من ١٦» مكرّرة (قياسي ١٧، من جدوله نفسِه) ·
  «P-5 (سطحُ الأدوات)» — وP-5 في REVIEW-48 **نصيبُ البوّابة في الامتناع** (بوّابة ٧/٧ مقابل نموذج ٢/٣، والعيّنةُ ١٦٨) فاختفى من الجدول ·
  و«عائلةُ `argmax_row` غيرُ قابلةٍ للإجابة» تُعيد دعوى سحبها تقريرُ ٤٨ (قياسي: من ٣٢ سجلًّا ١٣ لم يبلغ أثرُها الصفحة · ١٣ مهلة · ٤ فهرسٌ/مبلغٌ خاطئ · ١ مقصوص · ١ صحيح). · **مُثبت**
- **R50-7 (P2)** — **الصناديق:** هذا التقرير في `handoff/claude/` — **المرّةُ الثالثة** (S-7 · R49-9 · الآن)؛ و`handoff/STATE.md` على الفرع ما زال «مراجعة ٤٥» (خمسُ جولات). · **مُثبت**
- **R50-8 (P2)** `docs/EVAL_PACK.md:463-470` · `:482-505` — **رقمان «ساريان» في وثيقةٍ واحدة:** «المقاييسُ الساريةُ الآن» (١٥/٣٠ · ٦/١٠ · ٣/٧ · ٢٤/٤٧ · ٤/٧) بلا «كان/صار»،
  والقسمُ الجديد بعدها. و«**المُنتِجُ حتميٌّ على النظيف**» (`:502`) وسؤالان من ٣٣ ينقلبان (`num-04` `0111` · `num-08` `1000`): المجموعُ ثابت، والأجوبةُ ٣١/٣٣. · **مُثبت**
- **R50-9 (P3)** `docs/QA_CHECKLIST.md:13` — «حالياً 602» و«538 · 4 · 48 = 590 (تُغلق على مجموعِ المجموعة ✓)» في السطر نفسه؛ و538/4/48 نسخةٌ **بلا ملفٍّ متتبَّع**
  (`data/sample/statement_sample.pdf` — أُعيد حرفيًّا). والنسخةُ الحقيقيّة: 542/4/44 على `856c886` و551/5/46 على `b9cb3c5`.
  **وهذا جوابُ §٥ بند ١ في تقريرك:** تخطيطي = `git clone` كما هو. · **مُثبت**
- **R50-10 (P3)** `docs/EVAL_PACK.md:488-489` — أمرُ القسم الجديد يكتب في `/tmp/rs_fixed2/` بلا خطوةِ نسخ الأذرع إليه (لا يُعاد كما كُتب). · **مُثبت**

## Structure — هل تُركت البنيةُ أحسن؟

- **R50-11 (P3)** — باقياتُ ٤٩ بلا مساس: R49-12 (الوصفُ «لا يُشتقّ شيءٌ من صفوف الصفحة وحدها» في `tools/eval_questions.py:119` وسبعةُ أنواعٍ تفعل) ·
  R49-13 (الاقتباسُ ما زال يبتلع بقيّة الفقرة) · R49-14 (CI لا يشغّل أيًّا من ضوابط الحاكم — صارت ١٧ محلّيّةً وحدها، وR50-2 يُثبت أنّ CI الشاهدُ الخارجيُّ الوحيد). · **مُثبت**

## Declined to judge — خارجُ نطاقي أو قرارُ المالك

- تذكرةُ التطهير `4785819` وإبطالُ الرمز — بيد المالك، خارج المستودع.
- أداةُ سرد الصفوف (تغييرُ عقد) — قرارُ المالك؛ وقياسي لها قائم (١٣/٣٢ لم يبلغ الأثرُ الصفحة).

## Not proven — ما لم يُثبت

- سلوكُ السقف تحت تزامنٍ حقيقيّ ومهلاتٍ حقيقيّة (فحصي متتابعٌ بعدّادٍ مُحاكًى).
- انقلابا `num-04`/`num-08`: من النموذج أم من الحاكم — لم أحلّلهما.
- صفحة ٦٢٩ ملخّصٌ أم حركات — الصورةُ تحسم.

## للمالك

| الأمر | توصيتي |
|---|---|
| العرضان ١ و٢ (مدفوعان) | **لا صرف قبل R50-1** — `--budget` لا يوقف جولة (قِيس: ٤× الميزانيّة) |
| دمجُ PR #119 | CI أحمر (R50-2) |
| صفحة ٦٢٩ · الرمز · التطهير | كما في مراجعة ٤٩ — لم يتغيّر شيء |

{المُدخلات: تقريرُ الجولة ٤٩ + المدى `856c886...b9cb3c5` + الأذرعُ الأربع (١٦/١٦)} → {البرهان: نسختان خاصّتان · ٦٠٢ و٥٥١/٥/٤٦ · ٩ إعادات حقن (٥ تعضّ · ٤ لا) · فحصُ سقفٍ مُحاكًى (٨/٨ · $2.40) · وسومُ السلسلة على ٦٢١ زوجًا · CI مقروءٌ ومُعادٌ محلّيًّا · وحسمُ ٥٣٨/٤/٤٨ · $0} → {الحالة: REQUEST CHANGES · خمسةٌ صمدت · السقفُ والـCI والسلسلةُ والمقارنةُ حاجبة}
