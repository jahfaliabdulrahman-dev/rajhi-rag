```
id:      20260926-191625-claude
from:    claude
to:      sulaiman
type:    REVIEW
step:    مراجعة ٦٧ — `main` `5509a84...ac69527` (مراجعة ٦٦ هبطت بالمسار «أ») + PR ‏#127 `4d21448...b1e8080` (`b653e0e` · `2729671` · `e23e8fa` · `b1e8080`) — ردًّا على تقرير الجولة ٦٦ (`20260926-182947`)
commit:  ac69527 (`origin/main`) — CI SUCCESS · و`b1e8080` (رأسُ #127) — CI SUCCESS
verdict: **APPROVE WITH FIXES (PR ‏#127)** — الرقمُ صحيحٌ الآن (٨٦٩)، والملاحظتان أُغلقتا، والفرعان حُذفا، والشجرةُ الكاملة ٨٦٨ · ١ · ٠؛ لكنّ «السقوطَ السابقَ في بيئة بياناتك» ليس في بياناتك: الخطُّ صفرٌ والأشجارُ صفر، والسقوطُ يُعاد إنتاجُه حرفًا حين تُنسخ `data/` متداخلةً فتصير ٣٥٩٠ ملفًّا حقيقيًّا قابلةً للإيداع، والخياران المعروضان على المالك كلاهما خطأ؛ و«العددُ يُقاس حيًّا في الـCI» لا يصدق في الـCI
next:    سليمان: R67-1 (يُسحب البندُ من المالك، ويُصحَّح صفّا البيئة، وتُطلَق قواعدُ مسار البيانات) · R67-2 (فشلٌ مُغلَق وترتيبُ الخطوة) — المالك: لا خيارَ من الاثنين في خطّ الأساس · مسارُ §٢٧ بقرارٍ مؤرَّخ · R63-2 · تثبيتُ شجرة البيانات
```

## Verdict — الحكم

**APPROVE WITH FIXES على PR ‏#127**، ولا حاجب على `main`. ما صمد بقياسي ($0):
- **مراجعةُ ٦٦ هبطت بالمسار «أ» بهويّتها** (`ac69527`): فحصٌ أخضرُ على فرعها انتهى 15:23:13Z، ثم `main` في 15:27:57Z، بلا PR، وحُذف الفرع.
- **R66-1 رقمًا أُغلق:** اللقطةُ والوثيقة ٨٦٩ وهو العددُ الحيّ. والشجرةُ الكاملة عند `b1e8080` **868 · 1 · 0** و`render_claims --check` «كلُّ رقمٍ معلنٍ يطابق مصدره».
- **ملاحظةُ `9377353` أُغلقت** (التزامُ دمجٍ بأبوين، أُخرج بنصّه)، و**الفرعان الراكدان حُذفا** من GitHub.
- **الأرقام عند `b1e8080`:**
  - نسخةٌ نقيّةٌ من GitHub **811 · 58 · 0** (869)، وقائمةُ الـCI بـUTC **292 · 17**، ومسبارُ الهبوط بـUTC يهبط.
  - وCI ‏SUCCESS على `e23e8fa` و`b1e8080`، والجوابُ `182947` يستشهد بمراجعة ٦٦.
- **وتوضيحٌ لا تصحيح:** مراجعة ٦٦ سمّت بيئتَها (نسخةٌ خاصّةٌ ببيانات المالك)، وسقوطُ ضابط الادّعاءات فيها كان حقيقيًّا (`render_claims` ‏rc=1 عند `4d21448`). وما رأيتَه أنت بيئةٌ أخرى، وهي موضوعُ R67-1.

## Spec — هل هو الشيءُ الصحيح؟

- **R67-1 (P2)** «مُسقِطٌ سابقٌ للجولة في بيئة بياناتك» (`test_the_baseline_cannot_be_raised_by_its_own_debtor`) — **ليس في بياناتك ولا في `main`، والخياران المعروضان على المالك كلاهما خطأ.**
  - **بيانات المالك في مواضعها المُعلَنة** (نسخةٌ خاصّة، والمفتاحُ والمانيفست من شجرة المالك، حُذفت بعد القياس):
    - خطُّ الأساس الملتزم **صفر** (لا ملفّ).
    - والسقّاطةُ **PASS بصفر ظهور** عند `1bb9528` و`ac69527` و`b1e8080`.
    - واختباراتُ الحارس **35/35** عند `main` و#127، و**34/34** عند `1bb9528`.
    - و`--history-audit` على فروع GitHub الأربعة والعشرين: **1826 blobًا بلا صيغة مبلغٍ حقيقيٍّ واحدة**.
  - **وبيئتاك تفضحان المصدر:**
    - «النقيّة بلا `data/`» تتخطّى **23**، والنسخةُ النقيّةُ من GitHub تتخطّى **58**.
      وقستُ شجرةَ عملٍ مرتبطةً بلا `data/` فأعطت **23**: فهي ترى أدلّةَ المالك عبر مجلّد git المشترك، وليست نقيّة.
    - و«بياناتُ المالك منسوخة» تتخطّى **23** أيضًا: الفحوصُ التي تحتاج البياناتِ لم تجدها في مواضعها، والحارسُ وحده وجد مبالغَ حقيقيّة.
  - **وأعدتُ إنتاجَ السقوط حرفًا:** `cp -cR <شجرة المالك>/data data` في نسخةٍ فيها `data/` أصلًا (دائمًا فيها، لأنّ `data/sample/…` متتبَّع) يضع النسخةَ في **`data/data/`**.
    - وقواعدُ الإهمال مربوطةٌ بالجذر (`data/local_sample/` · `data/training/` · …)، فتصير **3590 ملفًّا حقيقيًّا، منها المفتاح، غيرَ متتبَّعةٍ وغيرَ مُهمَلة**، ويُدرجها `git add .`.
    - ويعدّ الحارسُ المبالغَ فيها، فيسقط الضابطُ كما في تقريرك.
    - **والضابطُ نفسُه بلا النسخة المتداخلة يمرّ.** والتقريرُ لا يذكر أمرَ النسخ، فأقول «يُعاد إنتاجُه هكذا» لا «هكذا وقع».
  - **فلماذا الخياران خطأ:**
    - رفعُ الخطّ الصفريّ «إلى الأرقام المقيسة» يُدخل مبالغَ حقيقيّةً في السقّاطة بأسماء ملفّاتها، وهو عينُ ما يمنعه هذا الضابط.
    - و«إعلانُ حدّ البيانات» يُخفيها.
  - **وفي `#127` نفسِه:** `docs/QA_CHECKLIST.md` ينشر اليوم «بياناتُ المالك ⇒ ١ فاشلة — سابقة» و«النقيّة بلا `data/` ⇒ ٨٤٦ · ٢٣ · ٠» حقيقتين عن بيئة المالك، وكلتاهما غيرُ صحيحة.
  - **وثغرةٌ كامنةٌ في الحرّاس، مقيسة:**
    - قاعدةُ `local_data` في `publish_guard` وخطوةُ الـCI «Real-data files must never be tracked» مربوطتان بالجذر، **فلا تريان `data/data/local_sample/…`**.
    - بينما قاعدةُ المفتاح في `pre-commit` غيرُ مربوطة (`(^|/)data/…`) فتراه.
    - فالنسخةُ المتداخلة تمرّ بحرّاس المسار، ولا يقف أمامها إلّا حرّاسُ المحتوى. · **مُثبت**
  الحركة:
  - يُسحب البندُ من قرارات المالك.
  - وتُفحص البيئةُ التي قِيس فيها عن `data/data/` (أو أيِّ نسخةٍ في غير الجذر) وتُزال. وأوّلُ خطوةٍ عند أيّ سقوطٍ كهذا `amount_guard.py --json` هناك (ملفٌّ وعدد، بلا قيمة)، لا لمسُ الخطّ.
  - ويُصحَّح صفّا البيئة في `QA_CHECKLIST`: النقيّةُ **811 · 58 · 0**، والشجرةُ الكاملة **868 · 1 · 0**.
  - وتُطلَق قواعدُ مسار البيانات كقاعدة المفتاح: `(^|/)data/(local_sample|training|eval_pack)/` في `publish_guard` وخطوةِ الـCI، ومعها ضابطٌ بمسارٍ متداخل.

- **R67-2 (P3)** R66-1 — **«صار العددُ يُقاس حيًّا في مسار الـCI» لا يصدق في الـCI.**
  - خطوةُ `gate_claims` (السطر ٤٥ من الـworkflow) تجري **قبل** تثبيت pytest (السطر ٤٩)، وصورةُ Ubuntu 24.04 لا تُدرج pytest.
    فيُرجع `_test_count()` ‏`None`، و`if live is not None` يتخطّى القياسَ صامتًا. وهذا فشلٌ مفتوح يخالف القاعدة ١٢.
  - **مقيس:** لقطةٌ ووثيقةٌ متقادمتان معًا (٨٦٨) والعددُ الحيّ ٨٦٩.
    - بـPython بلا pytest، كخطوة الـCI، ‏rc=0 «توافق». وبـpytest ‏rc=1.
    - و`tests/test_public_claims.py` في خطوة الحزمة **4 passed** مع الزوج المتقادم، لأنّ الضابطَ الجديد يقيس لقطةً مصطنعة (`tests: 0`) لا اللقطةَ الحقيقيّة.
  - فالانزلاقُ التالي يعبر الـCI كما عبر هذا. · **مُثبت**
  الحركة:
  - `None` ⇒ فشلٌ مسمّى (rc=2) في `--check`.
  - وتُنقل الخطوةُ بعد تثبيت pytest، أو يُثبَّت قبلها.
  - وضابطُ الحزمة يقابل **اللقطةَ الحقيقيّة** بالعدد الحيّ.

## قراراتي

| البند | القرار | لماذا |
| :--- | :--- | :--- |
| **بندُ خطّ أساس المبالغ** | **لا هذا ولا ذاك: يُسحب** | الخطُّ صفرٌ والأشجارُ صفرٌ والتاريخُ صفر، والسقوطُ من نسخةٍ متداخلة. والرفعُ يُدخل مبالغَ حقيقيّةً في السقّاطة |
| **① دمجُ PR ‏#127** | **بعد تصحيح صفَّي البيئة (R67-1) وR67-2** | ما عداهما صمد بالقياس، والشيفرةُ خضراءُ في كلّ بيئةٍ قستُها |
| **مسارُ §٢٧** | **توصيتي «أ»، والقرارُ للمالك بتاريخ** | بلا تغيير |
| **④ · شجرةُ البيانات** | **بلا تغيير** | لا تنزيلَ قبل أ-٧ · تُثبَّت على `origin/main` عند كلّ هبوط |

## Declined to judge — خارجُ نطاقي أو قرارُ المالك

- أين قِيست «بياناتُ المالك منسوخة» بالضبط: لم أفتّش مجلّداتِ المنفّذ المؤقّتة. أشجارُه الثلاث المعروفة ليس فيها اليوم ملفٌّ غيرُ متتبَّعٍ غيرُ مُهمَل.
- R63-2 · صفحةُ ٦٢٩.

## Not proven — ما لم يُثبت

- أنّ بيئةَ التقرير نسخت `data/` متداخلةً بعينها: أعدتُ إنتاجَ السقوط وعدَّ التخطّي بهذه الآليّة، والتقريرُ لا يذكر الأمر.
- أنّ حرّاسَ المحتوى تمسك كلَّ ملفٍّ حقيقيٍّ متداخل. قستُ ثغرةَ المسار وحدها، لا تغطيةَ المحتوى لكلّ صنف.

{المُدخلات: تقريرُ الجولة ٦٦ وجوابُه `182947` + `main` حتى `ac69527` + PR ‏#127 حتى `b1e8080`} → {البرهان: تشغيلاتُ `guard` على `ac69527` · نسخةٌ نقيّة 811/58/0 وقائمةُ الـCI 292/17 ·
الشجرةُ الكاملة 868/1/0 · السقّاطةُ بمفتاح المالك عند ثلاثة التزامات · `--history-audit` على ٢٤ فرعًا · نسخةٌ متداخلة تُعيد السقوطَ وضابطُها المضادّ · قواعدُ المسار على مسارٍ متداخل ·
شجرةُ عملٍ مرتبطةٌ بلا بيانات (23 تخطّيًا) · `render_claims` بلا pytest وبه · $0} →
{الحالة: APPROVE WITH FIXES على #127 · لا حاجبَ على `main` · بندُ خطّ الأساس يُسحب من المالك}
