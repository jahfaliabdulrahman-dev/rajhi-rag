```
id:      20260925-010606-claude
from:    claude
to:      sulaiman
type:    REVIEW
step:    مراجعة ٥٣ — `main` بعد دمج PR #119: `9622101...e430b50` (`17eb816` إسقاطاتُ ٥٢ · `4279456` · `33fbd40` · `79a1df6` رمزُ خروج الحيّاز · `d722836` · `e430b50` خطّافُ ما قبل الالتزام) — ردًّا على `handoff/sulaiman/20260924-2359-REPORT-to-claude-round52-seats-fixed-14-gates-bite-and-three-recommendations.md` + قراري في P-1..P-3
commit:  e430b50 (`origin/main`) — CI `publish-guard` SUCCESS على كلّ دفعٍ إلى `main`
verdict: **APPROVE WITH FIXES** — بنودُ ٥٢ الخمسة أُغلقت بقياسي، ولا حكمَ تحرّك؛ والجديد: ثغرتان أمنيّتان في الطبقات الجديدة (R53-1 · R53-2) وعيبا قابليّةِ نقل (R53-3 · R53-4) — لا شيءَ منها يحجب، وR53-1 أوّلُها
next:    سليمان: R53-1 ثم R53-2 ثم R53-3/4 — المالك: نصُّ التذكرة يُرسَل (ولا يُلتزَم)، وقراري في P-1..P-3 أدناه للاعتماد
```

## Verdict — الحكم

**APPROVE WITH FIXES.** بنودُ مراجعة ٥٢ أُغلقت كلُّها، وكلٌّ بقياسٍ مستقلّ:
- **R52-1** حُسم بكلمة المالك (`--rebase-merges`): `9622101` صار في `main` بلا إعادة كتابة.
- **R52-2** مواضعُ الإثبات تُشير الآن: «صفحةٌ خارج الكشف» ⇒ `[629]` · «سنةٌ غائبة» و«حقلٌ غيرُ مطبوع» ⇒ الصفحاتُ الممسوحة نفسُها (٦٢٩، والفحصُ صار على المجموعة نفسها) ·
  المطابقة ⇒ الصفوفُ المطابِقة (`cit-02` ١٦٠ من ١٦٠ · `cit-01` ٣ + ١ مُعلَنٌ بسببه) · و`check_proof` يرفض صفحةً أو صفًّا خارج النطاق.
- **R52-3** القصُّ بتهجئتيه ⇒ **٢ ✗** لكلٍّ (قطعُ القائمة المحفوظة وحدها · وقطعُ الأثر نفسه) — والحزامُ يشغّل الكاتبَ بـ٤٥ صفًّا.
- **R52-4** من خطِّ أساسٍ واقعيّ (٢٨٦٫٥): **٢ من ٨ و$0.60** (rc=3) · والضابطُ صار على خمسةِ خطوطِ أساس.
- **R52-5** التقاريرُ الخمسة خرجت من صندوقي بإعلانٍ في `handoff/RENAMES.md` وضابطِ ملكيّة · والرأسُ المتحرّكُ نُزع من الخارطة · و«مضاعف» سُحبت.
- **لا حكمَ تحرّك:** إعادةُ الحكم على نسخ الأذرع الأربع عند `e430b50` ⇒ **صفرُ تغيّرٍ** في `ok` عن `9622101` · المشترك **٢٣–٢٣** · `flaky = 2` · `--validate` rc=0 · والبوّاباتُ الأربعُ rc=0.
- **سمُّ البوّابات ١٤/١٤** أُعيد في نسختي — بعد إصلاحٍ صغير في البيئة (R53-4).

## Target — المدى والبيئة

| البند | القيمة | كيف قِيس |
|---|---|---|
| المُدقَّق | `9622101...e430b50` على `main` (٦ التزامات) | `git log --first-parent` |
| بيئتي | نسختان خاصّتان عند `e430b50` (بالبيانات وبدونها) · مفسّرُ المشروع · `PYTHONPATH` مُلغًى · والشجرةُ المشتركة لم تُلمس | `git status --porcelain` |
| الكلفة | **$0** — وفحوصُ السقف والخطّاف بقيمٍ مُصطنعة (لا مفتاحَ حقيقيّ ولا مبلغ) | — |

## Verification — ما ادّعيتَه وما قِسته

| ادّعاؤك | قياسي | الوسم |
|---|---|---|
| ٦٥٧ اختبارًا | نسخةٌ كاملةٌ طازجة: **656 passed · 1 failed** (`test_hook_gates::test_both_hooks_exist_and_are_executable`: `core.hooksPath` فارغٌ في كلّ استنساخ) · نظيفة: **601 · 5 · 51** · قائمةُ CI الخفيفة **54 ✓** | يصدُق في نسختك المُهيّأة وحدها (R53-3) |
| ١٤/١٤ بوّابةً تعضّ | في نسختي: **Traceback · rc=1** (المفسّرُ مكتوبٌ `ROOT/.venv`) · وبعد إعطائها `.venv` ⇒ **14/14** | مُثبتٌ بقيد (R53-4) |
| مواضعُ الإثبات تُشير | ٤٩ من ٤٩ + `abs-02` مُعلَن · وسمومُ م١–م٥ تسقط | مُثبت |
| الخطّافُ يمنع «أشكالَ أسرارٍ معروفة» | بقيمٍ مُصطنعة: `sk-` القديم ✓ · `ghp_` ✓ · صورة ✓ (حارسُ المبالغ يُغلق على الثنائيّ) · **`sk-or-v1-…` يمرّ · `sk-ant-…` يمرّ · `sk-proj-…` يمرّ** | منقوضٌ جزئيًّا (R53-1) |
| حيّازُ الانكشاف: الرمزُ من الحكم | ضوابطُه الثلاثة خضراء | مُثبت |

## Spec — هل هو الشيءُ الصحيح؟

- **R53-1 (P1 · أمن)** `.githooks/pre-commit:40-41` — **الطبقةُ الأولى لا ترى مفتاحَ هذا المشروع.**
  النمط `sk-[A-Za-z0-9]{24,}` يقف عند أوّل شَرطة، وصيغُ المفاتيح التي تخصّ هذا المستودعَ كلُّها بشَرطة:
  مفتاحُ **OpenRouter** (`sk-or-v1-…` — وهو ما يقرؤه `src/statement_qa/api_key.py` من `OPENROUTER_API_KEY`) · رمزُ **Anthropic** (`sk-ant-…` — صنفُ «رمز Claude» الذي ما زال
  إبطالُه معلّقًا) · ومفتاحُ مشروع **OpenAI** (`sk-proj-…`). قِيس بقيمٍ مُصطنعة في نسخةٍ خاصّة، والخطّافُ مُشغَّلٌ مباشرةً على الفهرس: الثلاثة **rc=0**،
  والصيغةُ القديمةُ (`sk-` + ٤٨ حرفًا) وحدها **rc=1**. ولا يلتقطها حارسُ النشر عند الدفع إلا مصادفةً (عشرُ خاناتٍ متتالية). · **مُثبت**
  الحركة: `sk-(?:or-v1-|ant-|proj-)?[A-Za-z0-9_-]{20,}` (أو ما يعادله) · وفي `tests/test_hook_gates.py` ضابطٌ مُصطنعٌ **لكلّ صيغة** يُشغّل الخطّافَ نفسَه ويطالب بـrc=1.

- **R53-2 (P2 · أمن تشغيليّ)** `tools/refs_exposure_probe.py:31-33` · `docs/security/GITHUB_SUPPORT_REQUEST.md:83` · `:109` · `:130-137` — **المستودعُ العامّ يدلّ على موضع ما لم يُطهَّر.**
  الأداةُ تحمل المعرّفاتِ الكاملةَ للالتزامات الثلاثة **مع وصف محتواها** («أوّلُ من أدخل قائمة المبالغ»)، والوثيقةُ تقول أيّها **ما زال يُرجع 200**.
  الجذرُ يبقى مراجعَ `refs/pull/*` (استنساخُ مرآةٍ كاملة يجلبها بلا معرّف)، لكنّ الملفّين يحوّلانها إلى **رابطٍ مباشرٍ موسوم** — وهما في `main` العامّ منذ `eb5bc9d` (٢٠٢٦-٠٩-٢٣). · **مُثبت**
  الحركة: قائمةُ المعرّفات تُقرأ من ملفٍّ **غير متتبَّع** (بجوار المفتاح في `data/`) أو من متغيّر بيئة · ونصُّ التذكرة يُرسَل إلى GitHub **ولا يُلتزَم** · وتُنزع أوصافُ المحتوى.
  ولا تُعاد كتابةُ التاريخ لأجل هذا — العلاجُ الحقيقيّ تطهيرُ GitHub؛ والمطلوبُ ألّا يُعمَّق المكشوف.

## Standards — هل تُرك العملُ نظيفًا؟

- **R53-3 (P3)** `tests/test_hook_gates.py:33` — الضابطُ يشترط **إعدادًا محلّيًّا** (`core.hooksPath`) لا ينتقل مع أيّ استنساخ (وفي شجرةٍ مرتبطةٍ يكون مسارًا مطلقًا إلى النسخة الرئيسيّة فلا يطابق أيضًا)
  ⇒ «٦٥٧» خاصٌّ بنسختك، وأيُّ استنساخٍ طازجٍ يرى أحمر. والحقيقةُ التي يكشفها صحيحةٌ ومهمّة: **الطبقتان ١ و٢ تحرسان النسخَ المُهيّأة وحدها**، وCI هو الطبقةُ العامّة الوحيدة. · **مُثبت**
  الحركة: فحصُ الإعداد يتخطّى بسببٍ مُعلَن حين لا يُهيّأ (أو ينتقل إلى أداة إعداد) · وتُكتب هذه الحقيقةُ بجوار «الطبقات الأربع».
- **R53-4 (P3)** `tools/guard_bite_sweep.py` (`PY = str(ROOT / ".venv/bin/python")`) — في شجرةٍ مرتبطةٍ أو استنساخ ⇒ **Traceback · rc=1**، وهو نفسُ رمز «بوّابةٌ لا تعضّ».
  و`.githooks/pre-push` يحلّ المسألةَ نفسَها (الشجرة ⇒ `git-common-dir` ⇒ `python3`). · **مُثبت**
  الحركة: الترتيبُ نفسُه أو `sys.executable` · ورمزٌ مستقلٌّ لتعطّل الأداة.
- **(P3)** `docs/ROADMAP.md:10` ما زال يسمّي «الفرعَ العامل `feat/item4-pack-free-capture-44` (PR #119)» وقد دُمج وحُذف.

## قراري في P-1..P-3 — كما طلبتَ صراحةً

| # | القرار | لماذا |
|---|---|---|
| **P-1** فصلُ ملفّ الحالة | **تبنٍّ** — بأبسط صوره: حالةُ كلِّ طرفٍ في صندوقه (`handoff/sulaiman/STATE.md` · `handoff/claude/STATE.md`)، يكتبها صاحبُها وحده (وضابطُ الملكيّة يغطّيها بلا قاعدةٍ جديدة)، و`handoff/STATE.md` **يُشتقّ للقراءة ولا يُحرَّر بيد** | يقتل **صنفَ** «ملفٌّ واحدٌ بكاتبَين» لا حادثتَه، ويُغني عن `--union` · **وبدأتُ بنصفي:** من هذه المراجعة حالتي في `handoff/claude/STATE.md`، ولا أكتب في `handoff/STATE.md` بعد اليوم |
| **P-2** بوّابةٌ لـ`render_state.py` | **رفضُ الربط ببوّابة · وتبنّي الإعلان المكتوب الآن** — ثم يتقاعد `--union` بعد P-1، ويبقى من الأداة الاشتقاقُ وحده باختبار وحدة | بوّابةٌ تحلّ تعارضَ ملفٍّ مشترك آليًّا تُثبّت البنيةَ الخاطئة؛ وP-1 يُلغي التعارضَ من أصله |
| **P-3** المقاعدُ المعزولة شرطَ دمج | **رفضٌ كشرطٍ في `main` · وتبنٍّ كمبدأ** | شرطُ الدمج يجب أن يُتحقَّق منه **من المستودع** (القاعدة ٦): CI أخضر + حكمُ المدقّق في `handoff/claude/` على المدى. وتقاريرُ المقاعد خارج المستودع (R51-6) ⇒ لا تصلح شرطًا ما لم تُلتزَم كما REVIEW-44/46/48. و«توصيةٌ في كلّ تقرير» تبقى عُرفًا لا بوّابة — وأجيب عنها صراحةً كما هنا |

## Declined to judge — خارجُ نطاقي أو قرارُ المالك

- مهمّةُ Hermes `refs-exposure-watch` (خارج المستودع) · إرسالُ نصّ التذكرة · إبطالُ الرمز · حذفُ الفروع الخمسة عشر الباقية.

## Not proven — ما لم يُثبت

- أنّ الأشكالَ التي يلتقطها الخطّاف كافيةٌ لكلّ مزوّدٍ مستعمَل — قِست خمسَ صيغٍ مُصطنعة لا كلَّ الصيغ.
- سلوكُ السقف مع عدّاد المزوّد الحيّ — ما زال على عدّادٍ مُحاكًى.

{المُدخلات: تقريرُ الجولة ٥٢ + `main` حتى `e430b50` + الأذرعُ الأربع} → {البرهان: نسختان خاصّتان · 656/1 و601/5/51 · CI مقروء · إعادةُ حكمٍ بلا تغيّر · ٤٩ موضعًا · إعادتا حقنٍ للقصّ (٢ ✗ لكلٍّ) · ٣ فحوصِ سقف · سمُّ ١٤/١٤ · ٦ صيغٍ مُصطنعة على الخطّاف · $0} → {الحالة: APPROVE WITH FIXES · R52 أُغلق · R53-1 أوّلًا · P-1 مُتبنّى وبدأتُ بنصفي}
