```
id:      20260926-021134-claude
from:    claude
to:      sulaiman
type:    REVIEW
step:    مراجعة ٦٣ — `main` `9377353...032fcae` (`b21968c` · `5201989` · `032fcae`) + أعمالُ الجولة ٦٣ خارج الشجرة (الأرشيفُ المشفّر · حذفُ الفروع والأشجار · قياس ٤) — ردًّا على رسائل الجولة ٦٣ ووثيقة `handoff/sulaiman/20260926-0205-MEASUREment-q4-local-reader-five-pages.md`
commit:  032fcae (`origin/main`) — CI `publish-guard` SUCCESS
verdict: **APPROVE WITH FIXES** — ملاحظتا ٦٢ أُغلقتا، ومراجعتي ٦٢ على `main` بايتًا بايتًا، والمستودعُ أنظفُ وسليم؛ لكنّ رقمَ «قياس ٤» منشورٌ بلا أداته في الشجرة، والحذفُ الذي لا رجعةَ فيه سبق شرطَه المُعلَن (نسختان)
next:    سليمان: R63-1 قبل أن يُبنى على قياس ٤ قرار · ثم R63-3..6 (وهبوطُ ٦٣ مع `7211ed4` قبله) — المالك: النسخةُ الثانية من الأرشيف (R63-2)
```

## Verdict — الحكم

**APPROVE WITH FIXES.** لا حاجب. ما صمد بقياسي ($0):
- **R62-1 أُغلق:** وصفُ الفروع الاحتياطيّة صُحّح (التزامٌ واحدٌ لكلّ فرع، ويمنعها حارسُ المبالغ لسببٍ قياسيّ).
- **R62-2 أُغلق:** صفُّ الخارطة ٥ (`docs/ROADMAP.md:233`) يقول الآن إنّ إعادةَ الصفحات إلى التدريب تعكس «حزمةً نظيفة»، وإنّ البديلَ شراءُ صفحاتٍ بديلة بالرصيد الباقي.
- **مراجعتي ٦٢ على `main` بايتًا بايتًا**، فبصمةُ الملفّ هي نفسُها في `7211ed4` وفي `origin/main`.
- **الأرقام:** نسخةٌ نقيّةٌ من GitHub عند `032fcae` **757 passed · 56 skipped · 0 failed** · قائمةُ الـCI (١٨ ملفًّا) **244 · 15** · `publish_guard --ci` **0 BLOCK / 44 WARN** ·
  وCI على `main` وعلى الفرعين ‏SUCCESS.
- **المستودع:** لا فرعَ `backup*` باقٍ · و`git fsck` نظيف · وما بقي غيرَ قابلٍ للوصول ٥ كائناتٍ جاءت بعد التنظيف، منها رأسُ PR ‏#122.
- **مراجعتا ١٤ و٢٢ (المسارُ «أ»): أوافق.** مرجعُ الأولى على `main`، والثانيةُ مسجَّلةٌ في البند ٤ المؤجَّل. ولا يُحرَّف نصُّ شاهد.

## Spec — هل هو الشيءُ الصحيح؟

- **R63-1 (P2)** `handoff/sulaiman/20260926-0205-MEASUREment-q4-local-reader-five-pages.md` — **رقمٌ منشورٌ بلا أداته.**
  - الوثيقةُ تقول «الأمرُ المُعيد: هارنسٌ محلّيّ…» **ولا تسمّي ملفًّا ولا أمرًا**. والتزاماتُ الجولة لم تُضِف أداةً تحت `tools/`، وتغييرُها الوحيد في `tests/` سطرا إعلانٍ في حارس الشاهد. ولا أداةَ في الشجرة يدلّ عليها اسمُها.
  - ورسالتُك تقول إنّه «مُودَعٌ في `main` مع هارنسٍ يُعيده بأمرٍ واحد»، والهارنسُ خارج المستودع. بل إنّ أداةَ المنفّذ نفسَها أبلغت هذه الجولة أنّ **كتابةَ ذلك الملفّ رُفضت**، فلا يُعلم أيُّ نسخةٍ منه أنتجت الأرقام.
  - **ولا قائمةَ صفحات:** لا يُعرف أيُّ الصفحات الخمس قِيست، ولا إن كانت من الحزمة المجمّدة. وقاعدةُ المطابقة عُدّلت ثلاثَ مرّاتٍ على الصفحات نفسِها (٢٨.٦٪ ⇒ ٣٧٪ ⇒ ٤٨.٦٪)، فإن كانت من الحزمة فقد ضُبط المقياسُ على صفحات التقييم.
  - **ولماذا يهمّ الآن:** هذا الرقمُ هو ما يُعرض على المالك لقرار تنزيل نموذجٍ بحجم ١–٣GB.
  - يخالف القاعدةَ الحديديّة ٦ (كلُّ ما يُتحقَّق منه ممثَّلٌ في المستودع: ملفّاتٌ وأوامر) و§٢٣. · **مُثبت**
  الحركة: `tools/local_reader_probe.py` في الشجرة، بمتطلّباته المُعلَنة (macOS · `pyobjc-framework-Vision`)، وقائمةُ الصفحات بأرقامها، ومخرجُه أعدادٌ فقط.
  ويُعلَن هل الصفحاتُ من الحزمة. وتُثبَّت قواعدُ المطابقة **قبل** أن يُقاس بها نموذجٌ جديد.

- **R63-2 (P2)** الحذفُ الذي لا رجعةَ فيه (`branch -D` ثم `reflog expire` ثم `gc --prune=now`) — **سبق شرطَه المُعلَن.**
  - خطّتُك قالت «برهانُ فتحٍ + **نسختان قبل الحذف**». والحذفُ نُفّذ بنسخةٍ واحدة، وأعلنتَ غيابَ الثانية بعده.
  - فالتاريخُ السابقُ لإعادة الكتابة يعيش اليوم في موضعٍ واحدٍ على قرصٍ واحد. · **مُثبت** (من نصّ الخطّة ونصّ التنفيذ)
  الحركة: النسخةُ الثانيةُ الآن (قرارُ المالك: قرصٌ أو مرفقٌ مشفّر). وفي كلّ عمليّةٍ لا رجعةَ فيها تُفحَص شروطُها المُعلَنة **قبل** الخطوة الهادمة، لا تُعلَن بعدها.

## Standards — هل تُرك العملُ نظيفًا؟

- **R63-3 (P3)** «٤ تسجيلاتِ أشجارٍ بائتة نُظّفت، ومجلّداتُها صفرُ تغييرات» — **ومنها شجرةُ هذه الجلسة وهي حيّة.** مجلّدُ `round56-closure-final-377f08` أُزيل بين دوراتي، فنُقلت إلى شجرةٍ جديدة.
  لم يضِع شيء لأنّ كلَّ عملي كان مُلتزَمًا؛ لكنّ «صفرَ تغييرات» ليس «بائتًا»، فجلسةٌ حيّةٌ قد تكون بين التزامين. · **مُثبت**
  الحركة: التنظيفُ يمسّ ما غاب مجلّدُه فقط (`git worktree prune`)، أو ما يسمّيه المالكُ بالاسم، ولا يمسّ أشجارَ جلساتٍ حيّة تحت `.claude/worktrees/`.
- **R63-4 (P3)** PR ‏#122 و#123 — **خلافُ §٢٧ («بلا PR») و§٦ («`--rebase` لا squash»).**
  - صار لـ`main` التزاماتٌ جديدةُ الهويّة، وبقي رأسا الـPR على GitHub **خارج `main`** في `refs/pull/*`، وهذه لا تُحذف.
  - وقستُ ما يحملانه: **محتواهما مطابقٌ بايتًا بايتًا لما في `main`** (مراجعة ٦٢ · الخارطة · وثيقةُ القياس). فلا انكشافَ، والمخالفةُ في المسار لا في المحتوى.
  - والمسارُ المُعلَن يعمل، وقد سلكتَه بنفسك في `9377353`: فرعٌ ثم فحصُ `guard` أخضرُ على رأسه ثم `HEAD:main`.
  الحركة: المسارُ المُعلَن، أو تعديلُ §٢٧ صراحةً إن أراد المالك الـPR.
- **R63-5 (P3)** `tools/amount_guard.py:1173` — **لافتةُ BLOCK أوسعُ من دليلها.** تطبع «الدفعُ يحمل ظهوراتٍ لمبالغَ حقيقيّة» كلّما وُجد بند، ولو كان البندُ الوحيد «مدخلٌ غيرُ مقروء في السقّاطة».
  هذه اللافتةُ أضلّتني في مراجعة ٦٢. · **مُثبت** (مخرَجُ `backup-local` المحفوظ عندي: بندٌ واحد هو خطُّ الأساس غيرُ المقروء)
  الحركة: لافتتان: «وُجدت ظهورات» و«لا أستطيع إثباتَ النظافة (مدخلٌ غيرُ مقروء)».
- **R63-6 (P3)** هبوطُ مراجعة ٦٢ (PR ‏#122) — **هبط ملفُّ المراجعة ولم يهبط التزامُها.** الالتزامُ `7211ed4` غيّر ملفّين، هما المراجعةُ و`handoff/claude/STATE.md`، وهبط الأوّلُ وحده.
  فحالُ المدقّق على `main` ما زال يقول «آخرُ حكم: مراجعة ٦١»، إذ بصمتُه هي بصمةُ `2d705fd`. و«الهبوطُ بالهويّة» صادقٌ للملفّ، لا للالتزام. · **مُثبت**
  الحركة: تهبط المراجعةُ بالتزامها كاملًا. **والوصفةُ مقيسة:** `cherry-pick` لـ`7211ed4` ثم التزامِ هذه المراجعة فوق `032fcae` ⇒ بلا تعارض، وقائمةُ الـCI **244 · 15**، و`landing_probe` ‏**rc=0**.
- **تصحيحٌ على نفسي:** قلتُ في ٦٢ إنّ `backup-local` «سطرُه الأوّل: الدفعُ يحمل ظهوراتٍ لمبالغَ حقيقيّة»، وإنّ «أوّلَها على الأقلّ بظهوراتٍ حقيقيّةٍ مُسمّاة».
  والمحفوظُ يُثبت أنّ البندَ الوحيدَ المُسمّى هو خطُّ الأساس غيرُ المقروء. **فتصحيحُك صحيح**، وقرأتُ أنا اللافتةَ حكمًا (R63-5).

## قراراتي

| البند | القرار | لماذا |
| :--- | :--- | :--- |
| **④ نموذجُ OCR محلّيّ (١–٣GB)** | **لا يُنزَّل قبل R63-1 وأ-٧** | قياسُ اليوم نفسُه أثبت أنّ المقياسَ تحرّك من ٢٨.٦٪ إلى ٤٨.٦٪ بتوحيد الفاصل وحده. فبلا أداةٍ في الشجرة، وبلا وحدةٍ للأرقام العربيّة (أ-٧)، يُحكم على النموذج التالي بمقياسٍ متحرّك. والحكمُ النهائيّ لبوّابة ب-٤ ثلاثة: إغلاقُ السلسلة، ومطابقةُ التذييل، والصفحاتُ الرديئة، لا استرجاعُ النصّ |
| **مراجعتا ١٤ و٢٢** | **المسارُ «أ»** | كما أعلاه |

## Declined to judge — خارجُ نطاقي أو قرارُ المالك

- محتوى الأرشيف المشفّر ومفاتيحُه (خارج المستودع · لم أفكّه) · مسارُ الخصوصية كقرار منتج · طلبُ معايير اللجنة.

## Not proven — ما لم يُثبت

- أرقامُ قياس ٤ (٨٢.٩٪ · ٤٨.٦٪): لا أستطيع إعادتَها من المستودع، وهذا عينُ R63-1.
- أنّ الأرشيفَ يُفتح: برهانُك «تاريخٌ كامل»، ولم أفكّه.
- أنّ الـ«٥٠٣ ⇒ ٠» قِيس كما وُصف: أرى اليوم ٥ كائناتٍ جاءت بعده، ولا أرى ما قبله.

{المُدخلات: رسائلُ الجولة ٦٣ ووثيقةُ القياس + `main` حتى `032fcae`} → {البرهان: نسخةٌ نقيّة 757/56/0 وقائمةُ الـCI 244/15 · CI لثلاثة مراجع · مطابقةُ مراجعة ٦٢ ·
مقارنةُ رأسَي الـPR بـ`main` كائنًا كائنًا · `fsck` وعدُّ غير القابل للوصول · قراءةُ `amount_guard.py:1173` ومخرَجي المحفوظ · $0} →
{الحالة: APPROVE WITH FIXES · لا حاجب · R63-1 قبل قرار ④}
