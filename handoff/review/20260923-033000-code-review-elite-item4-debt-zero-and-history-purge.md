# مراجعة مستقلّة (code-review-elite) — الدَّينُ صفر · تطهيرُ التاريخ · مسارُ إعادة الكتابة

```
id:       20260923-033000-review            type: REVIEW
target:   ec087f0..188259b   (+ المدى الأقدم من المرآة: 2d9fb60..e2c9344)
seats:    Standards · Spec · Structure      (ثلاثةُ مقاعد معزولة: deleg_97ea23fa)
fixpoint: reviewed SHA == HEAD عند المراجعة == 188259b
```

**الأساسُ المُشترك:** أوقف المالك قرارين: (١) «لا يُعفى موضع، تُغيَّر القيمة» ⇒ انتهاءُ دَين المبالغ،
(٢) إخراجُ مفتاح التشفير ونشرتِه من التاريخ وتدويره. وأُضيف إلى `tools/amount_guard.py` وضعان
(`--history-audit` · `--history-rewrite`) لأنّ إعادةَ الكتابة لا «مدى دفعٍ» لها.
**G5** لا findings على كودٍ لم تلمسه التغييرات · **G6** المحاورُ غيرُ مخلوطة · **G7** المقاعدُ للقراءة فقط.

## Standards

- **[P1] S1 · citation: `tools/amount_guard.py:820-822` (`history_forms`: قراءةُ `cat-file --batch`) · tag: مُثبت ·**
  الأثر: **فشلٌ مفتوح** — `returncode` لا يُفحَص، وتدفّقٌ فارغٌ يُنتج `(0,0)` ⇒ يُطبع
  `PASS — مسحُ كلّ التاريخ: 0 blobاً` ⇒ بوّابةٌ لا تستطيع القياس تمرّ، وهي تنقض قاعدةَ الأداة نفسِها
  («صفرُ مدخلٍ ليس نتيجة»). **الحركة:** فحصُ `returncode` ورفضُ الحكم عند صفرِ blob. **✅ أُغلق**
  (المسبار: تدفّقٌ فاشلٌ مُصرَّح ⇒ «cat-file --batch فشل (rc=1) ⇒ مسحٌ لم يقع ⇒ لا أمرّ»).
- **[P1] S2 · citation: `tools/amount_guard.py:828-830` (فرع `head[1] == "missing"`) · tag: مُثبت ·**
  الأثر: كائنٌ مفقودٌ يُتخطّى بـ`continue` بلا عدّادٍ ولا إعلان ⇒ تاريخٌ ناقصٌ يُقرأ نظافةً، والصنفُ كان
  مُغلَقاً في فرعَي «الجسم المقصوص» و«الترويسة» فبقي مفتوحاً في الثالث. **الحركة:** عدُّ المقروء وربطُه
  بعدد المطلوب. **✅ أُغلق** — قيدٌ واحدٌ (`seen != len(shas)`) حلّ محلَّ ثلاثةِ شروطٍ دفاعيّة ⇒ نقصَ شرطٌ وقويَ الفحص.
- **[P2] S3 · citation: `tools/amount_guard.py:363` (`load_deny()`) مع مانيفستٍ فارغ · tag: مُثبت ·**
  الأثر: `{"fingerprints": []}` ⇒ `set()` لا `None` ⇒ كلُّ عدّادٍ صفر ⇒ **PASS بلا فحصٍ لأيّ دفع**.
  **الحركة:** المانيفستُ الفارغُ ليس أدلّة. **✅ أُغلق** (`None` ⇒ `--ratchet` = **rc=2**).
- **[P2] S4 · citation: `tools/amount_guard.py:982-984` (`--pre-push`: `if base is None: base = read_baseline()`) · tag: مُثبت ·**
  الأثر: **البابُ ٣أ/٣ب (من مراجعة ٣٩) رجع صيغةً لا مكاناً:** عند تعذّر قراءة عتبة `origin/main` صارت
  العتبةُ ملفَّ الدافع ⇒ يرفع عتبتَه بنفسه. **الحركة:** لا استبدالَ لغير المقروء، وسلسلةُ الإرجاع المنصوصة
  في ٣٩ (`rev-list` — ثم سقوطٌ مُغلَق). **✅ أُغلق** (`--pre-push` بلا `origin/main` ⇒ **rc=2**، والمراجعُ
  غيرُ المحلولة تُشتقّ من حالة git بإعلانٍ بدل الحجب).
- **[P3] S5 · citation: `docs/security/GITHUB_SUPPORT_REQUEST.md:31-33` · tag: مُثبت ·**
  الأثر: رقمٌ منشورٌ في وثيقةٍ عامّة غيرُ مُشتقّ بدقّة («266 = تاريخ الفرع») — والمقيسُ 221 على `main`
  و266 على كلّ المراجع. **الحركة:** الأمرُ والمخرَجُ مع كلّ رقم. **✅ أُغلق** (صُحّح بالقياس).

**ورُفض finding بلا تحقّق:** ادّعى المقعدُ بقاءَ قيمٍ حقيقيّةٍ في `tests/test_gap_ledger.py:4,6,89` ⇒
قيستُ مستقلًّا: `TOKENS_IN_FILE 0 · IN_CORPUS 0 · REAL_BUT_NOT_IN_DENY 0` و`--probe` يمرّ ⇒ **لا قيمةَ
كوربوسيّةً واحدة** في الملفّ. وبقي من الادّعاء **جزءٌ واحدٌ صحيح**: ترويسةٌ تقول «المقيسةُ فعليًّا على
كشف الراجحي» وأرقامُها أُعيدت كتابتُها ⇒ **[P3] S11 · citation: `tests/test_gap_ledger.py:4-5` ·
tag: مُثبت · الأثر:** جملةٌ تشهد بقياسٍ لم يبقَ كما هو. **الحركة:** وسمُ الرقم مُصنَّعاً أو حرسُ العلاقة
بـ`assert`. **⏳ مفتوح (نصّي).**

## Spec

- **[P0] B1 · citation: قرارُ المالك ٢ مقابل `docs/security/GITHUB_SUPPORT_REQUEST.md:31-37` · tag: مُثبت ·**
  الأثر: **«التاريخُ مُطهَّر» صحيحٌ للفروع وحدها** — مراجعُ `refs/pull/*` على الريموت العامّ تحفظ المفتاحَ
  المحروق ونشرتَه (قياسُ المرآة: **598** التزاماً مقابل **266** مُطهَّراً · **123** صيغةَ مبلغٍ حقيقيّ ·
  **2** مساراً حسّاساً)، ومع المفتاح والبصمات تبقى المبالغُ قابلةً للجرْد ⇒ والتدويرُ لا يُصلح تسريباً حيّاً.
  **الحركة:** إرسالُ الطلب (**نقرةُ المالك**) ثم إعادةُ قياس `git clone --mirror` حتى يصيرا 0. **⏳ مفتوح (حاجب).**
- **[P1] B2 · citation: `handoff/claude/20260923-004227-third-eye-review-39-the-gate-has-four-open-doors.md:80` مقابل رأس `--pre-push` · tag: مُثبت ·**
  الأثر: البابُ ٢ أُغلق نصفَ إغلاق: لا فشلَ مفتوح، لكنه فشلٌ صلبٌ يُنتج حافزَ `--no-verify` (وفيه يسقط
  الحارسان معاً). **الحركة:** تنفيذُ سلسلة الإرجاع المنصوصة. **✅ أُغلق.**
- **[P2] S6 · citation: `tools/ratchet_proof.py:35` (`_real_values`) · tag: مُثبت ·**
  الأثر: `sorted(src)[0]` بلا تصفية ⇒ سلامةُ القيمة المحقونة رهنُ ترتيبٍ أبجديّ لا شرطٌ معلَن.
  **الحركة:** مُصفّي `is_significant`/`fingerprint in deny` نفسه. **⏳ مفتوح.**
- **[P2] S7 · citation: `tests/test_amount_guard.py:383-384` · tag: مُثبت ·**
  الأثر: فرعُ `isinstance(..., dict)` ميّت (النوعُ `set[str]`) ومسارٌ بديلٌ هشّ. **الحركة:** استعمالُ مُصفّي
  واحدٍ مرّةً واحدة. **⏳ مفتوح.**
- **[P2] S8 · citation: `tools/amount_guard.py:969-974` (`AMOUNT_GUARD_HISTORY_REWRITE` + `AMOUNT_GUARD_MIRROR`) · tag: مُثبت ·**
  الأثر: ارتباطُ الحارس بمدى الدفع صار اختياريّاً بمتغيّرِ بيئةٍ **بيد الدافع** ⇒ **قرارُ صلاحيّةٍ لا قرارُ
  تنفيذ** (الأثرُ مُخفَّف: المسحُ البديلُ أشمل). **الحركة:** اعتمادٌ صريحٌ من المالك، أو الإزالة.
  **⏳ يحتاج إقرارَ المالك.**
- **[P3] S9 · citation: `handoff/sulaiman/20260923-040000-item4-ALL-DECISIONS-CLOSED-debt-zero-and-history-purged.md:8` (`commit: ec087f0`) مقابل `HEAD = 188259b` · tag: مُثبت ·**
  الأثر: ثلاثةُ التزاماتٍ (منها تعديلُ الخطّاف) خارجَ التسليم ⇒ المصادقةُ على غيرِ ما يُدفع.
  **الحركة:** إعادةُ إصدار التسليم مثبّتاً على الرأس/المدى. **✅ أُغلق.**
- **[P3] S10 · citation: `handoff/sulaiman/20260923-040000-item4-ALL-DECISIONS-CLOSED-debt-zero-and-history-purged.md:35` (§٢ «أُخرج ودُوِّر») · tag: مُثبت ·**
  الأثر: التدويرُ وقع (بصمةُ المفتاح الحاليّ ≠ المنشور) لكن بلا صفِّ قياسٍ في المُخرَج.
  **الحركة:** صفُّ قياسٍ للتدوير + إعلانُ أنّ المنشور المفتاحيّ صفر. **✅ أُغلق.**

## Structure

**الحكم: تراجعت** — بسببٍ واحد: صافي المفاهيم ارتفع ولم يُحذف فرعٌ (وضعٌ كاملٌ جديد · مساعدُ git ثانٍ ·
`None` بمعنيَين · دخولٌ متكرّرٌ في `main`) — وصدقَ هذا حتى أُغلقت S1–S4.

- **[P1] T1 · citation: `tools/amount_guard.py:612-618` (`pushed_counts`: `continue` صامتٌ عند `rc != 0`) مقابل `:838-847` · tag: مُثبت ·**
  الأثر: مسحُ الـblobs مكتوبٌ بمفهومَي فشلٍ مختلفين، والمُصلَحُ ليس المسارَ الذي يحرس كلَّ دفع ⇒ الإصلاحُ لم
  يُعمَّم على توأمه. **الحركة:** دالّةٌ واحدة `blob_texts(...)` يُستدعى منها الموضعان (حذفٌ لا نقل).
  **⏳ مفتوح** — مع تحفّظٍ مُعلَن: التخطّي عند `rc != 0` **مشروعٌ** في `pushed_counts` (ملفٌّ لم يكن موجوداً في
  التزامٍ أقدم)، فالتوحيدُ يجب أن يفصل «غيرُ موجودٍ في هذا الالتزام» عن «غيرُ مقروء».
- **[P2] T2 · citation: `tools/amount_guard.py:1058` (الطولُ الكاملُ بعد الإصلاح) = كان **1041** عند المراجعة بعد أن كان 946 عند `ec087f0` · tag: مُثبت ·**
  الأثر: **بوابةُ الألف سطر** (presumptive blocker) عُبِرت في هذا التغيير، والدافعُ هو الحلقةُ المكرَّرةُ نفسُها
  (T1) ⇒ العبورُ غيرُ مبرَّرٍ إلّا بحذفها (وهو ما جرى جزئيًّا ⇒ الملفُّ الآن 1013). **الحركة:** تفكيكُ وحدة
  المسح عن وحدة الواجهة. **⏳ مفتوح.**
- **[P2] T3 · citation: `tools/amount_guard.py:806-818` (`rev-list --all --objects`) مقابل عنوان المخرَج «مسحُ كلّ التاريخ» · tag: مُثبت ·**
  الأثر: المخرَجُ يشهد على سطحٍ **أضيقَ من عنوانه**: المراجعُ المحليّةُ فقط، و`refs/pull/*` البعيدةُ (سطحُ
  التسريب المعلَن) خارجُه ⇒ يُقرأ كدليلِ تطهيرٍ للسطح البعيد. **الحركة:** تسميةُ المخرَج بحدوده.
  **✅ أُغلق** (قُيِّد العنوان، ودخل الحاجبُ B1 في الحكم).
- **[P3] T4 · citation: `tools/amount_guard.py:533-539` (`_git_here` = نسخةُ `_git` بفرق `cwd`) · tag: مُثبت ·**
  الأثر: مساعدان بمفهومَين لأيِّ مستودعٍ يُقاس. **الحركة:** `_git(*args, at: Path = ROOT)` واحدة.
  **⏳ مفتوح.**
- **[P3] T5 · citation: rule (القاعدة ١٦ مقلوبةً: لا تحرس ملفًّا بلا مُنتِج) + `./.gitignore:27` · tag: مُثبت ·**
  الأثر: ضجيجُ إعدادٍ يحرس ملفًّا لا يكتبه أحد. **الحركة:** يُحذف السطر. **⛔ رُفض ببرهان:** الحذفُ من
  التتبّع لا من أقراص النسخ الأخرى ⇒ السطرُ يمنع عودةَ أثرٍ **مقاس** (أوقف التزاماً في هذه الجولة).

## Not proven

لم أُثبت: **(١)** اختفاءَ النسخِ التي بحوزة الغير (مرايا/استنساخاتٌ سابقة) — وأمانُ المفتاح المحروق لا
يُستعاد. **(٢)** أنّ مراجعَ `refs/pull/*` نُظّفت — بل القياسُ يقول إنّها لم تُنظَّف (B1) ⇒ **لا يجوز** قراءةُ
«التاريخُ مُطهَّر» دليلَ تطهيرٍ للسطح البعيد قبل الإرسال وإعادةِ القياس. **(٣)** `qa_gate --full` (له ثمن)
· ولم أُمشِ في `.parquet`/الصور/PDF · والأسماءُ والتواريخُ وأرقامُ الحسابات لم تُسح. **(٤)** البوابةُ (٤)
مُهيّأةٌ لا مُختبَرة · **ف١** (`test_skip_declares_itself`) يسقط في مستنسخٍ نظيف. **(٥)** لم تُراجَع
القاعدةُ ٢٠ (`docs/handoff-protocol.md`) ولا `SOUL.md` — كُتبتا **بعد** تثبيت المدى (إفصاحٌ لا تبرئة).
**(٦)** المسحُ يقيس أجسامَ الـblobs فقط، ولا جهازَ يقيس صيغةَ مبلغٍ في **رسائل** الالتزامات (فُحصت يدويًّا: صفر).

## Verdict

**REQUEST CHANGES**

الحاجبان: **B1** — مراجعُ `refs/pull/*` على GitHub تحفظ المفتاحَ المحروق ونشرتَه والمبالغ (نقرةُ المالك)
· **B2/T3** — «التاريخُ مُطهَّر» مدّعاه أوسعُ من مداه (مراجعُ محليّة).

وأُغلق في هذه الجولة: **S1 · S2 · S3 · S4 · B2 · S5 · S9 · S10** (بأوامرَ ومخارِجَ في الرأس).
والمتبقّي **بمالكٍ مُسمّى لا بـ«لاحقاً»**: T1 (توحيدُ دالّة المسح) · T2 (تفكيكُ الوحدة) · T4 (`_git` واحدة)
· S6 (مُصفّي `_real_values`) · S7 (الفرعُ الميّت) · S11 (وسمُ الرقم المُصنَّع) · **S8 (إقرارُ المالك
لاستثناء `AMOUNT_GUARD_HISTORY_REWRITE`)**.
