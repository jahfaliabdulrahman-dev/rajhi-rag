# تقريرُ مراجعةٍ مستقلّة — الجولة ٤١ (code-review-elite · ثلاثةُ مقاعدَ معزولة)

**النطاقُ المُثبَّت:** `cb10383...682fd25` (سبعة ملفّات، ٧٥٠+/٦٧−) · **ثم** دُفعةُ الإغلاق `3febcaf` بعد حكم المقاعد.
**المراجعون:** ثلاثةُ وكلاءَ معزولين (Standards · Spec · Structure) — **والكاتبُ لم يُراجع نفسَه**، وكلٌّ قرأ المدى من git لا من وصفي.
**الحكمُ المجمَّع أوّلاً (على 682fd25):** `REQUEST CHANGES` ×٣ — وحاجبٌ في كلّ مقعد، وثلاثتُها **العلّةُ نفسُها**: «الفارغُ» يُقرأ شرعاً وهو قد يكون عطبَ قراءة.

## Verdict

- **الحكمُ النهائيّ: `APPROVE WITH FIXES`** (الحاجبُ مُغلقٌ ومُقيس · والتأجيلاتُ مُعلَنةٌ بتواريخها)

- **P1** · citation: الحكمُ المجمَّع أعلاه · tag: **مُثبت** · consequence: لا يُسلَّم التزامٌ عليه حاجب · move: أُغلق الحاجبُ في `3febcaf` وقيست الحالاتُ الثلاث؛ والالتزامُ الحاليّ `3febcaf` + هذا التقرير هو المُسلَّم.

## Standards

- **P1** · citation: `docs/handoff-protocol.md:106,117` مقابل `:170` والحاشية `:119` · tag: **مُثبت** · consequence: اختباري يعلن «رقمٌ واحدٌ لكلّ قاعدة» والملفُّ يحمل الرقم ١٢ لمعنيين ⇒ بوابةٌ خضراءُ على ضمانٍ ينقضه الملفُّ المقيس · move: **مؤجَّلٌ مُعلَن** — توحيدُ السلسلة يمسّ بنوداً بلا عنوانٍ مقابل ⇒ دفعةُ توثيقٍ خاصّة، لا دفعةُ إغلاقِ فتحٍ أمنيّ؛ وحُصر ادّعاءُ الاختبار بنصِّه (فرادةُ العناوين + حلُّ الإحالة).

- **P2** · citation: `tools/amount_guard.py:1070` («القاعدة ١٥» والمقصود «١٣») و`tests/test_amount_guard.py:169` · tag: **مُثبت** · consequence: إحالةٌ تُرسل القارئَ إلى قاعدةٍ أخرى · move: **مؤجَّلٌ** مع البند أعلاه (السلسلةُ تُوحَّد مرّةً واحدة).

- **P2** · citation: `scripts/check_review_report.py:84-95` · tag: **مُثبت** · consequence: عنوانٌ فرعيّ داخل قسم المحور يُنهيه ⇒ يمرّ findingٌ بلا مصدرٍ ولا وسم (قِيس بفيكسترا الأداة: `violations=[]`) · move: **مؤجَّلٌ** — يُصلَح بشرط المستوى (`len('#') <= level`)؛ والأثرُ اليوم محدودٌ لأنّ التقرير يُكتب بعناوين الأقسام الإنجليزيّة.

- **P2** · citation: `scripts/check_review_report.py:25` مقابل نصّ القاعدة ٢٠ العربيّ · tag: **مُثبت** · consequence: من ينفّذ القاعدة بلغتها تُرفَض مراجعتُه بالأداة التي تستشهد بها القاعدة ⇒ حافزُ تجاوز · move: **مؤجَّلٌ**: يُسمّى الصيغةَ في نصّ القاعدة ٢٠ (عناوينُ الأقسام + مفرداتُ الخطورة).

- **P3** · citation: `docs/security/amount-baseline.json:4` · tag: **مُثبت** · consequence: الخطُّ المدفوع — الذي يقرأه CI — يسمّي القاعدةَ الخطأ (١٦ = الاستدارة) · move: **مؤجَّلٌ**: يُعاد توليدُه عند أوّل `--baseline-write` (العدّادُ صفرٌ ⇒ فرقُ سطر).

- **P3** · citation: `tools/amount_guard.py:601-605` قبل الإغلاق · tag: **مُثبت** · consequence: عدّادُ النجاح قد يحمل مدىً مُستبدَلاً بلا إعلان ⇒ يُقرأ وصفاً لما فُحص وليس هو · move: **أُغلق في `3febcaf`**: مُحلِّلٌ واحد (`parse_refs`) و`_unpublished` واحدة، وكلُّ تدهورٍ يُعلَن أو يُسقط — ولا احتياطَ صامت.

## Spec

- **P1** · citation: `docs/handoff-protocol.md:239` (القاعدة ١٩) + `tools/amount_guard.py:1020-1026` · tag: **مُثبت** · consequence: `stdin` فارغٌ يسقط `rc=2` وهو دفعٌ لا يُحدّث مرجعاً (المقعدُ قاس عند git: bytes=0) ⇒ سقوطٌ كاذبٌ على عملٍ روتينيّ = حافزُ `--no-verify` · move: **أُغلق في `3febcaf`** — الحالاتُ الثلاث مفصولة، والاختبارُ يقيس ثلاثتَها.

- **P1** · citation: `tools/amount_guard.py:593-605,1020-1026` قبل الإغلاق · tag: **مُثبت** (rc=1 بريموتٍ واحد، rc=0 بعد ريموتٍ ثانٍ) · consequence: `--not --remotes` تطرح مراجعَ كلّ ريموت ⇒ التزامٌ تعرفه نسخةٌ احتياطيّة يُقرأ منشوراً والدفعُ إلى `origin` ينشره · move: **أُغلق في `3febcaf`**: الوجهةُ تصل من الخطّاف (`$1`) ⇒ `--pre-push --remote`، والفراغُ يُقاس مقابلها؛ ووجهةٌ لا أعرف مراجعَها ⇒ سقوطٌ مُغلَق. واختبارٌ جديدٌ له أسنان (أسقط تطبيقي الأوّل: النجمةُ في `rev-list` تُقرأ pathspec ⇒ فراغٌ كاذب).

- **P1** · citation: `tools/amount_guard.py:1020-1026` قبل الإغلاق + القاعدة ١٩ · tag: **مُثبت** · consequence: مجرى مقصوص يمرّ `rc=0` بحكمٍ إيجابيّ كاذب · move: **أُغلق**: سطرٌ خارج الشكل الرباعيّ ⇒ `rc=2` («لم أقرأ» ≠ «لا شيءَ يُنشر»)، وثلاثةُ أشكالِ قصٍّ مقيسةٌ في الاختبار.

- **P2** · citation: `tools/amount_guard.py:460` (فرعُ `counts` في `baseline_audit`) · tag: **مُثبت** · consequence: البوّابةُ لا تفرّق «صفرُ دَين» من «لا ملفَّ عتبة» — والمعيارُ الذي وضعته لنفسها («بوّابةٌ لا تستطيع العمل لا تمرّ») لا يُطبَّق على جهة الشجرة · move: **مؤجَّلٌ مُعلَن**: غيابُ الملفّ من الشجرة ⇒ `rc=2`؛ الأثرُ محدودٌ لأنّه مُتتبَّعٌ ومقروءٌ في كلّ تشغيل.

- **P3** · citation: `tests/test_amount_guard.py:146,156` و`docs/QA_CHECKLIST.md:13` و`docs/security/GITHUB_SUPPORT_REQUEST.md:25` · tag: **مُثبت** · consequence: سقوطُ اختبارين من `worktree` (`relative_to(ROOT)` على مسارٍ تحت `DATA_ROOT`) · وجملةُ العدد لا تحصر نطاقَها · move: **مؤجَّلٌ** (يُصرَّح به في قائمة الملفّات)، **إلّا** عددَ طلب الدعم فقد صُرِّح به وقِيس (١١٣).

## Structure

- **P1** · citation: `tools/amount_guard.py:593-605,1020-1026` · tag: **مُثبت** · consequence: قلبُ حالةٍ كانت `rc=2` مُغلَقة إلى فتحٍ صامت في الوجهة المتعدّدة · move: **أُغلق** (تفصيلُه في المقعد أعلاه).

- **P2** · citation: `tools/amount_guard.py:597,604,634` ومُحلِّلان للنصّ قبل الإغلاق · tag: **مُثبت** · consequence: «ما لم يعرفه الريموت» في ثلاث نسخٍ جزئيّة، و`stdin` بمُحلِّلَين يفترقان، وقراران متضادّان على الحالتين المتجاورتين · move: **أُغلق في `3febcaf`** — طبقةٌ واحدة.

- **P2** · citation: `tools/amount_guard.py:904-926,928-1086` · tag: **مُثبت** · consequence: `argparse` يُحلّل ولا يوزّع: ١٥ علماً مسطّحة والأسبقيّةُ بالترتيب لا بالوسم (`--ratchet --tracking-audit` يُنفّذ واحداً) · move: **مؤجَّلٌ مُعلَن**: أوضاعٌ متعارضةٌ صريحة (`mutually_exclusive_group`/subparsers) — إعادةُ هيكلةٍ لا تُخلَط بدفعةِ إغلاقِ فتحٍ أمنيّ.

- **P3** · citation: `tests/test_amount_guard.py:299-303` · tag: **مُثبت** · consequence: تأكيدٌ لا يستطيع السقوط (`rc in (0,1,2)` مع نصَّين متضادّين) في ملفّ اختبارات حارسٍ أمنيّ · move: **أُغلق**: حُذف، والحالةُ مُغطّاةٌ باختبارِ الحالات الثلاث.

## Not proven

- **لم أُعِد قياسَ ادّعاء المرآة بنفسي** (الـ١٢٣ صيغةً): وثّقتُ قياسَ المدقّق وأصلحتُ صياغةَ دعواي، ولم أُعِد بناءَ المرآة.
- **لم يُجرَّ دفعٌ حقيقيٌّ فوق ريموتٍ حقيقيّ بعد هذه الدفعة**: القياسُ في sandbox + الخطّافُ الحقيقيّ على مراجعَ محلّيّة (`rc=0`).
- **البندُ ٤**: ٥ ✗ في نسخةٍ بلا `data/` (قِيسها المقعد) — أُصلح منها واحد، والبقيةُ مؤجَّلةٌ مُعلَنة.
- **الاقتراحاتُ ٦ (نصفُه الثاني: صفُّ `handoff/review/` في جدول §١) · ٧ · ٨ · ٩ · ١٠** لم تُلمس في هذا المدى — مفتوحةٌ ومُعلَنة.
- **حجم الملفّ (١٠٩٠ سطراً، ٥٠ دالّة)** لم يُثبَت أنّ تقسيمَه يُلغي تعقيداً أم ينقله.
