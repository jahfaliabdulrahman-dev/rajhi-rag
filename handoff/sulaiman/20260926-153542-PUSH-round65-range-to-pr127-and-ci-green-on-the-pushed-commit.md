# تقريرُ الدفع — الجولة ٦٥: المدى إلى رأس #127، ومخرَجُ `ci_report` على كلّ مرجعٍ دُفع

**وثيقةُ قياسٍ مُودَعة — لا تُجيب حُكماً ولا تُودِع شاهداً.** وبها يُسدّ شرطُ §٢٩ («كلُّ تقريرِ دفعٍ
يحمل مخرَجَ `tools/ci_report.py` على كلِّ مرجعٍ دُفع») ولمرجعَين: **`feat/r64-p5-tool-and-protocol`**
(رأسُ #127) و**`main`**.

**الزمن:** 2026-09-26 15:35 (+03) — والاسمُ يحمله.

## ١ · الدفع

```
$ git rev-parse HEAD
b5a73589f58e4857ceb3934fac39b944120db4a8
$ git push origin HEAD:refs/heads/feat/r64-p5-tool-and-protocol
   539a678..b5a7358  HEAD -> feat/r64-p5-tool-and-protocol      PUSH_RC=0
```

وتقدّمٌ مباشرٌ مُتحقَّقٌ منه قبل الدفع: `git merge-base --is-ancestor 539a678 HEAD` ⇒ `FF=yes`.
**ولم يُدفَع شيءٌ إلى `main` في هذه الجولة** (دفعُ الجولةِ الوحيدُ إلى `main` سبق الجواب: `§٢` من
`20260926-153028-REPORT-to-claude-round65-…md`).

## ٢ · تشغيلُ GitHub على الالتزام المدفوع

```
$ gh run list --commit b5a73589f58e4857ceb3934fac39b944120db4a8 --json name,status,conclusion
publish-guard = completed/success
$ gh pr view 127 --json headRefOid,state,mergeStateStatus
head=b5a7358 · OPEN · merge=CLEAN
```

⇒ **R65-1 و R65-2 صارا داخل #127** (رأسُه كان `539a678` لا يحملهما) — وهو ما اشترطه حكمُ مراجعة ٦٥
(«next: سليمان: R65-1 وR65-2 داخل #127 قبل دمجه`) وأمسكه مقعدُ المواصفة في الجولة ٦٥ (بندُه الأوّل).

## ٣ · مخرَجُ الأداة (شرطُ §٢٩) — على المراجع المدفوعة

```
$ python3 tools/ci_report.py feat/r64-p5-tool-and-protocol
✓ أخضر        feat/r64-p5-tool-and-protocol — publish-guard
PASS — كلُّ مرجعٍ دُفع (1) له تشغيلٌ أخضرُ على التزامه.

$ python3 tools/ci_report.py main
✓ أخضر        main — publish-guard
PASS — كلُّ مرجعٍ دُفع (1) له تشغيلٌ أخضرُ على التزامه.
```

**وقراءةُ المخرَج بحدِّه المُعلَن في الأداة نفسِها:** «يقيس نتيجةَ تشغيلٍ على GitHub لا صحّةَ الشجرة» —
فالأخضرُ **شهادةُ تشغيل**، والصحّةُ يُشهد لها بمجموعة البوّابات في شجرة الإغلاق (٨٤٥ مرّ · ٢٣ مُتخطّى ·
٠ سقوط · static/amount/claims/turn/publish_guard · والمسبارُ `rc=0` تحت `TZ=UTC` ومحلّيًّا).

**وأثرٌ مرصودٌ في المخرَج لا يُخفى:** الأداةُ تقرأ قيمةً «محلّيّة» للمرجع (ومنه `ba582ec` في نداءِ `main`)
تُخالف `origin/main` (= `5509a84`، قِيس بـ`git rev-parse origin/main`) — والسببُ أنّ في هذا الـworktree لا
فرعَ محلّيّاً بهذين الاسمين، فالأداةُ تُسمّي ما تجده. **والحكمُ (PASS/أخضر) يُقرأ من تشغيل GitHub على
الالتزام المدفوع**، وهو المقصودُ بـ§٢٩ — أُعلنه هنا بدل أن أقرأ الأخضرَ من قراءةٍ لم أتحقّق من مصدرها.

## ٤ · ما لم يُثبت

1. **تشغيلُ GitHub على التزامِ هذا الملفّ نفسِه** (`<هذا الملفّ>` بعد إيداعه): يقع بعد الإيداع، فأُعلنه في
   نداءٍ لاحقٍ بـ`ci_report` — والالتزامُ الأب `b5a7358` مُقاسٌ أخضرَ أعلاه.
2. **أنّ `mergeStateStatus=CLEAN` يعني جاهزيّةَ الدمج**: تعني خلوَّ التعارض، **لا** حكمَ المراجعة —
   ومراجعةُ ٦٥ على #127 = `REQUEST CHANGES`، والقاعدةُ «لا دمجَ بلا حكمٍ ثانٍ» (رأسُ التقرير).
3. **مسارُ §٢٧ (دفعٌ مباشر أم PR):** قرارُ مالكٍ مؤرَّخ — هذه الوثيقةُ لا تُرجّح.
