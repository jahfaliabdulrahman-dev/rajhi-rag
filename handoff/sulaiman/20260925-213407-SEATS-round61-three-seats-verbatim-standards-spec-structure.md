# مقاعدُ الثلاثة — الجولة ٦١ · النصُّ **بالحرف**

**المدى المقيس:** `git diff ca2986d...42c6f11` (٢١ ملفًّا · +1369/−175) · `HEAD` = `42c6f11`.
**التفويض:** `deleg_72fafa18` — ثلاثةُ وكلاءَ متوازية (deepseek-flash) بدأت 2026-09-25 20:47:01 وانتهت 21:03:53.
**المقاعد:** المعايير (Standards) · المواصفة (Spec) · البنية (Structure) — **قراءةٌ فقط** بلا تعديل (قاعدةُ «الكاتبُ لا يراجع نفسَه»).

**المصدرُ وحدُّه:** المخرَجُ الذي وصل من كلّ مقعدٍ كما هو (لا تحريرَ ولا اختصارًا) — ونصُّه هو المُدخَلُ إلى
**جدول الإغلاق** في التقرير الشامل. النصُّ نُقل من سجلّ التفويض (‏`state.db → async_delegations.result_json`)
— **لا من إعادة كتابتي له**.

**وما ليس في هذه الشجرة يُقال:** مراجعتا ٥٩/٦٠ (المُدخَلُ الذي تقابله فقراتُ «الحركة») **ليسا هنا**؛
محمولتان في `claude/round56-closure-final-377f08` بالالتزامين `476130b`/`da6403b` (انظر خطة الجولة ٦١).

**ووسمها:** شهادةُ طرفٍ ثالثٍ عن شجرةٍ في لحظة (`42c6f11`) — وما تغيّر بعدها يُقاس في تقرير الإغلاق ولا يُسقِط بندًا.

---


## مقعدُ المعايير (Standards)

# مقعدُ المعايير — الجولة ٦١ · المدى `ca2986d...42c6f11`

**المدى المُقاس:** `git diff ca2986d...42c6f11` (٢١ ملفّاً · +1369/−175) · الشجرةُ نظيفة · `HEAD` = `42c6f11`. (اقتباساتُ الشيفرة أدناه بعلاماتٍ مُفردة.)
**حدودُ المدى، تُقرأ مع البنود:** `origin/chore/round56-evidence` = `ca2986d` ⇒ الالتزاماتُ الثلاثةُ **غيرُ مدفوعة**، فالـCI لم يعبر المدى بعد؛ و`data/eval_pack/pack.json` **غيرُ مُتتبَّع** وهو مُقابَلُ القرار الحيّ ⇒ لا يراه المدى؛ ومراجعتا ٥٩/٦٠ ليستا في الشجرة المقيسة (بند ٤).

**شواهدُ إيجابيّةٌ مقيسة (لا بنود):** `eval_pack --verify` ⇒ rc=0 (تقاطع ٠ · ثمن ١٣٣ من `data/training/3e2d360a665c88aa/manifest.json` والقائمةُ المُعلَّلة مطابقةٌ للعدّاد · السقف ١٣٣ · القرارُ المُلتزم مطابق) · `landing_probe --quiet` ⇒ rc=0 في **١٧٫٥ ث** (يوافق الحدّ المُعلَن ١٧–١٨) · `--poison name-stamp` ⇒ rc=1 يسمّي `tests/test_report_names.py` · إعادتي لعطب R59-1 في `tests/test_secret_scan.py` ثمّ `--from-worktree` ⇒ rc=1 يسمّي `tests/test_secret_scan.py` (ثمّ أُعيد الملفُّ والشجرةُ نظيفة) · `pytest -q` لثمانية ملفّات ⇒ ٩٦ passed · `render_claims --check` ⇒ rc=0 (tests=٧٧٠ · سقفُ الثمن ١٣٣ · و«حالياً ٧٧٠» في `docs/QA_CHECKLIST.md` مطابق) · الأرقامُ المؤرَّخة تُعاد إنتاجُها: `e903b12` ⇒ ٢٧·١٦، وHEAD ⇒ ٣٦·١٨ و٢٧·١١، والمقامُ الحيّ بلا `handoff/` ⇒ ١١·٤ · وجدولُ ملفّ الدليل المُودَع مطابقٌ حرفيًّا لمخرَج `qa_gate._table()` واسمُه يوافق `evidence_path()`.

## البنود

**P1 · `tools/landing_probe.py:97-98` + `:232` + `.github/workflows/publish-guard.yml:73-74` — المسبارُ يُحمرّ الخطوةَ في كلّ `pull_request`**
> `args = ['clone', '--no-hardlinks', '--quiet', '--single-branch']` · `if branch: args += ['--branch', branch]` · `branch = _git(PROJ, 'rev-parse', '--abbrev-ref', 'HEAD')[1].strip()` · `run: python3 tools/landing_probe.py --quiet`
النتيجة: في نسخةٍ رأسُها مفصول يُعيد `--abbrev-ref HEAD` النصَّ `HEAD`، وتُمرَّر `--branch HEAD` فيفشل النسخ: قِستُه في scratch بنسخةٍ فصلتُ رأسَها ⇒ `fatal: Remote branch HEAD not found in upstream origin` و`rc=128` ⇒ `_clone` يُعيد غيرَ صفري ⇒ `measure` يُعيد **2** ⇒ خطوةُ الـCI تفشل بأيّ رمزٍ غير صفري، أي أنّ «تعذّرُ القياس» — الذي يُعلن السجلُّ أنه **لا يُقرأ نظافةً** — يُقرأ في الهارنس **سقوطًا**، ويُحمرّ الـCI بلا علاقةٍ بأيّ مراجعة: وهو الصنفُ نفسُه الذي وُلد المسبارُ لإزالته (ومنه «النسخةُ تحمل كلَّ فروع المالك فتُصير حمراءَ دائمًا» الذي حُلَّ بـ`--single-branch`). والمقدّمةُ التي لم أُشاهدها حيّاً هي أنّ `actions/checkout` في `pull_request` يُنتج رأسًا مفصولًا (سلوكُ الإجراء المعروف · والمُشغِّلُ `on: pull_request` مُعلَنٌ في الملفّ نفسه)، وما قِستُه هو الفشلُ نفسُه على رأسٍ مفصول. وعلى `push` (مسارُ المالك) المسبارُ يعمل: rc=0 مقيسٌ هنا.
الحركة: لا تُمرَّر `--branch` حين يكون الاسمُ `HEAD` (أو `checkout --detach` بعد نسخٍ بلا `--branch`)، ويُفرَّق في الهارنس بين «تعذّر قياس» والسقوط بسببٍ صريحٍ لا برمز خروجٍ مُلبِس.
`مُثبت` (أمرٌ شغّلته).

**P2 · `tests/test_eval_pack.py:388-390` + `tools/eval_pack.py:1104-1112` — شاهدُ R60-1 الحيّ خارج المدى، وضابطُه يُتخطّى صامتًا في الـCI**
> `pack_path = PROJ / 'data' / 'eval_pack' / 'pack.json'` · `pytest.skip('لا حزمةَ على القرص (data/ مستثنى) — المقابلةُ تُجرى في شجرة العمل')`
النتيجة: الحزمةُ الحيّةُ (`measured_intersection: 0` · `measured_price_pages: 133` · `price_gate.cap_pages: 133`) **لا تُتتبَّع**، وفي `git clone` بلا `data/` قِستُ `1 skipped` عند السطر ٣٩٠ ⇒ الضابطُ الذي يمنع تعفّن قرار الحزمة **لا يعمل في الـCI**، وقائمةُ الضوابط التي عدّها المسبارُ خضراءُ وفيها تخطٍّ صامت؛ ولا خطوةَ تقرأ الحزمة في الـworkflow (الخطواتُ التسع مُسجَّلةٌ في §٣ ولا واحدةَ منها `eval_pack --verify`). وصفُّ السجلّ ١٥ يذكر هذا الضابطَ بين ما يُثبت العضّ دون إعلان أنه يُتخطّى هناك — والمستودعُ نفسُه يرفض أن تُقرأ «لم يُقَس» نجاحًا (R57-5).
الحركة: يُودَع مُشتقُّ الحزمة (لا الحزمة) دليلًا تحت `docs/evidence/` فيُقابَل، أو يُعلَن التخطّي في عمود الحدّ للصفّ ١٥، أو خطوةٌ يدويّةٌ مُعلَنةٌ كما بقيّةُ ما لا يعبره الـCI.
`مُثبت` (تخطٍّ مقيس في نسخةٍ ناقصة + `git ls-files`).

**P2 · `tools/eval_pack.py:468-475` + `:768` — «ثمنٌ غيرُ مقيس ⇒ فشلٌ مُغلَق» لا يشمل حقلًا منطقيًّا ولا عدّادًا يناقض قائمتَه**
> `pages = ((d.get('pages') or {}).get('pack_reserved'))` · `if not isinstance(pages, int):` · `'listed_matches': len(listed) == pages,` · `return price_pages <= int(decision['price_cap_pages'])`
النتيجة (نداءاتٌ خالصة): (أ) مانيفستٌ حقلُه `true` ⇒ `pages = True` (و`bool` صنفٌ من `int`) و`gate3_price_within_cap(True, …)` = True، والحكمُ المطبوع `PASS — … (True ≤ 133 صفحة محجوبة)` ⇒ «غيرُ عدد» يمرّ مُبرَّأً؛ (ب) مانيفستٌ عدّادُه ٥ وقائمةُ `skipped` فارغة ⇒ `listed_matches = False` و`why` يقول «تُقرأ لا تُصدَّق»، ثمّ **PASS** لأنّ لا فرعَ يحكم بها. فالمقابلةُ القائمةُ (العدّاد ↔ القائمةُ المُعلَّلة) تُحسب وتُخزَّن وتُطبَع بلا قارئٍ يحكم — وهو صنفُ «قيمةٌ مُخزَّنةٌ بلا قارئ» الذي أُغلق في review-60 لـ`measured_intersection`.
الحركة: `isinstance(x, int) and not isinstance(x, bool)`، وإدخالُ `listed_matches=False` في `blockers`/`failures` باسمه.
`مُثبت` (نداءاتٌ خالصة).

**P2 · `handoff/sulaiman/20260925-2009-PLAN-…md:3-4` + `docs/GATES.md:45-46` — سلطةُ نقلِ قرارٍ مالكيّ غيرُ مقروءةٍ من الشجرة المقيسة**
> «**المُدخَل:** مراجعةُ ٥٩ (`handoff/claude/20260925-175736-…`) ومراجعةُ ٦٠ (`handoff/claude/20260925-195208-…`) — كلتاهما `REQUEST CHANGES`»
النتيجة: `git ls-tree -r HEAD -- handoff/claude` مع تصفية `175736|195208` ⇒ **٠** ملفّاً؛ وآخرُ حُكمٍ في الشجرة المقيسة هو مراجعة ٥٥ (`20260925-021953`). والملفّان موجودان في شجرةِ عملٍ أخرى (`claude/round56-closure-final-377f08` · الالتزامان `476130b` و`da6403b`)، و**لا وجودَ لمراجعة ٦١ في أيّ مرجع** (`git log --all` لا يحمل `review(61)`) بينما الوسمان `R61-1`/`R61-2` يُستعملان في `docs/GATES.md` والضوابط وتعليقات الأدوات. فوثائقُ هذه الشجرة تستشهد بحُكمٍ لا تقرؤه، والوسمان يُنسبان إلى مستندٍ لا وجودَ له ⇒ لا يتحقّق قارئٌ لاحقٌ من أنّ **قرارَ مالكٍ مؤرَّخًا** نُقل بسلطة (قاعدةُ المستودع: «الشاهدُ يُدفَع قبل أن يُجاب» · «بندٌ بلا استشهادٍ ليس بندًا»).
الحركة: يُودَع نصُّ المراجعتَين في الشجرة المقيسة (أو يُسمّى الالتزامُ الحاملُ لهما)، ويُعاد تسميةُ `R61-x` بمصدرٍ قائمٍ فلا يُوهم المستند.
`مُثبت` (أوامرُ git على الشجرة والمراجع).

**P3 · `tests/test_secret_scan.py:31-33` + `:471` — المقابلةُ تقرأ أوّلَ ورودٍ وحدَه**
> `m = re.search(pattern, text.translate(_AR_DIGITS))` · `assert _pair_at(text, pat) == (TATWEEL_FORM_LINES, TATWEEL_FORM_FILES)`
النتيجة: `re.findall` بالنمط نفسِه على `docs/handoff-protocol.md` ⇒ **ورودان**: (١١·٤) أوّلًا ثمّ (٣١·١٢) المؤرَّخ؛ والضابطُ يقابل الأوّلَ وحدَه (وفي `tools/secret_scan.py` ورودٌ واحد). فأيُّ ورودٍ ثانٍ لصيغة «N سطراً في M ملفّاً» يمرّ بلا مقابلة ⇒ رقمٌ مُتقادم يسكن بلا أن يصرخ أحد — وهو صنفُ R59-3 بعينه.
الحركة: تُقابَل كلُّ الورود بمجموعةٍ مسموحةٍ صريحة (الحيُّ + الإنتاجاتُ المؤرَّخة بسمتها وتاريخها).
`مُثبت` (findall بأمري).

**P3 · `handoff/FLAKY_READS.md:35` + `tests/test_flake_ledger.py:84-97` — الميزانيّةُ لا ترى قيدَها الوحيد**
> `| 1 | 2026-09-25 · بلا زمنٍ مُقيَّد (لم يُحفَظ مخرجُه) | … | **لم يُحفَظ** … | 0/1 في نافذة 5 |` · `tail = runs[-window:]` (والنافذةُ تُقرأ من ملفّات الأدلّة المحفوظة)
النتيجة: الواقعةُ التي وُلد منها R60-2 (٤/٥) مُقيَّدةٌ في السجلّ، لكنّها **خارج النافذة** (لا ملفَّ دليل) ⇒ الميزانيّةُ تقرأ «٠/١» بينما السجلُّ يحمل واقعةً مُعلَنةً ألغت الإبراء؛ فالحدُّ المُعلَن («النافذةُ من المحفوظ») صار بعد R60-2 الثغرةَ الباقية: عطبٌ مُقيَّدٌ لا يُحمرّ بها، ومعه `DECLARED_LEDGER_ENTRIES = 1` يمنع الإضافةَ الصامتةَ إلى القيود لا مواجهةَ القيود بالأدلّة.
الحركة: تُقابَل القيودُ بأدلّتها في الاتّجاهين (قيدٌ بلا دليلٍ يُسمّى كما يُسمّى وسمٌ بلا قيد)، أو تُحتسب الميزانيّةُ من (الأدلّة ∪ القيود).
`مُثبت` (قراءةُ السطرين + منطق النافذة).

**P3 · `tests/test_secret_scan.py:436-448` — وسمُ «مؤرَّخ» يُعفي رقمًا حيًّا بكلمة**
> `if 'مؤرَّخ' in para and re.search(<أربعةُ أرقام-رقمان-رقمان>, para): continue`
النتيجة (نداءاتٌ خالصة): `_undated_snapshot_pairs('الشجرة كلها 99·99 حسّاساً و 88·88 بلا حسّاس.')` ⇒ يرصد الزوجَين؛ وبإضافة «مؤرَّخ» وتاريخٍ في الفقرة نفسِها ⇒ `[]` (يمرّ). فالإعفاءُ نصّيٌّ: رقمٌ **حيٌّ** يشمل الصناديق يخرج من كلّ مقياس بكلمةٍ وتاريخ، ولا ضابطَ يتحقّق أنّ «المؤرَّخ» يساوي قياسَ شجرته المُسمّاة ⇒ فتحُ الحاجزِ الذي أُغلق في R59-1 صار اختياريًّا بكلمة.
الحركة: يُقابَل المؤرَّخُ بقياسٍ على شجرته المُعلَنة بالأمر (فيصير التأريخُ نفسُه مقيسًا)، أو تُلزَم صيغةٌ آليّة (اسمُ الالتزام + الأمر).
`مُثبت` (نداءاتٌ خالصة).

## ما لم يُثبت
- كتابةُ الدليل في تشغيلٍ `--full` **ساقط**: قُرئ المسارُ (الكتابةُ قبل `sys.exit` · `gate()` تُحوّل الاستثناءات إلى صفوف FAIL · وملفُّ الدليل المُودَع مطابقٌ لمخرَج الأداة حرفيًّا) ولم أُشغّل `--full` (يستهلك نداءاتِ نموذجٍ وخدمة) ⇒ مُرجّح لا مُثبت.
- لا شهادةَ CI على `42c6f11`: الالتزاماتُ الثلاثةُ غيرُ مدفوعة (origin = `ca2986d`) ⇒ خطوةُ `gate_landing` لم تعمل في هارنس Actions ولو مرّة، وبندُ P1 غيرُ مُجرَّبٍ عمليًّا في CI.
- أثرُ تغييراتٍ غيرِ مُودَعةٍ أو غيرِ مُتتبَّعة: لا يُرى في المدى؛ والقياسُ الحيّ (`--verify`) اعتمد `data/eval_pack/pack.json` غيرَ المُتتبَّع ⇒ المدى لا يستطيع أن يشهد له.
- صحّةُ «كونِ الحُكمِ هو المراجعةَ المقصودة بعينها» (حدٌّ مُعلَنٌ في السجلّ) وسلامةُ نصَّي المراجعتَين ٥٩/٦٠ (خارج الشجرة المقيسة) ⇒ مجهول.

---


## مقعدُ المواصفة (Spec)

# مراجعةُ المدى `ca2986d...42c6f11` — فقراتُ «الحركة» في بنود مراجعتَي ٥٩/٦٠ + التوصية P-11 + الاشتقاق

**القياساتُ الحيّة في هذه الجلسة (شجرة `42c6f11` نظيفة):** `.venv/bin/python -m pytest -q` ⇒ **`770 passed in 48.28s`** · `tools/eval_pack.py --verify` ⇒ **rc=0** والتقاطعُ ٠ والثمنُ «133 صفحة محجوبةٍ عن التدريب (الالتقاط 133 · ويجب إفراجُه 0) ⇒ داخلَ السقف ✓» · `python3 tools/render_claims.py --check` (مفسّرٌ مجرّدٌ بلا تبعيّات ML كالـ CI) ⇒ **rc=0** و`gate3_price_cap_pages: 133` · `tools/landing_probe.py` ⇒ **rc=0** في **١٦.٨ ث** («المراجعةُ هبطت: الخطواتُ وقائمةُ الـCI خضراءُ على شجرةٍ تحملها») · `tools/landing_probe.py --poison name-stamp` ⇒ **rc=1** وأسقط `tests/test_report_names.py::test_every_recent_report_name_matches_when_it_landed` · وأرضيّةُ بناءٍ صناعيّة بتداخلِ صفحةٍ واحدة ⇒ `intersection_gate.pass=false` و`rc=1`.

---

## ① R60-1 — سقفُ القرار على **الثمن** لا على التداخل (حاجبُ مراجعة ٦٠)

**الحالة: مُغلَق بالقياس — لا حركةَ لازمة على أصل البند.** فقراتُ الحركة الأربع:
1. «البوابةُ تبقى «∩ = ∅»، وأيُّ تداخلٍ **FAIL باسمه** بقرارٍ أو بلا قرار» — `tools/eval_pack.py:749-755` (`def gate3_overlap_ok(...)` ⇒ `return not inter`) و`tools/eval_pack.py:785-790` (فرعُ التداخل **قبل** كلّ شيء في `classify_verdict`)؛ والقرارُ أُخرج من محور التداخل بالكامل (`tools/eval_pack.py:81` ⇒ `"price_cap_pages": 133`؛ ومفتاحُ الرخصة محرَّمٌ: `tests/test_eval_pack.py:337` ⇒ `assert "accepted_pages" not in GATE3_DECISION`).
2. «السقفُ ١٣٣ يقع على **الثمن**: الصفحاتُ المحجوبة عن التدريب ≤ ١٣٣، تُقاس وتُقابَل» — `tools/eval_pack.py:758-768` (`gate3_price_within_cap`) · `:452-480` (`capture_withheld` من **مانيفست الالتقاط** لا من الحزمة) · `:857` (الثمن = المحجوب + ما يجب إفراجُه) · `:1180-1182` (إعادةُ القياس نفسه في `--verify`). مقيسٌ: الثمنُ ١٣٣ و`rc=0`.
3. «يُعاد نصُّ `text` بمعنى المالك · ويُقلَب الضابطُ · ويُوجَّه سمُّ م١٩ إلى سقف الثمن» — النصُّ الجديد في `tools/eval_pack.py:64-79` («حزمةٌ نظيفة — لا صفحةَ من الحزمة في بيانات التدريب…») · `tests/test_eval_pack.py:343` (`test_the_owner_decision_closes_the_price_not_the_overlap`) · `:367` (`test_an_overlap_is_a_named_defect_even_with_a_dated_owner_decision`؛ يمرّ ١/٥٠/١٣٣/٢٠٠ صفحةً تداخلًا ⇒ FAIL) · `tools/guard_bite_sweep.py:195` (م١٩ على `price_cap_pages`) و`:203` (م٢٠ على `gate3_overlap_ok`).
4. «انتقالُ `--build` إلى `explicit-list` لا يجوز أن يحوّل تداخلًا إلى PASS» — `tools/eval_pack.py:890-891`: `pack_pages = pack_pages_all` ثم `inter = sorted(set(pack_pages) & cap[\"mine\"])`، والنمطُ يؤثّر في `choose_ranges(exclude_capture=…)` وحدَه (`:832`) ⇒ فبوابةُ التقاطع تُحسب من القرص في النمطين. **قِيس على أرضيّةِ بناءٍ صناعيّة** (تداخلٌ [3]، قرارُ مالكٍ قائم، والثمن 3 ≤ 133): `intersection_gate.pass = False` و`BUILD_RC = 1` والبوابةُ مطبوعةٌ «**مفتوحةٌ — تداخلٌ قائم**» — فالحاجبُ يسقط بالتداخل لا بالثمن. `مُثبت`.

**بندٌ داخل البند:** **P3** — `tools/eval_pack.py:999` — `ok = not blockers and gate3_overlap_shut and price_shut` مع ترتيبِ `classify_verdict` (`:782-784` ⇒ `if blockers: return f"FAIL — عطبٌ مُسمّى أعلاه (…)"`) — **المواصفة:** خطةُ الجولة ٦١ §٣ المرحلة ١: «وسمٌ اصطناعيٌّ بتداخل ١ صفحة ⇒ **rc=1 باسمه**». — **النتيجة:** الرمزُ ١ ✓ والتداخلُ مُسمًّى في سطر البوابة ✓، لكن في الأرضيّة المقيسة كان معها حاجبٌ آخر (المجمَّعةُ المُلتزمة) فطبع **سطرُ الحكم الأخير اسمَ الحاجب الآخر وحدَه** ⇒ قارئُ سطرٍ واحدٍ لا يرى التداخل. أثرٌ في الرؤية لا في الحكم. — **الحركة (للمنسّق):** إضافةُ التداخل إلى `blockers` باسمه بدل فرعٍ متقدّمٍ عليه، أو نصُّ سطرٍ ثانٍ يعدّه. — `مُثبت`

## ② R60-2 — الثقبُ عطبُ نظافة، والدليلُ يكتبه المنتِج

**الحالة: مُغلَق بالقياس.**
1. «شرطُ التغطية يدخل `_is_clean`» — `tools/qa_gate.py:263` ⇒ `and bool(cov) and f_possible == cov[1])` · الضابطُ `tests/test_gate_flake_policy.py:89-110` (الثقب `footer=(9,9)` مع `cov=(9,10)`) · السمُّ **م٢٣** `tools/guard_bite_sweep.py:231`.
2. «`--full` يكتب ملفَّ دليله بنفسه لكلّ تشغيل، ناجحًا أو ساقطًا، لا `tee` بيد الكاتب» — `tools/qa_gate.py:51-52` · `:63-68` · `:86-105` · `:472-476` (الكتابةُ **قبل** `sys.exit`)، والكاتبُ والقارئُ على **موضعٍ واحد** يقابلهما `tests/test_flake_ledger.py:104-119` (والمفسّرُ نسبيٌّ لا مطلق: `:121-134`، وهو ما قاسه المسبارُ قبل الدفع).
3. «ويُقيَّد تشغيلُ ٤/٥ اليوم في السجلّ» — `handoff/FLAKY_READS.md:35` (القيدُ ١) مع `tests/test_flake_ledger.py:42` ⇒ `DECLARED_LEDGER_ENTRIES = 1` (الرفعُ في الالتزام نفسه). والدليلُ المولودُ بالمحرّك الجديد: `handoff/sulaiman/20260925-204451-gate-evidence-full-run.txt:2` ⇒ «`# rc=0 · GATES: 5/5 passed · المفسّر=.venv/bin/python`».

**بندٌ داخل البند:** **P3** — `tools/qa_gate.py:472-476` — `if args.full:` … `path.write_text(evidence_body(ok, RESULTS, full=True), encoding="utf-8")` بعد نداءات البوّابات كلِّها وبلا `try/finally` — **المواصفة:** حركةُ R60-2: «`qa_gate --full` **يكتب ملفَّ دليله بنفسه لكلّ تشغيل** … ناجحًا أو ساقطًا» + `handoff/FLAKY_READS.md:20-24` («والكتابةُ **قبل** الخروجَين فلا يضيع دليلُ السقوط»). — **النتيجة:** السقوطُ «بـ`FAIL`» مُغطّى، أمّا **سقوطُ الأداة نفسِها** (استثناءٌ من بوّابةٍ كسقوط الخدمة قبل قيد نتيجتها) فيسبق الكتابة ⇒ «تشغيلٌ بلا دليل» يعود من بابٍ ضيّق — وهو الصنفُ الذي وُلد منه R60-2. — **الحركة (للمنسّق):** نقلُ الكتابة إلى `finally`. — `مرجّح` (قراءةُ مسارٍ تنفيذيّ لا تشغيلُ سقوط).

## ③ R59-1 — نصُّ المدقّق لا يعود مُدخَلًا إلى راتشتٍ يملكه المنفّذ

**الحالة: مُغلَق في المسار الرئيس.**
1. «خروجُ المقارنة الشاملة» — `tests/test_secret_scan.py:451-479`: لا `excluded_prefix=""` في المقارنة الحيّة؛ المقابَلُ هو المقامُ المُعلَن (`TATWEEL_FORM_LINES/FILES = 11/4`, `:146-147`)، و`:466-467` ⇒ `assert excluded_files > 0` فلا استثناءَ صامت.
2. «وما يشمل الصناديق يُكتب تاريخًا مؤرَّخًا لا يُقابَل» — `tests/test_secret_scan.py:436-448` (`_undated_snapshot_pairs`) و`:476-480`، والنصُّ المُلتزم `docs/handoff-protocol.md:388-395` («⏳ [قياسٌ مؤرَّخ — لا يُقابَل بمقياسٍ حيّ …]»).
3. «إعادةُ إنتاج السقوط القديم ثمّ براءتُه» — سمُّ **م٢٢** `tools/guard_bite_sweep.py:216-226` يُعيد حرفيًّا `excluded_prefix=""` ويلزم أن يسقط **باسم** `tests/test_secret_scan.py` (`MUST_NAME`, `:73`). ودليلُ البراءة الحيّ: `landing_probe` على نسخةٍ تحمل مراجعةً **مقتبِسةً للصيغة** ⇒ **rc=0** (١٦.٨ ث · ١٧ ملفًّا + `publish_guard --tree --ci`). `مُثبت`.

**بندٌ داخل البند:** **P3** — `tests/test_secret_scan.py:447` — `bad += [m.group(0) for m in re.finditer(r"\d+·\d+\s*(?:حسّاساً|بلا حسّاس)", para)]` — **المواصفة:** حركةُ R59-1: «وما يشمل الصناديق يُكتب **تاريخًا مؤرَّخًا لا يُقابَل**». — **النتيجة:** شرطُ التأريخ يغطّي اللفظ `N·M حسّاساً` وحدَه؛ واللفظُ الشقيق `N سطراً في M ملفّاً` — وهو **بعينه الرقمُ الذي كسر الـCI في R58-1** — لا يدخل الدالّة: قِيس أن `_undated_snapshot_pairs(open("docs/handoff-protocol.md").read()) == []` بينما الملفُّ يحمل في السطر ٣٨٤ «**٣١ سطراً في ١٢ ملفّاً**» (رقمٌ **يشمل الصناديق**) بلا ضابطِ تأريخ، و`_pair_at` يقابَل بـ**أوّل** ورودٍ وحده (`:376` = ١١/٤) ⇒ فالورودُ الثاني بلا حارس. الأثرُ محصورٌ في إحكام النثر: الاستثناءُ البنيويُّ أغلق مسارَ احمرار الـCI. — **الحركة (للمنسّق):** توسيعُ الدالّة للفظ «سطراً في … ملفّ» أو إلزامُ التأريخ للورودات كلِّها. — `مُثبت`

## ④ R59-2 — الشاهدُ حُكمٌ يحمل زمنَه وأحدثُ ما سبق الجواب

**الحالة: مُغلَق بالقياس.**
1. «زمنٌ في اسمه (أي أنّه مراجعة)» — `tests/test_answer_names_witness.py:227-232` ⇒ فرعُ «**بلا زمنٍ في اسمه** ⇒ ليس حُكماً».
2. «وهو **أحدثُ** مراجعةٍ زمنُها قبل زمن الجواب» — `:175-186` (`newest_verdict_before`) و`:237-240` (رفضُ ما ليس الأحدث، والرسالةُ **تُسمّي الأحدثَ المطلوب**).
والشوكاتُ تمرّ/تسقط بأسمائها: `:244-272` يشهد صريحًا أنّ `handoff/claude/STATE.md` و`gate-injection-harness.py` كانا يمرّان وسقطا، وأنّ حكمًا قديمًا يسقط وأنّ الأحدثَ يمرّ (فلا موتَ دائريًّا للفروع)، وأنّ ما لا حكمَ قبله لا يُلزَم. وحدُّ الطريقة مُعلَن («هذا **ربطٌ** لا إثباتُ نيّة») في `docs/GATES.md:42` وفي `:175-183`. `مُثبت`.

## ⑤ R59-3 — الرقمُ يُعاد إنتاجُه بالأمر

**الحالة: مُغلَق بالقياس.** التصحيحُ المؤرَّخ في `tests/test_secret_scan.py:136-140` («والأمرُ يُعيده: قاعدةُ الماسح نفسُها … على ملفّات الشجرة `e903b12` ⇒ `27 16`»). أعدتُ إنتاجَه في هذه الجلسة بأمر المدقّق نفسِه على `e903b12` ⇒ **`27 16`** ✓، والقاعدةُ (§٢٣) مثبَّتةٌ في النصّ: كلُّ رقمٍ مع شجرته، والورودان (٢٧·١١ · ٣٦·١٨) تاريخٌ مؤرَّخ في `docs/handoff-protocol.md:388-395`. `مُثبت`.

## ⑥ P-11 — مسبارُ الهبوط (توصيةُ ٥٩ التي تبنّاها المالك)

**الحالة: مبنيٌّ وموصولٌ وعاضٌّ — وله حالٌ غيرُ معلَنة أدناه.**
1. **أداة:** `tools/landing_probe.py:1-29` (العلّةُ والحدود)، تُودِع مراجعةً اصطناعيّةً (`:137-196`) وتقرأ **قائمةَ الـCI من الـworkflow** (`:56-72`، مُقابَلةً بقراءةٍ مستقلّة في `tests/test_landing_probe.py:23-30`).
2. **خطوةُ CI ومعرّفٌ مُسجَّل:** `.github/workflows/publish-guard.yml:72-74` (`id: gate_landing` · `run: python3 tools/landing_probe.py --quiet`) · `docs/GATES.md:65` (القائمةُ المُعلَنة بالضبط) و`:76` (المعرّفاتُ التسعة) و`:46` (صفُّ البوّابة) — والاتّجاهان ٤ و٥ في `tests/test_gate_registry.py` يقابلانها تساويًا.
3. **عضّةٌ مقيسة لا موعودة:** `tools/landing_probe.py:132-134` (السمُّ مُنفَّذٌ في `_name`/`_synthetic_review`، و`tests/test_landing_probe.py:84-92` يرفض سمًّا بلا أثر) ⇒ **rc=1** وأسقط `tests/test_report_names.py::test_every_recent_report_name_matches_when_it_landed`؛ وعلى الشجرة نفسِها بلا سمٍّ **rc=0**.
4. **الصنفُ العائد يُمسَك:** م٢٢ `tools/guard_bite_sweep.py:216-226` (يُعيد المقارنةَ الشاملةَ حرفيًّا) و م٢١ `:208-214`، مع `MUST_NAME` `:71-73` و`:378-382` (عضٌّ بلا الاسم المتوقَّع يُحصى «تعذّر قياس» لا شهادة).

**البند:** **P2** — `tools/landing_probe.py:232` و`:241` — `branch = _git(PROJ, "rev-parse", "--abbrev-ref", "HEAD")[1].strip()` ثم `rc, out = _clone(PROJ, dst, sha, branch)`، مع `tools/landing_probe.py:97-99` ⇒ `args = ["clone", "--no-hardlinks", "--quiet", "--single-branch"]` و`args += ["--branch", branch]` — **المواصفة:** المواصفةُ تُوصي المسبارَ كخطوةِ CI (`docs/GATES.md:46` · «**الـCI (خطوةُ `gate_landing`)**») والـworkflow يعبره على حدثَي `push` و`pull_request` (`.github/workflows/publish-guard.yml:7-8`)، وترويسةُ الأداة تعلن حدودَها (`tools/landing_probe.py:22-28`) ولا تذكر هذا الحال. — **النتيجة:** على `HEAD` **منفصل** تُعيد `--abbrev-ref` القيمة `HEAD` ⇒ يصير الأمر `git clone … --single-branch --branch HEAD` ⇒ `fatal: Remote branch HEAD not found in upstream origin`. **قِيس:** نسخةٌ منفصلةُ الـHEAD من هذا المستودع ⇒ `tools/landing_probe.py` يعود **rc=2** و«⛔ تعذّر القياس: فشلُ بناء النسخة عند 42c6f1182f29». وهذا حالُ `actions/checkout` في `pull_request` (مرجعُ الدمج) ⇒ خطوةُ `gate_landing` تفشل **مُغلَقةً في كلّ طلبِ دمج** لا لعطبِ هبوطٍ بل لبيئةٍ لا يقرأها المسبار — وهو المسارُ الذي يُقتل به الصنفُ بالتعطيل لا بالإصلاح. وله مثالٌ حيّ في هذا المستودع الآن: `.claude/worktrees/key-unpublished-three-defects-0f312f` منفصلُ الـHEAD (`git worktree list`) ⇒ القياسُ اليدويُّ منه، ومنه `--from-worktree` الموصى به في `docs/GATES.md:46`، يقع في العطب نفسه. — **الحركة (للمنسّق):** اشتقاقُ مرجع النسخ من الالتزام وحدَه (`git rev-parse HEAD`) أو إسقاطُ `--branch` عند انفصال الـHEAD ثمّ `checkout --detach <sha>` (قائمٌ فعلًا في `:103`)، وإضافةُ الحال إلى الحدود المُعلَنة، وشوكةٌ في `tests/test_landing_probe.py` على نسخةٍ منفصلة. **وتصحيحُ الحدّ:** `rc=2` فشلٌ مُغلَقٌ صحيحُ الاتجاه لا شهادةُ نظافة — العطبُ في الأنبوب لا في الحكم. — `مُثبت` (القياسُ على HEAD منفصل) · `مرجّح` (وقوعُه في `pull_request`: سلوكُ Actions موثَّقٌ لم يُقَس هنا).

## ⑦ توسّعان بمعرّفَين لا بندَ لهما في الخطة

**P3** — `tests/test_answer_names_witness.py:88-103` و`:391` — `NON_ANSWER_FILES_SINCE_RULE = {`…`}` (قاموسُ مساراتٍ بأصنافها بدل العدّاد `6`، والتعليقُ يسمّيه «R61-1») ومثلُه `tools/guard_bite_sweep.py:71-73` (`MUST_NAME` المُعلَن «R61-2») — **المواصفة:** خطةُ الجولة ٦١ §١ (جدولُ البنود: R60-1 · R60-2 · R59-1 · R59-2 · R59-3 · P-11 — **لا بندَ بهذين المعرّفين**) وحركةُ R59-2 في مراجعة ٥٩ (فقرتان لا ثالثة)، والأصلُ في الفقرة (٦) من ملفّ الشاهد. — **النتيجة:** سلوكٌ لم يُطلب: الآن كلُّ ملفٍّ جديدٍ غيرِ جوابٍ في صندوق المنفّذ **يُحمرّ الـCI حتى تُحرَّر الشيفرة**، والسمُّ الذي يعضّ بلا الاسم المتوقَّع يُنزَل إلى «تعذّر قياس». الاتّجاهُ تشديدٌ لا ترخية (لا يُمرّر عطبًا) وقِيس أخضرَ (٧٧٠ تمرّ)، ومُعلَنٌ في `docs/GATES.md:42` و`:46`. — **الحركة (للمنسّق):** إمّا إدراجُهما في جدول بنود الجولة بتسميةٍ مُتّفقٍ عليها، وإمّا ردُّهما إلى نطاقهما — ولا يُتركان تحت معرّفَين لا بندَ لهما. — `مُثبت`

## ⑧ الاشتقاقُ المعلَّق (review-60 · «الاشتقاق»)

**الحالة: مُغلَق بالقياس.** `tools/render_claims.py:57-70` (`_gate3_price_cap` من `GATE3_DECISION` لا من النثر) · `:102` · `:124-127` (مقابلةٌ في `docs/EVAL_PACK.md` و`docs/GATES.md`)، واللقطةُ `docs/claims.json:20` (`gate3_price_cap_pages: 133`) والنصُّ المُطابَق `docs/EVAL_PACK.md:112`. **قِيس بمفسّرٍ مجرّد** (شرطُ خطوة `gate_claims` في الـCI بلا تبعيّات ML): `python3 tools/render_claims.py --check` ⇒ **rc=0** «كل رقم معلن يطابق مصدره» — فلا يسقط الـCI لأجل استيراد `tools.eval_pack`. `مُثبت`.

---

## ما لم يُثبت
- **سمومُ `guard_bite_sweep` م١٩–م٢٣ لم تُشغَّل:** الأداةُ ليست في الـCI وتشغيلُها يمسّ الملفّات. المُثبَت: السمُّ المقيسُ بـ`landing_probe --poison name-stamp` (rc=1 باسم السقوط) وأنّ السمومَ مُنفَّذةٌ في `CASES`. غيرُ المثبَت: أنّ م٢١/م٢٢/م٢٣ تعضّ **باسمها** في مشغّل السموم، وأنّ م٢٢ تُسقط الـCI فعلًا عند تراجع الضابط.
- **`--build` على بيانات القضيّة لم أُشغّله** (يكتب `pack.json` الحيّ) ⇒ الثمنُ ١٣٣ مُثبَتٌ من `--verify` ومن أرضيّةٍ صناعيّة، لا من بناءٍ حيّ.
- **`qa_gate --full` ٥/٥:** من ملفّ الدليل المُلتزم وحدَه؛ لم أُعِد تشغيله (كلفةٌ والخدمةُ مطلوبة).
- **`pull_request` على GitHub:** قِيس «HEAD منفصل ⇒ rc=2» محلّيًّا؛ ولم أُشغّل Actions لأقيس الحدثَ بعينه.
- **حدُّ R59-2 المعلَن** باقٍ كما أُعلن: صحّةُ كونِه المراجعةَ المقصودة بعينها غيرُ مقيسة، والربطُ إلى «الأحدث» تقريبٌ لا إثباتُ نيّة.
- **ملفّا المواصفة (مراجعتا ٥٩ و٦٠) غائبان عن شجرة المدى** — `git ls-tree 42c6f11 handoff/claude/` لا يحملهما، وقُرِئا من `.claude/worktrees/round56-closure-final-377f08/handoff/claude/` كما أذنت الخطة (وهي نسخةُ عملٍ لا شجرةٌ مدفوعة) — حدُّ قياسٍ أُعلنه لا بند.
- **`--from-worktree` على شجرةٍ نظيفة** لم أُشغّله (قاسه مسبارُ المنفّذ في سياق إصلاح `home_path`).

---


## مقعدُ البنية (Structure)

# تقريرُ مقعد البنية — المدى `ca2986d...42c6f11` (٢١ ملفًّا · +1369/−175)

**الجوابُ في سطر:** التصميمُ خرج **أأمنَ بلا شكّ، وأنظفَ لا**. البوابةُ (٣) أُغلقت ثقبًا حقيقيًّا (رخصةُ إدخال ١٣٣ صفحةً إلى التدريب)، والأداةُ الجديدة تُقيس الثابتَ لا العدّادات — لكنّ **عددَ المفاهيمِ في مجال البوابة (٣) زاد (٥ ⇒ ٨)**: كائنُ قرارٍ ثانٍ (`price_gate`)، وصيغةُ ثمنٍ مكتوبةٌ مرّتين، ومفتاحُ ثمنٍ ثانٍ يحمل الاسمَ نفسه (`capture_remedy_price_pages`)، وجدولُ توقُّعٍ (`MUST_NAME`) يُقابَل بالنصّ لا بالهويّة. والملفُّ الذي يحملها نما من ١١٥١ إلى **١٢٥٩ سطرًا** وهو يصرّح في ترويسته أنّ رقمَه «يُقرأ بـ`wc -l` لا يُحفَظ».

**الحركةُ القاضيةُ المفقودة** (حركةُ الجودو): استخراجُ وحدةِ البوابة (٣) إلى `tools/gate3.py` — كائنُ القرار + دالّتان نقيّتان + دالّةُ قراءةِ الثمن + دالّةُ صياغةِ نصِّه. هذا **يُذيب أربعةَ بنودٍ** في هذا التقرير معًا (١ · ٣ · ٥ · ٦)، ويُنهي استيرادَ `render_claims` لوحدةِ البناء كاملةً (٥٩٥ سطرًا + `pack_io`) لقراءةِ عددٍ واحد، ويُلغي `sys.path.insert` الداخليَّ والاستثناءَ الصامت.

**لا P0** (لا بندَ يُبطل عملًا قائمًا: كلُّ ما أُعلن مُنفَّذٌ فعلًا في الشيفرة).

---

## البنود

### P1-١ · صيغةُ ثمن الإفراج مكتوبةٌ حرفيًّا في موضعين (الكاتبُ والقارئُ لقاعدةٍ واحدة)
`tools/eval_pack.py:856-857` و`tools/eval_pack.py:1180-1182`
> ```
> withheld = capture_withheld(_path(args.capture), run.identity)
> price_pages = (withheld["pages"] + len(remedy)) if isinstance(withheld["pages"], int) else None
> ```
> ```
> _live_withheld = capture_withheld(_path(args.capture), pack["identity"])
> live_price = ((_live_withheld["pages"] + len(remedy_live))
>               if isinstance(_live_withheld["pages"], int) else None)
> ```
**النتيجة:** قاعدةُ «الثمن = المحجوبُ بنيويًّا + ما يجب إفراجُه» لها **تنفيذان** (وسطرُ العرض نفسه مكرَّرٌ أيضًا: `1020-1021` `_price_txt` مقابل `1210` `_price_show`). والتعليقُ في `--verify` يقول «⇒ قياسٌ واحدٌ لا نسختان» وهو صحيحٌ في **المُدخَل** (remedy ≡ inter) لا في **الصيغة**؛ فمن أضاف مكوّنًا ثالثًا للثمن عدّل موضعًا ونسي الآخر ⇒ يُقابل `--verify` ثمنًا آخر غيرَ الذي كتبه `--build` ويسمّيه انحرافًا كاذبًا.
**الحركة:** `def price_of_pack(capture_dir: Path, doc_id: str, to_release: int) -> int | None` ودالّةُ عرضٍ واحدة `price_label(price, why)` — تُنادى من `--build` و`--verify` ومن الطباعة معًا.
**الوسم:** مُثبت

### P1-٢ · «العَضُّ بالاسم» مُقابَلٌ بنصٍّ منسوخٍ لا بهويّة — إعادةُ تسميةٍ تُطفئ الحارسَ صامتًا
`tools/guard_bite_sweep.py:71-73` · `tools/guard_bite_sweep.py:211` · `tools/guard_bite_sweep.py:378-379`
> ```
> MUST_NAME: dict[str, str] = {
>     "م٢١ · مسبارُ الهبوط يكشف مراجعةً لا تهبط (P-11)": "tests/test_report_names.py",
> ```
> ```
>     ("م٢١ · مسبارُ الهبوط يكشف مراجعةً لا تهبط (P-11)", LP,
> ```
> ```
>         want = MUST_NAME.get(name)
>         if want and kind == "bite" and want not in out:
> ```
**النتيجة:** اسمُ الحالة مكتوبٌ مرّتين (في `CASES` وفي `MUST_NAME`)، والمقابلةُ **بحثٌ في قاموس** (لا `KeyError`)، ولا ضابطَ في `tests/` يقرأ `MUST_NAME` أصلًا (فحصتُ الشجرةَ: `MUST_NAME` لا يظهر إلّا في `tools/guard_bite_sweep.py`). فإعادةُ تسمية حالةٍ (وهو العُرفُ في هذا المستودع: `م١٩` تغيّر اسمُه في هذا المدى نفسِه إلى «سقف ثمنه») تُعيد الحالةَ إلى «عَضّت» بلا شهادةٍ من الاسم — **صمتٌ لا حمرة**. والقاعدةُ التي سنّها المستودع في هذا الفنّ نفسه (`FLAKY_MARK` يُستورَد من مُنتِجه، `docs/GATES.md` الصفّ ١٤: «فلا حرفان يفترقان») لم تُطبَّق هنا.
**الحركة:** الاسمُ المتوقَّعُ يصير **حقلًا في الحالة** (خُماسيٌّ بدل رباعيٍّ) فيسافر معها، أو ضابطٌ يُقابِل `MUST_NAME.keys() ⊆ أسماء CASES` تساويًا (بندٌ واحد يمنع الصنف كلَّه).
**الوسم:** مُثبت

### P1-٣ · وحدةُ البوابة (٣) نمت داخل ملفٍّ بلغ ١٢٥٩ سطرًا، ووثيقةُ الاستدلال تستوردها كاملةً لقراءة عددٍ واحد
`tools/eval_pack.py:15-20` · `tools/render_claims.py:64-69` · (`wc -l tools/eval_pack.py` = 1259 عند `42c6f11`)
> ```
> 7. **عبورُ ١٠٠٠ سطر مُبرَّرٌ لا مُتجاوَز** (مراجعةُ المقاعد ٢٠٢٦-٠٩-٢٤: ٩٢٥ ⇒ ١٠٤٦ · **و١٠٤٦ ⇒ ١١٥١ في إغلاق البوابة (٣) ٢٠٢٦-٠٩-٢٥ — والمقيسُ يُقرأ بـ`wc -l` لا يُحفَظ**)
> ```
> ```
>     try:
>         sys.path.insert(0, str(PROJ))
>         from tools.eval_pack import GATE3_DECISION                      # noqa: PLC0415
>         return int(GATE3_DECISION["price_cap_pages"])
>     except Exception:                                                   # noqa: BLE001
>         return None
> ```
**النتيجة:** التعليلُ المكتوبُ يقيس نفسَه ويُخالف نفسَه: يصرّح «يُقرأ بـ`wc -l` لا يُحفَظ» ثم يحفظ «١١٥١»، والقياسُ اليوم **١٢٥٩** (+١٠٨ في هذا المدى وحدَه). وأخطرُ من الرقم: الاستدلالُ العامُّ (حرسُ الوثائق) صار **مُستهلِكًا لوحدة البناء كلِّها** — يستورِد `eval_pack` (الذي يستورِد `pack_io` ويحسب `_data_root()` عند الاستيراد) عبر `sys.path.insert` داخل دالّة، ويبتلع أيَّ فشلٍ في `None`. وحين يعود `None` يُصاغ المطلبُ نصًّا: `f"سقفُ ثمنِ الإفراج {None} صفحة"` (سطر ١٢٤/١٢٦) ⇒ الضابطُ يحمرّ بمطلبٍ لا معنى له، وتُكتب `null` في `docs/claims.json` عند `--write`. هذا **استيرادٌ حساسٌّ للترتيب + تخطٍّ صامت + تبعيّةٌ عكسية** (أداةُ وثائقٍ تشدُّ أداةَ بناء) في ثلاثة أسطر.
**الحركة:** `tools/gate3.py`: `GATE3_DECISION` + `overlap_ok` + `price_within_cap` + `price_of_pack` + `price_label` — بلا استيرادِ `pack_io`، ويستورده `eval_pack` و`render_claims` و`tests/test_eval_pack.py` معًا (`eval_pack` ينزل نحو ١١٣٠ سطرًا، ويُحذف `sys.path.insert` والاستثناءُ الصامت).
**الوسم:** مُثبت (الرقمُ والشيفرة) · مرجّح (أثرُ `None` على `--write` — لم أُشغّله)

### P2-٤ · مخرَجٌ مُعلَنٌ بلا قارئ: `capture_withheld` يحسب مقابلةً لا يقرؤها أحد، ويَعِدُ بأنّها «تُقرأ لا تُصدَّق»
`tools/eval_pack.py:472-477`
> ```
>     listed = sorted(int(s.get("page")) for s in ((d.get("pages") or {}).get("skipped") or [])
>                     if s.get("why") == "pack_reserved" and s.get("page") is not None)
>     return {"pages": pages, "listed": listed, "source": str(mf),
>             "listed_matches": len(listed) == pages,
>             "why": ("القائمةُ المُعلَّلة تطابق العدّاد" if len(listed) == pages else
>                     f"القائمةُ ({len(listed)}) تخالف العدّاد ({pages}) ⇒ تُقرأ لا تُصدَّق")
> ```
**النتيجة:** لا قارئَ لـ`listed` ولا لـ`listed_matches` في الشجرة (فحصتُ: تظهرُ فقط في موضع تعريفها؛ وفي `--build` تُقرأ `pages`/`source`/`why` وحدَها). وأسوأُ: فرعُ المخالفة (`تُقرأ لا تُصدَّق`) لا يصل إلى أيّ مخرَجٍ مرئيّ — `why` تُطبع فقط في فرع **غير المقيس** (`1021`)، أي أن **حالةَ «العدّادُ يخالف قائمتَه» تُمَرّ بصمتٍ تامّ**. فهذا فحصٌ يُوفّي الشكل (`listed_matches`) بلا أثر.
**الحركة:** إمّا يُستهلك (يُضاف إلى `blockers` في `--build` وإلى `failures` في `--verify`)، وإمّا يُحذف الحقلان ويُبقى `pages` — والاثنان معًا لا.
**الوسم:** مُثبت

### P2-٥ · افتراضٌ افتراضيّ يفتح لا يُغلق: `price_pages=0` يخالف قاعدةَ الملفّ نفسِه («`None` = غيرُ مقيس ⇒ فشلٌ مُغلَق»)
`tools/eval_pack.py:771-772`
> ```
> def classify_verdict(*, ok: bool, inter: list, blockers: list, remedy_rows: int,
>                      price_pages: int | None = 0, decision: dict | None = None) -> str:
> ```
**النتيجة:** الغائبُ يُقرأ **صفرًا** — والصفرُ داخل السقف، فيُنتج `PASS` نصًّا: «(0 ≤ 133 صفحة محجوبةٍ عن التدريب)»؛ وهذا بعينه ما منعه المدى («ثمنٌ غيرُ مقيس (`None`) لا يُقرأ صفرًا ولا يُبرَّأ — فشلٌ مُغلَق»، `764`). اليومَ لا مُناديَ يستفيد من الافتراض (المُستدعيان يُمرّران القيمة صراحةً، والضابطُ الأوّلُ في `test_eval_pack.py:312` يعتمد عليه ليمرّ — وهو ما يُثبته فعلًا)، فهو **فخٌّ مُؤجَّل** لا عطبٌ قائم.
**الحركة:** الافتراضُ `None` (الفشلُ المُغلَق هو الافتراضُ الصحيح)، والنداءُ الفاقدُ لقيمةٍ يصير خطأً ظاهرًا.
**الوسم:** مُثبت

### P2-٦ · كلمتان لنفس المعنى: كائنان في `pack.json` باسم «الثمن» ورقمان مختلفان
`tools/eval_pack.py:933-944` · `tools/eval_pack.py:985-993`
> ```
>         "capture_remedy_price": {
>             "pages": len(remedy), "rows": remedy_rows,
> ```
> ```
>     pack["price_gate"] = {
>         "price_pages": price_pages,
> ```
**النتيجة:** القارئُ للحزمة يجد «الثمن» بمعنيَين: `capture_remedy_price_pages` (= ما يجب إفراجُه، صفرٌ في `strict`) و`price_gate.price_pages` (= المحجوبُ + ما يجب إفراجُه، ١٣٣). وفي نمط `strict` **يُطبع** في المخرَج «ثمنُ العلاج المقيس … 0 صفًّا/0 صفحة» (`863-867`) ثمّ «ثمنُها المقيسُ: 133 صفحة» (`1023-1025`) — كلمتان متجاورتان ورقمان متضادّان بلا اسمٍ يفصل بينهما، ودليلٌ مؤرَّخٌ في الشجرة يحمل `capture_remedy_price_pages: 100` (`docs/evidence/20260922-item4-step1-gate3-eval-pack.json:202`) فيُضاعف الالتباس.
**الحركة:** تسميةٌ معنى-واضحة: `to_release` (ما يجب إفراجُه) · `withheld_by_capture` · `price_of_release` — داخل كائنٍ واحد `price_gate` يُدمج مع `capture_remedy_price` (أو يبقى الثاني بحقلٍ يُنفي اسمَ الثمن عنه).
**الوسم:** مُثبت

### P2-٧ · الإصلاحُ وقع على الموضع لا على المصدر: «لا مسارَ منزلٍ في الدليل» تحرسه ترويسةٌ واحدة وضابطٌ على مُدخَلٍ مصنوع
`tools/qa_gate.py:71-83` · `tools/qa_gate.py:313` · `tests/test_flake_ledger.py:136`
> ```
>     exe = Path(sys.executable)
>     try:
>         return str(exe.relative_to(PROJ))
>     except ValueError:
>         return exe.name
> ```
> ```
>     assert pdf.exists(), f"missing {pdf}"
> ```
> ```
>     assert str(Path.home()) not in body, "الدليلُ يحمل مسارَ المنزل ⇒ BLOCK في السطح العامّ"
> ```
**النتيجة:** قاعدةُ الحارس (`tools/publish_guard.py:81`: `home_path` = `BLOCK` على `r"/Users/[A-Za-z0-9_.-]+"`) تُطبَّق على الملفّ كلِّه، والعلاجُ في المدى غطّى **ترويسةَ المفسّر** وحدَها. وأسطرُ تفاصيل البوّابات تُكتب كما هي بقصد («المخرَجُ كاملًا»، `evidence_body` ٨٨-٩١) وفيها مساراتٌ مطلقة مُعلَنة: `assert pdf.exists(), f"missing {pdf}"` حيث `pdf = PROJ / "data/..."` مطلقٌ يحمل اسمَ المستخدم. فالضابطُ الجديد يعلن «ولا مسارَ منزلٍ في **الملفّ كلِّه**» لكنّه يقيس **نصًّا مصنوعًا في الاختبار** بدلالةٍ ثابتةٍ (`("بوّابة", True, "تفصيل")`) ⇒ وسمٌ يعدو دليلَه. وفي هذا المدى صار الخطرُ أعلى: الدليلُ يُكتب آليًّا في كلّ تشغيل (وإن سقط)، و`landing_probe --from-worktree` يُودِع ما يراه `git add -A` (ومنها ملفُّ الدليل غيرُ المتتبَّع) ثمّ يُشغّل `publish_guard --tree --ci` ⇒ تعطيلٌ للهبوط سبّبُه غيرُ الشاهد.
**الحركة:** التنقيةُ عند **الكتابة** (`evidence_body` يمرّ على السطور ويستبدل `PROJ`/`Path.home()` بـ`<repo>`)، والضابطُ يقيس مُدخَلًا فيه مسارٌ مطلق فيسقط إن بقي.
**الوسم:** مُثبت (الشيفرة والقاعدة) · مرجّح (السلسلة الطرفية: لم أُشغّل `--from-worktree` على دليلٍ ساقط)

### P2-٨ · قائمتان لِـ«قائمة الـCI» داخل الأداة الواحدة، وتُعلن «مصدرٌ واحد»
`tools/landing_probe.py:223` · `tools/landing_probe.py:252` · (`tools/landing_probe.py:11-12` «⇒ **مصدرٌ واحد** لا قائمةُ نسخٍ ثانية تتقادم»)
> ```
>     files = ci_files()
> ```
> ```
>         files = ci_files(dst / WORKFLOW_REL)
> ```
**النتيجة:** الحراسةُ القبليّةُ (٢٢٣) والقياسُ الفعليّ (٢٥٢) يقرآن ملفَّ الـworkflow من **شجرتين مختلفتين**، والافتراضُ الخفيُّ في `ci_files()` هو `PROJ` (شجرة المنفّذ). فـ`--list` (الذي يُعلن «ما سيقيسه») يُقرأ من شجرة المنفّذ دائمًا، بينما `measure` يقيس من النسخة ⇒ بعد تعديل الـworkflow في شجرة العمل يطبع `--list` قائمةً لن تُقَاس، وهو عينُ الصنف الذي أُصلح (سمُّ م٢١) لكنّه بقي في مسار `--list`.
**الحركة:** `tree` وسيطٌ صريحٌ لا افتراضيّ (وبدونه خطأ)، و`--list` يعلن الشجرةَ التي قرأ منها.
**الوسم:** مُثبت

### P2-٩ · جردُ الوسائط صار بالنصّ: `_run` يفهم نوعَ الأمر من احتواء الاسم على `.py`
`tools/guard_bite_sweep.py:291-294`
> ```
>     if ".py" in test and not test.startswith("tests/"):
>         cmd = [PY, *shlex.split(test)]
>     else:
>         cmd = [PY, "-m", "pytest", "-q", "-x", test]
> ```
**النتيجة:** الحقلُ نفسه (`test`) يحمل اليوم نوعَين (مسارَ ضابطٍ وسطرَ أمر)، والنوعُ يُستدلّ عليه من **محتوى السلسلة**: ضابطٌ خارج `tests/` (أو سكربتٌ بوسيطٍ فيه `.py`) يُشغَّل تشغيلًا خاطئًا — ونوعُ الخطأ صنفُ «تعذّر قياس» أو، أسوأ، «عَضّ» لسببٍ غير الذي أُعلن.
**الحركة:** الحالةُ تحمل `argv: list[str]` صريحًا (`["-m","pytest","-q","-x",path]` أو `[path,"--from-worktree"]`) — والنوعُ يُصرَّح لا يُخمَّن.
**الوسم:** مُثبت · مرجّح (الحالةُ السيّئة بعينها لم تُقَس)

### P3-١٠ · الحكمُ والعدُّ يُحسَبان في ثلاثة مواضع
`tools/qa_gate.py:55-60` · `tools/qa_gate.py:96` · `tools/qa_gate.py:466`
> ```
>     ok_n = sum(1 for _, p, _ in results if p)
>     return [f"[{'PASS' if p else 'FAIL'}] {n:<{width}}  {d}" for n, p, d in results] + \
>            ["", f"GATES: {ok_n}/{len(results)} passed"]
> ```
> ```
>     ok_n = sum(1 for _, p, _ in results if p)
> ```
**النتيجة:** `_table` يحسبه ويطبعه، و`evidence_body` يعيد حسابَه ليكتب سطرَ الترويسة، و`main` يحسب `ok` ثالثًا — وتوثيقُ `_table` يقول «**موضعٌ واحد** … لا نسختان تفترقان»، وهو صحيحٌ للصفوف وحدَها. (والباقي شكليٌّ اليوم؛ لكنّه نفسُ صنف «نسختان» الذي بُنيت `_table` لقتله.)
**الحركة:** `counts(results) -> (ok_n, total)` تُنادى من الثلاثة، أو `main` يمرّر `ok_n` المُشتقَّ إلى `evidence_body`.
**الوسم:** مُثبت

### P3-١١ · تحليلُ ختم الزمن مكتوبٌ مرّتين في الملفّ الجديد
`tools/landing_probe.py:192-194` · `tools/landing_probe.py:204-205`
> ```
>     shifted = datetime.fromisoformat(f"{stamp[:4]}-{stamp[4:6]}-{stamp[6:8]}T"
>                                      f"{stamp[9:11]}:{stamp[11:13]}:{stamp[13:15]}")
> ```
> ```
>     when = datetime.fromisoformat(f"{stamp[:4]}-{stamp[4:6]}-{stamp[6:8]}T"
>                                   f"{stamp[9:11]}:{stamp[11:13]}:{stamp[13:15]}").astimezone()
> ```
**النتيجة:** الصيغةُ `%Y%m%d-%H%M%S` تُبنى في `measure` (٢٣٧) وتُحلَّل بشريحةٍ يدويّةٍ في موضعين متباعدين ⇒ تغييرُ الصيغة يكسر مُحلِّلَين يجب أن يتقادما معًا.
**الحركة:** `def _stamp_dt(stamp: str) -> datetime` واحدة.
**الوسم:** مُثبت

### P3-١٢ · حقلٌ واحدٌ بكاتبَين في `pack.json`
`tools/eval_pack.py:950` · `tools/eval_pack.py:982`
> ```
>                               "intersection": inter, "pass": not inter,
> ```
> ```
>     pack["intersection_gate"]["pass"] = gate3_overlap_shut
> ```
**النتيجة:** بقايا النموذج القديم: `pass` تُكتب في القاموس الحرفيّ ثمّ تُعاد كتابتُها من الدالّة النقيّة بالقيمة نفسها (لأنّ `gate3_overlap_ok` = `not inter`). كاتبان لقيمةٍ واحدة = الشقُّ الذي يُصلح أحدُهما ويُنسى الآخر (وهو ما حذّرت منه `cmd_build` نفسُها في تعليقٍ أسفلَ السطر: «فلا يُكتب في الملفّ ما يخالف ما يُطبع»).
**الحركة:** تُحذف `"pass"` من القاموس الحرفيّ وتُكتب مرّةً من الدالّة.
**الوسم:** مُثبت

### P3-١٣ · اسمُ دالّةٍ واحد لمعنيَين في الحزمة نفسها
`tools/qa_gate.py:63` · `tools/pack_io.py:80`
> ```
> def evidence_path(now=None, directory: Path = EVIDENCE_DIR) -> Path:
> ```
> ```
> def evidence_path(start: Path | None = None) -> Path | None:
> ```
**النتيجة:** `tools.evidence_path` (سجلُّ تشغيل البوّابة) و`tools.pack_io.evidence_path` (شهادةُ الختم) — اسمٌ واحد ومسؤوليّتان. ومن يقرأ `--verify` يرى `_evidence_path()` (شهادة) ويظنّه دليلَ التشغيل الذي يُنشئه `--full` الآن.
**الحركة:** `run_log_path()` للبوّابة، وإبقاءُ `evidence_path` للشهادة (أو العكس بربطٍ مُعلَن).
**الوسم:** مُثبت

### P3-١٤ · جملةٌ يتيمةٌ في ترويسة `eval_pack` (سابقةٌ للمدى، والإدراجُ الجديد وقع في وسطها)
`tools/eval_pack.py:14-16` · `tools/eval_pack.py:27-28`
> ```
> 6. **ضابطٌ سلبيّ من القرص**: صفحةٌ من عشر صفحاتٍ مشتركةِ الرقم بين مستندين ⇒ **مفتاحُ
> 7. **عبورُ ١٠٠٠ سطر مُبرَّرٌ لا مُتجاوَز** …
> ```
> ```
>    الصفحةِ وحده كان سيُسقطها**، والمفتاحُ الثنائي `(doc_id, page)` يُبقيها ⇒ يثبت أن
>    المفتاح الثنائي ضرورةٌ مقيسة لا تفضيل.
> ```
**النتيجة:** البندُ ٦ مقطوعٌ في منتصفه، وذيلُه وقع **بعد** البندَين ٧ و٨ (تحقّقتُ في `ca2986d` و`42c6f11`: القطعُ سابقٌ للمدى، والبندُ ٨ الجديد أُدرج داخل الفجوة فوسّعها). فترويسةُ «مالك حُكم الختم» تُقرأ اليوم جملةً مبعثرة.
**الحركة:** لمّا كانت المنطقةُ قد مُسّت في هذا المدى، إعادةُ نظم البندَين ٦⇒٧ ودمجُ ٧ و٨ في بندٍ واحد (كلّاهما «لماذا كبر الملفّ»).
**الوسم:** مُثبت

---

## عدُّ المفاهيم — هل نقص؟

**المجالُ (٣) قبل:** `GATE3_DECISION(accepted_pages)` · `gate3_closed` (٣ حدود) · `classify_verdict` (فرعان) · `remedy`/`capture_remedy_price` · `pass = not inter or gate3_shut` ⇒ **٥**.
**بعد:** `GATE3_DECISION(price_cap_pages)` · `gate3_overlap_ok` · `gate3_price_within_cap` · `capture_withheld` (**٤ حقول، ٢ بلا قارئ**) · صيغةُ الثمن (**موضعان**) · `price_gate` (كائنٌ ثالث) · `measured_price_pages` (قياسٌ مشتقٌّ جديد يُقابَل) · `MUST_NAME` · `POISONS`/`--poison` · قائمةُ الـCI المقروءة من شجرتَين ⇒ **٨–١٠**.
**النتيجة:** التعقيدُ **انتقل وتفرّع، ولم يُحذف**. لكنّ الحصيلةَ ليست صفرًا في الاتّجاه الآخر: انقلبت قاعدةٌ كان ثمنُها مدفوعًا (السقفُ على التقاطع)، ونتجت منها دالّتان نقيّتان قابلتان للسمّ (م١٩/م٢٠) بدل شرطٍ داخل دالّةٍ واحدة.

## ما نقص فعلًا (جودو نُفِّذ) — بلا وسوم

- `tools/qa_gate.py:55-60` — جدولٌ واحد (`_table`) يُطبع على الشاشة ويُكتب في الدليل: قُتلت نسختان كانتا تفترقان.
- `tests/test_answer_names_witness.py:84-101` — `NON_ANSWER_FILES_SINCE_RULE` من **عدد** (`= 6`) إلى **هويّةٍ مُسمّاة**، ويُقابَل في الاتّجاهين: صنفٌ جديد لا يمكن أن يُدخل نفسَه برفع رقم (نفسُ مبدأ `DECLARED_NON_VERDICT`).
- `tests/test_secret_scan.py:436-447` — `_undated_snapshot_pairs`: قاعدةٌ واحدة («كلُّ رقمٍ مع شجرته») بدل ثلاث مقابلاتٍ حيّةٍ كانت تُحمرّ الـCI بمجرّد اقتباس المدقّق للصيغة؛ وحُذف `monkeypatch`/`sys` معها.
- `tools/eval_pack.py:749-768` — شرطٌ ثلاثيٌّ في دالّةٍ واحدة (`gate3_closed`) صار حدَّين مُوزَّعين على دالّتين نقيّتين **مع الحفاظ على الثلاثة كلِّها** (السقف · القرار · لا عطب) — إعادةُ توزيعٍ لا حذف: وهو الشكلُ الصحيح لأنّ كلَّ حدٍّ صار له سمٌّ يخصّه (م١٩ · م٢٠) وضابطٌ يخصّه.
- `tools/qa_gate.py:259-263` — «نظافةُ قراءة» واحدة يقرؤها القاضي والطباعة والحزام الثاني (`g_end_to_end`) ⇒ ثقبُ المقام لم يبقَ له مسارٌ صامت.

## ما لم يُثبت

1. **لم أُشغّل شيئًا** (مقعدُ قراءة): لا `pytest`، ولا `landing_probe`، ولا `publish_guard`. كلُّ استنتاجٍ عن سلوكٍ زمنيّ (١٧–١٨ ثانية · زمنُ الـCI) منقولٌ من الوثائق لا من قياسي.
2. **سلسلةُ P2-٧ الطرفيّة**: قِستُ أطرافَها (القاعدة `home_path` في `publish_guard.py:81` · `f"missing {pdf}"` مطلق · الدليلُ يُكتب آليًّا · `_copy_worktree` يُودِع ما يراه `git add -A`) ولم أُثبت وقوعَ التعطيل بتشغيلٍ واحد من طرفٍ إلى طرف.
3. **لم أُثبت عددَ الاختبارات** (٧٧٠ في اللقطة/الوثيقة) — نقلٌ لا قياس.
4. **لم أُقِس أثرَ `--list` في P2-٨ على الـCI** (لا خطوةَ تُشغّله بـ`--list`): البندُ عيبُ واجهةٍ داخليّةٍ لا حاجبٌ قائم.
5. **حالةُ الشجرة أثنائي:** رأيتُ لحظةً `M tools/landing_probe.py` و`?? tools/landing_probe.py.bak_bite` ثمّ `M tests/test_secret_scan.py` (أثرُ تشغيلٍ متزامنٍ لأداة لاحتميّة/عضّ — سمُّ م٢٢ يعيد حرفيًّا ما كان). التهميشُ لا يمسّ المدى، وقد تحقّقتُ بـ`git hash-object` أنّ كلَّ ملفٍّ استشهدتُ به == `HEAD` (‏`42c6f11`) لحظةَ قراءتي، فأرقامُ الأسطر صحيحةٌ للمُدخَل.
6. **`POISONS` في `landing_probe` يحمل سمًّا واحدًا** (`name-stamp`) وسلوكُه موزَّعٌ على دالّتين (`_name` تُزيح الزمن · `_synthetic_review` تُدرج تحذيرًا): ضابطُ `test_every_declared_poison_is_implemented` يقيس «أثَرٌ ما» ولا يقيس أنّ الأثرَ هو **السقوطُ المطلوبُ في الضابط المطلوب** — لم أُثبت أنّه لا يمرّ لسببٍ آخر (وهو ما يمنعه `MUST_NAME` لحالتَي `guard_bite_sweep` وحدَهما). هذا **حدٌّ مُعلَن** في الأداة لا بندَ عطب.

---
