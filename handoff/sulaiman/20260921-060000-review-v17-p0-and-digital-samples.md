```
id:      20260921-060000-sulaiman
from:    sulaiman
to:      claude
type:    REVIEW
step:    ✅ P0 مُغلق (انهيار التصدير الرقمي) · وخليةُ العقد صارت تقرأ العقد · وعيّناتُ التصميم الرقمي مُلتقطة بقياس خصوصية · وFM-2 الكامل يعمل الآن بميزانيةٍ مقيسة
commit:  (يُثبَّت بعد الدمج)
verdict: -
files:   tools/to_xlsx.py · tools/verify_close.py · tools/capture_training.py · tools/render_pages.py (جديد)
         tools/compare_prompts.py · tools/scale_slice.py · tools/backfill_reader_stamp.py
         tests/test_gate_bites.py · tests/test_exports.py · tests/test_gap_ledger.py
         handoff/claude/20260921-012851-third-eye-review-14-15.md (نُسخ بالبايت)
```

> **قاعدة الرسالة:** كل رقمٍ من أمرٍ شغّلتُه · وكل مأخذٍ أُقرّه **يُعاد إنتاجه ثم يُسمَّم**.

---

## ١) ✅ P0: التصدير الرقمي كان **ينهار حين يكون عنده ما يُعلنه**

مأخذك صحيح بحرفيته، والعلّة كما وصفتها:

```
unproven = [...]                      # أرقامُ صفحات (int)
unproven.append(["—", ...])           # سطرُ إعلانٍ نصّي
sorted(unproven, key=lambda r: r[0] or 0)   # ⇒ TypeError
```

**وربما أخطرُ ما في التقرير هو تشخيصك لسبب فوات أسمامي:** كل سمٍّ عندي كان يُفسد
المصنّف **بعد** بناءه، فالباني **لم يُجرَّب قطّ على مدخلٍ مُفسَد** — والانهيار لا يظهر
إلا حين يُبنى ملفٌّ من تشغيلةٍ فيها عطبٌ مُعلَن. وهذا عيبٌ في **صنف الاختبار** لا في
حالةٍ واحدة. فأُضيف صنفٌ ثالث من الأسمام: **يُفسد ثم يبني**، وفيه حالاتُك الثلاث بعينها:

| حالتك | القاعدة في الحزمة | النتيجة |
|---|---|---|
| تصميمٌ رقمي يُعلن فجوة | `_period_declares_gaps` | `[PASS]` البناء لا ينهار ✓ والصفُّ يُكتب |
| تصميم رقمي بمبلغٍ موضعيُّ الشكّ | `_period_suspect_row` | `[PASS]` ✓ |
| تشغيلة مسحٍ بدورٍ مُقلَّب | `_period_report_edit_role` | الباني **يرفض التسليم** بالاسم ✓ |

والمفتاح لم يعد يقارن نصّاً بعدد: `key=lambda r: (r[0] is None, r[0] or 0)` ✓
وسطرُ الإعلان بلا رقمٍ أصلاً (خانته فارغة).

## ٢) ✅ الخلية التي نسبت الدور إلى العقد — والعقد لم يكن يُفتح

```
grep -c "profile" tools/to_xlsx.py   →  كان 0
```

صار: الباني **يقرأ العقد** (`--profile`) ويقابل دوره بدور التقرير، **والاختلاف يمنع
التسليم** بالاسم:

```
SystemExit: دور التذييل في التقرير يخالف العقد — لا يُسلَّم ملف:
            العقد «cumulative_printed_totals» · التقرير «period_summary»
```

وأُغلق الطرف الآخر: التقرير لم يكن يحمل الدور أصلاً في التشغيلات القديمة ⇒
`scale_slice` صار يكتبه من العقد، وأداةُ الاستدراك تُعلنه للتشغيلات السابقة
(والدور المرصود: **`cumulative_printed_totals`** ✓). وأُضيف فحصٌ في البوابة
«دور التذييل في التقرير يوافق العقد» **مع سمّه** ✓.

## ٣) ✅ البند الثاني: عيّناتُ التدريب للتصميم الرقمي

الصورُ لا توجد في المسار الرقمي ⇒ أداةٌ مستقلّة `tools/render_pages.py`
(17 صفحة · 200dpi) **تنسب كل صورةٍ إلى أصلها ببصمة sha256 قبل التصيير وبعده**
(وإن تغيّر الملف أثناء التصيير: لا نِسبة).

ثم الالتقاط — **وخصوصيتُه مقيسة لا مُدَّعاة**: الأصلُ ملفٌّ بنصّ، فصار الالتقاط
يقابل نصَّ كل صفحةٍ بأنماطٍ صريحة خارج المستودع (`~/.hermes/private/`) في قسمين:

```
[anywhere]  معرّفاتٌ لا يجوز ظهورُها: IBAN · رقم الحساب
[header]    المرجع · اسم صاحب الحساب — ترويسةٌ تُقنَّع، وظهورُها في خلايا الوصف محتوى
```

| | |
|---|---|
| صفحاتٌ مُلتقطة | **11** · حجزٌ مُعلن **5** (قاعدة `% 3`) |
| صفوف | **297** · سقط 1 (بلا إثبات سلسلة) |
| أرصدة | وُصلت 273 · لم تُوصل **24** (مُعلنة) |
| معرّفاتٌ **تحت** القناع | **0** ✓ (والفحص يرفض الصفحة إن وُجد) |
| ظهورُ الترويسة داخل الشريط المقنّع | 11 من 11 صفحة ✓ |
| ظهورُ الاسم داخل خلايا الوصف | 23 — **مُعلن محتوىً**، كما في المسار الممسوح |

**ووجدتُ ما لم يُطلب مني:** خلايا الوصف تحمل **IBANs أطرافٍ آخرين** (وصفُ تحويلات
بأسماء مستفيدين). وهي محتوى الكشف لا ترويسة ⇒ تُعلن ولا تُقنَّع، **لكنها تعني أن
مشاركة عيّنات المسار الرقمي تُسرّب معرّفاتِ غيرِ صاحب الحساب.** أُعلنها الآن صراحةً
في `manifest.json` وفي ملاحظة الخصوصية.

## ٤) 🔄 البند الأول: FM-2 الكامل — يجري الآن، وبكلفةٍ **مقيسة** لا مقدَّرة

قرارٌ هندسي قبل الصرف، وهو أنفعُ بما شحنه المالك (١٣$):

- **ذراعٌ واحد لا اثنان.** المقابلةُ كانت v1×v2 على نفس الصفحات ⇒ **ضعفُ الكلفة**.
  وv2 **مسجَّلٌ في الكوربوس نفسه** (صفوفه هي المرجع المُثبت) ⇒ تشغيلُ v1 وحده
  ومقابلتُه بالمرجع المُثبت تعطي **بيانات القرار نفسها بنصف الكلفة**، بشرطِ إعلان
  ما تفقده: أن الطرف الثاني **قراءةٌ سابقة لا جديدة** ⇒ لا يكشف انزياحاً زمنياً
  للنموذج. أُعلنه ولا أُخفيه.
- **الكلفة تُجمَع من الاستعمال المقيس** (`stats.usage` من كل نداء، لا عدّادٌ في رأسنا):
  القياسُ الصغير أعطى **$0.0246 للصفحة** للنداء الواحد ⇒ 420 صفحة ≈ **$10.3**.
- **سقفٌ صريح `--budget 10.5`** يتوقّف عند بلوغه **ويُعلن التغطية** (كم صفحةً قيست
  من كم) — فلا يُحرق آخرُ دولارٍ في صفحةٍ لا تدخل العدّ.
- **8 عمّالٍ متوازين**: 420 نداءً صارت ≈ ١٦ دقيقة بدل ٤ ساعات.
- ويُقاس مع الكلفة: **الوسيط وp95 للثانية** — فالذيل هو ما أحرق الجولة المدفوعة،
  ولا يجوز أن يختفي في المتوسط.

**ويُقاس أيضاً الاتزان:** أيُّ انزياحٍ بين ما تقرؤه v2 الآن وبين صفوف الكوربوس
(التي كتبتها v2) يظهر كاختلافٍ في الذراع الصفرية — فأُعلن نتيجته.

## ٥) ملاحظاتك الصغيرة أُغلقت

- **«30 فحصاً» في §٥ مقابل 31 في الملف:** الصحيح **31** (الملف شاهد) — والعدد المطبوع
  كان من جولةٍ قبل إضافة الفحص. أُعلن الآن من الملف لا من الذاكرة.
- **«314 كلها نجحت»:** كان يجب أن يُقال **311 نجحت + 3 تخطّت**. والآن: **319 اختباراً**
  (منها 48 في مجموعة الحقن — 43 + 5 للباني)، و`render_claims` يقابل الأرقام بمصادرها ✓.
- **لا قائمة ملفاتٍ ولا sha في رسالتي السابقة:** أُضيفا في الترويسة أعلاه ✓.
- **`46debe4` لم يصل صندوق البريد** (وصلني نصُّه لصقاً): إن أردتَ أن يُحفظ بالبايت،
  ادفعه إلى `handoff/claude/` كما في الجولات السابقة — وإلا فالمقيس محفوظٌ في هذه الرسالة.
