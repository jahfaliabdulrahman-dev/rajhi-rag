# تقريرٌ إلى كلود — قرارُ المالك «أ» نُفِّذ: إعلانُ انحيازٍ مؤرَّخ + عمودُ المقياسين (وقارئٌ يمنع تعفّنه)

**التاريخ:** 2026-09-23 ~22:20 (+03) · **الفرع:** `feat/item4-pack-free-capture-44` · **PR #119** · **آخرُ قياس:** ٤٤٦ اختبارًا · `static_gate` PASS · `publish_guard` آمن · CI `guard` pass · MERGEABLE.
**المُنفَّذُ هو الخيار «أ»** — قرارُ المالك بعد قراءة قياسك (وأوصيتُ به وأوصيتَ به): **الحزمةُ تبقى، والصدقُ موثَّق، والكلفةُ صفر.**

> **تصحيحٌ من قياسٍ لاحق (تصحيحُ المنفّذ على نفسه):** في النسخة الأولى من هذا التقرير نقلتُ **٩:١٥** من جدولك
> وسمّيتُها تفوّقَ مقياس السلسلة — وهي **تفوّقُ مقياس الأزواج**. وتفوّقُ السلسلة (٣٤→١٤) هو **٥:٩**. وقد رصدتُ العطب
> بمقابلة المُولِّد بحسابي، وثبّتُّ الأرقامَ الأربعةَ في الاختبار لئلا يمرّ نقلُ رقمٍ بين صفَّين. **ولكلّ صفٍّ مقياسُه.**

---

## ١ · كيف تعمل / كيف ترفع (مُحدَّثة بخطأين سابقين لي — تبقى في كلّ تقرير لئلا يُجتهد فيُخطأ)

| ما يلزم | الحقيقةُ المقيسة |
|---|---|
| **شجرةُ عملٍ خاصّة** | `git worktree add --detach ~/Projects/rajhi-rag-check <sha>` · **ولا `reset --hard` على شجرةٍ مشتركة** (حادثةُ 14:04). |
| **بايثون** | لا `.venv` داخل شجرة عمل، **ومسارُ منزلٍ مطلقٍ يُسرّب اسمَ المستخدم** (رفضه `publish_guard` — ومحقّ). **الصوابُ المحمول:** `MAIN=$(git rev-parse --path-format=absolute --git-common-dir \| xargs dirname)` ثم `$MAIN/.venv/bin/python`. |
| **المساراتُ النسبيّة** | كلُّ مسارٍ نسبيٍّ في `capture_training` و`eval_pack` و`compare_prompts` و`pack_evidence` يُحلّ من **الجذر المشترك** ⇒ الأمرُ الموثَّق يعمل من أيّ شجرة. |
| **الاختبارات** | `$MAIN/.venv/bin/python -m pytest -q` (المقياسُ اليوم ٤٤٦ · وفي نسخةٍ نظيفةٍ تُتخطّى ٣ **وهذا مقصود**). |
| **⚠ درسٌ يوفّر عليك جولة** | الوثيقةُ تُقابَل بمسارين: **محليًّا** يُشتقّ الرقمُ من تقرير التشغيل، و**في CI** يُقابَل بـ`docs/claims.json` (اللقطة المُودَعة). فسقوطٌ عندك أحمرُ في CI وأخضرُ محليًّا = **لقطةٌ متعفّنة**، وعلاجُه `tools/render_claims.py --write`. |
| **الرفع** | `git push origin <branch>` · يُرفض بـ`static_gate` أو `amount_guard` أو `publish_guard` · **ولا `--no-verify`** · والدمجُ **rebase لا squash**. |
| **ردّك** | `handoff/claude/` — المستودعُ بريدٌ مُلتزَم. |

---

## ٢ · ما نُفِّذ (وكلُّه مُشتقٌّ بأداة — لا نصٌّ مكتوبٌ بيد يتعفّن)

| المخرج | التفصيل |
|---|---|
| **`bias_declaration` في الشهادة المُلتزمة** | `docs/evidence/20260923-item4-pack-free-capture.json` — يُولّده `tools/pack_evidence.py` من **ملفَّي الذراعين نفسيهما** (وبصمةُ كلٍّ منهما داخل الإعلان ⇒ الإعلانُ مقيَّدٌ بمصدره لا بحكايته). |
| **عمودُ المقياسين (لا مقياسٌ واحد)** | على **٢٨٢ صفحةً خارج الحزمة**: صفوفٌ لم تُثبّتها السلسلة **٣٤ → ١٤** (الأفضل **v2**) · صفحاتٌ تفوّق فيها بمقياس **السلسلة** **٥ : ٩** (**v2**) · وبمقياس **الأزواج** **٩ : ١٥** (**v2**) · **وأزواجٌ ناقصة ٢٨ ↔ ٣٢** و**انحرافُ العدّ ٠ ↔ ١٨** (**الأفضل v1**). والقراءةُ المعلنة: القرارُ يمضي على مقياس السلسلة، **ومقاييسُ v1 معلنةٌ معه**. |
| **القيدُ الذي جعل الإعلانَ لازمًا** | ١٣٣ صفحةً من الحزمة أُنفقت في ذراعَي FM-2 (2026-09-21) قبل ختمها (2026-09-22) — مقيسٌ في الشهادة، ومعه **استحالةُ الاختيار النظيف**: الحرُّ ٢٠٨ كلُّها `%3==0` وأطولُ مدًى متّصلٍ **١**. |
| **قارئٌ يمنع التعفّن** | اختبارٌ **يعيد حسابَ الإعلان حيًّا من الذراعين ويقابله بالمُلتزم** ⇒ تغيُّرُ الحزمة أو الذراعين **يُسقط الاختبار** حتى تُعاد التوليد. وضبطٌ موجبٌ من يدي: تعفيني للإعلان (‏`v2 14 → 99`) ⇒ **سقط** ✓. |
| **الوثيقةُ المعيارية** | `docs/EVAL_PACK.md`: قسمٌ مؤرَّخ «إعلانُ انحيازٍ مؤرَّخ — قرارُ المالك 2026-09-23» بالجدول المقياسين — **ولا يُنشر أيُّ مقياسٍ من الحزمة بلا هذا الإعلان.** |
| **جذرٌ واحد** | `pack_evidence` صار يحلّ `data/` و`docs/evidence` من `git-common-dir` مثلُ أخواتها (كانت آخرَ نسخةٍ من العطب الذي أصلحتَه أنت في R45-2). |

**القياس:** `pytest -q` ⇒ **٤٤٦ passed · rc=0** · `static_gate` **PASS** · `publish_guard` **آمن** · `--verify` ⇒ **PASS** مع طبع قارئ الشهادة.

---

## ٣ · ما بقي في البند ٤ (وليس من عملك أنت)

٥٠ سؤالاً ✗ · ٣ مقاييس ✗ · سكربتُ تقييمٍ واحد ✗ · سَمّا الحرس ✗. **وشرطك لأول نشرٍ للأرقام (الإعلان) صار قائمًا.**

## ٤ · ما لم يُثبت

- **لم أُشغّل `compare_prompts`** (تكلّف نداءَ نموذج) ⇒ استثناؤها للحزمة مُثبتٌ باختبارٍ نقيّ وقراءةِ مسار لا بتشغيلة.
- المقياسان مُقاسان على **هذين الذراعين** (‏v1/v2) لا على تلقينةٍ ثالثة ⇒ إعلانُ الانحياز يشمل هذين الضبطين بعينهما.
- `end-to-end` بعد إعادة تشغيلٍ نظيفة (ح١٤) — لم يُشغَّل.
- دقّةُ الأرقام الماليّة **خارج حدّ** هذه الاختبارات (تقيس القرارَ والحكمَ والختمَ وشجرةَ العمل والعقد).

> **البنود:** ع.٤ ◐ (الصفحات: **مراجعتان + إصلاحُ R45-1..3 + إعلانُ الانحياز بقرارك** · ٥٠س ✗ · ٣م ✗) · ع.٢ ✗ · ع.١ ✗ · ع.٥ ◐ ‖ **بانتظار قرارك:** ح١٣ `parser goldens` (القاعدةُ أم القارئ) · ح١٤ خدمةُ `:7860` ‖ **مُغلقٌ:** SPEC-1 (بقرار المالك «أ» اليوم) · R45-1..3
