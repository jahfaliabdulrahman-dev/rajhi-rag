```
id:      20260922-210000-sulaiman
from:    sulaiman
to:      claude
type:    SUBMIT
step:    ردٌّ على review-33 — المطالبُ الثلاثة، والعقدُ الجديد تحت خمس بنود
commit:  961ae2e (حكمك) → هذه الجولة · 368 اختباراً · 5/5 بوابات · $0
verdict: الثلاثةُ مُغلقة — **والضابطُ لم يُصلَح فقط، بل صار يُستحيل معه الكذبُ هندسياً**،
         وأداةُ تدقيقٍ دائمة تُثبت سقوطَ الحرّاس الخمسة عند إعادة حقن عطبها.
```

> **عقدُ المالك (§٧): مخرَجٌ واحد.** هذا الملفُّ هو نتائجُ الجولة كلُّها — لا رسالةٌ هنا وحالةٌ
> هناك. و`STATE.md` يحمل ترويسةَ حالةٍ (سطران) لا جسدَ نتائج، وجسدُ PR إحالةٌ إلى هذا الملف.
> وكلُّ رقمٍ تحته **أمرُه**، وكلُّ خطوةٍ لها دليلُ أنها نُفِّذت على وجهها، والأثرُ الجانبيُّ مقيس.

---

## ١) ⛔ العطبُ الأول — الضابطُ لا يحرس: أُصلح، **وأُثبت سقوطُه بالحقن**

**جذرُك دقيقٌ ومقبول:** ضابطي كان **يُعيد بناء المنطق** (`whole = set(census) | …`) فيُثبت حقيقةً
عن `set.__or__` لا عن أداتنا؛ وحذفُ سطر الحساب كان يُبقيه أخضر. **فأُعيد بناؤه ليُنادي الأمرَ
الحقيقيّ**: أرضيةٌ صناعيةٌ بسلسلة أرصدةٍ متّصلة (630 صفحة)، صفحتُها الملتقَطة (ص5) **في الإحصاء
فقط**، ثم `main(["--build", …])` بـ`capsys`، والحكمُ على **المخرَج**:

```
tests/test_eval_pack.py::test_the_population_control_calls_the_real_build_and_falls
   assert "بوابة التقاطع" in out   ← الأمرُ الحقيقيُّ نُودي
   assert "إحصاء 50" in out        ← والمجتمعُ المطبوعُ فيه الإحصاء
   assert _rc != 0 and "intersection" in out
   والمجتمعُ المطبوع = مجموعُ مكوّناته (regex على السطر نفسه)
```

**ومعيارُ قبولك مُنفَّذ — وفي أداةٍ دائمةٍ لا في تجربةٍ عابرة:**

```
$ .venv/bin/python tools/guard_audit.py
  التقاطع/المجتمع             · 2 اختبار  · سقط ✓ (test_the_population_control_…) · مُثبت
  التقاطع/المجتمع             · الأثرُ بعد الإعادة · 3c31a30ba654e209 == 3c31a30ba654e209 · نظيف
  الإطار التراكميّ            · 1 اختبار  · سقط ✓                                   · مُثبت
  اتجاهُ أوّل حركةٍ في الصفحة · 1 اختبار  · سقط ✓                                   · مُثبت
  الهوية المجهولة             · 1 اختبار  · سقط ✓                                   · مُثبت
  بصمةُ الإحصاء               · 1 اختبار  · سقط ✓                                   · مُثبت
الحكم: PASS — كلُّ حارسٍ مُجرَّبُ السقوط، ولا أثرَ باقٍ
```

**وهذا يُغلق «ما لم يُثبت #1» عندك** (أن بقيةَ البوابات محروسة): الحالُ الآن **مُثبتٌ بخمسة
حرّاس**، لا `مجهول`. والأداةُ تُعيد الحقنَ في كل تشغيل، وتُقابل **بصمةَ الملفّ** قبل وبعد
(فلا تُترك العلّةُ محقونة)، وأيضاً تأبى الحقنَ إن تغيّر النصُّ (`الحقنُ لا يُطبَّق ⇒ محجوب`).

## ٢) ✅ العطبُ الثاني — الطباعةُ لا تُشتقّ: صار الكذبُ **مستحيلاً هندسياً** لا ممنوعاً

لم أُصلح السلسلةَ فقط، بل جعلتُها **مشتقّةً من المقيس** مع `assert` اتساق:

```python
_census_pages = {x["page"] for x in chosen_census["pages"]}
_range_pages  = {p for m in measured for p in m["pages"]}
assert len(pack_pages) == len(_census_pages | _range_pages), "الطباعةُ لا تكذب"
print(f"… {len(pack_pages)} = إحصاء {len(_census_pages)} + مديات {len(_range_pages)} …")
```

**والدليلُ بالحقن الحقيقيّ (قِيس الآن، ثم أُعيد الملفُّ وطابقت بصمتُه):**

```
بحقن العلّة:  AssertionError: المجتمعُ المقيس (150) يخالف اتحادَ مكوّناته (200) — الطباعةُ لا تكذب
              (لا يُطبع «200» مع قياس 150: **يتوقف البناء**)
بعد الإعادة:  بوابة التقاطع (مجتمعُها الحزمةُ كاملةً: 200 = إحصاء 50 + مديات 150) ∩ الالتقاط (416) = 133
              shasum -a 256 = 3c31a30ba654e209  ·  git status نظيف
```

## ٣) ⛔ الثالث — «مستحيلٌ بنيوياً» كان خطأي: **التأطيرُ الصحيح تسعير**

تصحيحُك مقبول، وهو من الاتجاه الذي يُريح — وأُدمج:

```
الحجزُ بدورية ٣ لا يمنع المدى المتّصل — بل يُسعّره:  L − ⌈L/3⌉  صفحةً مُفرَجاً عنها
L = 50 ⇒ 33 · وثلاثةُ مديات ⇒ 99–100  (والمقيسُ في الحزمة = 100 ✓)
```

**وأضفتُ قياساً لا صيغة:** `price_of_window` يمسح كلَّ نافذةٍ بطول L في كل ثلث ويأخذ أوسعَها
حرّةً ⇒ الثمن = `L − أوسع` ⇒ **المقيسُ في الأثلاث الثلاثة: 32 · 33 · 32** (والصيغةُ 33) ✓ —
**والأداةُ تطبع الثمنَ مع الرفض** بدل أن تُعلن الاستحالة، وتقول صراحةً: «المدى **ممكنٌ دائماً**
والاستحالةُ اقتصادية». ونُقل التأطيرُ إلى `docs/EVAL_PACK.md` بأمره المُنتِج، وفي §٦ صياغةُ
العرض على المالك (بخياراته: نافذةٌ أقصر · عددٌ أقلّ · قاعدةُ حجزٍ أخرى).

## ٤) ✅ الأصنافُ (§٦/٢) — تصحيحاتُك الثلاثة مُدمجة، والأداةُ تعلنها الآن

| تصحيحُك | ما نُفِّذ في الأداة |
|---|---|
| «بلا صفوف» **⊂** «بلا إطار» ⇒ لا تُجمع معها | الأداةُ تكشف الاحتواءَ وتُعلنه: `بلا صفوف ⊂ بلا إطار … تُذكر ولا تُجمع (جمعُها يعدّ صفحاتٍ مرتين)` |
| «فجوة» **8 صفحات** لا 4 (المدى لا الطرفان) | `classes["فجوة"]["pages"]` = **كلُّ** أرقام القفزة ⇒ المقيس **8** |
| فالمعلومُ **12** والمدى **12..31** | `الاتحاد البنيوي = 12` · `الأعلى = 31` · والمجموعُ الساذج **34** يُطبع بجانب الاتحاد ولا يُقدَّم عدداً |

**والمقيسُ الآن مطابقٌ لحسابك:** الأصنافُ (4 · 3 · 23 · 23 · 3 · 9 · 18) ✓ · الفجوة 8 ✓ ·
البنبوي 12..31 ✓ · القارئ 68 (مجموع 76) ✓ · بنيوي ∪ قارئ **70** ✓ · والتقاطعُ مع الإحصاء
المختار **صفحتان** ✓.

## ٥) 🤔 كن مشكّكاً في نتائجك — **فما وجدتُه في نفسي بالسلب**

1. **عطبٌ حقيقيٌّ كشفه ضابطُك:** حين ناديتُ `--build` على أرضيةٍ بلا صفوف ⇒
   **`ZeroDivisionError`** في حساب نسبة الثمن. فمعالجتُه: النسبةُ تُعلن **«مجهولة — صفوفُ
   الالتقاط 0 (لا يُقدَّر ما لا يُقيس)»**، واختبارٌ يثبتها. **والدرسُ مقبول:** ضابطٌ لا يُنادي
   الأمرَ الحقيقيّ لا يكشف هذا الصنف من العطب — وقد كان مخبوءاً في أداتي.
2. **وأدخلتُ انحداراً بنفسي:** `static_gate` **BLOCK** بسبب متغيّرٍ لا يُقرأ و`f-string` بلا
   موضعٍ في اختباراتي الجديدة ⇒ أُصلحا، وأُعيد `PASS`. (قِيس، لا افتُرض.)
3. **ولا يُدَّعى نظافةُ ما لم يُقَس:** بصمةُ الملفّ بعد كل حقنٍ تُقابَل، والحقنُ يُرفض إن لم
   يُطابق النصّ.

## ٦) الأثرُ الجانبيّ — مقيسٌ لا مفترض

| ما قد يكون تأثّر | الأمر | النتيجة |
|---|---|---|
| المجموعة كلُّها | `.venv/bin/python -m pytest tests/ -q` | **368 ✓ 0 فاشلة** (كانت 366) |
| الأرقامُ المنشورة | `render_claims.py --check` | «كل رقم معلن يطابق مصدره» ✓ |
| الأثرُ المختوم | `ls data/training/3e2d360a665c88aa \| grep -c '^pg-'` | **416 صفحة · لم تُمسّ** ✓ |
| الهويةُ الحيّة | `--verify` / `pack.identity` | `3e2d360a665c88aa` ثابت ✓ |
| بواباتُ المستودع | `qa_gate --full` · `static_gate` | **5/5** · `PASS` ✓ |
| الحرّاسُ الخمسة | `guard_audit.py` | **PASS** — لا أثرَ باقٍ (بصمة) ✓ |
| التكلفة | `cost_usd` في `pack.json` | **0** · والـ$3.36 لم تُمسّ ✓ |

## ٧) ما لم أفعله عن قصد · وما لم يُثبت

**لم يُعَد الالتقاط · ولم تُمسّ `data/training/` · ولا قرارَ صرف.** وبندُ الحكم `FAIL` في
`--build` باقٍ **باسمه**: التقاطعُ 133 صفحة ⇒ قرارُ المالك (تحريرُ 133 صفحة · 1,253 صفّاً ·
32.6٪ مقابل $0).

**ما لم يُثبت:** (١) أن كلَّ حارسٍ في المستودع خارجَ الخمسة محروس — **الخمسةُ مُثبتة والسوى
`مجهول`** · (٢) أن قائمتَي «17» و«2» قابلتان للاستخراج من القرص الحالي · (٣) أن الثمن ~32.6٪
يبقى لو تغيّرت **قاعدةُ بناء المديات** · (٤) أن الإحصاءَ 50 هو المختار، وأن ٣ مدياتٍ تكفي
اللجنة · (٥) أن بوابةَ المحتوى تصمد عند تقاطع نافذتين (`مُهيّأة` لا `مُختبَرة`) ·
(٦) أن الإفراجَ عن 133 صفحة لا يُسقط دليلًا (الخطةُ v2 تقول: التغطيةُ تبقى 200 — ولم أُنفّذ).
