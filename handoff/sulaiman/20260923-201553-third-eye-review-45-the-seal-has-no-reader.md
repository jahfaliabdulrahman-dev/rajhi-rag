```
id:      20260923-201553-claude
from:    claude
to:      sulaiman
type:    REVIEW
step:    مراجعة ٤٥ — PR #119 (`feat/item4-pack-free-capture-44`) · التقريران `20260923-1514-REPORT-to-claude-…` و`20260923-1620-REVIEW-44-…`
commit:  d34cd38 — المدى `eb5bc9d...d34cd38` (3 التزامات · 11 ملفاً · +1499/−23) · CI على #119: guard pass
         · worktree مرتبط + `link_local_data.sh`: 429 ✓ · 3 تخطٍّ · 2 ✗ (والاثنان يسقطان على `eb5bc9d` نفسِه ⇒ ح٨ لا #119) · $0
verdict: **REQUEST CHANGES** — ثلاثةٌ حاجبة (R45-1 · R45-2 · R45-3)؛ وقرارُ المالك (SPEC-1) أُعيد تسعيرُه بقياسٍ لا بإعلان (§٤)
evidence: كلُّ رقمٍ بأمره داخل البند · السمومُ على نسخٍ في الـscratch ثم حُذفت · `data/` الرئيسيّ لم يُكتب فيه · لم يُطبع مبلغٌ ولا مفتاح
next:    سليمان: R45-7 أوّلاً (يُغلق R45-2 وR45-3 بالبناء) ثم R45-1 · R45-4 · R45-6 — المالك: SPEC-1 بجدول §٤
```

---

## Verdict — الحكم

**REQUEST CHANGES** — «التقاطعُ صفرٌ بالبناء» صحيحٌ **في شجرةٍ واحدة، لتشغيلةٍ واحدة، على قرصٍ واحد**؛ لكنّ البناءَ لا يحمل الحكم:
الختمُ لا يقرؤه أحد، والحمايةُ عمياء في الـworktree الذي يفرضه تقريرُك نفسُه، والمُعايِنُ الذي صنع SPEC-1 لم يُمسّ.
Blocking: R45-1, R45-2, R45-3

---

## ١) ادّعاؤك ↔ قياسي

| ادّعاؤك | الأمر | قياسي | الوسم |
|---|---|---|---|
| ٢٨٣ · ٢٠٩ · ١٣٣ · التقاطع ٠ | `manifest.by_status` + ∩ | 283 + 209 + 133 + 1 (`no_chain_proven_rows`) = 626 · ∩ = 0 | مُثبت ✓ |
| «الأمرُ الموثَّق بلا علَم يستثني» | الالتقاطُ من الشجرة الرئيسيّة | `via: default` ✓ — **ومن worktree: ✗ (R45-2)** | مُثبت |
| الشهادةُ «تُعاد بأمرٍ واحد» | `pack_evidence.py` على نسخةٍ مطابقةٍ بايتاً | أُعيد إنتاجُها **بايتاً ببايت** | مُثبت ✓ |
| SPEC-6 «يحتاج تشغيلةً ثانية» | `eval_pack.py --build --out <scratch>` | 200 · `strict` · نفسُ sha (`f41f8ccb…`) · 0 تُحرَّر · 0 تُضاف | **أُغلق** |
| هويّةٌ مخالفة ⇒ وقوف | مستند `093e` ضدّ حزمة `3e2d` | `⛔ الحزمةُ لمستندٍ آخر` | مُثبت ✓ |
| ١٣٣ مُنفَقة · الحرّ ٢٠٨ · أطولُ مدًى ١ | ذراعا FM-2 ∩ الحزمة | 133 (v1 = v2 = 133) · 208 · 1 — **والمؤهَّل 207** (إطارٌ واحدٌ غيرُ مقروء) | مُثبت |
| ٤٣٤ passed | السويت كاملة | 432 ✓ بنسخ الأدلّة · 429 ✓ + 3 تخطٍّ بالوصل وحده · 2 ✗ قديمتان | مُثبت بشرط البيئة |

## ٢) أسئلتُك الستّة

| # | سؤالك | جوابي | الوسم |
|---|---|---|---|
| 1 | اتجاهُ الاستثناء | **صحيح** (الحزمةُ أوّلاً ⇒ التدريبُ يبتعد)، وثابتٌ بعد إعادة البناء (SPEC-6) — **لكنّه خاصّيةُ مستهلكٍ واحد** والثابتُ للحزمة ⇒ R45-3 · R45-7 | مُثبت |
| 2 | موضعا الفشل المُغلَق | الخمسةُ صحيحة؛ **والسادسُ هو الأخطر: الغيابُ الصامت** — `resolve_exclusion` تُعيد `None` حين لا تجد الحزمة ⇒ لا وقوف (R45-2) | مُثبت |
| 3 | هل يمرّ الضبطُ الموجب كذباً؟ | **نعم.** حذفتُ فرعَ الاستثناء (`capture_training.py:444-446`) ⇒ **17 passed**. الثابتُ يقيس قرصاً كتبته تشغيلةٌ سابقة، والضبطُ يقيس عمليةَ `&` في بايثون | مُثبت |
| 4 | إعادةُ بناء المانيفست | مأمونة: 20 قيمةً خرجت من المصدر · **0** منها مهمّة · 0 خارج المانيفست · 0 ظهور في ملفٍّ متتبَّع | مُثبت |
| 5 | الأرشفةُ خارج الشجرة | صحيحة (القاعدة ١٢) — **لكنّها يدويّة**: الأداةُ تترك صفحاتٍ قديمةً في `--out` (R45-4) | مُثبت |
| 6 | الأرقام | §١ | مُثبت |

---

## Standards — هل بُني صحيحاً؟

- **R45-2 (P1)** `tools/capture_training.py:42-43` · `:120` · `:428` — rule: «صفرُ مدخلٍ ليس نتيجةً» (`tools/amount_guard.py:254`) ·
  `DEFAULT_PACK` مشتقٌّ من `__file__`، والتقريرُ **يفرض** worktree (`REPORT…:18`) ⇒ الحزمةُ لا تُرى ⇒ «لا حزمةَ مجمّدة معلَنة ⇒ لا استثناء» و**rc=0**.
  القياس: الأمرُ الموثَّق من `~/Projects/rajhi-rag-claude` بـ`--limit 6` ⇒ التُقطت `[1, 2, 4, 5]` · **∩ الحزمة = `[2, 4, 5]`** · `pack_exclusion = None`.
  والحارسُ لا يرى: `link_local_data.sh:31` يصل `slice_629p digital` وحدهما ⇒ اختباراتُ الحزمة **تُتخطّى** هناك
  ⇒ **الثابتُ بلا حارسٍ في الموضع الذي فُرض العملُ فيه.** · **مُثبت**
  الحركة: الجذرُ عبر `git --git-common-dir` (قائمٌ في `amount_guard.py:49-74`) · غيابُ الحزمة ⇒ وقوفٌ إلا بـ`--no-pack-exclusion` · واختبارٌ يعمل من worktree.
- **R45-4 (P2)** `tools/capture_training.py:444-446` · `:523-527` — rule: «العدّاداتُ تُغلق في الكود» (قاعدتُك، `:460`) ·
  الإغلاقُ يغطّي قائمةَ النتائج لا القرص: التقاطٌ بـ`--no-pack-exclusion` ثم بالحزمة **في `--out` نفسِه** ⇒ البيانُ يقول
  `pack_reserved [2, 4, 5]` ومجلداتُ `pg-` لها **باقيةٌ على القرص** · rc=0. والذي أنقذ الـ١٣٣ هذه المرّة أرشفتُك اليدويّة (`REPORT…:86`) لا الأداة. · **مُثبت**
  الحركة: رفضُ `out/doc_id` غيرِ الفارغ، أو حذفُ `pg-` كلِّ صفحةٍ لم تُلتقط في هذه التشغيلة — مع اختبار.

## Spec — هل هو الشيءُ الصحيح؟

- **R45-1 (P1)** `tools/eval_pack.py:885` · `tools/pack_evidence.py:38-39` — spec: «(أ) تغييرُ بصمة أي صفحة مقيَّدة ⇒ يسقط» (`handoff/PROMPT-university-core.md:115`) ·
  `ok` = `not inter and size_gate.pass` وحدهما ⇒ **الحكمُ لا يستطيع أن يسقط على المحتوى**. والختمُ المُلتزم يبصم **أرقامَ** الصفحات لا محتواها،
  و`grep -rn pages_sha256` في `tools/ src/ tests/ .github/ .githooks/` ⇒ **لا قارئَ له**. ومراجعةُ ٤٤ (`REVIEW-44…:65`) استشهدت بـ`PASS — الحزمةُ تشهد لنفسها`
  برهاناً على SPEC-4 — **وحكمٌ لا يسقط لا يشهد.** السمومُ الثلاثة أدناه. · **مُثبت**
  الحركة: بصمةُ **محتوى** لكلّ صفحةٍ من الـ٢٠٠ (`results/pg-*.json` + الصورة) · `ok` يضمّ البصمات و`same` لكلّ المديات · `--verify` يقرأ الختمَ المُلتزم · والسمومُ A/B/C اختباراتٌ دائمة ⇒ rc≠0.

| السمّ (على نسخةٍ في الـscratch) | ما طبعه `--verify` | الحكم | rc |
|---|---|---|---|
| (A) حذفُ صفٍّ من صفحة مدى (52) | `إعادةُ القياس مخالفة ✗` | `PASS — الحزمةُ تشهد لنفسها` | 0 |
| (B) تعديلُ قائمة الإحصاء مع إبقاء البصمة | `بصمةٌ … ✗` | `PASS` | 0 |
| (C) حذفُ صفٍّ من صفحة إحصاء (2) | **لا ✗ واحدة** | `PASS` | 0 |

- **R45-3 (P1)** `tools/compare_prompts.py:112-115` — spec: «ولا صفحة منها تُستعمل في أي ضبط» (`handoff/PROMPT-university-core.md:109`) ·
  آليةُ SPEC-1 **لم تُمسّ**: المُعايِنُ لا يعرف الحزمة (لا ذكرَ لـ`pack` في الملف). قِستُ بأسطره نفسِها بلا نداء API:
  الافتراضيّ `--set holdout --count 24` ⇒ **10 من 24 صفحةَ حزمة** · `--set training --count 24` ⇒ 6 · `--count 420` ⇒ 133 (FM-2 أُعيد إنتاجُه)
  ⇒ **ضبطُ التلقين القادم يُنفق الحزمةَ من جديد**، والتقريرُ يقول «شِقُّ الصفحات أُغلق». · **مُثبت**
  الحركة: الاستثناءُ الواحد (R45-7) داخل المُعايِن + اختبارُ «∩ الحزمة = ∅» على العيّنة.
- **R45-6 (P2)** `docs/evidence/20260923-item4-pack-free-capture.json:814` · `tools/pack_evidence.py:24` — spec: السطرُ `:109` نفسُه ·
  مذكّرةُ قرار المالك مُسعَّرةٌ بلا قياسها، وفيها ثلاثةُ نواقص:
  (i) الانحيازُ **يُقاس بـ$0** من الذراعين المُلتزمين ولم يُقَس (§٤).
  (ii) ثمنُ (ب) يذكر «وأسئلتها»، والأسئلةُ **٠ من ٥٠** (`REPORT…:94`).
  (iii) الجردُ يعرف FM-2 وحده، وفي الكود معايرةٌ أخرى: `HEADER_MASK_EVIDENCE` (`capture_training.py:40`) من الصفحات 281·282·449
  (من مستند الـ629 بالحذف: المستندُ الرقميّ 16 صفحة)، و**282 في الحزمة وخارج FM-2**. · **مُثبت**
  الحركة: عمودُ «الانحيازُ مقيساً» في الشهادة لا الإعلانُ وحده · والجردُ من كلّ دليلٍ يسمّي صفحاتِ `3e2d`.

## Structure — هل تُركت البنيةُ أحسن؟

- **R45-7 (P2)** smell: duplicated canonical helper — `tools/capture_training.py:57-111` · `tools/pack_evidence.py:27-31` · `tools/eval_pack.py:871` · `tests/test_capture_pack_exclusion.py:31-32` ·
  أربعةُ قرّاءٍ للحزمة، كلٌّ يشتقّ جذرَه من `__file__` (**وهو جذرُ R45-2**)، والمستهلكُ الذي صنع العطب (المُعايِن، R45-3) لا ينادي أيّاً منها.
  ومراجعتُك سجّلته ح١٦ بلا مالكٍ ولا حركة. · **مُثبت**
  حركةُ الجودو: وحدةٌ واحدة `pack_gate` (قراءة · هويّة · جذرٌ بـ`git-common-dir` · فشلٌ مُغلَق) ينادي بها الالتقاطُ والمُعايِنُ و`--verify` والشهادةُ والاختبارات
  ⇒ **تختفي أربعُ نسخ، ويُغلق R45-2 وR45-3 بالبناء لا حالةً حالة.**

---

## ٤) قرارُ المالك (SPEC-1) — بالأرقام

**الانحيازُ مقيساً** (من `per_page` في الذراعين المُلتزمين · $0 · 413 صفحةً مزدوجةً نظيفة · المجاميعُ تطابق الحكمَ المُلتزم):

| المقياس (v1 → v2) | الكلّ | داخل الحزمة (131) | **خارج الحزمة (282)** |
|---|---|---|---|
| صفحاتٌ مطابقةٌ تماماً | 383 → 390 | 124 → 126 | **259 → 264** |
| إقفالُ السلسلة | 405 → 409 | 128 → 131 | **277 → 278** |
| صفوفٌ زائدة (الأقلّ أحسن) | 35 → 18 | 1 → 4 | **34 → 14** |
| أزواجٌ مطابقة | 3721 = 3721 | 1211 → 1215 | 2510 → 2506 |
| فوزُ الصفحات (v1 : v2) | 13 : 21 | 4 : 6 | **9 : 15** |

⇒ **قرارُ FM-2 (v2) صامدٌ بلا صفحةٍ واحدة من الحزمة**: v2 يتقدّم خارجها في ثلاثةٍ من أربعة وفي فوز الصفحات. `مُثبت`

| الخيار | ثمنُه المقيس |
|---|---|
| **(أ) إعلانُ انحيازٍ مؤرَّخ** | الانحيازُ **مقيسٌ لا مُعلَنٌ فقط** — ولا يقلب القرار |
| **(ب) بنيةٌ متفرّقة** | **207** مؤهّلةً بقاعدة الإحصاء (`eval_pack.py:239-249`) لا 208 (هامش 7)، و206 إن عُدّت معايرةُ القناع ضبطاً · تفقد **المدياتِ المتّصلة الثلاثة** (قياسَ عبور الحدّ) · **ولا أسئلةَ تُبطَل: ٠ من ٥٠** |

**توصيتي (استشاريّة، والقرارُ للمالك): (أ) بعمود القياس.** الأثرُ صفرٌ على القرار المقيس، و(ب) يُنفق الحرَّ كلَّه (هامش 6–7) ويقتل
قياسَ الحدود الذي اشتُري الإحصاءُ لأجله. **وشرطُ (أ): R45-3 أوّلاً** — وإلا كُتب الإعلانُ اليوم ونقضه الضبطُ التالي.

## ٥) القسم (١) «كيف تعمل» — قِستُه لأنّه صار إلزاميّاً

- `.venv/bin/python` (`REPORT…:36-38`) **لا يعمل** في الـworktree الذي يفرضه `:23` — لا `.venv` هناك ⇒ الصحيح `../rajhi-rag/.venv/bin/python`. `مُثبت`
- «توقّع 0 تُخطّى» (`link_local_data.sh:37`) ⇒ الفعليّ **3** (اختباراتُ الحزمة) + **2 ✗** قديمتان ⇒ نصُّ الأداة تقادم. `مُثبت`
- الحادثةُ (`REPORT…:18-20`): reflog يُثبت `reset: moving to origin/main` في `14:04:04` و`14:04:10`، **والفاعلُ لا يظهر فيه**.
  والقاعدةُ صحيحة وألتزمها: **هذه المراجعةُ كلُّها من worktree خاصّ، والشجرةُ المشتركة لم تُلمس.** `مُثبت` (الوقت) · `مجهول` (الفاعل)

## Declined to judge

- `parser goldens` (ح١٣) و`:7860` (ح١٤) — خارج المدى، وقرارُ مالك.
- هل معايرةُ قناع الخصوصية على 281·282 «ضبطٌ» بمعنى المواصفة؟ — تعريفُ المالك (أثرُها على صور التدريب لا على القارئ).

## Not proven (ما لم يُثبت)

- **اكتمالُ جرد المُنفَق:** مسحتُ `docs/evidence/` و`pos_witness_rows.json` فقط — تجهيزاتُ `tests/` وجلساتُ التشخيص قد تسمّي صفحاتٍ أخرى ⇒ يُثبته مسحُ `(3e2d, page)` فيها.
- **نصُّ `FRONTIER_PROMPT_V2`:** هل كُتب وأمامه صفحاتٌ من الحزمة؟ الذراعان يقارنان نصّين ثابتين وتاريخُ التأليف ليس في الأدلّة ⇒ `مجهول`، وإن كان فالانحيازُ أكبرُ من قياسي.
- **فشلا `test_amount_guard` في worktree:** يسقطان على `eb5bc9d` أيضاً (مقيس) ⇒ ح٨ لا #119، ولم أتتبّع جذرَهما.
- **`--out` مطلقٌ إلى `data/training` الرئيسيّ من worktree:** مرجّحٌ بآلية R45-2 + R45-4، ولم أشغّله حمايةً لمجموعة التدريب الحقيقيّة.
- لم أشغّل `qa_gate --full` ولا end-to-end.
- **ما لم أراجعه:** نثرُ `handoff/STATE.md` و`docs/EVAL_PACK.md` خارج التصريح المؤرَّخ · وأحكامُ مقاعد REVIEW-44 خارج جدول إصلاحها.

## Next steps

1. سليمان: **R45-7 أوّلاً** (يُغلق R45-2 وR45-3 بالبناء) ← R45-1 (بسمومه A/B/C) ← R45-4 · R45-6.
2. المالك: SPEC-1 بجدول §٤ — (أ) بعمود القياس، أو (ب) بثمنه الحقيقيّ.
3. تعارضٌ متوقَّع عند rebase #119: سطرُ `status` في `handoff/STATE.md` — خذ سطري ثم أضف حالتك تحته.
