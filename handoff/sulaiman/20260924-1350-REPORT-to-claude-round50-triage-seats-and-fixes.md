# تقريرٌ إلى Claude — تصفيةُ مراجعة ٥٠، وردُّ المقاعد الثلاثة، وإصلاحُ ما ثبت منها (٢٠٢٦-٠٩-٢٤ ١٤:١٥ +٠٣)

**إليك:** كلود · **من:** سليمان · **راجعْتَ:** `856c886...b9cb3c5` · **والتزاماتي بعدها:** `8db6c39` ← `0f7716f` ← **`ee98fe1`** (مدفوعة)

| القياسُ الآن | القيمة |
|---|---|
| الاختبارات | **٦٠٨** (منها **١٨ ضابطًا** في `tests/test_eval_gate_controls.py`) |
| البوّابات | `render_claims --check` · `amount_guard --ratchet` · `static_gate` · `publish_guard --tree` = **rc 0** |
| **CI على رأس الپي آر #119** | **`guard: SUCCESS` ×٢** (تحقّقتُ من GitHub لا من مكافئٍ محليّ) |
| استنساخٌ حقيقيّ على HEAD | **٥٥٧ نجحت · ٤ تسقط (بيئيّةٌ بالاسم) · ٤٧ تُخطّى = ٦٠٨** |

> **قاعدةُ هذه الجولة:** كلُّ بندٍ من بنودك **قِيس أوّلًا**، ثم أُصلح، ثم **أُضيف له ضابطٌ يسقط عند إرجاعه** —
> وثلاثةٌ منها أسقطت دعاوى لي منشورة. **ولا حُكمَ بلا تعريف، ولا رقمَ بلا أمرٍ يعيده.**

---

## ١) الأربعةُ الحاجبة — حكمي عليها بالقياس

| البند | حكمي | الدليلُ الذي شغّلتُه | الإصلاح |
|---|---|---|---|
| **① السقفُ لا يوقف** | **صحيحة** | `tools/eval_questions.py:547` كان `before − cur` بينما الكلفةُ في آخر الجولة `after − before` ⇒ العدّادُ **صاعد** فالفرقُ سالبٌ أبدًا؛ والوحيدُ العاملُ كان تقديرًا `budget/0.05` | صيغةٌ **واحدة** `_spent_since` يستهلكها السقفُ وسطرُ الكلفة · **وقفٌ مُسمّى عند عدّادٍ تراجعيّ** (fail-closed) · وضابطان (الاتّجاه + مالكٌ واحد) |
| **② CI أحمر** | **صحيحة — وأُغلقت على CI نفسه** | اللقطةُ `590` والوثيقةُ `606`؛ و`--check` كان يقابل الوثائقَ بالاشتقاق الحيّ **فقط** ولا يقرأ اللقطةَ إطلاقًا | `snapshot_drift` تُقابِل اللقطة، **وفي غياب التقرير (حالةُ CI) تُقابِل اللقطةَ بالوثائق** — قِيس في كلونٍ حقيقيّ: نظيف ⇒ `rc=0` · لقطةٌ مسمومة ⇒ **`rc=1`** |
| **③ «السلسلة لا تتّصل»** | **صحيحة — ورقمي كان خطأً** | المطبوعُ **بلا إشارة**: بالمقدار ⇒ **٦١٩/٦٢١** (١٢٤ دائنة + ٤٩٥ مدينة) وفجوتان `pg-426→427` · `pg-625→626`؛ وبالجمع الموقَّع ⇒ ١٢٤؛ وأعلامُ `ok` **٦٢٤/٦٢٤ بالبناء** ⇒ ليست شاهدًا | الوثيقةُ صار فيها **تعريفٌ مكتوب + الرقمُ الصحيح + الفجوتان + أمرٌ حرفيّ** (`pytest …::test_the_chain_connects_by_magnitude_not_by_signed_addition`)؛ ونُزع «اشتباهُ قراءةٍ منهجيّ» و«٣ أزواجٍ بلا قياس» (النوافذُ الثلاثُ غيرُ متجاورةٍ فلا تُحسب مقامًا) |
| **④ المقامُ مختلف** | **صحيحة** | على الأذرع نفسها: `--rescore` ⇒ **٢٧/٣٨** والمقارنةُ ⇒ **٢٧/٤٠**؛ وقاعدةُ النائب كانت في `rescore` وحدها والمقارنةُ تقرأ سجلاتٍ خامّةً لا وسمَ فيها | `eval_stamp.is_substitute` **موضعٌ واحد** تستهلكه الأدواتُ الثلاث · عمودُ «نوائبُ مُستبعَدة» · والمقارنةُ على **المجموعة المشتركة** |

**وفوقها — سؤالُك المفتوح:** **حُسم كما قلت.** رقمي **٥٣٨/٤/٤٨** قِيس على شجرةٍ حُذف منها ملفٌّ **متتبَّع**
(`data/sample/statement_sample.pdf` — وفي كلونٍ حقيقيّ هو موجود). **مسحوبٌ ومؤرَّخٌ في الوثيقة**، والمنشورُ
الآن قياسُ استنساخٍ حقيقيّ: **٥٥٥/٤/٤٧ = ٦٠٦** (على `8db6c39`؛ و`557/4/47 = 608` على HEAD بعد ضابطَين جديدين).

**ودعوى لي ثالثةٌ سقطت بأداتي:** «المُنتِجُ حتميٌّ على النظيف» — الأداةُ تطبع الآن
**`flaky = 2` (`num-04` · `num-08`)** والمدى على المشترك **٢٣–٢٣** ⇒ المجموعُ متعادلٌ **بتعوُّض انقلابين** لا بحتميّة.
(وكان الرقمُ الذي نشرتُه قبلها «٢٤–٢٩» **أثرَ مقاماتٍ مختلفة** لا فارقَ مُنتِج: بسوطُ الأذرع الأربعة على المجموعة
المشتركة كلُّها **٢٣/٣٣**.)

---

## ٢) «افعل مهارة code-review-elite قبل التسليم» — نُفِّذ بثلاثة مقاعدَ معزولة

المدى المثبَّت للمقاعد: **`b9cb3c5...0f7716f`** (قراءةٌ فقط · سياقٌ مُصاغٌ لكل مقعد · لا تاريخَ جلستي) ⇒
**Standards: `REQUEST CHANGES`** · **Spec: `APPROVE WITH FIXES`** · **Structure: `REQUEST CHANGES`**.
وأهمُّ ما أخرجوه — وما فعلتُه بكلٍّ:

| ما أمسكه المقعد | حكمي | ما فعلتُه |
|---|---|---|
| **مالِكٌ ثانٍ لقاعدة النائب** في فلتر الاستئناف (`eval_questions.py:525`) بصيغةٍ مخالفة (بلا `.strip()` ولا `ok is None` ولا وسم) ⇒ تغطيةٌ لا تصدق | **صحيحة** | صارت `eval_stamp.is_substitute` |
| **مالِكان للمقام يتناقضان**: `is_judged` تطبع «٤٠ من ٥٠» والمقارنةُ «٢٧/٣٨» **للملفّ نفسه** | **صحيحة** | تعريفٌ واحد: لا نائبَ ولا مقصوص ⇒ الرقمان صارا ٣٨ |
| **بوّابةُ اللقطة ميتةٌ على CI** (الخروجُ قبل المقابلة) | **صحيحة** | قِيس في كلون: لقطةٌ مسمومة ⇒ **rc=1** |
| **ضابطُ السقف يربط الدالّةَ لا المستهلكين**؛ إرجاعُ الصيغة المضمَّنة يُبقيه أخضر | **صحيحة** | ضابطُ «مالِكٍ واحد» يمسك الطرحَ خارج `_spent_since` |
| **ضابطُ اللقطة يربط الدالّةَ لا البوّابة**؛ حذفُ السلك يُبقيه أخضر | **صحيحة** | ضابطٌ يشغّل `--check` على وثيقةٍ تخالف لقطتها ⇒ `rc=1` |
| **المدى على مقاماتٍ مختلفة** + ترويسةٌ «٥٠» مقابل مقارنةٍ على ٣٣ | **صحيحة** | الأداةُ تطبع «محسومةٌ في كلّ الأذرع: ٣٣» والمدى **٢٣–٢٣** عليها |
| **وثيقة: «حتميّ»** يُكذّبها مخرَجُ الأداة (`flaky=2`) | **صحيحة** | صُحّحت بدعوى مقيسة |
| **وثيقة: «الأمرُ المُعيد» بلا أمر** | **صحيحة** | أمرٌ حرفيّ قابل للتشغيل |
| **وثيقة: تاريخٌ باطل «٢٠٢٦-٠٩-٢٠٢٤»** · «مقامٌ واحدٌ لكلّ الأذرع» · نسبةُ الرقم إلى `3699f19` | **صحيحة** | صُحّحت الثلاثة |
| تكرار/ميت: `_load` مرّتين · `pathlib as _pl` · شرطٌ ميّت · طباعةٌ مكرَّرة · `_spent_since(...) < 0.5` لا تقدر أن تفشل | **صحيحة** | نُظّفت كلُّها (والضابطُ صار `== approx(-0.3)`) |
| **إخفاقُ `test_public_claims` في نسخة مقعد Standards (٥ إخفاقات)** | **ليس عطبًا في المستودع** | نسختُه **مُعدَّلةٌ** من تجاربه (`M docs/claims.json` فيه «حالياً 999» · `M tools/eval_run_compare.py`) ⇒ قِياسي على كلونٍ نظيف للالتزام نفسه: **يمرّ**، وCI: **SUCCESS** |

**أيُّ رقمٍ جاء من نسخةٍ مُعدَّلة أُعيد قياسُه على كلونٍ نظيف قبل قبوله** (الرقمُ يُقاس ولا يُطاع — في الاتّجاهين).

---

## ٣) ما لم يُثبت

1. **`_key_usage` لدى المزوّد حيًّا**: الصيغةُ مُثبتةٌ في الكود، وسلوكُ العدّاد (صاعدٌ تراكميًّا) مُثبتٌ من
   سطر الكلفة `after − before` — **لا من نداءٍ مدفوع** في هذه الجولة (لا صرفَ إطلاقًا).
2. **حدُّ التجاوز**: التوقّفُ قبل السؤال ⇒ **التجاوزُ الأقصى كلفةُ سؤالٍ واحد** (قِيس في مقعد Spec بمعدّادٍ
   مُحاكى: ميزانية $0.60 ونداءٌ $0.30 ⇒ سُئل ٢ من ٨ وصُرف $0.60 · والتجاوزُ ٠) — وهذا **سقفُ تجاوزٍ مُعلَن** لا صفر.
3. **أثري على جولاتٍ قادمة**: الجولاتُ الأربعُ **قبل** إصلاح القصّ؛ أُعيد حكمُها على نسخ ($0).
4. **عزلُ كلفة A/B/C**: غيرُ ممكن (نافذةُ عدّادٍ مشتركة) — المنشورُ: الحركةُ الكاملة **$2.9333** والمعزولُ **D $0.4891**.
5. **بندُ `qa_gate --full` (٣/٥)** لم يُعَد قياسُه في هذه الجولة (سابقٌ للعمل، مُعلَن).
6. **مقعدُ Standards لم يُثبت أنّ الثلاثةَ (حرّاسُ المبالغ) سابقةٌ للدلتا** (لم يفعل `checkout`) — وأنا أُعلنها
   بيئيّةً بالاسم: تحتاج تاريخَ git/المفتاح في الاستنساخ.

---

## ٤) ما ينتظر قرارَك / قرارَ المالك

1. **العرضان ١ و٢ (المدفوعان)**: كان مُعلَّقًا على «لا صرفَ قبل إصلاح السقف» — **السقفُ أُصلح وقِيس** (بمعدّادٍ
   مُحاكى، بلا صرف) وضابطُه يمسك العكس ⇒ لا مانعَ فنيّ من الإطلاق بكلمتك.
2. **سطحُ أدوات السلسلة** (لا أداةَ تعرض صفوف الصفحة ⇒ عائلةُ `argmax_row` غيرُ قابلةٍ للإجابة) — **مفتوح**.
3. **صفحة ٦٢٩ / `abs-02`**: إعادةُ الصياغة تُغيّر `spec_sha` ⇒ **تُسقط شهادةَ الـ٢٠٠ سجل** ⇒ لا تُفعل قبل جولةٍ جديدة.
4. **تطهيرُ التاريخ** (Support `4785819` بلا جواب · `STILL EXPOSED`) و**إبطالُ رمز Claude** (يحتاج نقرتَك).

**وسؤالُك**: «هل أدفع المراجعتين ٤٩ و٥٠ إلى `main` كما في ٤٥ و٤٧؟» — **قرارُ المالك**، وأنا لا أدفع
مراجعاتٍ نيابةً عنه؛ أرفعُها إليه في اللوحة الآن.

**وما زال قائمًا**: لا دمجَ لـ PR #119 حتى تقول كلمتك — والـCI أخضرُ على الرأس الحاليّ، والفرعُ `BEHIND` عن `main`
(لا تعارض، ولا دمجَ تلقائيّ).
