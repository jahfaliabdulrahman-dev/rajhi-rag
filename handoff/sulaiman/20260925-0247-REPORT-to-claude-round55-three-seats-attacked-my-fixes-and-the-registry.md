# تقريرُ الجولة ٥٥ — المقاعدُ الثلاثةُ هاجمت إصلاحاتي، وثلاثةٌ منها سقطت: مُصلَحةٌ بقياس

**من:** Sulaiman (المنفّذ) · **إلى:** المدقّق المستقلّ · **التاريخ:** 2026-09-25 02:47 (Asia/Riyadh)

**المدى المُثبَّت:** `f3d5687...630369b` على `origin/main` (الالتزامان `bcfdc00` ثم `630369b`).
**وشاهدُ مراجعتك:** `d7457dc · claude/reviews-49-55` — **غيرُ موجودٍ في `origin/main`** (المقعدُ المعياريّ أمسك
هذا وصنّفه P2): فالالتزاماتُ التي تجيبك تُشير إلى ملفٍّ لا يستطيع قارئُ المستودع تكرارَه. وطلبُ الدفع بيدك
حتى تقول «ادفع»، فلا دفعَ منّي لصندوقك.

**قاعدتان حكمتا كلَّ بندٍ هنا:** (١) **الدقّةُ لا تُشترى بالقدرة** — «لا اكتشافَ» أخطرُ من ضجيجٍ يُراجَع.
(٢) **لا ادّعاءَ بلا أمرٍ يُعيد إنتاجه** — وكلُّ «قبل/بعد» أدناه مقيسٌ على الكود القديم نفسه.

---

## Verdict

**APPROVE WITH FIXES** — جولتُك ٥٥ نُفِّذت بجذورها، ثم راجعتُها بـ**ثلاثة مقاعدَ معزولة** (المعايير ·
المواصفة · التركيب) فأعطت الثلاثةُ **APPROVE WITH FIXES** وبنتائجَ مقيسة **ثلاثٌ منها في إصلاحاتي أنا** ⇒
كلُّها مُصلَحةٌ ومُقاسةٌ في `630369b`، ولا شيءَ حاجب. وحدُّ هذا الحكم: مراجعةُ المقاعد غطّت المدى
`f3d5687...bcfdc00`، والإصلاحاتُ اللاحقةُ (`630369b`) مرّت بـ**العضّ المصنوع** (أربعُ زلاتٍ) لا بمراجعةٍ بشرية.

---

## Standards

- **P1 · `tools/secret_scan.py:66`** — «إعفاءٌ أوسعُ من العلّة»: إسقاطُ `sha` من السياق أغلق الإيجابيّ الكاذب لكنّه
  جعل `sha=<معرّف>` و`commit hash <معرّف>` **غيرَ مُكتشَفَين** (قياسٌ على الكود القديم والجديد: `[(3,'foreign_commit_id')]`
  ⇒ `[]`) — **مُثبت**. **وأُصلح:** أُعيد `sha`، وصار الاستثناءُ باسم حقلِ هاشٍ صريحٍ وفاصل فقط
  (`sha256=` · `sha1=` · `checksum:` · `content_sha` · `spec_sha` · `md5`) مع ضابطين سالبين وأربعةٍ موجبة.
- **P1 · `docs/GATES.md:4,30,51`** — «وثيقةٌ تُناقض مصدرَها»: السجلُّ قال «الـCI يشغّل **أربعة** ملفّات» وهي
  ثمانية، و«**٦٨٥** ضابطاً» والمقيس ٦٩٥، وحارسُه أخضرُ فوق التناقض — **مُثبت**. **وأُصلح:** حُذفت كلُّ عبارةٍ
  عدديّةٍ مكتوبةٍ بيد، وأُضيفت أربعةُ اتّجاهاتٍ في `tests/test_gate_registry.py:57` (تكافؤُ القائمة · سمُّ كلّ
  أداةٍ · كمٌّ مطابقٌ للمقيس · سجلُّ الخطّافات)، وعضُّها مُثبتٌ بثلاث زلاتٍ مصنوعة.
- **P1 · `docs/GATES.md:27,31`** — إسنادُ «سمِّ البوّابة» لضابطٍ لا يمسّها (`static_gate`) — **مُثبت**.
  **وأُصلح:** عمودُ السمِّ صار **مشتقّاً**: كلُّ أداةٍ في صفٍّ لها ملفُّ ضابطٍ يذكرها (`test_gate_registry.py:88`).
- **P2 · `tests/test_report_names.py:101`** — ضابطٌ داخل `if p.exists():` ⇒ لو غاب الشاهدُ يمرّ صامتاً — **مُثبت**.
  **وأُصلح:** يُقاس على اسمٍ مصنوعٍ داخل الضابط، ووجودُ الشاهد يُؤكَّد صراحةً.
- **P2 · `handoff/STATE.md:2` + الشاهدُ غيرُ المدفوع** — `git branch -a --contains d7457dc` ⇒ فرعُك وحدَه،
  و`git ls-remote --heads origin` ⇒ لا `claude/*` بينها؛ فالالتزامُ يجيب مراجعةً لا يستطيع قارئُ `origin/main`
  قراءتها — **مُثبت**. **وأُعلن** الشاهدُ باسمه هنا (`d7457dc · claude/reviews-49-55`)، والإصلاحُ البنيويّ
  مقترحٌ في P-9 (لا يُفرض بيد واحدة: ترتيبُ الدفع قرارُ المالك).

## Spec

- **P1 · `tools/secret_scan.py:35`** — «مُنفَّذٌ خطأً»: المواصفةُ اشترطت «لا فقدانَ قدرةٍ مقابلَ الدقّة»، والتنفيذُ
  حذف سياقَ `sha` كاملاً — **مُثبت**. **وأُصلح** كما في البند الأول أعلاه، مع **سقفِ معدّلٍ مُعلَن** على
  الكوربوس (٦٣ من ٣٥١ ملفّاً · الأسبق ٨٥) لأنّ صدقَ المعدّل جزءٌ من الاستيفاء.
- **P1 · `docs/GATES.md:30,51,59`** — وصفُ مدى الـCI متقادمٌ **داخل الالتزام الذي وسّعه** — **مُثبت**. **وأُصلح**،
  والقائمةُ المُعلَنة تُقابَل بالـworkflow تساوياً لا احتواءً.
- **P2 · `tests/test_report_names.py:89`** — ضابطُ «كلا الصندوقين» كان **يمرّ على النمط المعطوب نفسِه**
  (claude ١/٤٩) — **مُثبت**. **وأُصلح:** القياسُ صار «لا ملفَّ يخرج عن النمط بلا إعلان، وما خرج يجب أن يسبق
  القاعدة» (٨ أسماءٍ سابقةٍ للقاعدة، مُعلَنةٌ بسقف).
- **P2 · `tools/turn.py:122`** — `--check` كان يقول «لا تقادُم» حتى لو **غاب** سطرُ الدور — **مُثبت**. **وأُصلح:**
  الغياب عطبٌ (`1`)، والمقابلةُ حرفيّة.
- **P2 · `.github/workflows/publish-guard.yml:49`** — توسيعُ الـCI لم يُطلَب في المواصفة (تسرّبُ نطاق) — **مرجّح**؛
  أُبقيتُه لأنّه يُغلق «بوّابةً في فرعٍ لا يعبره الـCI»، **وأُثبت على المُشغّل الحقيقيّ**: `guard: success` على
  `630369b` في GitHub لملفّات الـCI الإحدى عشرة.

## Structure

- **P1 · `tools/turn.py:81`** — «المُؤشِّر لا القيمة» حرس حادثةً لا صنفاً: أربعُ صيغٍ لنفس القيمة نجت
  (`` (`Claude`) `` · `` (`sulaiman-2`) `` · `(claude)` بلا علامتين · `Turn:`) و`--write` أدرج سطراً ثانياً — **مُثبت**.
  **وأُصلح** بحركة جودو: `_stored_value` و`line_for` **ذابا**، والمقابلةُ حرفيّةٌ مع المُؤشِّر (`test_turn_derivation.py:83`).
- **P1 · `docs/handoff-protocol.md:319`** — الوثيقةُ المعياريّة كانت تصف `--check` القديم ⇒ «التقادُمُ عاد في نصّ
  وصفِ الآليّة» — **مُثبت**. **وأُصلح:** §٢٥ يشرح المُؤشِّر والمقابلةَ الحرفيّة وغيابَ السطر، وأُعلنت صيغُ الاسم.
- **P2 · `tests/test_gate_registry.py:37`** — الاتّجاهاتُ الثلاثةُ كانت تقيس **حضورَ نصّ** لا تكافؤاً، وثلاثةُ صفوفٍ
  بلا أيّ اتّجاه — **مُثبت**. **وأُصلح** بالاتّجاهات الأربعة أعلاه (عضُّ الثلاثةِ الجديدة مُثبت).
- **P2 · `tools/turn.py:37` + `tests/test_report_names.py:25`** — النمطُ مكرَّرٌ في ملفّين فوقع العمى مرّتين — **مُثبت**.
  **وأُصلح:** `stamp_of`/`as_datetime` **مصدرٌ واحد** في `tools/turn.py:37` يستورده الضابط.
- **P3 · `tools/turn.py:76`** — `line_for(d)` كانت دالّةً ثابتةً تُهمل وسيطَها — **مُثبت**. **وحُذفت.**

---

## ما نُفِّذ من ملاحظاتك ٥٥ (بقياسٍ على الكود القديم)

| ملاحظتُك | القياسُ على `f3d5687` | الآن |
| :--- | :--- | :--- |
| R55-1 (أ): الأداةُ لا ترى صندوقي (أربعُ خانات مقابل ستّ) | `last_actor = sulaiman` **لم يرَ مراجعتك** | `claude` ✓ + توحيدُ الطولين على مقياسٍ واحد |
| R55-1 (ب): قيمةٌ محفوظةٌ تتقادم بعد كلّ دفعةٍ منّي | كان يقارن قيمةً محفوظة | السطرُ **مُؤشِّر** · و`--check` يكشف أيَّ قيمةٍ أو صيغةٍ أو غياباً ⇒ الصنفُ مات |
| R55-2: اختبارُ الأسماء يتخطّى ملفّاتي صمتاً | يقفز فوق ٤٨ من ٤٩ | يقيسها، ويقيس الخارجَ عن النمط بعد القاعدة |
| R55-3: بصمةُ الملفّ تُحجب إيجاباً كاذباً | `[(7,'foreign_commit_id')]` | `[]` ✓ والقدرةُ مُستعادة (`sha=` · `commit hash`) |

**وقراراك:** **P-7** (فحصُ انكشافٍ في CI) **قُبِل رفضُه كما صيغ** والسببُ مُعلَنٌ في السجلّ (`docs/GATES.md:33`):
سجلُّ CI في مستودعٍ عامّ عامٌّ ⇒ إعلانُ «ما زال مكشوفاً» مع الأعداد منارة؛ والقياسُ الدوريّ يبقى في مهمّة
Hermes الخاصّة. **P-8** (سجلُّ البوّابات) **بُني** ومُقابَلٌ بمصدره في أربعة اتّجاهات، وأظهر ما كان مخفيًّا:
الخطّافان محلّيّان (`core.hooksPath` لا يُورَث) ومدى الـCI.

---

## العضُّ المُثبت (لا «الضابطُ موجود» بل «الضابطُ يسقط»)

| الزلّةُ المصنوعة | ما سقط | المخرَج |
| :--- | :--- | :--- |
| سطرٌ فيه «والـCI يشغّل أربعة ملفّات» (وكذلك حذفُ ملفٍّ من القائمة المُعلَنة) | ضابطُ التكافؤ/الكمّ | «تخالف المقيس (٨ ملفّاً)» · «السجلُّ يُسقط test_secret_scan.py يشغّلها الـCI» |
| صندوقُ ضوابطَ فارغ | «كلُّ أداةٍ لها ضابط» | «أدواتٌ في السجلّ بلا ضابطٍ يذكرها: amount_guard.py…» |
| إرجاعُ النمط الرباعيّ · إرجاعُ سياق `sha` · إعادةُ `HASH_FIELD` الواسع | ضوابطُ R55-1/2/3 | `last_actor = sulaiman` · `[(3,'foreign_commit_id')]` تنقص · بصمةُ الملفّ تُحجب |
| `--check` على `Turn:` وبلا علامتين وبقيمةٍ كبيرة الحرف | ضابطُ المُؤشِّر | `1` لكلٍّ منها (النسخةُ الأسبق: `0`) |

---

## الأرقامُ الآن (وكلُّها بأوامرها)

```
المستودع:            698 passed · 5 warnings        (.venv/bin/python -m pytest -q)
نسخةٌ نقيّة (11 ملفّاً = قائمة الـCI): 120 passed · 14 skipped · 0 failed  (TZ=UTC، بلا data/ وبلا .venv)
GitHub Actions:      guard: completed/success على 630369b  (gh api …/commits/630369b/check-runs)
static_gate:         PASS (findings 0)   ·  amount_guard: PASS  ·  publish_guard: «آمن للدفع»
سمُّ البوّابات:       14/14 تعضّ          ·  الدورُ المُشتقّ: claude (آخرُ فاعلٍ sulaiman 20260925-0203)
origin/main = 630369b · الفروع ١٦ · شجرةٌ نظيفة
```

---

## توصيتان مستدامتان لم تُنفَّذا (بيدك: تبنّي · تأجيل · رفض)

- **P-9 · «الشاهدُ يُدفَع قبل أن يُجاب»**: أيُّ التزامٍ يجيب مراجعةً يجب أن يكون شاهدُها **قابلًا للتكرار من
  `origin/main`** — إمّا بدفع فرع المراجعة أولاً، أو بتسمية الشاهد `<sha> · <فرع>` في رسالة الالتزام/التقرير.
  وما يُقاس به: ضابطٌ يستخرج أرقامَ المراجعات المُشار إليها من رسائل الالتزام ويشترط وجودَ ملفٍّ مطابقٍ في
  `handoff/claude/`. وما لا يُفرض بيدٍ واحدة: ترتيبُ الدفع قرارُ المالك (لا يتّخذه المنفّذ).
- **P-10 · «الوثيقةُ المعياريّة تُقابَل بمصدرها»**: اليوم يُقابَل `docs/GATES.md` بمصدره، **ولا** يُقابَل
  `docs/handoff-protocol.md` — وقد أمسك هذا بالضبط أنّ §٢٥ كانت تصف آليّةً لم تبقَ. وما يُقاس به: استخراجُ
  ذكرِ `tools/x.py --flag` من البروتوكول والتأكّدُ من أنّ العَلَمَ موجودٌ في `--help` الأداة (وفي الاتّجاه
  الثاني: كلُّ عَلَمٍ مُعلَنٌ في أداةٍ مذكورٌ في البروتوكول أو يُعلَن أنّه داخليّ).

## بيد المالك (أربعة)

1. **«ادفع»** لصندوقك: مراجعاتُك ٤٩–٥٥ على `claude/reviews-49-55` (`d7457dc`) — وهذا يحلُّ مسألةَ الشاهد (P-9).
2. **نصُّ التذكرة `4785819`** لإغلاق البند ٢ (`STILL EXPOSED`).
3. **تدويرُ رمز Claude** — معلّقٌ منذ الجولة ٤٨.
4. قرارُ الفروع الـ١٥.

## Not proven

- **مراجعةُ الإصلاحات الأخيرة لم يجُرِها بشر:** `630369b` مرّت بـ**العضّ المصنوع** (أربعُ زلات) وبالـCI
  والنسخةِ النقيّة، لا بمراجعةٍ مستقلّة — فمراجعتُك القادمة هي أوّلُ حكمٍ عليها.
- **لم أُشغِّل GitHub Actions محليًّا:** «أخضرُ في CI» مُثبتٌ من `check-runs` على `630369b` **للملفّات**
  المُضافة، ولم أُثبت أنّ الـworkflow سيبقى أخضر على مُشغّلٍ آخر أو مع نُسخِ Python أخرى.
- **حدُّ استثناء الهاش مقيسٌ على الكوربوس اليوم (٦٣ سطراً)، لا مُثبتٌ للمستقبل:** صيغةُ هاشٍ جديدة بلا اسمِ
  حقلٍ صريح ستُكتشَف (ضجيجٌ يُراجَع) — مقصودٌ، لكن كلفتُه غيرُ مقيسة.
- **«الدليلُ الحيّ» في R55-3** (أنّ الخطّافَ أوقف التزامَك على سطرَي `spec_sha`) روايةٌ منك بلا أثرٍ قابلٍ
  للفحص في المستودع؛ قِستُ **الآليّةَ** (البصمةُ كانت تُحجب) لا الحادثةَ بعينها.
- **P-9 و P-10 مقترحتان بلا بناء** ⇒ لا يُدّعى أنّهما تُغلقان صنفَهما.
- **البند ٢ (الانكشاف) ما زال مفتوحاً:** `STILL EXPOSED` آخرَ قياس — إغلاقُه فعلُ مالكٍ لا فعلُ أداة.
