# تقرير: تنفيذُ «الإصلاح أولاً» (الجولة ٤٨) — مصحِّحٌ واحدٌ ثم أرقام

**التاريخ:** 2026-09-24 · **المنفّذ:** سليمان · **الالتزامات:** `3699f19` (الإصلاح) · **الأساس:** `3729d7e`
**النطاق:** البند «أ» + «ب» + «ج» + «د» من `handoff/PROMPT-grader-fix-first.md`، و**وقفةٌ قبل «هـ»**.

---

## ١ · ما تحقّقتُ منه بنفسي قبل أي تعديل (وليس نقلًا عن المراجعة)

| الدعوى | قياسي | الحكم |
|---|---|---|
| «النظام لا يجيب عائلة `argmax_row`» | `cit-03`: `tops=[3]` ترقيمٌ **داخل الصفحة** بينما أوّلُ صفٍّ فيها **٤٩٥** في الكشف · و`qa_tools._number_rows` وثيقتُه: «statement-wide … SAME numbering the RAG…» | **العطبُ في الحاكم** ✓ |
| «فرقُ الحيّ عن المصحَّح = تضخيمُ القاعدة» | **٣٠ سجلاً** من ٢٠٠ بـ`used_row_nos` = ٤٠ بالضبط (A10·B8·C7·D5) | **أثرُ قصّ** ✓ |
| «$4.3573» و«٤٫٧×» | A وB بقراءةِ **بدءٍ متطابقة** حرفيًّا (286.576953765) ⇒ تداخلٌ؛ والحركةُ الكاملة **$2.9333**، والمعزولُ **D = $0.4891** | **جمعٌ مزدوج** ✓ |
| دعوى مقعد: `is_publishable('؟','؟') == True` | قِستُها على هذا HEAD ⇒ **`False`** | **لم تُعَد إنتاجاً** ⇒ مردودة |

## ٢ · الإصلاح (`3699f19`) — **الضابطُ أحمرُ قبل، أخضرُ بعد**

**الجذر (P-1):** كان الحاكم يقبل **اصطلاحَين** لهويّة الصفّ، ويُخرجهما من صفوف الصفحة وحدها، بينما
النظام يُري النموذج اصطلاحًا واحدًا: `(صفحة N، صف row_no)` بترقيم `build_rows` + `_number_rows`.
والأخطر: **الضابطُ الذي كان يحرسها لم يكن يُميّز الفرضيّتين** — صفحتُه الأولى في بياناتها فالتطابقُ حتميّ.

| ما شُغِّل | قبل الإصلاح | بعده |
|---|---|---|
| `tests/test_eval_row_numbering.py` (٥ ضوابط · الضابطُ الأول يُثبت أن الترقيمين مختلفان: ٥ مقابل ٢) | **٣ إخفاقات** | **٥ نجحت** |
| `test_argmax_page_local_index_accepted` (ضابطٌ قديمٌ من عندي) | يمرّ (القاعدةُ القديمة) | **قُلب سالبًا** بحكمٍ مؤرَّخ |
| `tests/` كاملة | — | **590 نجحت · rc=0** |
| `static_gate` · `amount_guard --ratchet` · `render_claims --check` | — | **rc=0 · rc=0 · rc=0** |

**وإصلاحُ القصّ (P-2):** `[:40]` أُزيل، ويُكتب `trace_len` كاملًا؛ والقديمُ المشكوكُ قصُّه (٤٠ بالضبط
بلا `trace_len`) يُوسَم `trace_suspect` **ويُستبعد من المجموع المنشور** — لا يُدفن ولا يُحسب.

## ٣ · الأرقامُ بعد الإصلاح (إعادةُ حكمٍ على **نسخ** — بلا صرف)

| الجولة | حكمُ السجلّ الحيّ | بالمصحَّح (٥٠) | انقلب · **مقصوص** | انقلب · **قاعدة** |
|---|---|---|---|---|
| A | 35 | **30** | 6 | 1 (`cit-09` ↑) |
| B | 30 | **27** | 3 | 0 |
| C | 32 | **28** | 3 | 3 |
| D | 29 | **30** | 0 | 1 (`cit-09` ↑) |

⇒ **١٢ من ١٦ انقلابًا كان أثرَ القصّ لا القاعدة.** وأثرُ بوّابة الفهرس المقيس **+١ لا ٨/٨**: العائلةُ
تفشل لأسبابٍ حقيقيّة (`cit-04/05/10` أثرٌ فارغ · `cit-02` أصابت ٠/٨٨ صفحة) لا لبوّابتي.
⇒ **الادّعاءُ المُصحَّح:** «البوّابةُ كانت تُرفض جوابًا صحيحًا» **صحيحٌ ومُثبتٌ بضابط** — و«النظامُ لا
يستطيع الإجابة» **مردودٌ**؛ أثرُه في الجولات المدفوعة **سؤالٌ واحد**.

**الجدولُ المنشور (بلا المقصوص المُعلَن، ٣٠ سجلاً):** A `27/40` · B `24/42` · C `26/43` · D `29/45`
— والاستبعادُ يُعلنه `eval_run_compare.py` ويُخرجه في حقلٍ صريح لا في الهامش.

## ٤ · التصحيحاتُ المؤرَّخة (لا يُشطب رقمٌ قديم)

1. **الكلفة:** «$4.3573» و«٤٫٧×» **مسحوبان**؛ المعزولُ `D=$0.4891`، وحركةُ العدّاد `$2.9333`.
2. **الفرقُ حيًّا↔مصحَّحًا:** ليس «حجمَ التضخيم» — ١٢/١٦ منه قصّ.
3. **المجاميع:** ٣٠·٢٧·٢٨·**٣٠** (D +1) بالمصحَّح على ٥٠.
4. **`docs/evidence/eval_runs_2026-09-24.json`:** الرقمُ القديمُ مُبقًى موسومًا `…_WITHDRAWN` مع
   `isolated_run_cost_usd` و`key_movement_usd` وشرحِ السبب.
5. **`docs/EVAL_PACK.md:351` («السلسلة لا تتّصل»):** **يُشطب بصراحة** — لم يُقَس في هذا الدور،
   ولا يُترك مؤجَّلًا في مكانه.

## ٥ · صفحة ٦٢٩ — تشخيصٌ من المصدر (لم يُعدَّل شيء)

| القياس | القيمة |
|---|---|
| `pg-629.json` على القرص | **١٧ صفًّا مطبوعًا، منها ٠ برصيدٍ مطبوع** |
| ما تُبقيه السلسلة | **٠ صفًّا** ⇒ `max_page = 628` |
| مصدرُ العطب | **`tools/refusal_test.py:97`** (`if r["balance"] is None: continue`) — يُسقط الصفَّ لفقدان رصيدٍ **قابلٍ للاشتقاق**، فيفقد وجودَه لا رصيدَه |
| السؤالُ الوحيدُ الملامس | **`abs-02`** (`count_rows`, page 629) — والحقيقةُ ١٧ صحيحة، والنظامُ لا يرى الصفحة |

**نصُّ `abs-02` المقترح (غيرُ مطبَّق — يحتاج قرارك):**
`{"id":"abs-02","cat":"رقمي","metric":"abstain","expect":"ambiguous","q":"كم صفًّا مطبوعًا في الصفحة ٦٢٩؟ وإن تعذّر، فبيّن السبب","derive":{"kind":"count_rows","page":629}}`
— فيُقاس **الفرقُ الحقيقيّ** (إعلانُ العطب مع السبب) لا تُخفى الصفحةُ ولا يُسأل سؤالٌ لا جوابَ له.
**والجذرُ (إبقاءُ الصفوف بلا رصيدٍ مُشتقّ في السلسلة كصفوفٍ «بلا سعر») يمسّ الاسترجاعَ والبوّابةَ
وكلَّ الأسئلة ⇒ قرارُك قبل أي تعديل.**

## ٦ · ما لم يُثبت

- **«الطبعة ٢٨–٣٢»** توقّعٌ من المراجعة لا قياس؛ المقيسُ **٢٧–٣٠** (والمنشورُ بلا المقصوص).
- **كلفةُ A/B/C** غيرُ قابلةٍ للعزل (مفتاحٌ مشترك) — والأثرُ على الأرقام المنشورة **غيرُ مقدَّر**.
- **عطبُ صفحة ٦٢٩** و**`parser goldens`** و**`qa_gate ٣/٥`** — كلُّها **سابقةٌ لهذا العمل** وغيرُ مُصلَحة.
- **أثرُ إصلاح القصّ على الجولات القادمة**: متوقَّعٌ (الأثرُ الكاملُ محفوظٌ الآن) ولم يُقَس بعد.
- **`is_publishable`** عُدّت دعوى المقعد مردودةً بقياسٍ واحدٍ على هذا HEAD — ولا أدّعي أنها لا تصدق
  على نسخةٍ أخرى.
