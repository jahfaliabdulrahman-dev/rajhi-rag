id: 20260917-172327-sulaiman
from: sulaiman
to: claude
type: SUBMIT
step: bootstrap — خط الأساس الكامل للمشروع بانتظار أول مراجعة مستقلة
commit: 30167ef484fef8497b107bf0db254d14f00ba201
verdict: -
evidence:
  - `.venv/bin/python -m pytest tests/ -q` → 86 passed in 0.73s
  - `.venv/bin/python tools/qa_gate.py --full` → GATES: 5/5 passed
    (service 200 · redirect 302 · e2e sample بقيم مقفلة · فوتر 10/10 · قيم p1/p2/p9/p10)
  - `.venv/bin/python tools/publish_guard.py` → 0 BLOCK / 0 WARN
  - تقرير الكوربوس المعتمد (629 ورقة، `data/local_sample/slice_629p/slice_report.json`):
    footer = {ok: 621, mismatch: 0, unchecked: 2, absent: 4, gap: 2}
    page_numbers.gaps = [[426,427]] و [[625,626]] · duplicates = {} · backwards = []
    ledger = $8.809917 على 649 نداءً (مسار الراجحي المدفوع)
findings: -
next: مراجعة مستقلة وفق `docs/handoff-protocol.md` §4 (البوابات الست) + البنود المفتوحة أدناه

---

# ما هو المشروع (سياق للمدقّق)

قارئ كشوف حساب مصرف الراجحي الممسوحة ضوئياً (عربي أولاً). الفكرة الجوهرية:
**الرقم الذي لا يمكن إثباته لا يُclaim** — كل صفحة تحمل حكماً مُشتقاً من **مرجع
مستقل** (الإطار التراكمي المطبوع في أسفل كل صفحة: مدين/دائن/رصيد)، وأي انحراف
يُعزل بالدلتا (`footer(pg) − footer(pg−1)`) بدلاً من التراكمي.

**الحالة المُثبتة على الملف الكامل (629 ورقة / 630 صفحة مطبوعة أصلاً):**
| المؤشر | القيمة |
|---|---|
| نظافة السلسلة الرصيدية | 5770/5793 (99.6%) |
| صفحات موثّقة مقابل الفوتر | **621 مطابق · 0 منزاح** |
| فجوات مسح يكتشفها النظام | 2: ص427 (ينقص ٤٢٦،٤٢٧) · ص626 (ينقص ٦٢٥،٦٢٦) |
| غير قابلة للتحقق (جارها بلا إطار) | 2 · بلا إطار: 4 |

# آخر التسليمات (شاهد حقيقي)

```
30167ef feat(export): transaction dates in the table + honest carry-forward
2ba9665 feat(preflight): local readiness checker for a new statement file (no API)
b71dcdf feat(tool): mechanical duplicate scan (ink-distance, no API) + count closed
0b6c7e1 docs+tool: full page-number sweep (26 sheets, zero API cost) + onboarding guide
5fe9251 docs(ledger): count reconciled (630 printed vs 629 sheets) + field-reliability lesson
cc07768 fix(report): never attribute a divergence to a page we cannot isolate
38daca0 feat(reader): printed page number in the main read + scan-gap detection (G2 path)
4af5a3c feat(tools): page_numbers montage mode + verified sample mapping
```

# جدول أعمال المراجعة (مرتّب بالأولوية)

1. **`30167ef` — عمود التاريخ + الاستكمال** (`app.py::_date_cell`،
   `vlm_reader.fill_missing_dates`): هل الاستكمال أمين؟ (الصفوف بلا تاريخ: افتتاحي/ترحيل
   يجب ألا تُملأ أبداً). اقرأ `tests/test_dates.py` وحاول كسره.
2. **جوهر المصداقية — أساس الدلتا** (`src/statement_qa/footer_oracle.py`):
   `check_page_footer` (تراكمي) مقابل `delta_status`/`delta_checkable` (دلتا)، وقاعدة
   «لا تُنسب إزاحة إلى صفحة لا يمكن عزلها». اختبر: هل يمكن أن تمرّ صفحة منزاحة كـ`ok`؟
3. **`cc07768`** — إصلاح أساس التقرير: تأكد أنه لا يوجد مسار يُنتج `mismatch` بلا عزل.
4. **`tools/publish_guard.py`** — هل يمكن لبيانات كشف حقيقية أن تتسرب شجرةً أو تاريخاً؟
   (الملف `.gitignore` يمنع `data/local_sample/`؛ الحارس يفحص الشجرة والتاريخ وقبل الدفع.)
5. **`docs/suspects_log.md`** — كل بند مصنَّف؛ هل يوجد ادّعاء بلا دليل؟

# بنود مفتوحة — أطلب منك مهاجمتها (Red Team)

| # | البند | الحد المعروف |
|---|---|---|
| 1 | **الواجهة (Gradio) لا تتحمل 629 صفحة**: بلا checkpoint/resume/sقف كلفة، وطلب واحد يمتد ~4 ساعات (وسيط مقيس 22.2ث/صفحة) والمعرض يحمّل كل الصور | المعالج الحقيقي = `tools/scale_slice.py` (نقاط استئناف + دفتر كلفة + حواجز توقف) |
| 2 | **تباين حقل التاريخ**: قراءة المالك أعطت 93/104، وقراءتنا المرجعية 103/104 لنفس الصفحات | عيب قراءة عابر؛ المصلح أُضيف اليوم لكن **بلا اختبار على قراءة حقيقية جديدة** |
| 3 | **لوحة الأدلة في «سؤال وجواب» تعرض صفوفاً لأي سؤال** (بلا عتبة صلة — ملاذ أخير بعد الاسترجاع) | مقصود مؤقتاً؛ الحل المقترح: عتبة تشابه + حالة «لا أدلة» |
| 4 | **العدّ لا يتوازن**: 630 مطبوعاً مقابل 629 ورقة مع فقد 4 ⇒ يفترض أوراقاً غير مرقّمة؛ موضع تحوّل الانزياح (−1) غير محسوم | مُثبت ميكانيكياً: **لا ورقة مكررة** (أدنى مسافة حبر 0.273) |
| 5 | **التعميم على كشف/بنك آخر غير مُثبت** (لم يُقرأ إلا ملف واحد) | أُضيف `preflight` + `docs/ONBOARDING_NEW_FILE.md` كخطوة أولى |

# ملاحظة كلفة (مهمة للمدقّق)

البوابة ② (`qa_gate --full`) تُشغّل تشغيلاً **حياً** على العينة وتكلّف ~$0.20 من رصيد
المزود. شغّلها عندما يلمس التسليم مسار القراءة/العرض (وهذا التسليم يلمسه)، واذكرها
في حكمك. بقية البوابات محلية ومجانية تماماً.
