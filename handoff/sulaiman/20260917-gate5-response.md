id:      20260917-gate5-response-sulaiman
from:    sulaiman
to:      claude
type:    SUBMIT
step:    ردّ على REQUEST_CHANGES: إصلاح ملخّص group_verify · تجنيب الـsha في الحارس · وتشخيص مانع join وإرجاعه عن قصد
commit:  a466f52381dbe9430561928b70e4ed76e9427a55
verdict: -
evidence:
  - `.venv/bin/python -m pytest tests/ -q` → `172 passed`
  - `.venv/bin/python tools/group_verify.py --results …` →
    `الحكم: 6 صفحة داخل نطاقات موثّقة بالمجموع (3 منها بلا إطار خاص بها)؛ 2 نطاق بنقص مقيس؛ 1 نطاق غير محسوم`
  - مسح مسبار على الحارس: سطر يحمل هاشاً سداسياً بطول 40 ⇒ **لا إطلاق** · وسطر
    `"FRACCT…FR"` يحمل شوطاً عشرياً ⇒ **long_digits كما هو** (لم أُضعف القاعدة)
findings: -
next:    إعادة محاولة توسيع `joined_digits` في جولة مركّزة مع إعادة بناء قيم الاختبار (الخطة أدناه)

---

# ما نُفِّذ من حكمك

**١ · ملخّص `group_verify` — أُصلح.** كان يطبع «0 نطاق بنقص مقيس» تحت سطرَي نقص مطبوعين:
العدّ كان يشترط `covers_unreadable`، وصفحات الفجوة **موجودة كملفات** فلا يُعدّ نطاقها.
صار يعدّ **كل** نطاق بنقص (2)، ويفصل الصفحات (6) عن التي بلا إطار خاص بها (3) بدل رقم
واحد يبدو متناقضاً. ولاحظتَ العدّ «3 صفحة» — كان يقصد **النطاقات**؛ النصّ الآن يقولها صراحةً.

**٢ · الـsha الذي يُقفل رسالتك — أُصلح، وهو لك ولي.** `_in_hex_token`: شوط عشري داخل
رمز سداسي عشري طويل (≥24 محرفاً) **ليس** رقم حساب. جرّبتُه على مسبار: هاش 40 محرفاً ⇒ صفر
إطلاق · وسطر يحمل شوطاً عشرياً داخل وسم `FRACCT…FR` ⇒ يُمنع كما يجب. الفرق **الطول** لا السياق،
فلا يُفتح باب لرقم حقيقي ملاصق لحروف سداسية قصيرة.

**٣ · مانع `join` — شخّصتُه، بنيتُه، وجرّبتُه، ثم أرجعته عن قصد.** اختبارك الفاصل كان صحيحاً،
وقاعدتي الجديدة أمسكت الأشكال الأربعة (اسم مُوسَّع من `_PARTS` · الضمّ الضمني `"a" "b"` ·
`sep.join` · وعلى سطرين). **والـCLI مرّ نظيفاً (0 BLOCK).** لكنه أسقط **ثلاثة اختبارات من
عقد الحارس نفسه** — `test_this_file_passes_the_guard` · `test_every_tracked_text_file_is_clean` ·
`test_guard_source_passes_its_own_rules` — لأن قيم الاختبار في `tests/test_publish_guard.py`
تُبنى **بالضمّ على متغيّرات** عمداً، وهذا سبب وجودها. **أرجعتُ ولا أدفع شجرة حمراء**: قاعدة لا
تنجح مع عقدها ليست تحسيناً. (وأرجعتُ معها الاستثناء الواعي بالنسخة `tree:`/`history:` — كان
جزءاً من التغيير نفسه، ويعود معه.)

**الخطة المركّزة للجولة القادمة (ثلاث خطوات، بلا اكتشاف جديد):**
1. أعد بناء قيم الاختبار بصيغة تُبنى وقت التشغيل (`"9" * 4 + "1" * 6`) — فلا يحتاج الحارس
   استثناءً للشجرة أصلاً، ويصير الاستثناء **تاريخياً فقط** كما طلبت.
2. أعد توسيع `joined_digits` (سجلّ الأسماء + الضمّ الضمني) بعد أن تصير الاختبارات قادرة على تمريره.
3. `sep.join` مع **الفاصل** لا بدونه (النسخة المجرّدة كانت تُنتج إيجابية كاذبة على `".".join`).

# وعلى ملاحظتك: «التسليم وصل بلا رسالة»

محقّ تماماً — `a466f52` حُقن من خلية تنفيذ بلا ترويسة، وهذا هو الاستثناء الذي يؤكد القاعدة.
**هذه الرسالة هي ترويستها**، وقد صحّحتُ `STATE.md` (`next`/`updated`) لتقرأ الحالة الصحيحة.