# تقريرٌ إلى Claude — تصفيةُ الجولة ٤٩ وإصلاحُ الخمسة (٢٠٢٦-٠٩-٢٤ ١٢:٤٢ +٠٣)

**إليك:** كلود · **من:** سليمان · **المدى الذي راجعته:** `3729d7e...856c886` · **الـHEAD الآن:** **`3bd5ab6`**
**اختبارات:** ٦٠٢ (كانت ٥٩٠) · **البوّابات:** `render_claims --check` rc=0 · `amount_guard --ratchet` rc=0 ·
`static_gate` rc=0 (`files: 131 · findings: 0`) · `publish-guard` 0 BLOCK · الپي آر **#119** `OPEN`/`MERGEABLE`.

> هذه التصفيةُ **قِيست دعوى دعوى** لا قُبلت ولا رُدّت جملةً: الثلاثةُ التي أسقطت دعاواي أُعلنت، والذي ثبت
> أنّه لا يقف أُبقي عليه بمخالفةِ أمرِ المراجعة مكتوبةً. **والنتيجةُ تُقبل ولو ضدّي.**

---

## ١) الخمسة — حكمي على كلٍّ بدليله

| # | الدعوى | حكمي | دليلي (الأمرُ أو القياس) |
|---|---|---|---|
| 1 | **S-4**: `is_publishable({"spec_sha":"?"}, "?") == True` | **مُثبتة — وكان مردودي خطأً** | قِيست بـ`?` لاتينيّة على HEAD ⇒ `True`؛ المُنتِجُ `_spec_sha` يُخرج `?` عند فشل القراءة، وقاعدتي كانت تحرس `؟` عربيّةً ⇒ مرّت بصمةُ الفشل |
| 2 | **أربعةُ بنودٍ من ٤٨ لم تُمسّ** (P-2 · S-1 · S-2 · P-3) | **مُثبتة — نُفِّذت كلُّها الآن** | §٢ أدناه، ولكلٍّ ضابطٌ يسقط عند الإرجاع |
| 3 | **التذبذبُ ٢٧–٣٠ ليس من النموذج**: على ٣٣ سؤالًا نظيفًا الأذرعُ ٢٣/٢٣/٢٣/٢٣ | **مُثبتة — وأعدتُ قياسها بنفسي** | `[23, 23, 23, 23] من 33` على أسئلةٍ بلا مهلةٍ ولا مقصوص في أيّ ذراع؛ وعلى الخمسين `[30,27,28,30]` ⇒ **حتميٌّ على النظيف** |
| 4 | **«السلسلة لا تتّصل» قديمة/خاطئة** (لديك ٦١٩/٦٢١) | **مُثبتة — والخلافُ تعريفٌ لا رقم** | بتعريف «الرصيد↔الرصيد أو تفسّره أوّلُ حركة»: **١٢٤/٦٢٤** · وبتعريف **إقرار السلسلة نفسِها** (أعلامُ `ok` على أوّل صفٍّ): **٦٢٤/٦٢٤** ⇒ رقمان صحيحان وتعريفان؛ جملتي كانت **بلا تعريف** وهي العلّة الحقيقيّة |
| 5 | **ثلاثةُ إصلاحاتٍ بلا ضوابط** (P-2 · S-1 · S-3) | **مُثبتة** | `tests/test_eval_gate_controls.py` — **١٢ ضابطًا**؛ وضابطُها الأوّل أسقط **علّتين حقيقيّتين** لم أكن أراهما: القاعدةُ كانت تعتمد على مَن شغّل `--rescore` قبلها، والبصمةُ كانت تشهد على **مسارٍ مكتوبٍ في الكود** لا على الملفّ المستعمل |

---

## ٢) ما أُصلح في هذه الجولة (كلُّه مُختبر)

| البند | العلّةُ الجذريّة | الإصلاح |
|---|---|---|
| **S-4** | القاعدةُ تقارن **قيمةً بعينها** (`؟`) والمُنتِجُ يُخرج `?` | البصمةُ **بصيغتها** `^[0-9a-f]{12}$` ⇒ لا تموت العلّةُ بموضعٍ منها · **وصارت تشهد على الملفّ المستعمل فعلًا** (`_spec_sha(a.questions)`) لا على مسارٍ في الكود |
| **S-3** | فرعُ `--rescore` يعود **قبل** فحص المراسي؛ والكتابةُ **قبل** الحكم | البوّابةُ قبل الفرعَين · **الحكمُ قبل الكتابة** · رفضٌ بلا كتابة (`rc=3`) |
| **S-2** | السقفُ `int(budget/0.01)` = ٦٠ لخمسين سؤالًا ⇒ **لا يُلجِم** | سقفٌ **مقيسٌ من رصيد المزوّد قبل كلّ سؤال** + إعلانُ التغطية عند التوقّف |
| **P-3** | النائبُ (`<انتهت المهلة>`) يُحكم فشلًا ويدخل المقام | `substitute`: يُعلَن، ولا يُحكم عليه، ولا يُحسب ⇒ والمقامُ يُطبع بلا نوائبَ ولا مقصوص |
| **P-2 / S-1** | قاعدةُ الأثر المشكوك مكرّرة/غائبة | `eval_stamp.is_trace_suspect` **موضعًا واحدًا** تستهلكه الأداةُ والمقارنة معًا |
| **P-4** | جدولُ الأرقام السارية لا يصدق على الـHEAD | §جديد في `docs/EVAL_PACK.md`: **الأرقامُ مولّدةٌ بأمرٍ مكتوب** + «٢٣/٢٣/٢٣/٢٣ على النظيف» |

**الضابطُ المُلزم:** `tests/test_eval_gate_controls.py` — ١٢ ضابطًا، ومِنها أنّ **قيمة فشل المُنتِج نفسَها**
(`?`) يجب أن تُرفض، وأنّ `--rescore` **لا يكتب** عندما لا يُنشر، وأنّ النائبَ **لا يدخل المقام**.

---

## ٣) الأرقامُ السارية (بلا صرف) — ومَن قاسها

| الجولة | المجموع | بلا النوائب والمقصوص |
|---|---|---|
| A متزامنة | ٣٠ | ٢٧ |
| B متزامنة | ٢٧ | ٢٤ |
| C متزامنة | ٢٨ | ٢٦ |
| **D منفردة** | **٣٠** | ٢٩ |

**الأمرُ المُعيد:** `--rescore` على نسخٍ من الجولات الأربع ثم `tools/eval_run_compare.py`.
**والدرسُ المسجَّل:** على ٣٣ سؤالًا نظيفًا الأذرعُ **٢٣/٢٣/٢٣/٢٣** ⇒ الوصفُ الصحيح للنتيجة:
«حتميّ على الأسئلة النظيفة، والفرقُ كلُّه من المهلات والأثر المقصوص» — **لا** «تذبذبٌ ±١٠٪ في النموذج».

**ومسحوبٌ من دعاواي السابقة (ثلاثة):** «٨/٨ لا تجيب» (الأثرُ المقيس **+1**) · «فرقُ الحيّ عن المصحَّح
تضخيمُ قاعدة» (١٢ من ١٦ انقلابًا = **أثرُ القصّ `[:40]`**) · «$4.3573 و٤٫٧×» (نافذتا العدّ متداخلتان ⇒
المقيسُ الموثوق: الحركةُ الكاملة **$2.9333** والمعزولُ **D $0.4891**).

---

## ٤) حالُ بنود الجولة ٤٨ (تصفيةٌ نهائيّة)

| البند | الحالة |
|---|---|
| P-1 (ترقيمُ الصفّ) · P-2 (القصّ) · P-3 (النائب) · P-6 (الكلفة) | **مُغلقة** بإصلاحٍ + ضابط |
| S-1 (تغطية) · S-2 (السقف) · S-3 (البوّابة) · S-4 (البصمة) · S-5 (الضابط السالب) | **مُغلقة** الآن بإصلاحٍ + ضابط |
| P-4 (جدولُ الأرقام) | **مُغلقة** بـ§مولَّدٍ بأمر |
| **P-5 (سطحُ أدوات السلسلة)** | **مفتوحة كما هي** — لا أداةَ تعرض صفوف الصفحة؛ **لا أملك إصلاحاً صادقاً بلا تغييرِ عقدِ الأدوات** (وهو قرارُ المالك) |
| **عطبُ صفحة ٦٢٩** (`abs-02`: ١٧ صفًّا بلا رصيدٍ مطبوع ⇒ النظامُ يقول «ينتهي عند ٦٢٨») | **مفتوحة** — **وقرارُ إعادةِ الصياغة ليس بريئًا**: تعديلُ `questions.json` يُغيّر `spec_sha` ⇒ **يُسقط شهادةَ الـ٢٠٠ جوابٍ كلِّها** (وهذا قياسٌ بنيويّ: القاعدةُ على بصمة الملفّ) ⇒ **لا تُعاد الصياغة قبل جولةٍ جديدة** إلا بقرارٍ صريح |

---

## ٥) ما لم يُثبت (صراحةً)

1. **رقمُك ٥٤٢/٤/٤٤ في النسخة بلا بيانات: لم أُعيده.** قِستُ الالتزامَ نفسَه: بلا `data` ⇒ **٥٣٨/٤/٤٨** ·
   و`data/eval_pack` وحدها ⇒ **٥٤٧/٣٠/١٣** — والثلاثةُ مجموعه ٥٩٠ ✓. فالفرقُ **تخطيطُ بياناتٍ لا شجرة**،
   **وتوزيعُك لم يُعَد** ⇒ يبقى **مجهولاً** حتى تُرسل تخطيطَ نسختك (أمرُ الحسم جاهز).
2. **أثرُ إصلاح القصّ على جولاتٍ قادمة لم يُقَس** (الجولاتُ الأربع **قبل** الإصلاح؛ أُعيد حكمُها على نسخ).
3. **عزلُ كلفة A/B/C** غيرُ ممكن (نافذةُ عدّادٍ مشتركة وبدءٌ متطابق) — المُعلَنُ فقط ما قِيس.
4. **«الطبعة ٢٨–٣٢»** توقّعٌ لا قياس (بقي مُعلنًا كذلك).
5. **إبطالُ رمز Claude** ليس بيدي: `claude auth status` ⇒ `loggedIn:false` ولا مخزنَ محليًّا ⇒ **يحتاج نقرتَك**.

---

## ٦) ما ينتظر قرارَ المالك (واحدٌ من العروض الأربعة أُنجز أعلاه)

1. **ثقبُ سطح الأدوات** (P-5): أُضيف أداةُ سردِ صفوف الصفحة (تغييرُ عقدٍ) أم يُترك مُعَلنًا؟
2. **صفحة ٦٢٩**: إعادةُ صياغة `abs-02` **تُسقط شهادةَ الـ٢٠٠ سجل** ⇒ إمّا جولةٌ جديدةٌ كاملة أو إبقاءُ الحال.
3. **تطهيرُ التاريخ**: تذكرةُ GitHub Support `4785819` **بلا جواب**؛ `refs_exposure_probe.py` ⇒ `STILL EXPOSED`.
4. **رمزُ Claude المتسرّب**: الإبطالُ يحتاج نقرتَك.

**ولا حُكمَ إلا بضابط، ولا رقمَ إلا بأمرٍ يعيده، ولا تعريفَ بلا قياس** — وهذا ما تعلّمتُه من هذه الجولة.
