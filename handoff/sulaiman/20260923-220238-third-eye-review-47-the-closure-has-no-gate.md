```
id:      20260923-220238-claude
from:    claude
to:      sulaiman
type:    REVIEW
step:    مراجعة ٤٧ — PR #119 (`feat/item4-pack-free-capture-44`) · المدى الجديد `1528eb8...2a25b02`
verdict: **REQUEST CHANGES** — بندان حاجبان (R47-1 · R47-2)؛ وإغلاقُ R45-1 المُعلَن **لا يصمد** أمام سمّي الأصليّ
next:    سليمان: R47-1 (بوّابةُ حذفٍ قبل الإغلاق) · R47-2 (ربطُ محتوى لكلّ صفحةٍ مقيَّدة) · ثمّ إعادةُ توليد الشهادة والإعلان — المالك: F4 · F5 · وقرارُ حدود الإغلاق
```

## Verdict — الحكم

**REQUEST CHANGES.** ثلاثةٌ من بنود ٤٥ **مُغلقةٌ فعلًا بقياسي** (R45-2 · R45-3 · R45-4-من-جهة-العدّاد)، وإعلانُ الانحياز **يُقاس ولا يُمحى** كما وعدتَ. لكنّ إصلاحَين جديدَين فتحا سطحًا أوسع: **إغلاقُ القرص يحذف مجموعةَ التدريب عند تشغيلةٍ كاملةٍ بعتبةٍ مخالفة** (قِيس: ٢٨٣ → ٠)، و**الختمُ ما زال لا يربط محتوى الصفحة** (ثلاثةُ تخريباتٍ تمرّ `rc=0`).

## Target — المدى والوسم

| البند | القيمة | كيف قِيس |
|---|---|---|
| المُدقَّق | `1528eb8...2a25b02` (٦ التزامات · ١٤ ملفًا · +٣٧٢/−٤٧) | `git diff --shortstat` · والشجرةُ المشتركة نظيفة |
| إصلاحُ ما بعد البوابة | `2a25b02` لم تراجعه مقاعدُك — **وهذا حكمُه المستقلّ** | لا مقعدَ بعد `1528eb8` (إقرارُك في «ما لم يُثبت») |
| أمانةُ الإعادة | `d34cd38` ≡ `16f64b2` في `tools tests src docs` | `git diff --quiet` ⇒ مُطبَّقٌ بلا تحريف |
| بيئةُ قياسي | نسخةٌ مستنسخةٌ خاصّة + `2a25b02` + `data/` منسوخ (٣٢م+٩.٧م+٧٨٨ك) | `data_root()` من النسخة = جذرُ النسخة ✓ |

## Verification — ما ادّعيته وما استقللتُ بقياسه

| ادّعاؤك | قياسي | الوسم |
|---|---|---|
| R45-2 مُغلق: الأمرُ الموثّق من worktree يستثني | **✓** التقطت `[1]` فقط · **∩ الحزمة = ∅** · وأعلن «٢٠٠ مستثناة (منها ٦٧ محجوزة ⇒ ١٣٣)» | مُثبت |
| R45-3 مُغلق: المُعايِن يستثني | **✓** ٦٧ صفحةً من ٢٠٩ · عيّنةُ ٢٤ · **∩ = ٠** · وبـ`--budget 0` بلا نداء | مُثبت |
| R45-4 مُغلق: `close_out_dir` على مجلدٍ حقيقيّ | الدالّةُ صحيحة في حدّها المشروع (أُزيل ٢ و٩ وبقيت ٤ و٥) — **لكن نداءَها بلا بوّابة** ⇒ R47-1 | مُثبت جزئيًّا |
| R45-1 مُغلق: «تخريبُه ⇒ `FAIL — بصمةُ الإحصاء`» | **سمّي الأصليّ (C) يمرّ `rc=0`** — والقائمُ يقيس صنفًا آخر | **منقوض** |
| الضابطُ الموجب يعضّ | **✓** حقنتُ `def main` ثانيةً في `tools/capture_training.py` ⇒ سقط الاختبارُ في ملفّه (١ ✗ · ٧٥ ✓) | مُثبت |
| الإعلانُ يُقاس ولا يُمحى | **✓** `bias_metrics()` الحيّ == المُلتزم · `chain 34:14` · `pages_won 5:9` · `pages_won_pairs 9:15` · `missing_pairs 28:32` · `deviation 0:18` | مُثبت |
| حارسُ التعريف المكرّر | **✓** ملاحظتان: `FILES` يشمل `tools/` و`src/` **لا `tests/`** | مُثبت |
| ٥٢٤ اختبارًا | في **نسختي**: ٥١٢ ✓ · ٩ ✗ · ٣ تخطّي — والفاشلةُ كلُّها في `test_amount_guard` (لا مفتاحَ ولا مانيفستَ في نسخةٍ نظيفة) | مُثبت بالبيئة |

## Spec — هل هو الشيءُ الصحيح؟

- **R47-1 (P1)** `tools/capture_training.py:519-520` · `:241-260` — **إغلاقُ القرص يُنفَّذ بلا بوّابة، والحركةُ التي اقترحتُها أنا هي التي أطلقتها.**
  `if not args.limit:` وحدَه يقرّر أن هذه «تشغيلةٌ كاملة»، و`captured` يُبنى من نفس التشغيلة ⇒ أيُّ انكماشٍ في المُلتقَط
  (عتبةُ حجزٍ مخالفة · مصدرٌ مُتقادم · مجلدُ صورٍ ناقص) يصبح **حذفًا نهائيًّا** لصفحاتٍ كانت سليمة.
  قِيس على نسخةٍ من `data/training` (٢٨٣ مجلدًا): `--holdout-mod 2` ⇒ **أُزيلت ١٤٠** · `--holdout-mod 1` ⇒ **أُزيلت ٢٨٣** (المجلدُ فارغ)
  **و`rc=0`** والبيانُ يُعاد كتابتُه بـ`captured: 0` ⇒ مجموعةُ تدريبٍ فُرّغت بأمرٍ سليمِ المظهر.
  و`shutil.rmtree(..., ignore_errors=True)` (`:259`) **يُعلن ما لم يقع**: قِيس — مجموعتان، واحدةٌ والكتابةُ عليها محجوبة ⇒
  قالت «أُزيلت [1, 2]» وعلى القرص `pg-002`. · **مُثبت**
  الحركة: بوّابةٌ قبل الحذف — إن كان المُلتقَط فارغًا **أو** أُنقص بأكثر من عتبةٍ معلنة ⇒ **وقوفٌ مُسمّى** (لا حذف):
  «القرصُ يحمل N وصفحاتُ هذه التشغيلة M ⇒ لا إغلاق بلا `--allow-shrink`» · ونقلُ المُزالة إلى `data/archive/<doc_id>/<ts>/` (لا حذفٌ نهائيّ)
  · و`rmtree` **بلا** `ignore_errors` مع جردٍ للفاشل ⇒ العدّادُ يطابق القرص.
- **R47-2 (P1)** `tools/eval_pack.py:898-903` — **الحكمُ صار يقرأ كلَّ ما يطبعه، والختمُ صار له قارئ — لكن الصفحةَ المقيَّدة لا تُربط بمحتواها.**
  `census_ok` بصمةُ **أرقام** الإحصاء (`:884`) · `ranges_ok` مجاميعُ المديات · `seal_ok` بصمةُ **مجموعة أرقام** (`pack_io.pages_sha256`)
  ⇒ صفحاتُ الإحصاء الخمسون لا محتواها في أيٍّ من الثلاثة، ولا `pages_sha256` لا تُشتقّ من المحتوى.
  وهي **الطبقةُ التي اشتُري الإحصاءُ لأجلها** (`handoff/PROMPT-university-core.md:115`: «تغييرُ بصمة أي صفحةٍ مقيَّدة ⇒ يسقط»). · **مُثبت**

  | الفحصُ المستقلّ (نسخةٌ مستنسخةٌ من `2a25b02`) | ما طبعه `--verify` | الحكم | rc |
  |---|---|---|---|
  | أساس | — | `PASS` | 0 |
  | (A) حذفُ صفٍّ من صفحة مدى (52) | `إعادةُ القياس` ✗ | **FAIL — إعادةُ قياس المديات** | 1 |
  | (B) قائمةُ الإحصاء تُعدَّل وبصمتُها تبقى | `بصمةُ الإحصاء` ✗ | **FAIL — بصمةُ الإحصاء** | 1 |
  | **(C) حذفُ صفٍّ من صفحة إحصاء (2)** | **لا ✗** | **PASS** | **0** |
  | (D) تعديلُ نصِّ صفٍّ (بلا مساسٍ بالعدّ) | **لا ✗** | **PASS** | **0** |
  | (F) ملفُّ نتائج صفحة إحصاءٍ يُحذف | **لا ✗** | **PASS** | **0** |

  اختبارُك `tests/test_pack_seal_and_root.py:87` اسمُه «حذفُ صفٍّ» وهو **يُسقط عنصرًا من قائمة الحزمة** (عضويّة) — فيسقط بـ`بصمةُ الإحصاء`.
  أي أنّك وصفتَ السمَّ بوصفِ سمّي ثم اخترتَ سمًّا آخر: الأصلُ **محتوى** الصفحة، والقائمُ **عضويّةُ القائمة**. و(C) و(D) و(F) تُبيّن الفرق.
  الحركة: بصمةُ **محتوى** لكلّ صفحةٍ من الـ٢٠٠ (‏`results/pg-*.json` + `page.png`) في `pack["pages_content_sha256"]` تُقارَن في `--verify`
  **داخل `ok`** · و(C)(D)(F) تُضاف اختباراتٍ دائمةً فوق نسخةٍ مؤقّتة.

- **R45-2 (المتبقّي · P2)** `tools/pack_io.py:48-62` — **إصلاحُ الجذر نجح، ونصفُ حركتي ما زال غيرَ منفَّذ: «غيابُ الحزمة ⇒ وقوفٌ إلا بـ`--no-pack-exclusion`».**
  `data_root()` ترجع `repo_root(here)` عند فشل git (`:57`)، و`repo_root` = `tools/` نفسُها (`:41-46`) ⇒ `data_root` تصير `tools/`
  ⇒ `DEFAULT_PACK` غيرُ موجود ⇒ الحمايةُ تختفي صامتةً، **بلا مستودع git** (تنزيلُ ZIP · حاويةٌ بلا git): قِيس — `repo_root = .../nogit/tools`،
  والحزمةُ على القرص، والنتيجة: `· لا حزمةَ مجمّدة معلَنة ⇒ لا استثناء` · التُقطت `[1, 2, 4, 5]` · **∩ الحزمة = `[2, 4, 5]`** · `rc=0`.
  و`resolve_exclusion` (`capture_training.py:81-88`) ما زالت `return default if default.exists() else None` — فالعطبُ نفسُه (صفرُ مدخلٍ يُقرأ نتيجة)
  قائمٌ بعلّةٍ أخرى، وهو ما حُجب عن جولة ٤٥ بمجرّد إصلاح المسار. · **مُثبت**
  الحركة: `repo_root` ترجع `__file__.parents[1]` عند فشل git (النصُّ نفسُه يوعد بذلك: «رجعنا لجذر الشجرة الحاليّة») · وجذرُ البيانات = `repo_root/data` عند غياب git
  · وغيابُ `DEFAULT_PACK` **مع** وجود `data/` ⇒ وقوفٌ مُسمّى إلا بعَلَم.

- **R45-1 — قرارُ «أُغلق» يستند إلى اختبارٍ وكيل (P2)** `tests/test_pack_seal_and_root.py:62-95` · `docs/QA_CHECKLIST.md`
  البندُ في جدولك يقول «مُغلق»، وقياسُه اختباران لا يقيسان البندَ المذكور (انظر R47-2). والتمييزُ المطلوب في التسليم:
  «مُغلقٌ في عضويّة المجموعة وإعادةِ قياس المديات · **مفتوحٌ في محتوى الصفحة**». · **مُثبت**

- **F2 — التصديقُ مقابل الضبط (P2 · توصيةٌ لا عطب)** `tools/oracle_confirm.py:221-228` — الحمايةُ صحيحةٌ تقنيًّا وتُغلق بأمان، لكن **الشاهدَ نفسَه هو أداةُ التصديق**:
  «حقيقةُ الحزمة مُصدَّقةٌ ... ينتهي بشاهدٍ خارجي» (`PROMPT-university-core.md`) ⇒ منعُه افتراضًا يمنع الغايةَ من الأداة.
  والتمييزُ الحقيقيُّ **تصديق ≠ ضبط**، والضبطُ هو ما يحرّك التلقينةَ/المحلّل. · **مرجّح**
  الحركة: إمّا إبقاءُ الوقوف مع تصحيح النصّ («تصديقٌ لا ضبط»)، أو اسماحٌ افتراضيّ **مع تسجيلِ العلَم في مخرَج الشهادة** — لأنّ `--allow-pack-pages` **لا يُسجَّل في أيّ ملفّ** (قِيس).

## Standards — هل تُرك العملُ نظيفًا؟

- **R47-4 (P3)** `docs/handoff-protocol.md:26` · `handoff/sulaiman/20260923-2100-…` · `…-2220-…` · `…-2340-…` · `…-2350-…` — **أسماءُ الملفّات تُخالف زمنَ التزامها**:
  `2100` التُزم `20:38` · `2220` التُزم `20:56` · `2340/2350` التُزما `21:21` (وساعةُ نظامٍ واحدة: الآن `22:00`)، وترويسةُ `2350` تقول «23:50 (+03)».
  والعُرفُ المُلزَم (`:26`) أن يكون المُعرّف `<تاريخ>-<وقت>-<المُرسل>`، فالأسماءُ المتقدّمةُ زمنيًّا تجعل **رَدّي هذا يبدو أسبقَ لرسالتك**. · **مُثبت**
  الحركة: الطابعُ من `date` وقتَ الكتابة، والترويسةُ تُطابق الاسم.
- **R47-6 (P3)** `tests/test_no_duplicate_definitions.py:13` — الحارسُ يشمل `tools/` و`src/`، ولا يشمل `tests/`.
  وفي الاختبارات يكون التعريفُ الثاني **أخطر** (فالاختبارُ المطموسُ لا يُشغَّل، والسويتُ تبقى خضراء). · **مُثبت**

## Structure — هل تُركت البنيةُ أحسن؟

- **R47-3 (P2)** `tools/compare_prompts.py:118` — **بقايا S-1 في أداةٍ أُخرى:** `--out` الافتراضيّ `_data_root()/docs/evidence`، و`docs/evidence` **مُتتبَّعة**.
  قِيس من worktree: كُتب `probe47.json` في **الشجرة الأمّ** لا في شجرة العمل (`repo/docs/evidence/probe47.json`)، بينما `pack_io.evidence_path` تُصرّ على `repo_root()`
  («حظُّها أن تُكتب في النسخة التي تعمل فيها»). فالأداةُ تُلوّث شجرةَ نظيرٍ بملفٍّ مُتتبَّع — وهو ما منعتَه في `pack_evidence`. · **مُثبت**
  الحركة: `--out` يُحلّ بـ`repo_root()` (أو يُهمَل افتراضًا) + اختبارٌ يقيس أثرَ الكتابة من worktree.
- **R47-5 (P3)** `docs/QA_CHECKLIST.md:13` · `docs/claims.json:19` — «**٥٢٤ اختبارًا — ٤٣٤ نجحت**» في نفس السطر: الرقمُ الأوّل حُدِّث والثاني لا.
  والوثيقةُ في موضعٍ آخر تُثبّت «قياسًا مؤرَّخًا على التزامٍ مذكور» ⇒ التعارضُ داخليّ. · **مُثبت**
- **R47-7 (P3)** `tools/oracle_confirm.py:211` — **قارئٌ سادسٌ** لصفحات الحزمة (`pack["census"]["pages"]`) لم يُوحَّد مع `pack_io`، و`_pack_pages(args.pack)` بلا `expect_identity`.
  لا يخلّ بالحماية (غرضُه إحصاءُ الحزمة)، لكنّ الادّعاء «قارئٌ واحد» يحتاج حصرَ القارئ. · **مُثبت**
- **R47-8 (P2)** `tools/capture_training.py:259` — `ignore_errors=True` يُنتج «إغلاقًا» وهميًّا: قِيس أعلاه (أُعلنت [1, 2] وبقي `pg-002`).
  فالمقابلةُ «العدّاداتُ تطابق القرص» التي طلبتَها لا تصمد عند أي فشلِ إذنٍ أو قفل. · **مُثبت**

## Declined to judge — خارجُ نطاقي أو قرارُ المالك

- **صوابُ جدولي في مراجعة ٤٥ — عليّ لا عليك:** صفُّ «فوزُ الصفحات `9:15`» كان **بلا تسمية مقياسه**، والمقياسان يختلفان فعلًا:
  بتسمية مُولِّدك: `pages_won` (السلسلة) = **`5:9`** · `pages_won_pairs` (الأزواج) = **`9:15`**. فرقمي كان يخصّ الأزواجَ وأنا لم أقل ذلك،
  ثم وقع هو في صفّ السلسلة (`1528eb8`) فأصلحتَه بأربعة أرقامٍ مثبّتة. **الانحيازُ الذي قِسته يبقى قائمًا** (v2 يتقدّم خارج الحزمة على مقياس السلسلة ٣٤→١٤)،
  لكن **مصدرَ اللبس كنتُ أنا** — والدرسُ في `verification-discipline`: لكلّ صفٍّ اسمُ مقياسه. · **مُثبت**
- صحّةُ الأرقام الماليّة نفسِها، وأيّ مبلغٍ حقيقيّ.
- F4 (نشرُ الأدلّة ليرى CI) · F5 (أرقامُ الإعلان في `claims.json`) · ح١٣ · ح١٤ · ح١٥ · ح١٦ — قراراتُك، ولها موضعُها في التقرير.
- «الحادثة» (`reset` نحو `origin/main` عند 14:04) — لم أُعِد فتحَها؛ أُثبتُ فقط أنّي لم ألمس الشجرةَ المشتركة في هذه الجولة (كلُّ قياسي في نسخةٍ مستنسخة).
- هويّةُ الالتزامات: `flutter-state-engineer <swarm@hermex.local>` في **٢٢٣** التزامًا على `origin/main` ومنها راجعاتي — عطبُ `.gitconfig` لا عطبُك؛ و`2a25b02` جاء بها لأنّه التزامٌ من جلسةٍ استعملت الإعدادَ العامّ بعد أن كانت الالتزاماتُ السابقة `Sulaiman <sulaiman@local>`. أُعلن ولا أُطالبك به.

## Not proven — ما لم يُثبت

- لم أُشغّل المقاعدَ بعد الإصلاح (وأُقرّ بحدّها كما أقررتَ) — ولا ادّعاءَ عندي عن بنود المقاعد الثلاثة بعد `2a25b02`.
- `compare_prompts` لم أُجرِ فيه نداءً مدفوعًا: قياسي `--budget 0` (العيّنةُ تُطبع قبل الصرف) — كفيلٌ بالاستثناء لا بالأداء.
- `--exclude-pack` مع `--limit`/`--adopt-legacy` في تشغيلةٍ حقيقيّة · ثباتُ التقاطع عبر تشغيلات · end-to-end على خدمةٍ مُعاد تشغيلها.
- العددُ «٥٢٤» بالمعنى المُلتزم: في نسختي (بلا مفتاح) ٥١٢ ✓/٩ ✗، فالأرقامُ المعلَنة خاصّةٌ ببيئةٍ فيها المفتاح.
- (C)(D)(F) قِيست على صفحة إحصاءٍ واحدة (2) — أُعمّم بحكم البنية (لا محتوى الإحصاء في أيّ حكمٍ ثلاثة) ولم أمسح الخمسين كلَّها.

{المُدخلات: 3 مقاعدَ مسلَّمة + إصلاحان (1528eb8 · 2a25b02) + إعلانُ القرار (أ)} → {البرهان: ٣٥ قياسًا مستقلًّا في نسخةٍ منفصلة — منها 6 تخريبات على `--verify`، ٢ تشغيلُ حذفٍ حقيقيّان (٢٨٣→٠)، ٣ تشغيلُ التقاط (منها worktree وبدون git)، وحقنُ تعريفٍ مكرّر يسقط الحارس} → {الحالة: REQUEST CHANGES · R45-2/3 مُغلقان فعلًا · R45-1 مُغلقٌ في العضويّة لا في المحتوى · وإغلاقُ القرص يحتاج بوّابة}
