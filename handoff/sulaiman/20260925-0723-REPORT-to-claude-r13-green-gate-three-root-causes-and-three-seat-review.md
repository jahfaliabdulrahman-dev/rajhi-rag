```
id:      20260925-0723-sulaiman
from:    sulaiman
to:      claude
type:    REPORT
step:    إغلاق R13: بوّابةُ التسليم ٣/٥ ⇒ ٥/٥ — ثلاثُ عللٍ جذريّة · ومراجعةُ ثلاثة مقاعد · وبندُ «بينما ننتظر الجامعة»
commit:  aeed92d
```

# R13 — البوّابةُ الحمراءُ التي صار الأحمرُ فيها عاديًّا

**السؤالُ المُوجَّه من المالك:** «بينما ننتظر ردّ الجامعة، ما الخطوةُ التالية التي نستطيع تنفيذها؟»
**والجوابُ المُنفَّذ:** إغلاقُ **R13** (رافعة 36 — ثاني أعلى رافعة بعد R3 المنتظِرة): عطبٌ حقيقيّ، صفرُ
تكلفة، **بلا أيّ قرارٍ مطلوبٍ من المالك**، ويُصلح ما يحرس كلَّ تسليمٍ لاحق.

## ١ — العلّةُ الجامعة (Qass Al-Haq)
ورشةُ التطهير (الالتزام `c7b5483` · 2026-09-22 22:29) أبدلت قيمًا في **ملفّاتٍ متتبَّعة** بقيمٍ **لا من
المصدر** (مقيس: `4331.11` و`2980.59` **خارج** مجموعة بصمات المصدر الـ٤٠٩٥). ففقدت البوّابةُ **أرضيّتَها**،
وبقيت `[FAIL]` حتى صار الأحمرُ عاديًّا (إلى 2026-09-25 07:03 — **≈٢٫٤ يومًا**، لا «ستّة» كما كتبتُ أوّلًا).

## ٢ — العللُ الثلاث وأدلّتها
| # | العلّة | الدليلُ المقيس | العِلاج |
|---|--------|----------------|---------|
| أ | ثابتُ `parser goldens`: `35832.43` لمفتاحٍ يُحلّل إلى `61165.12` | الوثيقةُ `arabic_digit_parser.py:19` **وضابطٌ ناجحٌ في المجموعة** (`tests/test_parser.py`) يشهدان بالقيمة الصحيحة ⇒ نسختان من الحقيقة | `PARSER_GOLDENS` موضعٌ واحد + ضابطٌ يقابل الجدولَ بالمُحلّل |
| ب | ثابتا الصفّ الأوّل في ص١٠ لا يُغلقان السلسلة (`٦٧٦` + الأوّل ≠ الثاني) | رندرُ الصفحة ١٠ قُرئ **بصرًا** فوافق السلسلة؛ والقراءةُ الحيّة أعطت ١٠٤ صفوف وأوراكلَ فوتر 10/10 | حكمٌ **حسابيّ** (رصيدُ الصفّ = ختامُ ص٩ + دائنُه) بدل رقمين محفوظين |
| ج | قفلُ الأنواع على مبلغٍ ثابت: **صفرُ صفوفٍ** في العيّنة | قراءةٌ مباشرةٌ للخدمة على العيّنة: ١٠٤ صفوف، ولا صفَّ بالمبلغ الثابت | يُكتشف المبلغُ المتكرّر **حسابًا** (≥٤ صفوف) ثمّ تُقابَل أنواعُه الأربعة **بالاسم** |

**والأدلّةُ النهائيّة (على الشفرة المُصحَّحة):** `tools/qa_gate.py --full` ⇒ **`GATES: 5/5`** (ثلاث مرّات
متتالية: ٥/٥ · ٥/٥ · ٥/٥) · `pytest tests/ -q` ⇒ **730 نجحت** · `amount_guard --ci` PASS ·
`static_gate` PASS · `render_claims --check` ✓ · `guard_bite_sweep` ⇒ **15/15** (كان 14/14).

## ٣ — مراجعةُ ثلاثة مقاعد مستقلّة (والكاتبُ لا يراجع نفسَه)
`deleg_c5e95877` — المدى المُثبَّت `542a571..HEAD` · مقاعدُ معزولة · قراءةً فقط:
**المعايير: APPROVE WITH FIXES · المواصفة: APPROVE WITH FIXES · البنية: APPROVE WITH FIXES.**

أهمُّ ما اصطادته — **ادعاءاتٌ في نصّي تخالف مصادرها** (وهذا صنفُ العلّة الذي وُلد السجلُّ لإنهائه):
1. **«يفشل في الـCI» كان وعدًا بلا مصدر** (عالية): `tests/test_parser.py` لم يكن في قائمة الـCI، ولا خطّافُ
   الدفع يشغّل pytest ⇒ **صُيّر الوعدُ صحيحًا**: أُدرج في الـworkflow وفي `docs/GATES.md` (والاتجاهان
   يُقابَلان تساويًا في `tests/test_gate_registry.py` ⇒ 12 passed).
2. **لا سمَّ للقفل الجديد** (متوسطة): أُضيفت حالة **م١٢** في `tools/guard_bite_sweep.py` ⇒ **15/15**.
3. **الضابطُ يسقط مُفردًا** (عالية): `.venv/bin/pytest tests/test_parser.py` ⇒ `ModuleNotFoundError`
   (سقوطٌ بيئيٌّ يُقرأ حكمًا على المُحلّل) ⇒ تهيئةُ مسارٍ صريحة ⇒ 13 passed مُفردًا.
4. **«القاعدة ١٤» لا وجودَ لها** في المستودع؛ الصحيحُ **القاعدة ١٢** («لا يُعفى موضع، تُغيَّر القيمة» —
   `tools/amount_guard.py:539`)، و**الحارسُ كان أعمى** عن الثابتين القديمين فلا يُنسب إليه إبقاءُ الأحمر.
5. **المدةُ والعددُ:** ≈٢٫٤ يومًا لا ستّة · والملفّاتُ أربعةٌ لا خمسة ⇒ صُحّحت في الوثائق وفي رسالة الالتزام.

**ودرسٌ أمنيٌّ مقيس (وقعتُ فيه مرّتين):** كتبتُ قيمتَي الصفّ الحقيقيّتين نصًّا ⇒ سقط ضابطُ السقاطة
(`test_the_baseline_cannot_be_raised_by_its_own_debtor`)، ثمّ كتبتُ إحداهما **في تعليق** فعاد الدَّينُ
(`1 ظهورًا في 1 ملفًّا`). وبصمتُهما **داخل مجموعة المصدر** (تُعاد بالقياس) ⇒ أي أنّ القاعدة ١٢ منعت
**مبلغين حقيقيّين** من الدخول إلى ملفٍّ متتبَّع. **والحلُّ الصحيحُ ليس رقمًا آخر، بل أن يُقاس حسابيًّا.**

## ٤ — حدودٌ مُعلَنةٌ باقية (لم تُغلَق — ولا أُعلنها مُغلَقة)
- مقامُ أوراكل الفوتر يُسقِط الصفحاتَ غيرَ القابلة للتحقق **بلا وسم** (`MIN_FOOTER_COMPARABLE = 9`) ⇒ ثقبٌ
  ضيّقٌ يُوصى بإغلاقه باشتراط `f_possible == عدد الصفحات`.
- مفتاحُ كشف «المبلغ المتكرّر» **نصّيّ** لا قيميّ ⇒ صيغُ الكتابة تُفرّقه (والفشلُ **مغلقٌ**: أحمر لا أخضر).
- `tools/audit_types.py:93` ما زال يقفل على مبلغٍ أبدلته ورشةُ التطهير ⇒ **قياسٌ ميّت** يُصلَح أو يُنزَع.
- **القراءةُ غيرُ حتميّة** بمقياسٍ مُعلَن: تشغيلٌ بمشتبهٍ واحد، والتالي بصفر؛ وفي هذا التشغيل ظهر
  «الترقيم: متسلسل في المقروء (9/10)» بدل 10/10 — بلا أثرٍ على الفوتر ولا على الشكوك. **يُعالَج بقرارٍ
  (إعادةُ القراءة أو عتبةٌ مُعلَنة) لا بتخفيض الحكم.**
- **والأثرُ المطلق** للصفّ الأول في ص١٠ يعتمد اليوم على ختام ص٩ + أوراكل الفوتر، لا على ثابتين مطلقين.

## ٥ — خطُّ الرجوع (ثلاث درجات)
① لا شيءَ هبط على `main` · ② `git reset --hard 542a571` على هذا الفرع + `push --force-with-lease`
③ `git push origin :chore/round56-evidence`. والمدى المُراجَع: `542a571..aeed92d`.

## ٦ — الخطوةُ التالية (والقرارُ للمالك في واحدةٍ فقط)
① **إصلاحُ اللاحتميّة** (لاحتميّة القراءة) — لا ينتظر قرارًا · ② **R1: نسخةٌ احتياطيّة مشفَّرة** لـ`data/`
(المقيس: **358M** من أدلّة القضيّة، ولا نسخةَ موثّقة) — تنتظر **وجهةً** · ③ **قياسُ العموميّة** (٣٠ صفحةً من
بنكٍ ثانٍ وبوّابةُ ≥٩٠٪) — تنتظر ملفًّا · ④ **أ-٥ (القلبُ يقرأ العقد)** — تنتظر كلمةَ المالك على تجميد
الميزات حتى 2026-10-17.

**ما لم يُثبت:** أنّ `data/` بلا نسخةٍ احتياطيّة أصلًا (Time Machine لم يُفحَص) · أنّ اللاحتميّة من النموذج
لا من المستند (لم يُفصل بينهما) · ولم يُقَس التشغيلُ الشاملُ الخامسُ إلا على شفرةٍ انتهت قبل التزام التصحيح
(والبوّابةُ الخامسةُ لم تُعَد بعده — وإن كان منطقُها لم يتغيّر).
