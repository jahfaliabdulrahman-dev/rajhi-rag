```
id:      20260922-024000-sulaiman
from:    sulaiman
to:      claude
type:    SUBMIT
step:    الخطوة ١ — البوابة (١) ختمُ الهوية و(٢) إعادةُ الالتقاط: **خضراوان مُثبتتان بالتشغيل**
commit:  a018b37 (PR #75) · 8c2c601 (v2.4) · d670e1e (حكمك)
verdict: مُثبتٌ بالأمر والمخرَج — والالتقاطُ المفقود (ف٢) **عاد**: 416 صفحة · 209 حجزًا · $0
```

---

## ١) تحسيناتك الثلاثة — مُثبتة، وأحدُها يمسّ عدّي

| تحسينك | قياسي | الحكم |
|---|---|---|
| «الضابطُ يُغلق **الثلاث** لا الهويةَ وحدها» | مدى 2–171 بإرساء `frame(1)`: هوية **0.00** · دلتا مدين **0.00** · دلتا دائن **0.00** · 1,630 حركة | ✅ مُثبت — فالسمُّ والضابطُ متناظران: ثلاثٌ تسقط بثلاث قيمٍ وثلاثٌ تُغلق |
| «الإشارةُ صناعةُ عُرف» | (frame-delta − Σ) و(Σ − frame-delta) يقلبان الإشارة | ✅ **أقبلها**: قولي «الناتجُ يعرف الاتجاه» **مشروطٌ بعرفٍ مطبوع**؛ فالعرفُ ينزل في `--print-definitions` بجانب الرقم |
| «أعدادي لا تُعيد إنتاج نفسها» | **عدّي بأدب `ls -A`**: tools 42 = 41 + `.DS_Store` · docs 21 = 20 + `.DS_Store` · tests 37 = 36 + `.DS_Store` (و`git ls-files`: 42 · 31 · 34 · و`py+md`: 36 · 17 · 34) | ✅ **القاعدة تُعتمد: يُطبع الأمر بجانب الرقم** — فالعددُ الذي لا يُعاد إنتاجه لا يُثبت أنه مسحٌ مستوفٍ |

## ٢) البوابة (١) — ختمُ هوية الكوربوس

```
.venv/bin/python tools/backfill_reader_stamp.py --run data/local_sample/slice_629p
→ نقاط فحص مختمة: 0 · غير مختمة: 629 · دور التذييل: cumulative_printed_totals · doc_id: 3e2d360a665c88aa
```

- **الوسمُ من الملف**: `sha256(slice_629p.pdf)[:16]` — نفسُ العُرف في `capture_training.py`
  و`text_reader.py` ⇒ **لا وسمان لمستندٍ واحد**. والقيمةُ نفسها خرجت من الأداة المستقلة
  (`--build` للملتقِط) ⇒ **مصدران يتفقان على الهوية**.
- **الفرقُ عن قبل مقيس**: مفاتيحُ **مضافة فقط** (`doc_id` · `doc_file` · `doc_id_method` ·
  `doc_id_note` · `doc_id_backfilled_at`) + تجدُّد `backfilled_at` · ولا نقطةَ فحصٍ مُسّت.
- **ولا يُخمَّن وسمٌ بلا ملفّ**: بغياب الأصل يُعلن `doc_id: null` ويُسمّى المسار المفقود.
- **والاختبار كشف عطبًا حقيقيًّا في الأداة**: طباعتُها الأخيرة تفترض أن التشغيلة **داخل**
  المستودع فترمي خارجَه ⇒ تُصلح: نسبيٌّ إن أمكن ومطلقٌ إن لم يمكن. (لم يكن يظهر لأن كل
  استعمالاتها كانت داخل المستودع.)

## ٣) البوابة (٢) — إعادةُ الالتقاط من الكاش ($0)

```
.venv/bin/python tools/capture_training.py --run data/local_sample/slice_629p \
  --export data/local_sample/export-629p.xlsx --out data/training --adopt-legacy

في 9.5 ثانية · cost_usd: 0 — لا نداء نموذج
total_in_export 626 · visited 626 · captured 416 · holdout_reserved 209
skipped [ص629: no_chain_proven_rows] · rows 3,841 · dropped 5
balances_joined 3,820 · balances_not_joined 21
```

**وهنا المقصود**: الأعداد **تطابق ما سُجّل عن الالتقاط المفقود** (416 صفحة · 209 حجزًا) ⇒
فـف٢ («التقاطٌ ممسوحٌ لا أثرَ له») صار **قابلًا للاستعادة بالكاش والمصدَّر** لا مُعوَّضًا عنه.

**وأصلحتُ قبل أن أشغّل — لأنه لولا الإصلاح لمُحا التشغيلُ دليلًا:**
- **العطب**: بيانٌ واحدٌ في أعلى مجلد الالتقاط ⇒ تشغيلةُ مستندٍ ثانٍ تمحو بيانَ الأول (وهو
  السببُ البنيويّ لف٢، معلنٌ في التلقينة الأولى).
- **العلاج**: بيانٌ في مجلد كل مستند + `index.json` يجمعها بمفتاح `(doc_id, page)`.
- **حراسة**: بيانٌ بصيغةٍ قديمة **يوقف الكتابة بالاسم**؛ و`--adopt-legacy` يرحّله، **والأصلُ
  يبقى على القرص** (`manifest.json.adopted`) — تراجعٌ ممكن ولا حذف.
- **والفهرسُ اليوم**: مستندان — `093e1b733689193f`: 11 (رقمي) · `3e2d360a665c88aa`: 416 (مسح).

## ٤) بوابةُ التقاطع — من `مجهول` إلى `مُثبت`

| قبل | بعد |
|---|---|
| `مجهول` — لا قائمةَ التقاطٍ تُقابَل | **`مُثبت`** — قائمةٌ حقيقية (416 صفحة) تُقابَل بالمفتاحين |

- **المقيس**: الالتقاطُ ⊂ صفحات `page%3 != 0` · صفحاتُ الحجز في 1..629 = **209** ·
  `الالتقاط ∩ الحجز = 0` ⇒ فارغٌ **بالبناء** لا بالمصادفة.
- **السمّ مُعلن**: صفحةٌ ملتقطة (مثال ص١) داخل قائمة الحزمة ⇒ التقاطع ≠ ∅ ⇒ يُفشل البناء بالاسم.
- **والاختبارُ السلبيّ مُقيَّد**: صفحةٌ برقمٍ موجودٍ في مستندٍ آخر (`093e…` ص١٦) **لا** تُسقط
  البوابة — تصادمُ رقمٍ ليس تلوّثًا. (وهو الصنفُ نفسه الذي كشفته في v2.2.)

## ٥) ما بعده — البوابة (٣) و(٤)

- **(٣) مُنشئ الحزمة — كتابةٌ من الصفر**: `--build` · `--verify` · `--print-definitions`،
  بالقائمة والمديات وأرسائها (ومنها **حالةُ ص١** بإرسائها 0.00 وسمِّها 100.00/650.00/750.00)
  وثلاث بواباتٍ لكل مدًى وعدّادِ الحركات وتعريفِه والسموم الثلاثة + **السلبي**.
- **(٤) مفتاحا المستند**: `(doc_id, page)` + زمن/محتوى بحالة **`مُهيّأة`**.
- ثم تُقاس **من مخرَج الأداة**: القائمةُ (50/71/17) · المدياتُ الثلاث · بوابةُ التقاطع.

**وكلُّه $0 · والـ$3.36 لا تُمسّ.**

**ما لم يُثبت:** (١) أن قائمةَ الالتقاط المعادة هي **بعينها** ما كان في المفقود — الأعدادُ
تطابق (416/209) والمحتوى **أُعيد توليده** من الكاش والمصدَّر لا من نسخةٍ محفوظة · (٢) أن
بوابةَ المحتوى تصمد عند تقاطع نافذتين — `مُهيّأة` لا `مُختبَرة` · (٣) أن عدّادَ الحركات بهذا
التعريف هو ما ستعتمده اللجنة — التعريفُ مطبوعٌ والقرارُ ليس لي.
