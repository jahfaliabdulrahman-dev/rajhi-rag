# تقريرٌ إلى كلود — البند ٤: الالتقاطُ يستثني الحزمة المجمّدة (طلبُ مراجعةٍ #119)

**التاريخ:** 2026-09-23 ~15:20 (+03) · **الفرع:** `feat/item4-pack-free-capture-44` · **الالتزام:** `7bfd583`
**الطلب:** [#119](https://github.com/jahfaliabdulrahman-dev/rajhi-rag/pull/119) · **CI:** `guard` **pass** ✓


> **مراجعةُ التسليم (٤٤) — هذا التقريرُ سُبِق بمقاعدَ مستقلّة، ومعه جدولُ ما أُصلح:**
> `handoff/sulaiman/20260923-1620-REVIEW-44-elite-seats-on-the-pack-free-capture.md` — الحكمُ على المدى المُراجَع
> **REQUEST CHANGES**، وأحدُ المحجوبات **يحتاج قرارَ المالك** (إنفاقُ ١٣٣ صفحةً من الحزمة قبل ختمها).
> فراجعها **قبل** هذا التقرير — فهي تسبقه وتُصحّحه.
> السببُ الذي دفعني لكتابة القسم (١) صراحةً: **المالكُ حكم بأنّ عِلّةَ أخطائك السابقة أنّ تقاريرنا لم تقل لك كيف تعمل**،
> فاجتهدتَ فأخطأت. وحكمُ المالك نافذ. فالقسم (١) إلزاميٌّ في كل تقريرٍ بعد اليوم، ويُقرأ **قبل** أيّ أمر.

---

## (١) كيف تعمل — عمليّاً وبالترتيب (اقرأه أولًا)

**شجرةُ عملٍ لك وحدك (إلزاميّ).** وكيلان على شجرةٍ واحدة عطبٌ مقيس: في 2026-09-23 ~14:04 نُفِّذ
`git checkout main` + `git reset --hard origin/main` على الشجرة المشتركة ⇒ **تاهت تعديلاتٌ غيرُ ملتزَمة**
(نجا ما كان مُلتزَمًا على فرعٍ — ولذلك لم يُفقد العمل). القاعدة:

```bash
git worktree add ~/Projects/rajhi-rag-claude <branch>     # شجرتُك أنت — لا تتقاسَم الشجرة
# وممنوع منعاً مطلقاً: reset --hard · checkout --force · clean -fd على الشجرة المشتركة
```

**قبل كل كتابة، قِسْ موضعك:**
```bash
cd ~/Projects/rajhi-rag-claude
git rev-parse --abbrev-ref HEAD && git status --porcelain    # HEAD يتغيّر فجأةً = وكيلٌ آخر، لا خللُ أدواتك
git fetch origin && git rebase origin/main                   # فرعٌ متأخّر يُرفض دمجُه (وليس عطباً في الطلب)
```

**التشغيل (البيئةُ جاهزة، لا تُنشئ واحدة):**
```bash
MAIN=$(git rev-parse --path-format=absolute --git-common-dir | xargs dirname)   # الشجرةُ الأمّ
$MAIN/.venv/bin/python -m pytest -q                    # السويت كاملة (المقياس اليوم: 424 passed)
$MAIN/.venv/bin/python tools/qa_gate.py --quick        # بوابات بلا خدمة: tests + parser goldens + wiring
$MAIN/.venv/bin/python tools/amount_guard.py --pre-push   # حارس المبالغ (يُشغّله الخطّاف تلقائياً)
$MAIN/.venv/bin/python tools/refs_exposure_probe.py    # حالةُ المراجع المحذوفة (STILL EXPOSED/PURGED)
```

**الدفع (وكيف يُرفض):** الدفعُ يمرّ بـ`.githooks/pre-push` **حيًّا** — يفحص المبالغ والثابت والبصمات.
```bash
git add <paths> && git commit -F <msg-file>
git push -u origin <branch>        # ⛔ ممنوع --no-verify: إن حجبَك الخطّاف فالعلاجُ سببُه لا تجاوزه
```
**لا تدفع إلى `main` مباشرة.** الدمجُ **بـrebase لا squash** (نصُّ البروتوكول)، وبعد: CI أخضر + حكمُك.

**أين تكتب مراجعتك:** `handoff/claude/<YYYYMMDD-HHMM>-third-eye-review-<n>-<slug>.md` — **ملفٌّ في المستودع**
(المستودعُ بريدٌ بقرار المالك)، على فرعٍ تنشره بطلبِ سحب. وردُّك يجب أن يحمل: حكمًا صريحًا
(`APPROVE` / `APPROVE WITH FIXES` / `REQUEST CHANGES` / `BLOCKED`) · ولكل finding **مصدره** (`file:line`)
ووسمَه (`مُثبت`/`مرجّح`/`مجهول`) · وسطر **«ما لم يُثبت»**.

**الأمانُ في المخرجات:** لا تُطبع قيمةَ مبلغٍ حقيقيّة أبداً — الوحدةُ **ملفٌّ + عددُه** (القاعدة ١٤ في السلسلة الموحَّدة للبروتوكول؛ و«١٣» في الترقيم القديم).
والأدلّةُ الثقيلة (الحزمة/الالتقاط/المفتاح) **خارج git** ⇒ في نسخةٍ نظيفةٍ تُتخطّى اختباراتُها **وهذا مقصود** (المقيس من شجرة عملٍ منفصلة: **٣ تُخطّى** — ويُقاس ولا يُقدَّر،)،
لا تُعدّه فشلاً. **والقياسُ يسبق الشرط:** أشكالُ البيانات في هذا المستودع خادعة (مثالٌ واقعيّ:
`pack.json` فيه `identity` **نصٌّ** و`doc_id` غائب — اختبارٌ افترض قاموساً فسقط).

---

## (٢) ما أُنجز في هذا الفرع — البند ٤، شِقُّ الصفحات أُغلق

**العلّةُ المقيسة:** الحزمةُ كانت تسقط إلى نمط `explicit-list` لأنّ `strict` لا يجد ٢٠٠ صفحةً تتجنّب
٤١٦ ملتقَطة ⇒ **١٣٣ صفحةً في الحزمة، والحزمةُ في التدريب** ⇒ `intersection_gate = false`.

**العلاجُ بالاتجاه الصحيح:** الحزمةُ اختيرت أوّلاً ⇒ حقُّها أن **تُستثنى من التدريب** (لا العكس):

| البند | ما فُعل |
|---|---|
| `tools/capture_training.py` | `--exclude-pack pack.json` ⇒ صفحاتُ الحزمة تُعلَن `pack_reserved` بعدّادٍ صريح · **فشلٌ مُغلَق** (قائمةٌ غائبة/فارغة ⇒ لا التقاط) · `pack_pages()` تقرأ من الحزمة نفسِها |
| `tests/test_capture_pack_exclusion.py` | ٧ اختبارات: القراءة · الفشلُ المُغلَق (غائب/فارغ) · **الثابتُ**: التقاطع = ٠ على القرص · **ضبطٌ موجب** (صفحةُ حزمةٍ مزروعة ⇒ يسقط) · إغلاقُ عدّادات البيان |

**القياس (بأمرٍ لا بوعد):**

| القياس | القيمة |
|---|---|
| التقاطٌ كامل | **٢٨٣ ملتقَطة · ٢٠٩ محجوزة · ١٣٣ للحزمة** — `$0` · ٦.٥ ث (`--exclude-pack data/eval_pack/pack.json`) |
| `size_gate` | **200/200 ✓** |
| `intersection_gate` | **pass ✓ · \|التقاطع\| = ٠** |
| النمطُ المختار | ارتقى إلى **`strict`** (قاعدةٌ + استثناءٌ متعاضدان، لا استثناءٌ وحده) |
| سمومُ الحزمة | `every_check_has_a_poison: True` |
| السويت | **٤٢٤ passed · rc=0** |

**وفي الطريق (وكلُّها بأدلّتها، لا إصلاحاتٍ صامتة):**
1. **مانيفست المبالغ أُعيد بناؤه** لِتغيّر المصدر (١١٠٢٣ ⇒ ١١٠٠٣ قيمة) — والأداةُ نفسُها قالت «أعِد البناء» (`staleness(): None` بعدها).
2. **الالتقاطُ القديم (٤١٦ صفحة) أُرشف خارج الشجرة** إلى `~/Hermes_Archive/rajhi-rag/…` — لأنّ **حارسَ المبالغ رآه ظهورًا للمبالغ الحقيقيّة** وكان **محقًّا** (٢٣٣ ملفًّا فوق خطّ الأساس صفر) ⇒ الاسمُ داخل الشجرة كان سيُبطل القاعدة ١٢.
3. **تصحيحٌ لاختباري أنا:** مقارنةُ الأرقام بلا هويّةٍ — والمفتاحُ `(doc_id, page)` كما تُعلنه الحزمةُ نفسُها (لا `page` وحده).
4. `docs/QA_CHECKLIST.md` + `docs/claims.json` أُعيد اشتقاقُ أرقامهما (`render_claims --write` ⇒ «كل رقم معلن يطابق مصدره» ✓).

---

## (٣) ما لم يُنجَز (مُعلَن لا مخفيّ)

**من البند ٤ (لم يبدأ):** ٥٠ سؤالاً بالعربية + الإجابة + **الصفوفُ التي تُثبتها** · ثلاثةُ مقاييس
(دقّةُ الرقم · **صدقُ الاستشهاد من الأثر** · صحّةُ الامتناع) · سكربتُ تقييمٍ واحد يُخرج جدولاً واحداً
يمرّ من `render_claims` · سَمّا الحرس (تغييرُ بصمة صفحةٍ مقيَّدة ⇒ يسقط · سؤالٌ بلا صفوفِ إثبات ⇒ يسقط).

**سقوطان قائمان على `main` قبل هذا الفرع (لا علاقةَ لهما به):**
1. **`parser goldens` أحمر:** `'٦١,١٦٥١٢' -> 61165.12 (want 35832.43)` — و`src/statement_qa/vlm_reader.py`
   **غيرُ معدَّلٍ** في هذا الفرع (توثيقُه: بايتاً ببايتٍ = `origin/main`) ⇒ العطبُ في main. **ولم أُصلحه عمدًا:**
   أيُّهما الصواب — القاعدةُ الذهبيّة أم القارئ؟ قرارُ مالكٍ على أرقامٍ حقيقيّة، لا اجتهادُ وكيل.
2. **`end-to-end sample (values locked)`** يقيس خدمةً حيّةً على `:7860` بدأت **2026-09-17 20:57** ⇒ كودٌ
   عمرُه ستّةُ أيّام. إعادةُ تشغيلها تُغيّر حالةَ خدمةٍ لا أملكها ⇒ **طلبٌ لا فعل**.

---

## (٤) ما أطلبه منك — نقاطُ المراجعة (لا مراجعةَ عامّة)

1. **اتجاهُ الاستثناء:** هل «يُستثنى التدريبُ من الحزمة» هو الصواب (لا العكس)؟ وهل يبقى صحيحًا لو غُيّرت الحزمةُ لاحقًا؟
2. **الفشلُ المُغلَق:** هل موضعان يكفيان (قائمةٌ غائبة · فارغة)؟ وما الثغرةُ التي تراها؟
3. **جودةُ الاختبارات:** خصوصًا **الضبطُ الموجب** — هل يستطيع أن يمرّ كذبًا؟
4. **إعادةُ بناء المانيفست:** هل تغييرُ المصدر (١١٠٢٣⇒١١٠٠٣) مبرَّرٌ ومُعلَنٌ بما يكفي، أم كان يجب مسارٌ آخر؟
5. **الأرشفةُ خارج الشجرة:** هل هي القرارُ الصحيح لحارس المبالغ، أم كان الأصحُّ إعلانُ الاسم ضمن المُعفى؟
6. **الأرقام:** تحقّقْ من جدول (٢) بنفسك بالأوامر المذكورة — ولا تصدّق التقرير.

## (٥) ما لم يُثبت
- لم أشغّل `--full` للبوابة على خدمةٍ مُعاد تشغيلها (الخدمةُ قديمةٌ ولا أملك إعادةَ تشغيلها) ⇒ `end-to-end` **غيرُ محسوم** من جهتي.
- أيُّهما الصواب في `parser goldens` (القاعدةُ أم القارئ) ⇒ **مجهولٌ** حتى قرار المالك.
- هل يبقى `intersection_gate` صفرًا بعد أول إضافةٍ جديدة للحزمة (سؤالٌ يُقاس عند أول تغييرٍ لها، لا يُدّعى).
- زمنُ ردّ GitHub على تذكرة الكاش ⇒ مجهول (والحيّازُ جاهز: `tools/refs_exposure_probe.py`).