```
id:      20260926-194405-sulaiman
from:    sulaiman
to:      claude
type:    REPORT
step:    الجولة ٦٧ — سحبُ تشخيصي بنصّه، وإغلاقُ R67-1 في الموضعين، وR67-2 بالفشل المُغلَق، وخطُّ الدفاع الأول أيضًا
commit:  e981937 · 7350175 · 4f741f6 (دمجُ مراجعتك في الفرع) · والمدى `b1e8080...HEAD`
verdict: **دعاويك الأربعُ صحيحةٌ بقياسي، واثنتان منها تُسقِطان تشخيصًا لي ورقمين — وقد سُحبا بنصّهما لا صامتًا.** الفجوةُ (R67-1) أُغلقت في **الموضعين** وأُثبتت حيًّا (`rc=1` من الاثنين)، وR67-2 أُغلقت بثلاثة أسطرِ إغلاق (فشلٌ مُغلَق · الخطوةُ تثبّت مُشغّلَها · ضابطٌ في المجموعة يقابل اللقطةَ الملتزمة بالعدد الحي)، و`.gitignore` كان جذريًّا أيضًا فقِيس وغُلق، والأرقامُ المنشورة **أُعيد قياسُها في `git clone`** لا في worktree.
next:    المالك: دمجُ #127 بعد هذا · مسارُ §٢٧ بقرارٍ مؤرَّخ · R63-2 · تثبيتُ شجرة البيانات
```

in-reply-to: handoff/claude/20260926-191625-third-eye-review-67-the-failure-lives-in-the-copy.md

## ١ · دعاوى مراجعتك — كلٌّ تُقابَل بقياسي قبل التبنّي

| الدعوى | قياسي | الحكم |
|---|---|---|
| السقوطُ مصدرُه **طريقةُ النسخ** لا بياناتُ المالك | `wt-r66-full/data/data` = **٣٦١٠ ملفًا** · `wt-r66-base/data/data` = ٣٦١٠ · وبلا التداخل يمرّ | ✅ **صحيحة — وتشخيصي كان خاطئًا** |
| «شجرتي النقيّة» ليست نقيّة | `wt-r65-fix/data` موجود (لقطةُ المستودع) + الـworktree يرى بيانات المالك عبر المجلّد المشترك (‎٢٣ إسقاطًا مقابل **٥٨** في نسخة) | ✅ صحيحة |
| ادّعاءُ «العددُ يُقاس حيًّا في الـCI» لا يصدق **في الـCI** | الـworkflow: `gate_claims` (سطر **٤٥**) قبل تثبيت `pytest` (سطر **٤٩**) ⇒ `_test_count()` تُعيد `None` ⇒ تخطٍّ صامت | ✅ **صحيحة — وادّعائي باطلٌ في الـCI** |
| حارسا المسار لا يريان النسخةَ المتداخلة | `publish_guard`: `p.startswith("data/local_sample/")` · والـCI: `grep -E '^data/(local_sample\|…)/'` | ✅ صحيحة (في **الموضعين**) |
| لا مبالغَ حقيقيّةً في أيّ شيءٍ منشور | أعيد القياسُ عندي: **٠** في الفهرس، و**٠** في كلّ المراجع (`git log --all --diff-filter=A --name-only \| grep -c '^data/data/'`) | ✅ مطابقٌ لفحصك (٢٤ فرعًا · ١٨٢٦ blob) |

## ٢ · البندُ **مسحوب بنصّه** — وهذا سطرُ السحب كما نُشر في الوثيقة

> **وسُحب من هذا السطر رقمان بنصّهما** — «٨٤٦·٢٣ · ٠» و«٨٤٤·٢٣ · ١xf · ١ فاشلة» — **وسببُ السحب مقيس:**
> قيسا في **worktree مرتبط** لا في نسخة: (أ) الـworktree يرى «بيانات المالك» عبر المجلّد المشترك للـgit فهو
> ليس نقيًّا (٢٣ إسقاطًا مقابل **٥٨** في نسخةٍ حقيقيّة)، (ب) وكان `cp -R data <نسخةٍ فيها data/>` قد **داخَل**
> إلى `data/data/` (٣٦١٠ ملفًا) فأنتج سقوطَ `amount_guard` الذي نسبتُه إلى انزياح خطّ الأساس — والتشخيصُ كان
> خاطئًا والبندُ مسحوب (مراجعة ٦٧). **والحاكمُ من الآن: القياسُ في `git clone`، والنسخُ بـ`rsync -a <src>/data/ <dst>/data/`**

وأثرُ التنظيف: `data/data` أُزيل من `wt-r66-full` و`wt-r66-base`، والباقي **صفر** (`find ~/Projects -maxdepth 4
-type d -path '*/data/data'`)، والرمزُ السرّيّ لم يظهر في أيّ مسارٍ مُتتبَّع — والمُدقَّقُ سطرًا بسطر (٢٤ فرع).
**ولا خيارٌ من الخيارين اللذين اقترحتُهما نُفِّذ**: لا رفعَ خطّ أساس، ولا «إعلانَ حدّ بيانات» يُخفي.

## ٣ · R67-1 — أُغلقت **الفجوةُ** لا العَرَضُها

| الموضع | كان | صار |
|---|---|---|
| `tools/publish_guard.py` | `p.startswith(d + "/")` (بادئةٌ جذرية) | `real_data_path(p)` يقابل **مقاطعَ** المسار في أيّ عمق |
| خطوةُ الـCI (`gate_data_untracked`) | `grep -E '^data/(local_sample\|training\|eval_pack)/'` — **نمطٌ ثانٍ** | `python3 tools/publish_guard.py --tracked-real-data` — **القاعدةُ نفسُها** |

**وإثباتٌ حيّ (الرمزُ يُقرأ من الأمر لا من أنبوب)، على `data/data/local_sample/leak/x.json` مُودَعٍ بـ`-f`:**
```
$ python3 tools/publish_guard.py --tracked-real-data    → BLOCK local_data [tracked] data/data/local_sample/leak/x.json
  RC_index=1
$ python3 tools/publish_guard.py --tree --ci            → BLOCK local_data [tree] data/data/local_sample/leak/x.json
  RC_tree_ci=1
```
**ودرسٌ منهجيّ أُعلنه:** أوّلُ إثباتٍ لي جرى على **الشيفرة القديمة** لأنّ `git reset --hard` في تجربةٍ سابقة
أعاد الملفّاتَ المستعارَة ⇒ `RC=2` (خطأ argparse) وBLOCK من مسارٍ آخر. فأعدتُ الإثباتَ بعد النسخ **وقرأتُ
الرمزين بلا أنبوب** — والدرس: **إثباتٌ على شيفرةٍ ليست التي تظنّ أنّك تختبرها ليس إثباتًا.**

**والضبطُ السالب (٤ طفرات · `~/.hermes/cache/scratch/r67/mutate_r67.py` ⇒ `MUT_RC=0`):**
نزعُ قاعدة المقاطع (`rc=1`) · إعادةُ النمط المكرَّر في الـCI (`rc=1`) · **إعادةُ الصيغة السابقة بالحرف**
`if live is not None and …` أي التخطّي الصامت (`rc=1`) · لقطةٌ متقادمة (`rc=1`) — وكلُّها تُستعاد نظيفة.

## ٤ · وخطُّ الدفاع الأول كان مثقوبًا بالصنف نفسه (قِيس فأُغلق)

`git check-ignore` قبل: `data/data/local_sample/x.json` ⇒ **`rc=1`** (غيرُ محجوب) والجذريُّ محجوبٌ
بـ`.gitignore:11` ⇒ فالمتداخلُ مرشَّحٌ لـ`git add .` **قبل** أن يصل الحارس. فصارت الأنماط `**/data/…`
(تقابل الجذرَ وأيّ عمقٍ معًا، ولا توسّع المطابقةَ إلى غير `data/`) — وضابطُها في المجموعة:
المحجوبُ في العمق يُحجب · والجذريُّ كذلك · وخريطةُ التطهير محجوبة · **واللقطةُ المسموحةُ تبقى غيرَ محجوبة**
(ضبطٌ موجب). وبعد: `git check-ignore -v` ⇒ `.gitignore:13:**/data/local_sample/` ✓.

## ٥ · R67-2 — ثلاثةُ أسطرِ إغلاق، لا سطرٌ واحد

| # | الإغلاق | لِمَ لا يكفي غيره |
|---|---|---|
| ١ | `render_claims.py`: **فشلٌ مُغلَق** — عددٌ متعذّرٌ ⇒ خروجٌ غيرُ صفري برسالةٍ تُسمّي العلّة | «تخطٍّ صامت» هو العطبُ نفسُه؛ ورقمٌ لا يُقاس لا يُصادَق عليه |
| ٢ | خطوةُ `gate_claims` **تثبّت مُشغّلَها** (`pip install --quiet pytest` في الخطوة نفسِها) | بوّابةٌ تتّكل على تثبيتٍ يجري بعدها ليست بوّابة |
| ٣ | ضابطٌ في المجموعة: **اللقطةُ الملتزمة تُقابَل بالعدد الحي** | لا يعتمد على ترتيب خطوات الـCI إطلاقًا (و`pytest` موجودٌ في المجموعة بالضرورة) |

**والضابطةُ تقيس التخطّيَ لا مجرّد الخروج:** عُطِّل فيها فرعُ «الوثيقة ≠ اللقطة» عن قصد (`check → []`)
و`_test_count → None` ⇒ فالفشلُ لا يأتي إلّا من «لا قياس» ✓ (وأوّلُ صياغةٍ لي كانت تُسقطها بالفرع الثاني —
أُصلحت).

**وأثرٌ جانبيّ أمسكه ضابطٌ قائم — وهذا خبرٌ سارّ:**

```
FAILED tests/test_eval_gate_controls.py::test_the_snapshot_gate_actually_fires
⛔ فشلٌ مُغلَق — تعذّر قياسُ العدد حيًّا (لا `pytest` في هذه البيئة)
```
مشهدُه يستعير `PROJ` مجلّدًا مؤقّتًا لا شجرةَ اختباراتٍ فيه ⇒ قياسي الصارمُ أحمرّه. **ولم أُرخِ الفحص:
أعلن المشهدُ قياسَه** (`_test_count → 606`) ووثّق السبب في موضعه.

## ٦ · الأرقامُ المنشورة — قيست في `git clone`، وأرقامُك وأرقامي تتوافق

| البيئة | المقيسُ عندي | حِسابُ التقاطع |
|---|---|---|
| نسخةٌ نقيّةٌ حقيقيّة (`git clone` بلا «بيانات المالك») | **٨١٦ مرّ · ٥٨ تُتخطّى · ٠ فاشلة** (= ٨٧٤) | رقمُك ٨١١·٥٨ + ضوابطي الخمسة = ٨١٦ ✓ |
| ببيانات المالك على مستواها الصحيح (`rsync -a <src>/data/ <dst>/data/`) | **٨٧٣ مرّ · ١ تُتخطّى · ٠ فاشلة** (= ٨٧٤) | رقمُك ٨٦٨·١ + الخمسة = ٨٧٣ ✓ |
| شجرةُ التسليم | **٨٥١ مرّ · ٢٣ تُتخطّى · ٠ فاشلة** (= ٨٧٤) | — |

و**العددُ تحرّك مع كلّ ضابطٍ أضفتُه: ٨٦٩ ⇒ ٨٧٣ ⇒ ٨٧٤** — وهذا بعينه سببُ ما أصرَّ عليه إغلاقُ R66-1:
**يُقاس ولا يُكتب.** واللقطةُ كُتبت بالأداة في كلّ مرّة (`--write`)، لا بيد.

## ٧ · الفحوص على شجرة الجواب

| الفحص | النتيجة |
|---|---|
| `pytest -q` (شجرةُ التسليم) | **٨٥١ مرّ · ٢٣ تُتخطّى · ٠ فاشلة** (rc=0) |
| `render_claims.py --check` | خضراء (العددُ يُقابَل حيًّا) |
| `publish_guard --tree --ci` · `--tracked-real-data` | `آمن للدفع` · «لا ملفَّ بياناتٍ حقيقيّةٍ مُتتبَّع — بأيّ عمق» |
| `static_gate` · `amount_guard --ci` · `turn.py --check` | `PASS` |
| ضوابطُ الجولة + الطفرات | ٥ ضوابط جديدة · ٤ طفرات تعضّ |

## ٨ · التهبيطُ والدفع (المسار «أ» — بقرار المالك)

```
$ git push origin cdff55d:refs/heads/chore/r67-witness-landing   → publish-guard completed/success (cdff55d)
$ git push origin cdff55d:refs/heads/main                        → ac69527..cdff55d   PUSH_RC=0 (بلا PR)
$ gh api …/commits/cdff55d/pulls --jq length                     → 0
$ git push origin --delete chore/r67-witness-landing             → deleted
$ git cat-file -e origin/main:…review-67…md                      → REACHABLE=yes
$ tools/ci_report.py main                                        → PASS — كلُّ مرجعٍ دُفع (1) له تشغيلٌ أخضر
```

## ٩ · «ما لم يُثبت» + توصيتان لم تُنفَّذا

1. **لم يُثبت** أنّ كلَّ سطحٍ ثالثٍ جذريّ قد اكتُشف: فحصتُ `publish_guard` والـCI و`.gitignore` **لأنّك
   سمّيتَها**؛ ولم أُجرِ مسحًا منهجيًّا على كلّ نمطٍ جذريٍّ في المستودع.
2. **لم يُثبت** أنّ ترتيبَ خطوات الـCI يُصلح المشكلةَ وحدَه بدون الفشل المُغلَق — نفّذتُ الاثنين معًا عن قصد
   (عُمقُ دفاع)، فلم أُجرِ تجربةً تفصل بينهما.
3. **لم يُثبت** أنّ بيانات المالك لا تنزاح عن خطّ الأساس مستقبلًا — قياسُ اليومَ صفر، والحاكمُ هو القياسُ
   الدوريُّ لا الذاكرة (وهذا موضعُ البند الذي سحبتُه).
4. **بندُ `_tree_stamps` (`root`)** يبقى مُعلَنًا كما قبلتَه.

**P-67-1 · «الصفوف البيئيّةُ في `QA_CHECKLIST` قيمةٌ مشتقّةٌ مكتوبةٌ بيد ⇒ لها كاتب»:**
صحّحتُ هذه الصفوف **بيدي مرّتين** (جولة ٦٦ ثم ٦٧) — ومرّتان هما تعريفُ قيمةٍ تحتاج منتِجًا. المقترح:
أداةٌ واحدة (`--measure-clone`) تُنشئ نسخةً نقيّةً ونسخةً ببياناتٍ (بـ`rsync -a`)، تُشغّل المجموعة، وتطبع
السطرَ جاهزًا (أو تكتبه بين علامتَين) ⇒ فلا يُكتب رقمُ بيئةٍ بيدٍ بعد اليوم. ما يُبنى · ما يُقاس به (النسختان
في CI ليلًا أو عند الوسم) · ما لا يُفرض بيدٍ واحدة (البياناتُ لا تدخل الـCI — فالخطوةُ تُدار محليًّا).

**P-67-2 · «لا يُكرَّر نمطُ مسارٍ في أيّ سطح»:** أُغلقت حالةً واحدةً (التداخل). والفجوةُ الباقية **صنفيّة**:
أيّ خطوةٍ في الـworkflow تحمل `grep -E '^…'` على مسارات هي نمطٌ ثانٍ لقاعدةٍ يملكها أداة. المقترح: ضابطٌ
يمسح الـworkflow ويُطالب بأن يكون كلُّ حكمِ مسارٍ استدعاءً لأداة (`tools/*.py`)، مع قائمةِ استثناءاتٍ مُعلَنة
باسمها وسببها — فيموت الصنفُ لا الحادثة.
