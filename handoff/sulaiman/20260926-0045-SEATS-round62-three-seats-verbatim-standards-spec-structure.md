# مقاعدُ مراجعة الجولة ٦٢ — نصوصُ المقاعد الثلاثة (المعايير · المواصفة · البنية)

**التاريخ:** ٢٠٢٦-٠٩-٢٦ · **المدقَّق:** المدى `cbc8f40...8a7b0cf` (١٨ ملفًّا · +1343/−9)
**الطريقة:** ثلاثةُ مقاعدَ معزولة (Standards · Spec · Structure) بسياقٍ مصوغٍ لكلٍّ منها، **قراءةً فقط**
(القيدُ منصوصٌ في كلّ مقعد: لا تعديلَ ولا `checkout`/`stash` ولا وكلاءَ فرعيّون)، والكاتبُ لا يُراجع نفسه.
**وتنزيهٌ مُعلَن:** مُسحت مساراتُ المنزل المطلقة من هذا السجلّ عند الإيداع («المجلّدُ الرئيسيّ» لا يُكتب)،
لأنّ `publish_guard` يمنعها بقاعدة `home_path`، وقد أوقعت الملفَّ النظيرَ في مراجعة ٦١ في BLOCK تاريخيّ.

## ١ · مقعدُ المعايير (Standards) — `APPROVE WITH FIXES`
**ما ثبت بالقياس (لا findings):** الأداةُ ← الحقيقة على GitHub مطابقةٌ حرفيًّا (٣ خضر و٢ حمراوان: `rc=1`) ·
الأعدادُ مُشتقّة (`--collect-only` = 792 وقتها، ثم 813 بعد الإغلاق) · كتلُ git لمراجعات المدقّق ٥٦–٦١
**بايتًا بايتًا** كما ادُّعي · `§٢٤` مُستوفًى (فروقُ الزمن داخل التسامح ٢٠ دقيقة).

| # | القوّة | الموضع | الدليل |
|---|--------|--------|--------|
| F1 | **P1 (حاجب)** | `.githooks/pre-push:52` — سطرُ التذكير آخرَ سطرٍ في الخطّاف | **مُثبت** بخطّافٍ مماثلٍ في مستودعٍ مؤقّت + `git push` حقيقيّ: فرعٌ `rc=0` · وسمٌ `rc=1` · حذفُ فرع `rc=1` · بلا جديد `rc=1` — بلا رسالةٍ تُفسّر ⇒ خلافًا لِما يُعلنه `docs/GATES.md:47` و`docs/handoff-protocol.md:489` («ولا يُسقط دفعاً») |
| F2 | P2 | `.githooks/pre-push:50` — `awk '{print $1}'` | **مُثبت**: الحقلُ الثالث (وجهةُ الدفع) مُهمَل ⇒ دفعُ `main:feature-x` يُقاس على `main` |
| F3 | P2 | `tools/ci_report.py:92` — `if head and not _same_commit(…)` | **مُثبت** لمسار الشيفرة (يُوسَم «أخضر» عند غياب `headSha`) · بلوغُه من `gh` الحقيقيّ: **مجهول** |
| F4 | P2 | تقرير ٦١ س ١٩١-١٩٢ — «القاعدةُ صارت آليّةً» | **مُثبت**: `grep -rn ci_report tests/ .github/` ⇒ لا حارسَ يقرأ تقريرَ الدفع ⇒ الوجوبُ نصٌّ + تذكيرٌ لا بوّابة |
| F5 | P2 | `workflows/publish-guard.yml:50-66` + `docs/GATES.md:55-71` | **مُثبت**: `tests/test_ci_report.py` ليس في قائمة الـCI ولا في §٣ ⇒ `success` لا يشمل ضوابطَ الأداة |
| F6 | P3 | صفّ ١٧ · `tools/guard_bite_sweep.py:81-247` | **مُثبت**: `grep -c ci_report tools/guard_bite_sweep.py` ⇒ 0 ⇒ عضُّ الصفّ ١٧ محروسٌ بضوابط كاتبه وحدها |

**ما لم يُثبت (بنصّه):** لم يُعَد قياسُ «الشجرة الكاملة» (قِيس الجمعُ والـCI الخفيفُ فقط) · بلوغُ حالة
«تشغيلٌ بلا `headSha`» من `gh` الحقيقيّ غيرُ مُشاهَد · نسبُ كتابة ملفّات `handoff/claude/*` لا يُميَّز فيه
موضعُ الكتابة من موضع النقل من الشجرة · **وظهر أثناء الجلسة ملفٌّ غيرُ متتبَّع في صندوق المنفّذ مُنتِجُه
مجهول** — والسجلُّ يوثّق الجواب: مُنتِجُه **المنفّذ نفسه** (`qa_gate --full` الذي أُمرتُ به)، وقد أُودِع في
التزام الإغلاق.

## ٢ · مقعدُ المواصفة (Spec) — `APPROVE WITH FIXES`
قياساته: الأداةُ تسمّي الأحمرَين و`rc=1` · الـCI على `8a7b0cf` أخضرُ · مراجعات ٥٦–٦١ مطابقةٌ بايتًا بايتًا ·
تواريخُ الإعادة = الأصليّة (٦/٦) · الفرعان المحجوبان غائبان عن `ls-remote` · تصحيحُ تقرير ٦١ **إضافةٌ
محضة** (+٢٧ سطرًا، صفرُ حذف) · `docs/claims.json` و`‏render_claims --check` متطابقان.

| # | الصنف | الموضع | الأثر |
|---|-------|--------|-------|
| (ج)+(ب) | **P1** منفَّذٌ خطأً + زائد | `.githooks/pre-push:50-52` | نفسُ حاجب المعايير: سطرُ التذكير يحكم رمزَ خروج الدفع ⇒ تُمنع ثلاثةُ أشكالِ دفعٍ مشروعة؛ وهو تغييرٌ **لم يطلبه** R61-1 (المطلوب: أداة + ضابط + سطرُ §٢٩) |
| (أ) | P2 ناقص | خطة ٦٢:51-52 (R62-4) و:47-49 (R62-3) | المدى لا يحمل أثرًا لـR62-4 ولا تقريرَ الجولة ٦٢ ⇒ بندٌ أمنيٌّ يُسقَط صامتًا (وهو صنفُ R61-1) |
| (ج) | P3 منفَّذٌ خطأً | `tools/ci_report.py:92` | غيابُ `headSha` يُقرأ نظافةً = fail-open |

**ما لم يُثبت:** لم يُعَد الشجرةُ الكاملة ولا `qa_gate --full` (قُرِئا من المستودع) · لم يُعَد مسبارُ
R62-2(a) في نسخةٍ نظيفة · لم يُقرأ سجلُّ التشغيل لأجل **اسم** الضابط الساقط في الفرعين الأحمرين.

## ٣ · مقعدُ البنية (Structure) — `REQUEST CHANGES`
**نعم في الحُكم، لا في السباكة:** `verdict()` (`tools/ci_report.py:68`) **مالكٌ واحدٌ للحُكم** لا ثانيَ له
(الأدواتُ الأخرى تقيس شجرةً أو حالةً أو نصَّ workflow، لا نتيجةَ تشغيل).

| # | القوّة | الموضع | الدليل |
|---|--------|--------|--------|
| F1 | **P1 (حاجب)** | `.githooks/pre-push:50-52` | **مُثبت** بـ`git push` حقيقيّ على أسطر الخطّاف نفسِها: وسمٌ/حذف/`HEAD:refs/heads/x`/بلا جديد ⇒ `exit 1` صامت؛ ويخالف **قاعدةً مقيسةً عند المالك الآخر**: `tools/amount_guard.py:1117-1124` يعيد 0 لِـ`stdin` الفارغ، وعلّتُه المنصوصة «حافزُ`--no-verify`» [بلا الشرطتين في الأصل] |
| F2 | P2 | `.githooks/pre-push:50` | **مُثبت**: «أيُّ المراجعِ دُفع» صار لها مُحلِّلان؛ ومُحلِّلُ المالك ينصّ «الحقلُ الأول ليس مرجعًا دائمًا — مقيسٌ من git» وبضابطٍ بأربع دفعاتٍ حقيقية (`tests/test_amount_guard.py:511`) |
| F3 | P2 | `tools/ci_report.py:125-131` | **مُثبت**: `json.loads(stdout)` ⇒ `JSONDecodeError` (جملةُ الحدّ تُطبَع بعد فرع `--json`) — ووعدُ «خرجٌ آليّ» في ثلاث وثائق ولا ضابطَ يُشغّله |
| F4 | P2 | `tools/ci_report.py:87-94` | **مُثبت** بپيلودٍ مُستبدَل: بلا `headSha` ⇒ «أخضر» و`PASS` بلا مقابلةِ التزام (fail-open) |
| F5 | P2 | `§٢٩` · خطة:37 | **مُثبت**: «قاعدةٌ تُقاس لا تُطبَع» — ولا موضعَ يقرأ تقريرًا ليتحقّق أنّه يحمل المخرج؛ والموضعُ القائم `tests/test_report_names.py:39` يقيس الأسماء |
| F6 | P3 | `docs/GATES.md:42 و:47` | **مُثبت**: المعرّف `R61-1` يسمّي بندَين مختلفَين، وكلٌّ يعلن «الملاحظة الوحيدة» |

**حركةُ الجودو المقترحة (وهي ما نُفِّذ):** تُحذف أسطرُ الاستخراج ويُقرأ `stdin` **داخل الأداة**
(`printf '%s\n' "$REFS" | "$PY" tools/ci_report.py --pre-push`) ⇒ يذوب F1+F2 معًا، ويصير قرارُ «لا مراجع/
مراجعُ بلا شكل» عند مُحلِّلٍ مختبَر، ويُصان كودُ خروج الخطّاف صفراً.

**ما لم يُثبت:** لم يُشغّل الـCI ولا `gh run view` (جدولُ مراجعِه جزئيّ: مرجعٌ أعطى «غيرُ مقروء») · لا وسمَ
على GitHub ⇒ صورتا الوسم/الحذف كامنتان والمقيسُ يوميًّا `stdin` الفارغ · F4 بپيلودٍ مُستبدَل · لم يُقَس
`guard_bite_sweep` · ولا إصلاحَ جُرِّب (مقعدُ قراءة).

## ٤ · الحكمُ المجمَّع قبل الإغلاق
`APPROVE WITH FIXES` (المعايير · المواصفة) و`REQUEST CHANGES` (البنية) — **وحاجبٌ واحدٌ مشترك**: سطرُ
التذكير في الخطّاف. وأُغلق في الجولة نفسها بحركة الجودو + ضابطٍ يُنفّذ الخطّافَ الحقيقيّ، ومعه F3/F4/F5/F6
وسمّا م٢٥ وم٢٦ — والتفصيلُ في تقرير الجولة ٦٢ الشامل.
