# إغلاقُ البوابة (٣) — قرارُ مالكٍ مؤرَّخ يُغلق التقاطع، وبلا إعادة بناءٍ وبلا دولار

```
id:      20260925-1808-sulaiman
from:    sulaiman
to:      owner
type:    SUBMIT
step:    إغلاقُ حكم البوابة (٣) (البند ٤): قرارٌ مُؤرَّخ كائنٌ مقروء + قارئان + ضابطٌ بسمّ (م١٩) + تصحيحُ رقمٍ مسحوب
commit:  يُقرأ من `git log -1 --format=%h` (الالتزامُ يحمل هذا التقرير)
verdict: -
```

## ١ · الحكم — بلا مقدمات

**قرارُ المالك 2026-09-25 («نفّذ هذي»):** «حكمُ البوابة (٣): التقاطعُ بين الإحصاء والمديات **قرارٌ لا عطب**»
⇒ أُغلق البندُ **بقرارٍ مؤرَّخ**، لا بإعادة بناء الحزمة. ولم يُدفع دولارٌ واحد: كلُّ القياس $0.

**{المُدخَل}** قرارُ المالك + الثلاثةُ المعلَنة في الخارطة (٠ + ١٣٣ + «يحتاج قراراً»)
**{الدليل}** 754 اختباراً · `qa_gate --full` **5/5** بدليلٍ محفوظ · `guard_bite_sweep` **22/22** · `render_claims` مطابق · publish/amount/static PASS · `--verify` حيّ ⇒ PASS
**{الحالةُ النهائية}** البوابة (٣) **`[x]`** في الخارطة، بقرارٍ له قارئٌ يمنعه من التعفّن

## ٢ · ما نُفِّذ — الخطّةُ التي أُعلنت قبل السطر الأول (٥ مراحل)

| # | الحركة | الموضع | الدليلُ المُعيد |
|---|---|---|---|
| ١ | **القرارُ كائنٌ مقروء** لا نثر: تاريخ · مجتمع · **سقفٌ معلَن ١٣٣** · حدود | `tools/eval_pack.py::GATE3_DECISION` | `grep -n GATE3_DECISION tools/eval_pack.py` |
| ٢ | **قارئٌ حيّ في البناء**: الحكمُ و`rc` و`pack.json` من **مصدرٍ واحد** (`gate3_closed`) | `tools/eval_pack.py::cmd_build` | `.venv/bin/python tools/eval_pack.py --build` ⇒ يطبع سطرَ البوابة (٣) |
| ٣ | **قارئٌ حيّ في التحقّق**: يقيس التقاطعَ الآن ويقابِل قرارَ الحزمة **بقرار الأداة** | `tools/eval_pack.py::cmd_verify` | `--verify` ⇒ «مُغلَقةٌ بلا تقاطع ✓ · قرارُ الحزمة: مطابقٌ لقرار الأداة ✓» |
| ٤ | **الحدُّ مُقاسٌ في دالّةٍ نقيّة** (لا يُغطّي عطباً · لا يتمدّد فوق سقفه · بلا قرارٍ يبقى شرطيًّا) | `gate3_closed` + `classify_verdict(..., decision=)` | `tests/test_eval_pack.py` (٤ ضوابط جديدة) |
| ٥ | **سمٌّ يُثبت أنّ الحدَّ يعضّ**: إلغاءُ السقف ⇒ يسقط ضابطُه | `tools/guard_bite_sweep.py` (**م١٩**) | `guard_bite_sweep.py` ⇒ **22/22** (كان 21/21) |
| ٦ | **الثقبُ القائمُ يُغلق**: `--verify` كان يقرأ التقاطعَ من الحزمة بالمصادقة لا يقيسه | `cmd_verify` (نفسُ الموضع) | القرارُ **لا يُقرأ من الحزمة** بل يُقابَل بها |
| ٧ | **تصحيحُ الوثائق المُتقادمة**: البندُ `[ ]` ⇒ `[x]` · و«١٠٠ صفحة» ⇒ رقمٌ **مسحوب** | `docs/ROADMAP.md` · `docs/EVAL_PACK.md` | `git diff` (المؤرَّخُ يبقى بتاريخه — يُقرأ بالمقارنة لا بالمحو) |
| ٨ | **صفُّ بوّابةٍ جديد** في السجلّ | `docs/GATES.md` **صفُّ ١٥** | `tests/test_gate_registry.py` يقيس السجلَّ نفسه |
| ٩ | **الأرقامُ تُقاس لا تُكتب**: 750 ⇒ **754** · و21/21 ⇒ **22/22** | `docs/QA_CHECKLIST.md` + `docs/claims.json` | `tools/render_claims.py --write` ثمّ `--check` ⇒ «كل رقم معلن يطابق مصدره» |

## ٣ · القياسُ الحيّ (يُعيده أيُّ أحدٍ بلا مال)

```
.venv/bin/python tools/eval_pack.py --verify
⇒ بوابة «٢٠٠»: 200 ✓ · التقاطع: 0 ✓
⇒ بوابةُ التقاطع (٣): 0 صفحةً · القرارُ المُلتزم: «حكمُ البوابة (٣)…» (مالك · 2026-09-25 · السقفُ 133)
   ⇒ مُغلَقةٌ بلا تقاطع ✓ · قرارُ الحزمة: مطابقٌ لقرار الأداة ✓
⇒ الحكم: PASS — الحزمةُ تشهد لنفسها
.venv/bin/python tools/eval_pack.py --build          ⇒ PASS · الحكمُ مُصنَّفٌ ومقروء
.venv/bin/python tools/guard_bite_sweep.py           ⇒ 22/22 بوّاباتٍ تعضّ
.venv/bin/python -m pytest -q tests/                 ⇒ 754 passed
```
**والدليلُ المحفوظ للبوّابة الكاملة:** `handoff/sulaiman/20260925-1808-gate-evidence-gate3-closure.txt` (**5/5**).
**ودليلُ ما بعد المراجعة (الشجرةُ النهائيّة):** `handoff/sulaiman/20260925-1833-gate-evidence-gate3-closure-after-review.txt`
(**5/5** · و**754** اختباراً · وبلا وسمِ `[flaky_read]`).
وثيقةٌ تُقابَل بمخرَجها: أمرُ الإغلاق في الخارطة **أمرٌ قابلٌ للتنفيذ** لا شهادةٌ عن نيّة.

## ٤ · الرقمُ المسحوب «١٠٠ صفحة» — ما جرى له بالضبط

كانت البوابةُ تقيس **المدياتِ وحدَها (١٥٠)** والإحصاءُ خارجَ مجتمعها ⇒ أعلنت ١٠٠، والمقيسُ بالمجتمع الصحيح
**١٣٣ صفحة · ١,٢٥٣ صفّاً · ٣٢.٦٪** (تصحيحُ المدقّق review-32). **والقاعدةُ المستخلَصة: عددٌ هو ادّعاءٌ عن فلترِ
الفحص لا عن الحزمة** — ولذلك صار **المجتمعُ** جزءاً من كائن القرار نفسه (`scope`) ولم يبقَ في النثر.
**والحالةُ يومَ القرار: التقاطع ٠** بالبناء (`capture_mode=strict` + استثناءُ الحزمة بنيويًّا) ⇒ **السقفُ حدُّ أمانٍ
لاحق، لا ثمنٌ يُدفع اليوم** — وهو ما صُرِّح به في القرار وفي الخارطة.

## ٥ · ما ظهر عرضاً في الفحوص — ولماذا مرّ (مُعلَنٌ لا مُخفى)

- **قراءةٌ غيرُ نظيفة في الخطوة الحيّة:** التشغيلُ الأول أعطى **4/5**: `footer denominator hole: 9/9 مقابل تغطية (9, 10)`
  ⇒ أي **صفحةٌ خرجت من الأوراق بلا حكم** في تلك القراءة. والقراءةُ الثانية **نظيفة (10/10)** ⇒ حُكِم بالسياسة
  المُعلَنة: **لا براءةَ بلا قراءةٍ نظيفة**، والقراءةُ الثانيةُ النظيفةُ هي البراءة. والدليلُ المحفوظ من التشغيل الثالث
  (**5/5** · `footer 10/10` · وبلا وسمِ `[flaky_read]`) ⇒ **والحدُّ مُعلَن:** مرّاتُ هذه الخطوة تُفلِك، فلا يُقرأ
  «5/5 في تشغيلٍ واحد» حكماً دائماً — والقاعدةُ القائمة (تسجيلُ الوسم+الميزانيّة) **لا تشمل هذه الخطوة بعد**.
- **سطرُ اللافتة التشخيصيّ ما زال غيرَ محكوم:** تنوّع في أربع قيم (`[1]` · `[1, 6]` · `[9]` · `[5]` · `[6]`)
  و**5/5 في كلّها** ⇒ مُرشَّحٌ للإصلاح أو النزع (توصيةٌ قائمة).

## ٦ · حدودُ ما أُغلق — «ما لم يُثبت»

- **لم يُثبت** أنّ أيَّ تقاطعٍ مستقبلي **دون** السقف سيُقبَل تلقائيًّا: القرارُ **مؤرَّخٌ ومُقيَّدٌ بمجتمعه**، والسقفُ
  يُقاس بـ`len(inter) ≤ 133` — وزيادةُ التقاطع **حدثٌ جديد** يستحقّ قراراً جديداً (والضابطُ يفشل عندها مُسمّى).
- **لم يُثبت** أنّ الحزمةَ مُحصَّنةٌ من إعادة بناءٍ خاطئة: القرارُ **يُغلق الحكمَ** لا يمنع `--build` — والمخرجُ
  محفوظٌ في `pack.json` (و`data/` غيرُ متتبَّع) ⇒ فالتاريخُ في Git هو ما يحمي القرار، لا الملفّ المحلّيّ.
- **لم يُثبت** أنّ `--verify` يمسك غشًّا **في الأداة نفسها** (قرارٌ يُعدَّل في الشفرة فيُطابق حزمتُه): الحمايةُ هنا
  هي المراجعةُ والسمومُ والـCI، لا الحساب.
- **لم يُقَس** أثرُ هذا الإغلاق على مقياس **صدق الاستشهاد** (المقياسُ المعلَّق P-5) — يبقى البندُ التالي في البند ٤.
- **لم تُنفَّذ المقاعدُ** حالةَ **0 < التقاطع ≤ 133** (تحتاج أرضيةً مصنوعة) ⇒ إغلاقُ القرار لتقاطعٍ حقيقيّ داخل سقفه
  **مُثبتٌ بالدالّة النقيّة لا بتشغيلٍ حيّ**.
- **لم تُعَد المقاعدُ** تشغيلَ `--build` (يكتب `pack.json`) ولا `guard_bite_sweep` (يستبدل ملفّاتٍ في مكانها):
  الحصيلةُ **22/22** ودعوى البناء أُعيدتا في شجرة العمل بيد المنفّذ، وقياسُهما مستقلًّا قائمٌ على العدّ من المصدر.
- **ضابطُ تعفّن الحزمة على القرص يتخطّى في استنساخٍ نظيف** (`data/` غيرُ متتبَّع) ⇒ ما يُقاس في الـCI هو الضابطُ
  **الجديد** (حزمةٌ صناعيّةٌ مُحرَّفة) لا الحزمةَ الحقيقيّة؛ فالحمايةُ في الاستنساخ **صنفيّة** بطبيعتها، والحزمةُ
  الحقيقيّةُ تُقابَل بقرار الأداة عند كلّ `--verify` على الجهاز.

## ٧ · المراجعةُ المستقلّة (٣ مقاعد · `code-review-elite`) — ولماذا لم يُسلَّم قبلها

المدى **مُثبَّت**: `a1c7541...1a81370` (three-dot) · والمقاعدُ **للقراءة فقط** (لا كتابةَ ولا `checkout` ولا وكلاء) ·
وثلاثتُها مستقلّةٌ بسياقٍ مُصاغٍ لها: **المعايير** (أبُني صحيحاً؟) · **المواصفة** (الشيءُ الصحيح؟) ·
**البنية** (أيُترك التصميمُ أفضل؟) ⇒ **APPROVE WITH FIXES ×٣**. والنقلةُ الحاكمةُ اتّفق عليها مقعدان
**وأُعيد إنتاجُها حيًّا**: **المخالفةُ كانت تُطبَع ولا تحكم** — حزمةٌ تُوسّع قرارَها بنفسها تُخرج
«مخالفٌ ⇒ يُعاد `--build`» ثمّ **«PASS» و`rc=0`**، وذلك صنفُ «يطبع ✗ ثم يقول PASS» بعينه، وصفُّ السجلّ **١٥**
يَعِد بـ`rc=1` في هذه الحالة. وأُغلقت البنودُ في **الالتزام نفسه**:

| # | البند (المصدر) | العِلاج | الدليلُ المُعيد |
|---|---|---|---|
| ١ | المخالفةُ لا تدخل `failures` (**معايير S-1 · بنية F3 · مواصفة F1**) | تدخل **بلا شرط تقاطع** ⇒ `rc=1` وباسمٍ مطبوع | ضابطٌ **ينادي الأمرَ نفسَه** على حزمةٍ مُحرَّفة |
| ٢ | القاعدةُ مكتوبةٌ مرّتين في `cmd_build` (**بنية F1**) | `ok = not blockers and (not inter or gate3_shut)` — **قائمةُ الحواجب وحدها** | مُطابقةُ التعبيرين على ٣٢ تركيبة = صفرُ اختلاف |
| ٣ | الضابطُ **خارج الـCI** ويُعيد بناء المنطق (**معايير S-2**) | `tests/test_eval_pack.py` أُدرج في الـCI وفي قائمة §٣ · والضابطُ الجديدُ ينادي `main(["--verify"...])` | `docs/GATES.md` §٣ + `.github/workflows/publish-guard.yml` (وتساويهما يقرؤه `test_gate_registry`) |
| ٤ | الفرعُ الحاسمُ يعتمد على المُنادي (**بنية F4**) | `inter` قبل `ok` في `classify_verdict` ⇒ الفخُّ مُغلقٌ في الدالّة | ضابط: `ok=True, inter=[52]` ⇒ لا «قطعت بواباتها» |
| ٥ | `== 0` يُجمّد المقيسَ على صفر (**معايير S-3 · بنية F2**) | المقيسُ يُقيَّد بسقف القرار · و`measured_intersection` **يُقابَل بحاضره** في `--verify` | ضابطٌ + `--verify` الحيّ |
| ٦ | حزمةٌ غائبة/تالفة/ناقصة ⇒ **أثرُ بايثون** (**بنية F3**) | وقوفٌ بالاسم (`Stopped`) للملفّ **وللبنية الناقصة** | تشغيلٌ حيّ |
| ٧ | إسنادُ سموم اللاحتميّة إلى م١٩ (**معايير S-6ب · مواصفة F2**) | النطاقُ مُسعَّر: م١٣–م١٦ للّاحتميّة · م١٧/م١٨ للجواب · م١٩ للقرار | `docs/QA_CHECKLIST.md` |
| ٨ | قيمةٌ محفوظة تتقادم في سطر الحالة (**معايير S-6أ · مواصفة F3**) | أُزيلت (`main`) — تُقرأ بأمرٍ | `handoff/STATE.md` |
| ٩ | ترويسةُ `eval_pack` تُعلن حدًّا برقمٍ متقادم (**بنية F6**) | الرقمُ المقيس + مبرّرُ الزيادة في الالتزام نفسه | `wc -l tools/eval_pack.py` |

**وعضّةُ الضابط الجديد مُثبتة بالأمر لا بالادّعاء:** على الشيفرة **قبل** الإصلاح يسقط
(`test_a_drifted_pack_fails_the_real_verify_with_a_named_reason` ⇒ `AssertionError: المخالفةُ تُسمّى في failures ولا تُختم بـPASS`)،
و**بعد** الإصلاح يمرّ — وهذا هو دليلُه المُعيد.

**وتوصيةٌ مستدامةٌ واحدة لم تُنفَّذ (تُعلَن):** أن يُشتقّ **سقفُ القرار** في `tools/render_claims.py` من
`tools/eval_pack.py::GATE3_DECISION` (ادّعاءٌ مشتقٌّ يقرؤه `--check`) ⇒ يسقط تكرارُ «١٣٣» المكتوب يدويًّا في
ثلاث وثائق. **وسببُ تأجيلها:** تُوسّع `render_claims` بعائلة ادّعاءاتٍ جديدة، وهو تغييرٌ أكبرُ من أمر المالك —
فلا يُنفَّذ بالأمر نفسه بلا قرارٍ صريح.
