# خطةُ الجولة ٦٥ — الختمُ بساعة الشجرة، و§٢٧ بالقياس، ومقاعدُ الجولة

**المرجعُ:** مراجعة ٦٥ (`handoff/claude/20260926-065039-third-eye-review-65-green-by-three-minutes.md` · `REQUEST_CHANGES` على PR 127).
**حكمُها:** البنودُ الأربعة لمراجعة ٦٤ **مُغلقةٌ على `main` بالقياس** — لكن `PR 127` «أخضرُ بثلاث دقائق»: عطبُ ساعةٍ في المسبار، و§٢٧ يعلن استحالةً يُكذّبها تاريخُ المستودع نفسُه.
**هذه الوثيقةُ خطةٌ قبل العمل** (لا جوابُ حُكمٍ ولا تُودِع شاهدًا)، وتُقيَّد في `NON_ANSWER_FILES_SINCE_RULE` باسمها وصنفها.

---

## القياساتُ السابقةُ للتنفيذ (لا ادّعاءَ بلا معدود)

| # | القياس | الوسم |
|---|---|---|
| م-١ | حماية `main`: `contexts:["guard"]` · `strict:true` · `enforce_admins:true` · **صفرُ مراجعة PR** · `rulesets:[]` | `مُثبت` |
| م-٢ | سابقةُ دفعٍ مباشرٍ بلا PR: `37c10cf` ⇒ `commits/<sha>/pulls` = **0**، وتشغيلان `guard/success` (23:54:49Z ثم 23:55:48Z) | `مُثبت` |
| م-٣ | ساعةُ الختم: `tools/landing_probe.py` يختم بـ`datetime.now().astimezone()` = ساعةُ العدّاء (UTC على GitHub) وأسماءُ الشجرة `+03` | `مُثبت` (قراءةً) |
| م-٤ | النافذةُ المقيسة: `الشاهد+3h → الجواب+3h` ⇒ أيُّ تشغيلٍ بين `(03:28:54, 04:55:00]Z` يسقط | `مُثبت` (نقلًا عن مراجعة ٦٥) |
| م-٥ | تشغيلٌ الآن تحت `TZ=UTC` (11:30Z) **يمرّ** (النافذةُ مضت) ⇒ إعادةُ الإنتاج الحيّة لا تُتاح، فتُعاد **بتجربةٍ مضبوطة** | `مُثبت` |

**وما لم يُثبت قبل التنفيذ:** أن الدفعَ المباشرَ **اليوم** يُقبل على التزامٍ فحصُه أخضر (م-٢ سابقةٌ لا قياسُ لحظة) ⇒ يُقاس في المرحلة ١ وهو نفسُه إهباطُ الشاهد.

---

## المراحلُ الثمانية — كلُّ مرحلةٍ ببوّابةٍ تُقاس قبل التالية

| # | المرحلة | البوّابة |
|---|---|---|
| ٠ | إعادةُ إنتاج R65-1 بتجربةٍ مضبوطة (الشاهدُ الاصطناعيُّ بختمٍ داخل النافذة بآلة المسبار نفسِها) | `tests/test_answer_names_witness.py` **يسقط** بالرسالة «ليس أحدثَ حُكم» |
| ١ | إهباطُ الشاهد (مراجعة ٦٥) قبل الجواب — §٢٧: فرعٌ ⇒ `guard` أخضرُ على الالتزام نفسِه ⇒ `HEAD:main` بلا PR ⇒ حذفُ الفرع | `ci_report main` = PASS + الملفُّ قابلٌ للوصول من `origin/main` + **مخرَجُ الدفع محفوظٌ بالحرف** |
| ٢ | R65-1: الختمُ بساعة الشجرة (`+03`) وفوق أحدثِ ختمٍ في الشجرة المقيسة + ضابطٌ يعضّ | الضابطُ يسقط على **طفرتين** (نزعُ الأرضيّة · نزعُ الوصل) ويمرّ على الأصل، تحت `TZ` ثلاثة |
| ٣ | R65-2: تصحيحُ §٢٧ نصًّا — يُرفع «بالقياس» عن الاستحالة، ويبقى الرابطُ الحاكم (الشاهدُ قبل الجواب)، والمسارُ قرارُ المالك بخيارَين وثمنِ كلٍّ | لا ادّعاءَ استحالةٍ في النصّ · والتناقضُ مع «الملحق» يُرفع أو يُوثَّق |
| ٤ | فحوصُ الجودة الشاملة | `pytest` · `static_gate` · `amount_guard --ci` · `render_claims --check` · `publish_guard --tree --ci` · `turn.py --check` · `landing_probe` (‎`TZ=UTC` ومحلّيًّا) |
| ٥ | مقاعدُ `code-review-elite` الثلاثة (معايير · مواصفة · بنية) على المدى المُثبَّت، معزولةٌ وقراءةٌ فقط | تقريرٌ يمرّ `check_review_report.py` · كلُّ مقعدٍ يُعلن `HEAD` أوّلًا · صفرُ تحريرٍ من المقاعد |
| ٦ | تخطيطُ إغلاقِ ما ظهر ثم تنفيذه، مصنَّفًا: مُنفَّذ / مؤجَّل بمالكه / مرفوض بسبب | إعادةُ قياسٍ بعد آخر تعديل + طفراتٌ للضوابط الجديدة |
| ٧ | الجوابُ (بشاهدٍ حقيقيّ إلى مراجعة ٦٥) + تحديثُ `SUBJECTS_LANDED` ونصُّ المقاعد في الالتزامات الحاملة | `pytest` أخضرُ و`SUBJECTS_LANDED` مقيسٌ في الاتّجاهين |
| ٨ | التسليم: دفعُ الفرع، انتظارُ `guard`، التسليمُ بالتقرير الشامل | `ci_report <branch> --commit <sha>` = أخضرُ على الرأس المدفوع |

**وما لا يُغلق في هذه الجولة (يُعلَن ولا يُخفى):** قرارُ المالك في مسار الهبوط (دفعٌ مباشر أم PR) — يُعرض بخيارَيه وثمنِ كلٍّ ولا يُقرَّر من جهتي.
