# مراجعةٌ مستقلّة (code-review-elite) — المدى `eb5bc9d...3530b0e`

**التاريخ:** 2026-09-23 ~16:20 (+03) · **المستودع:** rajhi-rag · **الفرع:** `feat/item4-pack-free-capture-44` (PR #119)
**النقطةُ الثابتة:** `eb5bc9d` → **HEAD:** `3530b0e` · **الفرق:** ٥ ملفّات · +٢٨٨ سطراً · **الشجرةُ نظيفةٌ وقت المراجعة** (الفرقُ ثلاثيٌّ يرى كلَّ شيء — G2).
**المقاعد:** ثلاثةٌ معزولة، كلٌّ بسياقٍ مُصاغٍ له (لا بتاريخ الجلسة) · للقراءة فقط · بلا تفويض (G7).
**ما لم يُراجَع:** البوابة (٣) `publish_guard` خارج مدى هذه المراجعة · ولا نسخةَ نظيفةٌ شُغّلت (يُقاس في تكاملٍ مستقلّ).

## Verdict

**REQUEST CHANGES** — لأنّ محجوبًا واحدًا **يحتاج قرارَ المالك** ولا يُصلحه المُنفّذ:
`SPEC-1` (إنفاقُ ١٣٣ صفحةً من الحزمة على ذراعَي FM-2 **قبل** ختمها — والاختيارُ النظيفُ غيرُ ممكنٍ ببنية المديات).
وما عداه من المحجوب أُصلح **بعد** المراجعة وأُعيد قياسُه: `SPEC-2` · `SPEC-3` · `S-1` · `T-1` (انظر «ما أُصلح»).
الحكمُ على المدى المُراجَع = REQUEST CHANGES · والحالُ الآن = **APPROVE WITH FIXES بانتظار قرار المالك** في `SPEC-1`.

## Standards

- **S-1 (P1)** `tools/capture_training.py:339` — rule: «الفارغ غيرُ المقروء = فشلٌ مُغلَق» + نصُّ الأداة: «حجزُ التقييم مبنيٌّ في الأداة» · الحمايةُ كانت **اختياريةً بعَلَم**، والأمرُ المرجعيُّ المسجّل بلا علَم (`docs/evidence/20260922-item4-step1-gates-1-2.json` ⇒ `captured: 416`) ⇒ إعادةُ تشغيلٍ بـ٤١٦−٢٨٣ = ١٣٣ صفحةً مُلوَّثة. · **مُثبت** · الحركة: افتراضٌ بنيويّ + إلغاءٌ بعَلَم صريح.
- **S-2 (P2)** `tools/capture_training.py:68-78` — rule: «كلُّ مسحٍ يُعلن ما قاسه، ويسقط إن لم يُكمله» · الفشلُ المُغلَق كان يعمل على **الفراغ التامّ** فقط: قراءةٌ ناقصةٌ (مفتاحٌ تغيّر) تمرّ باستثناءٍ جزئيّ و`rc=0`. · **مُثبت** · الحركة: مقابلةُ المقاس المُعلَن + رفضُ الأشكال غير المعروفة.
- **S-3 (P2)** `tests/test_capture_pack_exclusion.py` — rule: «الحارسُ الذي لا يسقط لم يُثبت أنه حارس» · لا اختبارَ ينادي `main()` ⇒ حذفُ فرع الاستثناء يُبقي السويتَ خضراء. · **مُثبت** · الحركة: اختبارٌ بأرضيةٍ صناعيّةٍ ينادي `main()`، أو بطاقةٌ في `guard_audit.CASES`.
- **S-4 (P2)** `tests/test_capture_pack_exclusion.py:102,111` — rule: «وما يُعلن من تخطٍّ يُقاس» · اختباران يقرآن `PACK` بلا حارسِ وجود ⇒ في نسخةٍ نظيفةٍ `SystemExit` = **failed** لا **skipped** (قِيس بتشغيل: اختبارٌ يرمي `SystemExit` ⇒ `1 failed, 1 skipped`). · **مُثبت** · الحركة: حارسُ وجودٍ قبل القراءة.
- **S-5 (P2)** `tools/capture_training.py:54-79` — rule/citation: `intersection_gate.key = "(doc_id, page) — لا page"` (الحزمةُ تُعلنه) · الاستثناءُ بالرقم المجرّد بلا هويّة ⇒ حزمةُ مستندٍ آخر تُحجز صفحاتِ مستندنا بصمت. · **مُثبت** · الحركة: `expect_identity`.
- **S-6 (P2)** `tools/capture_training.py:54-79` ↔ `tools/eval_pack.py:871` · `tools/oracle_confirm.py:200` — رائحة **Duplicated Code** (٤ نسخٍ لقارئ صفحات الحزمة، وواحدةٌ منها سبق أن انحرفت فعلاً في review-32) · **مُثبت** · الحركة: دالّةٌ واحدةٌ مُعلَنة يستهلكها الأربعة.
- **S-7 (P3)** `handoff/sulaiman/20260923-1514-REPORT-…md:49,65` — rule: «سلسلةُ البنود واحدةٌ مرقّمة» · التقريرُ استشهد بـ«القاعدة ١٣» والبندُ المقصود **١٤** في السلسلة الموحَّدة. · **مُثبت** · الحركة: الاستشهادُ بالنصّ أو بالرقم الموحَّد.

## Spec

- **SPEC-1 (P1)** `docs/evidence/20260921-fm2-v1-arm.json` · `20260921-fm2-v2-arm.json` — citation: المواصفة: «ولا صفحة منها تُستعمل في **أيّ ضبط**» · الذراعان: `sample.from="training (page % 3 != 0)"` · `picked=420` والقياسُ: **تقاطعُهما مع الحزمة = ١٣٣ من ٢٠٠** (مُلتزمٌ 2026-09-21 والحزمةُ 2026-09-22). · **مُثبت** · **والقياسُ الحاسم:** الحرُّ ٢٠٨ صفحات كلُّها `%3==0` وأطولُ مدًى متّصلٍ = **١** ⇒ الاختيارُ النظيفُ **مستحيلٌ** ببنية (٣ مديات × ٥٠). · الحركة: **قرارُ المالك** — إعلانُ انحيازٍ مؤرَّخ في الشهادة، أو بنيةُ اختيارٍ متفرّقة (٢٠٨ ≥ ٢٠٠ بهامش ٨).
- **SPEC-2 (P1)** `tools/capture_training.py:339` — citation: المواصفة صيغتُها مطلقةٌ («ولا صفحة…») والحمايةُ كانت مفتاحاً اختياريًّا (`default=None`) ⇒ الفرقُ يغلق الحالةَ لا الصنف. · **مُثبت** · الحركة: العقدُ لا سطرُ الأوامر.
- **SPEC-3 (P1)** `docs/evidence/20260922-item4-step1-gate3-eval-pack.json` · `docs/EVAL_PACK.md:38` — citation: المواصفة: «بصمةُ الحزمة مُلتزمة في `docs/evidence/`» · الشهادةُ المُلتزمة تقول `FAIL · captured 416 · intersection 133` والحالةُ الحيّة في ملفٍّ **مُتجاهَل** وحدَه، والفرعُ لم يُضِف شهادةً بديلة. · **مُثبت** · الحركة: شهادةٌ مُؤرَّخةٌ مُلتزمة + تصريحٌ يُبطل ويُحيل.
- **SPEC-4 (P2)** `tools/eval_pack.py:874` — citation: المواصفة: «تُختم ببصمة» + سمُّ (أ) «تغييرُ بصمة أيّ صفحةٍ مقيَّدة ⇒ يسقط» · البصمةُ تغطّي **الإحصاء ٥٠ من ٢٠٠** والمدياتُ بلا بصمة ⇒ السمُّ غيرُ قابلٍ للتنفيذ على ٧٥٪ من الحزمة. · **مُثبت** · الحركة: بصمةُ المجموعة كاملةً.
- **SPEC-5 (P2)** `tools/capture_training.py:415` — citation: قاعدةُ «الفحصُ يُعلن مجتمعه» · «١٣٣ للحزمة» تقيس الحزمةَ ناقصَ الحجز: المُعلَن ١٣٣ والمستثنى فعلًا **٢٠٠** (٦٧ منها `%3==0` سبقها وسمُ الحجز). · **مُثبت** · الحركة: إعلانُ المجموعتين باسمين.
- **SPEC-6 (P2)** `tools/eval_pack.py:299-305,708` — citation: «تُختار بقاعدةٍ حتمية … لا تُلمَس» · الاختيارُ صار دائريًّا (`strict` يستثني الالتقاط، والالتقاطُ يستثني الحزمة) وشرطُ إعادة الإنتاج يعتمد على حالةٍ خارج المستودع. · **مُثبت** · الحركة: بصمةُ مجموعة الالتقاط في `pack.json` والشهادة.
- **SPEC-7 (P2)** `handoff/STATE.md` — citation: «`STATE.md` يُحدَّث في **نفس التزام الإقفال** … الإقفالُ بلا تحديث الحالة يُضلّل» · الجلسةُ الطازجة كانت تقرأ ٤١٦ صفحة/٣٨٤١ صفًّا بلا ذكر البند ٤. · **مُثبت** · الحركة: تحديثُ الإقفال.

## Structure

- **T-1 (P1)** `tools/capture_training.py:391` — citation: المفتاحُ الثنائيُّ المُعلَن (`eval_pack.py:814`) والضابطُ السلبيّ المقيس `poisons['foreign_document_same_number']` · الفلترُ بالرقم وحدَه، و`doc_id` بين اليد لحظةَ النداء ⇒ على القرص مستندٌ ثانٍ فعلًا (`093e1b733689193f`). · **مُثبت** · الحركة: سطرُ مقابلةٍ واحد قبل الحلقة.
- **T-2 (P2)** `tools/capture_training.py:68-76` · `tools/eval_pack.py:729,871` · `tools/oracle_confirm.py:200` — رائحة **Duplicated Code** + حكمُ الجودو: جدولُ حجوزاتٍ واحدٌ + دالّةُ إسقاطٍ واحدة + مقابلةُ هويّةٍ واحدة تُذيب ثلاثتها. · **مُثبت** · الحركة: `pack_page_numbers()` في موضعٍ واحد.
- **T-3 (P2)** `tools/capture_training.py:413-419` — citation: «الاتحادُ ≤ المجموع … جمعُها يعدّ صفحاتٍ مرّتين» · `pack_reserved` مكتوبٌ مرّتين (عدّادًا **وداخل** `skipped`) والإغلاقُ مصونٌ بتعليقِ اختبارٍ لا بقاعدةٍ في الكود ⇒ متناولٌ ثالثٌ يجمع ٧٥٩ مقابل ٦٢٦. · **مُثبت** · الحركة: عدّاداتٌ مُشتقّةٌ مرّةً واحدة + إغلاقٌ في الكود.
- **T-4 (P2)** `tests/test_capture_pack_exclusion.py:109-117` — citation: «ضابطٌ يُعيد بناء المنطق ليس ضابطًا» · الثابتُ والضابطُ يقيسان **أثرَ تشغيلةٍ سابقة** ومشغّلَ المجموعات في الاختبار، لا فرعَ الإنتاج ⇒ حذفُ الشرط يُبقيهما خضراء. · **مُثبت** · الحركة: ضابطٌ ينادي `main()` بأرضيةٍ صناعيّة.
- **T-5 (P2)** `tools/amount_guard.py:1143,1154,1161` — citation: نصُّ الحارس على نفسه («أعِد البناء») · `staleness()` بعد `return` فرعَي `--pre-push`/`--ratchet` ⇒ **الخطّافُ لا يبلغه أبداً**، ومانيفستٌ متعفّن يمرّ من بوابة الدفع (وهذا الفرعُ نفسُه احتاج إعادةَ بناءٍ بعد تغيّر المصدر). · **مُثبت** · الحركة: رفعُ الفحص فوق التفريعات.
- **T-6 (P3)** `tools/capture_training.py:311-321` — الفهرسُ لا يحمل الصنفَ الثالث ⇒ لا يُفرَّق «مُحجوزةٌ للحزمة» من «ساقطةٌ بعلّة». · **مُثبت** · الحركة: إبلاغُ التوزيع في الفهرس.
- **T-7 (P3)** `tools/capture_training.py:64-66` — مجلدٌ بدل ملف ⇒ `IsADirectoryError` مكشوف · ملفٌّ غيرُ JSON ⇒ `JSONDecodeError` مكشوف ⇒ وقوفٌ بلا اسمٍ خلافَ عُرف الملفّ. · **مُثبت** · الحركة: تغليفٌ برسالةٍ مُسمّاة.
- **T-8 (P3)** `tests/test_capture_pack_exclusion.py:24-34` — تحميلُ الوحدة بمُحمِّلٍ محليٍّ بينما شقيقُه يستوردها (`tests/test_capture_training.py:19-37`) ⇒ نسخةٌ ثانيةٌ من اعتقادٍ قائم. · **مُثبت** · الحركة: الاستيرادُ كما في الشقيق.

## Not proven

- لم يُشغّل أيُّ مقعد `capture_training --exclude-pack` بنفسه (تشغيلُه يكتب) ⇒ **أنا** شغّلتُه بعدَ المراجعة وقارنتُ: النتيجةُ مطابقة (٢٨٣/٢٠٩/١٣٣) و`PASS — الحزمةُ تشهد لنفسها`.
- «٤+٢ سقوطًا في نسخةٍ نظيفة» مستنتَجٌ من قراءة الكود + قياسِ سلوك `SystemExit` في pytest، لا من نسخةٍ نظيفةٍ فعليّة.
- أثرُ `--exclude-pack` على مستندٍ ثانٍ لم يُقَس على أرضيّةِ مستندٍ آخر (لا مستندَ آخر بـ٦٢٦ صفحة) ⇒ الحاجةُ إلى الهويّة مُثبتةٌ بالضبط السلبيّ المُلتزم لا بتشغيلة.
- قيمُ مانيفست المبالغ قبل إعادة بنائه (١١٠٢٣) لم تُقَس من سجلٍّ على القرص ⇒ مُقتبسةٌ من الشهادة لا مُقاسة.
- `SPEC-6` (إعادةُ إنتاج اختيار الحزمة بعد أول تغيّرٍ في الالتقاط) يحتاج تشغيلةً ثانيةً ⇒ غيرُ مُثبت.
- لم يُراجَع ملفُّ التسليم كمُخرَجٍ أدبيّ (قُرئ لادّعاءاته فقط).

## ما أُصلح بعد هذه المراجعة (بأوامره)

| البند | الإصلاح | الدليل |
|---|---|---|
| S-1 · SPEC-2 | افتراضٌ بنيويّ: حزمةٌ على القرص تُستثنى بلا علَم + `--no-pack-exclusion` صريح يُعلَن في البيان | الالتقاطُ بالأمر الموثَّق **بلا علَم** ⇒ ٢٨٣/٢٠٩/١٣٣ (نفسُه بالحرف) |
| S-2 · T-7 | `pack_facts`: غائبٌ · لا يُقرأ · فارغ · **قراءةٌ ناقصة** · شكلٌ مجهول ⇒ وقوفٌ مُسمّى | ١٧ اختباراً للاستثناء (منها خمسةُ أصنافِ فشلٍ مُغلَق) |
| S-4 | حارسُ وجودٍ قبل قراءة الحزمة في الاختبارات | التخطّي في نسخةٍ نظيفةٍ صار مقصودًا ومقيسًا |
| S-5 · T-1 | `expect_identity`: المفتاحُ `(doc_id, page)` | اختبارُ هويّةٍ مخالفةٍ ⇒ `SystemExit` |
| T-3 | عدّاداتٌ من `Counter` + `by_status` + `assert` إغلاقٍ **في الكود** | `by_status` يُغلق ٦٢٦ = ٦٢٦ |
| T-5 | رفعُ فحص التعفّن فوق تفريعات الأوضاع ⇒ الخطّافُ يبلغه | `amount_guard.py` (نُقلت الكتلة) |
| SPEC-3 · SPEC-4 | شهادةٌ مُلتزمة `docs/evidence/20260923-item4-pack-free-capture.json` (قائمةُ الـ٢٠٠ + **sha256 للمجموعة كاملةً**) تُعاد بـ`tools/pack_evidence.py` + تصريحٌ مؤرَّخ في `docs/EVAL_PACK.md` يُبطل ويُحيل | `PASS — الحزمةُ تشهد لنفسها` |
| SPEC-5 | المجموعتان باسمين: `excluded_pages 200` · `labeled_pack_reserved 133` · `labeled_holdout_within_pack 67` | البيانُ والفهرسُ يحملان الثلاثة |
| SPEC-7 | `handoff/STATE.md` حُدّث في التزام الإقفال | الترويسةُ + مدخلُ «آخر ما استُلم» |
| T-6 · T-8 | الفهرسُ يحمل التوزيع · والاختبارُ يستورد كشقيقه | `pack_reserved: 133` في الفهرس · ١٧ اختباراً تمرّ |

**المفتوحُ بمالكه:** `SPEC-1` (**قرار المالك**) · `S-3` · `S-6` · `T-2` · `T-4` · `SPEC-6` (بنودٌ بنيويةٌ بحاجةِ قرارٍ أو دورةٍ مُقبلة).

**القياسُ بعد الإصلاح:** `pytest -q` ⇒ **٤٣٤ passed · rc=0** · `render_claims` ⇒ «كل رقم معلن يطابق مصدره» · `qa_gate --quick` ⇒ ٣/٤ (`parser goldens` أحمرُ على `main` قبل هذا الفرع — خارج المدى).
