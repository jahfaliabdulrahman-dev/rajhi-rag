# مراجعة ٤٦ — البوابة `code-review-elite` على الختمِ وإعلانِ الانحياز (مقاعدُ معزولة)

المراجِعُ: سليمان (البوابة) · التاريخ: 2026-09-23 23:40 (+03) · المدى: `d34cd38...1528eb8` (ما راجعه كلود في ٤٥ → آخرِ التزام). فرقٌ ثلاثيّ (شجرةٌ نظيفة) · ١٩ ملفًا · +٢٤٤٤/−٣٩.
البوابات: **G1 ✓** نقطةٌ ثابتة → HEAD · **G2 ✓** لا تغييرَ غيرَ مُودَع · **G3 ✓** المعاييرُ مُتتبَّعة: `docs/handoff-protocol.md` (٢٢ قاعدة) · `docs/QA_CHECKLIST.md` · `docs/EVAL_PACK.md` · المواصفة `handoff/PROMPT-university-core.md`.
المقاعد: ثلاثةٌ معزولة (Standards · Spec · Structure)، قراءةٌ فقط، بلا تفويض، ولا كاتبَ راجع نفسَه. مُدخلاتُ كلّ مقعدٍ صيغت له (لا تاريخَ جلستي).

## Verdict

**REQUEST CHANGES** على المدى كما روجع (`d34cd38...1528eb8`) — ثلاثةُ بنود P1: صنفُ R45-2 باقٍ حيًّا في مسارَي الشهادة/الختم (سقوطُ الأداة من شجرة عمل وكتابةٌ في نسخةٍ أخرى · قارئُ ختمٍ يقرأ غيرَ شجرته)، وبوابةُ مبالغَ نُقلت إلى الخطّاف بلا سمّ.

**وأُصلحت الثلاثةُ بعد المراجعة** وقِيست (`4e..` الأسفل: القسم `أُصلح بعد المراجعة`) — التقريرُ سجلٌّ للمدى المُروَّج، لا شهادةَ صلاحٍ للحيّ.

## Standards

- **S-1** — P1 · `tools/pack_evidence.py:26,29,184,186` — `EVID = DATA/"docs/evidence"` يجعل ملفَّ الشهادة **المُتتبَّع** يُكتب في الشجرة الأمّ، ثم `relative_to(ROOT)` يسقط (`ValueError`) بعد الكتابة. **مُثبت**.
  - الحركة: فصلُ الجذرين — مُهمَلٌ (`data_root`) ومُتتبَّع (`repo_root`) — وإعلانُ الشجرة في المخرَج.
- **S-2** — P1 · `tools/pack_io.py:70-76` · `tools/eval_pack.py:903,912` — قارئُ الختم يُبنى على `data_root()`: من شجرة عملٍ يحكم `--verify` بختمٍ من **نسخةٍ أخرى**. **مُثبت**.
- **S-3** — P1 · `tools/amount_guard.py:1081-1089` — الفحصان نُقلا فوق تفريعَي الخطّاف (تغييرُ سلوكٍ أمنيّ) **بلا سمٍّ ولا اختبار**، ولا يميّز الترتيبَ اختبارٌ قائمٌ (الحالتان فارغتان هنا). **مُثبت**.
  - الحركة: دالّةٌ مُسمّاة + ضابطٌ يُحقن فيه مانيفستٌ متعفّن.
- **S-4** — P2 · `tools/eval_pack.py:891-895` · `:899-902` — المدياتُ تُقاس مرّتين، و`assert same or True` لا يمكن أن تسقط. **مُثبت**.
- **S-5** — P2 · `tools/pack_evidence.py:33-40,101` — قارئٌ ثانٍ لصفحات الحزمة وصيغةُ ختمٍ ثانية في أداةٍ أعلنت «القارئُ واحد». **مُثبت**.
- **S-6** — P2 · `tests/test_pack_seal_and_root.py:31,150-153` — ضابطٌ يُقابِل `pack_io.pack_path()` بنفسه ⇒ لا يسقط، ويمرّ حتى لو عاد المسارُ من `__file__`. **مُثبت**.
- **S-7** — P2 · `handoff/sulaiman/20260923-2100-…:82` · `20260923-2220-…:52` — قائمةُ الحالة أغلقت R45-1..3 وصمتت عن R45-4/R45-6(iii)/R45-7 (وR45-4 مفتوحٌ في الكود). البروتوكول `docs/handoff-protocol.md:258-259` يُوجب بيانَ كلّ بندٍ بحالته. **مُثبت**.
- **S-8** — P2 · `tools/pack_evidence.py:163-166` مقابل `tools/capture_training.py:40` — حقلُ `spent_before_the_pack` يجرد FM-2 وحدَه، ومعايرةُ القناع تسمّي 281·282 وهما **داخل الحزمة** ⇒ اسمٌ يجزم بجردٍ لا يقيسه. **مُثبت**.
- **S-9** — P3 · `tools/capture_training.py:427` — ثابتُ الإغلاق بـ`assert` عارٍ بينما القارئُ الجديد يوقف بـ`SystemExit` مُسمّى. **مُثبت**.

**المقعدُ الأول: ٩ بنود** (٣ P1 · ٥ P2 · ١ P3) — وأخطرُها داخل محوره **S-1/S-2**: إصلاحي أنا نقض شرطَه في مسارٍ لم يخصّه (`docs/evidence` مُتتبَّعةٌ لا مُهمَلة).

## Spec

- **F1** — P1 · `handoff/PROMPT-university-core.md:109` مقابل `docs/evidence/20260923-item4-pack-free-capture.json` — شرطُ «ولا صفحة منها تُستعمل في أيّ ضبط» **منقوضٌ نهائيًّا** (١٣٣ من ٢٠٠ أُنفقت في FM-2)، والإعلانُ يُسعّره ولا يُعيد الخاصّية، ولا سكربتَ تقييمٍ يطبعه إلزامًا (`docs/ROADMAP.md:39` غيرُ منجز). **مُثبت**.
- **F2** — P1 · `tools/oracle_confirm.py:199-208` — **الشاهدُ المدفوع** يتّخذ إحصاءَ الحزمة مصدرًا افتراضيًّا لصفحاته ⇒ `--max-usd 1.5` بلا علَم يقرأ ٥٠ صفحةً مجمّدة: الحمايةُ مُسمّاةٌ لا صنفيّة. **مُثبت**.
- **F3** — P2 · `tools/pack_io.py:118-119` — اختلافُ هويّة الحزمة يرفع `SystemExit` ⇒ إعادةُ التقاط مستندٍ آخر (موثَّقة) تفشلُ بلا عطب. **مُثبت**.
- **F4** — P2 · `tests/test_pack_seal_and_root.py:33,119-120,168` — القياسُ الحيّ تحت `skip` بلا `data/` ⇒ في مستنسخٍ نظيف «أخضر» تعني «لم يُقس شيء»؛ لا حارسَ في CI للثابت. **مُثبت**.
- **F5** — P3 · `tools/pack_evidence.py:33-37` · `docs/claims.json` — أرقامُ الإعلان مُثبَّتةٌ في حرفيّات اختبارٍ فقط، بلا مدخلٍ في `claims.json` ⇒ انحرافُ الوثيقة بلا سقوط (صنفُ العطب الذي كلّف 1528eb8). **مُثبت**.

**المقعدُ الثاني: ٥ بنود** (٢ P1 · ٢ P2 · ١ P3) — وأخطرُها **F2**: الطبقةُ كلُّها تحرس ساحبَين بأسمائهما، والثالثُ المدفوعُ مفتوح.

## Structure

- **STR-1** — P1 · `tools/pack_io.py:19` · `tools/pack_evidence.py:24,33-37` — «القارئُ الواحد» أُنجز في موضعين ونُقض في ثالث: قارئٌ خامسٌ **أرخى** (يُمِرّ `"pages":["5","6"]` نصًّا حيث `pack_io` يقف) ⇒ بصمةُ الختم تُولَّد بقراءةٍ أضعفَ من القراءة المقابِلة لها. **مُثبت**.
- **STR-2** — P2 · `tools/pack_evidence.py:101` ↔ `tools/pack_io.py:130-132` — صيغةُ الختم مكتوبةٌ مرّتين (كاتبًا وقارئًا). **مُثبت**.
- **STR-3** — P2 · `tools/eval_pack.py:890-902` — شرطٌ واحدٌ صار فرعين في نفس الدالّة + `assert same or True` ميّت. **مُثبت**.
- **STR-4** — P2 · `tests/test_pack_seal_and_root.py:31,150-153` — حارسٌ لا يسقط (تعريفٌ يُقابَل بنفسه). **مُثبت**.
- **STR-5** — P2 · `tools/compare_prompts.py:129` ↔ `tools/capture_training.py:80-87` ↔ `tools/pack_evidence.py:97` — سياسةُ «لا حزمةَ على القرص» بثلاث إجاباتٍ مختلفة. **مُثبت**.

**المقعدُ الثالث: ٥ بنود** (١ P1 · ٤ P2) — وأخطرُها **STR-1** (نقضُ الوعد نفسه في الفرق الذي وعد به)، ومحورُ الجذر والفشل المُغلَق أُذيب فعلًا ✓.

## تحقّقي المستقلّ (لا finding بلا قراءةِ مصدره)

| الادّعاء | القياسُ الذي أجريتُه | النتيجة |
|---|---|---|
| S-1/S-2 | `git worktree add` ثم تشغيلُ الأمر الموثَّق من داخله | **صحيح**: سقط `ValueError` **وبعد** أن كتب في الشجرة الأمّ ✓ |
| STR-1 | تمرير `{"pages":["5","6"]}` للقارئين | **صحيح**: `pack_io` وقف، والقارئُ الثاني مّررها نصًّا ✓ |
| F2 | قراءةُ افتراضات `oracle_confirm` | **صحيح**: الإحصاءُ مصدره الافتراضيّ = ٥٠ صفحةً من الحزمة ✓ |
| S-6/STR-4 | قراءةُ الضابط | **صحيح**: تعريفٌ يُقابَل بنفسه ⇒ لا يستطيع السقوط ✓ |
| S-8 | عضويّة ٢٨١/٢٨٢ في الحزمة | **صحيح**: كلتاهما داخل الحزمة (و٤٤٩ خارجها) ✓ |
| S-3 | هل ثمّة اختبارٌ يميّز الترتيب؟ | **صحيح**: لا شيء في `tests/`، والحالتان فارغتان ⇒ لا مُميِّز ✓ |
| S-7 | `grep` على بنود المراجعة ٤٥ | **صحيح**: R45-4/R45-6/R45-7 موجودةٌ ولم تُذكر بحالتها ✓ |

## أُصلح بعد المراجعة (بدورةٍ واحدةٍ مُقيَّدة، كلُّ بندٍ بمصدره)

| البند | الإصلاح | الدليلُ المقيس |
|---|---|---|
| S-1 · S-2 · STR-5 | جذران مُسمَّيان: `repo_root()` لِما هو مُتتبَّع (`docs/evidence`) و`data_root()` للأدلّة الثقيلة · `absent_ok` سياسةٌ واحدةٌ للغياب · الختمُ يُعلن شجرته | الأمرُ الموثَّق من شجرة عمل: **اختبارٌ نائمٌ يقيسه** ⇒ `capture --limit 2` من worktree أعلن «٢٠٠ مستثناة» ✓ |
| STR-1 · S-5 · STR-2 | `pack_evidence` يستورد `pack_pages`/`pages_sha256` — حُذف القارئُ الخامس والصيغةُ الثانية | البصمةُ بعد الإصلاح **`f41f8ccb66f6f0c2`** = المُلتزمة ✓ (القارئُ نفسه كاتبًا وقارئًا) |
| S-3 | `pre_push_checks()` دالّةٌ مُسمّاة يبلغها الخطّاف + **سمّ**: مانيفستٌ متعفّن | الاختبارُ الجديد: مانيفستٌ متعفّن ⇒ `== 1` ✓ (والغائبُ ⇒ 0 لأنّه مسارُ «سطحٍ بلا أدلّة») |
| S-4 · STR-3 | قياسُ المديات مرّةً واحدة تُغذّي الطباعةَ والحكم · حُذف `assert same or True` | الدالّةُ الآن `ranges_ok = all(range_checks)` ✓ |
| S-6 · STR-4 | الضابطُ الفارغ استُبدل بقياسِ **الأمر نفسه** من شجرة عملٍ منفصلة | `test_documented_capture_from_a_worktree_announces_the_pack` ✓ (كان يمرّ كذبًا) |
| F2 | الشاهدُ المدفوع يقف على صفحات الحزمة إلا بعَلَمٍ صريح `--allow-pack-pages` | `oracle_confirm --dry-run` بلا علَم ⇒ rc≠0 بالاسم ✓ |
| F3 | اختلافُ الهوية ⇒ «لا استثناء + إعلان» لا وقف | اختبارٌ محدَّث: `pages == set()` و`note` تذكر «لمستندٍ آخر» ✓ |
| S-8 | الجردُ يُعلن مداه: مصدران + `other_known_tuning_pages` (281·282 داخل الحزمة، 449 خارجها) + تحذيرُ التعداد | الشهادةُ المُعاد توليدها ✓ |
| R45-4 | **إغلاقُ القرص**: تشغيلةٌ كاملةٌ تُزيل كلَّ `pg-` لا تُلتقط (والجزئيّةُ تُعلن ولا تُغلق) | `close_out_dir` + اختبارٌ على مجلدٍ حقيقيّ (أُزيل 2·9 وبقيت 4·5) ✓ |
| حارسٌ جديد | **لا تعريفٌ ثانٍ بالاسم نفسه** في `tools/` و`src/` (سقطتُ فيه بنفسي: تضاعفُ `capture_training.py`) | `tests/test_no_duplicate_definitions.py` — ٧٦ ملفًا ✓ |

**الاختباراتُ بعد الإصلاح:** **524 passed · rc=0** · `static_gate` PASS · `publish_guard` آمن · الشهادةُ مُعادُ توليدها ببصمةٍ مطابقة ✓.

## Not proven

- **F4 لم يُصلَح (مُعلَنٌ بمالكه):** الثابتُ الحيّ لا يراه CI لأنّ الأدلّةَ الثقيلةَ **خارج git بالتصميم**؛ وحارسُه الفعليّ في `.githooks/pre-push` حيث توجد `data/`. وجعلُه في CI يعني نشرَ الأدلّة ⇒ **قرارُ مالك**.
- **F5 لم يُصلَح:** أرقامُ الإعلان ليست في `docs/claims.json` ⇒ لا كاشفَ انزياحٍ آليّ (اليوم مطابقة، والانحرافُ محتملٌ لا واقع).
- **F1 لا يُغلق بحركةٍ تقنيّة:** المجموعةُ الحرّة ٢٠٨ كلُّها `%3==0` وأطولُ مدًى ١ ⇒ لا بديلَ انتقاءٍ ممكنًا؛ أُعلن الانحيازُ بقرار المالك (أ) 2026-09-23، والحركةُ القابلةُ أن يُطبع الإعلانُ إلزامًا في مخرَج أيّ جدولٍ لاحق.
- لم أُعِد تشغيل المقاعد بعد الإصلاح (لا ضمانَ تقارب) — البنودُ المُصلَحة قِيست بضوابطَ ساقطةٍ ثم خضراء لا بمراجعةٍ ثانية.
- ما لم يُقس: أثرُ التباعد على بياناتٍ حقيقيّة في `pack_evidence` (قِيسَ الشكلُ لا الأثر الماليّ) · `compare_prompts` لم تُشغَّل (تكلّف نداءً) · `end-to-end` على خدمةٍ مُعاد تشغيلها · دقّةُ الأرقام الماليّة نفسِها (خارج حدود اختباراتي).
