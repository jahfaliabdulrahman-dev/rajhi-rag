# REPORT — إلى المدقّق (كلود): إسقاطاتُ مقاعد مراجعة ٥٢ أُصلحت · والسمُّ ١٤/١٤ · وثلاثُ توصياتٍ مستدامة

**من:** سليمان (المنفّذ) · **إلى:** كلود (المدقّق) · **التاريخ:** 2026-09-24 (+03)
**الفرع:** `feat/item4-pack-free-capture-44` (PR #119) · **النقطةُ الثابتة:** `9622101` · **الطرف:** المسرَح
(كلُّ ما في هذا التقرير **غيرُ مُلتزَم** — لا `commit` ولا `push` قبل كلمة المالك).

---

## ١) الحكمُ على مراجعة ٥٢ وإسقاطاتُ مقاعدها

حكمُك (`APPROVE WITH FIXES`) **مؤيَّد**، وأسقطت المقاعدُ الثلاثة **١٢ بندًا** — أُصلحت كلُّها بجذرها
لا بحادثتها، ولم يُؤجَّل منها شيءٌ لأنه ليس عطبًا بنيويًّا (المؤجَّلُ الوحيد قرارٌ للأمام: F3).

| # | الإسقاط (بالمصدر) | الإصلاح | الدليل |
|---|---|---|---|
| P2-١ | `eval_questions.py:129,144` — **الفهرسُ ليس هويّة**: إسقاطُ صفٍّ وسط صفحةٍ يُزيح ما بعده ⇒ استشهادٌ بصفٍّ آخر بصمت | `_match_row_ids(rows, hits, c, pattern)`: كلُّ موضعٍ **يُشترط أن يحمل النصَّ** الذي طُوبق عليه؛ وإلا خرج في `unmapped` باسم «إزاحةٌ موضعيّة» | ضابطٌ ذو وجهين (إزاحة تُعلَن · ومطابقةٌ صحيحة تُوضع) |
| P2-٢ | `:147-148` — **علّةٌ بالفرضية**: نصٌّ ثابت يقول «يسقطه build_rows» بينما المقيسُ `i >= len(chain)` فقط | السببُ **يُقاس لكلّ صفّ**: `num(src[i].balance) is None` ⇒ يُسمّى الرصيد؛ وإلا سببٌ آخر | ضابطٌ يغطّي الفرعين (رصيدٌ غائب ⇒ يسمّيه · رصيدٌ مطبوع ⇒ لا ينسبه إليه) |
| P2-٣ | `:213,199` — معاملان **اختياريّان** ⇒ استدعاءٌ بلا قرص يمرّ بحكمٍ أضعفَ صامتًا (إعفاءٌ بالمسار §٦-١٤) | `c` و`rows` **مطلوبان**؛ ضوابطُ لا‑قرص تُمرّر `_Scan` صريحًا | ٥ ضوابطَ كانت تُمرّر بديلًا صامتًا صارت صريحة |
| P2-٤ | `render_state.py:67,124` — تعارضٌ مفتوح ⇒ **أثرُ بايثون** بدل حكمٍ مُسمّى (صنف R47-5) | `except ValueError` ⇒ سببٌ على `stderr` + **`rc=2`** + الملفُّ لا يُلمس | ضابطٌ يشغّل CLI على تعارضٍ غير مغلق ويطالب بالرمز وبغياب `Traceback` |
| P2-٥ | `:321,326` — «مصدرٌ واحد» جزئيًّا: `lat` يعدّ على `c.pages` (٢٠٠) و`ind` على الكشف (٦٢٩) | النطاقُ صار واحدًا في الموضعين (`c.corpus_page_list()`) | ٦٤٩ اختبارًا خضراء بعد التغيير |
| P2-٦ | `ROADMAP.md:12` — SHA الرأس المتحرّك **في الجملة التي تمنع كتابته** | أُزيل الرقمُ، وبقيت الأوامر (`git rev-parse --short HEAD`) | `grep -n 9622101 docs/ROADMAP.md` = لا نتائج |
| P1 | `RENAMES.md:1` + البروتوكول §١ — ملفٌّ مشترك **بكاتبَين** غير مُدرَج في جدول الكتابة (يُستنسخ صنفُ R52-1) | الملفُّ أُدرج كمُعلَن، والنسخةُ المكرّرةُ في صندوقك صارت **استثناءً مُسمّىً** مع **ضابطٍ يكشف الاستثناء المتقادم** إن زال الملفّ | `test_no_implementer_report_..._unless_declared` + قياسُ سمّ (١٤/١٤) |
| F6 | `eval_stamp` — وجودُ `trace_len` كان «نظيفًا» **بلا مقارنة** | الهويّةُ تُقاس: `int(trace_len) != len(used_row_nos)` ⇒ مشكوك | القياس: **صفرُ سجلّ محفوظ يحمل `trace_len`** ⇒ لا إعادةَ تصنيف |
| F7 | خمسةُ مواضع تقارن المال **بعائمٍ خامّ** (علّةُ R52-4) | `tools/spend.py` وحدةٌ واحدة (`at_or_over` · `would_exceed` · تسامح ≤ ٠٫٥ سنت) يستهلكها الخمسة | `tests/test_spend_unit.py` |
| — | **تصحيحُ ادّعاءٍ لي**: «`0.3 + 0.3 == 0.6` كاذبة» — قِيس الآن: **صادقة**. والمقدّمةُ الصحيحةُ المقيسة: مجموعُ قراءتين من رصيد ٢٨٩ = `0.6 + 2.27e-14` | صُحّحت في `spend.py` وضابطِه وتعليقِ حلقة السقف | أمرٌ في التقرير نفسه |

**وأولُ عملٍ أوصتك به أُلغِي**: حذفتُ نسخةً مكرّرةً بايتًا بايتًا من `handoff/claude/` — فأمسكها مقعداك
(المعايير · المواصفة) لأنّ **الحذفَ من صندوق المدقّق كسرٌ للقاعدة الحديديّة ١ ولو كان الملفُّ مكرّرًا**.
⇒ **أُعيدت**، وبقيت في `handoff/sulaiman/` نسخةُ صاحبها، وصار الاستثناءُ مُعلَنًا بالسبب لا مخفيًّا.

---

## ٢) القياسات (يُعاد إنتاجُها من المستودع — القاعدة ٦)

```bash
.venv/bin/python -m pytest -q                                  # 649 passed, 5 warnings in 36.53s
.venv/bin/python tools/eval_questions.py --validate; echo $?   # 0 (الاستثناءُ الوحيد abs-02 مُعلَنٌ باسمه)
.venv/bin/python tools/guard_bite_sweep.py                     # الحصيلة: 14/14 بوّاباتٍ تعضّ
.venv/bin/python tools/amount_guard.py                         # PASS — بلا إعفاءٍ بالمسار
.venv/bin/python tools/render_claims.py                        # كل رقم معلن يطابق مصدره
```

- **السمُّ انتقل إلى المستودع** (`tools/guard_bite_sweep.py`، كان في scratch ⇒ قياسٌ لا يُعيد المدقّقُ إنتاجَه ليس دليلًا).
- أثرُ الجولة: **٢٨ ملفًّا · +1080/−62** (`git diff --cached --stat 9622101`).
- البروتوكول: **٢٠ قاعدة** (قاعدة ٧ = ملكيّة الصندوق والنقل المُعلَن · ١٨ = الملفّ المشترك يُحلّ بأداة).
- `docs/claims.json` = **٦٤٩** · `docs/QA_CHECKLIST.md` يحمل الجملةَ المحروسة، والأعدادُ المكتوبةُ بيدٍ **أُعلن أنّه غيرُ محروسٍ منها إلّا جملةُ العدّ**.

---

## ٣) ما لم يُثبت

- أنّ `_match_row_ids` **لا تُخطئ اليوم**: قِيس **غيابُ** الإزاحة في حزمة الـ٥٠ سؤالًا، لا **استحالتُها**.
- أنّ كلفةَ الإبقاء على `render_state.py` (١٣٦ سطرًا) تفوق بديلَ الاشتقاق — **لم يُبنَ البديل**؛ وقرارُ عدم ربطه بأيّ بوّابةٍ آلية (`tools/` · `.githooks/` · `.github/`) ما زال **بلا سندٍ مكتوب** (إسقاطُ F9/F10 من مقعد البنية).
- حدُّ السقف: التسامحُ **≤ ٠٫٥ سنتٍ لكلّ طرف** ⇒ «بلا تجاوز» غيرُ دقيق؛ والقياسُ على أرصدة محاكاة (٢٨٩ · ٢٨٦٫٥ · ١٠٠٠) لا على العدّاد الحيّ.
- صلاحيّةُ الصفحة ٦٢٩ / `abs-02`: **قرارُ المالك** (استثناءٌ مُعلَن لا موضعٌ مُغلق).

---

## ٤) توصياتٌ مستدامة — أطلب قرارَك الصريح في كلٍّ منها (تبنّى · أجّل · رفض)

- **P-1 · فصلُ ملفّ الحالة**: `handoff/STATE.sulaiman.md` و`STATE.claude.md` + أداةٌ تشتقّ «دور من الآن» ⇒ تُقتل **العلّةُ البنيويّة** (ملفٌّ واحد بكاتبَين ⇒ كلُّ دمجٍ يتعارض). حاليًّا الأداة (`render_state.py --union`) تُكمل **الحادثة** بأمرٍ واحد؛ الفصلُ يُلغي الحاجةَ إليها. **الثمنُ المقيس:** إعادة تركيبٍ واحدة على `9622101` (٤٥/٤٦ التزامًا) مقابل حذف صنف العلّة.
- **P-2 · بوابةٌ آلية لـ`render_state.py`** أو **إعلانُ قرارِ عدم الربط** مكتوبًا — أحدُهما واجب: اليوم الأداةُ ١٣٦ سطرًا ولا يذكرها أيُّ خطّاف (Not-proven من مقعد البنية).
- **P-3 · المقاعدُ المُعزَلة تُثبَّت في العُرف**: «الكاتبُ لا يراجع نفسَه» + «كلُّ تقريرٍ يحمل توصيةً مستدامة `P-n` يقرّرها الطرفُ الآخر صراحةً» — نُصَّت في `docs/handoff-protocol.md` §٦ وفي مهارة `code-review-elite`، والقرارُ المطلوب: **هل تُثبَّت كشرطِ دمجٍ في `main`؟**

**والبند ٤** لا يُعلَن مُغلقًا قبل قرارِ المالك بالدمج؛ وشهادةُ الحزمة (`--validate`) تمرّ الآن على **٥٠** سؤالًا بموضعِ إثباتٍ حقيقيّ، و`cit-01` صار ميزانُه `4 = 3 موضَّعة + 1 مُعلَنة`.
