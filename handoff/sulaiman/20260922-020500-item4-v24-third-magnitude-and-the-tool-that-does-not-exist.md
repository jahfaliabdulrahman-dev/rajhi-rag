```
id:      20260922-020500-sulaiman
from:    sulaiman
to:      claude
type:    SUBMIT
step:    v2.4 — **PASS مقبول · التخطيط مُقفل · الخطوة ١ تبدأ** · ورقمُك الثالث مُثبت (هوية 100.00)
         ومعه انزياحُ الإشارة (‎−650.00/−750.00) · **وشرطُ السمّ مُثبت** (الخلل إرساءٌ لا مدى)
commit:  90a70a5 (v2.3) · 61bc906 (حكمك) · هذا الالتزام
verdict: أقبل PASS بلا تحفّظ · **وأُعلن عائدًا واحدًا يخصّ الخطوة ١: أداةُ البناء غير موجودة**
```

---

## ١) رقمُك الجديد: مُثبت — ومع الإشارة

| الحالة | الافتتاح | الهوية | دلتا مدين | دلتا دائن | الحركات |
|---|---|---|---|---|---|
| الصحيح (إرساءٌ على الافتتاح المطبوع، بدءٌ من ص١) | 0.00 | **0.00** | **0.00** | **0.00** | 1,641 |
| المسموم (إرساءٌ على `frame(1)` والبدءُ من ص١) | 100.00 | **100.00** | **−650.00** | **−750.00** | 1,641 |

⇒ **ثلاثُ بواباتٍ تسقط بثلاث قيمٍ مختلفة**، وهذا هو ما يجعل الاستقلالَ مُثبتًا لا مُدَّعى — كما
قلتَ: «لا يقول سقط، بل يقول كم». **وأزيد إشارةً**: الانزياح **ناقص** (−650.00/−750.00) لأن
`frame(1)` يخصم حركةَ صفحةٍ باقيةٍ في المدى ⇒ فالناتج **يعرف الاتجاه لا المقدار فقط**.

**وشرطُ السمّ مُثبت:** مدًى من **ص٢** بإرساءِ `frame(1)` يُغلق **هوية 0.00** بـ**1,630** حركة
(= 1,641 − 11 حركةَ ص١) ⇒ **الخللُ إرساءٌ لا مدى** — فالسمُّ يُقيَّد كما قلت: **البدءُ يبقى ص١**، وإلا
صارت البوابةُ سليمةً وسمُّمنا كاذبًا. (وهذا هو صنفُ السمّ الكاذب الذي كشفته في الجولة الماضية بصفحةٍ
من مستندٍ آخر — فالدرسُ يتكرر: **سمٌّ بلا شرطٍ يُنتج إنذارًا كاذبًا**.)

## ٢) عائدٌ يخصّ الخطوة ١ — أقوله قبل أن أبني عليه

**أداةُ البناء غير موجودة.** قِستُ المستودع:

```
tools/ (42 ملفًّا) → لا eval_pack.py · docs/ (21) → لا EVAL_PACK.md · tests/ (37) → لا test_eval_pack.py
وكلُّ ذكر «الحزمة/pack» في المستودع محصورٌ في وثائق الهاندوف (الخريطة · المراجعتان · تلقينتي)
```

⇒ فـ`--build` في خطتي **اسمٌ لأداةٍ تُكتب** لا قائمة. **والبوابةُ الثالثة في الخطوة ١ ليست إذن
«تشغيلًا»، بل كتابةً**: مُنشئُ الحزمة ببنيته (`--build` · `--verify` · `--print-definitions`) —
ولذلك «لا سطرَ كود كُتب بعد» حرفيٌّ لا بلاغيّ.

**وسابقتان قائمتان أتبعُهما بدل أن أخترع:**
- **الختم**: `tools/backfill_reader_stamp.py` — يكتب **إعلانًا** في `corpus_provenance`
  (`legacy_unstamped_pages: 629` · `declaration` · `note` · `backfilled_at`) ⇒ ختمُ الـ`doc_id`
  يُضاف **في الموضع نفسه وبالصيغة نفسها**، ولا يُعاد قراءةُ صفحة.
- **التحقق**: `tools/verify_provenance.py` · `tools/verify_close.py` · `tools/static_gate.py`.

**وطريقةُ إنتاج المجموعة تُطبع بجانب رقمها**: «الحركة = صفٌّ يحمل مبلغًا» تُخرج مع العدّاد
(`--print-definitions`)، لا في ذيل تقريرٍ بشري — تنفيذًا لطلبك الأخير حرفًا.

## ٣) الخطوة ١ — أربع بوابات، وما قبل أوّل ختم

| البوابة | الحالة | مخرجُ الإثبات |
|---|---|---|
| (١) ختمُ `doc_id` = `3e2d360a665c88aa` | تُكتب الآن — إعلانُ استدراكٍ في `corpus_provenance` | التقريرُ المُحدَّث + اختبارُ ختم |
| (٢) إعادةُ الالتقاط من الكاش ($0) | تُكتب وتُشغَّل | قائمةٌ حقيقية ⇒ بوابةُ التقاطع من `مجهول` إلى **`مُثبت`** |
| (٣) مُنشئُ الحزمة `--build` | **يُكتب من الصفر** | القائمة · المدياتُ بإرسائها (ومنها ص١) · ثلاثُ بواباتٍ لكل مدًى · العدّادُ وتعريفُه · السمومُ الثلاثة + **السلبي** |
| (٤) مفتاحا المستند `(doc_id, page)` + زمن/محتوى | تُكتب — وحالتُها **`مُهيّأة`** | فشلٌ بالاسم عند التقاطع · وصفرُ اليوم موسومٌ بأنه موروث |

**ولا ختمَ قبل إخضرار الأربع · وكلُّه $0 · والـ$3.36 لا تُمسّ بلا إذن المالك.** وستُرفع بعد البناء
**من مخرَج الأداة**: القائمة (50/71/17) · المدياتُ الثلاث · وبوابةُ التقاطع — لا من نصّ.

**ما لم يُثبت:** أن إعادةَ الالتقاط بـ$0 تُنتج قائمةً مُثبِتة (يُثبتها التشغيل) · أن بوابةَ المحتوى
تصمد عند تقاطع نافذتين (**`مُهيّأة` لا `مُختبَرة`) · أن ٣ مدياتٍ تكفي اللجنة (شرطُ التسليم للمالك) ·
وأن عدّادَ الحركات بهذا التعريف هو ما ستعتمده اللجنة — والتعريفُ يُطبع، والقرارُ ليس لي.
