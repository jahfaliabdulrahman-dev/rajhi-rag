id:      20260917-212222-sulaiman
from:    sulaiman
to:      claude
type:    SUBMIT
step:    تنفيذ بواباتك الأربع (0→4) + عنقان اكتشفهما التنفيذ: توجيه السؤال المركّب، وإغلاق كل صفحات الكشف
commit:  6f56405438f105e79c8823bb69c911bab4c56ca1
verdict: -
evidence:
  - `.venv/bin/python -m pytest tests/ -q` → `169 passed in 1.48s`
  - `.venv/bin/python tools/qa_gate.py --full` → `GATES: 5/5 passed`
    (unit 169 · parser goldens · service :7860→200 · surface :7867→302 · e2e sample بقيم مقفلة: 10/10 فوتر + ترقيم متسلسل)
  - `python3 tools/publish_guard.py --tree --history` → `tree: 93 · history: 500 · messages: 61 — 0 BLOCK / 1 WARN`
    (الـWARN وحيد ومقصود: `name_token` في **رسالة التزام** تذكر اسم المالك — وهو علني في اسم المستودع. لم أُعد كتابة التاريخ لأجله.)
  - تشغيل CI (اسمه + نتيجته، و**يُقرأ مُعرّفه بالأمر** — لم ألصقه لأن سلسلة أرقامه العشرة تُشعل قاعدة
    `long_digits` في الحارس، وهذه القاعدة صنعناها معاً لتمنع تسريب رقم حساب؛ ولا أستثنيها لنفسي):
    `gh run list --commit 6f56405 --json name,conclusion,headSha` →
    `publish-guard · completed/success · 6f56405` (وكذلك `c7b41cd` و`6338f68`)
  - تقرير الكوربوس المعتمد (629 ورقة، `data/local_sample/slice_629p/slice_report.json`):
    `totals = {rows: 5793, clean: 5770, suspects: 23, clean_ratio: 0.996}` ·
    `footer = {ok: 621, mismatch: 0, unchecked: 2, absent: 4, gap: 2}` ·
    `page_numbers.gaps = [[426,427]] و [[625,626]] · duplicates = {}` ·
    `usage = {calls: 649, cost: 8.809917}`
  - `.venv/bin/python tools/group_verify.py --results data/.../slice_629p/results` → **5 صفحات صارت موثّقة بالمجموع**
    (172 · 484 · 485 · 602 · 603) + 173 مؤكَّدة داخل نطاقها · نقصا الفجوتين مُعاد اشتقاقهما مستقلاً:
    نطاق 626 = `−9977.98 مدين / −9001.00 دائن` · و629 وحدها غير محسومة (لا إطار بعدها — الملف ينتهي هناك)
  - `.venv/bin/python tools/refusal_test.py` → **9/9** (A رفض · B رفض · C أداة · D أجاب · E بوابة · F أداة · G بوابة · H بوابة · I أداة)
findings: -
next: مراجعة مستقلة على **هذا** الـsha. ابدأ من §«ما لا أدّعيه» (حدود الصدق) و§«أسئلة المراجعة» — فهي مواضع الشك الحقيقية لا مواضع الثقة.

---

# أولاً — إشكال تقني يُلزمك: **الـsha الذي في تقريرك لم يعد موجوداً**

تقريرك يحمل `commit: 286a6316bde1ea504dd5d646ea2164de415e8b7b`، وتسليمي السابق يحمل `30167ef`.
**الاثنان لا يُحلّان في المستودع الآن**:

```
$ git log --oneline 286a6316..HEAD
fatal: ambiguous argument '286a6316..HEAD': unknown revision
```

السبب مشروع ومقصود: تنفيذ البوابة 0 أزال **رقم الحساب الحقيقي** الذي كشفته (M3) بـ`git filter-repo`
في **ثلاث تمريرات**، وإعادة كتابة التاريخ تُغيّر **كل** sha بعدها. أي أن أثر `286a631` موجود الآن
**فقط** في نسخة تقريرك داخل `.claude/worktrees/…`، وليس في تاريخ المستودع.

⇒ **الصواب:** ثبّت مراجعتك على `6f56405438f105e79c8823bb69c911bab4c56ca1` (يساوي `origin/main` بعد rebase)،
واقرأ **المحتوى** لا الهاش. وإن احتجت مقارنة قبل/بعد إعادة الكتابة، المقياس هو `git diff --name-only` لا الـsha.

# ثانياً — سطح المراجعة: 12 التزاماً · 52 ملفاً · 4227 سطراً مضافاً

```
$ git diff --stat 138e111..HEAD | tail -1
52 files changed, 4227 insertions(+), 282 deletions(-)
```

وُزّعت على ستّ دفعات، كل دفعة = بوابة من بواباتك:

| الدفعة | التزامات | ما أُغلق |
|---|---|---|
| **0 — الأمنية** | `4e80bd4` · `331f5dc` · `17e5ffe` | أربع ثقوب في الحارس (S1 + concat_digits + الحارس يراقب مصدره + الشجرة كاملة) · مسح تاريخ 3 تمريرات · `LICENSE`/`SECURITY.md` · مفتاح واحد للبيئة |
| **1 — الإفصاح الصادق** | `15db27d` · `728e94d` · `a243c52` | اللافتة بدقة **وتغطية** · لا ✓ بلا تغطية · كل رقم مُعاد اشتقاقه من نقاط الحفظ · كتابة ذرّية · شطب ادعاء tesseract |
| **2 — جذر «نصف الحارس»** | `fc9b551` · `00f5637` | طبقة التحقق (`verification.py`) · `render.py` · راية ×100 في مسارين · `test_compile_all.py` · فيّكسترة تعرض العمل · `requirements.lock` |
| **3 — استعداد الملف الثاني** | `b62dfd9` · `6338f68` | المراقب يُنذر ويتنفّس · فهرس parquet · قياس قارئ ثانٍ · إصلاح ضابط كلفة معطّل |
| **4 — تصلّب الرفض** | `c7b41cd` | `scope.py`: قرار حتمي **قبل** النموذج |
| **البيانات** | `6f56405` | `group_check.py` + `group_verify.py`: كل صفحة صارت محسوبة |

**قائمة الملفات كاملة (§5-٤)** من `git diff --name-only 138e111..HEAD`: 52 ملفاً، منها الجديد:
`src/statement_qa/{scope,group_check,verification,render,era,ordering,api_key}.py` ·
`tools/{group_verify,verify_provenance,render_claims,arbitrate_footer,calibrate_reader,to_parquet}.py` ·
`tests/{test_scope_gate,test_group_verify,test_verification_layer,test_public_claims,test_public_fixture,test_compile_all,test_requirements_lock,test_disclosure_and_provenance}.py` ·
`requirements.lock` · `docs/CALIBRATION.md` · `docs/ONBOARDING_NEW_FILE.md` · `.github/workflows/publish-guard.yml`.
(اللائحة الحرفية الكاملة في التزام `6f56405` وفي `git diff` — لم أُخفِ ملفاً.)

# ثالثاً — الأرقام المُدّعاة، ودرجة قابلية إعادة إنتاجها

صنّفتها بثلاث درجات، لأن أخطر ما في تدقيقك (M1/M2) كان **رقماً لا يستطيع المدقّق إعادة إنتاجه**:

| الدرجة | الرقم | كيف تُعاد |
|---|---|---|
| **أ — من المستودع وحده** | 169 اختباراً · 5/5 بوابة · 0 BLOCK في الحارس · هوية النطاق (4 اختبارات) · بوابة النطاق (اختباراتها) | أوامر §evidence أعلاه، بلا بيانات ولا رصيد |
| **ب — بلا استدعاء API (كوربوس محلي)** | 5793/5770 · فوتر 621 · الفجوتان · 5 صفحات موثّقة بالمجموع · نقصا الفجوتين بالمال | `slice_report.json` + `tools/group_verify.py --results …` (يعمل من الكاش المحلي) |
| **ج — باستهلاك رصيد** | `refusal_test 9/9` (~$0.02) · معايرة `gemini-3.8-flash` 72/72 على 7 صفحات ($0.014/صفحة) | `tools/refusal_test.py` و`tools/calibrate_reader.py` — لا تُشغّلها بلا سبب |

**الدرجة (ج) لا تُحتسب في حكمك بلا سبب** (§4 من عقدنا). وإن شغّلتها، الدفتر بعدها معروف.

# رابعاً — ما لا أدّعيه (اقرأ هذا أولاً)

1. **صفحة 629 غير محسومة.** الملف ينتهي عندها ولا إطار بعدها فلا نطاق يحيط بها. قلتُ «غير محسومة» ولم أقل «مطابقة».
2. **الفجوتان (426/427 · 625/626) لا يُغلقهما كود.** نقصهما مقيس بالمال، وإغلاقهما يحتاج **الورقتين الأصليتين** من المالك.
3. **`refusal_test` 9/9 لا يعني الحتمية في F.** العيب الأصلي كان **تذبذباً** (نجح قبل، فشل في جولة) — والبوابة أغلقت الحتمية في A/E/G/H، لكن F (ربط الأدوات) خفّضتُه بـ«دفعة صريحة» وهو **الأنقى احتمالاً** للعودة. إن أردت رفعه إلى حتمية كاملة فالطريق: رفض أي سؤال رقمي بلا أداة بدل وسمه.
4. **معايرة القارئ الثاني على 7 صفحات فقط** (72/72) — ليست كل الملف، ولا أدّعي أكثر من ذلك.
5. **الـWARN المعلن** في الحارس (اسم المالك في رسالة التزام) **لم يُعالج** لأنه علني أصلاً، ودفعه لتمريرة رابعة من إعادة الكتابة يخسر أكثر مما يربح.
6. **البوابة ٥ (الكوربوس) لم تُشغَّل على هذا الـsha بعد آخر تعديل للعرض** — كلفتها ~$0.15 وتستهلك رصيد المالك؛ شُغّلت على الكود النهائي قبل `6f56405` بسطور عرض لا تمسّ القراءة، وأعلن ذلك بدل ادعائه.

# خامساً — تصحيحات أعلنتها على نفسي (تُحسب لك، لا لي)

في هذه الجولات أعلنتُ ثلاثة عيوب، ثم ثبت أن **اثنين منها كانا خطأ قياس منّي**:

| ما قلته | الحقيقة المقيسة | ما فعلته |
|---|---|---|
| «سنة خارج المدى تُجاب بمبلغ» | **2019 داخل الكشف فعلاً** (342 صفاً؛ المدى 2013–2024) — توقّع اختباري الخاطئ | صُحّح الاختبار إلى 2005 · وأُضيف اختبار **معاكس** يمنع ابتلاع 2019 |
| «مبلغ غير موجود يُؤكَّد» | رسالة الرفض **تذكر المبلغ** الذي رفضته، فبدا كأنه اختراع | الحكم صار حتمياً (`scope != in_scope`) لا تعبيراً نمطياً على نص |
| «النقص في صفحات بلا إطار» | الربط كان خاطئاً **عندي**: النطاق هو كل ما بعد الإطار الأول حتى الثاني | أُصلح · و`harness` بنيتُه لمسار التطبيق أنتج 200 عدم-تطابق كاذب لأنه **أعاد تنفيذ** حساب الأوراكل ⇒ **حذفته ولم أصدّقه** |

العيب الحقيقي الوحيد من الثلاثة: **عدم حتمية الرفض** (رفض مرة وأجاب مرة) — وهي التي أغلقتها البوابة.

# سادساً — أسئلة المراجعة (اضرب هنا، لا في مواضع الثقة)

1. **`ungrounded`**: وسم الجواب الرقمي بلا أداة وتفريغ لوحة الأدلة — أم تفضّل **رفضه**؟ أيهما أصحّ لمن سيقدّم التقرير لمحكمة؟
2. **هوية النطاق**: هل الإطار المرجعي يشترط حكم `ok` فقط؟ (ارفضتُ `mismatch` كمرجع احترازاً) — وهل نطاق يشمل صفحة بلا صفوف له معنى أصلاً؟
3. **البوابة**: أين يمكنها أن **تبتلع سؤالاً حقيقياً**؟ أعطني جملة عربية واحدة تسقطها اختباراتُها — فهذا أنفع من ثناء.
4. **`test_public_claims.py`**: أيفرض صدق الوثائق أم يولّد churn (كل اختبار جديد يُلزم تحديث رقم في `docs/`)؟ هل نُبقيه؟
5. **التاريخ**: بعد ثلاث تمريرات `filter-repo`، هل تجد أثراً باقياً لرقم الحساب أو مسار منزل (reflog · notes · packed-refs · الـworktree)؟
6. **الصياغة**: الفجوتان و629 — «غير محسومة» أدق أم «معلومة النقص»؟ وماذا يستحق التقرير القضائي؟
7. **التراجع**: أي بند في هذه الدفعة يستحق **الحذف** لأنه زاد التعقيد بلا عائد مقيس؟ (أُفضّل أن تقول «احذف X» من أن تُجمّل.)

# سابعاً — ملاحظة على الـworktree

`.claude/worktrees/rajhi-rag-deep-audit-91bd79/` ما زال قائماً: نسخة عمل ثانية بنفس `.git`، وفيها
**نسخة ثانية من كل ما كان محلياً وقت إنشائه** (بحث المالك سأل عنه). إن انتهى تدقيقك، الأنظف
`git worktree remove` ثم `prune` — لا `rm -rf` (تُبقي تسجيلاً معلّقاً). وقبل الحذف: أبقِ تقاريرك
**داخل المستودع** (`handoff/claude/`) لأنها الآن فيه `.gitkeep` فقط.

---

**الخلاصة للمدقّق:** 169 اختباراً · 5/5 بوابة · 0 BLOCK · CI أخضر على `6f56405` · وكل صفحات الكشف
الـ629 صارت محسوبة: **621 بإطارها + 5 بالمجموع + 2 فجوة مسح بنقص مقيس + 1 غير محسومة**.
لا أطلب ثناءً — أطلب **هجوماً** على المواضع الستّة أعلاه.
