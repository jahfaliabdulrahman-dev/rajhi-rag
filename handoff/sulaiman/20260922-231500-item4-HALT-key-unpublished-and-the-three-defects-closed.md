# تسليمٌ إلى كلود — إغلاقُ «المفتاحُ في القفل» وثلاثةِ عطوبِ المراجعة ٣٧ (بنيويّاً لا ترقيعاً)

```
id:      20260922-231500-sulaiman
from:    sulaiman
to:      claude
type:    SUBMIT
step:    إخراجُ المفتاح من التتبّع وتدويرُه · إلغاءُ نشر البصمات · قتلُ الإعفاء بالمسار ·
         إزالةُ التصنيف بالاستدارة · مخرَجٌ آليٌّ لا يُقصّ · حارسُ الإهمال (بلا سرّ، في CI)
commit:  8bec6c8  ·  PR #100 (مدموج --rebase)  ·  CI guard أخضر  ·  393 اختباراً (1 xfail مُعلن)  ·  $0
verdict: -
evidence:
  - `git ls-files | grep -cE 'amount-guard-key|amount-manifest|amount-denylist'` → 0
  - `.venv/bin/python tools/amount_guard.py --tracking-audit` → PASS (rc=0)
  - `.venv/bin/python tools/amount_guard.py --inject` → كلُّ الأصناف سقطت على السطحين (rc=0)
  - `.venv/bin/python tools/amount_guard.py` → BLOCK · المجموع: 40 (بإعلان — البندُ المفتوح)
  - `.venv/bin/python -m pytest tests/ -q` → 393 passed · 1 xfailed
  - `.venv/bin/python tools/static_gate.py` → PASS · `render_claims.py --check` → مطابق
  - `.venv/bin/python tools/publish_guard.py` → آمن للدفع · `guard_audit.py` → PASS 5/5
next:    مراجعتُك على هذا الالتزام؛ ثم البندُ المفتوح (٤٠ قيمةً) أو قرارُ المالك (filter-repo)
```

---

## ٠) الحكمُ في سطر

المفتاحُ كان **في القفل** (مُلتزَماً في مستودعٍ عامّ) ⇒ منظومةُ البصمات **لم تكن تحمي شيئاً**.
والعلاجُ ليس ترقيعاً: **لا يُنشر ما يمكن مهاجمتُه** (المانيفستُ محليٌّ داخل `data/`)، ومعه **حارسٌ
ميكانيكيّ** يُثبت الإهمالَ **واقعاً**، و**الإعفاءُ بالمسار أُزيل** (قاعدتُك ١٢: لا يُعفى موضع).

## ١) ⛔ HALT — المفتاحُ في القفل: القياسُ والعلاج

**قياسي المستقلّ (بلا طباعة أيّ سرّ):**

| الفحص | النتيجة |
|---|---|
| `git ls-files -- data/.amount-guard-key` | **نعم ⛔ مُتتبَّع** |
| `git check-ignore -v data/.amount-guard-key` | **rc=1 ⇒ غيرُ مُهمَل ⛔** |
| `git log --all --oneline -- data/.amount-guard-key` | **اشتراكان**: `6689ff2` · `20dfa8b` |
| قيمةُ المفتاح في ملفٍّ متتبَّع | **نعم ⛔** |
| سببُ الوهم | `.gitignore` كان يُهمل `data/eval_pack/` **فقط**، لا `data/` ⇒ **الإهمالُ اعتقادٌ لا واقع** |

**والتدويرُ والعلاجُ بالترتيب الذي حدّدتَه:**

1. المفتاحُ الحاليُّ **محروقٌ نهائياً** — لا يُعاد استعمالُه ولو نُقل ⇒ **دُوِّر** (المفتاحُ على
   القرص 👁 مفتاحٌ جديد، والسرُّ في GitHub مُحدَّث).
2. **أُخرج من التتبّع** (`git rm --cached`) + `.gitignore` + **وتحقّقٌ ميكانيكيّ** (`check-ignore`).
3. **أُلغيت النشرة:** المانيفستُ انتقل إلى `data/eval_pack/amount-manifest.json` (مُهمَل، غيرُ
   منشور)، و`docs/security/amount-denylist.json` **حُذف من الشجرة**.
4. **والثالثُ يبقى قرارَ المالك**: المفتاحُ المحروقُ والمانيفستُ المنشورُ **باقيان في التاريخ**
   ⇒ `filter-repo` لم يعد اختيارياً، **وصار يشمل المفتاحَ والمبالغَ معاً** — لم أمسّ التاريخ.

**والحارسُ الجديدُ الذي كان غائباً** (`--tracking-audit`، بلا سرّ ⇒ يعمل في CI):

```
١) غيرُ متتبَّع في git         ٢) `git check-ignore` يمرّ
٣) قيمةُ المفتاح لا تظهر في أيّ ملفٍّ متتبَّع (البحث داخل العملية ⇒ لا سطر أوامر ⇒ لا تسرّب للسجلّات)
٤) ولا ≥٢٠ بصمةً منشورة في ملفٍّ متتبَّع
```

**نتيجتُه الآن على `8bec6c8`: `PASS` (rc=0)** — وسقطت في اختباره مرّتان قبل أن يمرّ (أولاً لأنّ
المانيفستَ كان منشوراً، وثانياً لأنّه كان مُخطَّأً في `.gitignore`).

## ٢) الأربعةُ — ولماذا كلُّ واحدٍ عطبُ صنفٍ لا حادثة

| # | العطبُ (مُثبتٌ منك) | الجذر | ما نُفِّذ |
|---|---|---|---|
| ١ | **`FIXTURE_PREFIXES = ("tests/","src/")`** ⇒ حقنتَ مبلغين حقيقيين في `tests/` ⇒ `PASS · exit 0` | **إعلانٌ ضمنيّ عرضُه مجلدان** — وهو النقيضُ لقاعدتنا التي ألغت قائمةَ «الصناعيّ المُعلَن» لأنّها تُخرج مبالغَ حقيقيّة | **قُتلع بالكامل**: لا `FIXTURE_*` في الأداة · **والحقنُ صار على السطحين** (`docs/` **و** `tests/`) |
| ٢ | **التصنيفُ بالاستدارة** في `:389` ⇒ مجاميعُ ص٦٢٩ «غيرُ مميّزة» | إعادةُ الشرط المحذوف في ٣٦ تحت اسمٍ آخر | أُزيل — **لا تصنيفَ يُعيد الشرطَ المحذوف** |
| ٣ | **`fx[:20]` بينما `fixture_count=40`** | المخرَجُ الآليُّ صار سطحَ العرض | `--json` يُعطي المجموعةَ **كاملةً** دائمًا (اختبارٌ يقارن `count` بطول `hits`) |
| ٤ | البصماتُ المنشورة + المفتاحُ المنشور | **فضاءُ المبلغ ~١٠⁸ ⇒ «مفتاحٌ سرّيّ» لا يحمي فضاءً ضعيفَ العشوائيّة** | أُلغيت النشرة · المانيفستُ محليّ فقط · + حارسُ الإهمال |

## ٣) ✅ ما صحّ — وتصحيحُ قياسي أنا

| الادعاء | قياسي المستقلّ على `8bec6c8` |
|---|---|
| **`slice_150p` = 100٪** (كنتَ محقّاً وأنا مخطئ) | **100.0٪**: ١٬٠٣٢ قيمةً مميّزة من **١٥١ ملفاً**، تقاطعُها مع بقيّة الشرائح **١٬٠٣٢/١٬٠٣٢** — وادّعائي السابق (٩٩.٣٪) كان **خطأً منهجيّاً في المقام**، وأُصلح في التقرير وفي §٧.٢ |
| **إلغاءُ قائمة «الصناعيّ المُعلَن» صحيح** | مُثبت: قيمُ المولّد المستديرةٌ موجودةٌ فعلًا في حركاتٍ حقيقيّة ⇒ الإعلانُ كان يُخرجها من الحماية |
| `--inject` طبقيّ يسقط الأصنافَ الأربعة | `rc=0`: **مستدير · كسريّ · سالب · طويل** × **دليل · صناعة** |
| الاشتقاقُ يُغلق | ١١٬٠٢٨ = **٤٬٢٧٢** داخل المدى + ٦٬٧٥٦ تافهة · **١٬٩٢٦ أثراً** · تغطية ✓ |

## ٤) أربعةُ عطوبٍ من صنعي — أمسكها البناءُ والحرّاسُ لا المراجعة

1. **تطبيعٌ مرجعيٌّ أفرغ شرطَ الكسر صامتاً:** أدخلتُ `canonical` داخل `normalize` ⇒ `9001.00` ⇒
   `1500` ⇒ **انتفى شرطُ الكسر** ⇒ عادت المبالغُ المستديرةُ خارج الحماية من بابٍ آخر (نفسُ عطب ٣٦).
   **العلاجُ الفصل:** `normalize` نصّيّ (وجودُ الكسر شرطُ المدى) · `canonical` للبصمة (تُلغي فروقَ
   الكتابة: نفسُ القيمة بكل صيغها) ⇒ واختبارٌ يمنع الخلط.
2. **مسبارُ البرهان كان مُهمَلًا في `.gitignore`** ⇒ **مسبارٌ لا يراه الحارسُ** ⇒ برهانٌ كان
   يسقط **زوراً**. أُزيل الإهمالُ عن المسبار.
3. **نمطُ التطهير `-?` التهم الشرطةَ الفاصلة** في المعرّفات (تاريخ-وقت) ⇒ التصقت الأجزاءُ في
   سلسلةٍ طويلة **أوقفها `publish_guard`** ⇒ أُصلح النمطُ `(?<![\d\w])-?` ورُمّمت الملفّات.
   **حارسان مستقلّان يمسك أحدُهما الآخر — بنيةٌ صحيحة.**
4. **مثالٌ في الوثيقة أعاد إنتاجَ العطب** (كتبتُ السلسلةَ الطويلة كمثالٍ على العطب) ⇒ أوقفه
   `publish_guard` مرّةً أخرى ⇒ عُدِّل المثال. **ومثله:** قياسٌ خلط الشرطين (`is_amount_shaped`
   بلا شرط الكسر + بصمةٌ مرجعيّة) أنتج **٣٧٤ إصابةً كاذبة** في التوثيق ⇒ **المدى شرطُ المطابقة**.

## ٥) القواعدُ المُقيَّدة (docs/handoff-protocol.md §٦)

- **١٢ — «لا يُعفى موضع، يُعفى قيمة»**: ولا يُصنَّف بالشكل (الاستدارةُ ليست دليلاً).
- **١٣ — المخرَجُ الآليُّ لا يُقصّ أبداً** (والقصُّ للعين وحدها، بإعلان).
- **١٤ — «سرٌّ» صفةُ موضعٍ لا صفةُ آلية** · والإهمالُ **واقعٌ يُقاس بـ`git`** لا اعتقادٌ يُكتب.

## ٦) البواباتُ على `8bec6c8` (أوامرُ حقيقية)

| البوابة | الأمر | النتيجة |
|---|---|---|
| الاختبارات | `.venv/bin/python -m pytest tests/ -q` | **393 passed · 1 xfailed** |
| حارسُ المبالغ | `python3 tools/amount_guard.py` | **BLOCK · المجموع 40** (بإعلان — §٧) |
| **حارسُ الإهمال** | `… --tracking-audit` | **PASS** (rc=0) |
| البرهانُ الطبقيّ | `… --inject` | **سقطت كلُّ الأصناف على السطحين** (rc=0) |
| الجودة الساكنة | `python3 tools/static_gate.py` | PASS |
| الأرقامُ المعلنة | `render_claims.py --check` | مطابق |
| حارسُ النشر | `publish_guard.py` | آمن للدفع |
| حرّاسٌ مُجرَّبةُ السقوط | `guard_audit.py` | PASS 5/5 |
| CI | PR #100 · `guard` | **pass** (وفيه خطوةُ `--tracking-audit`) |

**ولم أُشغِّل البوابةَ (٢) `qa_gate --full` ولا (٤) `preflight`** — وهذا التسليمُ **لا يلمس مسار
القراءة ولا العرض** (أداةُ أمنٍ وتوثيق)، وعقدُنا §٤ يمنع إحراقَ الرصيد بلا سبب.

## ٧) البندُ المفتوحُ المُعلن (وأطلبُ حكمَك فيه)

**٤٠ قيمةً** في أسطح الصناعة (`tests/` · `src/`) تتصادم مع مبالغَ حقيقيّة ⇒ الفحصُ **يُسقط
الشجرةَ** (rc=1). **وهذا هو السلوكُ الصحيح** بموجب قاعدتك ١٢: تُغيَّر **القيمةُ** ولا يُعفى الموضع.
- الإصلاح: **إعادةُ كتابة تلك القيم بقيمٍ آمنة + توقّعاتٍ تُعاد اشتقاقُها** (لا تُنقل من مخرَج
  الكود ⇒ وإلّا صار الاختبارُ تحصيلاً).
- التقدّمُ مُعلنٌ ومقيس: اختبارٌ `xfail` يعدّ الظهورات وينقلب إلى `strict` لحظةَ إتمامها، و`--json`
  يُعطي **القائمةَ الكاملة** (المجموعةُ كلُّها — بلا قصّ).
- **سؤالٌ صريح:** هل تُريدها في التسليم التالي كبندٍ واحد، أم تُفضّل أن أُقدّمها ملفّاً ملفّاً
  مع توقّعاتها لتُدقّق كلَّ تحويلٍ على حِدة؟

## ٨) قراراتُ المالك (لا أتجاوزها)

1. **`filter-repo`** — صار يشمل **المفتاحَ والمبالغَ**؛ ومن استنسخ منذ `6689ff2` **افترض الأسوأ**.
2. **١٣٣ صفحة** (32.6٪) أو سقفٌ أقلّ من `--price-surface`.
3. **طبقةُ السؤال** (~$0.02–0.05) · **معيارُ اللجنة**.
4. **الباقي ≈$4.32** من $6.52 (هذه الجولة ≈$0).

## ٩) ملفّاتُ التسليم (`git diff --name-only 9d52b11..8bec6c8`)

```
.github/workflows/publish-guard.yml      ← خطوةُ --tracking-audit (بلا سرّ)
.gitignore                               ← إهمالُ المفتاح والمانيفست والخريطة + إزالةُ إهمال المسبار
data/.amount-guard-key                   ← أُخرج من التتبّع (والملفُّ محليٌّ بمفتاحٍ جديد)
docs/security/amount-denylist.json       ← حُذف من الشجرة (لم يُعد يُنشر)
docs/handoff-protocol.md                 ← القواعد ١٢–١٤
docs/QA_CHECKLIST.md · docs/claims.json  ← العدّادُ 393 ولقطتُه
tests/test_amount_guard.py               ← أحدَ عشرَ اختباراً: المدى · الفصل · القناةُ المغلقة · الإهمال · بلا قصّ
tools/amount_guard.py                    ← الإعفاءُ مُقتلَع · السطحان في الحقن · canonical/normalize مفصولان
handoff/sulaiman/…ONE-DELIVERY….md       ← §٧.٢ و§٧.٣ (تسجيلُ الجولتين + تصحيحُ ٩٩.٣٪)
```

---

## ما لم يُثبت

1. **البندُ المفتوح (§٧) لم يُنفَّذ**: ٤٠ قيمةً في أسطح الصناعة ما زالت تتصادم ⇒ الفحصُ أحمرُ **بإعلان**.
2. **التاريخُ يحمل المفتاحَ المحروقَ والمانيفستَ المنشور** ⇒ المبالغُ قابلةٌ للاسترجاع عند من استنسخ.
   **وأمانُ المفتاح المحروق لا يُستعاد** — لا بالتدوير ولا بالإخراج.
3. **الأسماءُ والتواريخُ وأرقامُ الحسابات لم تُسح** (المبالغُ فقط).
4. **`.parquet` والصورُ و PDF لم تُمشَ** (المصدرُ `JSON/JSONL`).
5. **`guard_audit` لا يضمّ `amount_guard`** بعد (سقوطُه مُثبتٌ باختبارٍ وبخروجٍ غيرِ صفريّ، لا ببوّابة التسع).
6. **بوابةُ المحتوى** (مفتاحا الزمن/المحتوى) حالتُها `مُهيّأة` لا `مُختبَرة`.
7. **CI لا يستطيع إنفاذَ قاعدة المبالغ بنيوياً** (أيُّ قائمةٍ منشورةٍ تُجرَد) ⇒ الإنفاذُ محليٌّ +
   `.githooks/pre-push`، وCI يفحص حارسَ الإهمال بلا سرّ — **مُعلنٌ في الكود لا مخفيّ**.
