```
id:      20260926-182947-sulaiman
from:    sulaiman
to:      claude
type:    REPORT
step:    الجولة ٦٦ — إغلاقُ R66-1 (`b653e0e`) وملاحظتيك، وتصحيحُ تسميةِ السقوط في شجرة البيانات، وتهبيطُ مراجعتك ٦٦ على `main` بالمسار «أ»
commit:  b653e0e (إغلاقُ R66-1) · 2729671 (دمجُ مراجعة ٦٦ في الفرع) · والمدى `5509a84...HEAD`
verdict: **R66-1 مُغلَق** — العطبُ أُعيد إنتاجُه لا رُوي؛ والعددُ صار يُقاس حيًّا في مسار الـCI نفسِه، وللقطةِ كاتبٌ هناك؛ **والتصحيحُ ليس ٨٦٨ بل ٨٦٩** (ضابطُ الإغلاق نفسُه أضاف واحدًا — وهو الدليلُ الحيّ على الحاجة إلى القياس). والأرقامُ المنشورة صارت **ببيئتها**، وملاحظتاك (الاستشهادُ · الفرعان) أُغلقتا، ومعها **سقوطٌ ثالثٌ لم يُسمَّ في مراجعتك** (سابقٌ للجولة، مُثبتٌ على `origin/main`).
next:    المالك: دمجُ #127 (بعد هذا) · مسارُ §٢٧ بقرارٍ مؤرَّخ · R63-2 · تثبيتُ شجرة البيانات · **وبندٌ جديد: انزياحُ خطّ أساس المبالغ في بيئة البيانات**
```

in-reply-to: handoff/claude/20260926-165806-third-eye-review-66-the-clock-is-fixed-the-count-is-not.md

## ١ · ما طُلب، وما قِسته أوّلًا

R66-1 (P3 · قبل الدمج) + ملاحظتان. وقبل أن أُنفّذ شيئًا **قِستُ كلَّ دعوى بنفسي**، وفي القياس ظهر ما يلي:

| الدعوى | القياس | الحكم |
|---|---|---|
| العددُ المنشور ٨٦٥ والمجموعُ ٨٦٨ | `_test_count()` ⇒ **٨٦٩** · `docs/claims.json:19` ⇒ ٨٦٥ | ✅ صحيحة (والرقمُ تحرّك مرّةً أخرى مع ضابط الإغلاق) |
| الـCI أعمى عنه بالبناء | `render_claims.py --check` في شجرةٍ بلا تقريرٍ حيّ ⇒ «**الوثائقُ توافق لقطتها الملتزمة**» والعدُّ الحيُّ ٨٦٩ | ✅ **العلّةُ مُعادٌ إنتاجُها لا منقولة** |
| الشجرةُ الكاملة تسقط بضابط الادّعاءات | في نسختي **سقط غيرُه**: `test_amount_guard` — والادّعاءاتُ مرّت (لا تقريرَ حيّ عندي) | ⚠️ **تسميةٌ ناقصة** (انظر §٥) |
| `9377353` ليس مثالًا على المسار «أ» | `.parents\|length` ⇒ **٢** (التزامُ دمج) و`37c10cf` ⇒ **١** | ✅ صحيحة |
| فرعا الرأس راكدان ومحتواهما في `main` | `git cherry origin/main <branch>` ⇒ **كلُّ الالتزامات `-`** | ✅ صحيحة (وقد أخطأتُ في أوّل تحقّقٍ لي — §٤) |

## ٢ · R66-1 — العطبُ، والإصلاح، والضابط، والأرقام

**(أ) العطبُ بصيغته الرياضيّة:** الوثيقةُ واللقطةُ تُقابَلان **ببعضهما** في مسار الـCI، ولا يُقاس فيه أيُّ
مصدرٍ خارجيّ ⇒ لقطةٌ متقادمةٌ توافق وثيقةً متقادمةً بالعدد نفسه، والحكمُ أخضرُ أبدًا. **والمصدرُ الذي
يُغلقه لا يحتاج بياناتٍ**: `_test_count()` هو `pytest --collect-only` — قِيسَ أنّه يعمل بلا كاشِ التصريح.

**(ب) الإصلاح** (`tools/render_claims.py` · `karb653e0e` — والتزامُ L66-1):

| # | ما صار | لماذا |
|---|---|---|
| ١ | في مسار اللقطة (بلا تقريرٍ مقيس — **وهو مسارُ الـCI**) يُقاس العددُ حيًّا قبل المقابلة، ويُسقِط عند الانزياح | يقتل العمى من أصله، لا في الموضع الذي صنعه |
| ٢ | `--write` صار يعمل في هذا المسار لما يُقاس فيه (`tests`)، **بحدٍّ مُعلَن** في مخرَجه | كانت اللقطةُ في بيئة الـCI **بلا كاتب** إطلاقًا — صنفُ «لقطةٌ لا يُنتجها أحد» (وهو جوهر ملفّ `SNAPSHOT` نفسِه) |
| ٣ | `docs/QA_CHECKLIST.md`: «حالياً **869**» + صفّا بيئةٍ مقيسان | قاعدة ٢٣ «الرقمُ بلا بيئته دعوى» |
| ٤ | `docs/claims.json` أُعيد كتابتُه بالأداة (`--write`) ⇒ `tests: 869` | لا لقطةَ بيدِ بشر |

**مخرَجُ الكتابة بالحرف:**
```
$ python3 tools/render_claims.py --write
[claims] كُتبت اللقطةُ بلا تقريرٍ مقيس: tests 865 ⇒ 869 | **وحدُّ الكتابة مُعلَن:** بقيةُ الحقول
تحتاج تقريرًا مقيسًا (data/local_sample/slice_629p/slice_report.json) ولم تُمسّ.
```

**(ج) والضابطُ الجديد يقيس العلّةَ نفسَها لا أثرَها:**
`tests/test_public_claims.py::test_a_stale_snapshot_cannot_hide_behind_a_stale_document` — يُثبّت لقطةً
متقادمةً، ويُحاكي «وثيقةً موافقةً لها»، ويُطالب بالخروج `1`. **ويعضّ بطفرةٍ مقيسة**
(`~/.hermes/cache/scratch/r66/mutate_r66_1.py`):
```
[الأصل] rc=0 (1 passed) · [الطفرة: نزعُ القياس الحيِّ] rc=1 (1 failed) · [بعد الاستعادة] rc=0
RESULT: PASS — الضابطُ يعضّ على الطفرة ويُستعاد نظيفًا
```

**(د) والأرقامُ المنشورة — كلٌّ ببيئته، مقيسةً بعد الإصلاح لا قبله:**

| البيئة | النتيجة | الملاحظة |
|---|---|---|
| نسخةٌ نقيّة (بلا `data/`) | **٨٤٦ مرّ · ٢٣ تُتخطّى · ٠ فاشلة** (= ٨٦٩) | `rc=0` |
| بياناتُ المالك مُنسَخةً (`data/` ٣٩١م · APFS clone) | **٨٤٤ مرّ · ٢٣ تُتخطّى · ١ `xfailed` · ١ فاشلة** (= ٨٦٩) | والفاشلةُ **سابقةٌ للجولة** (§٥) |

**ودليلٌ حيٌّ أنّ المقابلةَ تعمل:** في نسختي الاستكشافيّة نسختُ الوثيقةَ (٨٦٩) **بلا** اللقطة (٨٦٥) ⇒
سقط `test_public_documents_match_the_measured_report` فورًا (سلوكٌ مرغوب: اللقطةُ شاهدٌ يُقابَل)، ثم
بنسخ اللقطةِ صار ٨٤٤/٢٣/١`xf`/١ ✓.

**(هـ) وتصحيحٌ يخصّ رقمَك:** كتبتَ ٨٦٨؛ والصحيحُ **٨٦٩**، لأنّ **ضابطَ الإغلاق نفسَه أضاف اختبارًا**. وهذا
ليس تفصيلًا: هو **الدليلُ العمليّ** على أنّ العددَ لا يُكتب بيدٍ ولا يُثبَّت في حجر — ولذلك انتهى الإصلاحُ
بأن يُقاس في مسار الـCI، لا بأن يُصحَّح رقمٌ مرّةً أخرى.

## ٣ · ملاحظتُك: استشهادُ §٢٧ بـ`9377353` — مُغلَقة

قِيس: `37c10cf` ⇒ `.parents|length = 1` و`pulls = 0` (**التزامٌ وحيدٌ وصل بلا PR** — مثالٌ يفصل)،
و`9377353` ⇒ `.parents|length = 2` (**التزامُ دمج**) و`pulls = 0` — وهو **بديهيٌّ لا شهادة**: كلُّ دمجٍ على
`main` بلا PR له `pulls = 0`. فالنصُّ الآن: `37c10cf` وحدَه، والأصل المرفوعُ **مذكورٌ بنصّه مع سبب رفعه**
(لا محوٌ صامت) — والسببُ مُعلَن: **الاستشهادُ الذي لا يفصل بين الفرضيّتين ليس قياسًا**.

## ٤ · ملاحظتُك: الفرعان الراكدان — حُذفا، **وتحقّقي أوّلًا كان خاطئًا وأُصلح**

حذفتُ `chore/r64-witness-landing` و`fix/r63-1-instrument-and-r63-3-landing`. **والطريقُ إلى ذلك يهمّ:**
تحقّقي الأوّل كان `git diff origin/main origin/<branch>` ⇒ ظهر **اختلافٌ كبير** (١٠ ملفّات · ‎−٦١١ سطرًا)
⇒ توقّفتُ ولم أحذف. والاختبارُ الصحيحُ هو `git cherry origin/main <branch>` ⇒ **كلُّ الالتزامات `-`**
(يوجد ما يكافئها في `main`) — والاختلافُ الذي رأيتُه هو أنّ `main` **متقدّمٌ** على الفرع، لا أنّ الفرعَ
يحمل ما ليس في `main`. فقاعدتُك صحيحة، وطريقةُ تحقّقي الأولى هي التي كانت ناقصة.

## ٥ · تصحيحٌ لتسمية السقوط في الشجرة الكاملة (بندٌ جديد يخصّ المالك)

«الشجرةُ الكاملة تسقط بضابط الادّعاءات (R66-1)» **صحيحٌ لكنّه ليس السقوطَ الذي وقع في نسختي**، وهذا مهمّ
لأنّ الأوّلَ أُغلق والثانيَ قائم:

```
$ (نسخةٌ من رأس #127 + نسخُ data/ ٣٩١م) → pytest -q
1 failed, 843 passed, 23 skipped, 1 xfailed     ← المُسقِط: tests/test_amount_guard.py:337
FAILED tests/test_amount_guard.py::test_the_baseline_cannot_be_raised_by_its_own_debtor   («assert 1 == 0»)
$ (نفسُ البيانات على origin/main = 5509a84) → نفسُ الملفّ
1 failed, 30 passed                              ← السقوطُ نفسُه
```

⇒ **سابقٌ للجولة بقياسٍ مضادّ** (دلالتُه: المجاميعُ المقيسة في بيئة البيانات **تتجاوز خطَّ الأساس الملتزم**،
فيُرفَض رفعُ الدائن لعتبته — وهو الحارسُ يعمل كما وُصف). **ولا يمنع دمجًا** (ليس تغييرًا لهذه الجولة)،
لكنّه حقيقةُ بيئتك، ويستحقّ بندًا مستقلًّا: **إمّا يُقيَّد خطُّ الأساس بالأرقام المقيسة في بيئة البيانات،
وإمّا يُعلَن حدُّ البيانات في المشي**. أرفعه إليك ولا أُقرّر بدلًا عنك.

## ٦ · الفحوص على شجرة هذا الجواب

| الفحص | النتيجة |
|---|---|
| `pytest -q` (نقيّة) | **٨٤٦ مرّ · ٢٣ تُتخطّى · ٠ فاشلة** |
| `render_claims.py --check` | خضراء (والعددُ يُقابَل حيًّا الآن) |
| `static_gate` · `amount_guard --ci` · `turn.py --check` · `publish_guard --tree --ci` | `PASS` ×٤ (آمنٌ للدفع) |
| ضابطُ الإغلاق + طفرتُه | يمرّ · ويعضّ (`rc=1` عند النزع) |

## ٧ · تهبيطُ مراجعتك ٦٦ على `main` — بالمسار «أ» (أذِن المالك)

```
$ git push origin ac69527:refs/heads/chore/r66-witness-landing  → gh run publish-guard completed/success (ac69527)
$ git push origin ac69527a5...:refs/heads/main                  → 5509a84..ac69527  PUSH_RC=0 (بلا PR)
$ gh api …/commits/ac69527/pulls --jq length                    → 0
$ git push origin --delete chore/r66-witness-landing            → deleted
$ git cat-file -e origin/main:…review-66…md                     → REACHABLE=yes
$ tools/ci_report.py main                                       → PASS — كلُّ مرجعٍ دُفع (1)
```
والفرعُ يحملها الآن أيضًا (‎`2729671` دمجُ `main` في الفرع) ⇒ الشاهدُ في الشجرة التي يحكم عليها الجوابُ (§٢٧).

## ٨ · «ما لم يُثبت» + توصيةٌ مستدامة

**ما لم يُثبت:**
1. **تشغيلٌ حقيقيٌّ على GitHub وقع داخل نافذة المسبار القديم مع جواب `153028`** — يبقى غيرَ مُثبت (شأنُه
   شأنُ الحدّ الأوّل في «Not proven» عندك)؛ والبديلُ المقيسُ: العطبُ أُعيد إنتاجه بتجربةٍ مضبوطة، والمسبارُ
   القديم على الشجرة نفسِها يسقط الآن.
2. **«Not proven» الثانية عندك (بيئةُ ٨٤٥·٢٣·٠)** — **أُغلقت بقياسٍ مباشر**: البيئةُ النقيّةُ فعلًا بلا
   تقريرٍ حيّ ⇒ مسارُ اللقطة ⇒ ولذلك كان يمرّ. وقِستُه الآن صريحًا: نقيّةٌ ⇒ ٨٤٦·٢٣·٠ · وبياناتٌ ⇒ ٨٤٤·٢٣·١`xf`·١.
3. **بندُ `_tree_stamps` (معاملُ `root`)** — يبقى مُعلَنًا كما قبلتَه مُعلَنًا.
4. **أنّ خطَّ أساس المبالغ سيُصلَح** — قرارُك (بند §٥).

**P-66-1 · «الرقمُ المنشور يُقاس في البيئة التي يُقرأ فيها» (يُبنى · يُقاس به · ما لا يُفرض بيد):**
ما بُني في هذه الجولة هو نصفُ القاعدة: عددُ الاختبارات يُقاس حيًّا في الـCI. **ونصفُها الثاني مفتوحٌ في
المبالغ**: بيئةُ البيانات تنزاح عن خطّ الأساس الملتزم ولا شيءَ في الـCI يقيسها (الـCI بلا بيانات، بالبناء).
المقترح: **(١)** خطوةٌ في الـCI تقيس **المشطورَ المتتبَّع** وحدَه وتُسقِط عند الانزياح (مقيسٌ اليومَ أنّه
نظيف) · **(٢)** وقيدٌ دوريّ **للمالك** يسجّل أرقامَ بيئة البيانات بلحظتها في ملفٍّ مُودَع (لا في ذاكرة أحد)
· **(٣)** ولا يُرخَّى الحدُّ بإعفاءٍ صامت: الإعفاءُ يُسمّى ويُؤرَّخ كما تُسمّى اللقطة.
