# مقاعدُ الجولة ٦٥ — نصُّها بالحرف (نقلُ مُدخَل، لا جوابٌ ولا شاهد)

**الدفعة:** `deleg_56d3b311` — **٣ مقاعد معزولة** (مقعدُ leaf · نموذج `deepseek-flash` · مدّةٌ إجماليّة 459.7ث)
أُرسِلت 2026-09-26 15:16:01. **المدى المُثبَّت الذي راجعته:** `5509a84...b02966d` (نقطةُ التثبيت `5509a84` =
`origin/main` يومَ الجولة) وكلُّ مقعدٍ أعلن `HEAD` **في السطر الأوّل** قبل أيّ قياس ⇒
`HEAD=b02966ddb5082c0fcdccfb4d826bea230e686bb8` في الثلاثة.

**وحدُّ المقاعد مُعلَن:** مراجعةٌ **للقراءة فقط** (لا تحريرَ شجرة/فهرس/`HEAD`/فرع، ولا `checkout`/`stash`/`commit`)،
وكلُّ قياسٍ وتشغيلٍ في **نسخٍ خارجَ المستودع** (`~/.hermes/cache/scratch/r65/seats/`: `tree` · `prefix` ·
`pr127-prefix` · ونسخُ الطفرات `mut-wire` · `mut-floor` · `mut-tz`) — فيبقى المدى ثابتًا أثناء القراءة.

**الأحكامُ الثلاثة:** `APPROVE WITH FIXES` × ٣، ولا واحدٌ منها يحجب الهبوط. والبنودُ المُجمَّعة:
**P2 × 6 · P3 × 4**، منها **ثلاثةٌ تقاطع فيها مقعدان** (الادّعاءُ الباطل في `§٢٧:449` · ترتيبُ «مُعلَنٌ على
`main`» في `§٢٧:445` · نسخةُ الزمن الثانية `_stamp_dt`) — والتقاطعُ أقوى إشارةٍ على العطب.

**وتحريرٌ واحدٌ مُعلَنٌ في هذا النقل:** مسارُ بيت المالك في نصّ مقعد البنية (`/Users/<owner>/Projects/…`)
**حُجِب** إلى `~/Projects/…` لأنّ `publish_guard` يمنع نشر مسارِ بيتٍ في الشجرة (`home_path` · قِيس: `BLOCK`
قبل الحجب) — وما سوى هذا الحجب **بالحرف**؛ فالنقلُ أمانةٌ والبوّابةُ حدٌّ لا يُرخَّى بالنيّة.

**والحركةُ المُسمّاةُ التي اتّفق عليها مقعدا المعايير والبنية واحدة:**
**«زمنُ الاسم ونطاقُه لهما مالكٌ واحد (`tools/turn.py`)»** — وهي ما نُفِّذ في الإغلاق.

---

## مقعدُ المعايير (Standards)

# مقعدُ المعايير — الجولة ٦٥ (المدى `5509a84...b02966d`)

HEAD=b02966ddb5082c0fcdccfb4d826bea230e686bb8

### Strengths
- **العطبُ أُعيد إنتاجُه لا رُوي:** على الشجرة الحقيقيّة، شاهدٌ اصطناعيٌّ بختم `20260926-034032` يُسقط جوابًا واحدًا فقط — `handoff/sulaiman/20260926-0455-REPORT-to-claude-round64-…md` برسالة «ليس أحدث» — وبالختم المُصلَح `20260926-064032` تسقط صفرًا؛ فنافذةُ مراجعة ٦٥ مُعاد إنتاجُها بالقياس على الشجرة نفسِها.
- **الضابطُ يعضّ على ما أُعلن أنّه يعضّ عليه:** نزعُ الأرضيّة (`_probe_stamp`) ⇒ `1 failed` ونزعُ الوصل في `measure` ⇒ `1 failed` — تحت `TZ=UTC` و`TZ=Asia/Riyadh` — ويقيس بآلة الحارس نفسِها (`ungated_answers`) لا بنسخةٍ ثانية من المنطق.
- **الطرفُ الحيّ أخضرُ بالمنفّذ:** `TZ=UTC tools/landing_probe.py --ref HEAD` ⇒ `PROBE_RC=0`، والختمُ `20260926-151935` (ساعةُ الشجرة +03)، و`19 ملفّاً (rc=0)`، و`publish_guard --tree --ci (rc=0)`.

### Findings
1. **P2** `docs/handoff-protocol.md:429-435` — تسجيلُ إهباط الشاهد (دفعُ `5509a84` إلى `main`) يحمل مخرَجَ الدفع ولا يحمل مخرَجَ `tools/ci_report.py`، وهو ما تشترطه §٢٩ على **كلِّ** مرجعٍ دُفع، وهو بعينه بوّابةُ المرحلة ١ في خطة الجولة (`handoff/sulaiman/20260926-1515-PLAN-…md:28`: «`ci_report main` = PASS + مخرَجُ الدفع محفوظٌ بالحرف»). لماذا يهمّ: §٢٩ وُلدت من ملحقِ دفعٍ قال «أخضر» بلا موضعِ قياس، وهذا هو دفعُ الجولة نفسِه — والادّعاءُ الآن صحيحٌ لكن بلا موضعه. الحركة: إدراجُ مخرَج الأداة بمرساة الالتزام في تقرير الجولة (المرحلة ٧ مفتوحة) لا ادّعاءُ أخضرَ في الوثيقة. الدليل: `grep -rn "PASS — كلُّ مرجعٍ دُفع" handoff/` ⇒ مراجعُ جولاتٍ أخرى وحدها؛ وقِيس الآن `tools/ci_report.py main` ⇒ `rc=0` · «PASS — كلُّ مرجعٍ دُفع (1)». الوسم: مُثبت.
2. **P2** `docs/handoff-protocol.md:449-450` — «وثيقةُ المدقّق `handoff/claude/STATE.md:8-9` **ما زالت تُؤطّر المسارَ بالقديم**» باطلٌ على الشجرة المقيسة: السطران أُعيد كتابتُهما في `5509a84` نفسِه، والسطر ٩ يقول «مسارُ §٢٧ (الدفعُ المباشرُ بعد اخضرار الفحص، أو PR بثمن `refs/pull/*` — الخياران قابلان للتنفيذ)». لماذا يهمّ: §٢٨ «الوثيقةُ المعياريّة تُقابَل بمصدرها» — النصُّ يُرسل الطرفَين إلى تصحيحٍ تمّ، ويُبقي في البروتوكول ادّعاءً يُكذّبه مصدرُه في الالتزام نفسه. الحركة: تصحيحٌ مؤرَّخ (محوُ البند، أو إحالتُه إلى التعارض القائم فعلًا `handoff/STATE.md:105` المذكورِ في البند ٣). الدليل: `sed -n '8,9p' handoff/claude/STATE.md` و`git show 5509a84 -- handoff/claude/STATE.md`. الوسم: مُثبت.
3. **P2** `tools/landing_probe.py:230-237` — مُحلِّلٌ ثانٍ لصيغة الختم باقٍ في الملفّ بعد أن صار المصدرُ `turn.as_datetime` (`:76` · `:282`)، وهو **أضيقُ من مصدره**: `_stamp_dt("20260922-240000")` ⇒ `ValueError: hour must be in 0..23` بينما `turn.as_datetime` ⇒ `2026-09-23 00:00:00` (اصطلاحُ نهاية اليوم المُعلَن في `tools/turn.py:92-102`). لماذا يهمّ: هذا صنفُ «نسختان تفترقان» الذي أُعلن رفضُه في السطر `:73-76` من الملفّ نفسِه، وضابطُه `tests/test_landing_probe.py:111-126` («مُحلِّلٌ واحد لصيغة الختم») — فالنسخةُ الثانية تمرّ اليوم في مسار السمّ وحدَه، وتنتظر أوّلَ اسمٍ بـ`24:00`. الحركة: استخراجُ الشكل المشترك (حذفُ `_stamp_dt`، واللحظةُ من `turn.as_datetime`/`stamp_of`). الدليل: مخرَجُ الاستيراد أعلاه (`RAISES ValueError` مقابل `2026-09-23 00:00:00`). الوسم: مُثبت.
4. **P3** `docs/GATES.md:86` و`tools/landing_probe.py:26,267` — «وفوق أحدثِ ختمٍ **فيها/في الشجرة المقيسة**» أوسعُ من المصدر: `_tree_stamps` يقرأ `BOXES = (handoff/claude, handoff/sulaiman)` (`:72`) و`handoff/review/` يحمل أسماءً مختومة (`20260923-033000-…` · `20260923-042000-…`). لماذا يهمّ: §٢٨ — وثيقةٌ تصف آليةً فتُقابَل بمصدرها؛ والحدُّ المُعلَن هو أساسُ الاستدلال عند أوّل ملفٍّ مختومٍ جديد، وأصنافُ الصناديق نفسُها نسخةٌ ثانية من أصناف الحارس (`tests/test_answer_names_witness.py:54,61`) بلا ضابطِ تساوٍ («possible Duplicated Code»). الحركة: مصدرٌ واحد للأصناف (استخراجُ الشكل المشترك) ونصٌّ يقول «صندوقَي المراسلة». الوسم: مُثبت.
5. **P3** `docs/handoff-protocol.md:444-445` — «نُفِّذ في الجولة ٦٥ ما هو **مُعلَنٌ على `main`** وهو (أ)»: الترتيبُ المقيسُ معاكس — الدفعُ `12:07:13Z` ثمّ الالتزامُ `79a295e` الذي أعاد نصَّ (أ) في `15:12:55+03` = `12:12:55Z`، وكان على `main` عند الدفع نصُّ R64-3 («غيرُ قابلٍ للتنفيذ»). لماذا يهمّ: يُقرأ كأنّ المسارَ كان مُعلَنًا **قبل** الفعل، وهذا القسمُ نفسُه يُحرَس بالترتيب لا بالاستحالة. الحركة: تسميةُ ما حكم فعلًا (إجازةُ مراجعة ٦٥ لمحاولةٍ حقيقيّةٍ مُسجَّلة) وموضعِ التصحيح بعده. الدليل: `gh api repos/…/events` ⇒ `2026-09-26T12:07:13Z refs/heads/main 5509a84`؛ و`git log -1 --format=%aI 79a295e`. الوسم: مُثبت.

### Declined to judge
- تعارضُ المسارَين كقرارِ مالك (`docs/handoff-protocol.md:442-445` مقابل `handoff/STATE.md:105`) — مُعلَنٌ في الوثيقة نفسِها ومرفوعٌ إلى المالك، ولا قاعدةَ تنفيذٍ تُخالف.
- رائحةُ `possible Primitive Obsession` في تمرير مفاتيح الزمن نصوصًا (`_probe_stamp(now, stamps)`) — مستبعَدة: المفتاحُ النصّي هو الصيغةُ الموحَّدة الموثَّقة (`turn.stamp_of`)، ونوعٌ صغيرٌ يجعله مصدرًا ثانيًا.

### Assessment
Verdict: APPROVE WITH FIXES — الهندسةُ سليمةٌ والمقيسُ يعضّ في الاتّجاهين (الإصلاحُ مُعادٌ إنتاجُه والطرفُ الحيّ أخضرُ على `TZ=UTC`)، والباقي ادّعاءاتُ وثائقَ ومصدرٌ واحدٌ ناقص، لا عطبُ سلوك؛ ولا شيءَ منها يحجب الهبوط.
ما لم يُثبت: أنّ الأرضيّةَ تحمي عند ورود اسمٍ مختومٍ متأخّرٍ في صندوقٍ ثالث (استُدلّ عليها من الشيفرة وحالة ‹٣› في الضابط فقط)؛ وسلامةُ `PUSH_RC=0` بنصّه (الموثوقُ سجلُّ GitHub المقيس `12:07:13Z/5509a84` لا المخرَجُ المحفوظ) — والبند ١ يُثبت ما تبقّى.

---

## مقعدُ المواصفة (Spec)

HEAD=b02966ddb5082c0fcdccfb4d826bea230e686bb8

مقعدُ المواصفة على مدى الرأس `b02966d` (نقطةُ التثبيت `5509a84` · الفرعُ `fix/r65-probe-clock-and-section27`)، ومواصفتُه ملفُّ الحُكم `handoff/claude/20260926-065039-third-eye-review-65-green-by-three-minutes.md` (`REQUEST CHANGES` على #127). قراءةٌ فقط: صفرُ تحريرٍ في الشجرة/الفهرس/`HEAD`/الفروع؛ كلُّ قياسٍ في نسخٍ خارجَ المستودع (`~/.hermes/cache/scratch/r65/seats/`).

### Strengths
- **R65-1 مُنفَّذٌ وسلوكُه مُثبتٌ في الاتّجاهين بقياستانِ متقابلتان:** شغّلتُ المسبارَ قبل الإصلاح (`539a678` = رأسُ #127، وفيه الجوابُ `0455` وشاهدُه `032854`) بساعةِ عدّاءٍ تقع داخل النافذة المُعلَنة (`TZ=Etc/GMT+8` ⇒ ‎04:18 محلّيّ) ⇒ `rc=1` وسقوطٌ واحدٌ بعينه: `tests/test_answer_names_witness.py::test_every_landed_answer_names_a_witness_in_the_same_tree` (الشاهدُ «032854» ليس أحدثَ حُكمٍ قبل الجواب — والأحدثُ الشاهدُ الاصطناعيُّ `041845`). وبالشروط نفسها على الرأس `b02966d` ⇒ `rc=0` والختمُ `20260926-151918` بمنطقة `+03` **فوق أحدث ختمٍ في الشجرة المقيسة** (`20260926-151500` ⇒ ملفُّ خطة الجولة). فالعطبُ صنفيٌّ وقد أُغلق من أساسين مستقلّين: منطقةُ التسمية (`NAMING_TZ`، `tools/landing_probe.py:69`) وأرضيّةُ `+1s` فوق أحدث ختمٍ **في الشجرة المقيسة نفسِها** (`_probe_stamp` `tools/landing_probe.py:266`)، والإيداعُ يأخذ اللحظةَ نفسَها (aware) فلا يفترق الاسمُ عن الإيداع في أيّ منطقةِ عدّاء.
- **والضابطُ الجديد يعضّ فعلًا (قياسي أنا، في نسخٍ من الشجرة):** اختبارٌ نظيف `11 passed` تحت `TZ` أربعة (`—` · `UTC` · `Asia/Riyadh` · `Etc/GMT+8`)، ويسقط على ثلاث طفرات: نزعُ الأرضيّة · `NAMING_TZ`⇒`UTC` · إعادةُ وصل `measure` بساعة العدّاء (`tests/test_landing_probe.py:172,226-228`). وحصيلةُ قائمة الـCI من الـworkflow على الرأس: **289 passed · 17 skipped** (وهي 288 · 17 على `539a678` + ضابطٌ واحد ⇒ الرقمُ زاد بالضابط لا بالصياغة)، وبوّاباتُ الجودة كلُّها `rc=0` (`turn.py --check` · `render_claims.py --check` · `static_gate.py` · `publish_guard.py --tree --ci` · `amount_guard.py --ci`).
- **R65-2 مُنفَّذٌ ومحاولتُه مُثبتةٌ من GitHub لا من الشجرة:** `commits/5509a847.../pulls` ⇒ `0` (بلا PR) · تشغيلٌ على فرعٍ أوّلًا `chore/r65-witness-landing` ناجحٌ ومنتهٍ **12:06:57Z** · ثم تشغيلُ `main` **12:07:15Z** ناجح (أي أنّ الالتزامَ اخضرَّ على فرعه **قبل** دفعِه، كما نصَّت الحركة) · وحمايةُ `main` اليوم: `contexts:["guard"]` · `strict:true` · `enforce_admins:true` · `pr_reviews:null` · `rulesets:0` · والشاهدُ `handoff/claude/20260926-065039-...md` مقروءٌ من `origin/main` · وتاريخُ المستودع المذكور في النصّ (`37c10cf` · `9377353` ⇒ `pulls=0`) صحيحٌ بالقياس · و#125/#126 `MERGED` و#127 `OPEN` كما كُتب.

### Findings

1. **P2** `gh pr view 127` + `git ls-remote --heads origin` (لا موضعَ في الشجرة) — **R65-1/R65-2 لم يدخلا #127 بعدُ:** رأسُ #127 هو `539a678871434b650c0a5f9eb6a11043e34661ec` (OPEN · base `main` · BEHIND)، وفرعُ الجولة `fix/r65-probe-clock-and-section27` **ليس له مرجعٌ على `origin`** (مقيسٌ بـ`git ls-remote --heads origin | grep r65` ⇒ لا شيء؛ وبـ`git rev-parse origin/fix/r65-...` ⇒ `unknown revision`). فالالتزاماتُ الثلاثة (`f0807cc` · `79a295e` · `b02966d`) محلّيّةٌ وحدَها، و#127 لا يحملها. لماذا يهمّ: شرطُ المواصفة صريحٌ بأنّ البندين **داخل #127 قبل دمجه**؛ ودمجُ #127 كما هو يُشحن العطبَ الزمنيّ (§٢٧ نصًّا، والختمُ بساعة العدّاء) — وقد قِستُه حيًّا: تشغيلٌ بساعةٍ داخل النافذة يسقط على جوابٍ صحيح. والنافذةُ زمنيّةٌ (`(03:28:54, 04:55:00]Z` للزوج المقيس)، فـ«أخضرُ الآن» ليس دليلًا: هي تعود مع كلّ جوابٍ/شاهدٍ جديد، وملفُّ المسبار نفسُه يُعلن ذلك. الحركة: دفعُ `b02966d` (أو الثلاثة) إلى رأس #127 أو فتحُ الـPR الذي يُدمجه المدقّق، ثم تسجيلُ `guard` أخضرَ **على الالتزام المدفوع** (`ci_report <branch> --commit b02966d`) — وهي المرحلة ٨ في `handoff/sulaiman/20260926-1515-PLAN-...md`. الدليل: «**ينتظر سليمان (قبل دمج #127):** R65-1 … · R65-2 …» (`handoff/claude/STATE.md:8`)، و«next: سليمان: R65-1 وR65-2 داخل #127 قبل دمجه» (`handoff/claude/20260926-065039-third-eye-review-65-green-by-three-minutes.md:9`). الوسم: **مُثبت**.

2. **P3** `docs/handoff-protocol.md:449` — البند ٥ يقول «**ووثيقةُ المدقّق `handoff/claude/STATE.md:8-9` ما زالت تُؤطّر المسارَ بالقديم** ⇒ … وتُصحَّح **بيد صاحبها**»، والملفُّ نفسُه يدحضه: `handoff/claude/STATE.md:9` (على `HEAD` وعلى `origin/main`) يقول «مسارُ §٢٧ (الدفعُ المباشرُ بعد اخضرار الفحص، أو PR بثمن `refs/pull/*` — **الخياران قابلان للتنفيذ**)»، و`:8` يُسمّي النصَّ القديم **مُكذَّبًا** (««غيرُ قابلٍ للتنفيذ … مقيس» غيرُ مقيس ويدحضه التاريخ»). أي أنّ جملةً عن وثيقةٍ أخرى كُتبت بلا قراءتها في حالها الراهن (نقلٌ من تصحيح الجولة ٦٤). لماذا يهمّ: هذا بعينه الصنفُ الذي وُلد منه R65-2 — **ادّعاءٌ عن وثيقة بلا قياس** — في الوثيقة التي أُمِر §٢٧ بتنقيتها؛ ويُطالب المالك/المدقّقَ بتصحيح صفحةٍ صحيحةٍ أصلًا، ويُربك القارئ في موضعٍ حاكم. الحركة: يُحذَف البند ٥، أو يُعاد صوغُه مقيسًا بلفظه: «قِيس 2026-09-26 أنّ `claude/STATE.md:8-9` صار يُؤطّر المسارَ بالخيارَين (والتصحيحُ الذي كان في الجولة ٦٤ زال)». الدليل: «**«وبلا PR غيرُ قابلٍ للتنفيذ على `main` … مُقاسٌ بأمرٍ ومخرَج»: الاستحالةُ لم تُقَس، وتاريخُ المستودع يدحضها.**» (`handoff/claude/…-review-65-….md:53`)، ومعه «وتُصحَّح **بيد صاحبها**» (السطر ٤٥٠ من الملفّ نفسه). الوسم: **مُثبت**.

3. **P3** `docs/handoff-protocol.md:445` — «(ولهذا نُفِّذ في الجولة ٦٥ ما هو **مُعلَنٌ على `main`** وهو (أ)، وسُجِّل مخرَجُه أعلاه)» يُثبّت لمسلكٍ ما يعلنه البندُ ٣ نفسُه **مُتنازَعًا**: السطرُ ٤٤٢–٤٤٤ يقول «وتعارضٌ مُعلَن لا يُرجَّح من جهتي … فالمسألةُ قرارُ مالكٍ … لا اجتهادُ المنفّذ»، و`handoff/STATE.md:105` (على `HEAD` وعلى `main`، مؤرَّخ ٢٠٢٦-٠٩-١٩) يأمر بالمسار (ب) نصًّا: «لا دفع مباشر إلى `main` … والمسار فرع ⇒ PR ⇒ فحص `guard` أخضر ⇒ دمج». فالقولُ إنّ (أ) هو «المُعلَن على `main`» انتقاءٌ لا قياس، والوثيقتان مُعلَنتان معًا. لماذا يهمّ: الجولةُ نفّذت فعلًا **دفعًا مباشرًا إلى `main` المحميّ** (مُثبتٌ من GitHub أعلاه)، وهذه الجملةُ تُورِّث قارئًا لاحقًا سندًا للدفع المباشر قبل قرار مالكٍ مؤرَّخ — وهو ما تنصّ المواصفةُ على أنّه قرارُ المالك. (وفيها أيضًا تسميةُ `handoff/STATE.md` «وثيقةً مالكيّة» وهو ملفُّ الطرفين المشترك بترويسته.) الحركة: يُقال «نُفِّذ (أ) **مرّةً واحدة** كمحاولةِ قياسٍ أذِنت بها R65-2، والتعارضُ قائمٌ حتى قرارٍ مالكيّ مؤرَّخ» — بلا «ما هو مُعلَنٌ على `main`». الدليل: «الحركة: المسارُ قرارُ المالك، والخياران قابلان للتنفيذ.» (`…-review-65-….md:66`)، و`:9` من `handoff/claude/STATE.md`. الوسم: **مُثبت**.

4. **P3** `tests/test_landing_probe.py:172` — الضابطُ المطلوبُ أُنفِذ **بشكلٍ مخالفٍ لصيغته المسمّاة**: المواصفةُ طلبت «ضابطٌ يُشغّل المسبارَ بـ`TZ=UTC` على شجرةٍ فيها جوابٌ حديثٌ وشاهدُه، وهي الحالةُ أعلاه بعينها». المُنجَزُ لا يُشغّل المسبارَ في أيّ `TZ`: يستدعي `_probe_stamp` بلحظةِ عدّاءٍ مصنوعة كائنًا، ويُمرّر أسماءَ الحالة الحقيقيّة (`032854` شاهدًا · `0455` جوابًا) في **حارس §٢٧ الحقيقيّ** (`ungated_answers` — مصدرٌ واحد، وهذا حسن)، ثم يُثبّت الوصلَ **بمطابقة نصّ الشيفرة** (`:226-228`). الناقصُ بالاسم: **تشغيلُ `tools/landing_probe.py` نفسِه تحت `TZ=UTC` على شجرةٍ تحمل جوابًا حديثًا وشاهدَه** (subprocess أو `measure` داخلَ الاختبار). لماذا يهمّ: (أ) مطابقةُ السطر الحرفيّة هشّةٌ في الاتّجاه المعاكس أيضًا — إعادةُ تنسيقٍ تُسقِط الضابطَ وكودُه سليم، وهو صنفٌ سبق أن ضُيّق في هذا المستودع («والضابطُ يُقاس بمُحلِّل المُنتِج نفسِه» — `tests/_local_evidence.py`، مقعدُ البنية · مراجعة إغلاق ٦٤)؛ (ب) ولا خطوةَ CI تُثبّت **الحالةَ المقيسة** (علامةُ العدّاء داخلَ النافذة): خطوةُ `gate_landing` تُشغّل المسبارَ بتوقيت العدّاء، لكنّ فشلَ العطب كان **مشروطًا بوقت التشغيل** فلا يُعاد إنتاجُه منها بحتميّة — ولهذا طلبت المواصفةُ ضابطًا يُشغّله على الحالة بعينها. القياسُ الاتّجاهيّ (سقوطٌ قبل الإصلاح · نجاحٌ بعده) **قياسي أنا خارجَ الشجرة**، لا حارسٌ في الشجرة. وهذه البوّابةُ باقيةٌ ناقصةً لا خاطئة: الضابطُ يسقط على ثلاث طفرات (مقيس) فقيمتُه قائمة. الحركة: يُضاف اختبارٌ يُشغّل المسبارَ بـ`TZ=UTC` (وأرضيّةً `TZ` ثانية) على شجرةٍ مصنوعةٍ فيها زوجُ (شاهد، جواب) حديث — أو يُنزَل الوصلُ من مطابقة النصّ إلى نداءٍ حقيقيّ. الدليل: «الحركة: يُختم الاسمُ بمنطقة التسمية التي تحملها الشجرة (+03)، أو بعد أحدث ختمٍ فيها، لا بمنطقة العدّاء. **ومعه ضابطٌ يُشغّل المسبارَ بـ`TZ=UTC` على شجرةٍ فيها جوابٌ حديثٌ وشاهدُه، وهي الحالةُ أعلاه بعينها.** ثم يُدمج #127.» (`…-review-65-….md:51`). الوسم: **مُثبت**.

### Declined to judge
- **مسارُ §٢٧ نفسُه (دفعٌ مباشر أم PR):** قرارُ المالك بنصِّ المواصفة والمدقّق («المسارُ قرارُ المالك، والخياران قابلان للتنفيذ» · `handoff/claude/STATE.md:9`) — فلا أُرجّح بين (أ) و(ب).
- **توقيتُ دمج #127 وقرارُ الدمج:** فعلُ مالكٍ وتقويمُه، و#127 مفتوحٌ الآن (لا ضررَ وقع).
- **الفرعُ المحلّيّ `chore/round56-seats-closure` وتثبيتُ شجرة البيانات:** لا سطرَ لهما في هذه المواصفة (نُقلا في مراجعة ٦٥ كملاحظتَي مالكٍ).
- **أثرٌ جانبيّ مقيسٌ لا سطرَ له:** ملفُّ خطة الجولة (`…20260926-1515-PLAN-…md`) يُزيح مُشتقَّ الدَّور في `tools/turn.py` من «sulaiman» إلى «claude» (مقيس: `rc=0`، والمُخرَج «الدورُ الآنيّ: claude (آخرُ فاعلٍ sulaiman (20260926-151500))»)، وهو اسمٌ مُعلَنٌ في `NON_ANSWER_FILES_SINCE_RULE` ولا يحكمه بندٌ هنا.
- **دلالاتُ `page_rows` وصفحةُ ٦٢٩ ودقّةُ Vision وبنودُ اللوحة:** أعلن المدقّقُ نفسُه استبعادَها.

### Assessment
Verdict: **APPROVE WITH FIXES** — R65-1 وR65-2 مُنفَّذان في المدى المُثبَّت ومُثبتان بالقياس (لا بالادّعاء): العطبُ الزمنيّ مُعادٌ إنتاجُه قبل الإصلاح ومسقَطٌ بعده بنفس الشروط، وضابطُه يعضّ على ثلاث طفرات، وحصيلةُ قائمة الـCI على `b02966d` = 289 · 17، وبوّاباتُ الجودة الخمسُ `rc=0`، ومحاولةُ §٢٧ المُسجَّلة صحيحةٌ بكلّ أرقامها من GitHub. ويبقى للإغلاق: (١) **دفعُ المدى إلى #127** (رأسُه `539a678` لا يحمله — والدمجُ اليوم يشحن العطب الزمنيّ)، و(٢) تصحيحُ سطرَي §٢٧ اللذين يَدّعيان بلا قياس (`:449` · `:445`)، و(٣) نَقصُ صيغة الضابط المسمّاة (`TZ=UTC` على شجرةٍ فيها جوابٌ وشاهدُه).

ما لم يُثبت: (أ) **أنّ ضابطًا في الشجرة يُشغّل المسبارَ بـ`TZ=UTC` على شجرةٍ فيها جوابٌ وشاهدُه** — غيرُ منفَّذ؛ والذي أُثبته هو قياسي الاتّجاهيّ في نسخٍ خارجَ الشجرة، ويُثبته اختبارٌ من هذا النوع مُودَع. (ب) **أيُّ تشغيلٍ من GitHub على `f0807cc` · `79a295e` · `b02966d`** — الفرعُ بلا مرجعٍ على `origin` ⇒ لا شهادةَ CI على الرأس؛ يُثبته دفعُ الفرع ثم `ci_report <branch> --commit b02966d` = أخضر (ولا يُقرأ أخضرُ المحلّيُّ شهادةً). (ج) **صحّةُ الشجرة الكاملة على `b02966d` في بيئة Vision/البيانات** — قِستُ النسخةَ النقيّةَ فقط (17 تخطّيًا مُعلَنًا)، ويُثبتها تشغيلُ المالك في بيئته. (د) **أنّ `main` سيبقى أخضر** بعد دفع المدى: يُثبته تشغيلٌ على `main` بعد الدفع داخل/خارج النافذة الزمنيّة.

---

## مقعدُ البنية (Structure)

HEAD=b02966ddb5082c0fcdccfb4d826bea230e686bb8

مؤكَّد: `git rev-parse HEAD` في `~/Projects/wt-r65-fix` ⇒ `b02966ddb5082c0fcdccfb4d826bea230e686bb8` (المطلوب). المدى: `8781ae1..HEAD` = الالتزاماتُ الثلاثة وحدها (٦ ملفّات · +222/−26)، وما قبلها داخل `5509a84...HEAD` جاء مع دمج `origin/main` وسِبقَ أن راجعه المدقّق في المراجعة ٦٥.

### Strengths

- **الساعةُ الثانية حُذفت لا أُصلحت:** الاسمُ وزمنُ الإيداع يُصاغان من **اللحظة نفسها** (`tools/landing_probe.py:348` → `_probe_stamp` → `_commit(review, dst, stamp_dt)` عند `:355`)، بعد أن كانا يُصاغان من ساعتين (`datetime.now()` للاسم و`_stamp_dt(stamp).astimezone()` للإيداع). حذفُ اشتقاقٍ ثانٍ = حذفُ صنف عطبٍ لا إصلاحُ حادثة.
- **القياسُ من الشجرة المقيسة لا شجرة المنفّذ:** الأرضيّةُ تُقرأ من `dst` (`_tree_stamps(dst)`) نظير ما فُعل بـ`ci_files(dst / WORKFLOW_REL)` — نفسُ الانضباط في الموضعين.
- **صفرُ تفرّعٍ جديد في المسار المزدحم:** `measure` كسب سطرَي إسنادٍ لا فرعًا؛ والمنطقُ الجديد في دالّةٍ صغيرةٍ **الساعةُ مُمرَّرةٌ إليها** (`_probe_stamp(now, stamps)`) ⇒ تُقاس بلا انتظار ساعة.
- **الضابطُ مبنيٌّ على آلة §٢٧ نفسِها لا على نسخةٍ ثانية:** `_witness_gate()` يستدعي `gate.ungated_answers` من `tests/test_answer_names_witness.py` (لا إعادةَ كتابةٍ لنافذة `(شاهد, جواب]`)، ويقيس الاتّجاهين + الأرضيّة + لا-اعتمادَ-على-منطقة-العملية.
- **الضابطُ يعضّ بقياسٍ مضادّ:** نزعُ الوصل في `measure` ⇒ سقوط `tests/test_landing_probe.py:226`؛ نزعُ الأرضيّة ⇒ سقوط `:213`؛ وبإعادة الأصل: `11 passed`. ومسبارُ الهبوط نفسه يهبط أخضرَ تحت ساعة العدّاء: `TZ=UTC python tools/landing_probe.py` ⇒ `rc=0` والشاهدُ `handoff/claude/20260926-151913-…` (‎+03).
- **الإعلانُ قبل القياس:** ملفُّ خطة الجولة قُيِّد في `NON_ANSWER_FILES_SINCE_RULE` باسمه وصنفه (الخامسُ من الصنف) ⇒ لم يشترِ أخضرَ بإسقاط الضابط أوّلًا. والوثيقةُ تبعَث الشيفرة: `docs/GATES.md:86-88` تُعلن القاعدةَ التي تقيسها `tests/test_landing_probe.py:172` بالحرف — لا ادّعاءَ وثيقةٍ بلا حارس.

### Findings

1. **P2** `tools/landing_probe.py:230` (مع `:69` · `:243` · `:282` و`tests/test_landing_probe.py:111`) — الملفُّ يحمل الآن **مُحلِّلَين** لصيغة الاسم نفسِها: `_stamp_dt` (شرائحُ يدويّة + `fromisoformat`) و`turn.as_datetime` (مستورَدٌ عند `:76` ومُستعمَلٌ للأرضيّة عند `:282`)؛ والنسخةُ المحلّيّة **أضيقُ من مصدرها بقياسٍ على الشجرة نفسِها**: `_stamp_dt("20260922-240000")` ⇒ `ValueError: hour must be in 0..23` بينما `as_datetime` ⇒ `2026-09-23 00:00`، والاسمُ `handoff/sulaiman/20260922-240000-item4-…md` **متعقَّبٌ في المستودع** (واصطلاحُ `24:00` موثَّقٌ في `tools/turn.py:92-102`، وله ضابطٌ في `tests/test_report_names.py:108`). وهذا بعينه الصنفُ الذي يُسمّيه الجارُ في `tests/test_answer_names_witness.py:167-168` («نسخةٌ ثانية محليّةٌ أضيقُ من مصدرها … الثواني واصطلاحُ `24:00`»). لماذا يهمّ: (أ) الشرحُ الحرفيّ لـ«مُحلِّلٌ واحد» في `:231` صار غيرَ صحيح، والنسخةُ الضيّقةُ على بُعد سطرٍ من مسار الأرضيّة ⇒ أيُّ تنظيفٍ لاحقٍ يُمرّر الأرضيّةَ من `_stamp_dt` يقلب `gate_landing` إلى `rc=2` «تعذّر قياس» على شجرةٍ سليمة؛ (ب) الضابطُ المسمّى `test_one_parser_for_the_stamp_format` **أخضرُ** لأنّه يعدّ الجزءَ النصّيَّ `"stamp[9:11]"` (‎=1) وهو ما لا يمكن أن يحتويه المُحلِّلُ الثاني (`fromisoformat`) ⇒ العَدُّ لا يتتبّع الاسمَ الذي يحمله، فيبقى أخضرَ عند كلّ افتراقٍ قادم؛ (ج) والحقيقةُ نفسها (‎+03) صارت مُعلَنةً **أربعَ مرّات في ثلاثة ملفّات**: `:69` · `:243` · `tests/test_report_names.py:21` · `tests/test_landing_probe.py:202,214` — بينما تعليقُ الملفّ عند `:73` يُعلن «نمطُ الاسم **واصطلاحُ زمنه** من مصدرٍ واحد». الحركة (جودو مُسمّاة: **زمنُ الاسم له مالكٌ واحد**): يُنقَل ثابتُ المنطقة إلى `tools/turn.py` بجانب `NAME_RE`/`as_datetime`، ويُحذف `_stamp_dt` (تذهب النسخةُ الثانيةُ والثالثة والرابعُ للثابتة نفسها)، ويُشتقّ إزاحةُ السمّ من مالكها (`as_datetime(stamp) - timedelta(hours=3)` = الإزاحةُ التي يُعيد إنتاجها)، ثم يُعاد توجيهُ الضابط ليقيس **الخاصّيّة** (لا مُحلِّلَ محلّيّ) لا جزءًا نصّيًّا — والقاعدةُ نفسُها مُقيَّدةٌ في المستودع: «المنتجُ يبنيه والحارسُ يستورده» (`tests/test_flake_ledger.py:31`). وأخيرًا `_tree_stamps` نفسُها إعادةُ كتابةٍ لمسح المالك `turn._reports` (`tools/turn.py:105-112`) ⇒ يُوسَّع المالك بمعامل `root` بدل نسخةٍ ثانية. الدليل:
   ```
   $ python probe_parsers.py        # على نسخةٍ من الشجرة عند b02966d
   مفاتيحُ صندوقَي المراسلة: 143 | تُسقط `_stamp_dt`: [('20260922-240000','ValueError','hour must be in 0..23')]
   تختلف فيها القيمة: []            # ١٤٢ تُوافق ⇒ التفرّدُ في حدِّ البيانات نفسِه
   count('stamp[9:11]') = 1 | count('fromisoformat') = 1 | نداءات _stamp_dt = 2
   $ pytest -q tests/test_landing_probe.py::test_one_parser_for_the_stamp_format  ⇒ 1 passed
   ```
   الوسم: مُثبت

2. **P2** `tools/landing_probe.py:72` — `BOXES = (BOX, "handoff/sulaiman")` إعلانٌ **ثانٍ** لنطاقِ حارس §٢٧ (ثوابتُه في `tests/test_answer_names_witness.py:54` و`:61`)، و**لا شيءَ يقيس الرابطَ بينهما** (`rg -n "BOXES" tests/` ⇒ لا نتيجة). لماذا يهمّ: الأرضيّةُ هي وحدها ما يُخرج الشاهدَ الاصطناعيَّ من نوافذ `(شاهد, جواب]`، وصحّتُها مشروطةٌ بأن يبقى `BOXES ⊇` صناديقَ الحارس؛ فلو وُسِّع نطاقُ الحارس (صندوقُ أجوبةٍ ثالث) لأرضت النسخةُ **دونَ أرضيّة** ⇒ يقع الشاهدُ في نافذة جوابٍ حقيقيّ ⇒ `gate_landing` يُعلن `rc=1` «المراجعةُ لم تهبط» على شجرةٍ **سليمة** — وهو صنفُ الفشل الكاذب الذي لاحقته الجولات (R57-1 · R58-1 · R59-1 · R61)، ورسالةُ العطب تُسمّي الهبوطَ لا النطاق. والنطاقان يتطابقان اليوم بقياسٍ لا بتصميم (`handoff/` لا يحمل صندوقًا ثالثًا مُوقَّعًا؛ 164 مفتاحًا من الصندوقَين)، والباقي من مُدخَلات المسبار مُثبَّتٌ بقياس: `test_the_list_follows_the_tree_it_is_pointed_at` و`test_the_probe_is_wired_into_ci_as_a_registered_step` ⇒ هذا **الموضعُ الوحيدُ غيرُ المربوط**. الحركة (جودو مُسمّاة: **مجموعةُ الصناديق تُعلَن مرّةً واحدة**): يُشتقّ `BOXES` من مالك التخطيط `tools/turn.py` (`SIDES`/`handoff/<side>` عند `:48-49` و`:105-112`)، ويُضاف قياسٌ واحدٌ يُثبّت التطابقَ مع ثابتَي الحارس ⇒ توسيعُ الحارس يكسر ضابطًا لا معنى `gate_landing`. الدليل:
   ```
   $ rg -n "BOXES" tests/                      # لا شيء
   tools/landing_probe.py:72: BOXES = (BOX, "handoff/sulaiman")
   tests/test_answer_names_witness.py:54: BOX = "handoff/sulaiman/"   :61: WITNESS_ROOT = "handoff/claude/"
   landing_probe.py:281-284: if keys: floor = as_datetime(keys[-1])…;  if t <= floor: t = floor + 1s
   ```
   الوسم: مُثبت (التكرارُ وغيابُ الرابط مقيسان؛ الأحمرُ الكاذبُ نتيجةٌ مشروطةٌ بتوسيعِ الحارس)

### Declined to judge

- **قياساتُ GitHub في البندين ٢ و٤ من تصحيح §٢٧** (`PUSH_RC=0` · `f37859b..5509a8`) · `commits/5509a84/pulls ⇒ 0` · `gh pr view 125/126/127`): لا تُقاس من داخل الشجرة بلا شبكة/`gh`، ولم أُجرِها — ولا تصديقَ ولا تكذيبًا.
- **ترجيحُ الصراع بين §٢٧ الأصليّ و`handoff/STATE.md:105`** (دفعٌ مباشر مقابل PR): وثيقتان مالكيّتان، والترجيحُ قرارُ مالكٍ لا قرارُ مقعد — والتصحيحُ يُعلنه ولا يُخفيه، وهو المطلوب.
- **شكلُ نصّ §٢٧** (موضعُ «تُرفَع» داخل قائمة قياسٍ من ستّة بنود مقابل علامة رفعٍ في الموضع): لم أحسم أيّهما يقتضيه R65-2، والرفعُ **ملاصقٌ** للمرفوع («الجملةُ السابقة تُرفَع») ⇒ لا أُبنيه finding، وأتركه لمقعد المواصفة/المدقّق.
- **ملفّاتُ المدى خارج الالتزامات الثلاثة** (`tools/ci_report.py` · `src/statement_qa/*` · `tests/_local_evidence.py` · `docs/QA_CHECKLIST.md` · `docs/claims.json`): جاءت مع دمج `origin/main` (‎`5509a84...8781ae1`) وسِبقَ أن راجعها المدقّق في المراجعة ٦٥، ولم يمسّها هذا التغيير.
- **`tools/qa_gate.py:55-60`** (`_stamp = datetime.now()` يُسمّي `handoff/sulaiman/<زمن>-gate-evidence-full-run.txt`): نظيرٌ للصنف نفسه على ساعة العملية، لكنّه ملفٌّ **لم يتغيّر** في هذه الجولة، ومُخرَجُه `.txt` خارج مُرشِّحَي `test_report_names` و§٢٧ (كلاهما `.md`) ⇒ لا أعُدّه انحدارًا هنا؛ يذوب في حركة البند ١ (مالكٌ واحدٌ للمنطقة).

### Assessment

Verdict: APPROVE WITH FIXES — التصميمُ خرج **أفضلَ**: الساعةُ الثانيةُ حُذفت، والقرارُ الزمنيُّ صار دالّةً واحدةً بساعةٍ مُمرَّرة، وحارسُ §٢٧ صار هو المُقيسَ به لا نسخةً منه، والوثيقةُ تبعَث الشيفرة. ويعضّ الضابطان الجديدان بطفرةٍ مقيسة، والمسبارُ يهبط `rc=0` تحت `TZ=UTC`. والفائتاتُ اثنتان لا ثالثَ لهما: تكرارُ الحقائق الزمنيّة للاسم داخل الملفّ الواحد بحارسٍ يقيس نصًّا لا خاصّيّة (P2)، وفصلُ أرضيّةِ النطاق عن نطاق الحارس بلا قياس (P2) — كلتاهما تُغلق بحركةٍ واحدةٍ مُسمّاة: **زمنُ الاسم ونطاقُه لهما مالكٌ واحدٌ (`tools/turn.py`)**، ولا واحدةَ منهما تُشكّك في هبوط الإصلاح.

Not proven: أنّ الالتزاماتَ المدقّقة تُنفّذ المسارَ الذي ستقرّره الوثيقة (وقياسَاتُ GitHub في البندين ٢ و٤) — يُثبتها أو يُبطلها قياسٌ على `gh` من جهة المالك/المدقّق. وأنّ توسيعَ نطاق الحارس يقلب `gate_landing` إلى `rc=1`: المحرّكُ مقيس، والنتيجةُ استنتاجٌ من قراءة `ungated_answers`/`newest_verdict_before` — يُثبتها طفرةٌ مصنوعةٌ في نسخة (صندوقُ أجوبةٍ ثالث + جوابٌ فيه) تُشغَّل على المسبار. وأنّ `_stamp_dt` يُسقط الإصلاحَ حيًّا: لا مسارَ إنتاجيَّ يمرّ به مُدخَلُ `24:00` اليوم (السمُّ يُبنى من لحظةٍ حقيقيّة) ⇒ العطبُ كامنٌ لا واقعٌ، ولذلك صار P2 لا P1.

---

