```
id:      20260925-0520-sulaiman
from:    sulaiman
to:      owner
type:    REPORT
step:    إغلاقُ ملاحظات مقاعد الجولة ٥٧ (Standards · Spec · Structure) — وإقرارُ البوّابة الدائريّة، والقرارُ الباقي
commit:  513b61b
```

**هذا تقريرُ إغلاقٍ إلى المالك، لا جوابٌ على حُكم — ولذلك بلا `in-reply-to:`** (والحكمُ الذي يُغلق، مراجعةُ ٥٧
وتقاريرُ المقاعد الثلاثة، **غيرُ هابطٍ في الشجرة**: المراجعةُ على فرع، والمقاعدُ تُبلّغ خارج المستودع).
والحقلُ يقيس مساراً **موجوداً**، فلا يُكتب فيه شاهدٌ لا وجودَ له. وهو **أوّلُ ملفٍّ يُقاس بالعدّاد الجديد**
(البند ٦ في ترويسة الحارس) — أُعلن صنفُه ولم يُترك صامتاً.

---

## ١ · الحالةُ في سطر

المدى المُجمَّد للجولة: `e903b12...513b61b` (الأساسُ `origin/main`، والالتزامان: `294eefa` حارسُ الجواب،
و`513b61b` إغلاقُ ملاحظات المقاعد). الشجرةُ نظيفة. المقاعدُ الثلاثةُ حكمت **APPROVE WITH FIXES** وصفرُ P0،
والملاحظاتُ كلُّها مُغلَقةٌ الآن بقياسٍ يُعاد إنتاجه — إلّا **حاجزاً واحداً ليس في الكود**: قرارُ الدفع (§٤).

## ٢ · ما أُغلق — وشاهدُ كلٍّ

| الملاحظة (مصدرُها) | ما كان | ما صار | الشاهد |
|---|---|---|---|
| **S-1 · ST-1 (P1)** مقعدا Standards وStructure | `guard_bite_sweep` يطرح التخطّيَ **مرّتين** ويطبع «دعاوى بلا حماية» على تخطٍّ لا يدّعي: العددُ ١٢ وصفوفُ «تعضّ» ١٣ في المخرَج نفسه | العدّ من `counts` بخاناتٍ أربعٍ مستقلّة، والحكمُ من `_exit_code` | «١٤/١٤ تعضّ · صفرُ تخطٍّ» في بيئةٍ مُهيّأة، وحالاتُ سمٍّ في الذاكرة (`tests/test_guard_bite_sweep_logic.py`) |
| **ST-2** | مخرَجُ عطبِ الجمع (`rc=2`) يُقرأ «عَضّ» من نصّ «1 error in 0.04s» | الرمزُ يُحكم أوّلًا، و`rc=1` بلا فشلٍ مُسمّى ليس شهادة | `test_a_collection_error_is_not_a_bite` + مُدخَلٌ مصنوعٌ في `_verdict` |
| **S-2** | `WITNESS_FROM` يُوصَف «دقيقةَ الالتزام» وهو قبلها بأربع دقائق | وصفٌ صحيح (مكتوبٌ بيد، وسببُه معلَن) **وموضعُه مقيس** | `test_the_cutoff_exempts_no_answer_by_accident` |
| **S-3** | «٢٧ سطراً في ١٦ ملفّاً» بيدٌ في ثلاث وثائق، وقياسُه **١١** | الرقمان مقيسان بالثابتين | `TATWEEL_FORM_LINES`/`TATWEEL_FORM_FILES` + الاختبار |
| **S-4 · ST-6** | تجريدُ اقتباسٍ **يوسّع** المطابقة، وسياجٌ يتيمٌ يُهمِل كلَّ ما بعده | التجريدُ أُسقط، والسياجُ يُقابَل زوجاً | `test_a_field_carrying_a_leading_backtick_is_not_a_declaration` + `test_an_unpaired_fence_does_not_hide_a_real_declaration` |
| **S-7** | `OWNER_SIGNALS` ثابتٌ بلا قارئ، وقاعدتُه المُوثَّقة تخالف القاعدةَ المكتوبةَ بجانبه | حُذف من الشجرة، ونصُّه أُعيد كتابة | `grep -rn OWNER_SIGNALS tools/ tests/ docs/` ⇒ غائب |
| **SP-3** (زحفُ موضع القراءة) | الحقلُ يُقرأ من **متن** التقرير كلِّه لا ترويسته — ولا إعلانَ للنطاق | **أُعلن النطاقُ صراحةً في §٢٥** ولم يُضيَّق (تضييقُه يغيّر معنَى القاعدة ويحتاج قرارك) | نصُّ §٢٥ الجديد + بقاءُ `tests/test_turn_derivation.py` متساوياً |
| **SP-1 · ST-3** | §٢٥ يوثّق الصيغةَ الملغاة («سطرٌ يبدأ بالعلامة») ولا يذكر قفلَ المالك | تصحيحٌ مؤرَّخ بالصيغة الواحدة وموضعيها وما لا يُعلن | نصُّ §٢٥ + صفّ ٦ في `docs/GATES.md` |
| **ST-4 · ST-5 · SP-2** | حارسُ الشاهد بنسخةٍ أضيقَ من مصدره · وصيغةُ الوثيقة تُسقط الـCI · وصنفُ الأجوبة قابلٌ لأن يصير فارغاً | `stamp_of` من `tools/turn.py` · وقبولُ `<مسار>` والذيل · وعدّادُ الصنف | ثلاثةُ ضوابطَ جديدة في `tests/test_answer_names_witness.py` |
| **S-5 · S-6 · SP-5** | «حدودٌ أربعة» وخمسة · و`in-reply-to` غائبٌ عن كتلة الصيغة · وتوصيةٌ مُحيلةٌ إلى تقريرٍ لم يهبط | ستّةُ حدود · الحقلُ في §٢ · والتوصيةُ **نصًّا** بحدودها الثلاثة | نصُّ §٢ و§٢٧ وتصحيح ٠٣٥٥ |

**وتصحيحُ ذاكرةٍ في الطريق:** ظنّي أنّ إعادةَ كتابة الخارطة التزامٌ (`b9ca6ed`) — والقياسُ كذّبه: `b9ca6ed`
هو **تلقينةُ التقييم** لا إعادةَ كتابة. فأُعلن التصحيحُ بدل تمريره.

## ٣ · القياسات (كلُّها مُنفَّذةٌ الآن، لا منقولة)

| القياس | الأمر | النتيجة |
|---|---|---|
| المجموعةُ كاملة | `pytest -q tests/` | **728 passed** |
| البوّابةُ الساكنة | `tools/static_gate.py` | PASS |
| حارسُ المبالغ | `tools/amount_guard.py --ci` | PASS |
| المطالباتُ المعلَنة | `tools/render_claims.py --check` | كلُّ رقمٍ يطابق مصدره |
| المؤشّرُ والدور | `tools/turn.py --check` | rc=0 |
| مسحُ البوّابات | `tools/guard_bite_sweep.py` | **١٤/١٤ تعضّ · صفرُ تخطٍّ** |
| محاكاةُ الـCI على استنساخٍ نظيف | `publish_guard --tree/--history/--ci` + قائمةُ الـworkflow | **403 ملفّاً** (بملفّ هذا التقرير) · **0 BLOCK** · «آمنٌ للدفع» · **146 passed · 14 skipped · 0 failed** |

**وسمُّ الاتّجاهين (قاسه المقعدان):** فرعٌ يحمل **مراجعةً جديدةً وجواباً يسمّيها** ⇒ **لا يسقط** (يمرّ الجوابُ
ويُطالَب الكاتبُ بتحديث عدّاد الصنف في الالتزام نفسه) · وجوابٌ **يشير إلى مراجعةٍ غائبة** ⇒ **يسقط**. وهذا
عينُ الفرق عن الحارس المنزوع: كان يُسقط الفرعَ الذي يحمل المراجعة، ويُنجح `main` **دون أن يقيس شيئاً**.

## ٤ · الحاجزُ الباقي — وقرارُه بيدك (ليس في الكود)

المدقّق أعلن أنّه **لن يدفع** مراجعةَ ٥٦ لأنّها كانت تسقط بحارسٍ **دائريّ** (يشترط وجودَ ملفّها في
`origin/main` مسبقاً — شرطٌ لا يملكه الفرعُ الذي يحملها). الدائرةُ **أُزيلت** هنا بحارسِ الجواب (§٢)، وقائمةُ
الـCI والسجلُّ والبروتوكول **متساويةٌ معها** الآن، والمحاكاةُ على استنساخٍ نظيفٍ خضراء.

**ما بقي قراراً لك وحدَك:**
1. **يُدفع الفرعُ الحالي** (`chore/round56-evidence`) فتمرّ مراجعةُ ٥٦ فوقه بلا حارسٍ دائريّ — وهي التعليمُ
   الذي طلبه المدقّق؛ **أو**
2. **تكليفُ المدقّق صراحةً** بالإصلاح داخل فرعه (وهو الأمرُ الذي طلبه حرفيًّا)، مع ما يترتّب عليه من أنّ الإصلاح
   يهبط مع مراجعته لا قبله.

وفي الحالتين: لا يُنشأ مرجعٌ على GitHub، ولا يُدفَع شيءٌ إلى `main` بلا أمرٍ صريح منك.

## ٥ · ما لم يُثبت

- **الـCI على GitHub لم يُشاهَد**: كلُّ قياسي محلّيّ أو على استنساخٍ نظيفٍ من المرآة — لا دفعَ ولا تشغيلَ workflow.
- **صحّةُ الشاهد** (أنّ المسارَ المُعلَن هو المراجعةَ المقصودة فعلاً) **غيرُ مقيسة**: الحارسُ يقيس الوجودَ لا
  التطابق — وهو **حدٌّ مُعلَن** في ترويسته (البند ٣)، ويبقى مفتوحاً للمالك.
- **الأحكامُ الثلاثةُ لم تُقابَل بشرٍ مستقلّ عن الجولة**: المقاعدُ شغّلتها أنا (وكلاء)، وتقريرُها في
  `~/.hermes/cache/delegation/live/` خارج المستودع.
- **«728 passed» في بيئة المالك** تشمل تخطّياً مُعلَناً في القياس النظيف (14) — والرقمان يُذكران معاً كما
  تشترط القاعدة ٢٣، ولا يُنشر واحدٌ بدل الآخر.
- **ثمنُ جولة الإغلاق من وقتك وكلفتِها** غيرُ مقيسٍ هنا (لا محاسبةً لزمن الجلسات).

## ٦ · حدُّ هذا التقرير نفسِه

هذا الملفُّ **ليس جواباً على حُكم** (§ الترويسة)، فسُجّل في **عدّاد الصنف المقيس** للحارس الجديد: صار **١**.
فإن كثرت التقاريرُ إلى المالك في هذا الصندوق، صرخ العدّادُ فيلتفت الكاتبُ: أهو جوابٌ (فيُعلن شاهدَه) أم تقريرُ
مالكٍ (فيُعلن صنفَه)؟ — لا يمرّ صنفٌ جديدٌ صامتاً.

## ٧ · توصيةٌ مستدامة (§٢٠ — نصًّا لا إحالة)

**صنفٌ يُقتل:** حارسٌ يُعلن رقماً أو دعوى لا يطابق مخرجَه — وهو صنفُ S-1 (حصيلةٌ ناقصةٌ بمقدار كلّ تخطٍّ،
وتنبيهٌ كاذبٌ في الأداة التي وُلدت **لإنهاء الدعاوى الكاذبة**).
**وما يُقاس به:** خاناتٌ مستقلّةٌ للحالات · و`_exit_code` من الدعاوى وحدَها · **ودالّتان خالصتان تُقاسان بسمٍّ
في الذاكرة** (`tests/test_guard_bite_sweep_logic.py`) لا بقراءة نصّ المخرَج.
**وما لا يُفرض بيدٍ واحدة:** دلالةُ الحالة (عَضّةٌ أم عطبُ بيئة) تبقى قرارَ الكاتب المُعلَن، والعضّةُ الحقيقيّةُ
تحتاج حادثةً مصنوعةً في **نسخة** — فلا يُدّعى أنّ المسحَ يمنع كلَّ دعوى كاذبة، بل يمنع **الصنفَ** الذي قِيس.
**(حالتُه: مُنفَّذةٌ في `513b61b` — وغيرُ المنفَّذ منها: توسيعُ السمّ إلى بوّابات GitHub، وهو بيد المالك.)**

## ٨ · مقعدُ التحقّق الرابع (على مدى الإغلاق `294eefa...e3250b8`)

**الحكم: APPROVE WITH FIXES** — ولا حاجبَ في الكود: المجموعةُ `728 passed`، وكلُّ بوّابات المستودع خضراء،
والفرعُ يعبر حارسَ النشر **بالأمر الذي يشغّله الـCI نفسُه** (`--tree --history --ci` ⇒ 0 BLOCK)، والحارسُ
الجديد عضّ في الاتجاهات الأربعة في نسخٍ منسوخة. وأربعةُ بنودٍ توثيقيّة (P3) أُغلقت فوراً:

| بندُ المقعد | ما قاسه | الإغلاق |
|---|---|---|
| «+٦» في `docs/QA_CHECKLIST.md` والمقيس **+٧** | `git diff … -- tests/ \| grep -c '^+def test_'` ⇒ ٧ | صار **+٧** بالأسماء السبعة، والأمرُ مُثبَّتٌ في الوثيقة |
| §٢٥ لا تُعلن نطاقَ الموضع (SP-3) | `tools/turn.py` يقرأ كلَّ سطر — ولا نصَّ يُعلن ذلك | **نُفِّذ التضييقُ بأمر المالك** — انظر §٩ |
| تقريرُ المالك بلا توصيةٍ مستدامة (§٢٠) | `grep -c توصية` ⇒ ١ (في جدولٍ لا توصية) | القسم ٧ أعلاه |
| «٤٠٢ ملفّاً» والمقيس **٤٠٣** | `git ls-tree -r e3250b8 \| wc -l` ⇒ ٤٠٣ | صُحّح إلى **٤٠٣** (العدُّ كان قبل هبوط هذا التقرير نفسِه) |

**ووسمان أضافهما المقعدُ بحقّ:** بقيّةُ S-2 (٠٤٥٠ في نصٍّ مقابل ٠٤٤٦٠٠ في الثابت، في ملفٍّ واحد) — صُحّحت · وS-7
كانت مُدرَجةً في جدول الإغلاق تحت وسم SP-3 — فُصلت في صفٍّ بنفسها.

## ٩ · تصحيحٌ مؤرَّخ (بعد أمر المالك) — SP-3 أُغلقت **بالتضييق** لا بالإعلان

في §٨ أُغلقت SP-3 بـ**إعلانِ النطاق** (وكان تضييقُه قرارَ المالك). ثمّ أمر المالك: **«الأكثرُ حرصاً ووضوحاً
أن يُنفَّذ»** ⇒ **نُفِّذ التضييق** في الالتزام التالي لهذا التقرير:

- **القاعدةُ الآن:** الإعلانُ (`status: AWAITING_FOUNDER`) يُقرأ من **ترويسة** آخر تقرير ومن **ترويسة**
  `handoff/STATE.md` — **قبل أوّل `## `** وضمن **أوّل ٣٠ سطراً** (أيّهما أسبق). وما بعدها **متنٌ يُوثّق ولا
  يُزيح الدور**.
- **ووضوحُه:** ما يُهمَل **يُعرَض** (`ignored_markers` في `--json`، وتنبيهٌ في المخرَج الافتراضيّ) — فلا يظنّ
  كاتبُه أنّه أوقف الدور، ولا يُهمَل شيءٌ صامتاً.
- **ولماذا هو الأحرص:** الصنفُ المقيسُ خطراً هو **التوقّفُ الزور** (سطرٌ **يشرح** القاعدة يوقف الطرفين)؛ ولم يقع
  في الشجرة مرّةً إعلانٌ من المتن — **قِيس: صفرُ نصوصٍ** ⇒ فالحدُّ يمنع الصنفَ ولا يُلغي إعلاناً واقعيّاً.
- **وخطُّ الرجوع:** `git revert` للالتزام الحامل للتضييق ⇒ تعود قاعدةُ «المتن كلُّه» كما كانت بلا فقدانِ نصّ.
- **والحدُّ المتبقّي (مُعلَنٌ ومقيس):** ملفٌّ بلا عنوانٍ أطول من ٣٠ سطراً لا يُقرأ إعلانُه بعده — وضابطٌ يقيسه،
  وأيُّ إعلانٍ بعده يُعرَض تنبيهاً لا يُهمَل صامتاً.
