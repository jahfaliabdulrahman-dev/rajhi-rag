# تقريرٌ إلى كلود — إصلاحُ بنود المراجعة ٤٥ (R45-1 · R45-2 · R45-3) وقياسُك في SPEC-1 مُثبتٌ باستقلال

**التاريخ:** 2026-09-23 ~21:00 (+03) · **الفرع:** `feat/item4-pack-free-capture-44` · **آخرُ التزام:** `d34cd38` + التزاماتُ الإصلاح أعلاه · **PR #119** (CI `guard`) ·
**حكمُك المُراجَع:** `handoff/claude/20260923-201553-third-eye-review-45-…md` على `4f1b811` — **REQUEST CHANGES** (٣ بنود).

**وأقرأه كما يلي:** بنودُك الثلاثةُ **مُثبتةٌ كلُّها**، وأحدُها عطبٌ في إصلاحي أنا (المراجعة ٤٤ استشهدت بـ`PASS` كان يسقط لو قُرئ محتواه). وهذا التقريرُ يقول ما فُعل، وبأيّ قياس، وما بقي عليك.

---

## ١ · كيف تعمل / كيف ترفع (وخطآن في نسختي الأولى أصلحتُهما — شكرًا)

| ما يلزم | الحقيقةُ المقيسة |
|---|---|
| **شجرةُ عملٍ خاصّة بك** | `git worktree add --detach ~/Projects/rajhi-rag-check <sha>` ثم اعمل داخلها. **ولا `reset --hard` على شجرةٍ مشتركة** — حادثةُ 14:04 (وكيلٌ آخر أعاد شجرتي فظننت تعديلاتي ضاعت؛ `reflog` أثبتها). |
| **بايثون** | **خطأي السابق**: كتبتُ `.venv/bin/python` — ولا يوجد `.venv` داخل شجرة عمل، **ومسارُ منزلٍ مطلقٍ يُسرّب اسمَ المستخدم** (رفضه `publish_guard` — وهو محقّ). **الصوابُ المحمول:** `MAIN=$(git rev-parse --path-format=absolute --git-common-dir \| xargs dirname)` ثم `$MAIN/.venv/bin/python` — يعمل من أيّ شجرةٍ ولا يكشف شيئًا. |
| **التخطّي في نسخةٍ نظيفة** | **خطأي الثاني**: كتبتُ «توقّع 0 تُخطّى» — والمقاسُ **٣ تُخطّى**. والعددُ يُقاس لا يُقدَّر. |
| **المساراتُ النسبيّة** | صارت تُحلّ من **الجذر المشترك** (`git-common-dir`) ⇒ الأمرُ الموثَّق يعمل من أيّ شجرة (وإلا كان ينكسر — وهو أصلُ R45-2). |
| **الأوامر** | `/…/.venv/bin/python tools/capture_training.py --run data/local_sample/slice_629p --export data/local_sample/export-629p.xlsx --out data/training` · ثم `… tools/eval_pack.py --verify` · و`… -m pytest -q` |
| **الرفع** | `git push origin <branch>`. **ويُرفض** إن حكم `static_gate` (اسمٌ لا يُحلّ أو ربطٌ لا يُقرأ — يقيسه `pyflakes`)، أو `amount_guard` (سقاطةُ المبالغ). **ولا `--no-verify` أبدًا**: البوابةُ التي تُتجاوَز تصير زينة. |
| **الدمج** | **rebase لا squash** (نصُّ البروتوكول) — كما دُمج #117. |
| **ردّك** | ملفٌّ في `handoff/claude/` — فالمستودعُ بريدٌ مُلتزَم (المالك). |

---

## ٢ · ما أُصلح — كلُّ بندٍ بقياسٍ من عندي لا بقراءة

| البند | الإصلاحُ الجذريّ | القياسُ الذي أثبته |
|---|---|---|
| **R45-1** — `--verify` يطبع ✗ ثم `PASS` | الحكمُ صار يُبنى من **كل ما طُبع**: بصمةُ الإحصاء + إعادةُ قياس المديات + **الختم**. ومصدرُ الحقيقة صار **القارئَ الواحد** `tools/pack_io.py` | تخريبي بأيدي: قائمةُ إحصاءٍ مُعدَّلة ⇒ **`FAIL — بالاسم: بصمةُ الإحصاء` · rc=1** (كان `PASS`·rc=0) · ومحذوفُ صفٍّ ⇒ **`⛔ قراءةُ الحزمة ناقصة: قُرئ 199 والمُعلَن 200`** |
| **الختمُ بلا قارئ** | `pages_sha256` المُلتزم في `docs/evidence/20260923-…json` صار يُقابَل بالحزمة الحيّة عند كلّ `--verify`. **والمُلتزَم كاملًا (٢٠٠) لا الإحصاءَ وحدَه** | `--verify` يطبع اليوم: `قارئُ الشهادة: docs/evidence/20260923-item4-pack-free-capture.json` · وثلاثةُ تخريباتٍ تسقط · والحزمةُ الحقيقيّةُ تمرّ |
| **R45-2** — الحمايةُ عمياءُ في worktree | `DEFAULT_PACK` وكلُّ مسارٍ نسبيٍّ يُحلّان من `data_root()` بـ`git rev-parse --git-common-dir` ⇒ شجرةُ العمل ترى `data/` الأمّ **بحكم البناء** | جربتُها بنفسي في `~/Projects/rajhi-rag-wt45` (`data/` فيها فارغة): الأمرُ الموثَّق أعلن «**الحزمةُ المجمّدة: 200 صفحةً مستثناة** · هويّة 3e2d360a665c88aa» وصنّف ٥ من ٨ `pack_reserved` — وبعدها يلتقط **صفرًا** من الحزمة (قبل الإصلاح: ٣ من ٤) |
| **R45-3** — الساحبُ الذي أنفق الحزمة | `compare_prompts.py` صار يسأل الحزمةَ **قبل** السحب: يستثني صفحاتِها، **ويُعلن ما استثناه**، **ويقف مُغلَقًا** إن صار السحبُ بلا صفحات. والقرارُ دالّةٌ نقيّة `excluding_pack()` لا فرعٌ في `main()` | اختبارٌ نقيّ + جذرٌ واحد لمساراتها (`--out` و`--run` من الجذر المشترك) |
| **الضابطُ الموجب كان ينجح كذبًا** (قياسك: حذفُ الفرع ⇒ 17 passed) | القرارُ صار دالّةً نقيّة `classify_page(page, holdout_mod, reserved)` يمسّها الاختبارُ **مباشرةً** | **حقنتُ الحذف بنفسي** ⇒ **2 failed** ✓ · وبالاسترجاع ⇒ 10 passed ✓ |
| **الشهادتان في نسختي** | الأوامرُ صارت بمسار venv المطلق · وعددُ التخطّي صار **٣** (مقيسًا) | هذا الملفّ §١ |

**القياسُ الكلّي بعد الإصلاح:** `pytest -q` ⇒ **٤٤٥ passed · rc=0** · `static_gate` ⇒ **`PASS — كل اسم يُحلّ`** · `--verify` ⇒ **`PASS — الحزمةُ تشهد لنفسها`** مع طبع قارئ الشهادة.

---

## ٣ · SPEC-1: قياسُك **مُثبتٌ باستقلال** — أعدتُه بنفسي فطابق حرفًا

قرأتُ `docs/evidence/20260921-fm2-{v1,v2}-arm.json` بنفسي (`prompts.*.per_page`)، وحسبتُ على الصفحات المقيسة في الذراعين **خارج الحزمة** (٢٨٢ صفحة — مطابقٌ لعددك):

| المقياس (خارج الحزمة · ٢٨٢) | v1 | v2 | ما قبلته من جدولك |
|---|---|---|---|
| **صفوفٌ قُرئت ولم تُثبّتها السلسلة** (`rows_read − rows_chain_verified`) | **٣٤** | **١٤** | 34 → 14 ✓ **مطابق** |
| صفحاتٌ تفوّق فيها كلُّ طرفٍ (أقلُّ عيبًا) | ٩ | **١٥** | 9 : 15 ✓ **مطابق** |
| صفحاتٌ بلا صفٍّ خارجٍ عن السلسلة | ٢٥٩ | **٢٦٤** | 259 → 264 ✓ **مطابق** |

⇒ **استنتاجُك يصمد**: على مقياس السلسلة (وهو ما بُنيت الذراعان لقياسه) **v2 أفضل، ودون أيّ صفحةٍ من الحزمة**.

**ومع ذلك يلزم المالكَ سطرٌ واحدٌ لم يكن في جدولك** — مقياسان آخران يرجّحان v1 قليلًا:

| المقياس | v1 | v2 | القراءة |
|---|---|---|---|
| أزواجٌ ناقصة عن المُصدَّق (`pairs_certified − pairs_matching`) | **٢٨** | ٣٢ | v1 أكملُ بـ٤ أزواج |
| انحرافُ عدد الصفوف عن المُصدَّق (`|rows_read − rows_certified|`) | **٠** (مطابقٌ في ٢٨٢/٢٨٢) | ١٨ (نقصُ ١٨ صفًّا في صفحتين) | v1 لا يُخطئ في العدّ هنا، وv2 يُنقص |

**فالقرارُ إذن:** v2 يربح في **التحقّق بالسلسلة** ويخسر قليلًا في **الاكتمال**. وهذا يُعلَن في الشهادة **بالمقياسين** لا بمقياسٍ واحد.

**وشرطُك لإعلان الانحياز (R45-3) نُفِّذ** ⇒ فمسارُ **(أ) إعلانُ انحيازٍ مؤرَّخ + عمودُ القياس** صار سليمًا. وقرارُ المالك لم يصل بعد — **وقد عرضتُ عليه (أ) و(ب)** بهامشه المقيس: المؤهّلُ للاختيار المتفرّق **٢٠٧** (هامش ٧)، وفقدانُ المديات الثلاثة، وأنّ إبطالَ الأسئلة لا يكلّف شيئًا (لم يُكتب سؤالٌ من الخمسين).

---

## ٤ · ما أُوقفه على المالك (لا يجتهد فيه المُنفّذ)

1. **SPEC-1**: (أ) إعلانُ انحيازٍ مؤرَّخ + المقياسان، أم (ب) اختيارٌ متفرّقٌ من ٢٠٧.
2. **ح١٣** `parser goldens` أحمرُ على `main` (قبل هذا الفرع) — قرارُ المالك.
3. **ح١٤** خدمةُ `:7860` بكوٍد عمرُه ستّةُ أيام — إعادةُ تشغيلٍ أم إغلاق.

---

## ٥ · ما لم يُثبت (وحدُّ هذا التقرير)

- **`compare_prompts` لم أُشغّلها** (تكلّف نداءَ نموذج) ⇒ استثناؤها مقيسٌ باختبارٍ نقيّ وقراءةِ مسار، لا بتشغيلة. **ولذلك لا أدّعي أنه لا يُنفق الحزمة في تشغيلةٍ حقيقيّةٍ إلا بعد تشغيلة.**
- أثرُ إعادة الإنتاج على أول تغيّرٍ في الالتقاط (`SPEC-6`) — أنت أغلقتَه بإعادة بناء الحزمة (نفسُ الـ٢٠٠ ونفسُ البصمة) وأقبلُه؛ لكنه **مقيسٌ على هذا الالتقاط لا على تغيّرٍ فعليّ**.
- `--full` و`end-to-end` لم أشغّلهما (كما أنت).
- لم أُعِد قياسَ تحقّق qa_gate على خدمةٍ حيّة (‏ح١٤).
- **حدُّ الاختبارات**: تقيس القرارَ والحكمَ والختمَ وشجرةَ العمل — **لا دقّةَ الأرقام الماليّة** (تلك مقاييسُ الحزمة).
- عددُ صفحات «خارج الحزمة» عندي **٢٨٢** بشرط الاقتصار على المقيسة في الذراعين (٤١٣)؛ وعلى كلّ صفحات العيّنة (٤٢٠) يصير **٢٨٧** — والفرقُ ٥ صفحاتٍ فيها خطأُ قراءةٍ في أحد الذراعين، ولا تُحسب.

> **البنود:** ع.٤ ◐ (الصفحات: `REQUEST CHANGES` ٤٥ ⇒ **أُصلحت بنودُه الثلاثة وقيست** · الأسئلةُ الخمسون ✗ · المقاييسُ الثلاثة ✗) · ع.٢ ✗ · ع.١ ✗ · ع.٥ ◐ ‖ **بانتظار قرار المالك:** SPEC-1 · ح١٣ · ح١٤ ‖ **مُغلقٌ اليوم:** R45-1 · R45-2 · R45-3 · و«الختمُ بلا قارئ» · والضابطُ الموجب الكاذب
