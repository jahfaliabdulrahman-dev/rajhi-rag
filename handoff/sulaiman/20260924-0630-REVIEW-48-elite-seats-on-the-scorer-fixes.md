# REVIEW-48 — بوّابةُ code-review-elite على المدى `da0989d...3729d7e`

**إلى:** المدقّق (Claude) — والنسخةُ التنفيذيةُ للمنفّذ في `handoff/PROMPT-grader-fix-first.md`
**من:** بوّابةُ المراجعة — من صندوقها `handoff/sulaiman/` (نظامُ الصناديق: كلُّ صندوقٍ يبعث من نفسه)
**التاريخ:** ٢٠٢٦-٠٩-٢٤ ٠٦:٣٠ (+٠٣)
**المدى المُثبَّت:** `da0989d...3729d7e` — عشرةُ التزامات · ١٤ ملفاً · **+924/−71** (three-dot diff؛ التغييراتُ غيرُ المُودَعة مستثناة)
**HEAD عند المراجعة:** `3729d7e` · الشجرةُ نظيفة (`git status --porcelain` فارغ)
**ودفعةُ تسليم هذه المراجعة وثائقيةٌ فقط** (هذا الملفّ + `handoff/PROMPT-grader-fix-first.md`) ⇒ لا تمسّ المدى المُراجَع ولا تُبطل قياساته.
**المقاعد المستقلّة:** STANDARDS · SPEC · STRUCTURE — ثلاثةُ مقاعدَ معزولة، للقراءة فقط، بتعليماتٍ متطابقة (سجلاتُها في مجلد تفويض Hermes: `live/deleg_adb085d5/task-{0,1,2}.log`). البعثةُ الأولى فشلت ٣/٣ بعطبِ توجيهِ اعتمادٍ في Hermes **لا علاقةَ له بالمستودع**، فأُعيدت بنفس النصّ.
**أحكام المقاعد:** REQUEST CHANGES · REQUEST CHANGES · APPROVE WITH FIXES — ودُمجت هنا، وكلُّ بندٍ تحقّقتُ منه بنفسي (بلاغاتُ المقاعد دعاوى حتى يقوم دليل).
**الكلفة:** **$0** — لا نداءَ نموذجٍ في هذه المراجعة؛ كلُّ براهينها قراءةٌ وتشغيلاتٌ مجّانية، ونصوصُها محفوظةٌ مع أرشيف الأدلّة المدفوعة (`pass8b · pass11–pass16 · pass18 · pass20–pass22 .out`).

| البوّابة | الأداة / الأمر | النتيجة |
|---|---|---|
| G1 المدى ثابت | `git diff --stat da0989d...3729d7e` | ١٤ ملفاً · +924/−71 |
| G2 الشجرة نظيفة | `git status --porcelain` | فارغ |
| G3 المعايير من المستودع | `docs/handoff-protocol.md` · `docs/QA_CHECKLIST.md` · `docs/EVAL_PACK.md` · وصفُ الأدوات الداخليّ | ٤ مصادر، لا من الذاكرة |
| G4 إسناد كلّ بند | `file:line` + وسم | ١٩ بنداً، كلُّها مسنَدة |
| G5 داخل المدى | `git diff -U0` (مجالاتُ الأسطر الجديدة) | ٥ ملاحظاتٍ نُقلت إلى «خارج المدى» |
| G6 عدم خلط المحاور | محاورُ منفصلة | لا خلط |
| G7 الفحص الميكانيكي | `check_review_report.py` على هذا الملف | exit 0 |
| G8 النِّتات لا تسبق | P3 = ٠ مقابل substantive = ١٩ | ✓ |
| G9 الحكم صريح | الأسفل | REQUEST CHANGES |
| G10 للقراءة فقط | لا كتابةَ على الشجرة | ✓ |

## Verdict

**REQUEST CHANGES** — **٩ بنودٍ مانعة (P1)** و١٠ غير مانعة. **لا يُدمج PR #119 قبل إغلاق المانعة.**
Blocking: 9

**ما ثبت فعلًا — ولا يُعاد قياسه:**
- الحُكمُ صار ينادي `score_answer` الحقيقيّة في ١٧ من ١٨ ضابطاً، والبصمةُ صارت موضعاً واحداً (`tools/eval_stamp.py`)، و`--rescore` يرفض الطبع على نصٍّ متغيّر (`rc=3`) — وهذا مكسبٌ حقيقيّ ولا يُنقص منه شيءٌ أدناه.
- أعدتُ العدَّ من `docs/eval_pack/questions.json`: **٥٠ معرّفاً · ٥٠ نصًّا مختلفاً · ٥٠ (نصّ، اشتقاق)** والمقاييس `number 30 · citation 10 · abstain 10`؛ وجدولُ الوثيقة مُشتقٌّ ويصدُق على القرص.
- الأدلّةُ المدفوعة محفوظةٌ ومختومةٌ **خارج** المستودع (بصماتٌ ١٦/١٦)؛ والاختباراتُ خضراء محليًّا (`585 passed · rc=0`) على هذا الـHEAD مرّتين.
- تقريرُ المنفّذ §٧ يعلن حدودَه بصدق (الكلفةُ عند المهلة · انحرافُ `num()` بأثرٍ صفريّ · «لا أدّعي صحّةَ الأجوبة»)، و§٩ سجّل درسَ «قاعدةٌ في موضعين تنقسم» — والمراجعةُ تطلُب **كنسَ الصنف** لا تسجيلَ الدرس فقط (T-1).

## Standards

- **S-1 P1** — `records_never_truncated` لا يمكن أن يكذّب، والتقاطعُ يُقلّص المقامَ صامتاً. `tools/eval_run_compare.py:53` + `:95`. الوسم: مُثبت
  وصفُ الملفّ `:7-9` يَعِد بفحص «طولُ القائمة = العدّاد» وهو **غير موجود** في الملف. والشرطُ `all(records <= ids_expected)` حيث `ids_expected` نفسه **من التقاطع**.
  البرهانُ المُعاد (`pass22.out`، بلا نداء نموذج): ذراعٌ مقصوصةٌ إلى ١٠ من ٥٠ معرّفاً ⇒ `rc=0 · questions_compared=10 · records_never_truncated=True · in_both=[10,10]` — أي **٤٠ سؤالاً سقطت من المقارنة بلا صوت**. وذراعان بلا معرّفٍ مشترك ⇒ `rc=0 · questions_compared=0 · invariants={"records_never_truncated":true,"no_unpublishable_arm":true}` — **مقارنةٌ فارغة تمرّ خضراء**.
  **Move:** قارِن كلَّ ذراعٍ بـ**معرّفات الحزمة** (٥٠) لا بالتقاطع؛ سمِّ الساقطَ بالاسم؛ واجعل `questions_compared == 0` أو أيُّ سقوطٍ `rc≠0`؛ واكتب ضابطَ تسميم: ذراعٌ مقصوصة ⇒ بوابةٌ حمراء.

- **S-2 P1** — سقفُ الصرف لا يُلجِم ولا يقيس. `tools/eval_questions.py:489-494`. الوسم: مُثبت
  `cap = int(budget/0.01)` وبالمعطى المُلتزَم `--budget 0.6` ⇒ **`cap = 60` > ٥٠ سؤالاً** ⇒ لا يتوقّف شيء (قِيس: `int(0.6/0.01) = 60`).
  والجذرُ نفسُه الذي أخرج «$4.3573» (P-6): العدّادُ التراكميّ للمفتاح لا يسعّر سؤالاً.
  **Move:** السقفُ من **المقيس** لا من «سؤالٍ = $0.01»: كلفةُ الطلب من ردّ المزوّد، وتوقّفٌ عند بلوغ السقف، وسطرُ إنفاقٍ في السجل.

- **S-3 P1** — `--rescore` يعبر البوّابة، ويحكم على المهلة كأنّها جواب، ويكتب **في مكانه** قبل الرفض. `tools/eval_questions.py:641` مقابل `:647-652`، و`:440`، و`:515-516`. الوسم: مُثبت
  فرعُ `a.rescore` **يعود قبل** فحص المراسي، فشرطُ `a.rescore` في السطر `:647` **غيرُ قابلٍ للوصول** — أي أنّ الحاجبَ الذي وُضع «في المسار المدفوع» يتخطّاه أوّلُ مسارٍ مُسمّى فيه.
  والقياس: من ٢٠٠ سجلٍّ في الأدلّة المدفوعة **١٧ نائبَ مهلة**، و١٣ منها في عائلة `argmax` — وكلُّها في المحفوظ تُقرأ «لم يستشهد بالصفحة» (١٣/١٣)، منقلبةً عن سببها الحقيقي؛ وفي نصّ الحزمة «**النائبُ (`<...>`) ليس جواباً فيُعاد سؤالُه**» (`docs/EVAL_PACK.md:432`).
  **Move:** بوابةٌ واحدة **قبل** الفرعين؛ والمهلةُ تُحفظ حالةً (`placeholder`) لا تُصحَّح؛ والتصحيحُ يكتب **ملفًّا جديداً** لا في مكانه (كتابةُ `:440` تسبق قرارَ النشر).

- **S-4 P1** — الختمُ لا يشهد على الملفّ المطلوب فعلًا، وعلامتُه الفارغة تُقبل بصمة. `tools/eval_questions.py:405-414` + `tools/eval_stamp.py:14·17-19·30`. الوسم: مُثبت
  `_spec_sha()` يقرأ المسارَ الافتراضيّ ويُهمل `--questions` ⇒ سجلٌّ يجيب على نصٍّ يُختم ببصمة نصٍّ آخر (بلاغُ مقعد البنية: بصمتان مختلفتان للأداة نفسها).
  والعلامة: المُنتِج يُرجع `?` (U+003F) في `:412`، والحاكمُ يعرف `؟` (U+061F) — والقيس: `classify([{spec_sha:"?"}], cur)` ⇒ سجلٌّ **stale** لا **unstamped**؛ و**`is_publishable({spec_sha:"?"}, "?") = True`** ⇒ تعذُّرُ قراءة الأسئلة يمرّ «منشوراً».
  وضابطُ الاختبار يُغذّي **الثابت** لا المُنتِج (`tests/test_eval_scorer_controls.py` يستورد `eval_stamp` باسمه فيصير وحدةً ثانية، ويُمرّر `PLACEHOLDER`) ⇒ لا يرى `?` أبداً.
  **Move:** علامةٌ واحدة يملكها `eval_stamp` ويُنتجها المُنتِج نفسه؛ و«تعذّرُ التقطيع» ≠ «تغيّرُ النصّ»؛ والضابطُ يشغّل **مُنتِج** البصمة لا نصَّها.

- **S-5 P1** — قاعدةُ الفهرس الجديدة بلا ضابطٍ يمكن أن يكذب. `tests/test_eval_scorer_controls.py:36-43` + `:74-79`. الوسم: مُثبت
  التجهيزةُ (`ROWS`) صفحاتُها ٧ و٨، والصفحةُ المسؤول عنها هي **أولاها** ⇒ الرقمُ العامُّ والرقمُ داخل الصفحة **يتطابقان**، فلا ضابطَ يفصل بين الاصطلاحين. والضابطُ الوحيد للصفحة المحليّة (`:74`) يقع في الحالة التي يتساويان فيها.
  ولا ضابطَ يشترط أن يُرفَض «الصفُّ **قبل** القمّة» — وهو بالضبط ما يقبله الحُكَم بتجربة مقيسة (P-1: `WRONG·row-before = 8/8`).
  **Move:** ضابطٌ موجب على صفحةٍ ليست الأولى، وضابطٌ سالبٌ باسم «الصفّ السابق للقمّة»، وكلاهما يجب أن **يفشل** عند إعادة الحقن.

- **S-6 P2** — العدّاداتُ نثرٌ يُقرأ بالبحث النصّي، فتُخلَط الفئات. `tools/eval_questions.py:567-572` + `:283`. الوسم: مُثبت
  «**لم يُعلن**» تطابق صنفين: «لم يُعلن التعادل» (سؤالُ تعادلٍ في عائلة الجواب) و«لم يُعلن امتناعاً». والقياس على الأدلّة: أعضاءُ عدّاد «الامتناع الصامت» هم `abs-08` أربعَ مرّات (وهو سؤالُ تعادلٍ حُكمه صحيحٌ في مكانٍ آخر) + نائبُ مهلةٍ واحد ⇒ **الامتناعاتُ الصامتةُ الحقيقيّة = صفر**.
  **Move:** حقلُ حكمٍ مُهيكل (`verdict: tie_undeclared | abstain_undeclared | …`) يُكتب عند الحكم، والمجاميعُ تُجمَع من الحقل لا من النثر.

- **S-7 P2** — نظافةُ الرسالة: صندوقٌ خاطئ وحالةٌ قديمة. `handoff/claude/20260924-0155-REPORT-item4-review-fixes-and-measured-runs.md` + `handoff/STATE.md:3`. الوسم: مُثبت
  تقريرُ المنفّذ إلى المدقّق موضوعٌ **في صندوق المدقّق فقط**، ولا أصلَ له في `handoff/sulaiman/` — ونظامُ الصناديق (§١ في `docs/handoff-protocol.md`) يجعل كلَّ صندوقٍ يبعث من نفسه؛ والسابقةُ نفسُها وقعت في الجولة ٤٧.
  و`handoff/STATE.md` ما زال `status: REQUEST CHANGES — مراجعة ٤٥` ⇒ **ثلاثُ جولاتٍ من التقادم**، وهو الملفّ الذي يُقرأ أوّلًا في كلّ جلسة.
  **Move:** نقل/نسخُ التقرير إلى صندوق المنفّذ، وتحديثُ `STATE.md` في التزام الإقفال (قاعدةٌ قائمةٌ في البروتوكول، لا تُنشئ قاعدةً جديدة).

## Spec

- **P-1 P1** — قاعدةُ الفهرس تحكم باصطلاحٍ لا يتكلّمه النظام. `tools/eval_questions.py:349-372`. الوسم: مُثبت
  القاعدةُ تقبل رقمين (`ORTS`) وتُهمل ما يطبعُه النظام؛ والقياسُ على ٨ صفحاتٍ حقيقيّة (`pass15.out`): `correct·global = 0/8` · `correct·local1 = 8/8` · **`WRONG·row-before = 8/8`** — أي أنّ الرقمَ **الذي يطبعه النظام** يُرفض، ورقمَ الصفِّ **السابق** للقمّة يُقبل. وفي `pass11.out`: `local_top − tops0 = 1` في ٨/٨، و`pass12.out`: ٦ سجلاتٍ سقطت لأجل القاعدة، منها ٥ لا يمكن الحكمُ عليها من الأثر المقصوص.
  وهذا يسحب أيضًا نتيجةَ التقرير الكبرى: «**لا يمكن الإجابةُ عنها صحيحةً**» غيرُ مُثبتة — فمن ١٩ سجلَّ argmax مُجاباً (بعد ١٣ نائباً) **٨** تنطق رقمَ القمّة العامّ الصحيح، وستّةٌ منها حكم الحاكمُ الحيّ عليها ✅ **قبل** دخول قاعدة الفهرس.
  **Move:** مصدرٌ واحد للفهرس: `truth()` يُرجع هويّةَ الصفّ **بترقيم النظام المطبوع**؛ ويُحذف الاصطلاحُ الثاني؛ والضابطُ من `qa_tools._ref` على صفحةٍ ليست الأولى.

- **P-2 P2** — المحفوظُ يخالف نفسه: صفحاتٌ كاملةٌ وأثرٌ مقصوص. `tools/eval_questions.py:526-538` + `:535-540`. الوسم: مُثبت
  `cited_pages` يُحفظ كاملاً بينما `used_row_nos[:40]` يُقصّ، وكلُّ إعادة حكمٍ تقرأ الأثرَ المقصوص ⇒ ما بعد الحدّ غيرُ قابلٍ لإعادة الحكم. القياس: على الأذرع المحفوظة ٣٠ سجلاً عند الحدّ (A 10 · B 8 · C 7 · D 5)، ومن ١٤ انقلاباً بين الحُكم الحيّ والمحفوظ **١٠ من القصّ**، و**٥ من ٦** حالاتٍ تحتاج الأثرَ الكامل لا يُثبتها المقصوص. وفي `pass15.out`: القصُّ وحده لم يقلب حكمًا **عند هذا الـHEAD** — لأنه محجوبٌ بالقاعدة المعطوبة (P-1) ⇒ العطبُ كامنٌ يظهر فورَ إصلاح P-1. **وهذا ما يجعل P-1 وP-2 متلازمتين.**
  **Move:** لا تُقصّ أثراً يُحكم به؛ أو احفظ `trace_truncated: true` وصرِّح بأنّ الحكم صفحة-فقط، ولا تُدخِله في رقمٍ رئيسيّ.

- **P-3 P1** — النائبُ يُحسب جواباً وفشلاً، وأرقامُ المهلات المنشورة تخالف الأدلّة المُلتزَمة. `docs/EVAL_PACK.md:432` + `tools/eval_questions.py:441-450`. الوسم: مُثبت
  نصُّ الحزمة: «النائبُ (`<...>`) ليس جواباً فيُعاد سؤالُه». والواقع: ١٧ نائباً من ٢٠٠، تُحسب في كلّ مقام، والمقارنةُ تعلنها «فشلاً» — والأخطر **فيضانُها على الحكم بالحتميّة**: من ٧ أسئلةٍ منشورةٍ «متذبذبة» **٣ منها نائبُ مهلةٍ لا تذبذبُ نظام**.
  وجدولُ التقرير (السطر ٧٤-٧٧) ينشر `B = ٤+` و`C = ٢` مهلة، وملفُّ الأدلّة المُلتزَم `docs/evidence/eval_runs_2026-09-24.json` يقول **٨** و**٣** — فخليّتان من أربع تخالفان الشاهد المحفوظ.
  **Move:** النائبُ يُخرَج من المقام ويُبلَّغ عنه عمودًا مستقلاً (تغطية)؛ والتذبذبُ يُحسب على المُجاب وحده؛ والجدولُ يُعاد توليدُه من ملفّ الأدلّة لا بيد.

- **P-4 P1** — «الأرقامُ السارية» في الوثيقة لا تصدُق على هذا الـHEAD. `docs/EVAL_PACK.md:454-466` (داخل المدى). الوسم: مُثبت
  الجدولُ يقول **24/47** و**6/10** استشهاداً، وإعادةُ الحكم على الملفّ المحفوظ بمصحِّح الـHEAD تعطي **20/47** و**2/10** (`pass18.out`) — و**٤ سجلاتٍ منقولة** في عائلة الاستشهاد. والمجاميعُ المنشورة (٢٧–٣٠) حدٌّ سفليّ مصنوعٌ من العطب: بالترقيم النظاميّ تكون ٢٨–٣٢ (توقّعٌ مشروطٌ بإصلاح P-1/P-2، لا قياساً).
  **Move:** توليدُ الجدول بالأمر نفسه في نفس الالتزام، أو تصحيحٌ **مؤرَّخ** («كان… وصار…») لا محو، وربطُ كلّ رقمٍ بأمره.

- **P-5 P2** — «دقّةُ الامتناع ٩/١٠» تقيس بوّابةً حتميّةً لا قدرةَ النظام. `tools/eval_questions.py:177-183`. الوسم: مُثبت
  من ١٠ أسئلة امتناع، **٧** قرّرتها بوّابةُ النطاق قبل النموذج (`scope=no_such_data`) في **كلّ ذراع** ⇒ `بوّابة 7/7` و`نموذج 2/3`. وبها يُفسَّر الزمن: ١٦٨ سجلًّا بزمنٍ > ٠ من ٢٠٠ ⇒ ٣٢ صفريّ الزمن (٢٨ = ٧×٤ امتناعٌ محكومٌ بوّابةً + ٤ لـ`abs-02`) — والعيّنةُ ١٦٨ غيرُ معلنةٍ في المنشور.
  **Move:** فصلُ نصيب البوّابة عن نصيب النموذج في كلّ تقرير، وذكرُ حجم العيّنة الفعليّة.

- **P-6 P1** — الكلفةُ نُشرت من عدّادِ المفتاح التراكميّ، والوثيقةُ تشترط كلفةَ المزوّد لكلّ نداء. `docs/EVAL_PACK.md:436-438`. الوسم: مُثبت
  نصُّ الوثيقة: «**الكلفة (من المزوّد، لا من عدّادنا)**». والمنشورُ من العدّاد: A/B/C عملت **متزامنةً على المفتاح نفسه** ⇒ رقمُ B ($2.2823) هو نافذةُ **A+B+C** كلِّها، و«$4.3573» يجمع A وC **مرّتين** (الشرائح: 0.6737 · 2.2823 · 0.9122 · 0.4891)، و«**$0.49 مقابل $2.28 (٤٫٧×)**» غيرُ قابلٍ للعزل أصلًا. المُثبتُ الوحيد: D منفردةٌ **$0.4891**، وحركةُ العدّاد للمجموعة.
  **Move:** قراءة `usage.cost` من ردّ كلّ طلب؛ وسحبُ الأرقام المركّبة بأرقامٍ مقيسة أو تصحيحٍ مؤرَّخ؛ وعدمُ بناءِ حكمٍ على فرقِ عدّادٍ مشترك.

- **P-7 P2** — كلمةُ «الاختراع» مفتوحةٌ من الطرفين. `tools/eval_questions.py:250` + `:264`. الوسم: مُثبت
  `_AMOUNT_TOK` **يشترط فاصلةً عشرية** ⇒ مبلغٌ صحيحٌ (بلا كسور) لا يُعدّ مبلغًا: من ١١ سجلَّ امتناعٍ قرّره النموذج، **واحدٌ** (`B:abs-08`) فيه رقمٌ صحيحٌ من ٤ خاناتٍ ليس في الأثر ولا في مجاميع الصفحات المستشهَد بها ⇒ «الاختراع = ٠» **حدٌّ سفليّ** لا صفر (`pass21.out`).
  وفي الاتجاه المعاكس: العدّادُ يرى الأثرَ وحده، ومجاميعُ الصفحات التي **تُعطيها الأداةُ نفسُها** تُقرأ «بلا سند» ⇒ إنذارٌ كاذب.
  **Move:** تعريفٌ واحدٌ معلنٌ للاختراع يُغطّي الصحيحَ والكسريّ، ويَعُدُّ مجاميعَ الصفحات سنداً؛ ويُعاد العدّ بالأداة المُصلَحة (مجّاناً).

- **P-8 P2** — `cit-02` يسأل عدًّا، والحاكمُ يقيس صفحةً. `tools/eval_questions.py:349-372` (فرع `rows_matching`). الوسم: مُثبت
  الشرطُ: تقاطعٌ غيرُ فارغٍ ودقّةٌ ≥ ٠٫٣ ⇒ جوابٌ يخطئ في العدّ و**٣٠٪** من صفحاته المستشهَد بها يمرّ. والسؤالُ يطلب «كم… وفي أيّ صفحة» ⇒ العدُّ غيرُ مقيس.
  **Move:** يُقاس العدُّ (أو يُحذف السؤال من العائلة)؛ والوثيقةُ تُعلن العتبةَ صراحةً.

- **P-9 P2** — `abs-02` غيرُ مصوغٍ، ويستند إلى رقمٍ لا يتغيّر بإصلاحٍ مُعلن. `tools/eval_questions.py:441-450` + `tools/eval_questions.py:98-102`. الوسم: مُثبت (الصياغة: مرجّح)
  يجعلُه المدى سؤالَ عدٍّ على **صفحة ٦٢٩** بـ«١٧ عملية»، و٦٢٩ صفحةٌ **يرفضها النظام** (`max_page=628`؛ ٣٢ سجلًّا بزمنٍ صفريّ) ⇒ سؤالٌ يستحيل نطاقيًّا.
  و«١٧» مُشتقّةٌ من `count_rows` الذي نُقل إلى `raw_rows` — و`raw_rows` **نسخةٌ حرفية** من `rows` (كلاهما يقرأ `d["raw_rows"]`)، وفي نقطة المدى كان `rows` يقرأ `raw_rows` أيضاً ⇒ **الإصلاحُ المعلن لم يغيّر قيمةً واحدةً**: التفريقُ الحقيقيّ في قارئ النظام (`tools/refusal_test.py`، غيرُ متغيّرٍ في المدى) لا في الحاكم.
  والدليلُ على أنّ ٦٢٩ ملخّصٌ لا حركات: مفرداتُ الملخّص في ٩ من ١٧ سطراً مقابل **صفرٍ** في ٢١ سطراً حقيقيًّا من الصفحات ٦٢٦–٦٢٨ (`pass14.out`).
  **Move:** إمّا سؤالٌ على صفحةٍ موجودةٍ فعلًا، وإمّا تصريحٌ بأنّ «العدّ على آخر صفحة» حالةُ حدودٍ مُعلَنة؛ وتصحيحُ ادّعاء «الإصلاح» في وصف الدالة.

## Structure

- **T-1 P2** — قاعدةٌ واحدة، عدّةٌ مالِكين — والدرسُ مسجَّلٌ والصنفُ غيرُ مكنوس. `tools/eval_questions.py:405` مقابل `tools/eval_run_compare.py:29`، و`:515-516` مقابل `:641-652`، و`tools/eval_stamp.py:14` مقابل `tools/eval_questions.py:412`. الوسم: مُثبت
  §٩ في تقرير المنفّذ سجّل «قاعدةٌ في موضعين تنقسم» — ثم بقي: بصمةٌ في ملفّين، وقاعدةُ «النائبُ ليس جواباً» في مسارٍ دون آخر، وعلامةُ «تعذّر التقطيع» في طرفين. انقسامٌ واحدٌ أُصلح، وثلاثةٌ لم تُكنَس. الوسمُ نفسُه يفسّر لماذا يُعاد العطبُ في موضعٍ جديد.
  **Move:** لكلّ قاعدةٍ مالكٌ واحد يستورده الجميع: `eval_stamp` يملك العلامةَ والبصمة، و«النائب» حالةٌ تُنتجها طبقةُ التشغيل لا تُخمَّن في الحاكم.

- **T-2 P2** — البصمةُ لكلّ **ملفّ** والعملُ لكلّ **سؤال**. `tools/eval_questions.py:539` + `tools/eval_stamp.py:22`. الوسم: مُثبت
  إعادةُ صياغة سؤالٍ واحد تُبطل ٥٠ سجلًّا في كلّ ذراع (و`classify` تُسقطها كلَّها) ⇒ إعادةُ السؤال تصير ٤×٥٠ بدل المعرّفات المتغيّرة وحدها. ولهذا أيضًا لم تُسمَّ `page_outside_pack` (§٥ في تقرير المنفّذ) — إصلاحٌ حقيقيٌّ جُمِد لأجل بصمةٍ فِليّة.
  **Move:** بصمةٌ لكلّ سؤال (من نصّه واشتقاقه) تُقابَل من `eval_stamp` بالمعرّف ⇒ إصلاحُ سؤالٍ يُعيد سؤالَه وحده.

- **T-3 P2** — فروعٌ ميّتةٌ وقارئٌ مكرّرٌ وُلِدت في هذه الجولة. `tools/eval_questions.py:98-107`. الوسم: مُثبت
  `Corpus.covers` فقدت آخرَ منادٍ لها في إعادة كتابة الحزمة (لا نتائج لـ`grep -rn "\.covers(" --include=*.py .`)، و`pack_meta`/`boundary` بلا سؤالٍ يحملهما، و`raw_rows` قارئٌ ثانٍ لعين ما يقرأه `rows` — والفارقُ المنشور («المطبوعُ الخامّ») غيرُ موجود.
  **Move:** احذف الميّت، واحتفظ بقارئٍ واحد؛ وإن أردتَ «الخامّ» فعلًا فهو فرقٌ **في قارئ النظام** يُسمّى هناك.

## Not proven (ما لم يُثبت — ويُعلن، لا يُخمَّن)

- **أنّ إصلاح P-1 يرفع المجاميع إلى رقمٍ بعينه**: المدى ٢٨–٣٢ **توقّعٌ** مشروطٌ بإصلاح P-1 وP-2 معاً، لا قياس؛ والقياسُ يتطلّب إعادةَ حكمٍ على الأثر الكامل ثم إعلانَ سجلات القصّ منفصلة.
- **أنّ الجوابين غير المحلَّلين (`cit-04` · `cit-09`) ينطقان الصفَّ الصحيح**: النصُّ المحفوظ لا يُعيد بناء ذلك الآن بلا استدعاء نموذج؛ يُحسَم مع إصلاح القاعدة.
- **كلفةُ A وB وC منفردة**: غيرُ قابلةٍ للعزل من عدّادٍ مشترك؛ D وحدها رقمٌ نظيف. القياسُ المعزول يحتاج كلفةً **وإذنَ المالك**.
- **أنّ ٦٢٩ صفحةُ ملخّص**: مرجّحٌ قويٌّ بمفردات الأسطر (٩/١٧ مقابل ٠/٢١)، والشاهدُ القاطعُ مطبوعٌ في الصورة — بيد المالك.
- **`585 passed` في نسخةٍ نظيفة**: أُثبت في هذه الشجرة مرّتين، لا في clone نظيف؛ الشاهدُ الخارجيُّ هو CI الخاصّ بـPR #119.
- **دعوى سابقةٌ لي مسحوبة**: كنت قلت «الحزمة ٤٨ نصًّا مختلفًا لا ٥٠» — القياسُ الآن: ٥٠ معرّفاً · ٥٠ نصًّا · ٥٠ زوجاً. تُسجَّل في سجل التطوّر لا في الوثيقة.

## خارج المدى (ملاحظاتٌ — ليست نتائج ولا تُحتسب في الحكم)

- `docs/EVAL_PACK.md:351` («السلسلة لا تتّصل») — سطرٌ **لم يمسّه** المدى، والقياسُ يخالفه (نجاحُ `row_chain` مع فشلين حقيقيّين). تصحيحٌ مؤرَّخ بيد المنفّذ.
- `tools/eval_questions.py:188-195` (نصفُ هجرة النطاق: سببان يقرآن صفحات الحزمة وبقيّةُ الأسباب تقرأ كوربوس النظام) — أسطرٌ غيرُ متغيّرة، والأثرُ المقيس اليوم **صفر**.
- `tools/refusal_test.py` — غيرُ متغيّرٍ في المدى، **وهو مالكُ عطب ٦٢٩**: قارئُ النظام يُسقط الصفوف بلا رصيد ⇒ ١٧ ⇒ ٠، و`max_page` يتوقّف عند ٦٢٨.
- ثقبُ الصيغة غير المعرَّفة في `scope.classify` («في ص #» مقابل «في الصفحة #») — سابقٌ للمدى، ولا سؤالَ في الحزمة يستخدم الصيغة غير المعرّفة ⇒ بلا أثر اليوم.
- فجوةُ «أداة سرد صفوف الصفحة» **حقيقيّة** (تهافتُ النموذج عليها صادق) — لكن فرضيّتها «لا يمكن الإجابة» غيرُ مُثبتة: ٨ من ١٩ سجلًّا مُجاباً تنطق الرقمَ الصحيح.
- `publish_guard.py --tree --history` ⇒ **BLOCK واحد**: `handoff/STATE.md:18` (سلسلةُ أرقامٍ طويلة) — سابقٌ للمدى، **ويوقف أي push** حتى يُعالَج أو يُستثنى بسببٍ مبرَّز؛ والباقي ٢٩ WARN في ملفّاتٍ أقدم. وهذا يُذكر لأنّه يحجز التسليم كله، لا هذه الجولة وحدها.

## ما لم يُراجَع

- صحّةُ أجوبة النموذج نفسِها بمعيارٍ خارج معايير الحاكم (لا نداءَ نموذجٍ في هذه المراجعة).
- نصوصُ المقاعد الثلاثة إلا تقاريرها النهائية؛ وما عدا ذلك في المدى لم أفتحه إلا حيث استُشهِد به كمعيار.
- ملفّاتٌ خارج المدى (١٤ ملفاً) — وُجِّهت إليها الملاحظاتُ أعلاه بحدودها.
