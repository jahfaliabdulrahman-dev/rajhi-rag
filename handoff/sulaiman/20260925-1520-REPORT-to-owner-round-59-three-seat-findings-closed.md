# تقريرُ الجولة ٥٩ — إغلاقُ بنود المقاعد الثلاثة (APPROVE WITH FIXES ×٣) وحدودُ ما بقي

```
id:      20260925-1520-sulaiman
from:    sulaiman
to:      owner
type:    SUBMIT
step:    إغلاقُ بنود مراجعة كودٍ مستقلّة (٣ مقاعد) بأربع عشرةَ حركةً مقيّدة + بوّابتين جديدتين (م١٧/م١٨)
commit:  ea18f6c (المدى) → يُقرأ التزامُ الإغلاق من `git log -1 --format=%h`
verdict: -
```

## ١ · الحكمُ كما أعادته المقاعدُ الثلاثة

| المقعد | الحكم | أعلى رافعة | الحصيلة |
| :--- | :--- | :--- | :--- |
| **المعايير** (هل بُني صحيحاً؟) | `APPROVE WITH FIXES` | **S-1 (P1)** — الشاهدُ لا يُطابَق بالسؤال، ويُقبَل القديمُ صامتاً | لا P0 |
| **المواصفة** (هل المواصفة نُفِّذت؟) | `APPROVE WITH FIXES` | **F1 (P2)** — عدّادُ المعرّفات الغريبة يقرأ صندوقَ المدقّق بلا الاستثناء نفسِه | بنودٌ ٦ |
| **البنية** (هل التصميم سليم؟) | `APPROVE WITH FIXES` | **P1-١/P1-٢** — عدٌّ متقادمٌ في وثيقةٍ معياريّة، والحرفُ الواحد في موضعين | بنودٌ ١٠ |

**وسجلُّ المقاعد:** `~/.hermes/cache/delegation/live/deleg_cff9a5f8/` (ثلاثةُ ملفّات، `leaf`، ٣٧٠/٣٠٤/٢٦٥ ثانية).
**وحدُّ هذه المراجعة مُعلَن:** أحكامُ الجولة ٥٩ **ليست ملفّاتٍ في المستودع** — وصلت كرسائلِ تفويض، فلا شاهدَ
تحت `handoff/claude/` يُشار إليه. ولذلك هذا التقريرُ **إلى المالك** (وكيلُ الحُكم) لا «جواباً على حُكمٍ في
الشجرة»؛ و§٢٧ يوجب على **المدقّق** دفعَ حُكمه إلى `main` ليكون شاهداً قابلاً لإعادة الإنتاج.

## ٢ · ما أُغلق — كلُّ صفٍّ بموضعه وقياسه

| # | البندُ (المقعد) | الحركة | الموضع | الدليل |
| :--- | :--- | :--- | :--- | :--- |
| ١ | **S-1** · الشاهدُ القديمُ يمرّ صامتاً | **الشاهدُ يُدفَع قبل أن يُجاب**: زمنُ الحُكم ≤ زمنُ الجواب (`stamp_of` على الطرفين) | `tests/test_answer_names_witness.py` (`ungated_answers`) | ضابطُ سمّ: حُكمٌ بـ`20260925-0900` مع جوابٍ قبله ⇒ يُسقط (`مُثبت`) |
| ٢ | **S-3 · P2-5** · الإعفاءُ بعدّادٍ يُنقل إلى جوابٍ آخر | الصنفُ المُعلَن **بالهويّة**: اسمُ ملفٍّ واحدٍ في ثابت، و`class:` فحصَ ترويسةٍ، و**لا يُقبَل استشهادٌ معه** (غطاء) | `tests/test_answer_names_witness.py:151+` | ثلاثُ حالاتٍ في ضابطٍ واحد: مُعلَنٌ بلا شاهد ⇒ يمرّ · مُعلَنٌ مع شاهد ⇒ يسقط · نصُّ العبارة في ملفٍّ آخر ⇒ يسقط (`مُثبت`) |
| ٣ | **S-4 · F5 · P2-3** · «حضورُ نصّ» لا مساواة (`"5" in "15"`) | مقابلةُ **الرموز**: عددُ الميزانيّة والنافذة يُستخرَج من السجلّ ومن صفّ `GATES.md` (أرقامٌ عربيّةٌ تُوحَّد) ويُساوي الثوابت | `tests/test_flake_ledger.py` (`declared_pairs`) | سمٌّ: نصٌّ يقول «3 … 15» يُكشَف (كان يمرّ) (`مُثبت`) |
| ٤ | **P1-٢** · وسمُ اللاحتميّة حرفان في موضعين | **مصدرٌ واحد**: `FLAKY_MARK` في `tools/qa_gate.py` ويستورده الحارس | `tools/qa_gate.py:38` · `tests/test_flake_ledger.py:27` | إعادةُ التسمية تُعمي السجلَّ صامتاً — صار استيراداً (`مُثبت`) |
| ٥ | **P3-٦** · فحصُ **شكل** الترويسة (معجمُ حقولٍ ثانٍ لـ§٢) | **فحصُ هويّة**: معرّفُ الكتلة == زمنُ اسم الملفّ — وسقطت ثلاثةُ مفاهيم (`HEADER_FIELD_RE` · `HEADER_MIN_FIELDS` · دالّةُ الشكل) | `tools/turn.py` (`_is_own_header`) | **١٠٣ من ١٠٣** ملفّاً مُتتبَّعاً يبدأ بكتلةٍ مسيّجة ومعرّفُها يطابق اسمَه (مقيسٌ بمسبار)، وسمُّ **م١٨** يُسقط الضابط (`مُثبت`) |
| ٦ | **P3-٧** · شرطٌ مكرَّر | `_leading_fenced_span` + `_is_own_header` بدل الشرطين المتراكبين | `tools/turn.py` | ١٩ ضابطاً في `tests/test_turn_derivation.py` تمرّ (`مُثبت`) |
| ٧ | **P3-٨** · أسماءُ الصناديق حرفان في الحارس | الصناديقُ تُشتقّ من `turn.SIDES` بأدوارها المُعلَنة | `tests/test_answer_names_witness.py` | لا اسمَ صندوقٍ مكتوبٍ ثانياً (`مُثبت`) |
| ٨ | **P3-٩** · «قاعدةُ الماسح `(?i)`» قولٌ لا قراءة | القاعدةُ تُشتقّ من `ID_CONTEXT.flags`، **ويُقاس أثرُها** على الحرفين | `tests/test_secret_scan.py` | ضابطٌ يقابل أعلامَ العدّاد بأعلام الماسح ويختبر «SHA/sha» (`مُثبت`) |
| ٩ | **P2-4** · رقمٌ واحدٌ في ثلاثة مواضع نثر | الأرقامُ تُقرأ من **البروتوكول وترويسة الماسح** وتُقابَل بالقياس: `11·4` (بلا `handoff/`) · `27·11` حسّاساً · `36·18` بلا حسّاس | `tests/test_secret_scan.py` | ثلاثةُ مواضعَ تُقابَل؛ وإعادةُ الإنتاج المؤرَّخة (`٣١·١٢`) تُقرأ تاريخاً لا قياسَ اليوم (`مُثبت`) |
| ١٠ | **F1** · عدّادُ المعرّفات الغريبة يقرأ صندوقَ المدقّق | المقامُ **بلا `handoff/`**: السقفُ صفرٌ خارجَه، والمُستثنى (٤٧ سطراً) **يُطبَع** ولا يُراتَش | `tests/test_secret_scan.py` (`_foreign_id_counts` · `FOREIGN_ID_DEBT_ON_CORPUS = 0`) | قياس: خارج `handoff/` = **0** · داخله = **47** · ٢٠٨ ملفّاً مقيساً (`مُثبت`) |
| ١١ | **S-2 · F3** · بصمةُ مفتاحٍ خصوصيّ باقيةٌ في التاريخ المدفوع | **إعلانٌ صريح** (لا حذفٌ ممكن): `git log -S` ⇒ البصمةُ دخلت في `911f5ff` (مدفوع) ونُزعت في `16d1b9f`؛ والشجرةُ الحاليّة **صفر** منها | `handoff/sulaiman/20260925-1330-…md` (ملاحظةُ R58-5) | القياسُ بلا كتابة القيمة: `git show ea18f6c:<تقرير> \| grep -o '\`[0-9a-f]\{16\}\`'` ثم `git log -S` (`مُثبت`) — **وقرارُ المسح للمالك** |
| ١٢ | **S-5** · حقلُ `class:` غيرُ موثَّق | أُضيف إلى قالب §٢ + توضيحُ «أيّ الحقول تُقاس فعلاً» | `docs/handoff-protocol.md:28-46` | (`مُثبت`) |
| ١٣ | **P1-١** · «حدودٌ ستّةٌ مُعلَنة» والترويسةُ ثمانية | **حُذف العدّ** (رقمٌ بلا مقياسٍ ليس قاعدةً) وأُعيدت صياغةُ القاعدة المُلزَمة | `docs/handoff-protocol.md` (§٢٧) | (`مُثبت`) |
| ١٤ | **F2** · `dee00196` (ثلاث e) في ثلاثة ملفّات | صُحّح إلى `de00196` بعد `git cat-file -t` على الاسمين | `tools/qa_gate.py:69` · `tests/test_gate_goldens.py:5` · `docs/QA_CHECKLIST.md:25` | `dee00196` ⇒ `fatal: Not a valid object name` (`مُثبت`) |
| ١٥ | **S-6 · S-7** · إحالةُ السموم في `GATES.md` | م١٦ انتقل من صفّ ١٣ إلى صفّ ١٤، وصفُّ ١٢ وُسِّع للقاعدة الفعليّة، وصفُّ ١٤ صار «م١٦–م١٨» | `docs/GATES.md` (١٢/١٣/١٤) | (`مُثبت`) |
| ١٦ | **F4** · لا سمَّ للشاهد الذاتيّ | **م١٧**: سمٌّ يُلغي شرطَ الذاتيّة ⇒ يُسقط الضابطَ المسمّى | `tools/guard_bite_sweep.py` | **21/21** (كان 19/19) (`مُثبت`) |
| ١٧ | **F6** · النافذةُ من أدلّةٍ محفوظة (اختيارٌ لصاحبها) | **حفظُ الدليل واجبٌ مُعلَن** + الغيابُ فشلٌ مُغلَق | `handoff/FLAKY_READS.md` | (`مُثبت`) |

## ٣ · حصيلةُ الفحص النهائيّ (بأوامر الـCI نفسِها)

| الفحص | الأمر | النتيجة |
| :--- | :--- | :--- |
| المجموعةُ كاملة | `pytest -q tests/` | **750 passed** (كانت ٧٤٨) |
| بوّابةُ التسليم | `python tools/qa_gate.py --full` | **5/5** — والدليلُ محفوظٌ: `handoff/sulaiman/20260925-1513-gate-evidence-full-run.txt` |
| عضُّ الحرّاس | `python tools/guard_bite_sweep.py` | **21/21** |
| الأرقامُ المُعلَنة | `python tools/render_claims.py --check` | «كل رقم معلن يطابق مصدره» |
| النشر (سطحُ الشجرة) | `publish_guard.py --tree` | «آمن للدفع» |
| المبالغ | `amount_guard.py --ci` · `--tracking-audit` · `--baseline-audit origin/main` | PASS ×٣ |
| الأسماء والربط | `static_gate.py` | PASS |

## ٤ · حدودٌ باقية — مُعلَنةٌ لا مُغلَقة («ما لم يُثبت»)

1. **بصمةُ المفتاح الخصوصيّ في التاريخ المدفوع** (`911f5ff` على `origin/chore/round56-evidence`): النزعُ من
   `HEAD` لا يمحوها من التاريخ؛ والمحوُ **إعادةُ كتابةِ تاريخِ فرعٍ مدفوع** ⇒ قرارُ المالك (لم أنفّذه).
2. **الشاهدُ يُطابَق وجوداً وزمناً لا هويّةً**: «حُكمٌ قديمٌ موجودٌ تحت `handoff/claude/`» يمرّ؛ ولا ربطَ بين
   سؤال الجواب والحُكم المقصود — يحتاج صيغةَ ربطٍ يكتبها المالك (مُوثَّقٌ كحدٍّ في §٢٧ وفي `GATES.md` صفّ ١٢).
3. **`--history` يمسح `--all`**: فحصي المحلّيُّ منعَ الدفعَ بعُثورين ليسا في المدفوع — قِيس: مسارُ منزلٍ في
   `handoff/sulaiman/…SEATS…:386` مصدرُه الفرعُ المحلّيُّ `chore/round56-seats-closure` (`2ffdcab`)، وسطرُ
   أرقامٍ في `handoff/STATE.md:18` مصدرُه `backup-review-18` (`d311c76`)؛ والصفرُ المؤكَّد على
   `origin/chore/round56-evidence` و`origin/main` ⇒ **سطحُ النشر نظيفٌ ونجاحُ الـCI مطابق**، والفرقُ أنّه
   محليٌّ لا منشور. (لا حذفَ لفروعٍ محلّيّةٍ بلا أمر.)
4. **سطرُ اللافتة التشخيصيّ في الفحص الشامل غيرُ محكوم**: أربعُ قيمٍ في أربعة تشغيلات (`[1]` · `[1, 6]` ·
   `[9]` · `[5]`) و**5/5 في كلّها** ⇒ لا يحكم النتيجة، وأُدرِج في التقرير كتوصيةٍ مستدامة (تثبيتُه أو نزعُه).
5. **F-5 (قديمٌ مُعلَن)**: `tools/qa_gate.py:37` يُدخِل `PROJ/src` في `sys.path` · و`:318` ما زال «~3-4 min».
6. **`NON_ANSWER_FILES_SINCE_RULE` = 5**: رُفع في هذا الالتزام لأنّ هذا التقرير ملفٌّ سادسٌ في الصندوق بعد
   القاعدة ليس من صنف الأجوبة (وهو الإعلانُ نفسُه الذي يطلبه الضابط).
7. **أحكامُ المقاعد الثلاثة ليست في المستودع** (وصلت كرسائل تفويض) ⇒ إعادةُ إنتاجها تحتاج سجلَّ التفويض
   المحلّيّ لا `git`. ومن أين جاء الإلزام: §٢٧ يُلقي الدفعَ على المدقّق.

**Not proven:** لم أُجرِ مراجعةً مستقلّةً **ثانيةً** على هذا الالتزام (المقاعدُ راجعت `ea18f6c...16d1b9f`
لا الالتزامَ الحاملَ للإصلاحات) — والفحصُ النهائيّ أعلاه هو القياسُ المتاحُ اليوم؛ وحُكمُ مقعدٍ رابعٍ مُعلَّقٌ
إن أراده المالك.
